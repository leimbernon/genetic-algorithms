---
phase: 09-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/visualization/mod.rs
  - tests/test_visualization.rs
autonomous: true
requirements:
  - VIZ-04
  - VIZ-01

must_haves:
  truths:
    - "Crate compiles with `cargo build` (no features) without exposing visualization module"
    - "Crate compiles with `cargo build --features visualization` and exposes `genetic_algorithms::visualization`"
    - "User can call `plot_fitness(stats, \"out.png\")` and get a PNG file with best/avg/worst fitness lines"
    - "User can call `plot_fitness(stats, \"out.svg\")` and get an SVG file"
    - "Calling `plot_fitness` with fewer than 2 data points returns `Err(VisualizationError::InsufficientData)`"
    - "Calling `plot_fitness` with a `.txt` extension returns `Err(VisualizationError::UnsupportedFormat)`"
  artifacts:
    - path: "Cargo.toml"
      provides: "plotters optional dependency and visualization feature flag"
      contains: "visualization"
    - path: "src/lib.rs"
      provides: "cfg-gated visualization module declaration"
      contains: "cfg(feature = \"visualization\")"
    - path: "src/visualization/mod.rs"
      provides: "VisualizationError enum, plot_fitness function"
      exports: ["plot_fitness", "VisualizationError"]
    - path: "tests/test_visualization.rs"
      provides: "Integration and unit tests for fitness chart and error cases"
      contains: "cfg(feature = \"visualization\")"
  key_links:
    - from: "src/visualization/mod.rs"
      to: "src/stats.rs"
      via: "use crate::stats::GenerationStats"
      pattern: "GenerationStats"
    - from: "tests/test_visualization.rs"
      to: "src/visualization/mod.rs"
      via: "use genetic_algorithms::visualization"
      pattern: "genetic_algorithms::visualization"
---

<objective>
Set up the `visualization` feature flag, create the visualization module with `VisualizationError` and `plot_fitness`, and establish the test file with feature-gated tests.

Purpose: Establishes the foundation (feature flag, error type, backend dispatch pattern) and delivers the first chart function (VIZ-01). Plan 02 adds the remaining two chart functions using the same patterns.
Output: Working `plot_fitness` function producing PNG/SVG files, feature-flag gating verified, test suite passing.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visualization/09-CONTEXT.md
@.planning/phases/09-visualization/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/stats.rs:
```rust
#[derive(Debug, Clone)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct GenerationStats {
    pub generation: usize,
    pub best_fitness: f64,
    pub worst_fitness: f64,
    pub avg_fitness: f64,
    pub fitness_std_dev: f64,
    pub population_size: usize,
    #[cfg_attr(feature = "serde", serde(default))]
    pub diversity: f64,
}
```

From src/error.rs (style reference for VisualizationError — separate type, same pattern):
```rust
use std::fmt;

#[derive(Debug, Clone, PartialEq)]
pub enum GaError {
    ConfigurationError(String),
    // ...
}

impl fmt::Display for GaError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
            GaError::ConfigurationError(msg) => write!(f, "Configuration error: {}", msg),
            // ...
        }
    }
}

impl std::error::Error for GaError {}
```

From Cargo.toml (existing feature pattern):
```toml
[features]
default = []
serde = ["dep:serde", "dep:serde_json"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }
```

From src/lib.rs (existing cfg-gate pattern):
```rust
#[cfg(feature = "serde")]
pub mod checkpoint;
```

From tests/test_serde.rs (feature-gated test pattern):
```rust
#![cfg(feature = "serde")]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feature flag, VisualizationError, and plot_fitness with tests</name>
  <files>Cargo.toml, src/lib.rs, src/visualization/mod.rs, tests/test_visualization.rs</files>
  <read_first>
    - Cargo.toml (current dependencies and features sections)
    - src/lib.rs (current module declarations, line 70-91)
    - src/stats.rs (GenerationStats struct fields)
    - src/error.rs (GaError style reference for VisualizationError)
    - tests/test_serde.rs (first 5 lines, feature-gate pattern)
    - .planning/phases/09-visualization/09-RESEARCH.md (plotters API patterns, pitfalls)
  </read_first>
  <behavior>
    - Test: plot_fitness with 5 GenerationStats entries and ".png" path creates a file that exists and has >0 bytes; clean up after
    - Test: plot_fitness with 5 GenerationStats entries and ".svg" path creates a file that exists and has >0 bytes; clean up after
    - Test: plot_fitness with 0 entries returns Err(VisualizationError::InsufficientData)
    - Test: plot_fitness with 1 entry returns Err(VisualizationError::InsufficientData)
    - Test: plot_fitness with valid stats and ".txt" extension returns Err(VisualizationError::UnsupportedFormat)
    - Test: plot_fitness with valid stats and no extension returns Err(VisualizationError::UnsupportedFormat)
    - Test: cargo build (no features) compiles cleanly — VIZ-04 verified by the #![cfg(feature = "visualization")] guard on test file
  </behavior>
  <action>
    **Step 1: Cargo.toml** — Add plotters optional dependency and visualization feature:
    - In `[features]` section, add line: `visualization = ["dep:plotters"]`
    - In `[dependencies]` section, add line: `plotters = { version = "0.3.7", default-features = false, features = ["bitmap_encoder", "svg_backend", "histogram"], optional = true }`

    **Step 2: src/lib.rs** — Add cfg-gated module declaration. Add this line after `#[cfg(feature = "serde")] pub mod checkpoint;` (line 71):
    ```rust
    #[cfg(feature = "visualization")]
    pub mod visualization;
    ```

    **Step 3: src/visualization/mod.rs** — Create new file with:
    - `VisualizationError` enum with variants: `DrawingError(String)`, `IoError(String)`, `UnsupportedFormat`, `InsufficientData`
    - `impl fmt::Display for VisualizationError` following GaError style (see interfaces above)
    - `impl std::error::Error for VisualizationError {}`
    - `pub fn plot_fitness(stats: &[GenerationStats], path: &str) -> Result<(), VisualizationError>` that:
      1. Returns `Err(VisualizationError::InsufficientData)` if `stats.len() < 2`
      2. Detects format from `Path::new(path).extension().and_then(|e| e.to_str())`: `"png"` -> BitMapBackend, `"svg"` -> SVGBackend, else -> `Err(VisualizationError::UnsupportedFormat)`
      3. Creates drawing area at 800x600, fills with WHITE
      4. Calls a private generic helper `draw_fitness_chart<DB: DrawingBackend>(root: &DrawingArea<DB, Shift>, stats: &[GenerationStats]) -> Result<(), DrawingAreaErrorKind<DB::ErrorType>>` where `DB::ErrorType: std::error::Error + Send + Sync` (adjust bounds as needed — see research open question #1; if generic bounds are too complex, duplicate the chart body in each match arm instead)
      5. The chart draws three `LineSeries`: best_fitness (BLUE), avg_fitness (GREEN), worst_fitness (RED)
      6. X-axis: `0usize..max_gen` where `max_gen = stats.last().unwrap().generation`
      7. Y-axis: compute min/max across all three fitness series; if `y_max - y_min < f64::EPSILON`, set `y_max = y_min + 1.0` to avoid degenerate range
      8. Configure legend via `configure_series_labels().position(SeriesLabelPosition::UpperRight).background_style(WHITE.mix(0.8)).border_style(BLACK).draw()`
      9. Call `root.present()` before returning
      10. All `DrawingAreaErrorKind` errors converted via `.map_err(|e| VisualizationError::DrawingError(format!("{:?}", e)))`

    **Step 4: tests/test_visualization.rs** — Create new file with:
    - Line 1: `#![cfg(feature = "visualization")]`
    - Import `genetic_algorithms::visualization::{plot_fitness, VisualizationError}`
    - Import `genetic_algorithms::stats::GenerationStats`
    - Helper function `fn make_stats(n: usize) -> Vec<GenerationStats>` that creates n GenerationStats with generation=0..n-1, varying best/avg/worst fitness values (e.g., best=100.0-i as f64, avg=50.0, worst=i as f64)
    - Test `test_plot_fitness_png`: call plot_fitness with make_stats(5) and a temp path ending in `.png`, assert file exists and metadata.len() > 0, then remove_file
    - Test `test_plot_fitness_svg`: same but `.svg`
    - Test `test_plot_fitness_insufficient_empty`: plot_fitness with empty slice returns InsufficientData
    - Test `test_plot_fitness_insufficient_one`: plot_fitness with make_stats(1) returns InsufficientData
    - Test `test_plot_fitness_unsupported_format`: plot_fitness with make_stats(5) and ".txt" path returns UnsupportedFormat
    - Test `test_plot_fitness_no_extension`: plot_fitness with make_stats(5) and path "output_no_ext" returns UnsupportedFormat
    - Use `std::env::temp_dir().join("test_viz_fitness.png")` pattern for temp paths; `let _ = std::fs::remove_file(&path);` for cleanup (ignore error if file doesn't exist)
  </action>
  <verify>
    <automated>cargo test --features visualization test_plot_fitness -- --test-threads=1 && cargo build && cargo clippy --features visualization</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'visualization = \["dep:plotters"\]' Cargo.toml
    - grep -q 'plotters.*optional = true' Cargo.toml
    - grep -q '#\[cfg(feature = "visualization")\]' src/lib.rs
    - grep -q 'pub mod visualization' src/lib.rs
    - grep -q 'pub fn plot_fitness' src/visualization/mod.rs
    - grep -q 'pub enum VisualizationError' src/visualization/mod.rs
    - grep -q 'DrawingError' src/visualization/mod.rs
    - grep -q 'UnsupportedFormat' src/visualization/mod.rs
    - grep -q 'InsufficientData' src/visualization/mod.rs
    - grep -q 'impl std::error::Error for VisualizationError' src/visualization/mod.rs
    - grep -q '#!\[cfg(feature = "visualization")\]' tests/test_visualization.rs
    - grep -q 'test_plot_fitness_png' tests/test_visualization.rs
    - grep -q 'test_plot_fitness_svg' tests/test_visualization.rs
    - grep -q 'test_plot_fitness_insufficient' tests/test_visualization.rs
    - grep -q 'test_plot_fitness_unsupported_format' tests/test_visualization.rs
    - `cargo test --features visualization test_plot_fitness -- --test-threads=1` passes (all tests green)
    - `cargo build` (no features) succeeds — confirms VIZ-04 feature isolation
    - `cargo clippy --features visualization` produces no warnings
  </acceptance_criteria>
  <done>
    - plotters 0.3.7 added as optional dep gated on `visualization` feature
    - `src/visualization/mod.rs` exists with `VisualizationError` enum and `plot_fitness` function
    - `plot_fitness` produces PNG and SVG files from `&[GenerationStats]`
    - Error cases (insufficient data, unsupported format) handled and tested
    - `cargo build` without features succeeds (VIZ-04)
    - All tests pass with `--features visualization`
  </done>
</task>

</tasks>

<verification>
1. `cargo build` (no features) — must compile without visualization module
2. `cargo test --features visualization test_plot_fitness -- --test-threads=1` — all fitness chart tests pass
3. `cargo clippy --features visualization` — no warnings
4. `cargo test` (no features) — existing tests unaffected
</verification>

<success_criteria>
- VIZ-04: `cargo build` without features compiles cleanly; visualization module absent
- VIZ-01: `plot_fitness` writes a valid PNG file when given >= 2 GenerationStats
- VIZ-01: `plot_fitness` writes a valid SVG file when given >= 2 GenerationStats
- VIZ-01: `plot_fitness` returns errors for insufficient data and unsupported formats
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-visualization/09-01-SUMMARY.md`
</output>
