---
phase: 25-alternative-metaheuristics
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - src/lib.rs
  - src/engines/ga.rs
  - src/engines/island/
  - src/engines/nsga2/
  - src/engines/de/mod.rs
  - src/engines/scatter/mod.rs
  - src/engines/cellular/mod.rs
  - src/engines/alps/mod.rs
autonomous: true
requirements: [STRUCT-01, STRUCT-04]

must_haves:
  truths:
    - "genetic_algorithms::ga::Ga resolves after restructure"
    - "genetic_algorithms::ga::TerminationCause resolves after restructure"
    - "genetic_algorithms::island::IslandGa resolves after restructure"
    - "genetic_algorithms::nsga2::Nsga2 resolves after restructure"
    - "genetic_algorithms::TerminationCause (top-level re-export) resolves after restructure"
    - "src/engines/de/ placeholder directory exists"
    - "src/engines/scatter/ placeholder directory exists"
    - "src/engines/cellular/ placeholder directory exists"
    - "src/engines/alps/ placeholder directory exists"
    - "Full test suite passes including serde, clippy, and rustdoc"
  artifacts:
    - path: "src/engines/ga.rs"
      provides: "GA engine orchestrator"
    - path: "src/engines/island/"
      provides: "Island model engine"
    - path: "src/engines/nsga2/"
      provides: "NSGA-II multi-objective engine"
    - path: "src/engines/de/mod.rs"
      provides: "DE placeholder for Phase 26"
    - path: "src/engines/scatter/mod.rs"
      provides: "Scatter Search placeholder for Phase 27"
    - path: "src/engines/cellular/mod.rs"
      provides: "Cellular GA placeholder for Phase 28"
    - path: "src/engines/alps/mod.rs"
      provides: "ALPS placeholder for Phase 29"
  key_links:
    - from: "src/lib.rs"
      to: "src/engines/ga.rs"
      via: "#[path] attribute"
      pattern: '#\[path = "engines/ga.rs"\]'
    - from: "src/lib.rs"
      to: "src/engines/island/mod.rs"
      via: "#[path] attribute"
      pattern: '#\[path = "engines/island/mod.rs"\]'
    - from: "src/lib.rs"
      to: "src/engines/nsga2/mod.rs"
      via: "#[path] attribute"
      pattern: '#\[path = "engines/nsga2/mod.rs"\]'
---

<objective>
Move ga, island, and nsga2 engine modules into src/engines/ group directory, create placeholder stubs for future engines (de, scatter, cellular, alps), and run the full validation suite to close the phase.

Purpose: Final wave of the restructure — engines group is the most cross-referenced and includes the phase gate (STRUCT-04 full suite validation).
Output: src/engines/ directory containing all engine modules and placeholder stubs; full test/clippy/doc suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alternative-metaheuristics/25-RESEARCH.md
@.planning/phases/25-alternative-metaheuristics/25-PATTERNS.md
@.planning/phases/25-alternative-metaheuristics/25-01-SUMMARY.md
@.planning/phases/25-alternative-metaheuristics/25-02-SUMMARY.md

<interfaces>
<!-- From src/lib.rs — current declarations for modules being moved (after Plan 01 and 02 edits) -->
```rust
// These three lines change in this plan:
pub mod ga;    // becomes #[path = "engines/ga.rs"] pub mod ga;
pub mod island;  // becomes #[path = "engines/island/mod.rs"] pub mod island;
pub mod nsga2;   // becomes #[path = "engines/nsga2/mod.rs"] pub mod nsga2;
```

<!-- Internal crate:: references in island/mod.rs that must still resolve -->
```rust
use crate::configuration::{GaConfiguration, ProblemSolving};
use crate::error::GaError;
use crate::island::configuration::IslandConfiguration;
use crate::island::migration::migrate;
use crate::observer::IslandGaObserver;
```

<!-- Internal crate:: references in nsga2/mod.rs that must still resolve -->
```rust
use crate::configuration::GaConfiguration;
use crate::error::GaError;
use crate::nsga2::configuration::{Nsga2Configuration, ObjectiveDirection};
use crate::nsga2::crowding_distance::assign_crowding_distance;
use crate::observer::Nsga2Observer;
```

<!-- Top-level re-export that depends on ga module -->
```rust
pub use ga::TerminationCause;  // stays as-is; works because pub mod ga is still at crate root via #[path]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ga, island, nsga2 into src/engines/ and create placeholder stubs</name>
  <files>src/engines/ga.rs, src/engines/island/, src/engines/nsga2/, src/engines/de/mod.rs, src/engines/scatter/mod.rs, src/engines/cellular/mod.rs, src/engines/alps/mod.rs</files>
  <read_first>
    - src/lib.rs
    - src/ga.rs (first 10 lines only — confirm it exists and is the right file)
    - src/island/mod.rs (first 10 lines only)
    - src/nsga2/mod.rs (first 10 lines only)
  </read_first>
  <action>
1. Create directory `src/engines/`.

2. Move ga engine:
   - `mv src/ga.rs src/engines/ga.rs`
   (Single file move — ga.rs is a flat module, no subdirectory)

3. Move island engine:
   - `mv src/island/ src/engines/island/`
   (Entire directory with mod.rs and all sub-files)

4. Move nsga2 engine:
   - `mv src/nsga2/ src/engines/nsga2/`
   (Entire directory with mod.rs and all sub-files)

5. Create placeholder directories and stubs (4 files, each containing ONLY a doc comment):

   Create `src/engines/de/mod.rs` with content:
   ```rust
   //! Differential Evolution engine — placeholder for Phase 26.
   ```

   Create `src/engines/scatter/mod.rs` with content:
   ```rust
   //! Scatter Search engine — placeholder for Phase 27.
   ```

   Create `src/engines/cellular/mod.rs` with content:
   ```rust
   //! Cellular Genetic Algorithm engine — placeholder for Phase 28.
   ```

   Create `src/engines/alps/mod.rs` with content:
   ```rust
   //! Age-Layered Population Structure (ALPS) engine — placeholder for Phase 29.
   ```

6. CRITICAL: Do NOT declare `mod de;`, `mod scatter;`, `mod cellular;`, or `mod alps;` anywhere. These stubs are filesystem-only — they exist for Phase 26-29 developers to discover but are never compiled until those phases add a `mod` declaration.

7. Do NOT modify any content in the moved files. All `crate::` references (e.g., `crate::island::configuration::IslandConfiguration` inside island/mod.rs, `crate::nsga2::configuration::Nsga2Configuration` inside nsga2/mod.rs) resolve unchanged because lib.rs still declares `pub mod island` and `pub mod nsga2` at the crate root via `#[path]`.
  </action>
  <verify>
    <automated>ls /Users/luis/RustroverProjects/genetic-algorithms/src/engines/ga.rs /Users/luis/RustroverProjects/genetic-algorithms/src/engines/island/mod.rs /Users/luis/RustroverProjects/genetic-algorithms/src/engines/nsga2/mod.rs /Users/luis/RustroverProjects/genetic-algorithms/src/engines/de/mod.rs /Users/luis/RustroverProjects/genetic-algorithms/src/engines/scatter/mod.rs /Users/luis/RustroverProjects/genetic-algorithms/src/engines/cellular/mod.rs /Users/luis/RustroverProjects/genetic-algorithms/src/engines/alps/mod.rs 2>&1 && test ! -f /Users/luis/RustroverProjects/genetic-algorithms/src/ga.rs && echo "OLD ga.rs REMOVED OK" || echo "OLD ga.rs STILL EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/ga.rs` exists (moved from src/ga.rs)
    - `src/engines/island/mod.rs` exists (moved from src/island/)
    - `src/engines/nsga2/mod.rs` exists (moved from src/nsga2/)
    - `src/engines/de/mod.rs` exists and contains `//! Differential Evolution`
    - `src/engines/scatter/mod.rs` exists and contains `//! Scatter Search`
    - `src/engines/cellular/mod.rs` exists and contains `//! Cellular Genetic Algorithm`
    - `src/engines/alps/mod.rs` exists and contains `//! Age-Layered Population Structure`
    - `src/ga.rs` no longer exists at old location
    - `src/island/` directory no longer exists at old location
    - `src/nsga2/` directory no longer exists at old location
    - `grep -rn 'mod de\|mod scatter\|mod cellular\|mod alps' src/lib.rs src/engines/` returns NO matches (placeholders are not declared as modules)
  </acceptance_criteria>
  <done>All engine files exist under src/engines/, placeholder stubs created, old locations cleaned up, no mod declarations for placeholder engines.</done>
</task>

<task type="auto">
  <name>Task 2: Update lib.rs with #[path] attributes for engines group and run full validation</name>
  <files>src/lib.rs</files>
  <read_first>
    - src/lib.rs
  </read_first>
  <action>
In src/lib.rs, make these exact replacements:

1. Find:
```rust
pub mod ga;
```
Replace with:
```rust
#[path = "engines/ga.rs"]
pub mod ga;
```

2. Find:
```rust
pub mod island;
```
Replace with:
```rust
#[path = "engines/island/mod.rs"]
pub mod island;
```

3. Find:
```rust
pub mod nsga2;
```
Replace with:
```rust
#[path = "engines/nsga2/mod.rs"]
pub mod nsga2;
```

Do NOT add `pub mod engines;` — the group directory is invisible to downstream users. The `#[path]` attributes preserve the original module names at the crate root.

Do NOT change the `pub use ga::TerminationCause;` line or any other `pub use` re-export. They work as-is because `pub mod ga` is still declared at the crate root.

After updating lib.rs, run the full validation suite as the STRUCT-04 phase gate:
```bash
cargo test && cargo test --features serde && cargo clippy && cargo doc --no-deps
```

All four commands must exit 0 with zero warnings.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && cargo test 2>&1 | tail -3 && cargo test --features serde 2>&1 | tail -3 && cargo clippy 2>&1 | tail -3 && cargo doc --no-deps 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'path = "engines/ga.rs"' src/lib.rs` returns exactly 1 match
    - `grep -n 'path = "engines/island/mod.rs"' src/lib.rs` returns exactly 1 match
    - `grep -n 'path = "engines/nsga2/mod.rs"' src/lib.rs` returns exactly 1 match
    - `grep -c 'pub mod engines' src/lib.rs` returns 0 (no group module exposed)
    - `grep -n 'pub use ga::TerminationCause' src/lib.rs` returns exactly 1 match (unchanged)
    - `cargo test` exits 0
    - `cargo test --features serde` exits 0
    - `cargo clippy` exits 0
    - `cargo doc --no-deps` exits 0 with zero warnings
  </acceptance_criteria>
  <done>lib.rs uses #[path] for ga, island, nsga2; full validation suite (cargo test, cargo test --features serde, cargo clippy, cargo doc --no-deps) passes with zero failures and zero warnings. Phase 25 restructure is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — source-file-only reorganization.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | pub mod declarations | accept | No runtime behavior change; compilation + full test suite is the gate |
</threat_model>

<verification>
After both tasks complete — the STRUCT-04 full phase gate:
1. `cargo test` — all existing tests pass
2. `cargo test --features serde` — checkpoint/serde tests pass
3. `cargo clippy` — zero warnings
4. `cargo doc --no-deps` — zero rustdoc warnings
5. `src/engines/` contains ga.rs, island/, nsga2/, de/, scatter/, cellular/, alps/
6. Old locations (src/ga.rs, src/island/, src/nsga2/) no longer exist
7. All `genetic_algorithms::ga::*`, `genetic_algorithms::island::*`, `genetic_algorithms::nsga2::*` paths resolve identically
8. All top-level re-exports (TerminationCause, all observer types) resolve identically
</verification>

<success_criteria>
- All `genetic_algorithms::ga::*` paths resolve identically to before
- All `genetic_algorithms::island::*` paths resolve identically to before
- All `genetic_algorithms::nsga2::*` paths resolve identically to before
- `genetic_algorithms::TerminationCause` top-level re-export works
- src/engines/ contains all 7 subdirectories (ga, island, nsga2, de, scatter, cellular, alps)
- Placeholder stubs are filesystem-only (not compiled)
- Full suite: zero test failures, zero clippy warnings, zero rustdoc warnings
- Phase 25 directory restructure is complete
</success_criteria>

<output>
After completion, create `.planning/phases/25-alternative-metaheuristics/25-03-SUMMARY.md`
</output>
