---
phase: 25-alternative-metaheuristics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/types/chromosomes.rs
  - src/types/chromosomes/binary.rs
  - src/types/chromosomes/list.rs
  - src/types/chromosomes/range.rs
  - src/types/genotypes.rs
  - src/types/genotypes/binary.rs
  - src/types/genotypes/list.rs
  - src/types/genotypes/range.rs
autonomous: true
requirements: [STRUCT-02]

must_haves:
  truths:
    - "genetic_algorithms::chromosomes::Binary resolves after restructure"
    - "genetic_algorithms::chromosomes::Range resolves after restructure"
    - "genetic_algorithms::chromosomes::ListChromosome resolves after restructure"
    - "genetic_algorithms::genotypes::Binary resolves after restructure"
    - "genetic_algorithms::genotypes::Range resolves after restructure"
    - "genetic_algorithms::genotypes::List resolves after restructure"
  artifacts:
    - path: "src/types/chromosomes.rs"
      provides: "Chromosome module re-exports"
    - path: "src/types/chromosomes/"
      provides: "Chromosome implementations (binary, list, range)"
    - path: "src/types/genotypes.rs"
      provides: "Genotype module re-exports"
    - path: "src/types/genotypes/"
      provides: "Genotype implementations (binary, list, range)"
  key_links:
    - from: "src/lib.rs"
      to: "src/types/chromosomes.rs"
      via: "#[path] attribute"
      pattern: '#\[path = "types/chromosomes.rs"\]'
    - from: "src/lib.rs"
      to: "src/types/genotypes.rs"
      via: "#[path] attribute"
      pattern: '#\[path = "types/genotypes.rs"\]'
---

<objective>
Move chromosomes and genotypes modules into src/types/ group directory, preserving all existing public paths via #[path] attributes in lib.rs.

Purpose: First wave of the directory restructure — types group has the fewest cross-references, establishing the pattern for subsequent waves.
Output: src/types/ directory containing chromosomes and genotypes with zero impact on downstream consumers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alternative-metaheuristics/25-RESEARCH.md
@.planning/phases/25-alternative-metaheuristics/25-PATTERNS.md

<interfaces>
<!-- From src/lib.rs lines 70-93 — current pub mod declarations that will be modified -->
```rust
// Lines 72, 78 are the ones this plan changes:
pub mod chromosomes;   // line 72 — becomes #[path = "types/chromosomes.rs"]
pub mod genotypes;     // line 78 — becomes #[path = "types/genotypes.rs"]
```

<!-- From src/chromosomes.rs — the module root file being moved -->
```rust
//! Built-in chromosome types.
pub mod binary;
pub mod list;
mod range;
pub use binary::Binary;
pub use list::ListChromosome;
pub use range::Range;
```

<!-- From src/genotypes.rs — the module root file being moved -->
```rust
//! Built-in gene (genotype) types.
pub mod binary;
pub mod list;
pub mod range;
pub use binary::Binary;
pub use list::List;
pub use range::Range;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move chromosomes and genotypes into src/types/</name>
  <files>src/types/chromosomes.rs, src/types/chromosomes/, src/types/genotypes.rs, src/types/genotypes/</files>
  <read_first>
    - src/chromosomes.rs
    - src/genotypes.rs
    - src/lib.rs
  </read_first>
  <action>
1. Create directory `src/types/`.

2. Move chromosomes:
   - `mv src/chromosomes.rs src/types/chromosomes.rs`
   - `mv src/chromosomes/ src/types/chromosomes/`
   (The chromosomes.rs file and chromosomes/ subdirectory both move into src/types/)

3. Move genotypes:
   - `mv src/genotypes.rs src/types/genotypes.rs`
   - `mv src/genotypes/ src/types/genotypes/`
   (The genotypes.rs file and genotypes/ subdirectory both move into src/types/)

4. Do NOT modify any content in the moved files — zero edits to the Rust source. All `crate::` references inside these files resolve unchanged because lib.rs will still declare `pub mod chromosomes` and `pub mod genotypes` at the crate root.
  </action>
  <verify>
    <automated>ls -la /Users/luis/RustroverProjects/genetic-algorithms/src/types/chromosomes.rs /Users/luis/RustroverProjects/genetic-algorithms/src/types/chromosomes/binary.rs /Users/luis/RustroverProjects/genetic-algorithms/src/types/genotypes.rs /Users/luis/RustroverProjects/genetic-algorithms/src/types/genotypes/binary.rs 2>&1 && test ! -f /Users/luis/RustroverProjects/genetic-algorithms/src/chromosomes.rs && echo "OLD FILES REMOVED OK" || echo "OLD FILES STILL EXIST" && cd /Users/luis/RustroverProjects/genetic-algorithms && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/chromosomes.rs` exists (moved from src/chromosomes.rs)
    - `src/types/chromosomes/binary.rs` exists
    - `src/types/chromosomes/list.rs` exists
    - `src/types/chromosomes/range.rs` exists (private module)
    - `src/types/genotypes.rs` exists (moved from src/genotypes.rs)
    - `src/types/genotypes/binary.rs` exists
    - `src/types/genotypes/list.rs` exists
    - `src/types/genotypes/range.rs` exists
    - `src/chromosomes.rs` no longer exists at old location
    - `src/genotypes.rs` no longer exists at old location
    - `src/chromosomes/` directory no longer exists at old location
    - `src/genotypes/` directory no longer exists at old location
  </acceptance_criteria>
  <done>All chromosome and genotype files exist under src/types/ and no longer exist at old locations.</done>
</task>

<task type="auto">
  <name>Task 2: Update lib.rs with #[path] attributes for types group</name>
  <files>src/lib.rs</files>
  <read_first>
    - src/lib.rs
  </read_first>
  <action>
In src/lib.rs, find the two lines:
```rust
pub mod chromosomes;
```
and
```rust
pub mod genotypes;
```

Replace them with:
```rust
#[path = "types/chromosomes.rs"]
pub mod chromosomes;
```
and
```rust
#[path = "types/genotypes.rs"]
pub mod genotypes;
```

Do NOT change any other line in lib.rs. The `pub use` block at the bottom (lines 95-107) stays exactly as-is. All other `pub mod` declarations stay as-is.

The key insight: with `#[path]`, the module name `chromosomes` at the crate root is preserved. All `crate::chromosomes::*` and `genetic_algorithms::chromosomes::*` paths continue to resolve identically. No shim modules, no re-exports, no `pub mod types` declaration needed.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'path = "types/chromosomes.rs"' src/lib.rs` returns exactly 1 match
    - `grep -n 'path = "types/genotypes.rs"' src/lib.rs` returns exactly 1 match
    - `grep -n '^pub mod chromosomes;' src/lib.rs` returns 0 matches (bare declaration replaced with #[path] version)
    - `grep -n '^pub mod genotypes;' src/lib.rs` returns 0 matches (bare declaration replaced with #[path] version)
    - `cargo test` exits 0
    - `cargo clippy` exits 0
  </acceptance_criteria>
  <done>lib.rs uses #[path] for chromosomes and genotypes; cargo test and cargo clippy pass with zero failures/warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a source-file-only reorganization with no runtime behavior changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | pub mod declarations | accept | No runtime behavior change; compilation is the gate |
</threat_model>

<verification>
After both tasks complete:
1. `cargo test` — all existing tests pass (proves STRUCT-02 path preservation)
2. `cargo clippy` — zero warnings
3. `src/types/` directory contains chromosomes.rs, chromosomes/, genotypes.rs, genotypes/
4. `src/chromosomes.rs` and `src/genotypes.rs` no longer exist at root
</verification>

<success_criteria>
- All `genetic_algorithms::chromosomes::*` paths resolve identically to before
- All `genetic_algorithms::genotypes::*` paths resolve identically to before
- src/types/ contains both module groups
- Zero test failures, zero clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/25-alternative-metaheuristics/25-01-SUMMARY.md`
</output>
