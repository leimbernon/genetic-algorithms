---
phase: 78-replace-user-input-panics-with-gaerror-issue-279
plan: 04
type: execute
wave: 3
depends_on: ["78-02", "78-03"]
files_modified:
  - tests/engines/eda/test_eda.rs
  - tests/engines/pso/test_pso.rs
  - tests/engines/cma/test_cma.rs
  - tests/engines/cellular/test_cellular.rs
  - tests/engines/alps/test_alps.rs
  - tests/operations/test_mutation.rs
  - tests/operations/test_operations.rs
  - benches/cellular.rs
  - benches/alps.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every existing test/bench caller compiles against the new Result-returning run()/new()/select() signatures"
    - "New error-path tests assert the exact GaError variant for each former panic"
    - "GP subtree crossover/mutation that exceeds max_depth/max_node_count returns GaError::TreeDepthExceeded/TreeSizeExceeded (SC-1), proven by error-path tests"
    - "cargo test, cargo test --features serde, and cargo clippy all pass"
    - "A grep audit confirms no user-input-reachable panics remain in the converted src/ files"
    - "D-02: GP chromosome helper panic messages ('not supported — use GpChromosome with GpGa, not Ga') are deliberately unchanged — audited and confirmed sufficient per discuss-phase decision"
  artifacts:
    - path: "tests/engines/cma/test_cma.rs"
      provides: "test asserting InitializationError for all 3 empty-pop sites"
    - path: "tests/engines/cellular/test_cellular.rs"
      provides: "tests asserting ConfigurationError from new() for zero rows/cols"
    - path: "tests/engines/alps/test_alps.rs"
      provides: "tests asserting ConfigurationError from new() for zero layer_size/n_layers"
    - path: "tests/operations/test_mutation.rs"
      provides: "test asserting CrossoverError for OX non-unique gene IDs"
    - path: "tests/operations/test_operations.rs"
      provides: "test asserting SelectionError for Lexicase via the trait"
    - path: "tests/gp.rs"
      provides: "EXISTING tests test_bloat_limit_crossover + test_bloat_limit_mutation assert TreeDepthExceeded/TreeSizeExceeded from GP subtree crossover/mutation when max_depth/max_node_count are violated (SC-1, already passing from Phase 53)"
  key_links:
    - from: "tests/engines/cellular/test_cellular.rs"
      to: "src/engines/cellular/engine.rs"
      via: "asserts matches!(CellularEngine::new(...), Err(GaError::ConfigurationError(_)))"
      pattern: "GaError::ConfigurationError"
    - from: "tests/operations/test_mutation.rs"
      to: "src/operations/crossover/order.rs"
      via: "asserts order() with non-unique IDs returns CrossoverError"
      pattern: "GaError::CrossoverError"
    - from: "tests/gp.rs"
      to: "src/engines/gp/node.rs"
      via: "EXISTING tests assert check_limits-backed GP crossover/mutation return TreeDepthExceeded/TreeSizeExceeded (SC-1)"
      pattern: "GaError::Tree(Depth|Size)Exceeded"
---

<objective>
Make the whole workspace compile and pass after the breaking signature changes from Plans 02 and
03: update every existing test and bench caller of the changed `run()` / `new()` / `select()`
signatures, add explicit error-path tests for each former panic (Wave 0 gaps from VALIDATION.md),
and run the final grep audit (SC-6) confirming no user-input-reachable panics remain.

Purpose: SC-7 (existing tests still pass) and SC-2/3/4/5 (new error-path coverage) both require
this caller-update + test-authoring pass. Without it the workspace does not build.
Output: Green `cargo test`, `cargo test --features serde`, `cargo clippy`; new error-path tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-CONTEXT.md
@.planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-RESEARCH.md
@.planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-VALIDATION.md
@.planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-02-SUMMARY.md
@.planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update EDA/PSO/CMA run() callers and Cellular/ALPS new() callers in tests + benches</name>
  <files>tests/engines/eda/test_eda.rs, tests/engines/pso/test_pso.rs, tests/engines/cma/test_cma.rs, tests/engines/cellular/test_cellular.rs, tests/engines/alps/test_alps.rs, benches/cellular.rs, benches/alps.rs</files>
  <read_first>
    - tests/engines/eda/test_eda.rs (all `.run()` call sites on EdaEngine/EdaRealEngine)
    - tests/engines/pso/test_pso.rs (all `.run()` call sites on PsoEngine)
    - tests/engines/cma/test_cma.rs (all `.run()` call sites on CmaEngine — research counts 17+)
    - tests/engines/cellular/test_cellular.rs (3 CellularEngine::new() call sites)
    - tests/engines/alps/test_alps.rs (6 AlpsEngine::new() call sites)
    - benches/cellular.rs (6 CellularEngine::new() call sites)
    - benches/alps.rs (4 AlpsEngine::new() call sites)
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-RESEARCH.md (Pattern 2: run() callers need updating; Anti-pattern: bench new() callers use .expect("valid config"))
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-02-SUMMARY.md and 78-03-SUMMARY.md (exact final signatures)
  </read_first>
  <action>
    Update every caller of the changed signatures so the workspace compiles. The `run()` methods
    now return `Result<XxxResult<U>, GaError>` and `new()` returns `Result<Self, GaError>`.

    TESTS (happy-path callers): append `.expect("engine run should succeed")` to each
    `engine.run()` whose result is used as a value, e.g.
    `let result = engine.run().expect("engine run should succeed");`. For
    `CellularEngine::new(...)` and `AlpsEngine::new(...)` in tests, append
    `.expect("valid test config")`.

    BENCHES (`benches/cellular.rs`, `benches/alps.rs`): criterion bench bodies cannot use `?`.
    Append `.expect("valid bench config")` to every `CellularEngine::new(...)` and
    `AlpsEngine::new(...)` call (6 + 4 sites). If any bench calls `.run()` on
    EDA/PSO/CMA-style engines, append `.expect("bench run should succeed")` likewise.

    Do NOT change test assertions on the unwrapped result value. Only thread through the new
    Result. Use grep to confirm you found every site before building:
    `grep -rn '\.run()\|CellularEngine::new\|AlpsEngine::new' tests/ benches/`.
  </action>
  <verify>
    <automated>cargo build --tests 2>&1 | tail -10 ; cargo build --benches 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --tests` exits 0
    - `cargo build --benches` exits 0
    - No remaining compile error referencing `Result<...>` vs `...Result<U>` mismatch in tests/ or benches/
    - benches/cellular.rs and benches/alps.rs use `.expect(` on every `::new(` call
  </acceptance_criteria>
  <done>All existing test and bench callers compile against the new Result-returning run()/new() signatures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add engine error-path tests (EDA/PSO/CMA empty-init, Cellular/ALPS zero-size new())</name>
  <files>tests/engines/eda/test_eda.rs, tests/engines/pso/test_pso.rs, tests/engines/cma/test_cma.rs, tests/engines/cellular/test_cellular.rs, tests/engines/alps/test_alps.rs</files>
  <read_first>
    - tests/engines/eda/test_eda.rs (existing test patterns: how engines are constructed, what chromosome type is used, imports)
    - tests/engines/pso/test_pso.rs (same)
    - tests/engines/cma/test_cma.rs (same)
    - tests/engines/cellular/test_cellular.rs (existing CellularEngine::new usage + config builder)
    - tests/engines/alps/test_alps.rs (existing AlpsEngine::new usage + AlpsConfiguration builder)
    - src/error.rs (GaError variants: InitializationError, ConfigurationError)
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-RESEARCH.md ("Test for Cellular/ALPS new() returning ConfigurationError"; Wave 0 Gaps list)
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-VALIDATION.md (per-task verification map)
  </read_first>
  <behavior>
    - EDA Bernoulli run() with init_fn returning empty Vec → Err(GaError::InitializationError(_))
    - EDA Real run() with init_fn returning empty Vec → Err(GaError::InitializationError(_))
    - PSO run() with init_fn returning empty Vec → Err(GaError::InitializationError(_))
    - CMA run() with init_fn returning empty Vec → Err(GaError::InitializationError(_)) (covers all 3 sites via a single empty-init reproduction; if restart/first-init require distinct configs, add one test each)
    - CellularEngine::new() with rows=0 → Err(GaError::ConfigurationError(_)); same for cols=0
    - AlpsEngine::new() with layer_size=0 → Err(GaError::ConfigurationError(_)); same for n_layers=0
  </behavior>
  <action>
    Add these tests (all in the `tests/` tree per CLAUDE.md — never inline in src/). Use the
    chromosome type and config builders already used by each engine's existing tests (read them
    first to match the exact generic params and constructors).

    - tests/engines/eda/test_eda.rs: `test_run_empty_init_returns_error` for the Bernoulli
      EdaEngine, and `test_real_run_empty_init_returns_error` for EdaRealEngine. Each constructs
      the engine with an `init_fn` that returns `Vec::new()` and asserts
      `matches!(engine.run(), Err(GaError::InitializationError(_)))`.
    - tests/engines/pso/test_pso.rs: `test_run_empty_init_returns_error` — same shape.
    - tests/engines/cma/test_cma.rs: `test_run_empty_init_returns_error` — assert
      `InitializationError`. If covering the `(first init)` and `(restart)` messages requires a
      restart-triggering config, add `test_run_empty_init_first` and
      `test_run_empty_init_restart` accordingly.
    - tests/engines/cellular/test_cellular.rs: `test_new_rejects_zero_rows` and
      `test_new_rejects_zero_cols`, each asserting
      `matches!(CellularEngine::<...>::new(config, init, fitness), Err(GaError::ConfigurationError(_)))`.
    - tests/engines/alps/test_alps.rs: `test_new_rejects_zero_layer_size` and
      `test_new_rejects_zero_n_layers`, asserting `ConfigurationError`.

    Import `GaError` from the crate's public error path used by sibling tests.
  </action>
  <verify>
    <automated>cargo test test_run_empty_init test_new_rejects_zero 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test test_run_empty_init` runs at least 4 tests (EDA Bernoulli, EDA Real, PSO, CMA) and all pass
    - `cargo test test_new_rejects_zero` runs at least 4 tests (cellular rows, cellular cols, alps layer_size, alps n_layers) and all pass
    - Each new test asserts the exact GaError variant via `matches!(..., Err(GaError::InitializationError(_)))` or `ConfigurationError`
    - All new tests live under tests/ (no inline #[cfg(test)] added to src/)
  </acceptance_criteria>
  <done>Error-path tests exist and pass for EDA(x2)/PSO/CMA empty-init InitializationError and Cellular/ALPS zero-size ConfigurationError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add operator error-path tests + final panic audit</name>
  <files>tests/operations/test_mutation.rs, tests/operations/test_operations.rs</files>
  <read_first>
    - tests/operations/test_mutation.rs (existing crossover test patterns; how chromosomes with gene IDs are built)
    - tests/operations/test_operations.rs (existing selection test patterns; how Selection variants and the trait are exercised)
    - src/operations/crossover/order.rs (order() returns Result<Vec<U>, GaError>; non-unique IDs → CrossoverError — final signature from Plan 03)
    - src/operations/selection.rs (SelectionOperator::select returns Result; Lexicase arm → SelectionError — final signature from Plan 03)
    - src/error.rs (GaError::CrossoverError, GaError::SelectionError)
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-VALIDATION.md (SC-3, SC-6)
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-03-SUMMARY.md (exact final operator signatures + cellular cascade handling)
  </read_first>
  <behavior>
    - order() with two parents having non-unique gene IDs within a parent → Err(GaError::CrossoverError(_))
    - Selection::Lexicase invoked through SelectionOperator::select(...) → Err(GaError::SelectionError(_))
    - Selection::EpsilonLexicase through the trait → Err(GaError::SelectionError(_))
  </behavior>
  <action>
    1. tests/operations/test_mutation.rs: add `test_ox_crossover_non_unique_ids_returns_error`.
       Build two parent chromosomes whose DNA contains duplicate gene IDs (so OX cannot fill every
       child position), call `order(&p1, &p2)`, and assert
       `matches!(order(&p1, &p2), Err(GaError::CrossoverError(_)))`. Match the chromosome
       construction style already used in this file.

    2. tests/operations/test_operations.rs: add `test_lexicase_selection_via_trait_returns_error`.
       Build a small population, then call
       `SelectionOperator::select(&Selection::Lexicase, &pop, 1, 1, 2)` (use the trait method on
       the `Selection` enum) and assert `matches!(result, Err(GaError::SelectionError(_)))`. Add a
       second assertion (or sibling test) for `Selection::EpsilonLexicase`. Import the
       `SelectionOperator` trait so the `.select()` method resolves.

    3. SC-1 regression verification (GP bloat enforcement — already satisfied by Phase 53, no
       code change required). SC-1 of ROADMAP requires GP subtree crossover/mutation to return
       `GaError::TreeDepthExceeded` / `GaError::TreeSizeExceeded` when `max_depth` /
       `max_node_count` (configured by the user in `GpConfiguration`) are exceeded. This is ALREADY
       implemented: `src/engines/gp/crossover.rs` and `src/engines/gp/mutation.rs` route bloat
       enforcement through `check_limits(...)?` in `src/engines/gp/node.rs`, which returns those
       two variants — there are zero `panic!` calls in either operator file (the only `.unwrap()`
       sites are the `Replacer`/`NodeReplacer` `self.replacement.take().unwrap()` internal
       invariants, excluded by D-08). Existing passing tests `test_bloat_limit_crossover` and
       `test_bloat_limit_mutation` in `tests/gp.rs` already assert both variants. Do NOT add or
       convert any GP-operator code for SC-1. Instead, run the existing GP bloat tests to confirm
       SC-1 still holds after this phase's edits, and record the result in the SUMMARY as the SC-1
       evidence (note: SC-1 was satisfied by Phase 53; this phase only re-verifies it).

    4. Final panic audit (SC-6): run the audit grep across the converted src/ files and confirm
       no user-input-reachable `panic!` / `lock().unwrap()` / `lock().expect` remains. The only
       allowed remaining panics in scope are:
       - src/engines/gp/chromosome.rs: the `dna()`/`dna_mut()`/`set_dna()` misuse panics with
         messages "not supported — use GpChromosome with GpGa, not Ga" (D-01 excluded, D-02
         confirms these messages are deliberate and sufficient — no improvement required)
       - GP-operator index/replacement internal-invariant `.unwrap()` calls (D-08)
       - Any panics explicitly excluded by D-08 with an invariant comment
       Record the audit grep output in the SUMMARY, noting D-02 as an explicit decision
       (panic messages confirmed sufficient, no change needed).
  </action>
  <verify>
    <automated>cargo test test_ox_crossover_non_unique test_lexicase_selection_via_trait 2>&1 | tail -15 ; cargo test test_bloat_limit_crossover test_bloat_limit_mutation 2>&1 | tail -10 ; grep -c 'panic!' src/engines/gp/crossover.rs src/engines/gp/mutation.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test test_ox_crossover_non_unique` passes and the test asserts `Err(GaError::CrossoverError(_))`
    - `cargo test test_lexicase_selection_via_trait` passes; asserts `Err(GaError::SelectionError(_))` for both Lexicase and EpsilonLexicase through the trait
    - `cargo test test_bloat_limit_crossover test_bloat_limit_mutation` passes — proving SC-1 (GP subtree crossover/mutation return TreeDepthExceeded/TreeSizeExceeded; these tests pre-exist from Phase 53 and require no new code)
    - `grep -c 'panic!' src/engines/gp/crossover.rs src/engines/gp/mutation.rs` returns 0 for both files (confirms GP bloat enforcement is panic-free)
    - Final audit grep over the converted src/ files returns no `panic!` / `lock().unwrap()` / `lock().expect` (excluding documented `debug_assert!` invariants, the out-of-scope GP chromosome misuse panics, and the GP-operator internal-invariant `.unwrap()` calls per D-08)
    - New tests live under tests/ (no inline #[cfg(test)] added to src/)
  </acceptance_criteria>
  <done>OX non-unique-ID test (CrossoverError) and Lexicase-via-trait test (SelectionError) pass; existing GP bloat tests confirm SC-1; final panic audit over converted src/ files is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite green gate</name>
  <files>tests/engines/eda/test_eda.rs</files>
  <read_first>
    - .planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-VALIDATION.md (full suite command, sign-off checklist)
    - CLAUDE.md (PRs must pass cargo test, cargo test --features serde, cargo clippy, zero rustdoc warnings)
  </read_first>
  <action>
    Run the full validation suite and resolve any remaining failures (e.g. doctests in changed
    files whose examples now need `?`/`.unwrap()`, or clippy lints introduced by the conversions
    such as `needless_return` or `result_unit_err`). Do NOT suppress lints with `#[allow(...)]`
    unless an existing project pattern already does so for the same lint; prefer fixing the code.
    If a public doctest in `src/operations/selection.rs`, `src/engines/cellular/engine.rs`, or
    `src/engines/alps/engine.rs` calls the changed `new()`/`select()` signatures, update the
    doctest to handle the `Result` (use `.unwrap()` or `?` consistent with sibling doctests).
    This task touches a test file only as a placeholder for the gate; the real work is fixing
    whatever the suite surfaces across the converted files.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -15 && cargo test --features serde 2>&1 | tail -15 && cargo clippy 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0 (all tests pass)
    - `cargo test --features serde` exits 0
    - `cargo clippy` exits 0 with no new warnings on the converted files
    - `cargo doc --no-deps 2>&1 | grep -c warning` returns 0 (zero rustdoc warnings, per CLAUDE.md)
  </acceptance_criteria>
  <done>cargo test, cargo test --features serde, cargo clippy, and cargo doc --no-deps all pass cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/bench harness → changed engine/operator APIs | Callers must thread the new Result returns; mis-threading would mask the error paths this phase introduces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-10 | Repudiation | error-path test coverage | mitigate | Add explicit tests asserting the exact GaError variant for each former panic, proving the recoverable-error contract holds |
| T-78-11 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — test/bench source edits only |
</threat_model>

<verification>
- `cargo build --tests` and `cargo build --benches` exit 0 (Task 1)
- New error-path tests pass: empty-init (EDA x2/PSO/CMA → InitializationError), zero-size new() (Cellular/ALPS → ConfigurationError), OX non-unique IDs (→ CrossoverError), Lexicase via trait (→ SelectionError)
- SC-1 re-verified: existing `test_bloat_limit_crossover` / `test_bloat_limit_mutation` in `tests/gp.rs` pass (GP subtree crossover/mutation already return TreeDepthExceeded/TreeSizeExceeded via check_limits — no GP-operator code change in this phase)
- Final panic audit over converted src/ files is clean (only documented out-of-scope/D-08 exclusions remain)
- `cargo test && cargo test --features serde && cargo clippy` all green; zero rustdoc warnings
</verification>

<success_criteria>
- Every test/bench caller compiles against the new Result-returning run()/new()/select() signatures
- Error-path tests added and passing for all six former-panic categories (SC-2, SC-3, SC-4, SC-5)
- SC-1 confirmed: GP subtree crossover/mutation return TreeDepthExceeded/TreeSizeExceeded on user-configured max_depth/max_node_count violations, proven by existing passing tests (satisfied by Phase 53; re-verified, no new code)
- SC-6 grep audit confirms no user-input-reachable panics remain in converted src/ files
- SC-7 full suite green; SC-8 clippy clean; zero rustdoc warnings
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- New test: `test_run_empty_init_returns_error` (EDA Bernoulli) + `test_real_run_empty_init_returns_error` (EdaRealEngine)
- New test: `test_run_empty_init_returns_error` (PSO)
- New test(s): CMA empty-init InitializationError (covering base + first-init + restart sites)
- New tests: `test_new_rejects_zero_rows`, `test_new_rejects_zero_cols` (Cellular)
- New tests: `test_new_rejects_zero_layer_size`, `test_new_rejects_zero_n_layers` (ALPS)
- New test: `test_ox_crossover_non_unique_ids_returns_error` (CrossoverError)
- New test: `test_lexicase_selection_via_trait_returns_error` (SelectionError, Lexicase + EpsilonLexicase)
- SC-1 re-verification (no new artifact): existing `test_bloat_limit_crossover` / `test_bloat_limit_mutation` in `tests/gp.rs` confirm GP subtree crossover/mutation return TreeDepthExceeded/TreeSizeExceeded — recorded as SC-1 evidence in the SUMMARY
- Updated callers: all EDA/PSO/CMA `.run()` and Cellular/ALPS `::new()` sites in tests/ and benches/ thread the new Result
</artifacts_produced>

<output>
Create `.planning/phases/78-replace-user-input-panics-with-gaerror-issue-279/78-04-SUMMARY.md` when done
</output>