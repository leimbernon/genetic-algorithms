---
phase: 83-wire-lexicase-selection-into-ga-run-close-sel-02-sel-03-trai
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - tests/engines/lexicase/test_ga_run_lexicase.rs
  - tests/test_engines.rs
autonomous: true
requirements: [SEL-02, SEL-03, TRAITS-01]
user_setup: []

must_haves:
  truths:
    - "Integration test file tests/engines/lexicase/test_ga_run_lexicase.rs exists and is registered in tests/test_engines.rs"
    - "Test proves Ga::<MultiCaseChromosome>::run_lexicase() completes without error (SEL-02)"
    - "Test proves Selection::EpsilonLexicase runs end-to-end via run_lexicase() (SEL-03)"
    - "Test proves ga.run() with Selection::Lexicase returns ConfigurationError naming run_lexicase (SEL-02)"
    - "Test proves scalar fitness is synced to mean case score after a lexicase run (TRAITS-01/D-04)"
    - "cargo test test_ga_run_lexicase exits 0"
  artifacts:
    - path: "tests/engines/lexicase/test_ga_run_lexicase.rs"
      provides: "End-to-end integration tests for the lexicase-wired GA run path"
      contains: "fn test_ga_run_lexicase_completes"
    - path: "tests/test_engines.rs"
      provides: "Module registration for the new lexicase test module"
      contains: "mod lexicase"
  key_links:
    - from: "tests/engines/lexicase/test_ga_run_lexicase.rs"
      to: "src/engines/ga/mod.rs::run_lexicase"
      via: "test constructs Ga<MultiCaseChromosome> and calls .run_lexicase()"
      pattern: "run_lexicase"
    - from: "tests/test_engines.rs"
      to: "tests/engines/lexicase/test_ga_run_lexicase.rs"
      via: "mod lexicase { mod test_ga_run_lexicase; }"
      pattern: "mod test_ga_run_lexicase"
---

<objective>
Add the missing integration tests that prove the Phase 83 wiring works end-to-end: a `Ga<MultiCaseChromosome>` configured with `Selection::Lexicase` / `Selection::EpsilonLexicase` runs to completion through `run_lexicase()` / `run_lexicase_with_callback()`, and the standard `run()` path returns a clear error for the lexicase variants.

Purpose: Phase 50 only tested `lexicase_selection()` and `factory_lexicase()` in isolation; there are NO tests exercising a full GA run on the lexicase path. These tests are the proof that SEL-02 / SEL-03 / TRAITS-01 are closed at the engine-integration level.
Output: New test module `tests/engines/lexicase/test_ga_run_lexicase.rs` registered in `tests/test_engines.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-wire-lexicase-selection-into-ga-run-close-sel-02-sel-03-trai/83-RESEARCH.md
@.planning/phases/83-wire-lexicase-selection-into-ga-run-close-sel-02-sel-03-trai/83-01-SUMMARY.md

# Test fixtures and builder patterns to mirror
@tests/structures.rs
@tests/operations/test_selection_lexicase.rs
@tests/engines/test_ga.rs
</context>

<tasks>

<task id="T01" type="implement">
  <name>Task 1: Create the lexicase GA-run integration test file</name>
  <read_first>
    - tests/structures.rs (the `MultiCaseChromosome` fixture at ~line 115 — it already implements `ChromosomeT`, `LinearChromosome`, `VectorFitness`, `ValueMutable`, `OperatorCompat`, `RealValuedMutation`; `calculate_fitness()` populates `fitness_values` from gene ids and sets scalar `fitness` to their mean; `Gene { id: i32 }` is the gene type)
    - tests/operations/test_selection_lexicase.rs (lines 1-37 — import style `use crate::structures::{Gene, MultiCaseChromosome};`, and the `make_multi_case_chromosome` / `pop_with_cases` helper patterns)
    - tests/engines/test_ga.rs (lines 494-509 — the canonical `Ga::new()...with_population_size(...).with_chromosome_length(ChromosomeLength::Fixed(n)).with_alleles(...).with_initialization_fn(generic_random_initialization::<T>)` builder + `ga.initialization()` pattern; note `use genetic_algorithms::ChromosomeLength;`)
    - src/engines/ga/mod.rs (the run_lexicase / run_lexicase_with_callback methods added in Plan 01, and the run_with_callback lexicase error guard — confirm exact method names and the error message substring `run_lexicase`)
    - src/configuration/builders.rs (~line 33 `with_epsilon_lexicase(f64)` — epsilon is set via this builder, NOT as a variant field; `Selection::Lexicase` and `Selection::EpsilonLexicase` are unit variants)
  </read_first>
  <action>
    Create `tests/engines/lexicase/test_ga_run_lexicase.rs`. Import `use crate::structures::{Gene, MultiCaseChromosome};` and the needed public items: `use genetic_algorithms::{ga::Ga, ChromosomeLength, operations::Selection, traits::{ConfigurationT, ChromosomeT, VectorFitness}, error::GaError};` (confirm exact module paths against the other test files while reading). Mirror the builder pattern from `tests/engines/test_ga.rs`: build a `Ga::<MultiCaseChromosome>` with a small population (e.g. 20-40), `ChromosomeLength::Fixed(4)`, an alleles vector of a handful of `Gene { id }` values, `generic_random_initialization::<MultiCaseChromosome>` as the init fn, and `with_max_generations(20)`. Because `MultiCaseChromosome::calculate_fitness()` derives `fitness_values` from the DNA itself, no `with_fitness_fn` is required — but verify by reading the fixture; if the engine requires a fitness fn to be set, provide a trivial closure that returns 0.0 (the fixture's `calculate_fitness` overrides it for case scores).

    Write these five tests (function names are load-bearing — the verify commands grep for them):

    1. `test_ga_run_lexicase_completes` — build the GA with `Selection::Lexicase`, set `with_number_of_couples` to roughly half the population, call `.build()` then `.run_lexicase()`, assert the result is `Ok` and the returned population is non-empty. (SEL-02)

    2. `test_ga_run_epsilon_lexicase_completes` — same as above but `Selection::EpsilonLexicase` plus `.with_epsilon_lexicase(0.5)`; call `.run_lexicase()`, assert `Ok`. (SEL-03)

    3. `test_run_lexicase_on_non_vector_fitness_returns_error` — build the GA with `Selection::Lexicase` and call the STANDARD `.run()` (not run_lexicase). Assert the result is `Err(GaError::ConfigurationError(msg))` and that `msg` contains the substring `run_lexicase`. (Proves the T01 guard from Plan 01.) Note: `MultiCaseChromosome` does implement VectorFitness, so this asserts the run()-path GUARD fires regardless of the chromosome type — it is the misuse-of-entry-point guard, not a trait-bound check. Name aside, the behavior under test is "run() rejects lexicase and points to run_lexicase".

    4. `test_lexicase_mean_sync_in_run` — run a 1-generation lexicase GA, then assert that after the run every chromosome in the final population has `fitness()` equal to the mean of its `fitness_values()` (within an epsilon like 1e-9), confirming the D-04 scalar-fitness sync performed inside `factory_lexicase`. Skip chromosomes whose `fitness_values()` is empty. (TRAITS-01 / D-04)

    5. `test_run_lexicase_diversity` — run a lexicase GA for ~20 generations on a population whose chromosomes have differing case-score profiles and assert the run completes and produces a population with more than one distinct `fitness_values()` profile (i.e. specialists are not all collapsed to one identical vector). Keep the assertion robust: assert the count of unique `fitness_values()` vectors in the final population is `>= 2`, OR if that is too strict for the random seed, assert the run merely completes and the best chromosome's `fitness_values()` is non-empty. Prefer the stronger assertion; fall back only if it proves flaky during execution.

    Do NOT inline any negative-grep literal that a downstream gate rejects. Keep all tests deterministic where possible by setting `with_rng_seed` if that builder exists (check while reading test_ga.rs); if seeding is unavailable, keep populations small and assertions tolerant.
  </action>
  <acceptance_criteria>
    - File `tests/engines/lexicase/test_ga_run_lexicase.rs` exists
    - It defines `fn test_ga_run_lexicase_completes`, `fn test_ga_run_epsilon_lexicase_completes`, `fn test_run_lexicase_on_non_vector_fitness_returns_error`, `fn test_lexicase_mean_sync_in_run`, `fn test_run_lexicase_diversity`
    - The file references `run_lexicase` and `Selection::Lexicase` and `Selection::EpsilonLexicase`
    - `test_run_lexicase_on_non_vector_fitness_returns_error` asserts the error message contains `run_lexicase`
  </acceptance_criteria>
  <done>The integration test file compiles and exercises the full lexicase run path plus the standard-path error guard.</done>
</task>

<task id="T02" type="implement">
  <name>Task 2: Register the lexicase test module and run the suite</name>
  <read_first>
    - tests/test_engines.rs (the `mod engines { ... }` block — new engine test modules are declared here as `mod <dir> { mod <file>; }`, e.g. the `mod cma { mod test_cma; }` pattern at ~line 18)
  </read_first>
  <action>
    Add a `mod lexicase { mod test_ga_run_lexicase; }` entry inside the `mod engines { ... }` block in `tests/test_engines.rs`, placed alphabetically/logically near the other engine modules (e.g. after the `mod island { ... }` block or grouped with the operator-adjacent engines — match the existing ordering style). Then run the verify commands: `cargo test test_ga_run_lexicase` must compile and pass all five tests; run the full gate `cargo test` and `cargo test --features serde` to confirm no regressions; `cargo clippy --all-targets -- -D warnings` clean; `cargo check --target wasm32-unknown-unknown` succeeds. If `test_run_lexicase_diversity` is flaky, adjust to the tolerant fallback assertion described in T01 (do not delete the test).
  </action>
  <acceptance_criteria>
    - `tests/test_engines.rs` contains `mod lexicase` with `mod test_ga_run_lexicase;`
    - `cargo test test_ga_run_lexicase` exits 0 with all five tests passing
    - `cargo test` exits 0 (no regressions)
    - `cargo test --features serde` exits 0
    - `cargo clippy --all-targets -- -D warnings` is clean
    - `cargo check --target wasm32-unknown-unknown` succeeds
  </acceptance_criteria>
  <done>The new lexicase integration test module is registered and the full test + lint + WASM gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → engine API | Tests exercise the public `run_lexicase` / `run` surface; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-T1 | Repudiation | test coverage | mitigate | Five explicit assertions cover happy path (Lexicase + EpsilonLexicase), the standard-path error guard, the D-04 mean-sync invariant, and diversity — closing the Phase 50 "method existence is sufficient" coverage gap |
| T-83-T2 | Denial of Service | flaky diversity test | accept | Diversity test uses small populations and a tolerant fallback assertion; deterministic via rng seed when available |
</threat_model>

<verification>
- `cargo test test_ga_run_lexicase` exits 0 (all five tests pass)
- `cargo test` and `cargo test --features serde` exit 0
- `cargo clippy --all-targets -- -D warnings` clean
- `cargo check --target wasm32-unknown-unknown` succeeds
- grep confirms `mod lexicase` in `tests/test_engines.rs` and `fn test_ga_run_lexicase_completes` in the new file
</verification>

<success_criteria>
End-to-end integration tests prove `run_lexicase()` / `run_lexicase_with_callback()` complete for both Lexicase and EpsilonLexicase, the standard `run()` rejects lexicase with a `run_lexicase`-naming error, and the scalar-fitness mean-sync invariant holds — all green across the full CI gate including WASM.
</success_criteria>

<output>
Create `.planning/phases/83-wire-lexicase-selection-into-ga-run-close-sel-02-sel-03-trai/83-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02)

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `test_ga_run_lexicase_completes` | new test fn | `tests/engines/lexicase/test_ga_run_lexicase.rs` |
| `test_ga_run_epsilon_lexicase_completes` | new test fn | `tests/engines/lexicase/test_ga_run_lexicase.rs` |
| `test_run_lexicase_on_non_vector_fitness_returns_error` | new test fn | `tests/engines/lexicase/test_ga_run_lexicase.rs` |
| `test_lexicase_mean_sync_in_run` | new test fn | `tests/engines/lexicase/test_ga_run_lexicase.rs` |
| `test_run_lexicase_diversity` | new test fn | `tests/engines/lexicase/test_ga_run_lexicase.rs` |
| `mod lexicase` registration | test module wiring | `tests/test_engines.rs` |
