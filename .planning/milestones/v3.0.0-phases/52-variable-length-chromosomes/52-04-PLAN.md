---
phase: 52-variable-length-chromosomes
plan: 04
type: execute
wave: 3
depends_on: ["52-03"]
files_modified:
  - src/configuration.rs
  - src/traits/configuration.rs
  - src/engines/ga.rs
  - src/operations/survivor.rs
  - src/validators/generic_validator.rs
  - tests/test_variable_length.rs
autonomous: true
requirements: [CHR-01, CHR-02]

must_haves:
  truths:
    - "User can configure ChromosomeLength::Variable { min, max } and ga.run() initializes individuals with lengths sampled uniformly from [min, max]"
    - "Extension regrowth under ChromosomeLength::Variable samples individual lengths from [min_obs, max_obs] of the surviving population"
    - "Crossover::VariableLength(AlignmentStrategy::Trim) is dispatched from ga.rs crossover call site before the trait path"
    - "Crossover::VariableLength(AlignmentStrategy::Pad) is dispatched from ga.rs with alleles_ref, producing offspring of max(len_a, len_b)"
    - "Mutation::Insertion and Mutation::Deletion are dispatched from ga.rs via factory_variable_length with chromosome_length and alleles"
    - "length_penalty: Option<f64> field exists on GaConfiguration; with_length_penalty(f64) builder method available on Ga<U>"
    - "Parsimony pressure adjusts effective fitness for sorting only; stored chromosome.fitness() is never mutated"
    - "ChromosomeLength::Variable { min, max } is validated: min >= 1 and min <= max"
    - "length_penalty is validated >= 0.0 in the builder"
  artifacts:
    - path: "src/configuration.rs"
      provides: "length_penalty: Option<f64> flat field on GaConfiguration"
      contains: "length_penalty"
    - path: "src/traits/configuration.rs"
      provides: "with_length_penalty(f64) -> Self method on ConfigurationT"
      contains: "with_length_penalty"
    - path: "src/engines/ga.rs"
      provides: "Variable init sampling, Variable extension regrowth, VariableLength crossover dispatch, Insertion/Deletion mutation dispatch, with_length_penalty impl"
      contains: "factory_variable_length"
    - path: "src/operations/survivor.rs"
      provides: "factory() accepts length_penalty: Option<f64> and applies parsimony adjustment"
      contains: "length_penalty"
    - path: "src/validators/generic_validator.rs"
      provides: "Variable { min, max } validation: min >= 1, min <= max"
      contains: "min >= 1"
  key_links:
    - from: "src/engines/ga.rs"
      to: "src/operations/mutation.rs"
      via: "mutation::factory_variable_length(method, individual, chromosome_length, alleles_ref)"
      pattern: "factory_variable_length"
    - from: "src/engines/ga.rs"
      to: "src/operations/crossover/variable_length.rs"
      via: "crossover::variable_length_trim / variable_length_pad direct call"
      pattern: "variable_length_trim|variable_length_pad"
    - from: "src/engines/ga.rs"
      to: "src/operations/survivor.rs"
      via: "survivor::factory(survivor, chromosomes, pop_size, limit_config, length_penalty)"
      pattern: "survivor::factory"
---

<objective>
Wire ChromosomeLength::Variable end-to-end: update GaConfiguration with length_penalty field and builder, update survivor::factory to accept and apply parsimony pressure, replace the three ChromosomeLength::Variable placeholder errors in ga.rs with functional variable-length init and regrowth, add direct crossover and mutation dispatch branches in ga.rs for the new variable-length operators, and add validator rules for the Variable configuration.

Purpose: Per D-10 through D-15 (CONTEXT.md). CHR-01 init/regrowth + CHR-02 parsimony.
Output: Fully functional variable-length chromosome support in the standard Ga<U> engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-variable-length-chromosomes/52-CONTEXT.md
@.planning/phases/52-variable-length-chromosomes/52-RESEARCH.md
@.planning/phases/52-variable-length-chromosomes/52-02-SUMMARY.md
@.planning/phases/52-variable-length-chromosomes/52-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/configuration.rs (GaConfiguration struct — current flat fields pattern):
```rust
pub(crate) elitism_count: usize,
pub(crate) stagnation_generations: Option<usize>,
pub(crate) convergence_threshold: Option<f64>,
pub(crate) max_duration_secs: Option<f64>,
// Add here:
pub(crate) length_penalty: Option<f64>,
```
GaConfiguration::default() initializes all Option fields to None.

From src/operations/survivor.rs (factory signature — current):
```rust
pub fn factory<U: LinearChromosome>(
    survivor: Survivor,
    chromosomes: &mut Vec<U>,
    population_size: usize,
    limit_configuration: LimitConfiguration,
) -> Result<(), GaError>
```
Target: add `length_penalty: Option<f64>` as fifth parameter. The SurvivorOperator trait is NOT changed (per Open Question 2 resolution — adjustment happens in factory() before delegating to the trait).

From src/engines/ga.rs (survivor call site — line ~1765):
```rust
survivor::factory(
    self.configuration.survivor,
    &mut self.population.chromosomes,
    initial_population_size,
    self.configuration.limit_configuration,
)?;
```
Update to pass `self.configuration.length_penalty` as fifth argument.

From src/engines/ga.rs (three placeholder errors — search string "not yet supported (Phase 52)"):
- Line ~1143: in initialize_random() — replace Variable arm with sampling logic
- Line ~1202: in initialize_with_seeds() — same
- Line ~1954: in extension regrowth deficit loop — same

From src/engines/ga.rs (mutation dispatch — lines ~2628-2679, pattern for else-if branches):
The new else-if for Insertion/Deletion goes BEFORE the final `else { mutation::factory_with_params(...) }` block. Must be added to both child_1 and child_2 mutation blocks (the two symmetrical blocks).

From src/engines/ga.rs (crossover dispatch — line ~2600-2608):
```rust
// Multi-parent variants already dispatched above
// Standard 2-parent crossover path — all other variants
_ => {
    let mut cx_config = configuration.crossover_configuration;
    cx_config.method = effective_method;
    crossover::factory(parent_1, parent_2, cx_config)?
}
```
Add a new branch BEFORE `_` for `Crossover::VariableLength(strategy)` that calls:
- `AlignmentStrategy::Trim` → `crossover::variable_length_trim(parent_1, parent_2)?`
- `AlignmentStrategy::Pad` → `crossover::variable_length_pad(parent_1, parent_2, alleles_ref)?`
The `alleles_ref` is `Option<&[U::Gene]>` already available in the ga.rs generation loop context.

Parsimony adjusted fitness helper (per D-11, inline or private fn in survivor.rs):
```rust
fn effective_fitness<U: ChromosomeT>(c: &U, problem: ProblemSolving, penalty: f64) -> f64 {
    let raw = c.fitness();
    let adjust = penalty * c.dna().len() as f64;
    match problem {
        ProblemSolving::Maximization => raw - adjust,
        ProblemSolving::Minimization | ProblemSolving::FixedFitness => raw + adjust,
    }
}
```
Note: `c.dna()` is a `LinearChromosome` method — survivor functions already use `LinearChromosome` bound.

Variable-length init sampling pattern (per D-13, from RESEARCH.md):
```rust
// In initialize_random() and initialize_with_seeds():
ChromosomeLength::Variable { min, max } => {
    // Per-individual length sampled inside closure, not fixed
    // (see extension regrowth pattern below for the closure structure)
}
// In extension regrowth:
let min_obs = self.population.chromosomes.iter().map(|c| c.dna().len()).min().unwrap_or(*min);
let max_obs = self.population.chromosomes.iter().map(|c| c.dna().len()).max().unwrap_or(*max);
// inside the init closure: rng.random_range(min_obs..=max_obs) for each individual
```

From src/validators/generic_validator.rs — search for existing ChromosomeLength validation to see what already exists (may have a stub or nothing).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: length_penalty field + builder + survivor factory parsimony pressure</name>
  <files>src/configuration.rs, src/traits/configuration.rs, src/engines/ga.rs (builder only), src/operations/survivor.rs, src/operations/survivor/fitness.rs, src/operations/survivor/age.rs, src/operations/survivor/mu_plus_lambda.rs, src/operations/survivor/mu_comma_lambda.rs, src/operations/survivor/deterministic_crowding.rs</files>
  <read_first>
    - src/configuration.rs (GaConfiguration struct fields lines 338–386; GaConfiguration::default() lines 387–423; accessor methods lines 425–470; with_* builder methods lines 660–750 — find with_elitism_count as template for with_length_penalty; also find where with_survivor_method is implemented in ga.rs builder)
    - src/traits/configuration.rs (ConfigurationT trait — with_survivor_method declaration at line ~182; find the correct insertion point for with_length_penalty)
    - src/operations/survivor.rs (factory() function lines 51–end; SurvivorOperator impl; which survivor variants call which submodule functions)
    - src/operations/survivor/fitness.rs (fitness_based — sorting pattern; this is the primary survivor to add parsimony to)
    - src/operations/survivor/mu_plus_lambda.rs (mu+lambda — may sort by fitness; add adjustment)
    - src/operations/survivor/mu_comma_lambda.rs (mu,lambda — similar)
    - src/operations/survivor/age.rs (age-based — sorts by age, not fitness; parsimony still applies per D-12; apply to fitness tiebreak or skip if sort is by age only — read to decide)
    - src/operations/survivor/deterministic_crowding.rs (crowding — pairwise comparison; apply parsimony to fitness comparison)
    - tests/test_variable_length.rs (parsimony stubs to activate)
  </read_first>
  <behavior>
    - Test: GaConfiguration built with .with_length_penalty(0.1) has length_penalty == Some(0.1)
    - Test: with_length_penalty(-0.1) panics or returns ConfigurationError (penalty must be >= 0.0)
    - Test: survivor selection with length_penalty=0.1 and Maximization selects the shorter chromosome over an equal-fitness longer chromosome
    - Test: after survivor selection with length_penalty set, stored chromosome.fitness() is unchanged (parsimony is comparison-only)
  </behavior>
  <action>
In src/configuration.rs:
1. Add `pub(crate) length_penalty: Option<f64>,` to GaConfiguration struct (after `local_search_configuration`, before closing brace). Add doc comment: "Optional parsimony pressure coefficient. When `Some(p)`, longer chromosomes receive a proportional fitness penalty during survivor selection (comparison only — stored fitness is unchanged). Typical range: `1e-4` to `1e-2`. `None` = disabled (default)."
2. In GaConfiguration::default(), add `length_penalty: None,`.
3. In GaConfiguration accessor impl block, add: `pub fn length_penalty(&self) -> Option<f64> { self.length_penalty }`.

In src/traits/configuration.rs:
4. Add `fn with_length_penalty(self, penalty: f64) -> Self;` to ConfigurationT trait, after `fn with_survivor_method`. Doc: "Sets the parsimony pressure coefficient. Longer chromosomes receive penalty × length subtracted from (or added to) their effective fitness during survivor selection. Stored `chromosome.fitness()` is never mutated. `penalty` must be >= 0.0. Typical starting values: `1e-4` (weak) to `1e-2` (strong)."

In src/engines/ga.rs (builder impl block for Ga<U>):
5. Implement `fn with_length_penalty(mut self, penalty: f64) -> Self`: validate `penalty >= 0.0`, panic with clear message if not (consistent with other builder validation patterns); set `self.configuration.length_penalty = Some(penalty);` and return `self`.

In src/operations/survivor.rs:
6. Update `factory()` signature to add `length_penalty: Option<f64>` as fifth parameter.
7. Add a private helper `fn effective_fitness<U: LinearChromosome>(c: &U, problem: ProblemSolving, penalty: f64) -> f64` that computes: for Maximization return `c.fitness() - penalty * c.dna().len() as f64`; for Minimization/FixedFitness return `c.fitness() + penalty * c.dna().len() as f64`.
8. In the `factory()` function body, after the NaN guard, when `length_penalty` is `Some(penalty)`: pre-sort or delegate to each survivor sub-function with the penalty. The cleanest approach: when penalty is set, call a wrapper that temporarily re-sorts `chromosomes` by adjusted fitness before delegating to the trait's `select_survivors`. Concretely: if `length_penalty.is_some()`, for `Survivor::Fitness` and `Survivor::MuPlusLambda` and `Survivor::MuCommaLambda`, sort `chromosomes` by `effective_fitness` (using `problem_solving` from `limit_configuration`) before calling the trait, then call the trait. The trait implementations sort by raw fitness internally — so the effective-fitness pre-sort will be re-overridden. Better approach: pass `length_penalty` into `select_survivors` via a wrapper struct, OR apply the parsimony adjustment in `factory()` by directly sorting when penalty is set (bypassing the trait's internal sort for the penalty case). Use the bypass approach: when `length_penalty.is_some()` and survivor is `Fitness`, implement parsimony truncation directly in factory (sort by adjusted fitness, truncate to `population_size`) instead of calling the trait. For `DeterministicCrowding`, Age: apply standard path (parsimony less meaningful for these). Document the scope of parsimony application in the factory function's doc comment.

In src/engines/ga.rs (survivor call site ~line 1765):
9. Update the call to `survivor::factory(self.configuration.survivor, &mut self.population.chromosomes, initial_population_size, self.configuration.limit_configuration, self.configuration.length_penalty)?;`

In tests/test_variable_length.rs:
10. Activate `test_parsimony_pressure_penalizes_longer_chromosomes_maximization` and `test_parsimony_no_fitness_mutation` stubs with minimal working bodies.
  </action>
  <verify>
    <automated>cargo test --test test_variable_length test_parsimony 2>&1 | tail -5</automated>
  </verify>
  <done>length_penalty field in GaConfiguration; with_length_penalty() builder on Ga; survivor::factory takes length_penalty; parsimony stubs in test_variable_length.rs pass; cargo clippy passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ga.rs Variable init/regrowth unlock + crossover/mutation dispatch + validator + final phase verification</name>
  <files>src/engines/ga.rs, src/validators/generic_validator.rs, tests/test_variable_length.rs</files>
  <read_first>
    - src/engines/ga.rs (three placeholder sites — grep for "not yet supported (Phase 52)"; also crossover dispatch block ~lines 2580-2610; mutation dispatch block ~lines 2628-2679 for child_1 and ~2682-2730 for child_2; also initialize_chromosomes_par function call at line ~1152 to understand how population_size/length/alleles flow)
    - src/validators/generic_validator.rs (full file — find existing chromosome_length validation or validate() function entry point to add min/max checks)
    - src/operations/crossover.rs (pub use variable_length_trim, variable_length_pad — confirm exports are in place after Plan 03)
    - src/operations/mutation.rs (confirm factory_variable_length is exported after Plan 02)
    - tests/test_variable_length.rs (remaining stubs to activate: test_variable_length_initialization_samples_lengths_in_range, test_variable_length_extension_regrowth_samples_from_population)
  </read_first>
  <behavior>
    - Test: Ga<Range<f64>> with ChromosomeLength::Variable { min: 2, max: 8 } initializes a population of 20 where every chromosome has length in [2, 8]
    - Test: ChromosomeLength::Variable { min: 0, max: 5 } fails validation (min must be >= 1)
    - Test: ChromosomeLength::Variable { min: 5, max: 3 } fails validation (min must be <= max)
    - Test: Ga<Range<f64>> with Crossover::VariableLength(AlignmentStrategy::Trim) and Variable { min: 2, max: 6 } completes run() without error
    - Test: Ga<Range<f64>> with Mutation::Insertion and Variable { min: 2, max: 6 } completes run() without error
  </behavior>
  <action>
In src/engines/ga.rs:

Site 1 — initialize_random() (line ~1143): Replace the `ChromosomeLength::Variable { .. } => return Err(...)` arm with functional code. The existing `initialize_chromosomes_par` function takes a single `length` — for Variable, each individual needs its own sampled length, so the population must be built differently. Use a sequential or parallel loop (with WASM cfg gate) matching the extension regrowth pattern: sample `rng.random_range(min..=max)` for each individual's `length`, call `init_fn(individual_len, alleles_ref)`, build chromosome, call fitness. Assign the resulting Vec to `self.population`. Preserve the `number_of_couples` default-setting logic.

Site 2 — initialize_with_seeds() (line ~1202): Same approach for the fill chromosomes section. The seed chromosomes already have their own lengths (no change). Only the random-fill loop needs per-individual length sampling from `[min, max]`.

Site 3 — extension regrowth (line ~1954): Replace the `Variable { .. } => return Err(...)` arm. Compute `min_obs` and `max_obs` from `self.population.chromosomes.iter().map(|c| c.dna().len())`. Handle the degenerate case `min_obs == max_obs` by using that length directly. In the per-individual closure (inside both the `#[cfg(not(target_arch = "wasm32"))]` and `#[cfg(target_arch = "wasm32")]` branches), sample `indiv_len = rng.random_range(min_obs..=max_obs)` and call `init_fn(indiv_len, alleles_ref)`. The `rng` must be local to the closure (call `crate::rng::make_rng()` inside the closure) for the parallel branch. For the wasm sequential branch, a single `rng` outside the loop is fine.

Crossover dispatch (lines ~2580-2610): In the crossover match (which already handles `Crossover::Undx { .. }` etc. in the first branch), add a new branch before `_` for `Crossover::VariableLength(strategy)`:
- Match strategy: `AlignmentStrategy::Trim` → call `crossover::variable_length::variable_length_trim(parent_1, parent_2)?` (returns `Vec<U>`)
- `AlignmentStrategy::Pad` → call `crossover::variable_length::variable_length_pad(parent_1, parent_2, alleles_ref)?` where `alleles_ref` is already the `Option<&[U::Gene]>` variable in scope
- The returned `Vec<U>` is assigned to `children` (same as the other branches)

Mutation dispatch child_1 block (lines ~2628-2679): Add a new `else if` branch BEFORE the final `else { mutation::factory_with_params(...) }`:
```
else if mutation_method == Mutation::Insertion || mutation_method == Mutation::Deletion {
    mutation::factory_variable_length(
        mutation_method,
        &mut child_1,
        self.configuration.limit_configuration.chromosome_length,
        alleles_ref,
    )?;
}
```
Duplicate this else-if for child_2 in the symmetrical block (lines ~2682-2730).

In src/validators/generic_validator.rs:
Find the chromosome_length validation or the main validate() function. Add: for `ChromosomeLength::Variable { min, max }`, return `Err(GaError::ConfigurationError(...))` if `min < 1` (with message "ChromosomeLength::Variable: min must be >= 1") or if `min > max` (with message "ChromosomeLength::Variable: min must be <= max"). For `ChromosomeLength::Fixed(n)`, existing validation (if any) is unchanged.

In tests/test_variable_length.rs:
Activate the remaining stubs: `test_variable_length_initialization_samples_lengths_in_range` and `test_variable_length_extension_regrowth_samples_from_population`. For the initialization test, build a `Ga<Range<f64>>` with a simple fitness function, Variable { min: 2, max: 8 }, population_size=20, run .build()?.run(), then assert all chromosome lengths are in [2, 8]. For the extension regrowth test, use a similar setup with an extension configured (MassGenesis or similar) and small population; after run, assert lengths remain in bounds.

After all changes, run the full verification gate.
  </action>
  <verify>
    <automated>cargo test && cargo test --features serde && cargo check --target wasm32-unknown-unknown && cargo clippy -- -D warnings 2>&1 | tail -20</automated>
  </verify>
  <done>All three ChromosomeLength::Variable placeholder errors in ga.rs replaced with functional code; VariableLength crossover and Insertion/Deletion mutation dispatch branches present in ga.rs; Variable validator rules present; all test_variable_length stubs pass; full cargo test green; wasm32 check passes; cargo clippy passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User configuration | Users provide min/max for Variable and length_penalty — validated at build time |
| Crossover allele sampling | Pad variant samples from user-provided alleles or falls back to cloning existing genes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Denial of Service | Variable { min=0, max=MAX } producing zero-length chromosomes | mitigate | Validator enforces min >= 1; ga.rs Variable init uses validated bounds |
| T-52-05 | Information Disclosure | length_penalty negative value reversing pressure | mitigate | with_length_penalty panics if penalty < 0.0 (documented in builder) |
| T-52-06 | Tampering | Parsimony mutating stored fitness | mitigate | D-11 enforced — effective_fitness helper is comparison-only, stored fitness never written |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
cargo test
cargo test --features serde
cargo check --target wasm32-unknown-unknown
cargo clippy -- -D warnings
</verification>

<success_criteria>
- ChromosomeLength::Variable runs end-to-end in Ga without errors
- Mutation::Insertion / Deletion dispatched from ga.rs via factory_variable_length
- Crossover::VariableLength(Trim/Pad) dispatched directly from ga.rs
- length_penalty field in GaConfiguration; with_length_penalty builder on Ga
- Parsimony pressure applied in survivor::factory without mutating stored fitness
- Validator rejects min < 1 and min > max for Variable configuration
- All tests in test_variable_length.rs pass (no remaining #[ignore] that aren't integration/manual)
- Full cargo test green including --features serde; wasm32 check passes
</success_criteria>

<output>
Create .planning/phases/52-variable-length-chromosomes/52-04-SUMMARY.md when done
</output>
