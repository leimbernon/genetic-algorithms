---
phase: 52-variable-length-chromosomes
plan: 02
type: execute
wave: 1
depends_on: ["52-01"]
files_modified:
  - src/operations.rs
  - src/operations/mutation.rs
  - src/operations/mutation/insertion.rs
  - src/operations/mutation/deletion.rs
  - src/operations/mutation/insertion_add.rs
  - tests/operations/test_mutation_insertion.rs
  - tests/test_variable_length.rs
autonomous: true
requirements: [MUT-06]

must_haves:
  truths:
    - "Mutation::PermutationInsert moves a gene to a different position without changing DNA length"
    - "Mutation::Insertion adds one gene at a random position, clamped to ChromosomeLength::Variable.max"
    - "Mutation::Deletion removes one gene at a random position, clamped to ChromosomeLength::Variable.min"
    - "Mutation::Insertion returns GaError::MutationError when ChromosomeLength::Fixed is configured"
    - "Mutation::Deletion returns GaError::MutationError when ChromosomeLength::Fixed is configured"
    - "factory_non_value() match in mutation.rs is exhaustive (covers all three variants)"
  artifacts:
    - path: "src/operations.rs"
      provides: "Updated Mutation enum with PermutationInsert, Insertion, Deletion"
      contains: "PermutationInsert"
    - path: "src/operations/mutation/deletion.rs"
      provides: "deletion_mutation() implementation"
      exports: ["deletion_mutation"]
    - path: "src/operations/mutation/insertion_add.rs"
      provides: "insertion_add_mutation() implementation"
      exports: ["insertion_add_mutation"]
    - path: "src/operations/mutation/insertion.rs"
      provides: "permutation_insert_mutation() (renamed from insertion_mutation)"
      exports: ["permutation_insert_mutation"]
  key_links:
    - from: "src/operations/mutation.rs"
      to: "src/operations/mutation/deletion.rs"
      via: "mutation::factory_variable_length dispatch"
      pattern: "factory_variable_length"
    - from: "src/operations/mutation.rs"
      to: "src/operations/mutation/insertion_add.rs"
      via: "mutation::factory_variable_length dispatch"
      pattern: "factory_variable_length"
    - from: "src/operations.rs"
      to: "src/operations/mutation.rs"
      via: "Mutation enum dispatch in impl MutationOperator for Mutation"
      pattern: "Mutation::PermutationInsert"
---

<objective>
Rename Mutation::Insertion (permutation move) to Mutation::PermutationInsert, and add two new length-mutating operators: Mutation::Insertion (add gene) and Mutation::Deletion (remove gene). These operators are dispatched via a new factory_variable_length() function that receives ChromosomeLength and alleles — they must NOT go through MutationOperator::mutate trait method, mirroring the pattern used for factory_multi_parent and factory_self_adaptive.

Purpose: Per D-01 through D-04 (CONTEXT.md). v3.0.0 breaking rename allowed.
Output: Updated Mutation enum, two new operator files, renamed insertion file, new factory_variable_length() function, updated test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-variable-length-chromosomes/52-CONTEXT.md
@.planning/phases/52-variable-length-chromosomes/52-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/operations/mutation/insertion.rs (current):
```rust
pub fn insertion_mutation<U: LinearChromosome>(individual: &mut U) -> Result<(), GaError>
```
This function body is correct — only its name and the dispatch arm change.

From src/operations/mutation.rs (relevant dispatch section):
```rust
Mutation::Insertion => {
    return insertion::insertion_mutation(individual);
}
```

From src/operations/mutation.rs (factory_non_value — separate match that must also be updated):
```rust
pub fn factory_non_value<U: LinearChromosome + ValueMutable + 'static>(
    mutation: Mutation,
    individual: &mut U,
) -> Result<(), GaError>
```
(This match block is separate from impl MutationOperator for Mutation — both must be updated.)

From src/engines/ga.rs (pattern for out-of-trait dispatch — factory_self_adaptive):
```rust
} else if mutation_method == Mutation::SelfAdaptiveGaussian {
    mutation::factory_self_adaptive(
        &mut child_1,
        configuration.mutation_configuration.self_adaptive_tau,
        ...
    )?;
}
```
Mutation::Insertion and Mutation::Deletion must use the same else-if pattern in ga.rs (Wave 3 handles ga.rs wiring; this plan adds the factory function only).

From src/types/chromosomes/length.rs:
```rust
pub enum ChromosomeLength {
    Fixed(usize),
    Variable { min: usize, max: usize },
}
```

GaError variants available (no new variant needed per D-09):
```rust
GaError::MutationError(String)
GaError::CrossoverError(String)
```

From src/rng.rs:
```rust
pub fn make_rng() -> impl Rng { ... }  // WASM-compatible
```

Cow pattern for DNA update (from insertion.rs):
```rust
individual.set_dna(Cow::Owned(dna));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename Mutation::Insertion → PermutationInsert + add enum variants</name>
  <files>src/operations.rs, src/operations/mutation/insertion.rs, tests/operations/test_mutation_insertion.rs</files>
  <read_first>
    - src/operations.rs (full Mutation enum definition — lines 161–239; doc comment table at top of module)
    - src/operations/mutation/insertion.rs (current insertion_mutation function — rename to permutation_insert_mutation)
    - tests/operations/test_mutation_insertion.rs (all test functions referencing Mutation::Insertion — each must be updated to Mutation::PermutationInsert)
  </read_first>
  <behavior>
    - Test: Mutation::PermutationInsert on a Range<f64> of length 5 moves one gene to a different position; DNA length remains 5
    - Test: Mutation::Insertion variant exists in the enum (compile-time check)
    - Test: Mutation::Deletion variant exists in the enum (compile-time check)
  </behavior>
  <action>
In src/operations.rs:
1. Rename the variant `Insertion` to `PermutationInsert` in the `Mutation` enum. Update its doc comment to: "Permutation-insert mutation (renamed from `Insertion` in v3.0.0). Removes a gene from one position and re-inserts it adjacent to another, preserving all alleles. Suited for permutation encodings."
2. Add new variant `Insertion` after `PermutationInsert` with doc: "Length-growing insertion mutation. Adds a new gene at a random position, clamped to `ChromosomeLength::Variable.max`. Returns `GaError::MutationError` if `ChromosomeLength::Fixed` is configured. Must be dispatched via `mutation::factory_variable_length()`."
3. Add new variant `Deletion` after `Insertion` with doc: "Length-shrinking deletion mutation. Removes a gene at a random position, clamped to `ChromosomeLength::Variable.min`. Returns `GaError::MutationError` if `ChromosomeLength::Fixed` is configured. Must be dispatched via `mutation::factory_variable_length()`."
4. Update the module-level doc comment table (line ~18) to reflect the rename: `Mutation` row now includes PermutationInsert, Insertion, Deletion.

In src/operations/mutation/insertion.rs:
5. Rename exported function `insertion_mutation` to `permutation_insert_mutation`. Update the function's doc comment first line to: "Permutation-insert mutation for permutation-based chromosomes (renamed from `insertion_mutation` in v3.0.0)." Function body is unchanged.

In tests/operations/test_mutation_insertion.rs:
6. Replace all occurrences of `Mutation::Insertion` with `Mutation::PermutationInsert`. Replace all calls to `insertion_mutation(` with `permutation_insert_mutation(` (if the function is called directly in tests). Update the import if the function is imported directly.
  </action>
  <verify>
    <automated>cargo test --test operations test_mutation_insertion 2>&1 | tail -5</automated>
  </verify>
  <done>cargo test --test operations test_mutation_insertion passes; Mutation::PermutationInsert, Mutation::Insertion, Mutation::Deletion all compile; mutation/insertion.rs exports permutation_insert_mutation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Mutation::Insertion and Deletion operator implementations + factory_variable_length</name>
  <files>src/operations/mutation/deletion.rs, src/operations/mutation/insertion_add.rs, src/operations/mutation.rs, tests/test_variable_length.rs</files>
  <read_first>
    - src/operations/mutation/insertion.rs (permutation_insert_mutation — full function body, use as structural template for the new files)
    - src/operations/mutation.rs (full file — need to: add mod declarations for deletion and insertion_add, update dispatch arms for PermutationInsert/Insertion/Deletion in impl MutationOperator for Mutation, update factory_non_value match, add new factory_variable_length function)
    - src/operations.rs (Mutation enum variants after Task 1 — confirm new variant names)
    - src/types/chromosomes/length.rs (ChromosomeLength enum — Variable { min, max } destructure pattern)
    - src/operations/mutation/cauchy.rs (alternative template showing logging target and error handling style)
    - tests/test_variable_length.rs (Wave 0 stubs to activate after implementation)
  </read_first>
  <behavior>
    - Test: deletion_mutation on a Range<f64> of length 5 with min_len=2 reduces DNA length to 4
    - Test: deletion_mutation when DNA length == min_len returns Ok(()) without changing length (no-op)
    - Test: insertion_add_mutation on a Range<f64> of length 3 with max_len=5 and non-empty alleles increases DNA length to 4
    - Test: insertion_add_mutation when DNA length == max_len returns Ok(()) without changing length (no-op)
    - Test: factory_variable_length with Mutation::Insertion on ChromosomeLength::Fixed(5) returns Err(GaError::MutationError(_))
    - Test: factory_variable_length with Mutation::Deletion on ChromosomeLength::Fixed(5) returns Err(GaError::MutationError(_))
  </behavior>
  <action>
Create src/operations/mutation/deletion.rs implementing `pub fn deletion_mutation<U: LinearChromosome>(individual: &mut U, min_len: usize) -> Result<(), GaError>`:
- If `individual.dna().len() <= min_len`, log `debug!(target="mutation_events", method="deletion"; "DNA at min length {}, skipping", min_len)` and return `Ok(())`.
- Otherwise: sample random position `pos` in `0..len` using `crate::rng::make_rng()`, build a `dna.to_vec()`, call `dna.remove(pos)`, call `individual.set_dna(Cow::Owned(dna))`.
- Log `debug!(target="mutation_events", method="deletion"; "Deletion at pos {}", pos)`.

Create src/operations/mutation/insertion_add.rs implementing `pub fn insertion_add_mutation<U: LinearChromosome>(individual: &mut U, max_len: usize, alleles: Option<&[U::Gene]>) -> Result<(), GaError>` where `U::Gene: Clone`:
- If `individual.dna().len() >= max_len`, log debug and return `Ok(())` (no-op at max).
- Sample `pos` in `0..=len` (inclusive upper bound allows append).
- Sample new gene: if `alleles` is `Some` and non-empty, pick a random allele; else clone a random existing gene from `individual.dna()` (fallback per D-04).
- Build `dna.to_vec()`, call `dna.insert(pos, new_gene)`, call `individual.set_dna(Cow::Owned(dna))`.
- Log `debug!(target="mutation_events", method="insertion_add"; "Insertion at pos {}", pos)`.

In src/operations/mutation.rs:
1. Add `pub mod deletion;` and `pub mod insertion_add;` declarations alongside the existing pub mod list.
2. In `impl MutationOperator for Mutation` match: rename arm `Mutation::Insertion => insertion::insertion_mutation(individual)` to `Mutation::PermutationInsert => insertion::permutation_insert_mutation(individual)`. Add arms for `Mutation::Insertion` and `Mutation::Deletion` that each return `Err(GaError::MutationError("Mutation::Insertion / Deletion require ChromosomeLength context. Use mutation::factory_variable_length().".to_string()))`.
3. In `factory_non_value()` match: add the same three arms (`PermutationInsert` → call `permutation_insert_mutation`, `Insertion` / `Deletion` → return same context error).
4. Add new public function `factory_variable_length<U: LinearChromosome>(mutation: Mutation, individual: &mut U, chromosome_length: ChromosomeLength, alleles: Option<&[U::Gene]>) -> Result<(), GaError>` where `U::Gene: Clone`. This function matches on `mutation`: `Mutation::Insertion` → check `ChromosomeLength::Fixed` → return `MutationError`; `ChromosomeLength::Variable { max, .. }` → call `insertion_add::insertion_add_mutation(individual, max, alleles)`. `Mutation::Deletion` → same pattern with `min` and `deletion::deletion_mutation(individual, min)`. Any other variant → delegate to `factory_with_params(mutation, individual, None, None)`.

In tests/test_variable_length.rs:
5. Remove `#[ignore]` from `test_mutation_permutation_insert_renames_correctly`, `test_mutation_insertion_adds_gene_clamped_to_max`, `test_mutation_deletion_removes_gene_clamped_to_min`, `test_mutation_insertion_on_fixed_returns_error`, `test_mutation_deletion_on_fixed_returns_error`. Fill in minimal working test bodies (no longer `todo!()`).
  </action>
  <verify>
    <automated>cargo test --test test_variable_length 2>&1 | grep -E "FAILED|passed|ignored" | tail -5</automated>
  </verify>
  <done>cargo test --test test_variable_length passes for the 5 MUT-06 stubs (PermutationInsert rename, Insertion add, Deletion remove, Fixed-config errors); cargo clippy -- -D warnings passes; factory_variable_length is pub in src/operations/mutation.rs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutation operator | Internal library — all inputs are Rust-typed; no untrusted user data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Denial of Service | insertion_add_mutation with max_len=0 | mitigate | Guard: if max_len == 0 return Ok(()) (same as at-max no-op) |
| T-52-02 | Tampering | Stored fitness after Insertion/Deletion | accept | Fitness recalculated by ga.rs run loop after each generation; operators do not mutate fitness |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
cargo test --test operations test_mutation_insertion
cargo test --test test_variable_length
cargo clippy -- -D warnings
cargo check --target wasm32-unknown-unknown
</verification>

<success_criteria>
- Mutation::PermutationInsert, Mutation::Insertion, Mutation::Deletion present in enum
- permutation_insert_mutation() in insertion.rs; deletion_mutation() in deletion.rs; insertion_add_mutation() in insertion_add.rs
- factory_variable_length() exported from src/operations/mutation.rs
- All test_mutation_insertion tests pass (rename verified)
- MUT-06 stubs in test_variable_length.rs pass
- cargo clippy clean; cargo check wasm32 passes
</success_criteria>

<output>
Create .planning/phases/52-variable-length-chromosomes/52-02-SUMMARY.md when done
</output>
