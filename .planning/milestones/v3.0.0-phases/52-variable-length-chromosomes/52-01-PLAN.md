---
phase: 52-variable-length-chromosomes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_variable_length.rs
autonomous: true
requirements: [MUT-06, CHR-01, CHR-02]

must_haves:
  truths:
    - "All Phase 52 behaviors have failing test stubs that compile and are marked #[ignore]"
    - "Wave 0 stubs cover MUT-06 (PermutationInsert rename, Insertion add, Deletion remove, Fixed-config errors), CHR-01 (VariableLength Trim/Pad crossover, incompatible-length guard on fixed operators, Variable init, extension regrowth), and CHR-02 (parsimony pressure math, stored fitness unchanged)"
  artifacts:
    - path: "tests/test_variable_length.rs"
      provides: "All Wave 0 test stubs"
      contains: "#[ignore]"
  key_links:
    - from: "tests/test_variable_length.rs"
      to: "src/operations.rs"
      via: "Mutation::PermutationInsert, Mutation::Insertion, Mutation::Deletion, Crossover::VariableLength"
      pattern: "Mutation::PermutationInsert|Mutation::Insertion|Mutation::Deletion|Crossover::VariableLength"
---

<objective>
Create Wave 0 test stubs for all Phase 52 behaviors. Tests must reference the final API names (Mutation::PermutationInsert, Mutation::Insertion, Mutation::Deletion, Crossover::VariableLength(AlignmentStrategy), length_penalty) so that compilation confirms the API contract before implementation.

Purpose: Nyquist compliance — every behavior introduced in Phase 52 has a failing test stub before implementation begins.
Output: tests/test_variable_length.rs with all stubs #[ignore]-marked and the file compiling (or failing only on missing enum variants, which will be resolved in Wave 1–3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-variable-length-chromosomes/52-CONTEXT.md
@.planning/phases/52-variable-length-chromosomes/52-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From src/operations.rs (current Mutation enum — BEFORE rename):
```rust
#[derive(Copy, Clone, Debug, PartialEq)]
pub enum Mutation {
    Swap, Inversion, Scramble, Value, BitFlip, Creep, Gaussian,
    Polynomial, NonUniform, Insertion, ListValue, Differential,
    Cauchy, LevyFlight, Uniform, SelfAdaptiveGaussian,
}
```

After Phase 52 Wave 1 the enum will have:
- `Mutation::PermutationInsert` (was `Insertion`)
- `Mutation::Insertion` (new — add gene)
- `Mutation::Deletion` (new — remove gene)

From src/operations.rs (Crossover enum — BEFORE Phase 52):
```rust
#[derive(Copy, Clone, Debug, PartialEq)]
pub enum Crossover {
    Cycle, MultiPoint, Uniform, SinglePoint, Order, Pmx, Sbx,
    BlendAlpha, Arithmetic, Clone, Rejuvenate, EdgeRecombination,
    MultiGroupPmx, MultiGroupOx,
    Undx { num_parents: usize },
    Spx { num_parents: usize },
    Pcx { num_parents: usize },
}
```

After Phase 52 Wave 2 the enum will add:
- `Crossover::VariableLength(AlignmentStrategy)` where `AlignmentStrategy` is `Trim | Pad`

From src/configuration.rs (GaConfiguration fields — BEFORE Phase 52):
```rust
pub(crate) elitism_count: usize,
pub(crate) stagnation_generations: Option<usize>,
// After Phase 52 Wave 3 adds:
pub(crate) length_penalty: Option<f64>,
```

Test file pattern (from tests/operations/test_mutation_insertion.rs):
```rust
use genetic_algorithms::chromosomes::Range;
use genetic_algorithms::operations::Mutation;
use genetic_algorithms::traits::{ChromosomeT, LinearChromosome};
```

Chromosome type for tests: `Range<f64>` from `genetic_algorithms::chromosomes`
ChromosomeLength import: `genetic_algorithms::chromosomes::ChromosomeLength`
Ga builder import: `genetic_algorithms::ga::Ga` and `genetic_algorithms::traits::ConfigurationT`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Wave 0 test stubs for Phase 52</name>
  <files>tests/test_variable_length.rs</files>
  <read_first>
    - tests/operations/test_mutation_insertion.rs (existing insertion test — naming conventions, imports, Range chromosome setup)
    - tests/operations/test_survivor.rs (survivor test patterns — how LimitConfiguration and ProblemSolving are used)
    - tests/operations/test_crossover_single_point.rs (crossover test setup with two parents)
    - src/operations.rs (current enum variants — to use correct names in stubs; note Mutation::Insertion is the current permutation-move operator)
    - src/configuration.rs (GaConfiguration struct — to reference fields in builder stubs)
    - src/types/chromosomes/length.rs (ChromosomeLength enum — Fixed and Variable variants)
  </read_first>
  <action>
Create tests/test_variable_length.rs with the following stub test functions, all annotated #[ignore] and containing a single `todo!()` body. The stubs MUST use the final API names (post-Wave 1-3) so that enabling them after implementation immediately validates the feature. Group stubs into three labelled sections with a comment header.

Import block at top of file:
- `use genetic_algorithms::chromosomes::{Range, ChromosomeLength};`
- `use genetic_algorithms::operations::{Mutation, Crossover};`
- `use genetic_algorithms::ga::Ga;`
- `use genetic_algorithms::traits::ConfigurationT;`
- `use genetic_algorithms::configuration::ProblemSolving;`

Note: `AlignmentStrategy` will be exported from `genetic_algorithms::operations` after Wave 2 adds it to `src/operations.rs`. Use the full path `genetic_algorithms::operations::AlignmentStrategy` in stub imports — compilation will fail until Wave 2 but that is expected.

Section 1 — MUT-06 stubs (5 tests):
1. `test_mutation_permutation_insert_renames_correctly` — verify `Mutation::PermutationInsert` moves a gene to a different position without changing chromosome length on a Range<f64> of length 5.
2. `test_mutation_insertion_adds_gene_clamped_to_max` — apply `Mutation::Insertion` to a Range<f64> of length 3 with max=5; assert DNA length increases to 4.
3. `test_mutation_deletion_removes_gene_clamped_to_min` — apply `Mutation::Deletion` to a Range<f64> of length 5 with min=2; assert DNA length decreases to 4.
4. `test_mutation_insertion_on_fixed_returns_error` — apply `Mutation::Insertion` when `ChromosomeLength::Fixed(5)` is configured; assert `Err(GaError::MutationError(_))` is returned.
5. `test_mutation_deletion_on_fixed_returns_error` — same as above but for `Mutation::Deletion`.

Section 2 — CHR-01 stubs (6 tests):
6. `test_crossover_variable_length_trim_produces_min_len_offspring` — cross two Range<f64> parents of lengths 3 and 5 using `Crossover::VariableLength(AlignmentStrategy::Trim)`; assert both offspring have length 3.
7. `test_crossover_variable_length_pad_produces_max_len_offspring` — cross two Range<f64> parents of lengths 3 and 5 using `Crossover::VariableLength(AlignmentStrategy::Pad)`; assert both offspring have length 5.
8. `test_crossover_incompatible_length_single_point_returns_error` — create two Range<f64> parents with lengths 3 and 5; apply `Crossover::SinglePoint`; assert `Err(GaError::CrossoverError(_))`.
9. `test_crossover_incompatible_length_uniform_returns_error` — same but for `Crossover::Uniform`.
10. `test_variable_length_initialization_samples_lengths_in_range` — build a `Ga<Range<f64>>` with `ChromosomeLength::Variable { min: 2, max: 8 }` and population_size=20; after initialization assert that all chromosome lengths are in `[2, 8]`.
11. `test_variable_length_extension_regrowth_samples_from_population` — integration stub (todo!) asserting regrowth individuals have lengths within `[min_obs, max_obs]` of surviving population.

Section 3 — CHR-02 stubs (2 tests):
12. `test_parsimony_pressure_penalizes_longer_chromosomes_maximization` — in a Vec of two Range<f64> chromosomes (same raw fitness, different lengths), apply survivor selection with `length_penalty = Some(0.1)` and `Maximization`; assert the shorter chromosome survives.
13. `test_parsimony_no_fitness_mutation` — apply parsimony pressure and assert the stored `chromosome.fitness()` value is unchanged after survivor selection.

All stubs: annotate with `#[test]` and `#[ignore]` and body `todo!()`.
  </action>
  <verify>
    <automated>cargo check 2>&1 | grep -c "test_variable_length" || true; echo "Stubs written — compilation errors are expected until Wave 1-3 add the enum variants"</automated>
  </verify>
  <done>tests/test_variable_length.rs exists with 13 #[ignore]-annotated stub test functions covering MUT-06, CHR-01, and CHR-02; file compiles or fails only on missing enum variants (Mutation::PermutationInsert, Mutation::Insertion, Mutation::Deletion, Crossover::VariableLength, AlignmentStrategy) that will be added in Wave 1-2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only file | No production code changes; zero attack surface increase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
cargo check (test stubs compile or fail only on missing symbols introduced by later waves)
</verification>

<success_criteria>
tests/test_variable_length.rs exists with 13 #[ignore]-annotated stubs. File is committed to the branch.
</success_criteria>

<output>
Create .planning/phases/52-variable-length-chromosomes/52-01-SUMMARY.md when done
</output>
