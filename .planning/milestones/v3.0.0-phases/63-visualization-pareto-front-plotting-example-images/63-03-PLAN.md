---
phase: 63-visualization-pareto-front-plotting-example-images
plan: 03
type: execute
wave: 3
depends_on: ["63-01", "63-02"]
files_modified:
  - docs/images/nsga2_zdt1.png
  - docs/images/spea2_zdt1.png
  - docs/images/sms_emoa_zdt1.png
  - docs/images/ibea_zdt1.png
  - docs/images/nsga3_dtlz2.png
  - docs/images/rastrigin.png
  - README.md
autonomous: true
requirements: []
tags: [visualization, docs, readme, images]

must_haves:
  truths:
    - "Six PNG files exist in docs/images/ and are committed to the repo (non-zero size, valid PNG header)"
    - "README.md's ### Visualization section has new sub-sections #### Multi-Objective Pareto Fronts and #### Single-Objective Fitness Progress linking all six images"
    - "All image links in README.md resolve (relative paths point to existing files)"
    - "Existing README content above and below the Visualization section is unchanged"
  artifacts:
    - path: "docs/images/nsga2_zdt1.png"
      provides: "NSGA-II ZDT1 Pareto front image"
      min_size_bytes: 1024
    - path: "docs/images/spea2_zdt1.png"
      provides: "SPEA2 ZDT1 Pareto front image"
      min_size_bytes: 1024
    - path: "docs/images/sms_emoa_zdt1.png"
      provides: "SMS-EMOA ZDT1 Pareto front image"
      min_size_bytes: 1024
    - path: "docs/images/ibea_zdt1.png"
      provides: "IBEA ZDT1 Pareto front image"
      min_size_bytes: 1024
    - path: "docs/images/nsga3_dtlz2.png"
      provides: "NSGA-III DTLZ2 three-panel Pareto front image"
      min_size_bytes: 1024
    - path: "docs/images/rastrigin.png"
      provides: "Rastrigin single-objective fitness progress line chart"
      min_size_bytes: 1024
    - path: "README.md"
      provides: "Multi-Objective Pareto Fronts and Single-Objective Fitness Progress sub-sections inside ### Visualization"
      contains: "docs/images/nsga2_zdt1.png"
  key_links:
    - from: "README.md ### Visualization"
      to: "docs/images/<six-png-files>"
      via: "Markdown image links"
      pattern: "!\\[.*\\]\\(docs/images/.*\\.png\\)"
---

<objective>
Generate six PNG images by running the Plan 02 examples with `--plot`, commit the images to `docs/images/`, and extend the existing `### Visualization` section of `README.md` with two new sub-sections that link the images.

Purpose: This is the "show, don't tell" closure of Phase 63 — turn the new library + example capability into rendered artifacts shipped with the repo. After this plan, a reader of `README.md` sees concrete Pareto fronts and a fitness curve directly in the project page on docs.rs / GitHub.

Output: Six committed PNG files in `docs/images/` plus an updated `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-CONTEXT.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-01-PLAN.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-02-PLAN.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit six PNG images by running each example with --plot</name>
  <files>docs/images/nsga2_zdt1.png, docs/images/spea2_zdt1.png, docs/images/sms_emoa_zdt1.png, docs/images/ibea_zdt1.png, docs/images/nsga3_dtlz2.png, docs/images/rastrigin.png</files>
  <read_first>
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-CONTEXT.md (D-07, D-08, D-10, D-11 — output paths)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-02-PLAN.md (acceptance criteria from Plan 02 listing each output path)
    - examples/nsga2_zdt1.rs (confirm --plot block landed in Plan 02 and writes the correct path)
    - examples/rastrigin.rs (confirm callback accumulator wired in Plan 02)
  </read_first>
  <action>
    From the repository root, run each example exactly as specified below. Each command must execute from /Users/luis/RustroverProjects/genetic-algorithms so the relative output path docs/images/<name>.png resolves correctly. Examples requiring the benchmarks feature run with both features; the rest run with only the visualization feature:

    1. cargo run --example nsga2_zdt1 --features visualization -- --plot
    2. cargo run --example spea2_zdt1 --features visualization -- --plot
    3. cargo run --example sms_emoa_zdt1 --features "visualization,benchmarks" -- --plot
    4. cargo run --example ibea_zdt1 --features "visualization,benchmarks" -- --plot
    5. cargo run --example nsga3_dtlz2 --features visualization -- --plot
    6. cargo run --example rastrigin --features visualization -- --plot

    After all six commands complete, verify the six files exist under docs/images/ with non-zero size and a valid PNG magic number (first 8 bytes equal to the PNG signature 89 50 4E 47 0D 0A 1A 0A).

    Stage and commit all six PNG files under docs/images/ with a single signed commit to the current working branch (feat/<issue>-visualization). Do not bypass GPG signing. Do not commit directly to the milestone branch.

    If any cargo run command exits non-zero or produces an empty/zero-byte PNG: stop, surface the failure, and request guidance. Do not retry indefinitely.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && ls -la docs/images/nsga2_zdt1.png docs/images/spea2_zdt1.png docs/images/sms_emoa_zdt1.png docs/images/ibea_zdt1.png docs/images/nsga3_dtlz2.png docs/images/rastrigin.png && for f in docs/images/nsga2_zdt1.png docs/images/spea2_zdt1.png docs/images/sms_emoa_zdt1.png docs/images/ibea_zdt1.png docs/images/nsga3_dtlz2.png docs/images/rastrigin.png; do head -c 8 "$f" | xxd | grep -q '8950 4e47 0d0a 1a0a' && echo "OK $f" || (echo "BAD PNG $f"; exit 1); done</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist at the listed paths with size >= 1024 bytes.
    - Each file begins with the 8-byte PNG signature (89 50 4E 47 0D 0A 1A 0A).
    - `git status docs/images/` shows the six files as committed (no untracked or modified).
    - `git log -1 --name-only` shows the commit added all six docs/images/*.png files.
    - The commit is signed (verified via `git log -1 --show-signature` showing a Good signature).
  </acceptance_criteria>
  <done>Six valid PNG files committed to docs/images/ on a signed commit, each produced from its own cargo-run-example execution.</done>
</task>

<task type="auto">
  <name>Task 2: Extend README.md ### Visualization section with two new image sub-sections</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — locate the existing `### Visualization` section so insertion is precise; preserve all other content)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-CONTEXT.md (D-12 — sub-section structure)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md (README extension table format)
  </read_first>
  <action>
    Locate the existing `### Visualization` heading in README.md. Inside that section (after any pre-existing prose about plot_fitness / plot_diversity, and BEFORE the next sibling heading of equal or higher level), append two new sub-sections in this exact order:

    First sub-section:

    #### Multi-Objective Pareto Fronts

    | Algorithm | Benchmark | Plot |
    |-----------|-----------|------|
    | NSGA-II | ZDT1 (2-obj) | ![NSGA-II ZDT1 Pareto front](docs/images/nsga2_zdt1.png) |
    | SPEA2 | ZDT1 (2-obj) | ![SPEA2 ZDT1 Pareto front](docs/images/spea2_zdt1.png) |
    | SMS-EMOA | ZDT1 (2-obj) | ![SMS-EMOA ZDT1 Pareto front](docs/images/sms_emoa_zdt1.png) |
    | IBEA | ZDT1 (2-obj) | ![IBEA ZDT1 Pareto front](docs/images/ibea_zdt1.png) |
    | NSGA-III | DTLZ2 (3-obj, three-panel) | ![NSGA-III DTLZ2 Pareto front](docs/images/nsga3_dtlz2.png) |

    Generate each plot by running the corresponding example with `--plot`, for example:

    ```bash
    cargo run --example nsga2_zdt1 --features visualization -- --plot
    cargo run --example sms_emoa_zdt1 --features "visualization,benchmarks" -- --plot
    ```

    Second sub-section:

    #### Single-Objective Fitness Progress

    ![Rastrigin fitness over generations](docs/images/rastrigin.png)

    Generated via:

    ```bash
    cargo run --example rastrigin --features visualization -- --plot
    ```

    Do NOT remove, reorder, or rewrite any existing content in the `### Visualization` section. Do NOT alter any other section of README.md. Do NOT change heading levels of surrounding sections.

    Commit README.md as a separate signed commit (or as part of Task 1's commit if the workflow prefers a single commit — defer to git status before committing; if Task 1 has already been committed, do this as a follow-up signed commit).
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && grep -c 'docs/images/nsga2_zdt1.png' README.md && grep -c 'docs/images/spea2_zdt1.png' README.md && grep -c 'docs/images/sms_emoa_zdt1.png' README.md && grep -c 'docs/images/ibea_zdt1.png' README.md && grep -c 'docs/images/nsga3_dtlz2.png' README.md && grep -c 'docs/images/rastrigin.png' README.md && grep -c '#### Multi-Objective Pareto Fronts' README.md && grep -c '#### Single-Objective Fitness Progress' README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'docs/images/nsga2_zdt1.png' README.md returns >= 1.
    - Same for spea2_zdt1.png, sms_emoa_zdt1.png, ibea_zdt1.png, nsga3_dtlz2.png, rastrigin.png — each returns >= 1.
    - grep -c '#### Multi-Objective Pareto Fronts' README.md returns 1.
    - grep -c '#### Single-Objective Fitness Progress' README.md returns 1.
    - The pre-existing `### Visualization` heading appears exactly once (count unchanged from before the edit).
    - Every image link in the diff resolves to a file that exists on disk: for each Markdown image link of the form `docs/images/<name>.png`, the corresponding file is present under docs/images/.
    - git log -1 shows a signed commit modifying README.md.
  </acceptance_criteria>
  <done>README.md ### Visualization section now contains both new sub-sections with the correct image links and runnable cargo commands; all links resolve; existing README content is byte-identical outside the inserted region.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/images/ checked into repo | Binary assets shipped with the library. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-07 | Tampering | Committed PNGs could be replaced by malicious content in future PRs | accept | Standard PR review applies; PNGs are produced deterministically from public examples and can be regenerated by reviewers |
| T-63-08 | Repudiation | Image provenance | mitigate | Each image is reproducible via the cargo command documented in README.md alongside it — reviewers verify by re-running the example |
</threat_model>

<verification>
- All six PNG files exist and pass PNG magic-number check
- README.md links resolve (grep + file existence)
- Existing README content above and below the Visualization section is untouched (diff inspection)
- Signed commits per CLAUDE.md branching policy
</verification>

<success_criteria>
- Phase 63 visual artifacts are in the repository.
- README.md surfaces the new visualization capability to first-time readers.
- A reader can reproduce every committed image by copying the cargo command directly under it.
</success_criteria>

<output>
Create `.planning/phases/63-visualization-pareto-front-plotting-example-images/63-03-SUMMARY.md` when done.
</output>
