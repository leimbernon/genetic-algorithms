---
phase: 80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is
plan: 03
type: execute
wave: 2
depends_on:
  - "80-01"
  - "80-02"
files_modified:
  - docs/engines.md
  - docs/index.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The engine overview table at the top of docs/engines.md has rows for CmaEngine, PsoEngine, and EdaEngine/EdaRealEngine"
    - "docs/engines.md has a short stub section per engine that links to the dedicated cma.md / pso.md / eda.md page"
    - "docs/index.md lists CMA-ES, PSO, and EDA under the Engines section, linking the three new pages"
    - "cargo doc --no-deps produces zero warnings for the whole phase"
  artifacts:
    - path: "docs/engines.md"
      provides: "Overview table rows + three stub sections for CMA/PSO/EDA"
      contains: "CmaEngine"
    - path: "docs/index.md"
      provides: "Three navigation links to cma.md, pso.md, eda.md"
      contains: "cma.md"
  key_links:
    - from: "docs/engines.md"
      to: "docs/cma.md"
      via: "stub section See Also link to the dedicated page"
      pattern: "cma\\.md"
    - from: "docs/index.md"
      to: "docs/pso.md"
      via: "Engines navigation entry"
      pattern: "pso\\.md"
---

<objective>
Integrate the three new engine pages into the documentation navigation: update the engine overview table at the top of `docs/engines.md` (D-04), add a short stub section per engine to `docs/engines.md` (D-02) following the existing NSGA-III inline-stub pattern, and add three navigation entries to `docs/index.md` (D-03). Finish with the phase-level rustdoc gate.

Purpose: Without these edits the new dedicated pages are unreachable from the docs index and the engine decision matrix. This completes GitHub issue #282.
Output: Edited docs/engines.md and docs/index.md; cargo doc --no-deps remains at zero warnings.

This plan depends on plans 80-01 and 80-02 so that the dedicated pages exist before they are linked (Pitfall 4 — avoid links to not-yet-created pages).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-CONTEXT.md
@.planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-RESEARCH.md
@docs/cma.md
@docs/pso.md
@docs/eda.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/engines.md overview table + add three stub sections</name>
  <files>docs/engines.md</files>
  <read_first>
    - docs/engines.md — read the overview table near the top (the "| Engine | Module | Output type | Use case |" table, currently 13 engine rows ending at IbeaGa) and the existing `## Nsga3Ga<U> — NSGA-III` stub section (the inline pattern: description paragraph, **Entry point:** line, ### When to Use, ### Configuration snippet, ### Key Parameters table, ### See Also). Replicate that stub structure.
    - docs/cma.md, docs/pso.md, docs/eda.md — to copy the When-to-Use bullets and key params so the stubs are consistent with the dedicated pages.
    - .planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-RESEARCH.md — "engines.md: Overview Table Additions" has the exact three rows to add and the header-count note.
  </read_first>
  <action>
    Two edits to docs/engines.md.

    EDIT 1 — overview table (D-04). Add exactly three rows to the engine overview table. Place them after the `GpGa<N>` row and before the `Nsga2Ga<U>` row (single-objective continuous/combinatorial engines grouped with GP, ahead of the multi-objective block). The rows, using the existing 4-column format:
      | `CmaEngine<U>` | `cma` | Best f64 vector | Continuous optimisation — self-adaptive covariance matrix |
      | `PsoEngine<U>` | `pso` | Best vector | Swarm-based continuous optimisation — few hyperparameters |
      | `EdaEngine<U>` / `EdaRealEngine<U>` | `eda` | Best individual | Probabilistic model-building — binary (Bernoulli) or continuous (Gaussian) |
    Also update the intro paragraph immediately above the table: it currently reads "The library provides twelve engines". Change "twelve" to "fifteen" so the count matches the table after adding three rows. (Use the exact replacement word "fifteen".)

    EDIT 2 — three stub sections (D-02). After the existing `## Nsga3Ga<U> — NSGA-III` stub section (or grouped with the other single-objective engine sections — place them after the GpGa section, before the Nsga2Ga section, to match table ordering), add three new stub sections, one each for CMA, PSO, EDA. Each stub MUST follow the existing NSGA-III stub structure exactly:
      - A `## ` heading: "## `CmaEngine<U>` — CMA-ES", "## `PsoEngine<U>` — Particle Swarm Optimization", "## `EdaEngine<U>` / `EdaRealEngine<U>` — Estimation of Distribution".
      - A one/two-line description paragraph.
      - An **Entry point:** line pointing at the source directory: `src/engines/cma/`, `src/engines/pso/`, `src/engines/eda/`.
      - A `### When to Use` bullet list (4 bullets: Problem type / Variable type / Key strength / Key weakness) — keep consistent with the dedicated page.
      - A `### Configuration` `rust,ignore` snippet showing key differentiating config only (NOT full boilerplate): CMA uses default_for_dim + with_sigma0 + RestartStrategy::Ipop; PSO uses default() + LinearDecay + Ring + c1/c2 = 1.49445; EDA shows EdaEngine (Bernoulli) and EdaRealEngine (Gaussian) one-liners. Use precise module paths, NOT glob. Remember CMA's cache builder is with_fitness_cache (no _size); never write RandomRange or VonNeumann anywhere.
      - A `### Key Parameters` table ("| Parameter | Type | Default | Description |") with 4-5 of the most important fields from the dedicated page.
      - A `### See Also` block linking the dedicated page: "[Full CMA-ES guide](cma.md)", "[Full PSO guide](pso.md)", "[Full EDA guide](eda.md)", plus the matching example link (../examples/cma_es_rastrigin.rs, ../examples/pso_rastrigin.rs, ../examples/eda_trap.rs).
    Keep each stub roughly 20-30 lines (short — the depth lives in the dedicated page, per D-02).

    Separate adjacent sections with the existing `---` horizontal-rule convention.
  </action>
  <verify>
    <automated>grep -q "| \`CmaEngine<U>\` | \`cma\`" docs/engines.md && grep -q "| \`PsoEngine<U>\` | \`pso\`" docs/engines.md && grep -q "EdaRealEngine<U>" docs/engines.md && grep -q "fifteen engines" docs/engines.md && grep -q "(cma.md)" docs/engines.md && grep -q "(pso.md)" docs/engines.md && grep -q "(eda.md)" docs/engines.md && ! grep -qE "RandomRange|VonNeumann" docs/engines.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The overview table contains a row whose first cell is `` `CmaEngine<U>` `` with module `` `cma` `` (grep the row).
    - The overview table contains a row for `` `PsoEngine<U>` `` with module `` `pso` `` and a row mentioning `EdaRealEngine<U>` with module `` `eda` ``.
    - The intro paragraph now says `fifteen engines` (not `twelve engines`) — `grep -c 'twelve engines' docs/engines.md` returns 0 and `grep -c 'fifteen engines' docs/engines.md` returns 1.
    - Three new `## ` stub headings exist for CMA, PSO, EDA, each containing a `### When to Use`, `### Configuration`, `### Key Parameters`, and `### See Also` subsection.
    - Each stub's See Also links its dedicated page: `grep -c '(cma.md)'`, `(pso.md)`, `(eda.md)` each >= 1.
    - File does NOT contain `RandomRange` or `VonNeumann` — `grep -cE 'RandomRange|VonNeumann' docs/engines.md` returns 0.
    - File does NOT contain a glob import in the new snippets — `grep -c 'use genetic_algorithms::\*' docs/engines.md` returns 0.
  </acceptance_criteria>
  <done>docs/engines.md overview table has three new engine rows, the intro count reads "fifteen engines", and three NSGA-III-style stub sections for CMA/PSO/EDA each link their dedicated page and example.</done>
</task>

<task type="auto">
  <name>Task 2: Add CMA/PSO/EDA navigation links to docs/index.md + final rustdoc gate</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md — read the "### Engines (12 total)" section (lines around 15-34): the heading with the count, and the bullet-list format like "- [NSGA-III](nsga3.md) — Reference-point based...". Replicate this entry format and decide the heading count update.
    - .planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-RESEARCH.md — "Pattern 3: index.md Engine Link Format" has the three exact new entries and the Open Question about the heading count.
  </read_first>
  <action>
    Two edits to docs/index.md.

    EDIT 1 — add three engine navigation entries under the "### Engines" section, following the existing bullet format `- [Name](page.md) — short description`. Place them adjacent to the other single-objective / dedicated-page entries (after the Genetic Programming entry and before the NSGA-II entry is a sensible spot; ordering is discretionary but keep single-objective engines grouped). Entries:
      - [CMA-ES](cma.md) — Covariance Matrix Adaptation Evolution Strategy for continuous optimization
      - [PSO](pso.md) — Particle Swarm Optimization with configurable inertia and topology
      - [EDA](eda.md) — Univariate Marginal Distribution Algorithm (Bernoulli and Gaussian models)

    EDIT 2 — update the section heading count. It currently reads "### Engines (12 total)". The accurate engine count after this phase is fifteen, so change it to "### Engines (15 total)" to keep it consistent with the engines.md "fifteen engines" wording. (Use exact replacement "### Engines (15 total)".)

    Do not touch any other part of index.md.
  </action>
  <verify>
    <automated>grep -q "(cma.md)" docs/index.md && grep -q "(pso.md)" docs/index.md && grep -q "(eda.md)" docs/index.md && grep -q "### Engines (15 total)" docs/index.md && echo "links OK" && test "$(cargo doc --no-deps 2>&1 | grep -c warning)" = "0" && echo "rustdoc OK"</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.md contains navigation entries `[CMA-ES](cma.md)`, `[PSO](pso.md)`, `[EDA](eda.md)` (grep each link target present).
    - The Engines heading reads `### Engines (15 total)` — `grep -c '### Engines (12 total)' docs/index.md` returns 0 and `grep -c '### Engines (15 total)' docs/index.md` returns 1.
    - `cargo doc --no-deps 2>&1 | grep -c warning` returns 0 (phase-level rustdoc gate; baseline was 0).
    - `cargo test --doc` passes (no broken doctests introduced; the new snippets are `rust,ignore` so they are not compiled, but this confirms nothing regressed).
  </acceptance_criteria>
  <done>docs/index.md lists the three new pages under a "### Engines (15 total)" heading, and cargo doc --no-deps reports zero warnings across the whole phase.</done>
</task>

</tasks>

<verification>
- `grep -c -E '\(cma.md\)|\(pso.md\)|\(eda.md\)' docs/engines.md` >= 3 and same for docs/index.md — both files link all three pages.
- `cargo doc --no-deps 2>&1 | grep -c warning` outputs 0 — phase-wide rustdoc gate (DOC-06).
- `grep -c 'twelve engines' docs/engines.md` outputs 0 — stale count removed.
- `grep -cE 'RandomRange|VonNeumann' docs/engines.md docs/index.md docs/cma.md docs/pso.md docs/eda.md` outputs 0 across all five files — no invented variants anywhere in this phase's docs.
</verification>

<success_criteria>
- docs/engines.md overview table has CmaEngine/PsoEngine/EdaEngine rows (D-04) and three NSGA-III-style stub sections each linking the dedicated page (D-02).
- docs/index.md links cma.md, pso.md, eda.md under the Engines section with an accurate "(15 total)" count (D-03).
- The engines.md intro and index.md heading counts are consistent ("fifteen engines" / "15 total").
- cargo doc --no-deps produces 0 warnings across the phase (DOC-06).
</success_criteria>

<output>
Create `.planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Artifact | Kind | Created by |
|----------|------|-----------|
| `docs/engines.md` (overview table rows + 3 stub sections) | Edited Markdown | Task 1 |
| `docs/index.md` (3 navigation links + count update) | Edited Markdown | Task 2 |
