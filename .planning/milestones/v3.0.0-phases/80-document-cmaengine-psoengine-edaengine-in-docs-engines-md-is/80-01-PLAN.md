---
phase: 80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cma.md
  - docs/pso.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A reader can open docs/cma.md and learn when to use CMA-ES, the sigma0 heuristic, and how to enable IPOP restarts"
    - "A reader can open docs/pso.md and learn the inertia strategies and topologies that actually exist in the crate"
    - "Every Rust type and builder method named in cma.md and pso.md exists in src/engines/cma and src/engines/pso"
    - "cargo doc --no-deps still produces zero warnings after these pages are added"
  artifacts:
    - path: "docs/cma.md"
      provides: "Dedicated CMA-ES guide page following the nsga3.md structure"
      min_lines: 90
      contains: "## See Also"
    - path: "docs/pso.md"
      provides: "Dedicated PSO guide page following the nsga3.md structure"
      min_lines: 90
      contains: "## See Also"
  key_links:
    - from: "docs/cma.md"
      to: "examples/cma_es_rastrigin.rs"
      via: "See Also link to the runnable example"
      pattern: "cma_es_rastrigin"
    - from: "docs/pso.md"
      to: "examples/pso_rastrigin.rs"
      via: "See Also link to the runnable example"
      pattern: "pso_rastrigin"
---

<objective>
Create two dedicated documentation pages — `docs/cma.md` (CMA-ES) and `docs/pso.md` (PSO) — following the exact section structure of `docs/nsga3.md` (D-01). Each page documents only the configuration fields, enum variants, and builder methods that exist in the crate source, with key differentiating snippets per D-05.

Purpose: CMA-ES and PSO are fully implemented engines with runnable examples but zero dedicated-page coverage in `docs/`. This closes part of GitHub issue #282.
Output: Two new Markdown files, each ~90-150 lines, with zero rustdoc warnings preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-CONTEXT.md
@.planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/cma.md (CMA-ES dedicated page)</name>
  <files>docs/cma.md</files>
  <read_first>
    - docs/nsga3.md — structural template: the exact section order (# Title, > tagline, ## Description, ## When to Use, ## Quick Reference with param tables, ## Complete Example, ## Configuration Tips, ## When to Choose This vs <Alternative>, ## References, ## See Also). Replicate this skeleton.
    - docs/moead.md — second tone/structure reference.
    - src/engines/cma/configuration.rs — source of truth for CmaConfiguration fields, defaults, and builder method names.
    - src/engines/cma/restart.rs — source of truth for RestartStrategy::Ipop and RestartStrategy::Bipop variants and their fields.
    - examples/cma_es_rastrigin.rs — the runnable example linked in See Also.
    - .planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-RESEARCH.md — "CMA-ES" section has the verified field table and the prescribed snippet.
  </read_first>
  <action>
    Create docs/cma.md following the docs/nsga3.md section skeleton in this order: title line "# CMA-ES", a one-line blockquote tagline, "## Description", "## When to Use", "## Quick Reference", "## Complete Example", "## Configuration Tips", "## When to Choose This vs PSO", "## References", "## See Also".

    Module path is `genetic_algorithms::cma`. Public exports to reference: CmaEngine, CmaConfiguration, CmaResult, RestartStrategy.

    Description: explain CMA-ES adapts a covariance matrix to learn the search-distribution shape; uses Jacobi eigendecomposition (no LAPACK, WASM-compatible) per the source. Continuous real-valued optimization.

    When to Use bullets (Problem type / Variable type / Key strength / Key weakness): continuous real-valued problems, RealGene-bound genotype, self-adapting covariance handles non-separable/correlated landscapes, weakness is O(n^2) covariance scaling limiting it to roughly 40 dimensions.

    Quick Reference parameter table using the header row "| Field | Type | Default | Description |". Include exactly these CmaConfiguration fields with their verified defaults from source: sigma0 (f64, 0.3), population_size (usize, 0 = auto, auto-computes 4 + floor(3*ln(n))), max_generations (usize, 1000), problem_solving (ProblemSolving, Minimization), fitness_target (Option<f64>, None), cc/cs/c1/cmu (Option<f64>, None = Hansen auto-formula), restart_strategy (Option<RestartStrategy>, None), fitness_cache_size (Option<usize>, None). Add a short note that the cmu constraint is c1 + cmu <= 1.

    Document RestartStrategy with both variants and their real fields: Ipop has population_scale: f64, stagnation_threshold: usize, max_restarts: usize; Bipop has population_scale: f64, small_population_size: usize, stagnation_threshold: usize, max_restarts: usize.

    Complete Example: a `rust,ignore` fenced block using precise module paths (NOT `use genetic_algorithms::*`). Use the helper constructor CmaConfiguration::default_for_dim(n), then the verified builder chain. Per D-05 show: the sigma0 heuristic via .with_sigma0(...), lambda = 0 left as auto (do not call a population-size override, or note the default_for_dim auto-sizing), and a RestartStrategy::Ipop { population_scale: 2.0, stagnation_threshold: 50, max_restarts: 9 } passed to .with_restart_strategy(...). IMPORTANT: the CMA fitness-cache builder is named `with_fitness_cache(size)` — do NOT write `with_fitness_cache_size` for CMA (that is the PSO/EDA spelling).

    Configuration Tips bullets: document the sigma0 heuristic as "typically 1/5 to 1/3 of the expected search range per dimension" (Pitfall 3 — do NOT state 1/3 as an exact formula); note population_size = 0 auto-computes Hansen's default; note IPOP restarts help on multimodal landscapes.

    "## When to Choose This vs PSO" comparison table (Factor / CMA-ES / PSO rows): mechanism (covariance adaptation vs swarm velocity), hyperparameters (more internal but mostly auto vs few), dimensionality (<=~40 due to O(n^2) vs higher), landscape (non-separable/correlated vs smooth/unimodal fast).

    References: cite Hansen & Ostermeier (2001) for CMA-ES.

    See Also bullets: link to [Engines Overview](engines.md) and the engines.md CMA stub anchor, link the runnable example [Example: cma_es_rastrigin](../examples/cma_es_rastrigin.rs), and link [docs.rs/genetic_algorithms::cma](https://docs.rs/genetic_algorithms/latest/genetic_algorithms/cma/index.html). Also add a [PSO](pso.md) cross-link.

    Do NOT mention any CMA field, variant, or builder method that is not present in src/engines/cma. Target 90-150 lines.
  </action>
  <verify>
    <automated>test -f docs/cma.md && grep -q "## See Also" docs/cma.md && grep -q "default_for_dim" docs/cma.md && grep -q "RestartStrategy::Ipop" docs/cma.md && ! grep -q "with_fitness_cache_size" docs/cma.md && ! grep -q "use genetic_algorithms::\*" docs/cma.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/cma.md` exists and is at least 90 lines.
    - File contains the sections "## Description", "## When to Use", "## Quick Reference", "## Complete Example", "## See Also" (grep each heading literal).
    - Parameter table uses the header row `| Field | Type | Default | Description |`.
    - File references `default_for_dim` and `RestartStrategy::Ipop` (both grep-present).
    - File does NOT contain the literal `with_fitness_cache_size` (CMA uses `with_fitness_cache`) — `grep -c 'with_fitness_cache_size' docs/cma.md` returns 0.
    - File does NOT contain a glob import — `grep -c 'use genetic_algorithms::\*' docs/cma.md` returns 0.
    - File does NOT mention `RandomRange` or `VonNeumann` — `grep -cE 'RandomRange|VonNeumann' docs/cma.md` returns 0.
    - See Also section links `../examples/cma_es_rastrigin.rs` (grep `cma_es_rastrigin`).
  </acceptance_criteria>
  <done>docs/cma.md exists, follows the nsga3.md skeleton, documents only real CMA API, links the runnable example, and uses the correct `with_fitness_cache` builder spelling.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/pso.md (PSO dedicated page)</name>
  <files>docs/pso.md</files>
  <read_first>
    - docs/nsga3.md — structural template (same section skeleton as Task 1).
    - src/engines/pso/configuration.rs — source of truth for PsoConfiguration fields/defaults, the PsoInertia enum (only Constant and LinearDecay), the PsoTopology enum (only Global and Ring), and the builder method names.
    - examples/pso_rastrigin.rs — the runnable example linked in See Also.
    - .planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-RESEARCH.md — "PSO" section: verified field table, the IMPORTANT CORRECTION about non-existent variants, and the D-05 prescribed snippet.
  </read_first>
  <action>
    Create docs/pso.md following the same docs/nsga3.md section skeleton: "# PSO", blockquote tagline, "## Description", "## When to Use", "## Quick Reference", "## Complete Example", "## Configuration Tips", "## When to Choose This vs CMA-ES", "## References", "## See Also".

    Module path is `genetic_algorithms::pso`. Public exports to reference: PsoEngine, PsoConfiguration, PsoInertia, PsoTopology.

    CRITICAL — only document variants that exist in source (Pitfall 1): PsoInertia has exactly two variants — Constant(f64) and LinearDecay { w_start: f64, w_end: f64 }. PsoTopology has exactly two variants — Global and Ring { neighborhood_size: usize }. Do NOT write `RandomRange`, do NOT write `VonNeumann` — these do not exist and any snippet using them fails to compile.

    Description: PSO evolves a swarm of particles; each particle updates velocity from inertia plus pull toward its personal best (cognitive) and its neighborhood best (social). Continuous real-valued optimization with few hyperparameters.

    When to Use bullets (Problem type / Variable type / Key strength / Key weakness): continuous real-valued problems where gradients are unavailable, RealGene-bound genotype, key strength is few hyperparameters and fast convergence on smooth landscapes (works to ~100 dims without covariance overhead), key weakness is premature convergence on multimodal problems with Global topology.

    Quick Reference parameter table "| Field | Type | Default | Description |" with the verified PsoConfiguration fields and defaults: population_size (usize, 30), max_generations (usize, 1000), problem_solving (ProblemSolving, Minimization), fitness_target (Option<f64>, None), inertia (PsoInertia, LinearDecay { w_start: 0.9, w_end: 0.4 }), c1 (f64, 2.0 — cognitive), c2 (f64, 2.0 — social), topology (PsoTopology, Global), fitness_cache_size (Option<usize>, None).

    Add a short "### Inertia strategies" and "### Topologies" subsection (bullet or mini-table) describing Constant(w) (fixed weight, common 0.729 with Clerc constriction c1=c2=1.49445) and LinearDecay { w_start, w_end } (decreasing, Shi & Eberhart 1998 default 0.9 -> 0.4); and Global (gbest, fast but premature-convergence risk) vs Ring { neighborhood_size } (lbest, slower but better exploration; for neighborhood_size = 2 the neighborhood is {i-1, i, i+1}).

    Complete Example: a `rust,ignore` fenced block with precise module paths (NOT glob). Per D-05 show the typical recommended config: PsoConfiguration::default() then .with_inertia(PsoInertia::LinearDecay { w_start: 0.9, w_end: 0.4 }), .with_topology(PsoTopology::Ring { neighborhood_size: 2 }), .with_c1(1.49445), .with_c2(1.49445). Builder method names are with_population_size, with_max_generations, with_problem_solving, with_inertia, with_c1, with_c2, with_topology, with_fitness_cache_size (PSO uses the `_size` suffix).

    Configuration Tips bullets: Global topology converges fastest on unimodal/smooth landscapes; Ring topology trades speed for multimodal exploration; Clerc's constriction (w=0.729, c1=c2=1.49445 with Constant inertia) is a stable alternative to LinearDecay.

    "## When to Choose This vs CMA-ES" comparison table (Factor / PSO / CMA-ES rows): hyperparameters (few vs mostly-auto internal set), dimensionality (higher, no covariance overhead vs <=~40), landscape correlation (no correlation modeling vs adapts to correlations), convergence (fast on smooth vs robust on rugged).

    References: cite Kennedy & Eberhart (1995) and Shi & Eberhart (1998).

    See Also bullets: [Engines Overview](engines.md) + the engines.md PSO stub anchor, [Example: pso_rastrigin](../examples/pso_rastrigin.rs), [docs.rs/genetic_algorithms::pso](https://docs.rs/genetic_algorithms/latest/genetic_algorithms/pso/index.html), and a [CMA-ES](cma.md) cross-link.

    Target 90-150 lines.
  </action>
  <verify>
    <automated>test -f docs/pso.md && grep -q "## See Also" docs/pso.md && grep -q "PsoInertia::LinearDecay" docs/pso.md && grep -q "PsoTopology::Ring" docs/pso.md && ! grep -qE "RandomRange|VonNeumann" docs/pso.md && ! grep -q "use genetic_algorithms::\*" docs/pso.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/pso.md` exists and is at least 90 lines.
    - File contains "## Description", "## When to Use", "## Quick Reference", "## Complete Example", "## See Also" (grep each).
    - Parameter table uses header row `| Field | Type | Default | Description |`.
    - File references `PsoInertia::LinearDecay` and `PsoTopology::Ring` (both grep-present).
    - File does NOT contain `RandomRange` or `VonNeumann` — `grep -cE 'RandomRange|VonNeumann' docs/pso.md` returns 0 (Pitfall 1).
    - File does NOT contain a glob import — `grep -c 'use genetic_algorithms::\*' docs/pso.md` returns 0.
    - See Also links `../examples/pso_rastrigin.rs` (grep `pso_rastrigin`).
  </acceptance_criteria>
  <done>docs/pso.md exists, follows the nsga3.md skeleton, documents only the real two-variant PsoInertia and two-variant PsoTopology enums, and links the runnable example.</done>
</task>

</tasks>

<verification>
- `test -f docs/cma.md && test -f docs/pso.md` — both pages exist.
- `cargo doc --no-deps 2>&1 | grep -c warning` outputs 0 (rustdoc warnings unchanged; baseline confirmed 0).
- `grep -cE 'RandomRange|VonNeumann' docs/cma.md docs/pso.md` outputs 0 for both files — no invented API variants.
- Each page contains a `## See Also` section linking its runnable example.
</verification>

<success_criteria>
- docs/cma.md and docs/pso.md exist, each 90-150 lines, following the docs/nsga3.md section skeleton (D-01).
- Both pages document only configuration fields, enum variants, and builder methods present in src/engines/cma and src/engines/pso.
- CMA snippet uses default_for_dim, sigma0 heuristic, auto lambda, and RestartStrategy::Ipop (D-05); CMA cache builder spelled with_fitness_cache.
- PSO snippet uses LinearDecay + Ring + c1/c2 = 1.49445 (D-05); no RandomRange/VonNeumann.
- cargo doc --no-deps still produces 0 warnings.
</success_criteria>

<output>
Create `.planning/phases/80-document-cmaengine-psoengine-edaengine-in-docs-engines-md-is/80-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Artifact | Kind | Created by |
|----------|------|-----------|
| `docs/cma.md` | New Markdown guide page (CMA-ES) | Task 1 |
| `docs/pso.md` | New Markdown guide page (PSO) | Task 2 |
