---
phase: 68-build-perf-m2-dependency-hygiene
plan: 02
type: execute
wave: 1
depends_on:
  - 68-01
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/observe/observer/mod.rs
  - src/observe/observer/log.rs
  - src/**/*.rs
  - .github/workflows/feature-matrix.yml
  - CHANGELOG.md
  - README.md
  - docs/getting-started.md
  - .planning/intel/feature-flags.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-02: log dep in Cargo.toml retains features = [\"std\", \"serde\", \"kv_unstable\"] unchanged — only optional = true is added; no call sites in LogObserver are reformatted"
    - "D-03: LogObserver is publicly available only when the `logging` feature is enabled — both src/observe/observer/mod.rs:551-552 and src/lib.rs:357 gated with #[cfg(feature = \"logging\")]"
    - "D-04: internal macro family (log_info!/log_debug!/log_trace!/log_warn!/log_error!) in src/lib.rs handles all 109+ call sites — zero inline #[cfg(feature = \"logging\")] annotations at individual call sites"
    - "`cargo build --no-default-features` exits 0 — no unconditional `log::` reference or `use log::` import remains in src/"
    - "`cargo build` (default features) exits 0 and behaves identically to before Plan 68-02 — log events still flow through the `log` crate when the `logging` feature is on"
    - "`cargo check --features logging` and `cargo check --no-default-features` both exit 0 in CI"
    - "`cargo check --target wasm32-unknown-unknown` exits 0 with default features"
    - "Feature-matrix CI includes a `no-default-features` row AND a `logging-explicit` row, both green"
    - "Every commit body contains `Revert plan:` per BUILD-PERF.md non-negotiable #5"
  artifacts:
    - path: "Cargo.toml"
      provides: "logging feature gating dep:log; default=[\"logging\"]; log marked optional=true with kv_unstable preserved"
      contains: "logging = [\"dep:log\"]"
    - path: "src/lib.rs"
      provides: "macro family log_info!/log_debug!/log_trace!/log_warn!/log_error! with cfg-gated bodies"
      contains: "macro_rules! log_info"
    - path: "src/observe/observer/log.rs"
      provides: "LogObserver implementation gated behind #[cfg(feature = \"logging\")]"
    - path: ".github/workflows/feature-matrix.yml"
      provides: "Matrix entries for --no-default-features and --no-default-features --features logging"
      contains: "no-default-features"
    - path: ".planning/intel/feature-flags.md"
      provides: "AI-readable note on the feature-flag philosophy (logging is default-on; dep:log; macro family canonical)"
  key_links:
    - from: "src/lib.rs macro family"
      to: "log::info!/debug!/trace!/warn!/error!"
      via: "macro_rules! delegating to ::log:: when feature=logging, expanding to () otherwise"
      pattern: "macro_rules! log_(info|debug|trace|warn|error)"
    - from: "every src/**/*.rs call site"
      to: "crate::log_*! macros"
      via: "search-and-replace of log::(info|debug|trace|warn|error)! and removal of `use log::{...}` imports"
      pattern: "crate::log_(info|debug|trace|warn|error)!"
    - from: "src/observe/observer/mod.rs"
      to: "LogObserver export"
      via: "#[cfg(feature = \"logging\")] on both `mod log;` and `pub use log::LogObserver;`"
      pattern: "#\\[cfg\\(feature = \"logging\"\\)\\]"
---

<objective>
Add a default-on `logging` Cargo feature that gates the `log` crate dependency, introduce a five-macro internal family in `src/lib.rs` (`log_info!`, `log_debug!`, `log_trace!`, `log_warn!`, `log_error!`) that delegate to `::log::*` when on and expand to `()` when off, convert every `log::*` call site and `use log::*` import across `src/` to use the macros, gate `LogObserver` behind the feature, extend feature-matrix CI to prove both states compile, and document the new flag across user/contributor/AI audiences.

Purpose: Lets `default-features = false` users (embedded, ultra-lean wasm) shed the `log` crate entirely while keeping default behaviour byte-identical.

Output: Cargo.toml feature additions + macro family in src/lib.rs + ~63 files of call-site/import conversion + cfg gates on LogObserver + CI matrix extension + 5 doc updates, committed atomically with `Revert plan:` lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/68-build-perf-m2-dependency-hygiene/68-CONTEXT.md
@.planning/phases/68-build-perf-m2-dependency-hygiene/68-RESEARCH.md
@.planning/phases/68-build-perf-m2-dependency-hygiene/68-VALIDATION.md
@.planning/phases/68-build-perf-m2-dependency-hygiene/68-01-PLAN.md
@CLAUDE.md
@Cargo.toml
@src/lib.rs
@src/observe/observer/mod.rs
@src/observe/observer/log.rs
@.github/workflows/feature-matrix.yml

<interfaces>
Per RESEARCH.md §Pattern 1, §Pattern 3 and CONTEXT.md §Files to change (Plan 68-02):

- `Cargo.toml` current line 43-ish: `log = { version = "0.4.22", features = ["std", "serde", "kv_unstable"] }`. Per D-02, the feature list MUST be preserved verbatim; only `optional = true` is added.
- `src/lib.rs` line ~357 currently re-exports `LogObserver`: `pub use observer::LogObserver;` — needs `#[cfg(feature = "logging")]` above it.
- `src/observe/observer/mod.rs` lines 551-552 currently contain `mod log;` and `pub use log::LogObserver;` — both lines need `#[cfg(feature = "logging")]` directly above them.
- `src/observe/observer/log.rs` — entire file gated behind `#[cfg(feature = "logging")]` at the `mod log;` declaration in mod.rs; file body remains untouched (D-02 says the `kv_unstable` KV syntax inside stays as-is).
- Macro family signature (D-04): `crate::log_info!(target = "...", key=value; "fmt {}", arg)` — pass-through `($($arg:tt)*)` matcher delegating to `::log::info!($($arg)*)` or expanding to `()`.
- `.github/workflows/feature-matrix.yml` — existing matrix rows include `default` and `all-features`. Two new rows required: name=`no-default-features` and name=`logging-explicit`.

The `log::*` call sites to convert number 51+ and the `use log::*` imports number ~52 — see `grep -rn "log::\(info\|debug\|trace\|warn\|error\)!" src/` and `grep -rln "^use log::" src/`. Plan 68-02 owns ALL of them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logging feature in Cargo.toml + define internal macro family in src/lib.rs + gate LogObserver re-exports</name>
  <files>Cargo.toml, src/lib.rs, src/observe/observer/mod.rs</files>
  <read_first>
    - Cargo.toml (current `[features]` block — verify `default = [...]` exists and the existing `serde = ["dep:serde", "dep:serde_json"]` pattern is the canonical optional-dep style)
    - src/lib.rs (top of file for macro placement; line ~357 for `pub use observer::LogObserver;`)
    - src/observe/observer/mod.rs (lines 551-552 with `mod log;` and `pub use log::LogObserver;`; observe the pattern already used for `observer-tracing` and `observer-metrics` cfg gates around lines 554-568)
    - .planning/phases/68-build-perf-m2-dependency-hygiene/68-RESEARCH.md §Code Examples (macro family snippet and Cargo.toml feature snippet)
  </read_first>
  <action>
    Per D-02, D-03, D-04 in 68-CONTEXT.md:

    (a) Cargo.toml `[features]` section — add (or augment) `default = ["logging", ...other-existing-defaults...]` so `logging` is in the default set alongside any flags already present (do not drop existing defaults). Add `logging = ["dep:log"]`. Add the `optional = true` flag to the existing log dep line WITHOUT touching its feature list: change `log = { version = "0.4.22", features = ["std", "serde", "kv_unstable"] }` to `log = { version = "0.4.22", features = ["std", "serde", "kv_unstable"], optional = true }`. Per Pitfall 3 in RESEARCH.md, do NOT collapse the feature list to bare `optional = true`.

    (b) src/lib.rs macro family — at the very top of the file (before the first `mod` declaration, but after the crate-level doc comment), insert the five `macro_rules!` definitions exactly as specified in RESEARCH.md §Code Examples. Each macro has two arms: one under `#[cfg(feature = "logging")]` delegating to `::log::<level>!($($arg)*)`, one under `#[cfg(not(feature = "logging"))]` expanding to `()`. Define them at crate root so intra-crate access via `crate::log_info!()` etc. works on Rust 2018+ without `#[macro_export]` (per RESEARCH.md A2). All five names: `log_info`, `log_debug`, `log_trace`, `log_warn`, `log_error`.

    (c) src/lib.rs line ~357 — add `#[cfg(feature = "logging")]` on the line directly above `pub use observer::LogObserver;`. The cfg attribute MUST be on its own line immediately preceding the `pub use`.

    (d) src/observe/observer/mod.rs lines 551-552 — add `#[cfg(feature = "logging")]` on its own line above `mod log;` AND a separate `#[cfg(feature = "logging")]` on its own line above `pub use log::LogObserver;`. Mirror the exact style already used at lines 554-568 for `observer-tracing` and `observer-metrics`.

    (e) Verify with `cargo check --features logging` and `cargo check --no-default-features`. Both must succeed before declaring the task done. Note: this task does NOT yet convert call sites, so `cargo check --no-default-features` will FAIL on `log::info!` references until Task 2 lands. Therefore the verify command for THIS task is only `cargo check --features logging` (i.e., default features). The `--no-default-features` build is gated by Task 2.
  </action>
  <verify>
    <automated>cargo check --features logging 2>&1 | tee /tmp/68-02-task1-features.log && cargo check 2>&1 | tee /tmp/68-02-task1-default.log && grep -c "^logging = \[\"dep:log\"\]" Cargo.toml && grep -c "optional = true" Cargo.toml && grep -c "macro_rules! log_info" src/lib.rs && grep -c "macro_rules! log_debug" src/lib.rs && grep -c "macro_rules! log_trace" src/lib.rs && grep -c "macro_rules! log_warn" src/lib.rs && grep -c "macro_rules! log_error" src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --features logging` exits 0
    - `cargo check` (default features) exits 0
    - `Cargo.toml` contains the literal line `logging = ["dep:log"]`
    - `Cargo.toml` `default = [...]` array contains the substring `"logging"`
    - `Cargo.toml` `log = { ... }` line contains BOTH `features = ["std", "serde", "kv_unstable"]` AND `optional = true`
    - `src/lib.rs` contains all five definitions: `macro_rules! log_info`, `macro_rules! log_debug`, `macro_rules! log_trace`, `macro_rules! log_warn`, `macro_rules! log_error`
    - `src/lib.rs` line near 357: the line immediately preceding `pub use observer::LogObserver;` is exactly `#[cfg(feature = "logging")]`
    - `src/observe/observer/mod.rs` near lines 551-552: both `mod log;` and `pub use log::LogObserver;` are preceded by `#[cfg(feature = "logging")]` on their own lines
    - Task 2 must run before `cargo check --no-default-features` can pass — this is expected and documented
  </acceptance_criteria>
  <done>
    Feature `logging` declared, `log` marked optional with feature list intact, macro family in place at crate root, LogObserver re-exports gated. Default build still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert every src/**/*.rs log::* call site and `use log::` import to the internal macro family</name>
  <files>src/**/*.rs (excluding src/observe/observer/log.rs which stays as-is per D-02)</files>
  <read_first>
    - src/lib.rs (the just-added macro family — to confirm exact macro names and `crate::` access path)
    - A representative call site file, e.g., the output of `grep -rln "^use log::" src/ | head -3` — pick one to see the current import style and call patterns
    - src/observe/observer/log.rs (DO NOT modify — it stays gated as a unit, retaining `use log::*` and `log::info!` calls inside; only the parent `mod log;` declaration in `mod.rs` is gated)
    - .planning/phases/68-build-perf-m2-dependency-hygiene/68-RESEARCH.md §Pitfall 2 (bare imports must be removed alongside call-site conversion) and §Pattern 1 (call site Before/After)
  </read_first>
  <action>
    Per D-04 and Pitfall 1/2 in RESEARCH.md: convert every unconditional `log::*` reference and matching `use log::*` import in `src/` (with the single exception of `src/observe/observer/log.rs`, the LogObserver file itself, which stays untouched because the entire file is now gated via its parent module per Task 1.d).

    Mechanical sweep procedure (apply to every file under `src/` EXCEPT `src/observe/observer/log.rs`):

    1. For each file containing `log::info!`, `log::debug!`, `log::trace!`, `log::warn!`, or `log::error!`: replace each call site so `log::info!(...)` becomes `crate::log_info!(...)`, `log::debug!(...)` becomes `crate::log_debug!(...)`, and so on for trace/warn/error. The macro argument list `(...)` is passed through unchanged — including the `kv_unstable` KV syntax (`target="ga_events", method="run"; "fmt {}", arg`).

    2. For each file containing a top-level `use log::{...}` or `use log::*;` import: if the items imported are exclusively macros (`info`, `debug`, `trace`, `warn`, `error`), DELETE the `use log::` line entirely. If the file also imports non-macro items from `log` (e.g., `LevelFilter`, `Log`, `Record`), keep ONLY those non-macro items in the `use log::{...}` list. Bare-macro callers like `info!(...)` then become `crate::log_info!(...)` per step 1.

    3. For any bare macro call (`info!(...)`, `debug!(...)`, etc.) that exists because of a `use log::{info, debug, ...}` import, convert to `crate::log_<level>!(...)` AND remove the corresponding name from the `use log::{...}` import in step 2.

    4. Do NOT touch `src/observe/observer/log.rs`. Its `use log::*` imports and `log::info!` calls remain — that whole file is feature-gated at the `mod log;` declaration in `mod.rs`.

    5. Do NOT add `#[cfg(feature = "logging")]` at any call site. The macro family already encodes the cfg gate; per D-04 the whole point is to avoid 109 inline cfgs.

    After the sweep, run BOTH compile checks:
      - `cargo check` (default features, logging on) — must pass; macros expand to real `log::` calls
      - `cargo check --no-default-features` — must pass; macros expand to `()`; no residual `log::` or `use log::` reference outside `src/observe/observer/log.rs`

    Then run `cargo check --target wasm32-unknown-unknown` (default features) per CLAUDE.md mandatory WASM rule. Must pass.

    Then run `cargo test` and `cargo test --no-default-features`. Both must pass. CC-3 golden tests must be byte-identical (the macro expansion when `logging` is on is `::log::info!($($arg)*)` — identical token tree to the original call).
  </action>
  <verify>
    <automated>cargo check 2>&1 | tee /tmp/68-02-task2-default.log && cargo check --no-default-features 2>&1 | tee /tmp/68-02-task2-nodef.log && cargo check --target wasm32-unknown-unknown 2>&1 | tee /tmp/68-02-task2-wasm.log && cargo test 2>&1 | tail -5 | tee /tmp/68-02-task2-test.log && cargo test --no-default-features 2>&1 | tail -5 | tee -a /tmp/68-02-task2-test.log && (grep -rn "log::\(info\|debug\|trace\|warn\|error\)!" src/ | grep -v "^src/observe/observer/log.rs" | grep -v "^[^:]*:[[:space:]]*//"; echo "---"; grep -rn "^use log::" src/ | grep -v "^src/observe/observer/log.rs") | tee /tmp/68-02-task2-residue.txt | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` exits 0
    - `cargo check --no-default-features` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - `cargo test` exits 0
    - `cargo test --no-default-features` exits 0
    - `grep -rn "log::\(info\|debug\|trace\|warn\|error\)!" src/ | grep -v "^src/observe/observer/log.rs"` filtered through `grep -v '^[^:]*:[[:space:]]*//'` returns zero lines (no residual unconditional `log::*!` call outside the gated LogObserver file)
    - `grep -rn "^use log::" src/ | grep -v "^src/observe/observer/log.rs"` returns zero lines (no residual `use log::` import outside the gated LogObserver file)
    - `grep -rn "crate::log_\(info\|debug\|trace\|warn\|error\)!" src/ | wc -l` is greater than or equal to 51 (≥ count of converted call sites; allows for splits)
    - CC-3 golden tests pass byte-identically (`cargo test --test golden_tests --release` exits 0 with no diff)
  </acceptance_criteria>
  <done>
    Every src/ file (except the gated LogObserver) routes log emissions through the internal macro family; both feature states and WASM compile and test green; CC-3 golden output unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend feature-matrix CI + write CHANGELOG/README/docs/intel updates + commit with Revert plan</name>
  <files>.github/workflows/feature-matrix.yml, CHANGELOG.md, README.md, docs/getting-started.md, src/lib.rs (Feature Flags table only), .planning/intel/feature-flags.md</files>
  <read_first>
    - .github/workflows/feature-matrix.yml (current matrix rows; identify the `default` and `all-features` entries and the `cmd:` field shape)
    - src/lib.rs (the Feature Flags table in the crate-level doc comment — usually near the top of lib.rs)
    - README.md (the Features table — search for `## Features` or a markdown table listing existing flags like `serde`, `visualization`, `benchmarks`)
    - CHANGELOG.md v3.0.0 section (the same one Plan 68-01 already amended — append, don't overwrite)
    - docs/getting-started.md (already updated by Plan 68-01 for env_logger::init; this task only adds a brief note about `default-features = false` for ultra-lean builds)
    - Existing files under .planning/intel/ (e.g., logger-history.md from Plan 68-01, build-profile.md from Phase 67) to mirror style
    - .planning/v3.0.0-BUILD-PERF.md §Action #2 docs list and §Non-negotiable guarantee #5 (`Revert plan:` requirement)
  </read_first>
  <action>
    (a) `.github/workflows/feature-matrix.yml` — add two new matrix rows immediately after the existing `default` / `all-features` rows. Use the YAML structure already present in the file. The two new rows are:
      - `name: "no-default-features"` with `features: "--no-default-features"` and `cmd: "cargo test --quiet --no-default-features"` (or whichever shape the file uses — mirror it exactly)
      - `name: "logging-explicit"` with `features: "--no-default-features --features logging"` and `cmd: "cargo test --quiet --no-default-features --features logging"`
    Do NOT remove or reorder existing rows. After saving, run `git diff .github/workflows/feature-matrix.yml` and confirm only additions.

    (b) `CHANGELOG.md` v3.0.0 `Added` bucket — append: "`logging` feature (default-on). Disable via `default-features = false` to shed the `log` crate entirely for ultra-lean / embedded / wasm-only builds. The library uses an internal macro family (`crate::log_info!` etc.) that expands to `::log::*` when enabled and to `()` when disabled."

    (c) `README.md` Features table — add a new row for `logging` matching the existing column layout (flag name, default state, purpose). Example purpose text: "Enables `log` crate emission of events (default-on). Disable to shed `log` for minimal binary size."

    (d) `docs/getting-started.md` — add a brief subsection (1-2 paragraphs) titled "Disabling logging for ultra-lean builds" explaining `default-features = false` and what it removes. Place it after the Quick Start.

    (e) `src/lib.rs` Feature Flags table (inside the `//!` crate doc comment, NOT the macro family code added in Task 1) — add a row for `logging` mirroring existing entries (`serde`, `observer-tracing`, `observer-metrics`, `visualization`, `benchmarks`).

    (f) Create `.planning/intel/feature-flags.md` with these sections in order: `# Feature Flags Philosophy`, `## Date: 2026-06-15`, `## Default set` (list current defaults including `logging`), `## Why `logging` is default-on but dep-optional` (zero-regression-on-default; users opt out for lean builds; macro family is the canonical pattern), `## Canonical pattern for new optional deps` (use `dep:foo` in Cargo, `#[cfg(feature = "...")]` at the module/use boundary, prefer macro families over per-call-site cfgs when call sites are numerous), `## What MUST NOT happen` (do not add a feature that is default-on without proving zero-regression in CC-3; do not gate items that are part of the stable public API without a MIGRATION entry), `## How to verify a new flag` (feature-matrix CI + cargo check --no-default-features + cargo check --features <flag> + WASM check + CC-3 golden).

    (g) Commit everything from Plan 68-02 (Task 1 + Task 2 + Task 3) as one or more commits. Every commit message body MUST contain a `Revert plan:` line. Suggested wording for the umbrella commit: `Revert plan: revert the macro family in src/lib.rs, restore unconditional log:: call sites and use log::{...} imports across src/, remove the logging feature and the optional flag from Cargo.toml, drop the new feature-matrix rows, and revert the doc updates.` Use signed commits (`git commit -S`) per CLAUDE.md. Push to the feat branch targeting the milestone branch (never main).
  </action>
  <verify>
    <automated>test -f .planning/intel/feature-flags.md && grep -c "logging" .planning/intel/feature-flags.md && grep -c "no-default-features" .github/workflows/feature-matrix.yml && grep -c "logging-explicit" .github/workflows/feature-matrix.yml && grep -c "logging" CHANGELOG.md && grep -c "logging" README.md && git log -n 5 --format=%B | grep -c "Revert plan:" && cargo doc --no-deps 2>&1 | grep -i "^warning" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/feature-matrix.yml` contains both substrings `no-default-features` and `logging-explicit` and the existing rows remain present (diff is purely additive)
    - `CHANGELOG.md` v3.0.0 `Added` bucket contains substring `logging` feature reference
    - `README.md` Features table contains a `logging` row
    - `docs/getting-started.md` contains substring `default-features = false`
    - `src/lib.rs` crate-doc Feature Flags table contains a `logging` row (`grep -c "logging" src/lib.rs` ≥ 6 — five macro defs + one table row)
    - `.planning/intel/feature-flags.md` exists and contains `2026-06-15` and the exact heading `## Canonical pattern for new optional deps`
    - `git log -n 5 --format=%B` shows at least one commit body containing the literal `Revert plan:`
    - All Plan 68-02 commits are GPG-signed (`git log -n 5 --show-signature` shows `Good signature` lines for the new commits)
    - `cargo doc --no-deps 2>&1 | grep -i "^warning"` returns zero lines
  </acceptance_criteria>
  <done>
    Feature-matrix CI extended with both new states; all five doc deliverables authored; commits signed and carry `Revert plan:` bodies; cargo doc warning-free.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan reduces compile-time dependency surface and adds a feature gate. No runtime trust boundary, no new input parsing, no network or filesystem access introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-N/A | N/A | Entire plan | accept | N/A — no new attack surface introduced. The `logging` feature is purely a compile-time switch; the underlying `log` crate is unchanged. Making `log` optional reduces supply-chain footprint when users opt out via `default-features = false`. No ASVS L1 category applies. |
</threat_model>

<verification>
- `cargo check` (default), `cargo check --no-default-features`, `cargo check --features logging`, `cargo check --no-default-features --features logging` all green
- `cargo test` and `cargo test --no-default-features` both green
- `cargo check --target wasm32-unknown-unknown` green (default features; logging on)
- `cargo doc --no-deps` zero warnings
- Feature-matrix CI green on every existing row plus the two new rows
- `cargo public-api` diff: `LogObserver` removal under `--no-default-features` is the only change; documented in CHANGELOG (Added → logging feature)
- CC-3 golden tests byte-identical
- Every commit body contains `Revert plan:` and is GPG-signed
</verification>

<success_criteria>
1. `logging` feature gates the `log` crate dependency; `default = ["logging", ...]` preserves current behaviour (ROADMAP SC-3)
2. Every `log::*!` call site and `use log::` import across `src/` (except the gated `observe/observer/log.rs`) is converted to the internal macro family
3. `LogObserver` is publicly available only when `logging` is enabled (D-03)
4. Feature-matrix CI includes a `no-default-features` row AND a `logging-explicit` row, both green (ROADMAP SC-7)
5. `.planning/intel/feature-flags.md` records the feature-flag philosophy dated 2026-06-15
6. CHANGELOG.md, README.md, docs/getting-started.md, src/lib.rs Feature Flags table all document the `logging` flag
7. CC-3 golden tests byte-identical (ROADMAP SC-8)
8. WASM check green with default features
</success_criteria>

<output>
Create `.planning/phases/68-build-perf-m2-dependency-hygiene/68-02-SUMMARY.md` when done. Summary must capture: number of files swept, count of call sites converted, count of `use log::` imports removed, transitive crate count under `--no-default-features` vs `default-features` (via `cargo tree | wc -l`), commit SHAs with their `Revert plan:` lines extracted, and any deviations.
</output>
