---
phase: 60-batch-fitness-fitness-cache-extension
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/engines/ga.rs
  - tests/engines/test_ga.rs
autonomous: true
requirements: []
tags: [rust, ga, fitness-cache, batch-evaluator]

must_haves:
  truths:
    - "D-03: User can configure `.with_batch_evaluator(Arc<dyn BatchFitnessEvaluator<U>>)` on `Ga`"
    - "D-02: When batch evaluator is configured, individual `calculate_fitness()` is never called for offspring (batch replaces calculate_fitness)"
    - "D-02: Initial population is batch-evaluated when batch evaluator is configured"
    - "D-06: When both batch evaluator and fitness cache are configured, only cache misses are sent to `evaluate_batch` (batch+cache partition algorithm)"
    - "D-03: `Ga::build()` returns `GaError::ConfigurationError` if both `fitness_fn` and `batch_evaluator` are set (mutual exclusivity)"
    - "D-07: `GenerationStats.cache_hits` / `cache_misses` carry per-generation delta values when cache is configured"
  artifacts:
    - path: "src/engines/ga.rs"
      provides: "batch_evaluator field, with_batch_evaluator builder (D-03), batch path in run() (D-02), cache delta wiring (D-07), batch+cache partition (D-06)"
      contains: "with_batch_evaluator"
  key_links:
    - from: "src/engines/ga.rs (run loop)"
      to: "BatchFitnessEvaluator::evaluate_batch"
      via: "self.batch_evaluator.as_ref().map(|e| e.evaluate_batch(&offspring))"
      pattern: "evaluate_batch"
    - from: "src/engines/ga.rs (generation loop)"
      to: "self.fitness_cache.lock().hits/misses"
      via: "saturating_sub delta read"
      pattern: "saturating_sub"
---

<objective>
Wire the `BatchFitnessEvaluator` trait (D-01 contract), cache delta stats (D-07), and batch+cache partition (D-06) into the `Ga<U>` engine end-to-end. Implements D-02 (batch replaces calculate_fitness in Ga) and D-03 (with_batch_evaluator builder + mutual exclusivity). Activate all `tests/engines/test_ga.rs` Wave 0 stubs from plan 60-01.

Purpose: Phase 60 SC #1 and SC #2 (Ga side) are delivered here; CMA wiring follows in plan 60-03.
Output: Production-quality batch + cache path in `Ga::run()`, including initial-population coverage (Pitfall 4) and lock-release-before-batch (Pitfall 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-CONTEXT.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-01-SUMMARY.md
@src/engines/ga.rs
@src/fitness/cache.rs
@src/fitness/batch.rs
@src/error.rs

<interfaces>
From Wave 1 (60-01):
- `pub trait BatchFitnessEvaluator<U: ChromosomeT>: Send + Sync` with method `fn evaluate_batch(&self, chromosomes: &[U]) -> Vec<f64>` in `src/fitness/batch.rs`
- `wrap_with_cache(fn, size)` returns `(Arc<FitnessFn<G>>, Arc<Mutex<FitnessCache>>)` in `src/fitness/cache.rs`
- `Ga<U>` already has `fitness_cache: Option<Arc<Mutex<FitnessCache>>>` field reserved
- `GenerationStats.cache_hits: Option<u64>` and `cache_misses: Option<u64>` already declared

Existing `Ga` integration points (per 60-PATTERNS.md):
- Builder pattern: `pub fn with_fitness_cache_size(mut self, size: usize) -> Self` (lines ~893-908)
- Cache wiring call site in `build()`: lines ~761-768 (already destructures tuple per 60-01, currently discards handle)
- Mutual exclusivity pattern: seeds/checkpoint check at lines 773-779 returns `GaError::ConfigurationError(String)`
- Initial population eval path: inside `self.initialization()` (`initialize_random`/`initialize_with_seeds` call `calculate_fitness` per-chromosome via `set_fitness_fn`)
- Per-generation offspring path: `parent_crossover` free function at line ~2482 sets fitness inline at lines 2753-2762 by injecting `fitness_fn` and calling `calculate_fitness`
- `gen_stats` construction: around line 1919; `dynamic_mutation_probability` is set on `gen_stats` at lines 1954-1955

`GaError::ConfigurationError(String)` exists in `src/error.rs`.
`hash_dna<G: Debug>(dna: &[G]) -> u64` in `src/fitness/cache.rs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ga batch_evaluator field, builder, mutual-exclusivity check, batch_evaluate_pop helper</name>
  <files>src/engines/ga.rs, tests/engines/test_ga.rs</files>
  <read_first>
    - src/engines/ga.rs (lines 250-330 struct fields, 760-790 build validation, 880-920 builders, 2480-2770 parent_crossover)
    - src/fitness/batch.rs
    - src/fitness/cache.rs (hash_dna signature; FitnessCache::get/put)
    - src/error.rs (confirm GaError::ConfigurationError variant)
    - tests/engines/test_ga.rs (find `mod batch_evaluator_tests` block stubbed in 60-01)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md (Pattern 3 batch+cache partition; Pitfalls 2 and 4)
  </read_first>
  <behavior>
    - Builder `Ga::with_batch_evaluator(Arc<dyn BatchFitnessEvaluator<U> + Send + Sync>) -> Self` stores the evaluator (D-03)
    - `Ga::build()` returns `Err(GaError::ConfigurationError(_))` when BOTH `fitness_fn` and `batch_evaluator` are set (D-03 mutual exclusivity)
    - The Wave 1 `wrap_with_cache` call site now assigns the returned handle into `self.fitness_cache` (no more `_cache_handle`)
    - Private helper `batch_evaluate_pop(&self, pop: &mut [U]) -> Result<(), GaError>`: no-op when evaluator unset; otherwise applies batch + (optional) cache-partition per RESEARCH Pattern 3 (D-06)
    - Helper releases the cache `Mutex` BEFORE calling `evaluate_batch`, re-acquires it to store misses (Pitfall 2)
    - Helper uses `debug_assert_eq!(values.len(), chromosomes.len())` for threat T-60-01
    - Active tests `ga_with_batch_evaluator_runs_to_completion`, `ga_batch_and_fitness_fn_mutually_exclusive_returns_configuration_error`, and `ga_batch_evaluator_called_once_per_generation` pass
  </behavior>
  <action>
    Add field `batch_evaluator: Option<Arc<dyn BatchFitnessEvaluator<U> + Send + Sync>>` to `Ga<U>` struct (per D-03), immediately after `fitness_cache_size` / `fitness_cache`. Initialize to `None` at every `Ga` construction site (Default impl, `new`, builder reset, etc.) — search for sibling field `observer:` initializations and add `batch_evaluator: None` next to each.

    Add builder method `pub fn with_batch_evaluator(mut self, evaluator: Arc<dyn BatchFitnessEvaluator<U> + Send + Sync>) -> Self { self.batch_evaluator = Some(evaluator); self }` directly below `with_fitness_cache_size` (per D-03).

    In `Ga::build()` immediately after the existing seeds/checkpoint mutual-exclusivity block (around line 779) insert a new check (per D-03): if `self.fitness_fn.is_some() && self.batch_evaluator.is_some()` return `Err(GaError::ConfigurationError("Cannot use both fitness_fn and with_batch_evaluator() — they are mutually exclusive".to_string()))`. Also if neither is set after build-time wiring, preserve any existing error path (do not duplicate it).

    Replace the Wave 1 `_cache_handle` placeholder with `self.fitness_cache = Some(cache_handle);` so the engine retains the handle for delta reads. The wrap-with-cache block remains gated by `self.fitness_cache_size.is_some() && self.fitness_fn.is_some()` (no change in semantics — when batch mode is on, `wrap_with_cache` is not used; cache handle is created directly in the helper path during run, see Task 2).

    Add private method `fn batch_evaluate_pop(&self, pop: &mut [U]) -> Result<(), GaError>` on `Ga<U>`. Behavior:
      Case A — `self.batch_evaluator.is_none()`: return `Ok(())`.
      Case B — evaluator set, cache unset: call `evaluator.evaluate_batch(pop)`; debug_assert lengths match; assign `pop[i].set_fitness(values[i])` for every i.
      Case C — both evaluator and `self.fitness_cache` set (implements D-06 partition):
        1. Acquire cache lock; for each `i` compute `key = hash_dna(pop[i].dna())`; on `Some(f)` store into `fitness_values[i]`; on `None` push `i` into `miss_indices`. Pre-allocate `fitness_values: Vec<f64>` with `vec![0.0; pop.len()]`.
        2. Drop the lock (RESEARCH Pitfall 2 — never hold lock across `evaluate_batch`).
        3. Build `miss_chromosomes: Vec<U>` by cloning `pop[orig_i].clone()` for each `orig_i in miss_indices` (assumption A3 — D-01 locks `&[U]`).
        4. Call `evaluator.evaluate_batch(&miss_chromosomes)` and `debug_assert_eq!(miss_values.len(), miss_indices.len())`.
        5. Re-acquire the cache lock; for each pair `(orig_i, f)` of `(miss_indices, miss_values)` write `fitness_values[orig_i] = f` and `cache.put(hash_dna(pop[orig_i].dna()), f)`. Drop lock.
        6. For every `i` call `pop[i].set_fitness(fitness_values[i])`.
      Return `Ok(())`.

    Activate three tests in `tests/engines/test_ga.rs::batch_evaluator_tests` by removing `#[ignore]` and providing real bodies:
      `ga_with_batch_evaluator_runs_to_completion` — define a local struct `CountingEvaluator { calls: AtomicUsize }` implementing `BatchFitnessEvaluator<...>` returning `vec![1.0; chromosomes.len()]`. Wire via `.with_batch_evaluator(Arc::new(CountingEvaluator::default()))`, run 3 generations, assert `Ok(_)` and `population.len() > 0`.
      `ga_batch_and_fitness_fn_mutually_exclusive_returns_configuration_error` — build a `Ga` with both `.with_fitness_fn(...)` and `.with_batch_evaluator(...)`, assert `build()` returns `Err(GaError::ConfigurationError(_))` (verifies D-03).
      `ga_batch_evaluator_called_once_per_generation` — using `CountingEvaluator`, run N=5 generations, assert `calls.load(Ordering::Relaxed) >= 6` (1 for initial population + N for generations) and that every chromosome in the final population has `fitness() != 0.0`.

    Leave the remaining 5 ignored stubs in place; Task 2 wires them.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms &amp;&amp; cargo build &amp;&amp; cargo test --test test_ga batch_evaluator_tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "with_batch_evaluator" src/engines/ga.rs` matches at least 2 lines (field assignment + builder method)
    - `grep -n "batch_evaluator: Option<Arc<dyn BatchFitnessEvaluator" src/engines/ga.rs` matches the field declaration
    - `grep -n "Cannot use both fitness_fn and with_batch_evaluator" src/engines/ga.rs` matches one error string
    - `grep -n "self.fitness_cache = Some(cache_handle)" src/engines/ga.rs` matches one line (Wave 1 placeholder removed)
    - `grep -n "fn batch_evaluate_pop" src/engines/ga.rs` matches one declaration
    - `grep -n "debug_assert_eq!" src/engines/ga.rs | grep -E "(miss_values|fitness_values|values\.len)"` shows the length check is present
    - `cargo test --test test_ga batch_evaluator_tests::ga_with_batch_evaluator_runs_to_completion` passes
    - `cargo test --test test_ga batch_evaluator_tests::ga_batch_and_fitness_fn_mutually_exclusive_returns_configuration_error` passes
    - `cargo test --test test_ga batch_evaluator_tests::ga_batch_evaluator_called_once_per_generation` passes
    - `cargo clippy --all-targets -- -D warnings` passes
  </acceptance_criteria>
  <done>
    Field (D-03), builder (D-03), build-time validation (D-03), and `batch_evaluate_pop` helper (D-06) are in place. The three activated Ga tests pass; no regressions in existing tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire batch path into Ga::run() (initial population + offspring) and cache delta stats</name>
  <files>src/engines/ga.rs, tests/engines/test_ga.rs</files>
  <read_first>
    - src/engines/ga.rs (run() body — generation loop region around lines 1090-1980; parent_crossover call site; initialization() call site; gen_stats construction around line 1919)
    - src/fitness/cache.rs (FitnessCache::hits / misses; FitnessCache::new)
    - tests/engines/test_ga.rs (remaining 5 ignored stubs)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md (Pattern 6 cache delta reading; Pitfall 4 initial population)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md (cache delta reading; "Dynamic mutation probability pattern to copy for gen_stats")
  </read_first>
  <behavior>
    - D-02: When `self.batch_evaluator.is_some()`, `parent_crossover` is invoked with `fitness_fn = None` (no per-child evaluation inside parent_crossover) and the batch helper is called on the returned offspring before they enter the population merge
    - D-02: When batch evaluator is set, initial population is batch-evaluated via `self.batch_evaluate_pop(&mut self.population.chromosomes)` immediately after `self.initialization()` returns
    - D-06: When batch evaluator AND no scalar `fitness_fn` but `fitness_cache_size` is set, an `Arc<Mutex<FitnessCache>>` is created at `run()` start and stored in `self.fitness_cache` so the helper can use Case C path
    - D-07: `gen_stats.cache_hits` and `gen_stats.cache_misses` are populated with `saturating_sub` deltas when `self.fitness_cache.is_some()`; otherwise they remain `None`
    - All remaining 5 ignored Ga test stubs from 60-01 are activated and pass
  </behavior>
  <action>
    In `Ga::run()` immediately after the call to `self.initialization()` (the line that fills `self.population.chromosomes`) add: `self.batch_evaluate_pop(&mut self.population.chromosomes)?;` — this covers Pitfall 4 (initial population eval) for batch mode (per D-02) and is a no-op otherwise.

    At the top of `Ga::run()`, before the generation loop, add cache-handle bootstrap for the batch-only-with-cache case (per D-06). If `self.batch_evaluator.is_some() && self.fitness_cache_size.is_some() && self.fitness_cache.is_none()` then construct `self.fitness_cache = Some(Arc::new(Mutex::new(FitnessCache::new(self.fitness_cache_size.unwrap()))));`. (When `fitness_fn` is set, `build()` already wired the cache via `wrap_with_cache`; this branch handles the batch-without-fitness-fn case.)

    Locate the `parent_crossover(...)` call site in `Ga::run()`. Change the `fitness_fn` argument so that when `self.batch_evaluator.is_some()` it is passed as `None` (or `Option::<Arc<FitnessFn<_>>>::None` per the actual signature) — children come back with default 0.0 fitness (per D-02). Immediately after `parent_crossover` returns the offspring `Vec<U>`, call `self.batch_evaluate_pop(&mut offspring)?;` so offspring fitness is populated before survivor selection or any sort by fitness.

    Implement cache delta reads using RESEARCH Pattern 6 around the generation loop body (per D-07). Before the parent selection step at the top of each generation iteration, snapshot:
      `let (prev_cache_hits, prev_cache_misses) = match &self.fitness_cache { Some(ch) => { let c = ch.lock().expect("fitness cache lock poisoned"); (c.hits(), c.misses()) } None => (0, 0) };`
    After `gen_stats` is constructed (line ~1919) but BEFORE it is pushed into `self.stats`, write:
      `if let Some(ref ch) = self.fitness_cache { let c = ch.lock().expect("fitness cache lock poisoned"); gen_stats.cache_hits = Some(c.hits().saturating_sub(prev_cache_hits)); gen_stats.cache_misses = Some(c.misses().saturating_sub(prev_cache_misses)); }`
    Follow the exact style used at lines 1954-1955 for `dynamic_mutation_probability`.

    Activate the remaining 5 tests in `tests/engines/test_ga.rs::batch_evaluator_tests`:
      `ga_batch_evaluator_replaces_calculate_fitness` — the evaluator returns `vec![42.0; n]`; after one generation, assert every offspring's `fitness() == 42.0` (proves D-02: `calculate_fitness` was bypassed).
      `ga_batch_evaluator_initial_population_evaluated` — after `build()` + `run(1 gen)`, assert the initial population's fitness values are all non-default and equal to the evaluator's deterministic output (e.g. all `1.0`) — verifies D-02 initial pop coverage.
      `ga_cache_stats_populated_in_generation_stats` — configure both `with_fitness_fn` and `with_fitness_cache_size(64)`; run 3 generations; assert `stats.last().cache_hits.is_some()` and `stats.last().cache_misses.is_some()` and that `cache_hits + cache_misses > 0` (verifies D-07).
      `ga_cache_stats_none_when_no_cache` — configure `with_fitness_fn` without cache; run 2 generations; assert every `gen_stats.cache_hits.is_none()` and `cache_misses.is_none()` (verifies D-07).
      `ga_batch_plus_cache_only_misses_evaluated` — use `CountingEvaluator` + `with_batch_evaluator(...)` + `with_fitness_cache_size(64)` (verifies D-06). Build a small population where DNA repeats are likely (low cardinality genes), run 3 generations, assert `last_gen.cache_hits.unwrap_or(0) > 0` and that `evaluator.calls.load() < total_offspring_evaluated` (i.e. cache hits short-circuited some batch calls).
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms &amp;&amp; cargo test --test test_ga batch_evaluator_tests &amp;&amp; cargo test &amp;&amp; cargo test --features serde &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo check --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "batch_evaluate_pop" src/engines/ga.rs` matches at least 3 lines (definition + initial-pop call + offspring call)
    - `grep -n "prev_cache_hits" src/engines/ga.rs` matches at least 2 lines (snapshot + delta read)
    - `grep -n "saturating_sub" src/engines/ga.rs` shows the cache delta computation
    - `grep -n "gen_stats.cache_hits = Some" src/engines/ga.rs` matches one line
    - `grep -n "gen_stats.cache_misses = Some" src/engines/ga.rs` matches one line
    - All 8 tests in `tests/engines/test_ga.rs::batch_evaluator_tests` run as active (no `#[ignore]`) and pass
    - `cargo test` passes (no regressions)
    - `cargo test --features serde` passes
    - `cargo clippy --all-targets -- -D warnings` passes (zero warnings)
    - `cargo check --target wasm32-unknown-unknown` succeeds (no `Instant`, no rayon added in batch path)
  </acceptance_criteria>
  <done>
    `Ga::run()` evaluates initial population and per-generation offspring via the batch helper when configured (D-02); cache delta stats appear in `GenerationStats` exactly when a cache is active (D-07); all 8 Ga batch tests pass; phase-wide gates green for Ga side.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User trait impl | `Arc<dyn BatchFitnessEvaluator<U>>` runs in-process; same trust posture as `Arc<dyn GaObserver<U>>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `evaluate_batch` returns wrong-length `Vec<f64>` | mitigate | `debug_assert_eq!` on both length-check sites in `batch_evaluate_pop` |
| T-60-02 | Denial of Service | Cache mutex poisoning | accept | `.expect("fitness cache lock poisoned")` matches existing pattern; surfaces panic immediately |
| T-60-04 | Tampering | Caller writes `fitness_fn` after `with_batch_evaluator` (or vice versa) | mitigate | `build()` returns `GaError::ConfigurationError` (per D-03) |
| T-60-05 | Denial of Service | Holding cache lock across an expensive `evaluate_batch` call (GPU latency) blocks observers and other readers | mitigate | Helper explicitly drops the lock before `evaluate_batch` and re-acquires for the put phase |
| T-60-SC | Tampering | npm/pip/cargo installs | n/a | No new external dependencies |
</threat_model>

<verification>
- `cargo build`, `cargo test`, `cargo test --features serde` all pass
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo check --target wasm32-unknown-unknown` passes
- All 8 tests in `batch_evaluator_tests` module of `tests/engines/test_ga.rs` are active and pass
</verification>

<success_criteria>
- Phase 60 SC #1 satisfied on the Ga side (D-02): batch evaluator fully replaces `calculate_fitness` for offspring and initial population
- Phase 60 SC #2 satisfied on the Ga side (D-07): cache hits/misses appear in `GenerationStats` deltas; `None` when no cache
- Phase 60 SC #3 satisfied: WASM compiles (no `Instant`, no rayon, only `Arc<Mutex<_>>`)
- D-06 batch+cache partition behaves correctly: only misses are batched; hits short-circuit `evaluate_batch`
</success_criteria>

<output>
Create `.planning/phases/60-batch-fitness-fitness-cache-extension/60-02-SUMMARY.md` when done.
</output>
</content>
</invoke>