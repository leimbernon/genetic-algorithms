---
phase: 55-rfc-multi-valued-fitness
plan: 03
type: execute
wave: 3
depends_on:
  - 55-01
  - 55-02
files_modified:
  - src/operations/selection.rs
  - src/operations/selection/lexicase.rs
  - src/engines/ga.rs
autonomous: true
requirements:
  - TRAITS-01
  - SEL-02
  - SEL-03
tags:
  - rust
  - lexicase
  - rename

must_haves:
  truths:
    - "Lexicase selection compiles against the renamed `VectorFitness` trait"
    - "`factory_lexicase<U: ChromosomeT + VectorFitness>()` is the dispatcher (per D-10)"
    - "`Ga::<U: VectorFitness>::select_parents_lexicase()` exists with the renamed bound (per D-11)"
  artifacts:
    - path: "src/operations/selection.rs"
      provides: "factory_lexicase with renamed bound + updated error messages"
      contains: "factory_lexicase"
    - path: "src/operations/selection/lexicase.rs"
      provides: "Lexicase operator with renamed methods"
      contains: "VectorFitness"
    - path: "src/engines/ga.rs"
      provides: "Ga::select_parents_lexicase impl block with renamed bound"
      contains: "VectorFitness"
  key_links:
    - from: "src/operations/selection/lexicase.rs"
      to: "VectorFitness::fitness_values"
      via: "method call sites"
      pattern: "\\.fitness_values\\(\\)"
    - from: "src/engines/ga.rs select_parents_lexicase"
      to: "factory_lexicase"
      via: "dispatch"
      pattern: "factory_lexicase"
---

<objective>
Update all lexicase callers (`src/operations/selection.rs`, `src/operations/selection/lexicase.rs`, `src/engines/ga.rs`) to use the new `VectorFitness` trait name and the renamed methods `fitness_values()` / `set_fitness_values()`. Per D-10 and D-11. Pure mechanical rename — no behavioral change.

Purpose: Eliminate the `MultiCaseFitness` symbol from lexicase code paths. After this plan, `cargo check` errors related to lexicase are resolved (MO engine errors remain — handled in Plans 04 + 05).
Output: Three files with renamed trait references, renamed method calls, updated error message strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rfc-multi-valued-fitness/55-CONTEXT.md
@.planning/phases/55-rfc-multi-valued-fitness/55-RESEARCH.md
@.planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md

<interfaces>
Wave 1 produced `pub trait VectorFitness: ChromosomeT { fn fitness_values(&self) -> &[f64]; fn set_fitness_values(&mut self, values: Vec<f64>); }`.

Rename map for this plan:
- bound: `MultiCaseFitness` → `VectorFitness`
- method call: `.case_fitness()` → `.fitness_values()`
- method call: `.set_case_fitness(...)` → `.set_fitness_values(...)`
- error message strings: any literal containing `case_fitness` / `set_case_fitness` / `MultiCaseFitness` rewritten to the new names
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update src/operations/selection/lexicase.rs — trait import, bounds, method calls</name>
  <files>src/operations/selection/lexicase.rs</files>
  <read_first>
    - src/operations/selection/lexicase.rs (full file — find every `MultiCaseFitness`, `case_fitness`, `set_case_fitness` occurrence)
    - src/traits/vector_fitness.rs (Wave 1 — confirm signatures)
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (section "src/operations/selection/lexicase.rs" lists call-site line numbers)
  </read_first>
  <behavior>
    - All occurrences of `MultiCaseFitness` in this file replaced with `VectorFitness`.
    - All call sites `chromosomes[i].case_fitness()` replaced with `chromosomes[i].fitness_values()`.
    - No `case_fitness` or `MultiCaseFitness` token remains in the file.
    - Lexicase tournament selection behavior unchanged (compare by `fitness_values` instead of `case_fitness`).
  </behavior>
  <action>
    Per D-10:
    1. Update the use statement (currently `use crate::traits::{ChromosomeT, MultiCaseFitness};`) to `use crate::traits::{ChromosomeT, VectorFitness};`.
    2. Replace every occurrence of `MultiCaseFitness` in trait bounds (generic constraints in `fn compute_mad_epsilons<U: MultiCaseFitness>`, in `where U: ChromosomeT + MultiCaseFitness` clauses for operator structs/methods) with `VectorFitness`.
    3. Replace every method call `.case_fitness()` with `.fitness_values()`. Per PATTERNS.md these are at approximately lines 30, 82, 84, 87, 129, 133, 180, 184 — use compiler errors to find the exact set, not just those line numbers.
    4. Replace every `.set_case_fitness(...)` with `.set_fitness_values(...)` (if any exist in this file).
    5. Update any rustdoc/comments mentioning `MultiCaseFitness` or `case_fitness` to use the new names. Update string literals in error messages only if they appear here (they likely live in `selection.rs`, but check).
  </action>
  <verify>
    <automated>grep -c "MultiCaseFitness" src/operations/selection/lexicase.rs ; grep -cE "[^_]case_fitness\b" src/operations/selection/lexicase.rs ; cargo check 2>&amp;1 | grep "src/operations/selection/lexicase.rs" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MultiCaseFitness" src/operations/selection/lexicase.rs` returns `0`.
    - `grep -cE "[^_]case_fitness\b" src/operations/selection/lexicase.rs` returns `0`.
    - `grep -c "set_case_fitness" src/operations/selection/lexicase.rs` returns `0`.
    - `grep -c "VectorFitness" src/operations/selection/lexicase.rs` is at least `1`.
    - `grep -c "\.fitness_values()" src/operations/selection/lexicase.rs` is at least `4` (PATTERNS.md identifies at least 4 read-site call locations).
    - `cargo check` produces no errors originating from this file (downstream engine errors are acceptable for this Wave 3 plan).
  </acceptance_criteria>
  <done>The lexicase operator implementation uses `VectorFitness` and `fitness_values()` throughout. No `MultiCaseFitness` or `case_fitness` token remains in live code in this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update src/operations/selection.rs factory_lexicase + src/engines/ga.rs select_parents_lexicase impl block</name>
  <files>src/operations/selection.rs, src/engines/ga.rs</files>
  <read_first>
    - src/operations/selection.rs (around line 11 imports; lines 164-224 factory_lexicase; line 71 panic message; lines 177-184, 216 method calls)
    - src/engines/ga.rs (around line 152 imports; lines 2924-2967 impl block for select_parents_lexicase; doc comments at lines 2941, 2957)
    - src/traits/vector_fitness.rs (Wave 1)
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (sections "src/operations/selection.rs" and "src/engines/ga.rs")
  </read_first>
  <behavior>
    - `factory_lexicase<U: ChromosomeT + VectorFitness + Sync + Send + 'static + Clone>()` is the new signature (per D-10).
    - `Ga::<U: VectorFitness>::select_parents_lexicase` impl block uses `VectorFitness` in its where clause (per D-11).
    - Error message strings reference `fitness_values` / `set_fitness_values` / `VectorFitness` instead of the old names.
    - All `.case_fitness()` calls replaced with `.fitness_values()`.
  </behavior>
  <action>
    Per D-10 and D-11:

    In `src/operations/selection.rs`:
    1. Update the use statement: replace `MultiCaseFitness` with `VectorFitness` in `use crate::traits::{...}`.
    2. In `factory_lexicase` (~line 164): change the trait bound on `U` from `ChromosomeT + MultiCaseFitness + Sync + Send + 'static + Clone` to `ChromosomeT + VectorFitness + Sync + Send + 'static + Clone`.
    3. Replace every `.case_fitness()` call with `.fitness_values()` (PATTERNS.md identifies sites at ~lines 177-184, 216 — verify exhaustively via grep).
    4. Update error message string literals:
       - `"case_fitness() is empty — call set_case_fitness in calculate_fitness"` becomes `"fitness_values() is empty — call set_fitness_values in calculate_fitness"`
       - `"NaN in case_fitness at chromosome {}"` becomes `"NaN in fitness_values at chromosome {}"`
       - The panic message at ~line 71: `"Island-model and NSGA-II paths do not support MultiCaseFitness."` becomes `"Island-model and NSGA-II paths do not support VectorFitness."`
    5. Update any other rustdoc/comments using the old names.

    In `src/engines/ga.rs`:
    1. Update the use statement (~line 152): replace `MultiCaseFitness` with `VectorFitness` in the traits import.
    2. In the `impl<U> Ga<U>` block at ~lines 2924-2936 that defines `select_parents_lexicase`: change the where clause from `U: LinearChromosome + MultiCaseFitness + Send + Sync + ...` to `U: LinearChromosome + VectorFitness + Send + Sync + ...` (keep all other bounds unchanged).
    3. Update doc comments on this impl block at ~lines 2941, 2957 that reference `MultiCaseFitness` to reference `VectorFitness`.
    4. If `select_parents_lexicase` body calls `.case_fitness()` directly, rename to `.fitness_values()`.

    Do NOT change the algorithm. Do NOT change the visible behavior. This is mechanical rename only.
  </action>
  <verify>
    <automated>grep -c "MultiCaseFitness" src/operations/selection.rs src/engines/ga.rs ; grep -cE "[^_]case_fitness\b" src/operations/selection.rs src/engines/ga.rs ; cargo check 2>&amp;1 | grep -E "(selection\.rs|ga\.rs)" | grep -v lexicase | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MultiCaseFitness" src/operations/selection.rs` returns `0`.
    - `grep -c "MultiCaseFitness" src/engines/ga.rs` returns `0`.
    - `grep -cE "[^_]case_fitness\b" src/operations/selection.rs` returns `0`.
    - `grep -cE "[^_]case_fitness\b" src/engines/ga.rs` returns `0`.
    - `grep -c "set_case_fitness" src/operations/selection.rs` returns `0`.
    - `grep -c "VectorFitness" src/operations/selection.rs` is at least `2` (use + bound).
    - `grep -c "VectorFitness" src/engines/ga.rs` is at least `2` (use + where clause).
    - `grep -c "fitness_values() is empty" src/operations/selection.rs` is at least `1` (updated error message).
    - `grep -c "support VectorFitness" src/operations/selection.rs` is at least `1` (updated panic message).
    - `cargo check` produces no errors originating in `src/operations/selection.rs` or in the `select_parents_lexicase` impl block of `src/engines/ga.rs` (MO engine errors remain — handled in plans 04 + 05).
  </acceptance_criteria>
  <done>Both files use `VectorFitness` and `fitness_values` exclusively. Error strings updated. `select_parents_lexicase` impl block bound is `VectorFitness`. Lexicase callers fully migrated.</done>
</task>

</tasks>

<verification>
- `grep -rn "MultiCaseFitness\|case_fitness\|set_case_fitness" src/operations/ src/engines/ga.rs | grep -v '^\s*//'` returns no matches in live code.
- `cargo check` errors remaining are confined to MO engine files (`src/engines/nsga2/mod.rs`, `nsga3/mod.rs`, `moead/mod.rs`, `spea2/mod.rs`, `sms_emoa/mod.rs`, `ibea/mod.rs`, `island/nsga2.rs`) and to tests/examples — these are addressed in plans 04, 05, and 06.
- `cargo clippy --no-deps` (when run later at the phase gate) produces no new lints originating from the modified files.
</verification>

<success_criteria>
- All lexicase code paths reference `VectorFitness`; no `MultiCaseFitness` symbol remains in `src/operations/` or in `src/engines/ga.rs`.
- All `.case_fitness()` calls have become `.fitness_values()`.
- All error message strings and panic messages updated.
- Behavior is identical to before — pure rename, no algorithmic change.
</success_criteria>

<output>
Create `.planning/phases/55-rfc-multi-valued-fitness/55-03-SUMMARY.md` when done.
</output>
