---
phase: 55-rfc-multi-valued-fitness
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - src/types/chromosomes/binary.rs
  - src/types/chromosomes/range.rs
  - src/types/chromosomes/list.rs
  - src/types/chromosomes/unique.rs
  - src/types/chromosomes/multi_range.rs
  - src/types/chromosomes/multi_unique.rs
  - src/engines/gp/chromosome.rs
autonomous: true
requirements:
  - TRAITS-01
tags:
  - rust
  - chromosomes
  - vector-fitness

must_haves:
  truths:
    - "Each built-in chromosome type stores a `fitness_values: Vec<f64>` field"
    - "Each built-in chromosome type implements `VectorFitness`"
    - "Existing serde checkpoints (without `fitness_values`) still deserialize — field defaults to `Vec::new()`"
  artifacts:
    - path: "src/types/chromosomes/binary.rs"
      provides: "Binary chromosome with VectorFitness impl"
      contains: "impl VectorFitness for Binary"
    - path: "src/types/chromosomes/range.rs"
      provides: "Range chromosome with VectorFitness impl"
      contains: "impl"
    - path: "src/engines/gp/chromosome.rs"
      provides: "GpChromosome with VectorFitness impl (private-field style)"
      contains: "impl"
  key_links:
    - from: "Each chromosome `Default::default()` / `new()`"
      to: "fitness_values: Vec::new()"
      via: "constructor initialization"
      pattern: "fitness_values: Vec::new\\(\\)"
    - from: "Each chromosome struct definition"
      to: "fitness_values field"
      via: "Vec<f64> storage"
      pattern: "fitness_values: Vec<f64>"
---

<objective>
Add a `fitness_values: Vec<f64>` storage field and a `VectorFitness` trait impl to every built-in concrete chromosome type. This satisfies the new `U: VectorFitness` bound that MO engines will require in Wave 3 (Plans 04 + 05). Per D-01 (opt-in, no blanket impl) and CONTEXT.md Pitfall 2.

Purpose: Unblock downstream MO engine bound additions. Without this wave, `Nsga2Ga::<Binary>` and similar usages would fail to compile.
Output: Seven chromosome files updated with the field and trait impl; serde compatibility preserved via `#[serde(default)]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rfc-multi-valued-fitness/55-CONTEXT.md
@.planning/phases/55-rfc-multi-valued-fitness/55-RESEARCH.md
@.planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md
@.planning/phases/55-rfc-multi-valued-fitness/55-01-PLAN.md

<interfaces>
Trait to implement (from Wave 1):
- `pub trait VectorFitness: ChromosomeT`
- `fn fitness_values(&self) -> &[f64];`
- `fn set_fitness_values(&mut self, values: Vec<f64>);`

Field pattern (from `tests/structures.rs::MultiCaseChromosome`):
```
#[cfg_attr(feature = "serde", serde(default))]
pub fitness_values: Vec<f64>,
```

Field visibility convention:
- All built-in flat chromosomes (`Binary`, `Range<T>`, `List<T>`, `Unique<T>`, `MultiRange<T>`, `MultiUnique<T>`) use `pub fitness: f64` — keep `pub fitness_values: Vec<f64>` consistent.
- `GpChromosome<N>` uses private fields — keep `fitness_values: Vec<f64>` private (no `pub`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fitness_values + VectorFitness impl to 6 flat chromosomes (Binary, Range, List, Unique, MultiRange, MultiUnique)</name>
  <files>src/types/chromosomes/binary.rs, src/types/chromosomes/range.rs, src/types/chromosomes/list.rs, src/types/chromosomes/unique.rs, src/types/chromosomes/multi_range.rs, src/types/chromosomes/multi_unique.rs</files>
  <read_first>
    - src/types/chromosomes/binary.rs (current struct definition + Default impl + new() — find existing `pub fitness: f64`, `pub age: usize`, and `fitness_fn` placement; replicate field order)
    - src/types/chromosomes/range.rs (same shape — confirm where `fitness_fn` sits)
    - src/types/chromosomes/list.rs (same shape)
    - src/types/chromosomes/unique.rs (same shape)
    - src/types/chromosomes/multi_range.rs (same shape)
    - src/types/chromosomes/multi_unique.rs (same shape)
    - tests/structures.rs (lines ~95-178 — `MultiCaseChromosome` reference impl pattern)
    - src/traits/vector_fitness.rs (the trait signature from Wave 1)
  </read_first>
  <behavior>
    - For each of the 6 flat chromosome types: round-tripping `set_fitness_values(vec![1.0, 2.0])` then `fitness_values()` returns `&[1.0, 2.0]`.
    - Default-constructed chromosomes have `fitness_values == Vec::new()` (empty slice).
    - Serde round-trip works under the `serde` feature: serialize a chromosome with `fitness_values = vec![1.0]`, deserialize, assert equality.
    - Deserializing an old JSON payload that lacks `fitness_values` succeeds and yields `Vec::new()` (via `#[serde(default)]`).
  </behavior>
  <action>
    For each of the 6 files listed, apply this exact pattern (verified against `tests/structures.rs::MultiCaseChromosome`):
    1. Add an import: `use crate::traits::VectorFitness;` (add to the existing `use crate::traits::{...}` line — keep alphabetical).
    2. In the struct definition, insert a new field `fitness_values: Vec<f64>` immediately after the existing `pub age: usize` field. Annotate it with `#[cfg_attr(feature = "serde", serde(default))]`. Visibility = `pub` (matches the type's existing convention).
    3. In every constructor / `Default::default()` / `new()` of the type, add `fitness_values: Vec::new(),` to the struct literal.
    4. After the existing `impl ChromosomeT for <Type>` block, add a new `impl VectorFitness for <Type>` block with:
       - `fn fitness_values(&self) -> &[f64] { &self.fitness_values }`
       - `fn set_fitness_values(&mut self, values: Vec<f64>) { self.fitness_values = values; }`
    5. Do NOT add a default impl on the trait itself (that's already settled in Wave 1).
    6. Do NOT use `serde(skip)` — the field must be serialized so checkpoints preserve objective values. Use `serde(default)` only.
    Apply identical changes to `range.rs`, `list.rs`, `unique.rs`, `multi_range.rs`, `multi_unique.rs`. Generic types (`Range<T>`, `List<T>`, `Unique<T>`, `MultiRange<T>`, `MultiUnique<T>`) keep their existing generic bounds — the `impl VectorFitness` block inherits the same bounds as the `impl ChromosomeT` block above it.
  </action>
  <verify>
    <automated>cargo check --features serde 2>&amp;1 | tail -20 &amp;&amp; grep -lc "impl VectorFitness for" src/types/chromosomes/binary.rs src/types/chromosomes/range.rs src/types/chromosomes/list.rs src/types/chromosomes/unique.rs src/types/chromosomes/multi_range.rs src/types/chromosomes/multi_unique.rs</automated>
  </verify>
  <acceptance_criteria>
    - For each of `binary.rs`, `range.rs`, `list.rs`, `unique.rs`, `multi_range.rs`, `multi_unique.rs`: `grep -c "fitness_values: Vec<f64>" {file}` returns ≥ `1`.
    - For each of the 6 files: `grep -c "impl VectorFitness for" {file}` returns ≥ `1`.
    - For each of the 6 files: `grep -c "fn fitness_values(&self) -> &\[f64\]" {file}` returns ≥ `1`.
    - For each of the 6 files: `grep -c "fn set_fitness_values(&mut self, values: Vec<f64>)" {file}` returns ≥ `1`.
    - For each of the 6 files: `grep -c 'serde(default)' {file}` returns ≥ `1` (annotation on the new field — count may be higher if other fields also use it).
    - For each of the 6 files: `grep -c "fitness_values: Vec::new()" {file}` returns ≥ `1` (constructor init present).
    - `cargo check --features serde` produces no errors originating from these 6 files; downstream errors from MO engines (still expecting `MultiCaseFitness`) are acceptable in Wave 2.
  </acceptance_criteria>
  <done>All 6 flat chromosome types have the new field, trait impl, and constructor initialization. Type-checks pass for the affected files. Serde compatibility preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fitness_values + VectorFitness impl to GpChromosome (private-field style)</name>
  <files>src/engines/gp/chromosome.rs</files>
  <read_first>
    - src/engines/gp/chromosome.rs (full file — find `pub struct GpChromosome`, `Default` impl, `Clone` impl, `with_root()` constructor, existing `impl ChromosomeT for GpChromosome<N>`)
    - src/traits/vector_fitness.rs (Wave 1)
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (section "src/engines/gp/chromosome.rs" — exact field placement guidance)
  </read_first>
  <behavior>
    - `GpChromosome::default()` has `fitness_values == Vec::new()`.
    - `set_fitness_values(vec![1.0, 2.0])` followed by `fitness_values()` returns `&[1.0, 2.0]`.
    - `clone()` preserves `fitness_values`.
    - Serde round-trip works under `serde` feature with `fitness_values = vec![0.5]`.
    - Old serialized GpChromosomes (without `fitness_values`) deserialize successfully with `fitness_values = Vec::new()`.
  </behavior>
  <action>
    Apply the private-field pattern from PATTERNS.md:
    1. Add `use crate::traits::VectorFitness;` to the imports (alphabetical with existing trait imports).
    2. In `pub struct GpChromosome<N: GpNode>`, add a new private field `fitness_values: Vec<f64>` immediately after the existing `age: usize` field (do NOT make it `pub` — preserves GpChromosome's encapsulation style). Annotate with `#[cfg_attr(feature = "serde", serde(default))]`.
    3. Update the manual `Clone` impl: add `fitness_values: self.fitness_values.clone(),` to the struct literal.
    4. Update the manual `Default` impl: add `fitness_values: Vec::new(),` to the struct literal.
    5. Update the `with_root()` constructor: add `fitness_values: Vec::new(),` to the struct literal.
    6. After the existing `impl<N: GpNode> ChromosomeT for GpChromosome<N>` block, add `impl<N: GpNode + Default> VectorFitness for GpChromosome<N>` with the two trait methods (`fitness_values` returns `&self.fitness_values`; `set_fitness_values` assigns). Match the generic bounds of the surrounding ChromosomeT impl block.
    7. Use `serde(default)`, NOT `serde(skip)` — the field must persist through checkpoint roundtrips.
  </action>
  <verify>
    <automated>cargo check --features serde 2>&amp;1 | tail -15 &amp;&amp; grep -c "impl.*VectorFitness for GpChromosome" src/engines/gp/chromosome.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fitness_values: Vec<f64>" src/engines/gp/chromosome.rs` returns ≥ `1`.
    - `grep -c "impl.*VectorFitness for GpChromosome" src/engines/gp/chromosome.rs` returns ≥ `1`.
    - `grep -c "fn fitness_values(&self) -> &\[f64\]" src/engines/gp/chromosome.rs` returns ≥ `1`.
    - `grep -c "fn set_fitness_values(&mut self, values: Vec<f64>)" src/engines/gp/chromosome.rs` returns ≥ `1`.
    - `grep -c "fitness_values: Vec::new()" src/engines/gp/chromosome.rs` returns ≥ `2` (Default + with_root constructors).
    - `grep -c "fitness_values: self.fitness_values.clone()" src/engines/gp/chromosome.rs` returns ≥ `1` (Clone impl).
    - The field is NOT declared `pub` — `grep -E "pub\s+fitness_values" src/engines/gp/chromosome.rs` returns empty.
    - `cargo check --features serde` produces no errors originating from this file.
  </acceptance_criteria>
  <done>GpChromosome carries `fitness_values` as a private field, implements `VectorFitness`, preserves Clone/Default/with_root semantics, and serde-roundtrips correctly with `serde(default)` for forward compatibility.</done>
</task>

</tasks>

<verification>
- `grep -l "impl VectorFitness for\|impl.*VectorFitness for" src/types/chromosomes/*.rs src/engines/gp/chromosome.rs` lists all 7 files.
- `cargo check` (no features) and `cargo check --features serde` produce no NEW errors originating in these 7 files. Downstream errors from MO engines and lexicase callers persist — handled in Wave 3.
- Existing serde checkpoint tests for these chromosome types continue to pass (verified by re-running them at the phase gate in Plan 06).
</verification>

<success_criteria>
- All 7 chromosome types store `fitness_values: Vec<f64>`.
- All 7 chromosome types implement `VectorFitness`.
- Serde compatibility preserved via `#[serde(default)]` on the new field.
- No type uses a blanket impl — every type has an explicit `impl VectorFitness for ...` block (per D-01).
</success_criteria>

<output>
Create `.planning/phases/55-rfc-multi-valued-fitness/55-02-SUMMARY.md` when done.
</output>
