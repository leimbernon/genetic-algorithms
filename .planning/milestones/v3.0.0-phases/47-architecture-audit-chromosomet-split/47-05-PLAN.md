---
phase: 47-architecture-audit-chromosomet-split
plan: 05
type: execute
wave: 5
depends_on:
  - 47-04
files_modified:
  - src/configuration.rs
  - src/traits/configuration.rs
  - src/engines/ga.rs
  - tests/test_stopping_config.rs
autonomous: true
requirements:
  - ARCH-04
  - ARCH-06
pr_group: 2
tags:
  - rust
  - configuration
  - stopping-criteria
  - encapsulation
  - wasm

must_haves:
  truths:
    - "User configures stopping behavior via `.with_stagnation_limit(n)`, `.with_convergence_threshold(t)`, and `.with_max_duration_secs(s)` builder methods"
    - "StoppingCriteria struct no longer exists in the type system"
    - "GaConfiguration sub-struct fields are pub(crate) with read-only sub-struct accessors (limit, selection, crossover, mutation, survivor, extension, log, adaptive_ga; stopping fields flat with accessors)"
    - "max_duration_secs preserves the WASM gate at the call site in ga.rs (#[cfg(not(target_arch = wasm32))]); the field is un-gated"
    - "Wave 0 test `test_stopping_config` passes"
    - "ga.rs paths updated: `self.configuration.stopping_criteria.X` -> `self.configuration.X`"
  artifacts:
    - path: "src/configuration.rs"
      provides: "GaConfiguration with pub(crate) fields and accessors; flat stagnation/convergence/max_duration fields; StoppingCriteria struct removed"
      contains: "pub(crate) stagnation_generations"
    - path: "src/traits/configuration.rs"
      provides: "Builder methods with_stagnation_limit, with_convergence_threshold, with_max_duration_secs"
      contains: "fn with_stagnation_limit"
    - path: "tests/test_stopping_config.rs"
      provides: "Wave 0 test for flat builder methods"
  key_links:
    - from: "src/engines/ga.rs"
      to: "src/configuration.rs"
      via: "self.configuration.stagnation_generations etc."
      pattern: "self.configuration.(stagnation_generations|convergence_threshold|max_duration_secs)"
    - from: "src/traits/configuration.rs"
      to: "src/configuration.rs"
      via: "builder methods that set the new flat fields"
      pattern: "with_stagnation_limit|with_convergence_threshold|with_max_duration_secs"
---

<objective>
Dissolve the `StoppingCriteria` struct into 3 flat `pub(crate)` fields on `GaConfiguration` (per D-08), add the corresponding flat builder methods (per ARCH-06), encapsulate the remaining `GaConfiguration` sub-struct fields with `pub(crate)` and sub-struct-level public accessors (per D-09 / ARCH-04), and update `src/engines/ga.rs` to use the new flat field paths. Add the Wave 0 test for the flat stopping builder methods.

Purpose: Second plan of PR 2. Completes ARCH-06 and the GaConfiguration encapsulation half of ARCH-04. After this plan + 47-06, PR 2 is mergeable. Critical WASM constraint: `max_duration_secs` field is kept un-gated as `Option<f64>` and ONLY the call site in ga.rs is `#[cfg(not(target_arch = "wasm32"))]`-gated (the current pattern, preserved per RESEARCH.md Pitfall 3 and PATTERNS.md WASM Gate Pattern).

Output: Modified configuration.rs and traits/configuration.rs, updated ga.rs paths, new tests/test_stopping_config.rs, and `cargo check --lib` compile failures now reduced to only the `pub` -> `pub(crate)` caller breakage (handled by plan 47-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- GaConfiguration fields BEFORE: -->
<!-- pub adaptive_ga: bool -->
<!-- pub limit_configuration: LimitConfiguration -->
<!-- pub selection_configuration: SelectionConfiguration -->
<!-- pub crossover_configuration: CrossoverConfiguration -->
<!-- pub mutation_configuration: MutationConfiguration -->
<!-- pub survivor_configuration: SurvivorConfiguration -->
<!-- pub extension_configuration: ExtensionConfiguration -->
<!-- pub stopping_criteria: StoppingCriteria -->
<!-- pub log_configuration: ... -->

<!-- GaConfiguration fields AFTER (per D-08 + D-09): -->
<!-- pub(crate) adaptive_ga: bool -->
<!-- pub(crate) limit_configuration: LimitConfiguration -->
<!-- pub(crate) selection_configuration: SelectionConfiguration -->
<!-- pub(crate) crossover_configuration: CrossoverConfiguration -->
<!-- pub(crate) mutation_configuration: MutationConfiguration -->
<!-- pub(crate) survivor_configuration: SurvivorConfiguration -->
<!-- pub(crate) extension_configuration: ExtensionConfiguration -->
<!-- pub(crate) stagnation_generations: Option<usize>    // flattened -->
<!-- pub(crate) convergence_threshold: Option<f64>      // flattened -->
<!-- pub(crate) max_duration_secs: Option<f64>          // flattened, UN-GATED at field level -->
<!-- pub(crate) log_configuration: ... -->

<!-- Accessors per D-09 (sub-struct level): -->
<!-- pub fn limit(&self) -> &LimitConfiguration -->
<!-- pub fn selection(&self) -> &SelectionConfiguration -->
<!-- pub fn crossover(&self) -> &CrossoverConfiguration -->
<!-- pub fn mutation(&self) -> &MutationConfiguration -->
<!-- pub fn survivor(&self) -> &SurvivorConfiguration -->
<!-- pub fn extension(&self) -> &ExtensionConfiguration -->
<!-- pub fn log(&self) -> &LogConfiguration -->
<!-- pub fn adaptive_ga(&self) -> bool -->
<!-- pub fn stagnation_generations(&self) -> Option<usize> -->
<!-- pub fn convergence_threshold(&self) -> Option<f64> -->
<!-- pub fn max_duration_secs(&self) -> Option<f64> -->

<!-- Builder methods on StoppingConfig trait BEFORE: -->
<!-- fn with_stopping_criteria(self, criteria: StoppingCriteria) -> Self  // REMOVE -->

<!-- Builder methods on StoppingConfig trait AFTER: -->
<!-- fn with_stagnation_limit(self, n: usize) -> Self -->
<!-- fn with_convergence_threshold(self, threshold: f64) -> Self -->
<!-- fn with_max_duration_secs(self, secs: f64) -> Self  // NOT gated; field is un-gated, gate is at usage site in ga.rs -->

<!-- WASM pattern preserved at call site only (src/engines/ga.rs lines ~2102-2104 and ~1433-1442): -->
<!-- #[cfg(not(target_arch = "wasm32"))] -->
<!-- if let Some(max_secs) = self.configuration.max_duration_secs { ... } -->
<!-- Plus warn-once at run start: #[cfg(target_arch = "wasm32")] if max_duration_secs.is_some() { log::warn!(...); } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test for flat stopping builders + flatten StoppingCriteria into GaConfiguration</name>
  <files>tests/test_stopping_config.rs, src/configuration.rs, src/traits/configuration.rs</files>
  <read_first>
    - src/configuration.rs (StoppingCriteria struct lines ~263-275; GaConfiguration lines ~312-352; current `stopping_criteria: StoppingCriteria` field placement)
    - src/traits/configuration.rs (StoppingConfig trait around lines 75-83 — current `with_stopping_criteria` method)
    - src/engines/ga.rs (the call sites that read `self.configuration.stopping_criteria.*` — lines ~1433-1442 and ~2102-2104 per RESEARCH.md)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ src/configuration.rs Flat config fields, § WASM Gate Pattern, § src/traits/configuration.rs Builder method pattern)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md (D-08)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 3 — WASM gate at usage site only)
  </read_first>
  <behavior>
    - Test `test_stopping_config_with_stagnation_limit` builds a GaConfiguration via `.with_stagnation_limit(50)` and asserts the accessor reads back `Some(50)`
    - Test `test_stopping_config_with_convergence_threshold` builds via `.with_convergence_threshold(0.001)` and asserts read-back `Some(0.001)`
    - Test `test_stopping_config_with_max_duration_secs` builds via `.with_max_duration_secs(10.0)` and asserts read-back `Some(10.0)`. Run-time gating is in ga.rs; the field+builder remain available on all targets so callers do not need cfg.
    - Test `test_stopping_config_default_is_none` asserts a freshly defaulted GaConfiguration has all three stopping fields equal to `None`
    - Tests use the public accessor methods (not direct field access)
  </behavior>
  <action>
    Step A — Write Wave 0 tests in `tests/test_stopping_config.rs`. Tests reference methods `.with_stagnation_limit`, `.with_convergence_threshold`, `.with_max_duration_secs` on the Ga builder, and accessors `.stagnation_generations()`, `.convergence_threshold()`, `.max_duration_secs()` on the resulting configuration. RED state expected — those methods do not yet exist.

    Step B — In `src/configuration.rs`:
    1. Locate the `StoppingCriteria` struct (~lines 263-275) and its `Default` impl. DELETE the struct definition entirely. Remove any `pub use` re-export.
    2. Locate the `stopping_criteria: StoppingCriteria` field in `GaConfiguration` (~line 326). REPLACE with three flat fields: `pub(crate) stagnation_generations: Option<usize>`, `pub(crate) convergence_threshold: Option<f64>`, `pub(crate) max_duration_secs: Option<f64>`. Do NOT cfg-gate `max_duration_secs` — keep it `Option<f64>` on all targets per PATTERNS.md WASM Gate Pattern.
    3. Update `impl Default for GaConfiguration` to remove the `stopping_criteria` line and add three `None` defaults.
    4. Add public accessors on `impl GaConfiguration`: `pub fn stagnation_generations(&self) -> Option<usize> { self.stagnation_generations }`, same shape for `convergence_threshold` and `max_duration_secs`.

    Step C — In `src/traits/configuration.rs`:
    1. Remove the `use crate::configuration::StoppingCriteria;` import.
    2. Remove `fn with_stopping_criteria(self, criteria: StoppingCriteria) -> Self;` from the StoppingConfig trait.
    3. Add three new trait methods: `fn with_stagnation_limit(self, n: usize) -> Self;`, `fn with_convergence_threshold(self, threshold: f64) -> Self;`, `fn with_max_duration_secs(self, secs: f64) -> Self;`. Do NOT add `#[cfg(not(target_arch = "wasm32"))]` to `with_max_duration_secs` — the field exists everywhere; only the usage call site is wasm-gated per RESEARCH.md A5.

    Step D — Implement the three builder methods on whatever type implements StoppingConfig (find via `grep -rn 'impl.*StoppingConfig' src/`). Each method body sets the corresponding flat field on `self.configuration` and returns `self`. Remove the old `with_stopping_criteria` impl.

    Step E — Verify `cargo test --test test_stopping_config` passes once Task 2 ends. If the wave-0 test fails here because ga.rs still uses the old path, defer the green check to Task 2.

    Commit message: `refactor(47-05): flatten StoppingCriteria into GaConfiguration; add with_stagnation_limit/convergence_threshold/max_duration_secs builders`.
  </action>
  <verify>
    <automated>cargo check --lib 2>&amp;1 | grep -E 'no field `stopping_criteria`|cannot find type `StoppingCriteria`' | head -5; grep -c 'pub struct StoppingCriteria' src/configuration.rs</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_stopping_config.rs contains all four named tests above
    - src/configuration.rs does NOT contain `pub struct StoppingCriteria` (grep returns 0)
    - src/configuration.rs GaConfiguration contains `pub(crate) stagnation_generations: Option<usize>` (grep returns 1)
    - src/configuration.rs GaConfiguration contains `pub(crate) convergence_threshold: Option<f64>` (grep returns 1)
    - src/configuration.rs GaConfiguration contains `pub(crate) max_duration_secs: Option<f64>` (grep returns 1)
    - src/configuration.rs `impl GaConfiguration` has accessors `pub fn stagnation_generations`, `pub fn convergence_threshold`, `pub fn max_duration_secs` (each grep returns >=1)
    - src/traits/configuration.rs contains `fn with_stagnation_limit`, `fn with_convergence_threshold`, `fn with_max_duration_secs`
    - src/traits/configuration.rs does NOT contain `fn with_stopping_criteria` (grep returns 0)
    - The `max_duration_secs` field declaration has no `#[cfg(...)]` attribute (preserves PATTERNS.md guidance)
  </acceptance_criteria>
  <done>StoppingCriteria struct dissolved into three flat fields with builder methods and accessors; structural assertions GREEN; ga.rs path update handled in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Encapsulate GaConfiguration sub-struct fields with pub(crate) + accessors and update ga.rs paths</name>
  <files>src/configuration.rs, src/engines/ga.rs</files>
  <read_first>
    - src/configuration.rs (GaConfiguration struct after Task 1; identify remaining sub-struct fields still `pub`)
    - src/engines/ga.rs (full file — every `self.configuration.stopping_criteria.X` path; every direct field read)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ pub(crate) field + accessor pattern; § src/engines/ga.rs StoppingCriteria path change pattern; § WASM Gate Pattern)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md (D-09)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 4 — multi-obj engines read fields directly; those callers are migrated in 47-06)
    - CLAUDE.md (WASM mandatory)
  </read_first>
  <action>
    Step A — Encapsulation in src/configuration.rs. For each sub-struct field in `GaConfiguration` that is currently `pub`, change visibility to `pub(crate)`. Fields covered: adaptive_ga, limit_configuration, selection_configuration, crossover_configuration, mutation_configuration, survivor_configuration, extension_configuration, log_configuration, and any other fields present (RESEARCH.md cites lines 312-352). Add sub-struct-level read-only accessors per D-09 on `impl GaConfiguration`: `pub fn limit(&self) -> &LimitConfiguration`, `pub fn selection(&self) -> &SelectionConfiguration`, `pub fn crossover(&self) -> &CrossoverConfiguration`, `pub fn mutation(&self) -> &MutationConfiguration`, `pub fn survivor(&self) -> &SurvivorConfiguration`, `pub fn extension(&self) -> &ExtensionConfiguration`, `pub fn log(&self) -> &LogConfiguration` (use actual sub-struct type names from configuration.rs). Add `pub fn adaptive_ga(&self) -> bool` accessor.

    Step B — Update src/engines/ga.rs to the new flat-field paths. Replace `self.configuration.stopping_criteria.stagnation_generations` -> `self.configuration.stagnation_generations`. Same for `convergence_threshold` and `max_duration_secs`. Use `sed -i ''`: `sed -i '' 's/self\.configuration\.stopping_criteria\./self.configuration./g' src/engines/ga.rs`. Then verify with grep that `stopping_criteria` no longer appears in ga.rs.

    Step C — Verify the WASM gate pattern is preserved at the usage site exactly as in PATTERNS.md § WASM Gate Pattern. The `#[cfg(not(target_arch = "wasm32"))]` block around `if let Some(max_secs) = self.configuration.max_duration_secs` must remain. The `#[cfg(target_arch = "wasm32")]` warn-once block at run start must remain. Confirm both with grep.

    Step D — Run `cargo check --lib`. Expected residual errors:
    - Multi-objective engine files reading `self.ga_config.limit_configuration.alleles_can_be_repeated` etc. (fields no longer exist post-47-04) — fixed in 47-06.
    - Examples that read or write `pub` sub-struct fields via direct access — fixed in 47-06.
    The ga.rs changes from THIS task introduce NO new error of their own.

    Step E — Verify WASM. Run `cargo check --target wasm32-unknown-unknown --lib`. May still surface errors from multi-obj engines — fixed in 47-06. The ga.rs-specific changes contribute NO new wasm32 errors.

    Commit message: `refactor(47-05): encapsulate GaConfiguration with pub(crate) + sub-struct accessors; update ga.rs paths to flat stopping fields`.
  </action>
  <verify>
    <automated>grep -c 'stopping_criteria' src/engines/ga.rs; grep -E 'pub fn (limit|selection|crossover|mutation|survivor|extension|log|adaptive_ga)\b' src/configuration.rs | wc -l; grep -c '#\[cfg(not(target_arch = "wasm32"))\]' src/engines/ga.rs</automated>
  </verify>
  <acceptance_criteria>
    - src/engines/ga.rs contains 0 occurrences of `stopping_criteria` (grep returns 0)
    - src/configuration.rs `impl GaConfiguration` has at least 8 sub-struct/scalar accessors (grep for `pub fn (limit|selection|crossover|mutation|survivor|extension|log|adaptive_ga)\b` returns >= 8)
    - All previously `pub` sub-struct fields in GaConfiguration are now `pub(crate)` (grep: `grep -E '^[[:space:]]*pub [a-z_]+_configuration:' src/configuration.rs` returns 0; `grep -E '^[[:space:]]*pub\(crate\) [a-z_]+_configuration:' src/configuration.rs` returns at least 7)
    - src/engines/ga.rs preserves at least one `#[cfg(not(target_arch = "wasm32"))]` block around a `max_duration_secs` usage (grep proof: `grep -B 1 'self.configuration.max_duration_secs' src/engines/ga.rs | grep -c 'cfg(not(target_arch'` returns >= 1)
    - src/engines/ga.rs preserves the wasm32 warn-once block (grep proof: `grep -B 1 'max_duration_secs is not supported' src/engines/ga.rs | grep -c 'cfg(target_arch = "wasm32")'` returns >= 1; if the warn message wording differs, check `grep -c 'cfg(target_arch = "wasm32")' src/engines/ga.rs` returns >= 1)
    - `cargo check --lib` failures (if any) are exclusively about (a) `no field 'alleles_can_be_repeated' / 'needs_unique_ids' / 'genes_per_chromosome'` carried over from 47-04, or (b) `pub` -> `pub(crate)` access violations from multi-obj engines / examples / tests. NO failures attributable to the ga.rs path change in this task.
  </acceptance_criteria>
  <done>GaConfiguration encapsulated with sub-struct accessors; ga.rs uses flat stopping field paths with preserved WASM gate; residual compile errors scoped to caller migration in 47-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GaConfiguration public API -> downstream consumers | Field encapsulation is a breaking change for any code that directly read/wrote sub-struct fields; intentional per ARCH-04 ("user cannot accidentally bypass builder validation"). |
| WASM target -> ga.rs runtime | Time-based stopping criterion must NOT call Instant::now() on wasm32; gate preserved at usage site only, field is plain Option<f64> on all targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-13 | Denial of Service (WASM regression on time-based stop) | src/engines/ga.rs max_duration_secs site | mitigate | Task 2 acceptance_criteria asserts the `#[cfg(not(target_arch = "wasm32"))]` and `#[cfg(target_arch = "wasm32")]` blocks are preserved around the max_duration_secs read; `cargo check --target wasm32-unknown-unknown` runs after 47-06 lands |
| T-47-14 | Tampering (silent semver break on GaConfiguration field visibility) | configuration.rs | mitigate | MIGRATION.md (plan 47-07) documents `pub` -> `pub(crate)` plus the accessor pattern; D-13 PR 2 boundary surfaces it as a single coherent change |
| T-47-15 | Information Disclosure (accessors leak internal state) | sub-struct accessors | accept | Accessors return `&SubStruct` not `&mut SubStruct`; read-only by design (D-09). Mutation only via builder methods. |
</threat_model>

<verification>
- After Task 1: structural assertions GREEN (StoppingCriteria gone, flat fields present, builder methods present, accessors present)
- After Task 2: ga.rs paths updated; WASM gates preserved; no new compile errors attributable to encapsulation work
- Wave 0 test_stopping_config GREEN after 47-06 completes the remaining caller migrations (single-engine path may already pass earlier — confirm)
- `cargo check --target wasm32-unknown-unknown --lib` becomes fully GREEN at end of 47-06
</verification>

<success_criteria>
- StoppingCriteria struct removed from type system
- 3 flat stopping fields on GaConfiguration with builders and accessors
- GaConfiguration sub-struct fields are pub(crate) with sub-struct accessors per D-09
- ga.rs uses flat field paths with preserved WASM gate at call site
- Wave 0 test covers the flat builders
- No new compile or WASM errors introduced beyond the caller migrations expected for 47-06
</success_criteria>

<output>
Create `.planning/phases/47-architecture-audit-chromosomet-split/47-05-SUMMARY.md` when done. Summary lists the precise list of remaining compile errors (file:line) as the hand-off to plan 47-06.
</output>
