---
phase: 47-architecture-audit-chromosomet-split
plan: 07
type: execute
wave: 7
depends_on:
  - 47-06
files_modified:
  - src/lib.rs
  - src/engines/ga.rs
  - src/observe/reporter/mod.rs
  - src/observe/reporter/duration.rs
  - src/observe/reporter/noop.rs
  - src/observe/reporter/simple.rs
  - src/observe/mod.rs
  - tests/test_observe.rs
  - tests/
  - examples/
  - MIGRATION.md
  - README.md
  - Cargo.toml
autonomous: true
requirements:
  - ARCH-03
pr_group: 3
tags:
  - rust
  - reporter-removal
  - breaking-change
  - documentation
  - migration

must_haves:
  truths:
    - "src/observe/reporter/ directory is deleted (all 4 files)"
    - "src/lib.rs no longer exposes `pub mod reporter` or any reporter re-export"
    - "src/engines/ga.rs has zero reporter fire points and no `reporter` field on Ga<U>; `with_reporter` builder method removed"
    - "Any test or example referencing Reporter / SimpleReporter / DurationReporter / NoopReporter / with_reporter is migrated to GaObserver or removed"
    - "MIGRATION.md exists at crate root with before/after examples for every Phase 47 breaking change (D-11)"
    - "Cargo.toml `include` array contains 'MIGRATION.md'"
    - "README.md links to MIGRATION.md"
    - "`cargo test`, `cargo test --features serde`, clippy, wasm32 check, and doc-zero-warnings all pass"
  artifacts:
    - path: "MIGRATION.md"
      provides: "v3.0.0 breaking changes migration guide"
      contains: "## Reporter → GaObserver"
    - path: "src/lib.rs"
      provides: "No pub mod reporter; LinearChromosome and ChromosomeLength remain exported"
  key_links:
    - from: "README.md"
      to: "MIGRATION.md"
      via: "markdown link"
      pattern: "MIGRATION\\.md"
    - from: "Cargo.toml"
      to: "MIGRATION.md"
      via: "include array entry"
      pattern: "MIGRATION.md"
---

<objective>
Remove the `Reporter<U>` trait, its 4 implementations (`SimpleReporter`, `DurationReporter`, `NoopReporter`, the trait file), and all 4 fire points in `src/engines/ga.rs` plus the `with_reporter()` builder per D-10. Migrate any tests or examples still referencing reporter types to `GaObserver<U>` or delete them if redundant. Publish `MIGRATION.md` at the crate root covering all Phase 47 breaking changes per D-11, link it from README.md, and add it to the Cargo.toml `include` array so it ships with the crate.

Purpose: First plan of PR 3. Satisfies ARCH-03 (Reporter removal + migration guide). After this plan, the only remaining work is the examples-smoke.yml CI workflow + final phase verification gate (plan 47-08).

Output: Deleted reporter module; clean ga.rs orchestrator; MIGRATION.md published with full before/after content; README link; Cargo.toml `include` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Reporter API to remove (D-10): -->
<!-- - pub trait Reporter<U: ChromosomeT> (deprecated since 2.2.0) -->
<!-- - SimpleReporter, DurationReporter, NoopReporter impls -->
<!-- - Ga<U>.reporter: Option<Box<dyn Reporter<U> + Send>> field -->
<!-- - Ga<U>.with_reporter(...) builder method -->
<!-- - 4 fire points in src/engines/ga.rs (~lines 1447, 1974-1975, 2060, 2125 per RESEARCH.md) -->
<!-- - pub mod reporter / pub use reporter::... re-exports in src/lib.rs -->

<!-- Replacement (already present, no work needed): GaObserver<U> in src/observe/observer/ -->

<!-- MIGRATION.md content per D-11 (full breaking changes guide): -->
<!-- 1. Reporter → GaObserver (with before/after Rust code blocks) -->
<!-- 2. genes_per_chromosome → ChromosomeLength::Fixed(n) -->
<!-- 3. StoppingCriteria struct → .with_stagnation_limit / .with_convergence_threshold / .with_max_duration_secs -->
<!-- 4. LimitConfiguration field removals (needs_unique_ids, alleles_can_be_repeated) -->
<!-- 5. ChromosomeT → ChromosomeT + LinearChromosome split (impl pattern guide) -->
<!-- 6. with_genes_per_chromosome → with_chromosome_length -->
<!-- 7. GaConfiguration sub-struct field access → accessor methods (.limit(), .selection(), etc.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Reporter trait + impls + all fire points in ga.rs + clean lib.rs re-exports</name>
  <files>src/lib.rs, src/engines/ga.rs, src/observe/reporter/mod.rs, src/observe/reporter/duration.rs, src/observe/reporter/noop.rs, src/observe/reporter/simple.rs, src/observe/mod.rs, tests/test_observe.rs</files>
  <read_first>
    - src/observe/reporter/mod.rs (full Reporter trait definition + deprecation annotation)
    - src/observe/reporter/duration.rs (DurationReporter impl)
    - src/observe/reporter/noop.rs (NoopReporter impl)
    - src/observe/reporter/simple.rs (SimpleReporter impl)
    - src/engines/ga.rs (locate `reporter` field on Ga<U> struct ~line 278-280; `with_reporter` builder ~line 848-858; 4 fire points ~lines 1447, 1974-1975, 2060, 2125 per 47-RESEARCH.md and 47-PATTERNS.md)
    - src/lib.rs (locate `#[path = "observe/reporter/mod.rs"] pub mod reporter` ~line 266 per 47-PATTERNS.md; also any `pub use reporter::*` re-export)
    - src/observe/mod.rs (locate any sub-module declaration of reporter)
    - tests/test_observe.rs (lines 12-14 reference `mod reporter { mod test_reporter; }` per 47-PATTERNS.md — find the actual reporter test file)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ src/observe/reporter/ deletion checklist; § src/engines/ga.rs reporter removal patterns; § Deprecated Attribute + Removal Pattern)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 5 — tests/ may reference Reporter types)
  </read_first>
  <action>
    Step A — Delete the reporter module files:
    1. `rm src/observe/reporter/mod.rs`
    2. `rm src/observe/reporter/duration.rs`
    3. `rm src/observe/reporter/noop.rs`
    4. `rm src/observe/reporter/simple.rs`
    5. `rmdir src/observe/reporter` (empty directory)

    Step B — Remove the reporter module declaration from src/lib.rs:
    1. Find the `#[path = "observe/reporter/mod.rs"] pub mod reporter;` line (~line 266 per PATTERNS.md) and DELETE it.
    2. Find any `pub use reporter::*;`, `pub use crate::reporter::Reporter;`, or related re-exports and DELETE them.

    Step C — Remove the reporter declaration from src/observe/mod.rs (if any submodule declares it).

    Step D — Clean src/engines/ga.rs:
    1. Remove the `reporter` field from the `Ga<U>` struct definition (~line 278-280 per PATTERNS.md).
    2. Remove the import lines `#[allow(deprecated)] use crate::reporter::Reporter;` (~lines 140-141 per PATTERNS.md). Also remove any `use crate::observe::reporter::...` lines.
    3. Remove the `with_reporter(...)` builder method block (~lines 848-858 per PATTERNS.md). This includes the `#[allow(deprecated)]` and `#[deprecated(since = ..., note = ...)]` attribute lines.
    4. Remove all 4 reporter fire points (per PATTERNS.md § Reporter fire point removal pattern — locations: on_start ~1447, on_generation_complete ~1974-1975, on_new_best ~2060, on_finish ~2125-2126). Each is a 1-3 line `if let Some(ref mut r) = self.reporter { ... }` block.
    5. Remove the `reporter` initialization in `Ga::new()` / `Ga::default()` / the struct constructor (whatever pattern is used) — likely `reporter: None` somewhere.

    Step E — Clean tests/. Grep for reporter usage in tests:
    ```
    grep -rln 'Reporter\|with_reporter\|SimpleReporter\|DurationReporter\|NoopReporter' tests/
    ```
    For each hit:
    - If the test file is `tests/test_observe.rs` and references a sub-module that contained reporter tests, REMOVE the `mod reporter { mod test_reporter; }` declaration AND delete the corresponding `tests/observe/reporter/` directory if it exists.
    - If a test instantiates `SimpleReporter` etc. for purposes unrelated to testing Reporter itself (e.g., as a quick way to log during a different test), replace with `LogObserver` from `crate::observe::observer::log_observer` (or whichever is the canonical small observer for testing). Use the GaObserver replacement pattern from MIGRATION.md (Task 2).
    - If a test exists solely to test Reporter behavior, DELETE the test file — its target no longer exists.

    Step F — Clean examples/. Grep for reporter usage:
    ```
    grep -rln 'Reporter\|with_reporter\|SimpleReporter\|DurationReporter\|NoopReporter' examples/
    ```
    Per RESEARCH.md "zero example usage" is the expected baseline, but verify anyway. If any hits, migrate to GaObserver-based equivalent (LogObserver is the simplest direct replacement).

    Step G — Compile gate: `cargo check --lib` GREEN, `cargo check --target wasm32-unknown-unknown --lib` GREEN, `cargo test --lib` GREEN.

    Commit message: `refactor(47-07): remove deprecated Reporter trait and impls; delete src/observe/reporter/`.
  </action>
  <verify>
    <automated>test ! -d src/observe/reporter &amp;&amp; echo "reporter dir deleted" || (echo "FAIL: reporter dir still exists" &amp;&amp; exit 1); grep -c 'pub mod reporter' src/lib.rs; grep -c 'reporter' src/engines/ga.rs; cargo check --lib 2>&amp;1 | tail -5; cargo test --lib 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/observe/reporter/ directory does not exist
    - src/lib.rs contains 0 occurrences of `pub mod reporter` (grep returns 0)
    - src/lib.rs contains 0 `use ...reporter::` or `pub use reporter::` lines (grep -E 'reporter::' src/lib.rs returns 0)
    - src/engines/ga.rs `Ga<U>` struct definition does NOT contain a `reporter:` field (grep: `grep -E 'reporter:.*Option<Box<dyn Reporter' src/engines/ga.rs` returns 0)
    - src/engines/ga.rs does NOT contain `with_reporter` method (grep: `grep 'fn with_reporter' src/engines/ga.rs` returns 0)
    - src/engines/ga.rs contains 0 reporter fire points (grep: `grep -c 'self.reporter' src/engines/ga.rs` returns 0)
    - tests/ contains 0 references to Reporter trait or its impls (grep: `grep -rln 'Reporter\|SimpleReporter\|DurationReporter\|NoopReporter\|with_reporter' tests/` returns nothing; any false positives e.g. `GaObserver` are fine because they do not match — verify carefully if the term `Reporter` appears as a substring inside something benign)
    - examples/ contains 0 references to Reporter (same grep returns nothing)
    - `cargo check --lib` exits 0
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - `cargo test --lib` exits 0
  </acceptance_criteria>
  <done>Reporter trait + 4 impls + all 4 fire points + builder method + struct field + module re-exports all removed. Tests and examples migrated to GaObserver where needed. Library and lib tests GREEN on native + WASM.</done>
</task>

<task type="auto">
  <name>Task 2: Publish MIGRATION.md, link from README, register in Cargo.toml include array</name>
  <files>MIGRATION.md, README.md, Cargo.toml</files>
  <read_first>
    - README.md (find a sensible insertion point for the migration link; usually near "## Installation" or at the top under the title)
    - Cargo.toml (locate the `include = [...]` array; if absent, the publish defaults are used — but D-11 requires explicit inclusion of MIGRATION.md)
    - src/observe/observer/mod.rs (GaObserver trait — the replacement; need its method signatures for the MIGRATION.md Reporter → GaObserver section)
    - src/observe/observer/log_observer.rs (concrete observer example for the migration code block)
    - src/chromosomes/length.rs (ChromosomeLength variants for the migration code block)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md (D-11 — full breaking changes guide)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ State of the Art table — has the canonical before/after summary)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ MIGRATION.md — no analog, create from scratch)
  </read_first>
  <action>
    Step A — Create `MIGRATION.md` at the crate root (sibling of README.md and Cargo.toml). Required sections (one per Phase 47 breaking change), each with a `Before` Rust code block and an `After` Rust code block:

    1. `# Migrating to v3.0.0` (top heading + one-paragraph overview that links the user to the v2.4.0 → v3.0.0 changelog)
    2. `## Trait split: ChromosomeT + LinearChromosome` — explain D-01/D-02; show before (single impl with all methods) and after (two impl blocks). Show a custom non-linear chromosome implementing ChromosomeT only (no LinearChromosome) to illustrate the new freedom.
    3. `## Reporter -> GaObserver` — explain D-10. Show before: a `Ga::default().with_reporter(Box::new(SimpleReporter::default()))` setup. Show after: `Ga::default().with_observer(Arc::new(LogObserver::default()))`. Reference docs.rs link to GaObserver.
    4. `## ChromosomeLength replaces genes_per_chromosome` — show `.with_genes_per_chromosome(8)` before, `.with_chromosome_length(ChromosomeLength::Fixed(8))` after. Mention `ChromosomeLength::Variable { min, max }` will come in Phase 52.
    5. `## Flat stopping builders replace StoppingCriteria` — show before: `.with_stopping_criteria(StoppingCriteria { stagnation_generations: Some(50), convergence_threshold: Some(0.001), max_duration_secs: Some(60.0) })`. After: `.with_stagnation_limit(50).with_convergence_threshold(0.001).with_max_duration_secs(60.0)`.
    6. `## LimitConfiguration field removals (needs_unique_ids, alleles_can_be_repeated)` — explain that these flags are removed without replacement (D-06); users who need permutation enforcement should wait for Phase 48's UniqueChromosome<T> or use a custom `with_initialization_fn` closure that builds permutations manually.
    7. `## GaConfiguration field access -> accessor methods` — show before: `let n = config.limit_configuration.genes_per_chromosome`. After: `let len = config.limit().chromosome_length`. Explain `pub(crate)` rationale.
    8. `## `default()` method renamed to `reset()` on LinearChromosome` — explain D-03; show before/after.

    Each code block should be runnable Rust (or pseudo-runnable — minimal context). Total length: 250-400 lines. Use docs.rs-style direct prose, no fluff.

    Step B — Update README.md. Locate an appropriate spot (top under the badges, or under "## Installation"). Add a single highlighted line/paragraph:
    ```
    > **v3.0.0 users:** see [MIGRATION.md](./MIGRATION.md) for the full list of breaking changes and migration recipes.
    ```

    Step C — Update Cargo.toml `include` array. Read the current `include` value (if it exists). Add `"MIGRATION.md"` to the array. If no `include` array exists, add it with the standard entries plus MIGRATION.md:
    ```
    include = [
        "/src",
        "/examples",
        "/tests",
        "/benches",
        "/Cargo.toml",
        "/README.md",
        "/LICENSE",
        "/MIGRATION.md",
    ]
    ```
    The exact entries depend on what is already there — preserve existing entries and only ADD `"MIGRATION.md"`. Confirm by running `cargo package --list 2>&1 | grep MIGRATION` (or `cargo publish --dry-run --allow-dirty 2>&1 | grep -i 'migration\|413'` to ensure the crate still packages within size limits — note from STATE.md commit history that v2.2.1 had a 413 issue resolved by excluding planning dirs).

    Step D — Run a final compile + doc check: `cargo check --all-features`, `cargo doc --no-deps --all-features` (zero warnings), `cargo test --doc` (verifies code blocks in rustdoc — MIGRATION.md is plain markdown so not subject to this, but make sure README.md links resolve in cargo doc output).

    Step E — Verify MIGRATION.md ships with the crate package. Run `cargo package --list --allow-dirty 2>&1 | grep -E 'MIGRATION.md|README.md|Cargo.toml'`. All three must appear.

    Commit message: `docs(47-07): publish MIGRATION.md for v3.0.0 breaking changes; include in package and link from README`.
  </action>
  <verify>
    <automated>test -f MIGRATION.md &amp;&amp; wc -l MIGRATION.md &amp;&amp; grep -c '^## ' MIGRATION.md; grep -c 'MIGRATION.md' README.md; grep -c '"MIGRATION.md"' Cargo.toml; cargo package --list --allow-dirty 2>&amp;1 | grep -c 'MIGRATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - MIGRATION.md exists at the crate root
    - MIGRATION.md contains all 7 required `## ` sections (grep: `grep -c '^## ' MIGRATION.md` returns >= 7)
    - MIGRATION.md contains a section titled exactly `## Reporter` followed by `GaObserver` (or `## Reporter → GaObserver` / `## Reporter -> GaObserver`) — grep `grep -E '## Reporter' MIGRATION.md` returns >= 1
    - MIGRATION.md contains a section about ChromosomeLength (grep: `grep -E '## ChromosomeLength' MIGRATION.md` returns >= 1)
    - MIGRATION.md contains a section about flat stopping builders (grep: `grep -iE '## .*[Ss]topping' MIGRATION.md` returns >= 1)
    - MIGRATION.md contains a section about the trait split (grep: `grep -iE '## .*[Ll]inear[Cc]hromosome' MIGRATION.md` returns >= 1)
    - README.md contains a markdown link to MIGRATION.md (grep: `grep -c '\[.*\](\./MIGRATION.md)' README.md` returns >= 1; or simpler: `grep -c 'MIGRATION.md' README.md` returns >= 1)
    - Cargo.toml `include` array contains "MIGRATION.md" (grep: `grep -c '"MIGRATION.md"' Cargo.toml` returns >= 1)
    - `cargo package --list --allow-dirty` output includes `MIGRATION.md` (grep: returns >= 1)
    - `cargo doc --no-deps --all-features` produces zero warnings
  </acceptance_criteria>
  <done>MIGRATION.md published with full v3.0.0 breaking-change content; README linked; Cargo.toml ships it. ARCH-03 fully satisfied. PR 3 is one step from mergeable (only the examples-smoke.yml workflow remains, plan 47-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crate user code -> GaObserver | Users who relied on Reporter must now use GaObserver. MIGRATION.md is the only documented path to discover the replacement. |
| Cargo publish flow -> crate package size | Adding MIGRATION.md to `include` must not push the crate over crates.io's size limit (see STATE.md note: v2.2.1 hit a 413 on crates.io). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-19 | Tampering (API removal without migration path) | Reporter removal | mitigate | MIGRATION.md publishes complete before/after Reporter -> GaObserver guide (D-11); README.md links it; Cargo.toml ships it |
| T-47-20 | Denial of Service (crate publish 413) | Cargo.toml include array | mitigate | Task 2 runs `cargo package --list` as part of acceptance to verify MIGRATION.md ships AND total package size remains within crates.io limit; commit message references the v2.2.1 lesson |
| T-47-21 | Information Disclosure (test file deletion strips coverage) | tests/observe/reporter/* | accept | Reporter trait deleted -> tests for it are no longer applicable; GaObserver coverage already established in v2.2.0 phases; net coverage loss is zero |
</threat_model>

<verification>
- Task 1 verify: reporter dir removed; lib + WASM + lib tests GREEN
- Task 2 verify: MIGRATION.md content checks; README + Cargo.toml include; cargo package --list includes MIGRATION.md
- Final phase verification gate (cargo test full + clippy + WASM full + doc-zero-warnings) deferred to plan 47-08 after examples-smoke.yml lands
</verification>

<success_criteria>
- Reporter trait, 3 impls, 4 fire points, struct field, builder method, module declaration, lib.rs re-exports all removed
- Tests/examples that referenced Reporter migrated or deleted
- MIGRATION.md published with all 7 breaking-change sections + before/after code
- README.md links MIGRATION.md
- Cargo.toml `include` ships MIGRATION.md
- `cargo doc --no-deps --all-features` zero warnings
- ARCH-03 fully satisfied
</success_criteria>

<output>
Create `.planning/phases/47-architecture-audit-chromosomet-split/47-07-SUMMARY.md` when done.
</output>
