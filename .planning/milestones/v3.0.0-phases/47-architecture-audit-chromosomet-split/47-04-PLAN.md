---
phase: 47-architecture-audit-chromosomet-split
plan: 04
type: execute
wave: 4
depends_on:
  - 47-03
files_modified:
  - src/chromosomes/length.rs
  - src/chromosomes/mod.rs
  - src/lib.rs
  - src/configuration.rs
  - src/traits/configuration.rs
  - src/initializers/binary_initializer.rs
  - src/initializers/range_initializer.rs
  - src/initializers/list_initializer.rs
  - src/initializers/mod.rs
  - tests/test_chromosome_length.rs
autonomous: true
requirements:
  - ARCH-04
  - ARCH-05
pr_group: 2
tags:
  - rust
  - configuration
  - enum-type
  - breaking-change

must_haves:
  truths:
    - "User can construct `ChromosomeLength::Fixed(n)` and `ChromosomeLength::Variable { min, max }`"
    - "User imports ChromosomeLength via `use genetic_algorithms::ChromosomeLength`"
    - "LimitConfiguration uses `chromosome_length: ChromosomeLength` (pub(crate)) instead of `genes_per_chromosome: usize`"
    - "LimitConfiguration no longer has `needs_unique_ids` or `alleles_can_be_repeated` fields"
    - "Initializer functions for Binary/Range/List drop the `_needs_unique_ids` parameter and accept length input compatible with ChromosomeLength::Fixed"
    - "Wave 0 test `test_chromosome_length` passes"
  artifacts:
    - path: "src/chromosomes/length.rs"
      provides: "ChromosomeLength standalone public enum"
      contains: "pub enum ChromosomeLength"
    - path: "src/configuration.rs"
      provides: "LimitConfiguration with chromosome_length field; needs_unique_ids/alleles_can_be_repeated removed"
      contains: "chromosome_length: ChromosomeLength"
    - path: "src/traits/configuration.rs"
      provides: "with_chromosome_length builder method"
      contains: "fn with_chromosome_length"
  key_links:
    - from: "src/lib.rs"
      to: "src/chromosomes/length.rs"
      via: "public re-export"
      pattern: "pub use .*ChromosomeLength"
    - from: "src/configuration.rs"
      to: "src/chromosomes/length.rs"
      via: "type usage in LimitConfiguration"
      pattern: "chromosome_length: ChromosomeLength"
---

<objective>
Introduce the standalone `ChromosomeLength` enum, swap `LimitConfiguration.genes_per_chromosome: usize` for `chromosome_length: ChromosomeLength`, and remove the `needs_unique_ids` and `alleles_can_be_repeated` fields from `LimitConfiguration` per D-06. Update the three built-in initializers to drop the now-removed parameters. Add the Wave 0 test for `ChromosomeLength`.

Purpose: First plan of PR 2. Implements ARCH-05 (ChromosomeLength type) and the LimitConfiguration half of ARCH-04 (field encapsulation prep + field removals). After this plan, `cargo check --lib` may surface callers across engines/examples — those callers are migrated in plan 47-06.

Output: New `src/chromosomes/length.rs`, modified configuration.rs and traits/configuration.rs, three updated initializer files, new Wave 0 test, and a partial-compile state where engine call sites still use the old field name (resolved in 47-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- New type per D-07: -->
pub enum ChromosomeLength {
    Fixed(usize),
    Variable { min: usize, max: usize },
}
<!-- Derives: Copy, Clone, Debug, PartialEq + cfg-gated serde Serialize/Deserialize -->

<!-- LimitConfiguration changes per D-06 + ARCH-05: -->
<!-- REMOVE: pub genes_per_chromosome: usize -->
<!-- REMOVE: pub needs_unique_ids: bool -->
<!-- REMOVE: pub alleles_can_be_repeated: bool -->
<!-- ADD: pub(crate) chromosome_length: ChromosomeLength -->

<!-- Initializer signature changes per D-06: -->
<!-- Before: pub fn binary_random_initialization(genes_per_chromosome: usize, _alleles: Option<&[BinaryGenotype]>, _needs_unique_ids: Option<bool>) -> Vec<BinaryGenotype> -->
<!-- After:  pub fn binary_random_initialization(genes_per_chromosome: usize, _alleles: Option<&[BinaryGenotype]>) -> Vec<BinaryGenotype> -->
<!-- Same shape applied to range_initializer and list_initializer (each loses the trailing _needs_unique_ids parameter) -->

<!-- Builder method changes per ARCH-05: -->
<!-- src/traits/configuration.rs (LimitConfig trait): -->
<!-- REMOVE: fn with_genes_per_chromosome(self, n: usize) -> Self -->
<!-- REMOVE: fn with_needs_unique_ids(...) -->
<!-- REMOVE: fn with_alleles_can_be_repeated(...) -->
<!-- ADD: fn with_chromosome_length(self, length: ChromosomeLength) -> Self -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChromosomeLength enum + Wave 0 test + re-exports</name>
  <files>src/chromosomes/length.rs, src/chromosomes/mod.rs, src/lib.rs, tests/test_chromosome_length.rs</files>
  <read_first>
    - src/chromosomes/mod.rs (current module aggregator; identify how binary.rs and range.rs are declared)
    - src/operations.rs (Selection enum at lines 43-70 — exact derive+serde pattern to copy)
    - src/configuration.rs (LimitConfiguration default impl; need to know current default genes_per_chromosome value to set sensible Default for the new field)
    - src/lib.rs (locate the existing `pub use crate::traits::ChromosomeT` or equivalent re-export — ChromosomeLength goes alongside it)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ src/chromosomes/length.rs section; § serde Feature Gate Pattern)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - `ChromosomeLength::Fixed(8)` constructible
    - `ChromosomeLength::Variable { min: 2, max: 16 }` constructible
    - Type is `Copy + Clone + Debug + PartialEq`
    - With `--features serde`, type round-trips via `serde_json::to_string` + `serde_json::from_str`
    - Test `test_chromosome_length_variants` constructs both variants and checks equality
    - Test `test_chromosome_length_default_is_fixed_zero` asserts `ChromosomeLength::default() == ChromosomeLength::Fixed(0)` if Default is implemented (D-07 example uses Default-able LimitConfiguration so a sensible default is needed)
    - Test `test_chromosome_length_serde_roundtrip` (gated `#[cfg(feature = "serde")]`) verifies serialization shape
  </behavior>
  <action>
    Create `src/chromosomes/length.rs` with the enum exactly as in 47-PATTERNS.md § `src/chromosomes/length.rs`. Derives: `#[derive(Copy, Clone, Debug, PartialEq)]` + `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`. Variants: `Fixed(usize)` and `Variable { min: usize, max: usize }`. Add `impl Default for ChromosomeLength { fn default() -> Self { ChromosomeLength::Fixed(0) } }` to make it embeddable in `Default`-deriving config structs.

    Update `src/chromosomes/mod.rs` to add `pub mod length;` and `pub use length::ChromosomeLength;`.

    Update `src/lib.rs` to re-export `ChromosomeLength` from the crate root alongside ChromosomeT — find the existing `pub use ...ChromosomeT;` line and add `pub use crate::chromosomes::ChromosomeLength;` immediately after it.

    Create `tests/test_chromosome_length.rs` with three tests as described in <behavior>. The serde roundtrip test is gated `#[cfg(feature = "serde")]`. Use `serde_json` (already a dev/optional dep — verify via Cargo.toml before assuming).

    Verify: `cargo check --lib` GREEN; `cargo test --test test_chromosome_length` GREEN; `cargo test --test test_chromosome_length --features serde` GREEN; `cargo check --target wasm32-unknown-unknown --lib` GREEN.

    Do NOT yet touch LimitConfiguration or the initializers — that is Task 2.

    Commit message: `feat(47-04): add ChromosomeLength enum with Fixed and Variable variants`.
  </action>
  <verify>
    <automated>cargo check --lib 2>&amp;1 | tail -5 &amp;&amp; cargo test --test test_chromosome_length 2>&amp;1 | tail -5 &amp;&amp; cargo test --test test_chromosome_length --features serde 2>&amp;1 | tail -5 &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/chromosomes/length.rs exists and contains `pub enum ChromosomeLength` with `Fixed(usize)` and `Variable { min: usize, max: usize }` variants
    - src/chromosomes/length.rs has `#[derive(Copy, Clone, Debug, PartialEq)]`
    - src/chromosomes/length.rs has `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]`
    - src/chromosomes/length.rs has `impl Default for ChromosomeLength` returning `Fixed(0)`
    - src/chromosomes/mod.rs declares `pub mod length;` and re-exports `ChromosomeLength`
    - src/lib.rs contains `pub use crate::chromosomes::ChromosomeLength` (or equivalent path) — grep verifies one match
    - tests/test_chromosome_length.rs contains `fn test_chromosome_length_variants` and `fn test_chromosome_length_default_is_fixed_zero` and a serde-gated `fn test_chromosome_length_serde_roundtrip`
    - `cargo test --test test_chromosome_length` exits 0
    - `cargo test --test test_chromosome_length --features serde` exits 0
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - LimitConfiguration field unchanged in this task (grep: `grep -E 'genes_per_chromosome|needs_unique_ids|alleles_can_be_repeated' src/configuration.rs` still returns the old fields — confirms scope is limited to type introduction)
  </acceptance_criteria>
  <done>ChromosomeLength enum exists, is publicly re-exported, has serde support, passes Wave 0 tests on native and WASM.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate LimitConfiguration to chromosome_length + remove deprecated fields + update initializer signatures</name>
  <files>src/configuration.rs, src/traits/configuration.rs, src/initializers/binary_initializer.rs, src/initializers/range_initializer.rs, src/initializers/list_initializer.rs, src/initializers/mod.rs</files>
  <read_first>
    - src/configuration.rs (LimitConfiguration struct definition, Default impl, current builder bodies)
    - src/traits/configuration.rs (LimitConfig trait — `with_genes_per_chromosome`, `with_needs_unique_ids`, `with_alleles_can_be_repeated` method declarations)
    - src/initializers/binary_initializer.rs (full file; signature with `_needs_unique_ids: Option<bool>`)
    - src/initializers/range_initializer.rs (full file; same parameter to remove)
    - src/initializers/list_initializer.rs (full file; same parameter to remove)
    - src/initializers/mod.rs (the `InitializationFn<G>` type alias — its signature MUST change to drop the third arg per RESEARCH.md Open Question 2)
    - src/chromosomes/length.rs (the new type from Task 1)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ src/configuration.rs, § src/initializers/binary_initializer.rs)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md (D-06, D-07)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 2 — alleles_can_be_repeated is read by 8 multi-obj engine files; that breakage is EXPECTED and will be fixed in 47-06)
  </read_first>
  <action>
    Step A — Update LimitConfiguration in src/configuration.rs:
    1. Remove the `pub genes_per_chromosome: usize` field. Replace with `pub(crate) chromosome_length: ChromosomeLength`.
    2. Remove the `pub needs_unique_ids: bool` field entirely.
    3. Remove the `pub alleles_can_be_repeated: bool` field entirely.
    4. Update the `impl Default for LimitConfiguration` body to drop the two removed defaults and set `chromosome_length: ChromosomeLength::default()`.
    5. Add `use crate::chromosomes::ChromosomeLength;` to the imports.

    Step B — Update the LimitConfig trait in src/traits/configuration.rs:
    1. Remove the trait method declarations `with_genes_per_chromosome`, `with_needs_unique_ids`, `with_alleles_can_be_repeated` (also remove from any blanket impl in the same file).
    2. Add the trait method declaration `fn with_chromosome_length(self, length: ChromosomeLength) -> Self;`.
    3. Add `use crate::chromosomes::ChromosomeLength;` to the imports.

    Step C — Update the LimitConfig impl on GaConfiguration (still in src/configuration.rs or traits/configuration.rs depending on the existing layout):
    1. Implement `with_chromosome_length` on whatever type currently implements LimitConfig — most likely the GaConfiguration builder impl block. Body: assigns `self.limit_configuration.chromosome_length = length` and returns `self`.
    2. Remove the corresponding old method impls for `with_genes_per_chromosome`/`with_needs_unique_ids`/`with_alleles_can_be_repeated`.

    Step D — Update the three initializer functions:
    1. binary_initializer.rs: drop the trailing `_needs_unique_ids: Option<bool>` parameter from `binary_random_initialization`. Keep `genes_per_chromosome: usize` and `_alleles: Option<&[BinaryGenotype]>`.
    2. range_initializer.rs: same parameter drop on `range_random_initialization`.
    3. list_initializer.rs: same parameter drop on `list_random_initialization`.

    Step E — Update the InitializationFn type alias in src/initializers/mod.rs (or wherever it lives — find with `grep -rn 'type InitializationFn' src/`):
    - Before (likely): `pub type InitializationFn<G> = fn(usize, Option<&[G]>, Option<bool>) -> Vec<G>;`
    - After: `pub type InitializationFn<G> = fn(usize, Option<&[G]>) -> Vec<G>;`
    Update all call sites that store/pass this type — there will be at least 3 in the initializer files plus engine wiring. Engine call site updates that take the now-shortened function pointer signature happen here (these are local fixes in src/initializers/ + src/configuration.rs); broader engine/example callers are PR 2 follow-up in plan 47-06.

    Step F — Run `cargo check --lib`. Expected output: errors at all `genes_per_chromosome`, `needs_unique_ids`, `alleles_can_be_repeated` read/write sites OUTSIDE the configuration module. Specifically:
    - src/engines/ga.rs (~26 references per RESEARCH.md)
    - src/engines/{nsga2,nsga3,moead,spea2,sms_emoa,ibea,island}/mod.rs (8 references to `alleles_can_be_repeated` per RESEARCH.md)
    - tests/* and examples/* may also break
    These breakages are EXPECTED and will be fixed by plan 47-05 (StoppingCriteria + GaConfiguration encapsulation) and plan 47-06 (caller migration). Do NOT fix them in this task — those are scoped to subsequent plans.

    Compile gate at the END of THIS task: `cargo check --lib` is allowed to fail with errors that are exclusively about the three removed field names. Capture the failure list as evidence that the breakage is scoped correctly. If failures reference anything else (e.g., LinearChromosome regressions), debug before declaring done.

    Commit message: `refactor(47-04): LimitConfiguration uses ChromosomeLength; remove needs_unique_ids and alleles_can_be_repeated`.
  </action>
  <verify>
    <automated>cargo check --lib 2>&amp;1 | tee /tmp/47-04-task2-check.log; grep -cE 'no field `(genes_per_chromosome|needs_unique_ids|alleles_can_be_repeated)`' /tmp/47-04-task2-check.log &amp;&amp; grep -E 'no field `(genes_per_chromosome|needs_unique_ids|alleles_can_be_repeated)`' /tmp/47-04-task2-check.log | sort -u | head -50</automated>
  </verify>
  <acceptance_criteria>
    - src/configuration.rs LimitConfiguration struct definition contains `pub(crate) chromosome_length: ChromosomeLength`
    - src/configuration.rs LimitConfiguration does NOT contain `genes_per_chromosome: usize` (grep: `grep 'genes_per_chromosome: usize' src/configuration.rs` returns nothing)
    - src/configuration.rs LimitConfiguration does NOT contain `needs_unique_ids` or `alleles_can_be_repeated` as fields (grep: `grep -E 'pub (needs_unique_ids|alleles_can_be_repeated)' src/configuration.rs` returns nothing)
    - src/traits/configuration.rs contains `fn with_chromosome_length(self, length: ChromosomeLength) -> Self;` in the LimitConfig trait
    - src/traits/configuration.rs does NOT contain `fn with_genes_per_chromosome`, `fn with_needs_unique_ids`, or `fn with_alleles_can_be_repeated` (grep returns nothing for each)
    - src/initializers/binary_initializer.rs `binary_random_initialization` signature has exactly 2 parameters (grep: `grep -A 4 'pub fn binary_random_initialization' src/initializers/binary_initializer.rs` does NOT include `_needs_unique_ids`)
    - src/initializers/range_initializer.rs and list_initializer.rs analogously drop `_needs_unique_ids`
    - src/initializers/mod.rs `InitializationFn<G>` type alias has exactly 2 parameters (fn pointer signature)
    - `cargo check --lib` may FAIL — but failures must be exclusively `no field 'genes_per_chromosome'`, `no field 'needs_unique_ids'`, or `no field 'alleles_can_be_repeated'` errors at call sites in src/engines/ and possibly tests/examples. Number of distinct such errors recorded in task output (expected: ≥ 20 unique sites per RESEARCH.md counts). NO OTHER unrelated compile errors are acceptable.
  </acceptance_criteria>
  <done>LimitConfiguration migrated to ChromosomeLength; deprecated fields removed; initializer signatures cleaned. Lib build intentionally surfaces caller breakage at the expected sites for 47-05 + 47-06 to resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LimitConfiguration public API → engine code, examples, tests | Removing `genes_per_chromosome` field (with no rename) is a breaking change; the compile error IS the migration signal per CONTEXT.md design intent ("makes the change auditable"). |
| ChromosomeLength enum → downstream user code | New public type; first piece of variable-length-chromosome support (Phase 52 will use the `Variable` variant). Phase 47 only proves `Fixed(n)` is behavior-equivalent to the old `usize`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-10 | Tampering (silent semver-incompatible field rename) | LimitConfiguration | mitigate | v3.0.0 milestone explicitly breaking; MIGRATION.md (47-07) documents `genes_per_chromosome → with_chromosome_length(ChromosomeLength::Fixed(n))` |
| T-47-11 | Denial of Service (WASM regression on serde of ChromosomeLength) | length.rs serde derive | mitigate | Cfg-gated derive; Task 1 verify step runs `cargo check --target wasm32-unknown-unknown --lib` |
| T-47-12 | Information Disclosure (test fixtures might depend on old fields) | tests/ | mitigate | Plan 47-06 audits all tests/ for old-field usage and migrates them via builder API |
</threat_model>

<verification>
- After Task 1: `cargo test --test test_chromosome_length` GREEN; lib compile GREEN; WASM GREEN.
- After Task 2: lib compile fails with field-not-found errors at the expected ~28 sites; NO other unrelated errors. This is the documented hand-off to plan 47-05 (which restores GREEN by flattening StoppingCriteria and adding accessors) and plan 47-06 (which migrates the remaining call sites).
</verification>

<success_criteria>
- ChromosomeLength enum exists, is re-exported, supports serde, has Wave 0 tests
- LimitConfiguration drops genes_per_chromosome / needs_unique_ids / alleles_can_be_repeated
- Three built-in initializer signatures and the InitializationFn type alias drop the `_needs_unique_ids` parameter
- Builder API trait has `with_chromosome_length` replacing the three removed methods
- Lib compile failures are scoped exclusively to old-field-name errors at downstream call sites — no unrelated breakage
</success_criteria>

<output>
Create `.planning/phases/47-architecture-audit-chromosomet-split/47-04-SUMMARY.md` when done. Summary lists the exact count and locations of remaining `genes_per_chromosome` / `needs_unique_ids` / `alleles_can_be_repeated` errors as the hand-off boundary to 47-05 and 47-06.
</output>
