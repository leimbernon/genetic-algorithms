---
phase: 47-architecture-audit-chromosomet-split
plan: 06
type: execute
wave: 6
depends_on:
  - 47-05
files_modified:
  - src/engines/ga.rs
  - src/engines/nsga2/mod.rs
  - src/engines/nsga3/mod.rs
  - src/engines/moead/mod.rs
  - src/engines/spea2/mod.rs
  - src/engines/sms_emoa/mod.rs
  - src/engines/ibea/mod.rs
  - src/engines/island/mod.rs
  - src/engines/de.rs
  - src/engines/scatter.rs
  - src/engines/cellular.rs
  - src/engines/alps.rs
  - examples/knapsack_binary.rs
  - examples/onemax_binary.rs
  - examples/onemax_extension.rs
  - examples/rastrigin.rs
  - examples/nsga2_zdt1.rs
  - examples/nsga3_dtlz2.rs
  - examples/moead_dtlz2.rs
  - examples/spea2_zdt1.rs
  - examples/sms_emoa_zdt1.rs
  - examples/ibea_zdt1.rs
  - examples/island_model.rs
  - examples/job_scheduling.rs
  - examples/niching.rs
  - examples/hall_of_fame_demo.rs
  - examples/aos_demo.rs
  - tests/
autonomous: true
requirements:
  - ARCH-04
  - ARCH-05
  - ARCH-06
pr_group: 2
tags:
  - rust
  - caller-migration
  - examples
  - tests
  - phase-gate

must_haves:
  truths:
    - "All engine modules read removed-field replacements via the new chromosome_length / stagnation_generations paths; all alleles_can_be_repeated and needs_unique_ids reads removed"
    - "All 19 examples build and run without referencing removed fields or the removed StoppingCriteria struct"
    - "All tests in tests/ compile and pass with the new builder API"
    - "ChromosomeLength::Fixed(n) is used everywhere `genes_per_chromosome: n` used to appear in user-facing call sites"
    - "PR 2 phase verification gate is GREEN: cargo test, cargo test --features serde, cargo clippy --all-features -D warnings, cargo check --target wasm32-unknown-unknown, cargo doc zero warnings"
  artifacts:
    - path: "examples/job_scheduling.rs"
      provides: "Migrated to .with_chromosome_length(ChromosomeLength::Fixed(N)) builder pattern (alleles_can_be_repeated removal)"
    - path: "src/engines/nsga2/mod.rs"
      provides: "alleles_can_be_repeated/needs_unique_ids reads removed; ChromosomeLength wiring through initializer"
  key_links:
    - from: "examples/*.rs"
      to: "src/chromosomes/length.rs"
      via: "ChromosomeLength::Fixed(n) usage"
      pattern: "ChromosomeLength::Fixed"
    - from: "src/engines/nsga2/mod.rs"
      to: "src/configuration.rs"
      via: "self.ga_config.limit().chromosome_length read via accessor"
      pattern: "\\.limit\\(\\)|chromosome_length"
---

<objective>
Migrate every remaining caller in src/engines/ (multi-objective + alt-metaheuristic engines), every example, and every test that previously read `genes_per_chromosome`, `needs_unique_ids`, `alleles_can_be_repeated`, or the `StoppingCriteria` struct, or that directly accessed now-pub(crate) sub-struct fields. Wire `ChromosomeLength` through engine initialization. Run the PR 2 phase verification gate.

Purpose: Final plan of PR 2. After this plan, ARCH-04, ARCH-05, ARCH-06 are fully satisfied and PR 2 (config cleanup) is mergeable to milestone/v3.0.0.

Output: All engine + example + test compile errors carried over from 47-04 and 47-05 resolved. Full cargo test / clippy / WASM / doc-warning gates GREEN. PR 2 ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-04-SUMMARY.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- After 47-04 + 47-05, the new public/crate API surface is: -->
<!-- - ChromosomeLength enum (crate-root re-export): use genetic_algorithms::ChromosomeLength -->
<!-- - LimitConfiguration.chromosome_length (pub(crate)) — accessed within crate via direct field, externally via .limit().chromosome_length or via .limit().chromosome_length pattern match -->
<!-- - GaConfiguration accessors: .limit(), .selection(), .crossover(), .mutation(), .survivor(), .extension(), .log(), .adaptive_ga(), .stagnation_generations(), .convergence_threshold(), .max_duration_secs() -->
<!-- - Builder: .with_chromosome_length(ChromosomeLength), .with_stagnation_limit(usize), .with_convergence_threshold(f64), .with_max_duration_secs(f64) -->

<!-- Removed surface (compile errors flag the migration sites): -->
<!-- - LimitConfiguration.genes_per_chromosome -->
<!-- - LimitConfiguration.needs_unique_ids -->
<!-- - LimitConfiguration.alleles_can_be_repeated -->
<!-- - StoppingCriteria struct -->
<!-- - ga_config.limit_configuration / .selection_configuration / ... direct field access from outside the crate -->
<!-- - .with_genes_per_chromosome / .with_needs_unique_ids / .with_alleles_can_be_repeated / .with_stopping_criteria builder methods -->

<!-- Initializer signature (per 47-04): -->
<!-- pub type InitializationFn<G> = fn(usize, Option<&[G]>) -> Vec<G>; -->
<!-- Engine code that constructs initial populations now reads chromosome_length out of LimitConfiguration. -->
<!-- For ChromosomeLength::Fixed(n) → pass `n` to InitializationFn -->
<!-- For ChromosomeLength::Variable { min, max } → sample length once (uniform in [min, max]) per chromosome and pass that — but Phase 47 only proves Fixed equivalence; Variable handling is Phase 52. -->
<!-- In THIS plan, engines must: (a) match on chromosome_length and pass the Fixed value to initializers; (b) for Variable, panic or error with a clear message that variable-length init is not yet supported (Phase 52). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate engine code (src/engines/*) — remove alleles_can_be_repeated/needs_unique_ids reads, wire ChromosomeLength through initializers</name>
  <files>src/engines/ga.rs, src/engines/nsga2/mod.rs, src/engines/nsga3/mod.rs, src/engines/moead/mod.rs, src/engines/spea2/mod.rs, src/engines/sms_emoa/mod.rs, src/engines/ibea/mod.rs, src/engines/island/mod.rs, src/engines/de.rs, src/engines/scatter.rs, src/engines/cellular.rs, src/engines/alps.rs</files>
  <read_first>
    - src/engines/ga.rs (existing population initialization code; ~26 `genes_per_chromosome` references)
    - src/engines/nsga2/mod.rs (existing population initialization; reads `alleles_can_be_repeated`)
    - src/engines/island/mod.rs (wraps inner engine)
    - src/configuration.rs (post 47-05 — LimitConfiguration shape; chromosome_length accessor)
    - src/chromosomes/length.rs (ChromosomeLength variants)
    - src/initializers/binary_initializer.rs (post 47-04 — new 2-arg signature)
    - src/initializers/range_initializer.rs (same)
    - src/initializers/list_initializer.rs (same)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-04-SUMMARY.md (list of error sites)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-05-SUMMARY.md (list of error sites)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 2 — 16 references in 8 multi-obj engine files; § Common Pitfalls 4)
  </read_first>
  <action>
    Step A — Replace all `genes_per_chromosome` field reads inside src/engines/*. The crate-local read pattern becomes: extract the `Fixed(n)` value from `self.ga_config.limit_configuration.chromosome_length` (or via the `.limit()` accessor where appropriate within the crate; direct `self.ga_config.limit_configuration.chromosome_length` works since visibility is `pub(crate)`).

    Concrete pattern to use everywhere an engine previously read `genes_per_chromosome: usize`:
    ```
    let length = match self.ga_config.limit_configuration.chromosome_length {
        ChromosomeLength::Fixed(n) => n,
        ChromosomeLength::Variable { .. } => {
            log::error!(target: "ga_events", "ChromosomeLength::Variable is not supported by this engine yet (Phase 52). Use ChromosomeLength::Fixed.");
            return Err(GaError::InvalidConfiguration("variable-length chromosomes require Phase 52 support".into()));
        }
    };
    ```
    Where the engine's init code did not have a Result return type, fall back to `panic!` with the same message. (Phase 52 will replace the panic/error with proper variable-length population init.) Choose the appropriate one based on each engine's existing error-handling convention.

    Step B — Remove all `alleles_can_be_repeated` and `needs_unique_ids` field reads. Per D-06 these are removed without replacement; initializers (now 2-arg) no longer take a uniqueness flag. Audit each multi-obj engine and the island engine — RESEARCH.md says 16 references in 8 files. Use `grep -rn 'alleles_can_be_repeated\|needs_unique_ids' src/engines/` to enumerate. For each hit:
    - Delete the read, and the parameter being passed to the initializer (since the initializer signature dropped the parameter in 47-04).
    - If the code branched on the value, delete the branching — collapse to the "no uniqueness enforcement" path.

    Step C — Audit all initializer call sites. After 47-04, calls like `binary_random_initialization(genes, alleles, needs_unique_ids)` should become `binary_random_initialization(genes, alleles)`. Same for range_random_initialization and list_random_initialization. The `genes` argument is the extracted Fixed length from Step A.

    Step D — Compile gate. Run `cargo check --lib`. All carryover errors from 47-04 / 47-05 should now be resolved within src/engines/. Remaining errors should be exclusively in examples/ and tests/ (Task 2). If lib compiles, run `cargo check --target wasm32-unknown-unknown --lib` — must also be GREEN.

    Commit message: `refactor(47-06): migrate engines to ChromosomeLength + drop alleles_can_be_repeated/needs_unique_ids reads`.
  </action>
  <verify>
    <automated>cargo check --lib 2>&amp;1 | tail -15 &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -10 &amp;&amp; grep -c 'alleles_can_be_repeated\|needs_unique_ids\|genes_per_chromosome' src/engines/*.rs src/engines/*/mod.rs 2>/dev/null | grep -v ':0$' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --lib` exits 0
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - No engine file in src/engines/ contains the strings `alleles_can_be_repeated`, `needs_unique_ids`, or `genes_per_chromosome` as field-access syntax (grep: `grep -E '\.(alleles_can_be_repeated|needs_unique_ids|genes_per_chromosome)\b' src/engines/ -r` returns 0)
    - Each engine file that initializes a population matches on `ChromosomeLength::Fixed(n)` and either passes `n` forward or errors/panics on `Variable` with a clear message (grep: `grep -l 'ChromosomeLength::Fixed' src/engines/ -r` includes at least the 12 engine files in files_modified)
    - Initializer call sites pass exactly 2 args to `binary_random_initialization` / `range_random_initialization` / `list_random_initialization` (audit: `grep -A 2 'random_initialization(' src/engines/ -r | head -40` shows 2 arguments per call)
  </acceptance_criteria>
  <done>All engine code compiles against the new configuration API; ChromosomeLength is wired through population init; carryover errors from 47-04/47-05 resolved at the engine layer.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate examples + tests; run PR 2 phase verification gate</name>
  <files>examples/knapsack_binary.rs, examples/onemax_binary.rs, examples/onemax_extension.rs, examples/rastrigin.rs, examples/nsga2_zdt1.rs, examples/nsga3_dtlz2.rs, examples/moead_dtlz2.rs, examples/spea2_zdt1.rs, examples/sms_emoa_zdt1.rs, examples/ibea_zdt1.rs, examples/island_model.rs, examples/job_scheduling.rs, examples/niching.rs, examples/hall_of_fame_demo.rs, examples/aos_demo.rs, tests/</files>
  <read_first>
    - examples/knapsack_binary.rs (representative single-objective example)
    - examples/nsga2_zdt1.rs (representative multi-objective example; per RESEARCH.md Pitfall 4 uses direct field access)
    - examples/job_scheduling.rs (uses uniqueness — historically relied on `alleles_can_be_repeated = false`; for Phase 47 it must compile with the old hack removed — Phase 48 replaces it with UniqueChromosome; for now make it work using a manual permutation initializer per the existing example's intent)
    - examples/island_model.rs (uses direct ga_config field access per RESEARCH.md)
    - tests/test_observe.rs, tests/structures.rs, tests/integration tests (grep for `genes_per_chromosome`, `StoppingCriteria`, `alleles_can_be_repeated`, `needs_unique_ids`, `with_stopping_criteria`, `with_genes_per_chromosome`)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 4 — 6 examples use direct field access; § Common Pitfalls 5 — tests/ may reference removed types)
  </read_first>
  <action>
    Step A — Audit all examples for removed API usage:
    ```
    grep -rln 'genes_per_chromosome\|alleles_can_be_repeated\|needs_unique_ids\|StoppingCriteria\|with_stopping_criteria\|with_genes_per_chromosome\|with_needs_unique_ids\|with_alleles_can_be_repeated' examples/
    ```
    For each hit:
    - Replace `with_genes_per_chromosome(n)` with `with_chromosome_length(ChromosomeLength::Fixed(n))`. Add `use genetic_algorithms::ChromosomeLength;` to the example.
    - Replace `with_stopping_criteria(StoppingCriteria { stagnation_generations: Some(s), convergence_threshold: Some(c), max_duration_secs: Some(d) })` with chained `.with_stagnation_limit(s).with_convergence_threshold(c).with_max_duration_secs(d)`. Drop the StoppingCriteria import.
    - Delete `with_needs_unique_ids(...)` and `with_alleles_can_be_repeated(...)` calls outright.
    - For examples that used direct field access (`ga_config.limit_configuration.genes_per_chromosome = N`), replace with the builder chain. Per RESEARCH.md Pitfall 4, this applies to: `sms_emoa_zdt1`, `nsga2_zdt1`, `nsga3_dtlz2`, `ibea_zdt1`, `spea2_zdt1`, `moead_dtlz2`, `island_model`.

    Step B — Migrate examples/job_scheduling.rs to the new API. This example previously used `alleles_can_be_repeated = false` to enforce unique IDs at initialization. Since D-06 removes that flag, Phase 47 cannot keep the hack. Per ROADMAP Phase 48 the proper fix is `UniqueChromosome<i32>`, but Phase 47 must still compile. Solution: replace the random-initialization closure with an explicit permutation-aware initializer (use `with_initialization_fn` with a closure that builds a shuffled `Vec<RangeGenotype<i32>>` from the job alphabet). Document this as a temporary measure in the example's top comment block: "Phase 47 compile-fix only; Phase 48 will migrate to UniqueChromosome<i32>." This keeps GEN-02 honest (Phase 48 owns the proper migration).

    Step C — Audit tests/ for removed API usage:
    ```
    grep -rln 'genes_per_chromosome\|alleles_can_be_repeated\|needs_unique_ids\|StoppingCriteria\|with_stopping_criteria\|with_genes_per_chromosome\|with_needs_unique_ids\|with_alleles_can_be_repeated\|Reporter\|with_reporter\|SimpleReporter\|DurationReporter\|NoopReporter' tests/
    ```
    Note Reporter-related strings — those are removed by plan 47-07, not here. For 47-06 scope, ONLY migrate the ChromosomeLength / StoppingCriteria / LimitConfiguration items. Leave Reporter references for 47-07 (they will compile through 47-06 because Reporter still exists at this PR boundary).

    Apply the same replacement patterns from Step A to each tests/ file.

    Step D — Run the PR 2 phase verification gate. All five must pass:
    1. `cargo test`
    2. `cargo test --features serde`
    3. `cargo clippy --all-features -- -D warnings`
    4. `cargo check --target wasm32-unknown-unknown`
    5. `cargo doc --no-deps --all-features` zero warnings

    Step E — Sanity-run a subset of examples manually to confirm the new builder chain produces the same behavior as before:
    - `cargo run --example knapsack_binary -- --release` runs and prints a best fitness within a short time bound
    - `cargo run --example nsga2_zdt1 --release` runs (multi-obj examples may need `--features` — check Cargo.toml for required features and add them to the command if applicable)
    - `cargo run --example job_scheduling --release` runs and produces a valid schedule (no duplicate job IDs in output — manual eyeball verification is acceptable here; Phase 48's UniqueChromosome will make this typesafe).

    Commit message: `refactor(47-06): migrate examples and tests to ChromosomeLength + flat stopping builders; PR 2 gate green`.
  </action>
  <verify>
    <automated>cargo test 2>&amp;1 | tail -10 &amp;&amp; cargo test --features serde 2>&amp;1 | tail -10 &amp;&amp; cargo clippy --all-features -- -D warnings 2>&amp;1 | tail -10 &amp;&amp; cargo check --target wasm32-unknown-unknown 2>&amp;1 | tail -10 &amp;&amp; cargo doc --no-deps --all-features 2>&amp;1 | grep -cE 'warning|error' || true</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0
    - `cargo test --features serde` exits 0
    - `cargo clippy --all-features -- -D warnings` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - `cargo doc --no-deps --all-features` zero warnings
    - No example file in examples/ contains `genes_per_chromosome`, `alleles_can_be_repeated`, `needs_unique_ids`, `StoppingCriteria`, `with_stopping_criteria`, `with_genes_per_chromosome`, `with_needs_unique_ids`, or `with_alleles_can_be_repeated` (grep: `grep -rln 'genes_per_chromosome\|alleles_can_be_repeated\|needs_unique_ids\|StoppingCriteria\|with_stopping_criteria\|with_genes_per_chromosome\|with_needs_unique_ids\|with_alleles_can_be_repeated' examples/` returns 0 files)
    - No test file in tests/ contains those strings either (same grep with `tests/` returns 0 files for non-Reporter strings; Reporter-related strings ARE still present at this PR boundary — that is deferred to 47-07)
    - Each multi-obj example contains `ChromosomeLength::Fixed` (grep: `grep -l 'ChromosomeLength::Fixed' examples/nsga2_zdt1.rs examples/nsga3_dtlz2.rs examples/moead_dtlz2.rs examples/spea2_zdt1.rs examples/sms_emoa_zdt1.rs examples/ibea_zdt1.rs` returns 6 paths)
    - examples/job_scheduling.rs compiles and produces a valid permutation when run (manual smoke test; record output in task summary)
    - Cargo.toml `include` array still contains its existing entries (do not modify yet — that is plan 47-07's job for MIGRATION.md)
  </acceptance_criteria>
  <done>PR 2 phase verification gate is GREEN. ARCH-04, ARCH-05, ARCH-06 fully satisfied. PR 2 (config cleanup) is ready to open against milestone/v3.0.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Builder API -> downstream user code | Examples are the canonical user-facing surface; their migration is the executable proof that the new API produces the same behavior with cleaner ergonomics. |
| WASM target -> full crate | Multi-objective engines now must NOT regress on wasm32; full `cargo check --target wasm32-unknown-unknown` is the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-16 | Tampering (silent behavior regression on Variable variant) | engine population init | mitigate | Variable variant explicitly errors/panics with a clear "Phase 52 support" message; behavior change is fail-loud not silent |
| T-47-17 | Denial of Service (WASM regression in multi-obj engines) | sms_emoa, ibea, moead, spea2 | mitigate | Task 2 phase verification gate runs `cargo check --target wasm32-unknown-unknown`; CI also enforces |
| T-47-18 | Information Disclosure (job_scheduling temporarily uses bespoke permutation init) | examples/job_scheduling.rs | accept | Documented as a Phase 47 compile-fix; GEN-02 (Phase 48) replaces with UniqueChromosome<i32>; risk is purely UX (slightly less clean example until Phase 48) |
</threat_model>

<verification>
## PR 2 Phase Verification Gate (this plan)
- `cargo test` exits 0
- `cargo test --features serde` exits 0
- `cargo clippy --all-features -- -D warnings` exits 0
- `cargo check --target wasm32-unknown-unknown` exits 0
- `cargo doc --no-deps --all-features` zero warnings
- Manual smoke run of `knapsack_binary`, `nsga2_zdt1`, `job_scheduling` examples confirms behavioral parity
</verification>

<success_criteria>
- All multi-obj + alt-metaheuristic engines compile against ChromosomeLength + the encapsulated GaConfiguration
- All 19 examples build and 3 representative examples run to completion
- All tests/ migrate the same APIs (Reporter strings deferred to 47-07)
- PR 2 phase verification gate GREEN — PR 2 mergeable
</success_criteria>

<output>
Create `.planning/phases/47-architecture-audit-chromosomet-split/47-06-SUMMARY.md` when done. Summary lists the precise PR 2 boundary contents and the Reporter-related cleanup items deferred to PR 3 (plans 47-07 and 47-08).
</output>
