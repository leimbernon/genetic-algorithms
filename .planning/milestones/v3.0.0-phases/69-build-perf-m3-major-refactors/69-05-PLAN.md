---
phase: 69-build-perf-m3-major-refactors
plan: 05
type: execute
wave: 2
depends_on: [69-04]
files_modified:
  - docs/ARCHITECTURE.md
  - .planning/intel/ga-internals.md
  - CHANGELOG.md
  - README.md
  - src/lib.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/ARCHITECTURE.md module map reflects the 11-submodule layout under src/engines/ga/"
    - ".planning/intel/ga-internals.md documents each submodule's responsibility for future agents"
    - "CHANGELOG.md v3.0.0 records the ga.rs split as Changed (internal)"
    - "README.md and src/lib.rs documentation references no longer mention src/engines/ga.rs as a single file"
    - "All BUILD-PERF.md acceptance-gate checks pass against the final phase 69 state"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "Updated module map showing engines/ga/ directory with 11 submodules"
      contains: "engines/ga/"
    - path: ".planning/intel/ga-internals.md"
      provides: "AI-readable submodule responsibilities and visibility rules"
    - path: "CHANGELOG.md"
      provides: "v3.0.0 Changed (internal) entry for ga.rs split"
      contains: "engines/ga"
  key_links:
    - from: "docs/ARCHITECTURE.md"
      to: "src/engines/ga/"
      via: "module map text"
      pattern: "engines/ga/"
    - from: ".planning/intel/ga-internals.md"
      to: "src/engines/ga/{mod,lifecycle,generation,...}.rs"
      via: "responsibility table"
      pattern: "lifecycle.rs"
---

<objective>
Documentation sweep closing phase 69: update docs/ARCHITECTURE.md module map, write .planning/intel/ga-internals.md, finalise CHANGELOG.md and README.md and src/lib.rs references for the ga.rs split, then run the BUILD-PERF.md acceptance gate one final time to confirm phase 69 ships.

Purpose: Wave M3 Action #4 docs deliverables (BUILD-PERF.md §Action #4). The intel file is the artefact that prevents future agents from re-merging the submodules or putting code in the wrong file. The final acceptance-gate run confirms all 6 BUILD-PERF.md success criteria are met across the entire phase.

Revert plan: revert this plan's commits to restore the pre-phase ARCHITECTURE.md, CHANGELOG.md, README.md, src/lib.rs documentation and remove .planning/intel/ga-internals.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/69-build-perf-m3-major-refactors/69-CONTEXT.md
@.planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md
@.planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/ARCHITECTURE.md module map and write .planning/intel/ga-internals.md</name>
  <files>docs/ARCHITECTURE.md, .planning/intel/ga-internals.md</files>
  <read_first>
    - docs/ARCHITECTURE.md (current module map and any mention of engines/ga.rs as a single file)
    - .planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md (Submodule Extraction Map at lines ~417–432 for verbatim responsibilities; Recommended File Layout at lines ~170–203)
    - .planning/v3.0.0-BUILD-PERF.md (Action #4 docs deliverables and proposed split section)
    - src/engines/ga/mod.rs and the 10 sibling files (verify actual responsibilities after the split — the canonical source is the code, not the spec)
    - .planning/phases/69-build-perf-m3-major-refactors/69-04-SUMMARY.md (from plan 69-04 — commit SHAs and confirmed structure)
  </read_first>
  <action>
    Step 1 — docs/ARCHITECTURE.md update:
    - Locate any "module map" table or section describing src/engines/ga.rs. Replace the single-file row with the directory layout: src/engines/ga/ containing mod.rs (orchestrator: Ga struct, builders, build, run, run_with_callback), lifecycle.rs (init_population, initialize_random, initialize_with_seeds, finalise_run), generation.rs (per-generation loop body), adaptive.rs (adaptive probabilities), aos.rs (Adaptive Operator Selection), extension.rs (extension trigger + diversity check), cache.rs (fitness cache helpers), batch.rs (batch_evaluate helper), stats.rs (GenerationStats collection), observer.rs (observer hook dispatch), stopping.rs (limit_reached + stopping criteria).
    - Preserve the existing markdown style (table vs bullets) used elsewhere in ARCHITECTURE.md.
    - Add a one-line note: "Split from a single 3342-line file in Phase 69 Action #4. See .planning/intel/ga-internals.md for per-submodule responsibilities and visibility rules."

    Step 2 — create .planning/intel/ga-internals.md as a 1–2 page AI-readable note with these exact section headers:

    `## Why this layout exists` — rationale per BUILD-PERF.md Action #4 (single most-loaded compilation unit; rustc frontend parallelisation; faster incremental compile).

    `## Submodule responsibilities` — a table mapping each of the 11 files to its responsibility, taken from the 69-RESEARCH.md Submodule Extraction Map (lines ~417–432). Use the same column structure as RESEARCH (file, key items, visibility).

    `## Visibility rules (D-11)` — bullet list:
      - Currently-pub items (Ga, run, build, stats, hall_of_fame) MUST remain pub. Zero public surface change.
      - Items called from 2+ ga submodules use pub(crate). Pitfall 4: pub(super) is NOT visible to siblings.
      - Items called only from mod.rs use pub(super).
      - Items only used within their own submodule use pub(super) or no modifier.

    `## What an agent must NOT reintroduce` — bullet list:
      - Do NOT re-merge submodules into a single ga.rs.
      - Do NOT add new fields to Ga<U> or new pub items without verifying cargo public-api diff is intentional.
      - Do NOT remove or bypass notify() call sites in generation.rs / observer.rs (CLAUDE.md observability mandate).
      - Do NOT escalate pub(super) → pub or pub(crate) → pub without an explicit v3.x SemVer-additive review.

    `## How to verify the invariant` — commands:
      - cargo expand --lib piped through pub-line grep+sort to capture the symbol table; diff against .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt baseline (captured in 69-04 Task 1).
      - cargo public-api (or +nightly if required) to verify zero public-api drift.
      - cargo test --all-features and cargo test --no-default-features --features logging both pass.

    `## Where to put new code` — decision tree for future agents adding features to the GA:
      - New stopping criterion → stopping.rs.
      - New cache strategy → cache.rs.
      - New stat field → stats.rs (and the GenerationStats struct).
      - New observer hook → observer.rs (preserve existing notify call sites).
      - New per-generation step → generation.rs (within the existing loop body).
      - New init strategy → lifecycle.rs.
      - New adaptive parameter → adaptive.rs.
      - New AOS algorithm → aos.rs.
      - New extension strategy → extension.rs.
      - New batch helper → batch.rs.
      - Builder API change or top-level orchestrator change → mod.rs.

    Commit message: docs(69): update ARCHITECTURE module map and ship ga-internals intel for the split
    Commit body MUST include: Revert plan: revert this commit to restore the pre-split ARCHITECTURE.md and remove ga-internals.md
  </action>
  <verify>
    <automated>grep -c 'engines/ga/' docs/ARCHITECTURE.md | grep -q '^[1-9]' &amp;&amp; ! grep -q '^.*engines/ga\.rs.*single' docs/ARCHITECTURE.md &amp;&amp; test -f .planning/intel/ga-internals.md &amp;&amp; grep -c 'Submodule responsibilities' .planning/intel/ga-internals.md | grep -q '^[1-9]' &amp;&amp; grep -c 'Visibility rules' .planning/intel/ga-internals.md | grep -q '^[1-9]' &amp;&amp; grep -c 'must NOT reintroduce' .planning/intel/ga-internals.md | grep -q '^[1-9]' &amp;&amp; grep -c 'How to verify' .planning/intel/ga-internals.md | grep -q '^[1-9]' &amp;&amp; grep -c 'Where to put new code' .planning/intel/ga-internals.md | grep -q '^[1-9]' &amp;&amp; cargo doc --no-deps 2>&amp;1 | (grep -c warning || true) | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - docs/ARCHITECTURE.md contains references to engines/ga/ directory and mentions at least mod.rs, lifecycle.rs, generation.rs, observer.rs, stopping.rs
    - .planning/intel/ga-internals.md exists
    - .planning/intel/ga-internals.md contains all 6 required section headers verbatim: "Why this layout exists", "Submodule responsibilities", "Visibility rules", "What an agent must NOT reintroduce", "How to verify the invariant", "Where to put new code"
    - cargo doc --no-deps produces zero warnings
    - Commit body contains Revert plan line
  </acceptance_criteria>
  <done>ARCHITECTURE.md module map reflects 11-submodule layout; ga-internals.md ships with rationale, responsibilities, visibility rules, anti-patterns, verification commands, and a "where to put new code" decision tree.</done>
</task>

<task type="auto">
  <name>Task 2: Final CHANGELOG entry, README/lib.rs sweep, and BUILD-PERF.md acceptance-gate confirmation</name>
  <files>CHANGELOG.md, README.md, src/lib.rs</files>
  <read_first>
    - CHANGELOG.md (current v3.0.0 section — needs Changed (internal) entry for the ga.rs split)
    - README.md (search for any references to src/engines/ga.rs as a single file)
    - src/lib.rs (search for any module-level doc comments referencing src/engines/ga.rs as a single file)
    - .planning/v3.0.0-BUILD-PERF.md (Acceptance gate per action — 9-gate merge checklist)
    - .planning/phases/69-build-perf-m3-major-refactors/69-04-SUMMARY.md (for the symbol/public-api diff confirmation)
  </read_first>
  <action>
    Step 1 — CHANGELOG.md final entries for phase 69:
    - Verify the following entries are present under v3.0.0 (some already added by earlier plans in this phase; this is the final consistency sweep):
      - Changed (internal): "Bench harness migrated from criterion to divan (Phase 69 Action #7). No public-API change." (added by 69-01).
      - Added: "parallel feature (default-on). Disable via default-features = false to shed rayon + crossbeam dependencies for embedded / wasm-only builds. Phase 69 Action #3." (added by 69-03).
      - Changed (internal): "engines/ga.rs (3342 lines) split into 11 submodules under src/engines/ga/. No API change; zero semantic change verified via cargo expand symbol diff. Phase 69 Action #4." (ADD in this task if not present).
    - If any of those are missing or worded differently, normalise. Order entries within v3.0.0 under their respective buckets (Added, Changed, Removed) per Keep-a-Changelog convention.

    Step 2 — README.md sweep:
    - Search for any references to src/engines/ga.rs as a single file (e.g., in code-walk documentation, file-size remarks, or contributor guides). Update to refer to src/engines/ga/ as a directory. If README has a "Project Structure" or "Codebase tour" section, update it.
    - If README has no such reference, no change needed in README — document this in the SUMMARY.

    Step 3 — src/lib.rs sweep:
    - Search src/lib.rs for module-level doc comments mentioning ga.rs. The #[path] attribute was updated in 69-04; check that ANY doc comment referring to "the GA engine in engines/ga.rs" or similar is updated to "the GA engine in engines/ga/". Likely only one or two lines; if none, no change needed.

    Step 4 — Final BUILD-PERF.md acceptance-gate confirmation (all 9 gates):
    Run each verification and record the result in the SUMMARY (not in source). For each of the 9 gates list pass or fail.
    Gate 1 — CC-1 build_perf script declared Δ: skip if CC-1 infrastructure was not exercised this phase; otherwise run bash bench/build_perf.sh and diff against baseline.
    Gate 2 — CC-3 golden tests byte-identical: cargo test --test golden_tests --all-features AND cargo test --test golden_tests --no-default-features --features logging both pass. Adapt test target name if different (check ls tests/golden/).
    Gate 3 — CC-2 feature-matrix CI green on every supported combination: not directly runnable locally; verify .github/workflows/feature-matrix.yml has the parallel-off combination AND the grep enforcement step (from plan 69-03).
    Gate 4 — WASM check passes: cargo check --target wasm32-unknown-unknown --lib exits 0.
    Gate 5 — cargo public-api zero unintended changes: cargo public-api (or +nightly) produces zero diff against /tmp/ga-public-api-before.txt (from plan 69-04).
    Gate 6 — cargo doc --no-deps zero warnings.
    Gate 7 — Documentation index updated for every audience the change touches: README.md, CHANGELOG.md, docs/benchmarks.md, docs/ARCHITECTURE.md, CLAUDE.md, src/lib.rs, .planning/intel/{bench-harness,parallel-feature,ga-internals}.md — verify each file was touched by an appropriate plan in this phase.
    Gate 8 — Every commit body contains Revert plan line: git log --since="<phase-69-start>" --format=%B | grep -c "Revert plan:" — must equal or exceed the commit count for this phase.
    Gate 9 — PR description links baseline diff and post-change measurement: document in the SUMMARY (PR creation is out of scope for the execute step).

    Commit message: docs(69): finalise CHANGELOG, sweep README/lib.rs, confirm acceptance gates
    Commit body MUST include: Revert plan: revert this commit to restore pre-finalisation CHANGELOG entries and README/lib.rs prose
  </action>
  <verify>
    <automated>grep -c 'engines/ga' CHANGELOG.md | grep -q '^[1-9]' &amp;&amp; grep -c 'divan' CHANGELOG.md | grep -q '^[1-9]' &amp;&amp; grep -c 'parallel' CHANGELOG.md | grep -q '^[1-9]' &amp;&amp; cargo test --all-features 2>&amp;1 | tail -3 &amp;&amp; cargo test --no-default-features --features logging 2>&amp;1 | tail -3 &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -3 &amp;&amp; cargo doc --no-deps 2>&amp;1 | (grep -c warning || true) | grep -q '^0$' &amp;&amp; grep -c 'parallel-off' .github/workflows/feature-matrix.yml | grep -q '^[1-9]' &amp;&amp; grep -c 'Enforce no unconditional rayon' .github/workflows/feature-matrix.yml | grep -q '^[1-9]' &amp;&amp; echo ACCEPTANCE_GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md v3.0.0 contains three Phase 69 entries (divan migration, parallel feature, ga split)
    - README.md and src/lib.rs no longer refer to engines/ga.rs as a single file (or document that no such reference exists)
    - cargo test --all-features passes
    - cargo test --no-default-features --features logging passes
    - cargo check --target wasm32-unknown-unknown --lib exits 0
    - cargo doc --no-deps produces zero warnings
    - .github/workflows/feature-matrix.yml contains both parallel-off matrix combination AND the grep enforcement step
    - cargo public-api diff against /tmp/ga-public-api-before.txt (captured in 69-04) is empty
    - Final commit body contains Revert plan line
    - SUMMARY records the pass/fail status of all 9 BUILD-PERF.md acceptance gates
  </acceptance_criteria>
  <done>CHANGELOG, README, lib.rs sweep complete; all 9 BUILD-PERF.md acceptance gates pass (or are explicitly documented as deferred to PR review); phase 69 is ready to merge.</done>
</task>

</tasks>

<verification>
- docs/ARCHITECTURE.md reflects the 11-submodule layout
- .planning/intel/ga-internals.md ships with all 6 required sections
- CHANGELOG.md v3.0.0 has all three Phase 69 entries (divan, parallel, ga split)
- README.md and src/lib.rs swept clean of stale single-file references
- All 9 BUILD-PERF.md acceptance gates pass (or are documented as deferred)
- cargo test, cargo doc, cargo check (wasm), cargo public-api all clean
- All commits GPG-signed with Revert plan body lines
</verification>

<success_criteria>
Documentation sweep complete; intel file equips future agents with responsibilities + visibility + anti-patterns + verification; BUILD-PERF.md acceptance gates explicitly verified; phase 69 ships.
</success_criteria>

<output>
Create .planning/phases/69-build-perf-m3-major-refactors/69-05-SUMMARY.md when done. SUMMARY must include: a pass/fail table for all 9 BUILD-PERF.md acceptance gates, the final commit count for the phase, and a one-line readiness statement ("Phase 69 ready to merge" or specific blockers).
</output>
