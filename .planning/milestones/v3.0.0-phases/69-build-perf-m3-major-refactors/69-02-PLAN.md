---
phase: 69-build-perf-m3-major-refactors
plan: 02
type: execute
wave: 1
depends_on: [69-01]
files_modified:
  - Cargo.toml
  - src/population.rs
  - src/traits/common.rs
  - src/engines/island/nsga2.rs
  - .github/workflows/feature-matrix.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Cargo.toml declares parallel = [\"dep:rayon\"] feature with default = [\"logging\", \"parallel\"]"
    - "rayon is optional in [dependencies]"
    - "All three previously-ungated rayon imports are gated with the combined cfg pattern"
    - "`cargo check --target wasm32-unknown-unknown` passes after these scaffolding changes"
    - "`cargo test --no-default-features --features logging` passes (proves parallel=off compiles for the 3 fixed files; other files gated in 69-03)"
    - "feature-matrix.yml CI matrix includes parallel-off combination"
  artifacts:
    - path: "Cargo.toml"
      provides: "parallel feature declaration and optional rayon"
      contains: "parallel = [\"dep:rayon\"]"
    - path: ".github/workflows/feature-matrix.yml"
      provides: "parallel-off CI combination"
      contains: "parallel-off"
  key_links:
    - from: "Cargo.toml"
      to: "rayon crate"
      via: "optional = true behind dep:rayon"
      pattern: "rayon = \\{ version = \"1.10\", optional = true \\}"
    - from: "src/population.rs"
      to: "rayon::prelude"
      via: "combined cfg gate"
      pattern: "cfg\\(all\\(not\\(target_arch = \"wasm32\"\\), feature = \"parallel\"\\)\\)"
---

<objective>
Scaffold the `parallel` feature: add the Cargo.toml declaration, make rayon optional, gate the THREE highest-risk ungated rayon imports (population.rs, traits/common.rs, engines/island/nsga2.rs), and add the parallel-off combination to feature-matrix CI.

Purpose: Wave M3 Action #3 part A. Without fixing the three ungated imports BEFORE making rayon optional, `cargo check --target wasm32-unknown-unknown` and `cargo test --no-default-features --features logging` will fail immediately (Pitfall 2 in RESEARCH.md). The remaining 17 rayon call-sites in other files are gated in plan 69-03.

The canonical gate (D-06) is established here and applied uniformly in 69-03.

Revert plan: drop `parallel` feature from Cargo.toml, restore `rayon = "1.10"` (non-optional), ungate the three import sites, remove parallel-off CI matrix entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/69-build-perf-m3-major-refactors/69-CONTEXT.md
@.planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md
@.planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md
@.planning/phases/68-build-perf-m2-dependency-hygiene/68-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parallel feature to Cargo.toml and gate the three ungated rayon imports</name>
  <files>Cargo.toml, src/population.rs, src/traits/common.rs, src/engines/island/nsga2.rs</files>
  <read_first>
    - Cargo.toml ([features] section line ~33–34; [dependencies] rayon line)
    - src/population.rs (lines 26 and 135 — UNGATED `use rayon::prelude::*;` and `par_iter_mut` per RESEARCH §Plan 69-02 inventory)
    - src/traits/common.rs (lines 8 and 148 — UNGATED `use rayon::prelude::*;` and `into_par_iter`)
    - src/engines/island/nsga2.rs (line 57 top-level UNGATED `use rayon::prelude::*;` + function-level imports in evolve blocks)
    - .planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md (Pattern B — Ungated top-level import; canonical combined gate D-06)
    - .planning/phases/68-build-perf-m2-dependency-hygiene/68-CONTEXT.md (`logging = ["dep:log"]` precedent for dep: prefix)
  </read_first>
  <action>
    Commit 1 — Cargo.toml feature declaration:
    - In [features]: change `default = ["logging"]` to `default = ["logging", "parallel"]`; add a new line `parallel = ["dep:rayon"]` directly after the existing `logging = ["dep:log"]` line.
    - In [dependencies]: change the rayon line from `rayon = "1.10"` to `rayon = { version = "1.10", optional = true }`.
    - Commit message: `build(69): add parallel feature gating rayon as optional dep`. Commit body must include `Revert plan: restore rayon = "1.10" and drop parallel feature from [features]`.

    Commit 2 — gate the three ungated rayon import files. For EACH file, the canonical pattern per D-06 is:
    - Top-level `use rayon::prelude::*;` becomes preceded by `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]`.
    - For each parallel call-site (`par_iter()`, `par_iter_mut()`, `into_par_iter()`, `par_sort_unstable_by()`, etc.), wrap with the SAME combined cfg attribute, and add a sibling sequential fallback (`iter()` / `iter_mut()` / `sort_unstable_by()`) gated with `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]`.

    Specific sites to gate in this task:
    - `src/population.rs`: gate the top-level rayon import; convert the `par_iter_mut().for_each(|chromosome| ...)` block at line 135 into a cfg-gated parallel arm + sequential `iter_mut().for_each(...)` arm carrying the same closure body. The NaN-check closure body MUST be byte-identical between the two arms.
    - `src/traits/common.rs`: gate the top-level rayon import; convert the `into_par_iter()` block at line 148 into the parallel + sequential cfg-gated pair. Preserve the exact same map/collect chain in both arms.
    - `src/engines/island/nsga2.rs`: gate the top-level rayon import at line 57; locate every block-local `use rayon::prelude::*;` inside function bodies and wrap each block (import + parallel work) in a single `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))] { ... }` block, with a sibling sequential `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))] { ... }` block carrying the same logic using `.iter()` / `.iter_mut()` / `.into_iter()`.

    Use exactly the gate strings (D-06 canonical form): parallel arm = `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]`; sequential fallback = `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]`. No other variants permitted. Do NOT use the negated `#[cfg(not(all(...)))]` form — CONTEXT.md D-06 and ROADMAP success criterion #2 mandate the `any(...)` form for the sequential arm.

    Commit message: `fix(69): gate three ungated rayon imports under combined wasm32+parallel cfg`. Commit body must include `Revert plan: revert this commit to restore ungated rayon imports in population.rs, traits/common.rs, engines/island/nsga2.rs`.

    Do NOT touch any other rayon call-sites in this plan — that is plan 69-03's job. Other files keep their existing `#[cfg(not(target_arch = "wasm32"))]` gates for now (they will compile because the parallel feature is default-on; without 69-03 they will NOT compile with `--no-default-features --features logging`, but this plan's verify command at least passes for `cargo check --target wasm32-unknown-unknown` and `cargo test --all-features`).
  </action>
  <verify>
    <automated>grep -E '^parallel = \["dep:rayon"\]$' Cargo.toml &amp;&amp; grep -E 'rayon = \{ version = "1.10", optional = true \}' Cargo.toml &amp;&amp; grep -E '^default = \["logging", "parallel"\]$' Cargo.toml &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -5 &amp;&amp; cargo test --all-features 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'parallel = \["dep:rayon"\]' Cargo.toml` returns 1
    - `grep -c 'rayon = { version = "1.10", optional = true }' Cargo.toml` returns 1
    - `grep -c 'default = \["logging", "parallel"\]' Cargo.toml` returns 1
    - `grep -B1 'use rayon::prelude' src/population.rs | grep -c 'cfg(all(not(target_arch = "wasm32"), feature = "parallel"))'` returns 1
    - `grep -B1 'use rayon::prelude' src/traits/common.rs | grep -c 'cfg(all(not(target_arch = "wasm32"), feature = "parallel"))'` returns 1
    - `grep -B1 'use rayon::prelude' src/engines/island/nsga2.rs | grep -c 'cfg(all(not(target_arch = "wasm32"), feature = "parallel"))'` returns >= 1
    - `grep -c 'cfg(any(target_arch = "wasm32", not(feature = "parallel")))' src/population.rs src/traits/common.rs src/engines/island/nsga2.rs` returns >= 3 (D-06 canonical sequential arm)
    - `grep -c 'cfg(not(all(not(target_arch = "wasm32"), feature = "parallel")))' src/population.rs src/traits/common.rs src/engines/island/nsga2.rs` returns 0 (forbidden form must not appear)
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - `cargo test --all-features` passes
    - Last 2 commits each contain `Revert plan:` in body
  </acceptance_criteria>
  <done>Cargo.toml has parallel feature; rayon optional; the three ungated import files now use the canonical combined gate (D-06); wasm32 check still passes; tests pass with default features.</done>
</task>

<task type="auto">
  <name>Task 2: Add parallel-off matrix combination to feature-matrix CI workflow</name>
  <files>.github/workflows/feature-matrix.yml</files>
  <read_first>
    - .github/workflows/feature-matrix.yml (existing matrix entries, especially the `logging-explicit` entry at lines ~43–45 to use as a copy template)
    - .planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md (exact yaml snippet for the new entry)
  </read_first>
  <action>
    Add a new matrix combination entry to the feature-matrix job in `.github/workflows/feature-matrix.yml`, copying the structure of the existing `logging-explicit` entry. The new entry must be:
    - `name: "parallel-off"`
    - `features: "--no-default-features --features logging"`
    - `cmd: "cargo test --quiet --no-default-features --features logging"`

    Insert the new entry directly after the existing `logging-explicit` entry to keep similar-named entries grouped. Do NOT add the grep enforcement step in this plan (that ships in 69-03 alongside the rest of the call-site gating).

    Note: This CI combination will FAIL on this plan's branch because the other ~17 files still have ungated rayon usage (Pattern A files from RESEARCH.md). The matrix entry is added now so 69-03's PR is the one that makes it green. Document this in the commit body verbatim: "CI matrix entry is intentionally added before 69-03 lands so the next plan's PR can flip from red to green as the gating completes. The parallel-off CI combination will be RED until plan 69-03 lands and gates the remaining ~17 rayon call-sites."

    Commit message: `ci(69): add parallel-off combination to feature-matrix workflow`. Commit body must include BOTH the rationale sentence above AND `Revert plan: remove the parallel-off matrix entry`.
  </action>
  <verify>
    <automated>grep -c 'parallel-off' .github/workflows/feature-matrix.yml | grep -q '^[1-9]' &amp;&amp; grep -A2 'parallel-off' .github/workflows/feature-matrix.yml | grep -c 'no-default-features --features logging' | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name: "parallel-off"' .github/workflows/feature-matrix.yml` returns 1
    - `grep -A3 'parallel-off' .github/workflows/feature-matrix.yml | grep -c -- '--no-default-features --features logging'` returns >= 2 (features + cmd lines)
    - YAML still parses: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/feature-matrix.yml'))"` exits 0
    - Commit body contains `Revert plan:` line
    - **Commit body contains explanation that parallel-off CI combination will be red until plan 69-03 lands.** Verify with: `git log -n 1 --format=%B | grep -qE '(red|fail|until.*69-03|69-03.*lands)'` returns 0 (matches the rationale).
  </acceptance_criteria>
  <done>feature-matrix.yml has parallel-off matrix combination matching the logging-explicit pattern; YAML still parses; commit signed with Revert plan line AND with rationale that parallel-off CI will be red until 69-03 lands.</done>
</task>

</tasks>

<verification>
- Cargo.toml feature declaration matches D-05 exactly
- The three previously-ungated import files now use D-06 canonical gate
- `cargo check --target wasm32-unknown-unknown --lib` exits 0
- `cargo test --all-features` passes
- feature-matrix.yml has parallel-off combination
- Commits GPG-signed with Revert plan body lines
</verification>

<success_criteria>
parallel feature scaffolded in Cargo.toml per D-05; the three Pattern B ungated imports gated with the D-06 canonical pattern (resolves Pitfall 2); CI matrix entry for parallel-off added (will be made green by 69-03). WASM check passes; default-features tests pass.
</success_criteria>

<output>
Create `.planning/phases/69-build-perf-m3-major-refactors/69-02-SUMMARY.md` when done. SUMMARY must record the exact gate strings used (parallel arm + sequential arm) so 69-03 can copy them verbatim.
</output>
</content>
</invoke>