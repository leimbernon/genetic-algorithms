---
phase: 69-build-perf-m3-major-refactors
plan: 04
type: execute
wave: 2
depends_on: [69-03]
files_modified:
  - src/lib.rs
  - src/engines/ga.rs
  - src/engines/ga/mod.rs
  - src/engines/ga/lifecycle.rs
  - src/engines/ga/generation.rs
  - src/engines/ga/adaptive.rs
  - src/engines/ga/aos.rs
  - src/engines/ga/extension.rs
  - src/engines/ga/cache.rs
  - src/engines/ga/batch.rs
  - src/engines/ga/stats.rs
  - src/engines/ga/observer.rs
  - src/engines/ga/stopping.rs
autonomous: true
requirements: []
must_haves:
  end_state_note: "The truths and artifacts below describe the END-STATE of plan 69-04 after ALL THREE tasks complete. Task 1 captures baselines only (zero source changes). Task 2 performs the 11-commit extraction (ga.rs is deleted in Task 2 Commit 1). Task 3 verifies semantic equivalence. Do NOT interpret these criteria as Task 1 acceptance criteria."
  truths:
    - "src/engines/ga/ directory holds mod.rs (orchestrator) plus 10 sibling submodule files (lifecycle, generation, adaptive, aos, extension, cache, batch, stats, observer, stopping)"
    - "src/engines/ga.rs file no longer exists (deleted in Task 2 Commit 1)"
    - "src/lib.rs #[path] attribute points to engines/ga/mod.rs"
    - "Ga<U> is reachable at crate::ga::Ga unchanged (zero public API surface change)"
    - "cargo expand symbol-table diff before vs after the full split is empty"
    - "All tests pass under --all-features AND --no-default-features --features logging"
    - "WASM check passes"
    - "Observability hooks (notify call-sites) are preserved in observer.rs"
  artifacts:
    - path: "src/engines/ga/mod.rs"
      provides: "Ga<U> struct, builder impls, build(), run(), run_with_callback() orchestrator"
      contains: "pub struct Ga"
    - path: "src/engines/ga/stopping.rs"
      provides: "limit_reached<U>() and stopping criteria check"
      contains: "fn limit_reached"
    - path: "src/engines/ga/cache.rs"
      provides: "fitness cache helpers"
    - path: "src/engines/ga/batch.rs"
      provides: "batch_evaluate<U>() helper"
      contains: "fn batch_evaluate"
    - path: "src/engines/ga/stats.rs"
      provides: "GenerationStats collection"
    - path: "src/engines/ga/observer.rs"
      provides: "Observer hook dispatch (notify fn)"
    - path: "src/engines/ga/generation.rs"
      provides: "Per-generation loop body"
    - path: "src/engines/ga/lifecycle.rs"
      provides: "init_population, initialize_random, initialize_with_seeds, finalise"
    - path: "src/engines/ga/adaptive.rs"
      provides: "Adaptive crossover/mutation probability recomputation"
    - path: "src/engines/ga/aos.rs"
      provides: "Adaptive Operator Selection integration"
    - path: "src/engines/ga/extension.rs"
      provides: "Extension trigger and diversity check"
  key_links:
    - from: "src/lib.rs"
      to: "src/engines/ga/mod.rs"
      via: "#[path = engines/ga/mod.rs] pub mod ga;"
      pattern: "engines/ga/mod.rs"
    - from: "src/engines/ga/mod.rs"
      to: "ga/{lifecycle,generation,adaptive,aos,extension,cache,batch,stats,observer,stopping}.rs"
      via: "submodule declarations"
      pattern: "pub(crate) mod"
---

<objective>
Split the 3342-line src/engines/ga.rs into 11 cohesive submodules under src/engines/ga/ as a pure move refactor. Verify zero semantic change via cargo expand symbol diff (D-12) and zero public API surface change via cargo public-api.

Purpose: Wave M3 Action #4. Enable rustc to parallelise the frontend per-file. Single most-loaded compilation unit becomes 11 cohesive files. BUILD-PERF.md guarantees #1 (zero semantic change) and #2 (zero API surface change) enforced via cargo expand and cargo public-api.

Wave 0 of this plan installs cargo-expand and cargo-public-api (RESEARCH Wave 0 Gaps); these tools are NOT installed today.

**End-state vs per-task acceptance:** The `must_haves` truths in frontmatter describe the END-STATE after all 3 tasks of this plan complete (Task 1 captures baselines only, Task 2 performs the 11-commit extraction including ga.rs deletion, Task 3 verifies semantic equivalence). Each task's `<acceptance_criteria>` block is the authoritative per-task gate.

**Baseline persistence:** Baseline artifacts captured in Task 1 are written to a workspace-local path (`.planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt` and `ga-public-api-before.txt`), NOT to `/tmp` which is ephemeral across sessions and CI runs. Task 3 reads those same files for the diff comparison. The files are git-ignored locally (added to .gitignore in Task 1 if not already excluded by `.planning/` rules) and recreated as needed — they are derivable artifacts, not source.

Revert plan: git revert range covering all 11 submodule extraction commits + the lib.rs path commit. Atomic per D-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/69-build-perf-m3-major-refactors/69-CONTEXT.md
@.planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md
@.planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — install cargo-expand and cargo-public-api, capture pre-split baseline</name>
  <files>(no source files modified — captures workspace-local baseline artifacts in .planning/phases/69-build-perf-m3-major-refactors/)</files>
  <read_first>
    - .planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md (Wave 0 Gaps + cargo-expand workflow lines ~455–466)
    - src/lib.rs (line ~333 current path attribute)
    - src/engines/ga.rs (full file — to be moved into mod.rs initially)
    - .gitignore (verify `.planning/` or specific entries already keep workspace-local baseline files out of git, OR add explicit ignore line for ga-symbols-before.txt and ga-public-api-before.txt)
  </read_first>
  <action>
    Step 1: Run `cargo install cargo-expand`. Verify with `cargo expand --version`.
    Step 2: Run `cargo install cargo-public-api`. Verify with `cargo public-api --version`. Per RESEARCH open question #2, if installation fails on stable, retry with `cargo +nightly install cargo-public-api` and document the nightly requirement in the SUMMARY. Use `cargo +nightly public-api` consistently throughout this plan if nightly is required.
    Step 3: Capture pre-split baseline artifacts to **workspace-local paths** (NOT /tmp — those files must survive session restarts and CI runs so Task 3's semantic-equivalence diff is repeatable):
    - `cargo expand --lib` piped through `grep '^pub \(fn\|struct\|impl\|trait\|enum\|type\|use\)' | sort` written to **`.planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt`**.
    - `cargo public-api` (or `cargo +nightly public-api`) written to **`.planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt`**.
    - Verify both files non-empty with `wc -l` reporting > 0 lines each.
    Step 4: Ensure the baseline files are not committed. If `.gitignore` does not already exclude them via a broader `.planning/` rule, add the two explicit paths. Verify `git status --porcelain` shows clean source-tree (the baseline files may show as untracked under .planning/ if not gitignored — that is acceptable as long as no actual source files changed; the truth is `git status --porcelain -- 'src/' 'Cargo.toml'` must be empty).

    Step 5: Capture a pre-split count of `notify(` call sites for the Task 2 acceptance criterion preservation check: `grep -c 'notify(' src/engines/ga.rs > .planning/phases/69-build-perf-m3-major-refactors/ga-notify-count-before.txt`.

    Do NOT commit baseline files. This task makes no source changes.
  </action>
  <verify>
    <automated>cargo expand --version 2>&amp;1 | grep -q expand &amp;&amp; (cargo public-api --version 2>&amp;1 || cargo +nightly public-api --version 2>&amp;1) | grep -q public-api &amp;&amp; test -s .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt &amp;&amp; test -s .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt &amp;&amp; test -s .planning/phases/69-build-perf-m3-major-refactors/ga-notify-count-before.txt &amp;&amp; test -z "$(git status --porcelain -- src/ Cargo.toml)" &amp;&amp; echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo expand --version` exits 0
    - `cargo public-api --version` (stable) OR `cargo +nightly public-api --version` exits 0
    - `.planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt` exists and has > 0 lines
    - `.planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt` exists and has > 0 lines
    - `.planning/phases/69-build-perf-m3-major-refactors/ga-notify-count-before.txt` exists and has > 0 lines
    - `git status --porcelain -- src/ Cargo.toml` is empty (no source changes in this task)
  </acceptance_criteria>
  <done>Both tools installed; pre-split symbol-table baseline, public-api baseline, and notify-count baseline captured to workspace-local `.planning/phases/69-build-perf-m3-major-refactors/`; no source files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the 11-commit submodule extraction in dependency order</name>
  <files>src/lib.rs, src/engines/ga.rs, src/engines/ga/mod.rs, src/engines/ga/lifecycle.rs, src/engines/ga/generation.rs, src/engines/ga/adaptive.rs, src/engines/ga/aos.rs, src/engines/ga/extension.rs, src/engines/ga/cache.rs, src/engines/ga/batch.rs, src/engines/ga/stats.rs, src/engines/ga/observer.rs, src/engines/ga/stopping.rs</files>
  <read_first>
    - src/engines/ga.rs (entire 3342-line file — must understand all boundaries before moving any code)
    - src/lib.rs (line ~333 path attribute; lines ~351–362 established engines/name/mod.rs pattern)
    - .planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md (Submodule Extraction Map lines ~417–432; extraction order ~441–453; Pitfalls 3 and 4)
    - .planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md (gp/mod.rs as analog; visibility rules D-11)
    - src/engines/gp/mod.rs (analog for directory-based engine module)
  </read_first>
  <action>
    Execute the split as exactly 11 GPG-signed commits in dependency-first order (D-10).

    Commit 1 — atomic file replacement + first extraction (stopping.rs):
    - Update src/lib.rs line ~333 from path attribute pointing at engines/ga.rs to path attribute pointing at engines/ga/mod.rs.
    - Create src/engines/ga/mod.rs containing the FULL current contents of src/engines/ga.rs MINUS the limit_reached function being extracted.
    - Create src/engines/ga/stopping.rs containing limit_reached<U>() (lines ~2778–2857 of original ga.rs). Visibility pub(crate).
    - DELETE src/engines/ga.rs.
    - Add pub(crate) mod stopping; declaration at top of src/engines/ga/mod.rs.
    - This atomic commit fixes Pitfall 3 (no intermediate state where both ga.rs and ga/mod.rs exist).
    - Commit message: refactor(69): extract stopping.rs from engines/ga.rs and convert to directory module
    - Commit body MUST include: Revert plan: revert this commit to restore engines/ga.rs as a single file

    Commits 2–10 — extract one submodule per commit, in this order:
    2. cache.rs — fitness cache helpers (lines ~2480–2682). Visibility pub(crate).
    3. stats.rs — GenerationStats collection helpers. Visibility pub(crate).
    4. observer.rs — observer hook dispatch (notify calls). Visibility pub(crate). CRITICAL preserve every notify() call-site (CLAUDE.md observability mandate).
    5. batch.rs — batch_evaluate<U>() (lines ~2682–2778). Visibility pub(crate) because lifecycle.rs AND generation.rs both call it (Pitfall 4: pub(super) is NOT visible to siblings).
    6. adaptive.rs — adaptive crossover/mutation probability recomputation. Visibility per call-graph: pub(crate) if generation.rs uses it (likely yes), else pub(super).
    7. aos.rs — Adaptive Operator Selection (get_aos_operator, credit assignment). Visibility per same rule as adaptive.rs.
    8. extension.rs — extension trigger + diversity check (lines ~2200–2280). Visibility per same rule.
    9. lifecycle.rs — init_population, initialize_random, initialize_with_seeds, finalise_run (lines ~1200–1484). Visibility pub(crate).
    10. generation.rs — per-generation loop body (lines ~1620–2280) plus free-standing helpers parent_crossover, extract_elite, reinsert_elite. Visibility pub(crate).

    Each of commits 2–10 must:
    - Add a pub(crate) mod <name>; declaration to src/engines/ga/mod.rs.
    - Move only the items belonging to that submodule out of mod.rs into the new file.
    - Adjust use statements in mod.rs to reference the new submodule path (use super::stopping::limit_reached; or similar via crate::ga::stopping::*).
    - Compile clean: cargo check --all-features exits 0.
    - Lib tests pass: cargo test --lib --all-features exits 0.
    - WASM check exits 0: cargo check --target wasm32-unknown-unknown --lib.
    - Commit message: refactor(69): extract <submodule>.rs from engines/ga.rs
    - Commit body MUST include: Revert plan: revert this commit to inline <submodule> contents back into engines/ga/mod.rs

    Commit 11 — final mod.rs cleanup pass:
    - mod.rs at this point contains only the Ga<U> struct definition, all impl ConfigurationT / SelectionConfig / ... builder blocks, with_* methods, build(), run(), run_with_callback(), stats(), hall_of_fame(), notify() entry point, plus the 10 submodule declarations.
    - Review and order the submodule declarations consistently (alphabetical or by execution order).
    - Add any necessary pub use submodule::TypeName; re-exports ONLY for items that ARE currently pub and must remain reachable at crate::ga::*. Verify against .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt — every pub item there must still be reachable at the same path.
    - Run the FULL test suite: cargo test --all-features AND cargo test --no-default-features --features logging.
    - Commit message: refactor(69): finalise engines/ga/mod.rs orchestrator after submodule split
    - Commit body MUST include: Revert plan: revert this commit to restore the pre-cleanup mod.rs state

    Visibility rules (D-11) applied per-item:
    - Currently pub items (Ga, run, build, stats, hall_of_fame, etc.) remain pub — zero public surface change.
    - Items called from 2 or more ga submodules use pub(crate) (Pitfall 4).
    - Items called only from mod.rs use pub(super).
    - Items only used within their own submodule use pub(super) or no modifier.

    Header for each new submodule file: a //! line stating "Extracted from src/engines/ga.rs in phase 69-04 — <one-line responsibility>" followed by use super::*; or explicit imports. Follow the established header pattern from src/engines/gp/engine.rs.

    Preserve verbatim:
    - All cfg(all(not(target_arch = "wasm32"), feature = "parallel")) attribute strings from plan 69-03 — they move with the rayon call-sites into generation.rs / lifecycle.rs.
    - All log macro call sites (log::info!, log::trace!, log::debug!, info!, trace!, debug!) — they move with the code.
    - All notify() observer call sites (CLAUDE.md observability mandate).
  </action>
  <verify>
    <automated>test ! -e src/engines/ga.rs &amp;&amp; test -e src/engines/ga/mod.rs &amp;&amp; test -e src/engines/ga/stopping.rs &amp;&amp; test -e src/engines/ga/cache.rs &amp;&amp; test -e src/engines/ga/stats.rs &amp;&amp; test -e src/engines/ga/observer.rs &amp;&amp; test -e src/engines/ga/batch.rs &amp;&amp; test -e src/engines/ga/adaptive.rs &amp;&amp; test -e src/engines/ga/aos.rs &amp;&amp; test -e src/engines/ga/extension.rs &amp;&amp; test -e src/engines/ga/lifecycle.rs &amp;&amp; test -e src/engines/ga/generation.rs &amp;&amp; grep -q 'engines/ga/mod.rs' src/lib.rs &amp;&amp; cargo check --all-features 2>&amp;1 | tail -3 &amp;&amp; cargo test --all-features 2>&amp;1 | tail -5 &amp;&amp; cargo test --no-default-features --features logging 2>&amp;1 | tail -5 &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/engines/ga.rs does NOT exist
    - src/engines/ga/mod.rs exists and contains pub struct Ga
    - All 10 sibling submodule files exist (lifecycle, generation, adaptive, aos, extension, cache, batch, stats, observer, stopping)
    - src/lib.rs contains the string "engines/ga/mod.rs" and does NOT contain the bare string "engines/ga.rs\"" (with the closing quote — distinguishes from the directory path)
    - cargo check --all-features exits 0
    - cargo test --all-features passes
    - cargo test --no-default-features --features logging passes
    - cargo check --target wasm32-unknown-unknown --lib exits 0
    - cargo doc --no-deps produces zero warnings
    - Exactly 11 commits added since Task 1, each with commit message starting refactor(69) and each body containing a Revert plan line: `git log -n 11 --format=%B | grep -c 'Revert plan:'` returns >= 11
    - Each new submodule file starts with the //! "Extracted from" header line
    - All notify() call sites preserved: total `grep -rc 'notify(' src/engines/ga/` across the new directory >= the count recorded in `.planning/phases/69-build-perf-m3-major-refactors/ga-notify-count-before.txt`
  </acceptance_criteria>
  <done>11 GPG-signed commits land the full split. Directory layout matches the canonical map; tests pass under all relevant feature combinations; WASM clean; cargo doc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Verify semantic equivalence via cargo expand and cargo public-api diff</name>
  <files>(no source changes — verification only; may add a workspace-local diff artifact)</files>
  <read_first>
    - .planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md (cargo-expand workflow lines ~455–466)
    - .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt (from Task 1)
    - .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt (from Task 1)
  </read_first>
  <action>
    Pre-condition check (Step 0): Before doing any work, verify the Task 1 baseline artifacts exist. If either file is missing the workflow cannot proceed — re-run Task 1 to recapture them:
    - `test -s .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt` — must exit 0
    - `test -s .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt` — must exit 0
    - If either check fails, halt this task with message: "Baseline files missing — re-run Task 1 of plan 69-04 to capture pre-split baselines, then retry Task 3."

    Step 1: Capture post-split symbol table:
    - `cargo expand --lib` piped through `grep '^pub \(fn\|struct\|impl\|trait\|enum\|type\|use\)' | sort` written to `.planning/phases/69-build-perf-m3-major-refactors/ga-symbols-after.txt`.

    Step 2: Diff the symbol tables:
    - `diff .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-after.txt` — MUST be empty.
    - If non-empty, the split has changed the public symbol surface. Stop and investigate before proceeding (typical causes: an item that was pub became pub(crate), or visibility was escalated by accident). Fix in a follow-up commit (refactor(69): restore visibility of <item> after split — Revert plan: ...).

    Step 3: Capture post-split public-api:
    - `cargo public-api` (or `cargo +nightly public-api` if Task 1 required nightly) written to `.planning/phases/69-build-perf-m3-major-refactors/ga-public-api-after.txt`.

    Step 4: Diff the public-api files:
    - `diff .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-after.txt` — MUST be empty.
    - If non-empty, the split has changed the public API surface (BUILD-PERF.md non-negotiable guarantee #2 violated). Stop and fix.

    Step 5: Run the full golden test suite under both feature combinations:
    - cargo test --test golden_tests --all-features (skip flag if differently named — check ls tests/golden/ and adapt target).
    - cargo test --test golden_tests --no-default-features --features logging.

    No git commit unless a fix-up commit is needed for a visibility regression. If a fix-up is required, the commit body must include Revert plan: revert this commit to restore the unintended visibility change introduced during split.
  </action>
  <verify>
    <automated>test -s .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt &amp;&amp; test -s .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt &amp;&amp; diff .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-after.txt &amp;&amp; diff .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-after.txt &amp;&amp; cargo test --all-features 2>&amp;1 | tail -3 &amp;&amp; cargo test --no-default-features --features logging 2>&amp;1 | tail -3 &amp;&amp; echo SEMANTIC_EQUIVALENCE_VERIFIED</automated>
  </verify>
  <acceptance_criteria>
    - Pre-condition check passes: `.planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt` and `ga-public-api-before.txt` both exist and are non-empty
    - `diff .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-before.txt .planning/phases/69-build-perf-m3-major-refactors/ga-symbols-after.txt` produces zero output (exit 0)
    - `diff .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-before.txt .planning/phases/69-build-perf-m3-major-refactors/ga-public-api-after.txt` produces zero output (exit 0)
    - cargo test --all-features passes
    - cargo test --no-default-features --features logging passes
    - Golden tests (if present at tests/golden/ or equivalent) pass in BOTH feature configurations
  </acceptance_criteria>
  <done>cargo expand symbol diff empty; cargo public-api diff empty; tests pass in both feature configurations; semantic and API-surface equivalence verified per D-12 and BUILD-PERF.md guarantees #1+#2.</done>
</task>

</tasks>

<verification>
- src/engines/ga.rs deleted; 11 files exist under src/engines/ga/
- src/lib.rs path attribute points at engines/ga/mod.rs
- cargo expand symbol-table diff (before vs after) is empty (D-12)
- cargo public-api diff is empty (BUILD-PERF.md guarantee #2)
- cargo test --all-features passes
- cargo test --no-default-features --features logging passes
- cargo check --target wasm32-unknown-unknown --lib passes
- cargo doc --no-deps zero warnings
- 11 GPG-signed commits each with Revert plan body line
</verification>

<success_criteria>
ga.rs split into 11 cohesive submodules; zero semantic change (cargo expand clean); zero public API surface change (cargo public-api clean); observability hooks preserved; tests green under all feature combinations; WASM clean; per-commit Revert plan lines enable atomic revert.
</success_criteria>

<output>
Create .planning/phases/69-build-perf-m3-major-refactors/69-04-SUMMARY.md when done. SUMMARY must include: the 11 commit SHAs in order, the captured ga-symbols-before.txt and ga-public-api-before.txt line counts, the empty-diff confirmation for both, and note whether cargo public-api required nightly (informs plan 69-05 docs).
</output>
</content>
</invoke>