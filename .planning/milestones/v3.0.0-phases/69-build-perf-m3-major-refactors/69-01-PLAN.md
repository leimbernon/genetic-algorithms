---
phase: 69-build-perf-m3-major-refactors
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - benches/metrics_observer.rs
  - benches/de.rs
  - benches/scatter.rs
  - benches/alps.rs
  - benches/cellular.rs
  - benches/crossover.rs
  - benches/nsga2.rs
  - benches/selection.rs
  - benches/rastrigin.rs
  - benches/survivor.rs
  - benches/mutation.rs
  - benches/ga_run.rs
  - benches/island_ga.rs
  - docs/benchmarks.md
  - .planning/intel/bench-harness.md
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 13 bench files compile and execute under divan harness"
    - "criterion is no longer declared anywhere in Cargo.toml"
    - "metrics_observer bench still feature-isolated via observer-metrics"
    - "de bench still feature-isolated via benchmarks feature"
    - "Median bench values stay within ±3% of pre-port baseline"
  artifacts:
    - path: "Cargo.toml"
      provides: "divan 0.1.21 in [dev-dependencies]; criterion removed"
      contains: "divan = \"0.1.21\""
    - path: "benches/metrics_observer.rs"
      provides: "divan-based metrics_observer bench"
      contains: "divan::main"
    - path: ".planning/intel/bench-harness.md"
      provides: "AI-readable rationale for divan choice + canonical patterns"
    - path: "docs/benchmarks.md"
      provides: "updated invocation snippets for divan"
  key_links:
    - from: "Cargo.toml"
      to: "benches/*.rs"
      via: "[[bench]] entries with harness = false"
      pattern: "harness = false"
    - from: "benches/*.rs"
      to: "divan crate"
      via: "fn main() { divan::main(); }"
      pattern: "divan::main"
---

<objective>
Port all 13 benchmark files from criterion to divan, remove criterion from [dev-dependencies], and ship updated docs/benchmarks.md plus .planning/intel/bench-harness.md.

**NOTE — bench file count:** CONTEXT.md and ROADMAP reference "12 bench files", but Wave 0 research discovered the actual count in `benches/` is **13 files**. This plan ports all 13. The thirteenth file is `benches/island_ga.rs` (HIGHEST complexity, ported in Task 2 alongside the other three HIGH-complexity files). Do NOT stop at 12 — verify with `ls benches/*.rs | wc -l` returning 13 before considering the port complete.

Purpose: Wave M3 Action #7 — reduce dev-build dep weight and bench compile time. Both harnesses coexist during the port (D-09); criterion is removed in this SAME plan once all files are ported and CI is green.
Output: All 13 benches running on divan; criterion removed; documentation updated.

Revert plan: revert this plan's commits; restore criterion = "0.8.2" in [dev-dependencies] and revert each bench file to its pre-port state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/69-build-perf-m3-major-refactors/69-CONTEXT.md
@.planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md
@.planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add divan dev-dependency and port LOW/MED complexity bench files (commit-per-file)</name>
  <files>Cargo.toml, benches/metrics_observer.rs, benches/de.rs, benches/scatter.rs, benches/alps.rs, benches/cellular.rs, benches/crossover.rs, benches/nsga2.rs, benches/selection.rs, benches/rastrigin.rs</files>
  <read_first>
    - Cargo.toml (current [dev-dependencies] and all 13 [[bench]] entries — keep harness = false on each)
    - benches/metrics_observer.rs (LOW; uses simple criterion_group!/bench_function/b.iter)
    - benches/de.rs (LOW; sample_size(10); already required-features=["benchmarks"])
    - benches/scatter.rs (LOW; sample_size(10), BenchmarkId)
    - benches/alps.rs (LOW; sample_size(10), BenchmarkId loop-over-params)
    - benches/cellular.rs (LOW; sample_size(10), BenchmarkId)
    - benches/crossover.rs (MED; BenchmarkId, PlotConfiguration)
    - benches/nsga2.rs (MED; Throughput::Elements, BenchmarkId, PlotConfiguration)
    - benches/selection.rs (MED; Throughput::Elements, BenchmarkId, PlotConfiguration)
    - benches/rastrigin.rs (MED; iter_batched, BenchmarkId)
    - .planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md (canonical divan patterns for each criterion API)
  </read_first>
  <action>
    Commit 1 (Cargo.toml only): add `divan = "0.1.21"` to [dev-dependencies] BELOW the existing `criterion = "0.8.2"` line (both coexist during porting per D-09). Do NOT change [[bench]] entries — `harness = false` stays for both harnesses. Commit body: "build(69): add divan 0.1.21 dev-dependency alongside criterion" plus `Revert plan: drop divan from [dev-dependencies]`.

    Then ONE COMMIT PER BENCH FILE in this order: metrics_observer.rs, de.rs, scatter.rs, alps.rs, cellular.rs, crossover.rs, nsga2.rs, selection.rs, rastrigin.rs (9 commits). For each file apply the canonical divan port:
    - Remove `use criterion::{...}` import block entirely; remove `criterion_group!` and `criterion_main!` calls; add `fn main() { divan::main(); }` at end of file.
    - `group.sample_size(N)` → `#[divan::bench(sample_count = N)]` attribute on the bench fn (applies to alps, cellular, de, scatter — use N=10 exactly).
    - `b.iter(|| work())` → `bencher.bench(|| work())`.
    - `b.iter_batched(|| setup(), |x| work(x), BatchSize::SmallInput)` → `bencher.with_inputs(|| setup()).bench(|_b, x| work(x))`. The setup closure MUST return a fresh owned value each call.
    - `BenchmarkId::new(name, param)` parameterised loops → single `#[divan::bench(args = [...])]` function. For rastrigin.rs which uses iter_batched with BenchmarkId, combine `args` + `with_inputs`.
    - `group.plot_config(PlotConfiguration::default().summary_scale(AxisScale::Logarithmic))` → DROP entirely; also drop `AxisScale`/`PlotConfiguration` imports.
    - `group.throughput(Throughput::Elements(n))` → DROP entirely; also drop `Throughput` from imports (open question in research — no equivalent in divan 0.1.21).
    - `group.benchmark_group("name")` + `group.finish()` → use module-level grouping via `#[divan::bench(name = "...")]` or distinct fn names; drop the group/finish scaffolding.
    - Preserve any `#[cfg(not(tarpaulin_include))]` attributes on bench/setup helper functions.
    - For metrics_observer.rs: takes simple `c.bench_function("name", |b| b.iter(...))` → `#[divan::bench] fn bench_name(bencher: divan::Bencher) { bencher.bench(|| ...); }`.
    - Apply D-07 light cleanup: dead/duplicate bench cases may be removed; complex setup may be simplified if median stays within ±3% of pre-port baseline.

    Each per-file commit body MUST include: `Revert plan: revert this commit to restore criterion bench for benches/<file>.rs`. Each commit message must be GPG-signed (project mandate).

    After each commit, run the verify command for that file (and for de.rs add `--features benchmarks`; for metrics_observer.rs add `--features observer-metrics`).
  </action>
  <verify>
    <automated>cargo bench --bench metrics_observer --features observer-metrics --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench de --features benchmarks --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench scatter --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench alps --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench cellular --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench crossover --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench nsga2 --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench selection --no-run 2>&amp;1 | tail -5 &amp;&amp; cargo bench --bench rastrigin --no-run 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^divan = ' Cargo.toml` returns >= 1
    - `grep -c '^criterion = ' Cargo.toml` returns 1 (still present at this stage)
    - For each ported file (metrics_observer, de, scatter, alps, cellular, crossover, nsga2, selection, rastrigin): `grep -c 'criterion' benches/<file>.rs` returns 0
    - For each ported file: `grep -c 'divan::main' benches/<file>.rs` returns >= 1
    - `cargo bench --no-run --all-features` exits 0
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0 (benches do not affect lib, but verify wasm stays clean)
    - `git log --oneline -n 11 | grep -c 'build(69)\|test(69)'` returns >= 10 (Cargo.toml commit + 9 file commits + any preceding)
    - Each commit body matched by `git log -n 10 --format=%B | grep -c 'Revert plan:'` returns >= 9
  </acceptance_criteria>
  <done>Cargo.toml has divan dev-dep alongside criterion; 9 LOW/MED bench files are fully ported to divan; all 9 benches compile (`cargo bench --no-run`); each file has its own GPG-signed commit with a Revert plan line.</done>
</task>

<task type="auto">
  <name>Task 2: Port HIGH complexity bench files, remove criterion, write docs and intel</name>
  <files>benches/survivor.rs, benches/mutation.rs, benches/ga_run.rs, benches/island_ga.rs, Cargo.toml, docs/benchmarks.md, .planning/intel/bench-harness.md, CHANGELOG.md</files>
  <read_first>
    - benches/survivor.rs (HIGH; iter_batched, BenchmarkId, PlotConfiguration)
    - benches/mutation.rs (HIGH; many iter_batched + BenchmarkId params)
    - benches/ga_run.rs (HIGH; iter_batched, BatchSize::SmallInput, PlotConfiguration — see RESEARCH §Plan 69-01 lines 167–175)
    - benches/island_ga.rs (HIGHEST; iter_batched + IslandGa builder setup — this is the 13th bench file noted in the objective)
    - Cargo.toml (to remove criterion = "0.8.2" line cleanly)
    - docs/benchmarks.md (existing invocation snippets to update)
    - .planning/v3.0.0-BUILD-PERF.md (Action #7 rationale for the intel file)
    - .planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md (HIGH-complexity port patterns)
    - CHANGELOG.md (existing entry format)
  </read_first>
  <action>
    ONE COMMIT PER BENCH FILE in this order: survivor.rs, mutation.rs, ga_run.rs, island_ga.rs (4 commits). Apply the same canonical port patterns as Task 1 with these specifics:
    - `iter_batched` blocks → `bencher.with_inputs(|| build_ga(ps, gl, mg)).bench(|_b, mut ga| { let _ = ga.run(); })`.
    - Parameterised `BenchmarkId::new("name", format!(...))` loops → `#[divan::bench(args = [(20,10,10usize), (50,10,10), ...])]` with destructured tuple parameter on the bench fn.
    - For mutation.rs (many parameterised cases) consider grouping into multiple `#[divan::bench]` fns by mutation strategy if a single `args` becomes unwieldy.
    - For island_ga.rs preserve the IslandGa builder helper as a standalone setup fn returning the owned IslandGa; pass via `with_inputs`.
    - Each per-file commit body MUST include `Revert plan: revert this commit to restore criterion bench for benches/<file>.rs`.

    After all 13 files are ported, one MORE commit removes criterion:
    - Remove the line `criterion = "0.8.2"` from [dev-dependencies] in Cargo.toml.
    - Verify zero references remain: `grep -rn criterion benches/ Cargo.toml` must return zero matches.
    - Commit body: "build(69): remove criterion from [dev-dependencies] — all benches now use divan" plus `Revert plan: restore criterion = \"0.8.2\" in [dev-dependencies] and revert per-bench ports`.

    Then update documentation in a final commit:
    - `docs/benchmarks.md`: replace any `cargo bench --bench <name> -- --quick` criterion invocations with divan equivalents; document `cargo bench --bench de --features benchmarks` and `cargo bench --bench metrics_observer --features observer-metrics`. Mention divan's default sample counts and that `sample_count` can be set per-bench via the attribute.
    - Create `.planning/intel/bench-harness.md` as an AI-readable 1–2 page note covering: why divan over criterion (transitive dep weight), the canonical divan patterns used in this codebase (basic bench / with_inputs / args / sample_count), invariants that must NOT be reintroduced (no criterion in benches/, no group/finish scaffolding), and how to verify (cargo bench --no-run). Reference D-07 (light cleanup allowed) and D-08 (feature-isolated benches stay separate).
    - `CHANGELOG.md`: under v3.0.0 "Changed — internal" add: "Bench harness migrated from criterion to divan (Phase 69 Action #7). No public-API change."
    - Commit body: "docs(69): update benchmarks.md + add bench-harness intel + changelog entry" plus `Revert plan: revert this commit`.
  </action>
  <verify>
    <automated>cargo bench --bench survivor --no-run 2>&amp;1 | tail -3 &amp;&amp; cargo bench --bench mutation --no-run 2>&amp;1 | tail -3 &amp;&amp; cargo bench --bench ga_run --no-run 2>&amp;1 | tail -3 &amp;&amp; cargo bench --bench island_ga --no-run 2>&amp;1 | tail -3 &amp;&amp; cargo test --all-features 2>&amp;1 | tail -5 &amp;&amp; cargo test --no-default-features --features logging 2>&amp;1 | tail -5 &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -3 &amp;&amp; cargo doc --no-deps 2>&amp;1 | grep -c 'warning' | grep -q '^0$' &amp;&amp; echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls benches/*.rs | wc -l` returns 13 (sanity check: count unchanged)
    - `grep -rn criterion benches/ Cargo.toml` returns zero matches
    - `grep -c 'divan::main' benches/survivor.rs benches/mutation.rs benches/ga_run.rs benches/island_ga.rs` returns >= 4
    - `cargo bench --no-run --all-features` exits 0
    - `cargo test --all-features` passes (no test regressions)
    - `cargo test --no-default-features --features logging` passes
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - `cargo doc --no-deps` produces zero warnings
    - File `.planning/intel/bench-harness.md` exists and contains at minimum: a "Why divan" section, a "Canonical patterns" section, a "Do not reintroduce" section
    - `docs/benchmarks.md` no longer mentions `criterion_group!` or `--features bench` for criterion-only flags
    - `grep -c 'divan' CHANGELOG.md` returns >= 1
    - Final 5 commits (4 HIGH ports + criterion removal + docs) each contain `Revert plan:` in their body
  </acceptance_criteria>
  <done>4 HIGH bench files ported, criterion removed from Cargo.toml, docs/benchmarks.md updated, .planning/intel/bench-harness.md created, CHANGELOG entry added. All tests pass, wasm compiles, cargo doc clean.</done>
</task>

</tasks>

<verification>
- `cargo bench --no-run --all-features` exits 0 (every divan bench compiles)
- `cargo bench --no-run --bench de --features benchmarks` exits 0
- `cargo bench --no-run --bench metrics_observer --features observer-metrics` exits 0
- `grep -rn criterion benches/ Cargo.toml` returns zero matches
- `cargo test --all-features` and `cargo test --no-default-features --features logging` both pass
- `cargo check --target wasm32-unknown-unknown --lib` exits 0
- `cargo doc --no-deps` zero warnings
- All commits GPG-signed; every commit body includes `Revert plan:` line
</verification>

<success_criteria>
All 13 bench files use divan; criterion dev-dep removed; ±3% median tolerance respected per D-07; docs/benchmarks.md and .planning/intel/bench-harness.md ship in same plan; CHANGELOG updated; full test suite green across feature combinations; WASM clean.
</success_criteria>

<output>
Create `.planning/phases/69-build-perf-m3-major-refactors/69-01-SUMMARY.md` when done. SUMMARY must list: each ported bench file (with the commit SHA), the criterion-removal commit SHA, and a one-line "Revert plan" reminder.
</output>
</content>
</invoke>