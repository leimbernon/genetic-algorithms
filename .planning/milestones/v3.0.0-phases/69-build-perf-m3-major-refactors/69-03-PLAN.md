---
phase: 69-build-perf-m3-major-refactors
plan: 03
type: execute
wave: 1
depends_on: [69-02]
files_modified:
  - src/engines/ga.rs
  - src/engines/nsga2/mod.rs
  - src/engines/nsga3/mod.rs
  - src/engines/spea2/mod.rs
  - src/engines/ibea/mod.rs
  - src/engines/moead/mod.rs
  - src/engines/sms_emoa/mod.rs
  - src/engines/gp/engine.rs
  - src/engines/island/mod.rs
  - src/engines/eda/engine.rs
  - src/operations/survivor/age.rs
  - src/operations/survivor/fitness.rs
  - src/operations/survivor/mu_comma_lambda.rs
  - src/operations/survivor/mu_plus_lambda.rs
  - src/operations/selection/tournament.rs
  - .github/workflows/feature-matrix.yml
  - CLAUDE.md
  - README.md
  - src/lib.rs
  - CHANGELOG.md
  - .planning/intel/parallel-feature.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every rayon call-site (~36 across ~17 files plus the 3 from 69-02) uses the D-06 canonical combined cfg gate"
    - "`grep -rn 'rayon::' src/ | grep -v '#\\[cfg'` produces zero matches"
    - "feature-matrix CI is green on ALL combinations including parallel-off"
    - "Golden tests are byte-identical with parallel=on AND parallel=off"
    - "WASM build passes (`cargo check --target wasm32-unknown-unknown --lib`)"
    - "CLAUDE.md WASM Compatibility section documents the new canonical gate"
  artifacts:
    - path: ".github/workflows/feature-matrix.yml"
      provides: "grep enforcement step after compile step"
      contains: "Enforce no unconditional rayon"
    - path: "CLAUDE.md"
      provides: "Updated WASM Compatibility rule with new canonical gate"
      contains: "cfg(all(not(target_arch = \"wasm32\"), feature = \"parallel\"))"
    - path: ".planning/intel/parallel-feature.md"
      provides: "AI-readable rationale + canonical pattern + invariant + verification"
    - path: "README.md"
      provides: "Features table row for parallel"
      contains: "parallel"
    - path: "src/lib.rs"
      provides: "Feature Flags table row for parallel"
      contains: "parallel"
  key_links:
    - from: "src/**/*.rs (17 files)"
      to: "rayon crate"
      via: "combined cfg gate"
      pattern: "cfg\\(all\\(not\\(target_arch = \"wasm32\"\\), feature = \"parallel\"\\)\\)"
    - from: ".github/workflows/feature-matrix.yml"
      to: "src/"
      via: "grep -rn 'rayon::' src/ | grep -v '#\\[cfg'"
      pattern: "Enforce no unconditional rayon"
---

<objective>
Gate every remaining rayon call-site in the codebase (the 17 files NOT covered by 69-02) under the D-06 canonical combined cfg pattern, add the grep CI enforcement step, update CLAUDE.md's WASM Compatibility section, document the feature in README/lib.rs/CHANGELOG, and ship `.planning/intel/parallel-feature.md`.

Purpose: Wave M3 Action #3 part B (completes Action #3). After this plan, `cargo test --no-default-features --features logging` and `cargo test --all-features` both pass; golden tests are byte-identical with parallel on AND off; the CI grep step enforces the invariant.

Revert plan: revert this plan's commits to restore wasm32-only gates on all 17 files; remove grep CI step; revert CLAUDE.md WASM section; remove parallel from README/lib.rs Features tables; revert CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/69-build-perf-m3-major-refactors/69-CONTEXT.md
@.planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md
@.planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate all Pattern A and Pattern C rayon call-sites under the combined cfg</name>
  <files>src/engines/ga.rs, src/engines/nsga2/mod.rs, src/engines/nsga3/mod.rs, src/engines/spea2/mod.rs, src/engines/ibea/mod.rs, src/engines/moead/mod.rs, src/engines/sms_emoa/mod.rs, src/engines/gp/engine.rs, src/engines/island/mod.rs, src/engines/eda/engine.rs, src/operations/survivor/age.rs, src/operations/survivor/fitness.rs, src/operations/survivor/mu_comma_lambda.rs, src/operations/survivor/mu_plus_lambda.rs, src/operations/selection/tournament.rs</files>
  <read_first>
    - .planning/phases/69-build-perf-m3-major-refactors/69-PATTERNS.md (Pattern A, Pattern A-sort, Pattern C — gate strings to copy verbatim)
    - .planning/phases/69-build-perf-m3-major-refactors/69-RESEARCH.md (lines 296–332 — per-file site inventory)
    - src/engines/ga.rs (lines ~157 import, ~1277, ~1941, ~2246, ~3173 and within evaluate_initial_fitness — 5 site pairs, currently wasm32-only gated)
    - src/engines/nsga2/mod.rs (lines ~123–124 import + 4 site pairs)
    - src/engines/nsga3/mod.rs (import + 2 sites)
    - src/engines/spea2/mod.rs (import + 2 sites)
    - src/engines/ibea/mod.rs (import + 2 sites)
    - src/engines/moead/mod.rs (import + 1 site)
    - src/engines/sms_emoa/mod.rs (import + 1 site)
    - src/engines/gp/engine.rs (lines 43–44 import + 3 site pairs at lines 166–176 region)
    - src/engines/island/mod.rs (line 511 — local `use rayon::prelude::*;` inside fn body — Pattern C)
    - src/engines/eda/engine.rs (lines 344–362 and 670–682 — Pattern C block pairs already wasm32-gated)
    - src/operations/survivor/age.rs (par_sort_unstable_by site — Pattern A-sort)
    - src/operations/survivor/fitness.rs (lines 12–13 import + 2 par_sort sites — Pattern A-sort)
    - src/operations/survivor/mu_comma_lambda.rs (2 par_sort sites — Pattern A-sort)
    - src/operations/survivor/mu_plus_lambda.rs (2 par_sort sites — Pattern A-sort)
    - src/operations/selection/tournament.rs (1 site — Pattern A)
  </read_first>
  <action>
    For EACH of the 15 files in `files`, replace every existing `#[cfg(not(target_arch = "wasm32"))]` gate that protects rayon usage with the combined gate per D-06.

    The canonical replacements (use these strings verbatim per D-06):
    - For each `#[cfg(not(target_arch = "wasm32"))]` on a rayon import or rayon call-site → replace with `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]`.
    - For each `#[cfg(target_arch = "wasm32")]` sibling fallback that exists today → replace with `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]`.
    - For Pattern A-sort files (the 4 survivor files): the `par_sort_unstable_by` line gets the parallel cfg `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]`; the sibling `sort_unstable_by` line gets `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]`. Closure body byte-identical between the two arms.
    - For Pattern C files (engines/eda/engine.rs and engines/island/mod.rs): update the BLOCK-LEVEL cfg attribute from `#[cfg(not(target_arch = "wasm32"))] { use rayon::prelude::*; ... }` and `#[cfg(target_arch = "wasm32")] { ... }` to the combined-cfg block pair using `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]` for the parallel arm and `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]` for the sequential arm. For engines/island/mod.rs line 511 where the import is inline (no enclosing cfg block today), WRAP the entire parallel block (import + work) in `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))] { ... }` and add a sibling `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))] { ... }` block carrying the equivalent sequential logic.

    Forbidden form: do NOT use `#[cfg(not(all(not(target_arch = "wasm32"), feature = "parallel")))]` — CONTEXT.md D-06 and ROADMAP success criterion #2 mandate the `any(...)` form for the sequential arm.

    Do NOT touch non-rayon wasm32 gates per D-02. Examples to leave UNCHANGED: any `#[cfg(not(target_arch = "wasm32"))]` on `Instant::now()`, `SystemTime`, or other stdlib calls that have no rayon involvement.

    Commit strategy: one commit per file (15 commits) OR three logical commits (engines, operations, plus a final sweep). Either is acceptable. Each commit message must reference the file(s): e.g., `fix(69): gate rayon under combined wasm32+parallel cfg in src/engines/ga.rs`. Each commit body MUST include `Revert plan: revert this commit to restore wasm32-only gate on <file>`.

    Files that observe::observer::log.rs and traits/linear_chromosome.rs are NOT modified — those references are pure comments (verified in research). Skip them.

    After all files are gated, run the full verification sequence (see verify) — this is the moment of truth for Pitfall 1 (par_sort_unstable_by) and Pitfall 2 (the 3 fixed in 69-02 must still be gated correctly).
  </action>
  <verify>
    <automated>cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -5 &amp;&amp; cargo test --all-features 2>&amp;1 | tail -5 &amp;&amp; cargo test --no-default-features --features logging 2>&amp;1 | tail -5 &amp;&amp; if grep -rn 'rayon::' src/ | grep -v '#\[cfg'; then echo FAIL_UNGATED_RAYON; exit 1; else echo GREP_CLEAN; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'rayon::' src/ | grep -v '#\[cfg'` produces zero matches (no ungated rayon references remain — imports OR call-sites)
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - `cargo test --all-features` passes
    - `cargo test --no-default-features --features logging` passes (this is the critical proof — proves the parallel=off build compiles)
    - `cargo doc --no-deps` produces zero warnings
    - For each file in the modified set: `grep -c 'cfg(all(not(target_arch = "wasm32"), feature = "parallel"))' <file>` returns >= 1
    - `grep -rc 'cfg(not(all(not(target_arch = "wasm32"), feature = "parallel")))' src/` returns 0 (forbidden form must not appear)
    - For each survivor file (age, fitness, mu_comma_lambda, mu_plus_lambda): the file contains BOTH `par_sort_unstable_by` AND `sort_unstable_by` (with the matching cfg pair)
    - Golden tests pass: `cargo test --test golden_tests --all-features` exits 0 AND `cargo test --test golden_tests --no-default-features --features logging` exits 0 (skip if golden test file is named differently — check `ls tests/golden/` and adapt the test target)
    - Every commit body contains `Revert plan:` line
  </acceptance_criteria>
  <done>All 17 files (15 in this task + 3 from 69-02 still gated) use the D-06 canonical gate. parallel=off and parallel=on builds both pass tests; golden tests byte-identical; WASM clean; cargo doc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add grep CI enforcement step, update CLAUDE.md WASM rule, ship docs and intel</name>
  <files>.github/workflows/feature-matrix.yml, CLAUDE.md, README.md, src/lib.rs, CHANGELOG.md, .planning/intel/parallel-feature.md</files>
  <read_first>
    - .github/workflows/feature-matrix.yml (current job structure — insert grep step AFTER the compile/test matrix step so compile errors fail first per D-13 + specifics)
    - CLAUDE.md (the "WASM Compatibility (mandatory)" section in "Important Constraints")
    - README.md (existing Features table — find the `logging` row to copy)
    - src/lib.rs (existing Feature Flags table near top of file — find the `logging` row to copy)
    - CHANGELOG.md (existing v3.0.0 section)
    - .planning/v3.0.0-BUILD-PERF.md (Action #3 §Docs deliverables for parallel-feature.md content guidance)
  </read_first>
  <action>
    Commit 1 — Grep enforcement step in CI:
    - Add a new step to `.github/workflows/feature-matrix.yml`, positioned AFTER the matrix compile/test step (so the fast grep runs only if compile succeeds — per "specifics" in 69-CONTEXT.md). Step exact form:
      - name: "Enforce no unconditional rayon references in src/"
      - run shell snippet: `if grep -rn 'rayon::' src/ | grep -v '#\[cfg'; then echo "ERROR: unconditional rayon reference found in src/ — must be cfg-gated"; exit 1; fi`
    - The grep token is `rayon::` (NOT `use rayon`) per D-13 exactly — this catches both bare `use rayon::prelude::*;` imports AND any non-import call-site references such as `rayon::join(...)` or `rayon::current_num_threads()` that are not behind a cfg attribute. The `grep -v '#\[cfg'` filter excludes lines beginning with the cfg attribute, so cfg-gated call-sites pass cleanly.
    - Commit message: `ci(69): add grep enforcement step for ungated rayon references`. Commit body: `Revert plan: remove the enforcement step from feature-matrix.yml`.

    Commit 2 — CLAUDE.md WASM rule update:
    - Locate the "## Important Constraints" → "### WASM Compatibility (mandatory)" section.
    - Update the rayon parallelism bullet to document the NEW canonical gate per D-06: `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]` for the parallel arm and `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]` for the sequential fallback. Provide the duplicated-iterator example using the new gates (parallel arm has `par_iter()`, sequential arm has `iter()`).
    - Leave the `Instant::now()` / `SystemTime` bullet UNCHANGED (D-02 — non-rayon wasm32 gates are NOT touched).
    - Add a one-line note: "After Phase 69, the canonical gate for rayon is the combined `wasm32+parallel` form above. Do not reintroduce `#[cfg(not(target_arch = \"wasm32\"))]` as the sole gate on rayon call-sites."
    - Commit message: `docs(69): update CLAUDE.md WASM Compatibility to document combined parallel+wasm32 gate`. Commit body: `Revert plan: restore the wasm32-only rayon gate documentation`.

    Commit 3 — README.md, src/lib.rs Features tables, CHANGELOG.md, and intel file:
    - In README.md Features table, add a row for `parallel` immediately after the `logging` row: short description "Parallel fitness evaluation via rayon (default-on; disable to shed rayon + crossbeam dependencies for embedded / wasm-only builds)".
    - In src/lib.rs Feature Flags doc table, add the same `parallel` row in the same format as the existing `logging` row.
    - In CHANGELOG.md under v3.0.0 → Added: "`parallel` feature (default-on). Disable via `default-features = false` to shed rayon + crossbeam dependencies for embedded / wasm-only builds. Phase 69 Action #3."
    - Create `.planning/intel/parallel-feature.md` as a 1–2 page AI-readable note containing the following sections (exact section headers):
      - `## Why this feature exists` — rationale per BUILD-PERF.md Action #3 (~4 transitive crates shed when off; opt-in for embedded/wasm).
      - `## Canonical gate pattern` — the D-06 strings verbatim for top-level import, individual call-site pair, and par_sort_unstable_by pair. Parallel arm: `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]`. Sequential arm: `#[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]`.
      - `## What an agent must NOT reintroduce` — bullet list: no `use rayon::prelude::*;` without the combined cfg gate; no `#[cfg(not(target_arch = "wasm32"))]` as the sole gate on rayon sites; no `#[cfg(not(all(...)))]` negated form for the sequential arm; no removal of the `dep:rayon` prefix from the parallel feature; the feature name is `parallel` (semantic), NOT `rayon`.
      - `## How to verify the invariant` — three commands: `grep -rn 'rayon::' src/ | grep -v '#\[cfg'` (must be empty), `cargo test --no-default-features --features logging` (must pass), `cargo check --target wasm32-unknown-unknown` (must pass). Plus a pointer to the feature-matrix CI enforcement step.
      - `## Why the name is "parallel" and not "rayon"` — implementation-detail naming; allows swapping to another work-stealing executor later without breaking the feature flag.
    - Commit message: `docs(69): document parallel feature in README, lib.rs, CHANGELOG, intel`. Commit body: `Revert plan: revert this commit to remove parallel docs and intel`.
  </action>
  <verify>
    <automated>grep -c 'Enforce no unconditional rayon' .github/workflows/feature-matrix.yml | grep -q '^[1-9]' &amp;&amp; grep -c "grep -rn 'rayon::' src/" .github/workflows/feature-matrix.yml | grep -q '^[1-9]' &amp;&amp; grep -c 'cfg(all(not(target_arch = "wasm32"), feature = "parallel"))' CLAUDE.md | grep -q '^[1-9]' &amp;&amp; grep -c '| parallel' README.md | grep -q '^[1-9]' &amp;&amp; grep -c 'parallel' src/lib.rs | grep -q '^[1-9]' &amp;&amp; grep -c '`parallel`' CHANGELOG.md | grep -q '^[1-9]' &amp;&amp; test -f .planning/intel/parallel-feature.md &amp;&amp; grep -c 'Canonical gate pattern' .planning/intel/parallel-feature.md | grep -q '^[1-9]' &amp;&amp; cargo doc --no-deps 2>&amp;1 | (grep -c warning || true) | grep -q '^0$' &amp;&amp; echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/feature-matrix.yml` contains a step matching `Enforce no unconditional rayon` placed AFTER the compile/test step (verify by line order with `grep -n`)
    - The CI step grep token is `rayon::` exactly: `grep -c "grep -rn 'rayon::' src/" .github/workflows/feature-matrix.yml` returns >= 1
    - The CI step does NOT use the older `use rayon` token: `grep -c "grep -rn 'use rayon'" .github/workflows/feature-matrix.yml` returns 0
    - YAML parses: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/feature-matrix.yml'))"` exits 0
    - CLAUDE.md "WASM Compatibility" section contains the string `cfg(all(not(target_arch = "wasm32"), feature = "parallel"))` AND `cfg(any(target_arch = "wasm32", not(feature = "parallel")))`
    - README.md Features table has a row containing `parallel`
    - src/lib.rs Feature Flags doc-comment contains `parallel`
    - CHANGELOG.md v3.0.0 contains the parallel feature line
    - `.planning/intel/parallel-feature.md` exists and contains all 5 required section headers verbatim
    - `cargo doc --no-deps` produces zero warnings
    - Every commit body contains `Revert plan:` line
  </acceptance_criteria>
  <done>CI grep step enforces invariant using the `rayon::` token per D-13; CLAUDE.md WASM rule updated per D-06; README/lib.rs Features tables show parallel; CHANGELOG updated; intel file documents rationale + canonical pattern + invariant + verification.</done>
</task>

</tasks>

<verification>
- `grep -rn 'rayon::' src/ | grep -v '#\[cfg'` produces zero matches
- `cargo test --all-features` passes
- `cargo test --no-default-features --features logging` passes
- `cargo check --target wasm32-unknown-unknown --lib` exits 0
- Golden tests byte-identical with parallel on AND off
- `cargo doc --no-deps` zero warnings
- CI enforcement step present and YAML-valid; grep token is `rayon::` per D-13
- All commits GPG-signed with Revert plan body lines
</verification>

<success_criteria>
All ~36 rayon call-sites across 17 files use D-06 canonical combined gate; parallel=off and parallel=on both compile and pass tests; CI grep step catches future regressions; CLAUDE.md documents new canonical gate; feature is documented in README/lib.rs/CHANGELOG; intel file ready for future agents.
</success_criteria>

<output>
Create `.planning/phases/69-build-perf-m3-major-refactors/69-03-SUMMARY.md` when done. SUMMARY must include: list of files modified with the cfg gate count per file, the grep CI step contents, and confirmation that golden tests passed in both parallel modes.
</output>
</content>
</invoke>