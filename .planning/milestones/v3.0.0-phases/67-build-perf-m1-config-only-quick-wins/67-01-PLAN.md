---
phase: 67-build-perf-m1-config-only-quick-wins
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - docs/DEVELOPMENT.md
  - .planning/intel/build-profile.md
  - CHANGELOG.md
autonomous: true
requirements: []
tags: [build-perf, cargo-profile, dev-experience]

must_haves:
  truths:
    - "D-01: Phase 66 baseline (.planning/baselines/v3.0.0-baseline.json), golden tests (tests/golden/), and build-perf-gate.yml exist before executing this plan"
    - "D-09: Cargo.toml declares [profile.dev], [profile.dev.package.\"*\"], and [profile.test] with the values locked verbatim from BUILD-PERF.md §Action #5/#6"
    - "D-10: This plan writes the docs/DEVELOPMENT.md \"Cargo profiles\" section + CHANGELOG.md Changed entry + .planning/intel/build-profile.md (all three deliverables required)"
    - "D-14: The commit body for this plan MUST include a `Revert plan:` line per BUILD-PERF.md non-negotiable guarantee #5"
    - "Local `cargo test` and `cargo build` continue to succeed with default + serde feature sets"
    - "`cargo check --target wasm32-unknown-unknown --lib` succeeds (no regression to WASM)"
    - "`cargo clippy --all-targets -D warnings` stays green"
    - "Golden tests in tests/golden/ produce byte-identical output to baseline"
    - "Contributors and AI agents can find the rationale for the profile choices in docs/DEVELOPMENT.md and .planning/intel/build-profile.md"
  artifacts:
    - path: "Cargo.toml"
      provides: "Three new [profile.*] blocks at end of file"
      contains: "[profile.dev]"
    - path: "Cargo.toml"
      provides: "Per-dep opt-level=1"
      contains: "[profile.dev.package.\"*\"]"
    - path: "Cargo.toml"
      provides: "Test profile with opt-level=1"
      contains: "[profile.test]"
    - path: "docs/DEVELOPMENT.md"
      provides: "Cargo profiles section explaining the tuning for contributors"
      contains: "Cargo profiles"
    - path: ".planning/intel/build-profile.md"
      provides: "AI-agent-facing rationale for the profile blocks (so they are not silently reverted)"
      min_lines: 20
    - path: "CHANGELOG.md"
      provides: "[Unreleased] section + Changed entry naming the profile additions"
      contains: "[Unreleased]"
  key_links:
    - from: "Cargo.toml"
      to: "tests/golden/"
      via: "test profile opt-level affects test runtime; golden outputs must remain byte-identical"
      pattern: "opt-level = 1"
    - from: "docs/DEVELOPMENT.md"
      to: ".planning/intel/build-profile.md"
      via: "Contributor doc references the AI-agent intel note"
      pattern: "build-profile.md"
---

<objective>
Land the Cargo profile tuning locked by D-09: three new `[profile.*]` blocks in `Cargo.toml` that shave dev-build and test wall-clock with zero behavioural change. Publish the contributor-facing rationale in `docs/DEVELOPMENT.md` and the AI-agent rationale in `.planning/intel/build-profile.md`, so future agents do not silently revert the change.

Purpose: This is plan 1 of 4 in Phase 67 Wave 0 (all four run in parallel). It owns the Cargo.toml change exclusively. It is also the CHANGELOG section-creator for this phase — it creates the `## [Unreleased]` section that plans 67-02 / 67-03 / 67-04 will append to.
Output: Three TOML profile blocks, two markdown files (one new, one appended), and one CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v3.0.0-BUILD-PERF.md
@.planning/phases/67-build-perf-m1-config-only-quick-wins/67-CONTEXT.md
@.planning/phases/67-build-perf-m1-config-only-quick-wins/67-RESEARCH.md
@Cargo.toml
@docs/DEVELOPMENT.md
@CHANGELOG.md
@CLAUDE.md

<interfaces>
<!-- Exact TOML to append to Cargo.toml. Source: BUILD-PERF.md §Action #5/#6, locked verbatim by D-09. -->

```toml
[profile.dev]
# debuginfo for line numbers in backtraces, no full DWARF.
debug = "line-tables-only"
# Default split-debuginfo on macOS is slow; line tables alone shave seconds.
split-debuginfo = "unpacked"

[profile.dev.package."*"]
# Optimise dependencies once; speeds up runtime tests on rand/rayon/log
# at the cost of ~5-10 s on first build (one-time, then cached).
opt-level = 1
debug = false

[profile.test]
# Tests run a lot of GA generations; -O1 cuts runtime ~50 % at no rustc cost.
opt-level = 1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append three [profile.*] blocks to Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (confirm no existing [profile.*] blocks; append at end of file, after the last [[example]] block)
    - .planning/v3.0.0-BUILD-PERF.md §Action #5 and §Action #6 (canonical TOML)
    - .planning/phases/67-build-perf-m1-config-only-quick-wins/67-CONTEXT.md §D-09 (locked spec)
  </read_first>
  <action>
    Append the three TOML blocks from the `<interfaces>` section above verbatim at the END of `Cargo.toml` (after the final `[[example]]` block). Keys MUST be exactly (per D-09):
    - `[profile.dev]` with `debug = "line-tables-only"` and `split-debuginfo = "unpacked"`
    - `[profile.dev.package."*"]` with `opt-level = 1` and `debug = false`
    - `[profile.test]` with `opt-level = 1`
    Do NOT modify `[dependencies]`, `[dev-dependencies]`, `[features]`, `[package]`, `[[bench]]`, or `[[example]]` sections. Do NOT add any new crate dependency (would trip `build-perf-gate` `dep_count` check per Pitfall 2 in RESEARCH.md). Preserve all existing comments and blank-line spacing. Quote the package glob exactly as `"*"` (double-quoted string).
  </action>
  <verify>
    <automated>grep -q '^\[profile\.dev\]$' Cargo.toml && grep -q '^\[profile\.dev\.package\."\*"\]$' Cargo.toml && grep -q '^\[profile\.test\]$' Cargo.toml && grep -q 'debug = "line-tables-only"' Cargo.toml && grep -q 'split-debuginfo = "unpacked"' Cargo.toml && cargo build --quiet 2>&1 | tee /tmp/67-01-build.log && cargo test --lib --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[profile\.dev\]$' Cargo.toml` returns 1
    - `grep -c '^\[profile\.dev\.package\."\*"\]$' Cargo.toml` returns 1
    - `grep -c '^\[profile\.test\]$' Cargo.toml` returns 1
    - `grep -c 'debug = "line-tables-only"' Cargo.toml` returns 1
    - `grep -c 'split-debuginfo = "unpacked"' Cargo.toml` returns 1
    - `grep -c '^opt-level = 1$' Cargo.toml` returns 2 (one for dev.package."*", one for test)
    - `cargo build` exits 0 (TOML accepted by cargo)
    - `cargo test --lib` exits 0 with no behavioural test failures
    - No new entry under `[dependencies]`, `[dev-dependencies]`, or `[build-dependencies]` (dep_count unchanged)
  </acceptance_criteria>
  <done>Cargo.toml contains the three profile blocks verbatim from D-09; the existing crate compiles and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Append "Cargo profiles" section to docs/DEVELOPMENT.md and create .planning/intel/build-profile.md</name>
  <files>docs/DEVELOPMENT.md, .planning/intel/build-profile.md</files>
  <read_first>
    - docs/DEVELOPMENT.md (read full file so the new section blends with existing voice and heading hierarchy)
    - .planning/v3.0.0-BUILD-PERF.md §Action #5/#6 (rationale text source)
    - .planning/phases/67-build-perf-m1-config-only-quick-wins/67-CONTEXT.md §D-10 (deliverable scope)
  </read_first>
  <action>
    Per D-10, this task produces TWO of the three D-10 deliverables (docs/DEVELOPMENT.md section + .planning/intel/build-profile.md; the CHANGELOG entry is Task 3).

    1. Append a new top-level section `## Cargo profiles` to the END of `docs/DEVELOPMENT.md`. The section MUST contain:
       - A one-sentence summary of what changed in `Cargo.toml`.
       - A subsection `### [profile.dev]` explaining `debug = "line-tables-only"` (line numbers in backtraces, smaller debug section, faster linking) and `split-debuginfo = "unpacked"` (macOS perf win, harmless on Linux).
       - A subsection `### [profile.dev.package."*"]` explaining `opt-level = 1` for dependencies (one-time ~5-10 s cost, then cached; speeds up `rand`/`rayon`/`log` at runtime) and `debug = false` for deps.
       - A subsection `### [profile.test]` explaining `opt-level = 1` (cuts GA test runtime ~50 %; no rustc-time regression).
       - A subsection `### Reverting` pointing at the canonical revert: delete the three blocks (and link to `.planning/intel/build-profile.md` for the AI-agent context).
    2. Create the new file `.planning/intel/build-profile.md` (the `.planning/intel/` directory does not exist yet — create it). Content MUST include:
       - Heading: `# Build profile rationale (Phase 67 / Action #5/#6)`
       - Why each TOML key is set the way it is (cite `.planning/v3.0.0-BUILD-PERF.md §Action #5` and `§Action #6`).
       - Explicit "DO NOT REMOVE" note explaining that future build-time refactors might tempt agents to drop these blocks; they were measured to deliver ≥5 % dev-build improvement vs the Phase 66 baseline (D-01).
       - "Why opt-level=1 in [profile.test]": tests exercise long GA loops; runtime dominates over rustc compile cost.
       - Cross-link back to `.planning/v3.0.0-BUILD-PERF.md` and the SUMMARY this plan will produce.
  </action>
  <verify>
    <automated>grep -q '^## Cargo profiles$' docs/DEVELOPMENT.md && grep -q '^### \[profile\.dev\]$' docs/DEVELOPMENT.md && grep -q '^### \[profile\.dev\.package\."\*"\]$' docs/DEVELOPMENT.md && grep -q '^### \[profile\.test\]$' docs/DEVELOPMENT.md && grep -q '^### Reverting$' docs/DEVELOPMENT.md && test -f .planning/intel/build-profile.md && grep -q 'Phase 67' .planning/intel/build-profile.md && grep -q 'DO NOT REMOVE' .planning/intel/build-profile.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/DEVELOPMENT.md` contains a top-level `## Cargo profiles` heading
    - `docs/DEVELOPMENT.md` contains four subsections: `### [profile.dev]`, `### [profile.dev.package."*"]`, `### [profile.test]`, `### Reverting`
    - `.planning/intel/build-profile.md` exists and is at least 20 non-blank lines
    - `.planning/intel/build-profile.md` references `Phase 67` and the string `DO NOT REMOVE`
    - `.planning/intel/build-profile.md` cites `.planning/v3.0.0-BUILD-PERF.md` (substring `v3.0.0-BUILD-PERF.md`)
    - `cargo doc --no-deps` exits 0 with zero warnings (markdown additions do not affect rustdoc)
  </acceptance_criteria>
  <done>Both markdown files exist with the required structure and content; contributors and AI agents can find the rationale for the TOML additions.</done>
</task>

<task type="auto">
  <name>Task 3: Create `## [Unreleased]` section in CHANGELOG.md and add profile-tuning Changed entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (FULL file — confirm whether `## [Unreleased]` already exists; if absent, this plan creates it per Pitfall 3 mitigation; if present, append under it)
    - .planning/phases/67-build-perf-m1-config-only-quick-wins/67-CONTEXT.md §D-10 (CHANGELOG deliverable scope — third of three D-10 deliverables)
  </read_first>
  <action>
    Per D-10, this task lands the CHANGELOG portion of the D-10 deliverable set.

    1. If `## [Unreleased]` is NOT already present in `CHANGELOG.md`, insert it immediately AFTER the file's top-level title and any introductory boilerplate but BEFORE the first existing version heading (e.g., `## [3.0.0]`). Use Keep-a-Changelog format with empty `### Added`, `### Changed`, `### Removed` subsection headers so that plans 67-02 / 67-03 / 67-04 can append into them.
    2. Under `## [Unreleased]` → `### Changed`, add a bullet:
       - `Tune \`[profile.dev]\`, \`[profile.dev.package."*"]\`, and \`[profile.test]\` for faster local dev/test wall-clock (~5-15 % clean dev build; ~50 % test runtime). See \`docs/DEVELOPMENT.md\` §Cargo profiles. (Phase 67 / Plan 67-01)`
    3. Do NOT alter any existing `## [3.0.0]` or earlier version sections.
    4. If `## [Unreleased]` already existed but lacks `### Added`/`### Changed`/`### Removed`, add only the missing subsection headers needed for this entry (at minimum `### Changed`).
  </action>
  <verify>
    <automated>grep -q '^## \[Unreleased\]$' CHANGELOG.md && grep -q '^### Changed$' CHANGELOG.md && grep -q 'profile.dev' CHANGELOG.md && grep -q 'Phase 67' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[Unreleased\]$' CHANGELOG.md` returns 1
    - `## [Unreleased]` appears BEFORE any `## [3.x.x]` heading (verify via `grep -n` line ordering)
    - At least one bullet under the Unreleased section mentions `profile.dev` and `Phase 67`
    - No existing `## [3.0.0]` (or earlier) entries were modified (`git diff` shows only additions in the Unreleased region)
  </acceptance_criteria>
  <done>CHANGELOG.md has `## [Unreleased]` with `### Changed` populated; downstream plans 67-02/03/04 can append cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.toml -> build system | TOML profile keys are parsed by cargo; malformed values cause build failure (immediate, loud). No supply-chain risk: no new crate deps added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-T | Tampering | Cargo.toml profile blocks | mitigate | Acceptance criteria assert exact key/value strings; CI `cargo build` proves cargo accepts the TOML |
| T-67-01-I | Information disclosure | debug section size in shipped artifacts | accept | `debug = "line-tables-only"` reduces (not increases) debug info; only affects dev profile, never release |
| T-67-01-SC | Tampering | npm/pip/cargo installs | accept | Plan adds zero crate dependencies (config-only change); `dep_count` invariant preserved per Pitfall 2 |
</threat_model>

<verification>
After all three tasks complete, run the Phase 67 non-negotiable verification suite:

```bash
cargo build --quiet
cargo test --quiet
cargo test --features serde --quiet
cargo clippy --all-targets -- -D warnings
cargo check --target wasm32-unknown-unknown --lib
cargo check --target wasm32-unknown-unknown --lib --features serde
cargo doc --no-deps 2>&1 | tee /tmp/67-01-doc.log
grep -c 'warning' /tmp/67-01-doc.log     # must be 0
cargo test --test golden_tests 2>/dev/null || cargo test --quiet -- golden    # golden tests byte-identical
```

All commands MUST exit 0. `cargo doc --no-deps` MUST emit zero warning lines.
</verification>

<success_criteria>
1. `grep -q '\[profile\.dev\]' Cargo.toml && grep -q '\[profile\.dev\.package' Cargo.toml && grep -q '\[profile\.test\]' Cargo.toml` succeeds (SC#1)
2. `cargo test` and `cargo test --features serde` both pass (non-negotiable guarantee #1)
3. `cargo clippy --all-targets -- -D warnings` exits 0 (SC#6, non-negotiable guarantee #2)
4. `cargo check --target wasm32-unknown-unknown` exits 0 (non-negotiable guarantee #3)
5. `cargo doc --no-deps` emits zero warnings (SC#6, non-negotiable guarantee #4)
6. Golden tests (tests/golden/) produce byte-identical output (non-negotiable guarantee, Phase 66 CC-3 — D-01 precondition)
7. `docs/DEVELOPMENT.md` has `## Cargo profiles` section with four subsections
8. `.planning/intel/build-profile.md` exists and explains the rationale
9. `CHANGELOG.md` has `## [Unreleased]` with the Changed entry
10. Commit body contains a `Revert plan:` line (D-14) — see output section
</success_criteria>

<output>
1. Create `.planning/phases/67-build-perf-m1-config-only-quick-wins/67-01-SUMMARY.md` describing what shipped, the measured before/after build times (`time cargo build` clean, `time cargo test`), and a pointer to `.planning/intel/build-profile.md`.

2. Commit with message body that MUST include a `Revert plan:` line (D-14). Suggested commit message template:

```
build(67-01): tune [profile.dev]/[profile.test] for faster local iteration

Adds [profile.dev] (debug=line-tables-only, split-debuginfo=unpacked),
[profile.dev.package."*"] (opt-level=1, debug=false), and [profile.test]
(opt-level=1) per .planning/v3.0.0-BUILD-PERF.md §Action #5/#6 (D-09).

Documents the change in docs/DEVELOPMENT.md §Cargo profiles and the
AI-agent rationale in .planning/intel/build-profile.md so future
refactors do not silently revert the tuning.

Revert plan: delete the three [profile.*] blocks at the end of Cargo.toml,
delete .planning/intel/build-profile.md, and remove the §Cargo profiles
section from docs/DEVELOPMENT.md. No code change required.
```
</output>
</content>
</invoke>
