---
phase: 76-parallelize-survivor-selection-and-non-dominated-sorting-iss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/nsga2/mod.rs
  - src/engines/nsga2/non_dominated_sort.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "nsga2::non_dominated_sort re-exports from multi_objective::non_dominated_sort"
    - "All existing imports via nsga2::non_dominated_sort still resolve"
    - "The duplicate non_dominated_sort.rs file no longer exists"
  artifacts:
    - path: "src/engines/nsga2/mod.rs"
      provides: "Re-export of shared non_dominated_sort module"
      contains: "pub use crate::multi_objective::non_dominated_sort;"
    - path: "src/engines/nsga2/non_dominated_sort.rs"
      provides: "Deleted duplicate"
      min_lines: 0
  key_links:
    - from: "src/engines/nsga2/mod.rs"
      to: "src/engines/multi_objective/non_dominated_sort.rs"
      via: "pub use re-export"
      pattern: "pub use crate::multi_objective::non_dominated_sort;"
---

<objective>
Delete the duplicate `non_dominated_sort.rs` from nsga2 and re-export from the shared multi_objective module. This eliminates code duplication so that parallel improvements in the shared module apply to all engines including NSGA-II.

Purpose: D-01 and D-02 from CONTEXT.md — deduplicate non-dominated sorting before parallelizing.
Output: Single shared `non_dominated_sort.rs` module; nsga2 backward-compatible re-export.
</objective>

<execution_context>
@/Users/luis/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/luis/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-parallelize-survivor-selection-and-non-dominated-sorting-iss/76-CONTEXT.md
@.planning/phases/76-parallelize-survivor-selection-and-non-dominated-sorting-iss/76-RESEARCH.md
@.planning/phases/76-parallelize-survivor-selection-and-non-dominated-sorting-iss/76-PATTERNS.md
@src/engines/multi_objective/non_dominated_sort.rs
@src/engines/nsga2/mod.rs
@src/engines/nsga2/non_dominated_sort.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete duplicate nsga2 non_dominated_sort.rs and re-export from shared module</name>
  <files>src/engines/nsga2/non_dominated_sort.rs, src/engines/nsga2/mod.rs</files>

  <read_first>
    - src/engines/nsga2/mod.rs — current module declarations (lines 107-110) and imports (lines 112-130)
    - src/engines/nsga2/non_dominated_sort.rs — the duplicate file to delete (205 lines, identical algorithm to multi_objective version, has extra doc-tests)
    - src/engines/multi_objective/non_dominated_sort.rs — the shared canonical implementation (147 lines)
    - src/engines/island/nsga2.rs — line 53: `use crate::nsga2::non_dominated_sort::{assign_ranks, non_dominated_sort};` — will still resolve after re-export
    - benches/nsga2.rs — line 2: `use genetic_algorithms::nsga2::non_dominated_sort::non_dominated_sort;` — will still resolve after re-export
    - tests/engines/nsga2/test_non_dominated_sort.rs — lines 1-5: imports via `genetic_algorithms::nsga2::non_dominated_sort::*` — will still resolve after re-export
  </read_first>

  <action>
    **Step 1: Delete the duplicate file.**
    Delete `src/engines/nsga2/non_dominated_sort.rs` entirely. This file is an exact duplicate of `src/engines/multi_objective/non_dominated_sort.rs` with extra doc-test examples.

    **Step 2: Update nsga2/mod.rs (per D-02).**
    In `src/engines/nsga2/mod.rs`, replace line 109:
    ```
    pub mod non_dominated_sort;
    ```
    with:
    ```
    pub use crate::multi_objective::non_dominated_sort;
    ```

    This re-export makes `nsga2::non_dominated_sort` an alias for `multi_objective::non_dominated_sort`. All existing import paths continue to resolve:
    - `use genetic_algorithms::nsga2::non_dominated_sort::non_dominated_sort;` (benches)
    - `use crate::nsga2::non_dominated_sort::{assign_ranks, non_dominated_sort};` (island engine)
    - `use genetic_algorithms::nsga2::non_dominated_sort::{...};` (tests)

    **Do NOT touch** `src/engines/nsga2/pareto.rs` — it is out of scope for this phase (per RESEARCH.md A3).
  </action>

  <verify>
    <automated>cargo test && cargo test --doc && cargo clippy --all-targets -- -D warnings</automated>
  </verify>

  <acceptance_criteria>
    - `src/engines/nsga2/non_dominated_sort.rs` does not exist
    - `src/engines/nsga2/mod.rs` contains `pub use crate::multi_objective::non_dominated_sort;`
    - `src/engines/nsga2/mod.rs` does NOT contain `pub mod non_dominated_sort;`
    - `cargo test` exits 0 (all 42+ tests pass)
    - `cargo test --doc` exits 0 (all doc-tests pass, including the deleted file's doctests which now resolve via re-export)
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>

  <done>
    The duplicate non_dominated_sort.rs is deleted. nsga2/mod.rs re-exports the shared module. All existing import paths via nsga2::non_dominated_sort still resolve. No behavioral change — pure deduplication.
  </done>
</task>

</tasks>

<verification>
After this plan completes:
1. `cargo test` — all tests pass (test_non_dominated_sort.rs still imports via nsga2 path)
2. `cargo test --doc` — doc-tests pass (re-exported module has the same public API)
3. `cargo clippy --all-targets -- -D warnings` — no warnings
4. `grep -r "pub mod non_dominated_sort" src/engines/nsga2/` returns 0 matches
5. `grep -r "pub use crate::multi_objective::non_dominated_sort" src/engines/nsga2/` returns 1 match
</verification>

<success_criteria>
The duplicate file is eliminated and all consumers now use the shared multi_objective::non_dominated_sort module via re-export. Zero behavioral change — same algorithm, same API, single codebase.
</success_criteria>

<output>
Create `.planning/phases/76-parallelize-survivor-selection-and-non-dominated-sorting-iss/76-01-SUMMARY.md` when done
</output>
