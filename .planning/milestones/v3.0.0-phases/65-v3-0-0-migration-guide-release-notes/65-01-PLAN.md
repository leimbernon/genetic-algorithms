---
phase: 65
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - MIGRATION.md
autonomous: true
requirements: []
tags:
  - documentation
  - migration-guide
  - v3.0.0
  - release

must_haves:
  truths:
    - "MIGRATION.md covers all 10 breaking changes from Phase 65 success criteria"
    - "Every breaking-change section has a `### Compiler error` subsection with fenced code block showing an `error[E...]` rustc message"
    - "MIGRATION.md adds a new `## DeGene → RealGene` rename section"
    - "MIGRATION.md adds a new `## SelectionOperator::select — new num_parents parameter` section"
    - "MIGRATION.md adds a new `## Mutation enum variant parameter changes` section"
    - "MIGRATION.md adds a new `## parallel feature — rayon is now optional` top-level section"
    - "MIGRATION.md adds a new `## logging feature — log crate is now optional` top-level section"
    - "The existing `## Trait split: ChromosomeT + LinearChromosome` section contains a prominent callout that custom chromosomes must implement `LinearChromosome` (not just `ChromosomeT`) to be compatible with built-in operators (D-03)"
    - "MIGRATION.md introduction sentence is updated to reflect the new count of breaking changes (no longer 'seven')"
  artifacts:
    - path: "MIGRATION.md"
      provides: "Full v3.0.0 migration guide with all breaking-change recipes"
      contains: "## DeGene → RealGene"
    - path: "MIGRATION.md"
      provides: "SelectionOperator section"
      contains: "## SelectionOperator::select"
    - path: "MIGRATION.md"
      provides: "Mutation enum section"
      contains: "## Mutation enum variant parameter changes"
    - path: "MIGRATION.md"
      provides: "parallel feature section"
      contains: "## parallel feature"
    - path: "MIGRATION.md"
      provides: "logging feature section"
      contains: "## logging feature"
  key_links:
    - from: "README.md"
      to: "MIGRATION.md"
      via: "upgrade banner line 4"
      pattern: "MIGRATION\\.md"
    - from: "CHANGELOG.md"
      to: "MIGRATION.md"
      via: "introduction prose in `## [3.0.0]` section"
      pattern: "MIGRATION\\.md"
---

<objective>
Author the complete v3.0.0 `MIGRATION.md` so every breaking change introduced between v2.4.0 and v3.0.0 has a copy-pasteable recipe (Before / After / Compiler error / Fix).

Purpose: Users upgrading from v2.x to v3.0.0 must find every breaking change in one authoritative file, with the exact compiler error rustc emits and the precise fix. This is the file the `README.md` upgrade banner already points at — it must be complete before v3.0.0 ships.

Output: Updated `MIGRATION.md` covering all 10 breaking-change entries plus 2 feature-flag sections, every entry containing a `### Compiler error` block with a real `error[E...]` example.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md
@MIGRATION.md
@CHANGELOG.md
@README.md
@src/traits/operators.rs
@src/traits/real_gene.rs
@src/operations/mutation.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `### Compiler error` subsections to the 8 existing breaking-change entries and reinforce the LinearChromosome bound callout (D-03, D-04)</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md (current 8-entry file — read fully to see exact section headings, ordering, and surrounding prose; do NOT guess at headings)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md (locked decisions D-03 and D-04)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"Compiler Error Reference — All 10 Breaking Changes" table (error code per entry) and §"MIGRATION.md Structure Pattern"
  </read_first>
  <behavior>
    - For each of the 8 existing `## ` sections in MIGRATION.md, an `### Compiler error` H3 subsection appears between the existing `### After` block and the closing `**Fix:**` line.
    - The 8 sections receiving the new subsection are exactly: `## Trait split: ChromosomeT + LinearChromosome`, `## LinearChromosome: default() renamed to reset()`, `## Reporter removed — use GaObserver`, `## ChromosomeLength replaces genes_per_chromosome`, `## Flat stopping builders replace StoppingCriteria struct`, `## LimitConfiguration field removals`, `## GaConfiguration field access → accessor methods`, `## Logger setup (v2 auto-init → v3 explicit)`.
    - Each `### Compiler error` block uses a fenced (non-rust) plaintext code block whose first line begins with `error[EXXXX]:` where the error code matches the table in RESEARCH.md §"Compiler Error Reference" (E0277, E0599, E0432/E0412, E0599, E0412, E0559, E0616, E0412 respectively — see acceptance criteria for the exact mapping per section).
    - Each error block contains the standard rustc `  --> src/file.rs:line:col` pointer line followed by the offending snippet with a caret marker line, matching the structure documented in 65-RESEARCH.md §"MIGRATION.md Structure Pattern".
    - The `## Trait split: ChromosomeT + LinearChromosome` section gains a clearly visible callout (blockquote `> **Note:** ...` or bolded paragraph) stating that custom chromosomes implementing only `ChromosomeT` will fail to compile against any built-in operator and must add an `impl LinearChromosome for ...` block — this is D-03 folded into the existing section, NOT a new top-level `##` section.
    - The introductory sentence "This guide covers all seven breaking changes" is updated to reflect the new total once Task 2 also lands (use "all breaking changes" or "every breaking change" so the count is not hard-coded — both Tasks 1 and 2 edit the introduction; coordinate the wording).
  </behavior>
  <action>
    Open MIGRATION.md. For every existing `## ` heading in the file (eight of them, listed in behavior), insert a new `### Compiler error` H3 subsection immediately after the closing fence of the `### After` code block and before the final `**Fix:**` bolded line.

    Use this exact mapping of section → error code → key message fragment (D-04 requires a real `error[E...]` line; the Plan 65-03 verifier may later refine wording but the error code and pointer structure must be correct now):

    1. `## Trait split: ChromosomeT + LinearChromosome` → `error[E0277]: the trait bound \`MyChromosome: LinearChromosome\` is not satisfied` with pointer at the operator call site (e.g. `Ga::new().with_chromosome_length(...)` line).
    2. `## LinearChromosome: default() renamed to reset()` → `error[E0599]: no method named \`default\` found for ...` (note: this is the `LinearChromosome::default()` method, not the `Default` trait — make sure the message text disambiguates).
    3. `## Reporter removed — use GaObserver` → `error[E0432]: unresolved import \`genetic_algorithms::reporter\`` (primary) plus a follow-up `error[E0599]: no method named \`with_reporter\` found for struct \`Ga\`` to cover the builder-method removal.
    4. `## ChromosomeLength replaces genes_per_chromosome` → `error[E0599]: no method named \`with_genes_per_chromosome\` found for struct \`Ga\``.
    5. `## Flat stopping builders replace StoppingCriteria struct` → `error[E0432]: unresolved import \`genetic_algorithms::configuration::StoppingCriteria\``.
    6. `## LimitConfiguration field removals` → `error[E0560]: struct \`LimitConfiguration\` has no field named \`needs_unique_ids\`` (use E0559 or E0560 depending on which rustc emits for missing-field; either is acceptable — pick one consistently).
    7. `## GaConfiguration field access → accessor methods` → `error[E0616]: field \`limit_configuration\` of struct \`GaConfiguration\` is private`.
    8. `## Logger setup (v2 auto-init → v3 explicit)` → `error[E0432]: unresolved import \`genetic_algorithms::configuration::LogLevel\`` plus a follow-up `error[E0599]: no method named \`with_logs\` found for struct \`Ga\``.

    Each error block follows the structure:

    ```
    error[EXXXX]: <message text>
      --> src/main.rs:<line>:<col>
       |
    <line> |     <offending v2 code snippet>
       |     ^^^^^^^^^^^^^ <inline annotation>
       |
       = help: <one-line suggestion when present in real rustc output>
    ```

    Use realistic file paths (`src/main.rs` or `src/lib.rs`), realistic line numbers (e.g. `5`, `12`), and caret lines whose `^^^` span matches the identifier length. Copy the exact phrasing from `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md` §"Compiler Error Reference" where it supplies the message body.

    For D-03 (LinearChromosome bound callout): in the `## Trait split: ChromosomeT + LinearChromosome` section ONLY, immediately after the `### Compiler error` block and before `**Fix:**`, add a blockquote callout in this exact form (the wording may be adjusted for readability but the prefix `> **Note:**` and the substantive content — that v3 operators require `LinearChromosome`, not `ChromosomeT` — must remain):

    > **Note:** In v3, all built-in operators (selection, crossover, mutation, survivor) bound on `LinearChromosome` instead of `ChromosomeT`. If your custom chromosome only implements `ChromosomeT`, every operator call site fails with the E0277 error above. Add an `impl LinearChromosome for YourChromosome { ... }` block (see the After example above) to restore compatibility. Tree-shaped chromosomes that intentionally do NOT support flat-slice access should implement `TreeChromosome: ChromosomeT` and use the `GpGa<U>` engine instead of `Ga<U>`.

    Finally, update the introductory paragraph (currently "This guide covers all seven breaking changes with before/after code for each."). Replace the phrase "all seven breaking changes" with "every breaking change" so it stays accurate as Task 2 adds 5 more sections.

    Do NOT touch any existing `### Before`, `### After`, or `**What changed**` content in the 8 sections — only add the `### Compiler error` subsection (and, for section 1, the D-03 callout). Do not reorder sections.
  </action>
  <verify>
    <automated>grep -c '^### Compiler error$' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - MIGRATION.md contains exactly 8 occurrences of the literal heading `### Compiler error` after Task 1 completes (one per existing `##` section). Command: `[ "$(grep -c '^### Compiler error$' MIGRATION.md)" = "8" ]` exits 0.
    - MIGRATION.md contains the literal string `error[E0277]` at least once (Trait split section).
    - MIGRATION.md contains the literal string `error[E0599]` at least three times (default→reset, with_reporter, with_genes_per_chromosome, with_logs).
    - MIGRATION.md contains the literal string `error[E0432]` at least three times (reporter import, StoppingCriteria import, LogLevel import).
    - MIGRATION.md contains the literal string `error[E0616]` at least once (GaConfiguration private field).
    - MIGRATION.md contains the literal string `> **Note:**` at least once within 100 lines of the heading `## Trait split: ChromosomeT + LinearChromosome` (D-03 callout). Command: `awk '/^## Trait split: ChromosomeT \+ LinearChromosome$/,/^## /' MIGRATION.md | grep -c '^> \*\*Note:\*\*'` returns a value ≥ 1.
    - MIGRATION.md no longer contains the literal phrase `seven breaking changes` (case-insensitive). Command: `grep -i -c 'seven breaking changes' MIGRATION.md` returns 0.
    - The 8 existing `## ` headings remain present unchanged. Command: `grep -c '^## ' MIGRATION.md` returns ≥ 8.
    - All `### Compiler error` blocks are followed within 30 lines by a `**Fix:**` bolded line (structural sanity).
  </acceptance_criteria>
  <done>
    Every one of the 8 existing breaking-change sections in MIGRATION.md has a `### Compiler error` H3 subsection with a real `error[E...]` line and caret-style rustc output. The `## Trait split` section additionally has a `> **Note:**` callout describing the LinearChromosome bound requirement. The introduction no longer claims "seven" breaking changes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the 3 missing breaking-change sections and 2 new feature-flag sections (D-01, D-02)</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md (post-Task-1 state — the 8 existing sections must remain untouched; only append new sections)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md (decisions D-01 and D-02)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"Breaking Change Detail — New Entries" (full pre-written content for entries 9, 10, 11 and the parallel feature section)
    - src/traits/operators.rs (lines 38–54: current `SelectionOperator::select` signature — copy exactly into the After block)
    - src/traits/real_gene.rs (current `RealGene` trait surface — confirm method names `real_value`, `with_real_value`, `bounds`)
    - src/operations/mutation.rs (current `Mutation` enum to confirm v3 variant names: `PermutationInsert`, `Insertion`, `Deletion`, struct-field variants `Gaussian { sigma }`, `Creep { step }`, etc.)
    - Cargo.toml lines 32–35 (current `[features]` block — confirms `default = ["logging", "parallel"]`, `logging = ["dep:log"]`, `parallel = ["dep:rayon"]`)
  </read_first>
  <behavior>
    - Five new top-level `##` sections are appended to MIGRATION.md AFTER the last existing section (currently `## Logger setup (v2 auto-init → v3 explicit)`). Order: (a) `## DeGene → RealGene rename`, (b) `## SelectionOperator::select — new num_parents parameter`, (c) `## Mutation enum variant parameter changes`, (d) `## parallel feature — rayon is now optional`, (e) `## logging feature — log crate is now optional`.
    - Each of sections (a), (b), (c) follows the established 5-part pattern: `**What changed (...):**` → `**Who is affected:**` → `### Before` → `### After` → `### Compiler error` → `**Fix:**`. The `### Compiler error` block is mandatory per D-04.
    - Section (a) describes the rename of `DeGene` → `RealGene` (Phase 56), pointing to import path change from `traits::DeGene` to `traits::RealGene`. Before block imports `DeGene`; After block imports `RealGene`; error block uses `error[E0432]` or `error[E0412]` for the unresolved trait name.
    - Section (b) describes the addition of the fourth parameter `num_parents: usize` to `SelectionOperator::select` and the return-type change from `Vec<(usize, usize)>` to `Vec<Vec<usize>>`. Before block shows a v2 custom impl returning pairs; After block shows the v3 impl returning vec-of-vec; error block uses `error[E0053]` (incompatible method type for trait).
    - Section (c) describes the v2→v3 `Mutation` enum changes: (i) `Mutation::Insertion` was renamed to `Mutation::PermutationInsert` because `Insertion` is now a variable-length variant; (ii) variants `Gaussian`, `Creep`, `Polynomial`, `NonUniform` gained inline struct fields (e.g. `Gaussian { sigma: Option<f64> }`) replacing the removed standalone builder methods `with_mutation_sigma` / `with_mutation_step`. Error block uses `error[E0599]` (variant not found, suggests `PermutationInsert`) and a second `error[E0308]` (mismatched types — expected struct variant) or equivalent for the unit-variant-now-struct-variant case.
    - Section (d) `## parallel feature — rayon is now optional` follows a simplified pattern (no compiler error required for the user — the feature is default-on so existing code is unaffected; the section documents how to opt out). Includes: `**What changed (Phase 69):**`, `**Who is affected:**`, an `### Opting out` subsection showing the Cargo.toml `default-features = false` snippet, and a closing note that all parallel call-sites in the library now use the canonical `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]` gate. No `### Compiler error` subsection is required for this section because v2 users see no compiler error — the feature defaults preserve v2 behavior.
    - Section (e) `## logging feature — log crate is now optional` follows the same pattern as (d): `**What changed (Phase 68):**`, `**Who is affected:**`, `### Opting out` showing Cargo.toml `default-features = false`. This is distinct from the existing `## Logger setup` section which covers the `env_logger` auto-install removal. This new section covers ONLY the `logging` feature flag mechanism (the user-facing opt-out switch). Cross-reference the existing `## Logger setup` section for the related env_logger / LogLevel removals. No `### Compiler error` subsection required (same rationale as the parallel feature).
    - After insertion, MIGRATION.md contains exactly 13 top-level `## ` sections total (8 existing + 5 new).
  </behavior>
  <action>
    Append 5 new top-level `##` sections to MIGRATION.md immediately after the existing `## Logger setup (v2 auto-init → v3 explicit)` section. The sections must be added in this exact order, separated by horizontal-rule lines (`---`) matching the existing style:

    Section (a) — `## DeGene → RealGene rename`
    Use the pre-written content from `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md` §"Entry 9: DeGene → RealGene rename" verbatim. The compiler error block must use `error[E0412]: cannot find trait \`DeGene\` in module \`genetic_algorithms::traits\`` with a `--> src/main.rs:3:34` pointer and a `= help: there is a trait with a similar name: \`RealGene\`` line. The `### Compiler error` H3 must be present per D-04.

    Section (b) — `## SelectionOperator::select — new num_parents parameter`
    Use the pre-written content from 65-RESEARCH.md §"Entry 10: SelectionOperator::select — new num_parents parameter". The After block's v3 signature must match `src/traits/operators.rs` lines 38–54 exactly (re-read that file before writing — do NOT paraphrase the signature). The compiler error block uses `error[E0053]: method \`select\` has an incompatible type for trait`.

    Section (c) — `## Mutation enum variant parameter changes`
    Use the pre-written content from 65-RESEARCH.md §"Entry 11: Mutation enum variant parameter changes". The After block must reference exact v3 variant names from `src/operations/mutation.rs` (re-read to confirm `PermutationInsert`, `Gaussian { sigma: Option<f64> }`, etc.). Include BOTH error blocks: the variant-rename E0599 with `help: there is a variant with a similar name: \`PermutationInsert\``, AND the unit-vs-struct-variant E0308 (or E0063) showing what users see when they pass `Mutation::Creep` without struct syntax. The "Fix table" markdown table from RESEARCH.md must be included.

    Section (d) — `## parallel feature — rayon is now optional`
    Use this skeleton (no `### Compiler error` subsection needed):

    ```
    ## parallel feature — rayon is now optional

    **What changed (Phase 69):** A new `parallel` feature flag (default-on) gates the `rayon` dependency. Users who previously had `rayon` as a mandatory transitive dependency can now opt out and shed `rayon` + `crossbeam` for embedded / wasm-only / ultra-lean builds. The default `cargo add genetic_algorithms` experience is unchanged — `parallel` and `logging` are both default-on.

    **Who is affected:** Users targeting `wasm32-unknown-unknown` or embedded environments who want to drop the rayon transitive deps. Standard native users: no action required.

    ### Opting out

    Disable both `default-features` and re-enable only the features you want:

    \```toml
    [dependencies]
    genetic_algorithms = { version = "3", default-features = false, features = ["logging"] }
    \```

    When the `parallel` feature is disabled, every `par_iter()` / `par_chunks*()` call-site in the library is replaced at compile time by a sequential `iter()` / `chunks*()` fallback. The canonical gate pattern (also enforced for `wasm32`) is:

    \```rust
    #[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]
    let results: Vec<_> = items.par_iter().map(|x| process(x)).collect();
    #[cfg(any(target_arch = "wasm32", not(feature = "parallel")))]
    let results: Vec<_> = items.iter().map(|x| process(x)).collect();
    \```

    See `CLAUDE.md` §"WASM Compatibility" and `.planning/intel/parallel-feature.md` for the full rationale.
    ```

    Replace the `\```` escape markers above with real triple-backticks when writing to the file.

    Section (e) — `## logging feature — log crate is now optional`
    Use this skeleton (no `### Compiler error` subsection needed):

    ```
    ## logging feature — log crate is now optional

    **What changed (Phase 68):** A new `logging` feature flag (default-on) gates the `log` crate dependency. Users who do not want any logging machinery — and want to shed the `log` crate from their transitive dep graph entirely — can disable it. This is distinct from the `## Logger setup (v2 auto-init → v3 explicit)` section above, which covers the related removal of the implicit `env_logger::init()` call inside `Ga::run()`.

    **Who is affected:** Users targeting `wasm32-unknown-unknown` or embedded environments who want to drop the `log` crate. Standard users: no action required.

    ### Opting out

    Disable both `default-features` and re-enable only the features you want:

    \```toml
    [dependencies]
    genetic_algorithms = { version = "3", default-features = false, features = ["parallel"] }
    \```

    When the `logging` feature is disabled, every internal `crate::log_info!`, `crate::log_debug!`, `crate::log_trace!`, `crate::log_warn!`, `crate::log_error!` macro expands to `()` instead of `::log::*` — the library emits no log events and the `log` crate is dropped from the transitive dep graph. `LogObserver` is also unavailable in this configuration (it depends on the `log` crate); construct your own `GaObserver<U>` implementor if you need event hooks without the `log` crate.

    See `.planning/intel/feature-flags.md` and `.planning/intel/logger-history.md` for the full rationale.
    ```

    Replace the `\```` markers with real triple-backticks.

    The five new sections must be separated from each other and from the preceding `## Logger setup` section by `---` horizontal rules, matching the style established in the existing file.

    Re-read the introductory paragraph at the top of MIGRATION.md once more after all edits; confirm it does not hard-code a numeric count of breaking changes. If Task 1 already changed "seven" to "every breaking change", leave it. If not, change it now.
  </action>
  <verify>
    <automated>grep -cE '^## (DeGene → RealGene rename|SelectionOperator::select|Mutation enum variant|parallel feature|logging feature)' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - MIGRATION.md contains the literal heading `## DeGene → RealGene rename`. Command: `grep -c '^## DeGene → RealGene rename$' MIGRATION.md` returns 1.
    - MIGRATION.md contains the literal heading `## SelectionOperator::select — new num_parents parameter`. Command: `grep -c '^## SelectionOperator::select — new num_parents parameter$' MIGRATION.md` returns 1.
    - MIGRATION.md contains the literal heading `## Mutation enum variant parameter changes`. Command: `grep -c '^## Mutation enum variant parameter changes$' MIGRATION.md` returns 1.
    - MIGRATION.md contains the literal heading `## parallel feature — rayon is now optional`. Command: `grep -c '^## parallel feature — rayon is now optional$' MIGRATION.md` returns 1.
    - MIGRATION.md contains the literal heading `## logging feature — log crate is now optional`. Command: `grep -c '^## logging feature — log crate is now optional$' MIGRATION.md` returns 1.
    - MIGRATION.md contains exactly 13 top-level `## ` headings. Command: `[ "$(grep -c '^## ' MIGRATION.md)" = "13" ]` exits 0.
    - MIGRATION.md contains exactly 11 `### Compiler error` subsections (8 from Task 1 + 3 new from this task — the parallel and logging feature sections do NOT contain a `### Compiler error`). Command: `[ "$(grep -c '^### Compiler error$' MIGRATION.md)" = "11" ]` exits 0.
    - MIGRATION.md contains the literal string `error[E0412]` (DeGene rename, may overlap with Task 1 errors).
    - MIGRATION.md contains the literal string `error[E0053]` (SelectionOperator signature mismatch).
    - The DeGene rename After-block contains the literal `use genetic_algorithms::traits::RealGene;`.
    - The SelectionOperator After-block contains the literal substrings `num_parents: usize` AND `Vec<Vec<usize>>`.
    - The Mutation section contains both literal variant names `PermutationInsert` and `Insertion`.
    - The parallel feature section contains the literal Cargo.toml snippet `default-features = false`.
    - The logging feature section contains the literal Cargo.toml snippet `default-features = false`.
    - The parallel feature section contains the canonical cfg gate `#[cfg(all(not(target_arch = "wasm32"), feature = "parallel"))]`.
    - `cargo doc --no-deps` exits 0 with zero warnings (MIGRATION.md is not compiled, but rustdoc must remain clean — sanity gate that we did not accidentally break any rustdoc link). Command: `cargo doc --no-deps 2>&1 | grep -v '^#' | grep -c '^warning:'` returns 0.
  </acceptance_criteria>
  <done>
    MIGRATION.md contains all 10 breaking-change entries (8 original + 3 new) and 2 new feature-flag sections. Total: 13 top-level `## ` sections, 11 `### Compiler error` subsections. Every breaking-change entry has a real `error[E...]` block. `cargo doc --no-deps` is still warning-free.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
The following new MIGRATION.md sections (literal heading text) are produced by this plan and must be excluded from cross-phase drift verification as "newly created in 65-01":

- `## DeGene → RealGene rename`
- `## SelectionOperator::select — new num_parents parameter`
- `## Mutation enum variant parameter changes`
- `## parallel feature — rayon is now optional`
- `## logging feature — log crate is now optional`

The following new heading-level subsection is added 8 times to existing sections (one per existing breaking-change entry):
- `### Compiler error`

The following new content callout is added once to an existing section:
- `> **Note:**` blockquote inside `## Trait split: ChromosomeT + LinearChromosome` (D-03 callout)

No new source files, types, traits, or public API symbols are produced. This is a documentation-only plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → User compilation | Migration recipes are copy-pasted by users; incorrect text leads to v2→v3 migration failures |
| MIGRATION.md → README upgrade banner | README links to MIGRATION.md; broken or missing sections cause 404-equivalent UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | MIGRATION.md `### Compiler error` blocks | accept | Error message text is approximate; Plan 65-03 v2 smoke-test will capture the exact rustc output and reconcile. Accepting because perfect rustc fidelity requires running the compiler against v2 patterns (Plan 65-03 job). |
| T-65-02 | Information disclosure | Public CHANGELOG / MIGRATION.md | accept | No secrets. Public release-notes content only. |
| T-65-03 | Repudiation | Git history | mitigate | All commits signed (CLAUDE.md mandatory rule). |
| T-65-SC | Supply chain | npm/pip/cargo installs | n/a | No package installs in this plan — documentation only. |
</threat_model>

<verification>
After both tasks complete, verify:

```bash
# Section count
[ "$(grep -c '^## ' MIGRATION.md)" = "13" ] && echo OK || echo FAIL

# Compiler error count (8 existing + 3 new = 11; parallel & logging feature sections have no compiler error)
[ "$(grep -c '^### Compiler error$' MIGRATION.md)" = "11" ] && echo OK || echo FAIL

# Rustdoc still clean
cargo doc --no-deps 2>&1 | grep -v '^#' | grep -c '^warning:' | xargs -I{} test {} = 0 && echo OK || echo FAIL

# README banner still points to MIGRATION.md
grep -c '\[MIGRATION\.md\](\./MIGRATION\.md)' README.md | xargs -I{} test {} -ge 1 && echo OK || echo FAIL
```

If any verification fails, fix in-plan before handing off to Plan 65-02.
</verification>

<success_criteria>
Plan 65-01 is complete when:
- MIGRATION.md contains all 13 `## ` top-level sections (8 existing + 3 missing breaking changes + 2 feature flags)
- Every breaking-change section (11 of them) has a `### Compiler error` subsection with a real `error[E...]` block — the 2 feature-flag sections (parallel, logging) do NOT have one because they describe opt-in/opt-out without v2 compile errors
- The `## Trait split: ChromosomeT + LinearChromosome` section has a `> **Note:**` D-03 callout about the LinearChromosome bound
- The introduction prose no longer claims "seven" breaking changes
- `cargo doc --no-deps` exits 0 with zero warnings
- All edits committed as a signed commit on the phase feat branch
</success_criteria>

<output>
Create `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-01-SUMMARY.md` when done.
</output>
</content>
</invoke>