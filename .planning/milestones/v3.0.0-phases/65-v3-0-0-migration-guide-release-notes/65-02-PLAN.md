---
phase: 65
plan: 02
type: execute
wave: 1
depends_on:
  - 65-01
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: []
tags:
  - changelog
  - release-notes
  - v3.0.0
  - keep-a-changelog

must_haves:
  truths:
    - "CHANGELOG.md no longer contains a top-level `## [Unreleased]` section"
    - "CHANGELOG.md contains the literal heading `## [3.0.0] - 2026-06-17` (date promoted from `Unreleased`)"
    - "The `## [3.0.0]` section's Added/Changed/Removed/Architecture & quality buckets cover phases 47–69 inclusive"
    - "The Architecture & quality bucket mentions Phase 64 (Test & Doc Quality — coverage baseline + rustdoc examples) and Phase 65 (Migration guide + release notes finalization)"
    - "The Architecture & quality bucket explicitly lists the build-perf phases (66 baseline harness + matrix CI + golden tests, 67 profile tuning + nextest + mold/lld + sccache, 68 logger removal + logging feature, 69 divan + parallel feature + ga.rs split)"
    - "The compare link at the bottom of CHANGELOG.md is correctly formed for the `[3.0.0]` entry (either `2.4.0...v3.0.0` if the tag exists, or `2.4.0...HEAD` if it does not — both acceptable)"
    - "The Phase 67 / 68 / 69 entries previously inside the `## [Unreleased]` section are merged into the `## [3.0.0]` section under the appropriate bucket (no entries lost)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Dated v3.0.0 entry"
      contains: "## [3.0.0] - 2026-06-17"
    - path: "CHANGELOG.md"
      provides: "Phase 64 acknowledgement"
      contains: "Phase 64"
    - path: "CHANGELOG.md"
      provides: "Phase 65 self-reference"
      contains: "Phase 65"
  key_links:
    - from: "CHANGELOG.md"
      to: "MIGRATION.md"
      via: "introductory prose link in the `## [3.0.0]` section"
      pattern: "MIGRATION\\.md"
    - from: "CHANGELOG.md compare link"
      to: "GitHub compare URL"
      via: "bottom-of-file link reference"
      pattern: "\\[3\\.0\\.0\\]: https://github\\.com/.*compare/2\\.4\\.0\\.\\.\\."
---

<objective>
Promote the `## [3.0.0]` CHANGELOG entry from "Unreleased" to the dated 2026-06-17 form, drop the now-redundant `## [Unreleased]` section by folding its entries into the `## [3.0.0]` section, and fill the two coverage gaps (Phase 64, Phase 65 self-reference) under the Architecture & quality bucket.

Purpose: A Keep-a-Changelog-compliant `## [3.0.0]` entry is the canonical release-notes document for crates.io and docs.rs. The current state has the `## [Unreleased]` and `## [3.0.0] - Unreleased` sections living side-by-side because Phase 67/68/69 added entries to `[Unreleased]` before Phase 65 ran. This plan consolidates them into a single dated v3.0.0 entry.

Output: Updated `CHANGELOG.md` with a single dated v3.0.0 entry covering phases 47–69, no orphan `## [Unreleased]` section, and a correctly formed compare link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-01-PLAN.md
@CHANGELOG.md
@MIGRATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fold `## [Unreleased]` content into `## [3.0.0]` and promote the date to 2026-06-17 (D-05, D-06)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (read the WHOLE file — every line of the `## [Unreleased]` block and the `## [3.0.0] - Unreleased` block must be visible before editing; do not guess at bucket contents)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md (D-05 drop Unreleased; D-06 coverage scope 47–69)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"CHANGELOG.md Audit — Current State" (lists what exists and the precise gap)
  </read_first>
  <behavior>
    - The `## [Unreleased]` heading and its three empty/non-empty bucket sub-headings (`### Added`, `### Changed`, `### Removed`) are removed from CHANGELOG.md.
    - The non-empty entries currently inside `## [Unreleased]` (the four Phase 67 bullets, the Phase 69 divan-bench bullet, and the Phase 69 ga.rs-split bullet — all currently under `### Changed` in the Unreleased block) are merged into the `## [3.0.0]` section under the `### Architecture & quality` bucket because they describe internal build-perf / repo-quality work, not user-facing API changes.
    - The heading `## [3.0.0] - Unreleased` is rewritten as `## [3.0.0] - 2026-06-17`.
    - The `## [3.0.0]` section gains a new bullet under `### Architecture & quality` for Phase 64 work that was completed before Phase 65 ran (Plan 64-01 coverage CI gate). Wording: "Coverage baseline harness via `cargo-llvm-cov` + new CI gate enforcing per-module thresholds. (Phase 64 / Plan 64-01)".
    - The `## [3.0.0]` section gains a self-referential bullet under `### Architecture & quality`: "v3.0.0 release notes & migration guide finalized — `MIGRATION.md` covers all 10 breaking changes with `### Compiler error` blocks; `## [3.0.0]` CHANGELOG entry dated and consolidated. (Phase 65 / Plans 65-01, 65-02)".
    - No bullet from the original `## [3.0.0]` content is removed or reordered (additive edit only).
    - The total number of `## [X.Y.Z]` top-level version headings does NOT decrease — the `## [Unreleased]` removal is balanced by the date promotion of `## [3.0.0]`.
  </behavior>
  <action>
    Open CHANGELOG.md. Perform these edits in order:

    Step 1 — Remove the `## [Unreleased]` block.
    Locate the heading `## [Unreleased]` (currently line 8) and the entire block of content under it through the `---` horizontal rule that precedes `## [3.0.0] - Unreleased` (currently line 23). Before deleting, capture the non-empty bullets currently in the Unreleased `### Changed` bucket — these are real Phase 67 and Phase 69 entries that must NOT be lost. Specifically, the bullets that begin with the following prefixes must be preserved (copy them to a scratch list):

    - "Tune `[profile.dev]`, `[profile.dev.package.\"*\"]`, and `[profile.test]`" (Phase 67 / Plan 67-01)
    - "CI switched from `cargo test` to `cargo nextest run`" (Phase 67 / Plan 67-02)
    - "CI now installs and uses `mold` as the Linux linker" (Phase 67 / Plan 67-03)
    - "CI now uses `mozilla-actions/sccache-action@v0.0.9`" (Phase 67 / Plan 67-04)
    - "Bench harness migrated from criterion to divan" (Phase 69 / Plan 69-01)
    - "`src/engines/ga.rs` (3342 lines) split into 11 submodules" (Phase 69 / Plan 69-04)

    Delete the `## [Unreleased]` heading, the three `### Added`/`### Changed`/`### Removed` sub-headings, all whitespace between them, and the trailing `---` rule that separated `[Unreleased]` from `[3.0.0]`. Result: the file should flow directly from the top introductory prose (Keep a Changelog reference, etc.) into `## [3.0.0]`.

    Step 2 — Promote the `## [3.0.0]` date.
    Find the heading `## [3.0.0] - Unreleased` (currently line 25). Change it to `## [3.0.0] - 2026-06-17`. Leave the introductory paragraph immediately below unchanged.

    Step 3 — Append the six preserved bullets to `### Architecture & quality` under `## [3.0.0]`.
    Locate the existing `### Architecture & quality` bucket inside the `## [3.0.0]` section (currently around lines 98–101). At the end of that bucket's bullet list (after the existing "Repo hygiene" bullet), append the six bullets you preserved in Step 1 in the same order. Preserve the exact wording, including the parenthetical `(Phase 67 / Plan 67-XX)` or `(Phase 69 / Plan 69-XX)` suffixes. Do NOT change their phrasing.

    Step 4 — Add Phase 64 acknowledgement.
    Inside the `### Architecture & quality` bucket of `## [3.0.0]`, append a new bullet after the Phase 67/69 bullets:

    - Coverage baseline harness via `cargo-llvm-cov` + new CI gate enforcing per-module thresholds. (Phase 64 / Plan 64-01)

    Step 5 — Add Phase 65 self-reference.
    Inside the `### Architecture & quality` bucket of `## [3.0.0]`, as the final bullet, append:

    - v3.0.0 release notes & migration guide finalized — `MIGRATION.md` covers all 10 breaking changes with `### Compiler error` blocks; `## [3.0.0]` CHANGELOG entry dated and consolidated. (Phase 65 / Plans 65-01, 65-02)

    Step 6 — Sanity verify no Phase 67/69 content was lost.
    After all edits, the literal strings `Phase 67 / Plan 67-01`, `Phase 67 / Plan 67-02`, `Phase 67 / Plan 67-03`, `Phase 67 / Plan 67-04`, `Phase 69 / Plan 69-01`, `Phase 69 / Plan 69-04` must all still appear in CHANGELOG.md, each exactly once.

    Do not edit any section other than `## [Unreleased]` (removed) and `## [3.0.0]` (date + Architecture & quality bucket additions). The 2.4.0 and earlier history must remain byte-identical except where required by Task 2 (compare-link update).
  </action>
  <verify>
    <automated>grep -c '^## \[Unreleased\]' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md no longer contains a top-level `## [Unreleased]` heading. Command: `[ "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" = "0" ]` exits 0.
    - CHANGELOG.md contains the literal heading `## [3.0.0] - 2026-06-17`. Command: `grep -c '^## \[3\.0\.0\] - 2026-06-17$' CHANGELOG.md` returns 1.
    - CHANGELOG.md no longer contains the literal phrase `## [3.0.0] - Unreleased`. Command: `grep -c '^## \[3\.0\.0\] - Unreleased$' CHANGELOG.md` returns 0.
    - All six preserved Phase 67/69 markers are present exactly once each. Each command must return 1: `grep -c 'Phase 67 / Plan 67-01' CHANGELOG.md`, `grep -c 'Phase 67 / Plan 67-02' CHANGELOG.md`, `grep -c 'Phase 67 / Plan 67-03' CHANGELOG.md`, `grep -c 'Phase 67 / Plan 67-04' CHANGELOG.md`, `grep -c 'Phase 69 / Plan 69-01' CHANGELOG.md`, `grep -c 'Phase 69 / Plan 69-04' CHANGELOG.md`.
    - CHANGELOG.md contains the literal substring `Phase 64 / Plan 64-01` exactly once. Command: `grep -c 'Phase 64 / Plan 64-01' CHANGELOG.md` returns 1.
    - CHANGELOG.md contains the literal substring `Phase 65 / Plans 65-01, 65-02` exactly once. Command: `grep -c 'Phase 65 / Plans 65-01, 65-02' CHANGELOG.md` returns 1.
    - CHANGELOG.md still contains exactly one `### Added — New engines` heading (proof we didn't delete the v3.0.0 Added content). Command: `grep -c '^### Added — New engines$' CHANGELOG.md` returns 1.
    - The line count of CHANGELOG.md decreased compared to the pre-task state (the `## [Unreleased]` removal is a net deletion of header/whitespace) but the file is still well-formed Markdown.
    - The historical sections `## [2.4.0] - 2026-05-18` and all earlier versions remain byte-identical between the section heading and the line "## [2.3.0]". Spot-check: `grep -c '^## \[2\.4\.0\] - 2026-05-18$' CHANGELOG.md` returns 1.
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a single dated `## [3.0.0] - 2026-06-17` section that contains every entry previously in the `## [Unreleased]` block PLUS new bullets for Phase 64 and Phase 65 under `### Architecture & quality`. The `## [Unreleased]` heading is gone. No prior version history was touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reconcile the bottom-of-file `[3.0.0]` compare link (D-05)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (post-Task-1 — read the compare-link block at the bottom of the file, currently lines 576+)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"What must change" item 3 (the `2.4.0...HEAD` vs `2.4.0...v3.0.0` decision)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §Common Pitfalls "Pitfall 3" (left-as-HEAD risk)
  </read_first>
  <behavior>
    - The compare link line `[3.0.0]: https://github.com/leimbernon/rust_genetic_algorithms/compare/2.4.0...HEAD` is updated as follows:
      - If a `v3.0.0` git tag exists in the working repository (`git tag -l v3.0.0` returns a non-empty line), the link is changed to `[3.0.0]: https://github.com/leimbernon/rust_genetic_algorithms/compare/2.4.0...v3.0.0`.
      - If the `v3.0.0` git tag does NOT yet exist (expected during Phase 65 execution since the tag is cut after Plan 65-03), the link is left as `...HEAD`. The tag-creation step (deferred to Plan 65-03 / human action) is responsible for the final rewrite to `...v3.0.0`.
    - The decision is recorded explicitly in the task output (commit message or summary): which form was used and why.
    - All other compare links (2.4.0, 2.3.0, ..., 0.1.0) remain byte-identical.
  </behavior>
  <action>
    Run `git tag -l v3.0.0` from the repo root.

    Case A — tag exists (output is non-empty):
    Locate the line `[3.0.0]: https://github.com/leimbernon/rust_genetic_algorithms/compare/2.4.0...HEAD` (currently line 576 of CHANGELOG.md). Rewrite it as:

    `[3.0.0]: https://github.com/leimbernon/rust_genetic_algorithms/compare/2.4.0...v3.0.0`

    Case B — tag does not exist (output is empty):
    Leave the `[3.0.0]: ...2.4.0...HEAD` line untouched. Document in the SUMMARY.md (or commit message) that the link was intentionally left as `...HEAD` because the `v3.0.0` git tag has not been created yet. Plan 65-03 must add a checklist item to rewrite the link to `...v3.0.0` once the tag is cut.

    Either way: do NOT modify any of the other `[X.Y.Z]: https://github.com/...` compare-link lines (2.4.0, 2.3.0, etc.). They are historical and stable.
  </action>
  <verify>
    <automated>tag_exists=$(git tag -l v3.0.0); if [ -n "$tag_exists" ]; then grep -c '^\[3\.0\.0\]: https://github\.com/leimbernon/rust_genetic_algorithms/compare/2\.4\.0\.\.\.v3\.0\.0$' CHANGELOG.md; else grep -c '^\[3\.0\.0\]: https://github\.com/leimbernon/rust_genetic_algorithms/compare/2\.4\.0\.\.\.HEAD$' CHANGELOG.md; fi</automated>
  </verify>
  <acceptance_criteria>
    - The compare-link decision logic above returns exactly 1 (the expected line is present in the chosen form).
    - CHANGELOG.md contains exactly one line matching `^\[3\.0\.0\]: https://github\.com/.*compare/2\.4\.0\.\.\.`. Command: `grep -c '^\[3\.0\.0\]: https://github\.com/leimbernon/rust_genetic_algorithms/compare/2\.4\.0\.\.\.' CHANGELOG.md` returns 1.
    - All earlier compare-link lines `[2.4.0]:`, `[2.3.0]:`, `[2.2.1]:`, `[2.2.0]:`, `[2.1.0]:`, `[2.0.0]:`, `[1.6.0]:`, `[1.5.0]:` remain present unchanged. Command: `grep -cE '^\[2\.[0-9]+(\.[0-9]+)?\]: https://github\.com/leimbernon/rust_genetic_algorithms/compare/' CHANGELOG.md` returns ≥ 6.
    - SUMMARY.md (or the commit message — written in `<output>`) records which case (A or B) was taken and why.
  </acceptance_criteria>
  <done>
    The bottom-of-file `[3.0.0]:` compare link is in the correct form for the current tag state. All other compare links untouched. The case decision is documented for Plan 65-03 to follow up.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
The following CHANGELOG.md content is produced by this plan and must be excluded from cross-phase drift verification as "newly created in 65-02":

- New literal heading: `## [3.0.0] - 2026-06-17` (replaces `## [3.0.0] - Unreleased`)
- Removal of: `## [Unreleased]` heading and its empty `### Added` / `### Changed` / `### Removed` sub-headings
- New bullets under `### Architecture & quality` in `## [3.0.0]`:
  - "Coverage baseline harness via `cargo-llvm-cov` + new CI gate enforcing per-module thresholds. (Phase 64 / Plan 64-01)"
  - "v3.0.0 release notes & migration guide finalized — `MIGRATION.md` covers all 10 breaking changes with `### Compiler error` blocks; `## [3.0.0]` CHANGELOG entry dated and consolidated. (Phase 65 / Plans 65-01, 65-02)"
- Re-located bullets (moved from `## [Unreleased]` `### Changed` into `## [3.0.0]` `### Architecture & quality`): the 4 Phase 67 bullets and the 2 Phase 69 bullets.
- Possibly updated compare link `[3.0.0]: https://github.com/leimbernon/rust_genetic_algorithms/compare/2.4.0...v3.0.0` (only if `v3.0.0` git tag exists; otherwise unchanged).

No new source files, types, traits, or public API symbols are produced. Documentation-only plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG.md → crates.io README rendering | crates.io extracts the top of CHANGELOG for the release-notes view; broken Markdown or missing date causes a degraded crates.io page |
| CHANGELOG.md compare link → GitHub | The compare link must resolve to a valid GitHub diff or the bottom-of-file link looks broken to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-02-01 | Tampering | CHANGELOG.md — accidental loss of entries during the Unreleased→3.0.0 merge | mitigate | Task 1 acceptance criteria enumerate all six Phase 67/69 markers that must survive the merge; `grep -c` gates fail-fast if any are missing |
| T-65-02-02 | Information disclosure | Public CHANGELOG | accept | No secrets; public release-notes content only |
| T-65-02-03 | Repudiation | Git history | mitigate | Signed commit per CLAUDE.md mandatory rule |
| T-65-02-SC | Supply chain | npm/pip/cargo installs | n/a | No package installs — documentation-only plan |
</threat_model>

<verification>
After both tasks complete, run:

```bash
# Unreleased heading is gone
[ "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" = "0" ] && echo OK || echo FAIL

# 3.0.0 is dated
grep -c '^## \[3\.0\.0\] - 2026-06-17$' CHANGELOG.md | xargs -I{} test {} = 1 && echo OK || echo FAIL

# All six preserved Phase 67/69 markers present
for marker in 'Phase 67 / Plan 67-01' 'Phase 67 / Plan 67-02' 'Phase 67 / Plan 67-03' 'Phase 67 / Plan 67-04' 'Phase 69 / Plan 69-01' 'Phase 69 / Plan 69-04'; do
  count=$(grep -c "$marker" CHANGELOG.md)
  [ "$count" = "1" ] && echo "OK: $marker" || echo "FAIL: $marker ($count)"
done

# Phase 64 + 65 markers
grep -c 'Phase 64 / Plan 64-01' CHANGELOG.md | xargs -I{} test {} = 1 && echo OK || echo FAIL
grep -c 'Phase 65 / Plans 65-01, 65-02' CHANGELOG.md | xargs -I{} test {} = 1 && echo OK || echo FAIL

# Compare link present in exactly one form
grep -cE '^\[3\.0\.0\]: https://github\.com/leimbernon/rust_genetic_algorithms/compare/2\.4\.0\.\.\.(HEAD|v3\.0\.0)$' CHANGELOG.md | xargs -I{} test {} = 1 && echo OK || echo FAIL

# Earlier versions still present
grep -c '^## \[2\.4\.0\] - 2026-05-18$' CHANGELOG.md | xargs -I{} test {} = 1 && echo OK || echo FAIL
```

If any verification fails, fix in-plan before handing off to Plan 65-03.
</verification>

<success_criteria>
Plan 65-02 is complete when:
- `## [Unreleased]` heading is removed from CHANGELOG.md
- `## [3.0.0] - 2026-06-17` heading replaces `## [3.0.0] - Unreleased`
- All non-empty bullets previously in `## [Unreleased]` are preserved under `## [3.0.0]` → `### Architecture & quality`
- New bullets for Phase 64 (coverage baseline) and Phase 65 (release notes / migration finalization) appear under `### Architecture & quality`
- Bottom-of-file `[3.0.0]:` compare link is in the correct form for the current tag state (`...v3.0.0` if tag exists, otherwise `...HEAD`)
- All pre-3.0.0 version sections remain byte-identical
- All edits committed as a signed commit on the phase feat branch
</success_criteria>

<output>
Create `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-02-SUMMARY.md` when done. Include the decision case (A or B) taken for the compare-link reconciliation in Task 2.
</output>
</content>
</invoke>