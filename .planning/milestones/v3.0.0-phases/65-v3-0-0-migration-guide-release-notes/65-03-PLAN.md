---
phase: 65
plan: 03
type: execute
wave: 2
depends_on:
  - 65-01
  - 65-02
files_modified:
  - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md
  - MIGRATION.md
  - Cargo.toml
autonomous: false
requirements: []
tags:
  - release-gate
  - verification
  - cargo-publish
  - ci-matrix

must_haves:
  truths:
    - "Full CI matrix (cargo test, cargo test --features serde, cargo clippy --all-targets -D warnings, cargo doc --no-deps --all-features, cargo check --target wasm32-unknown-unknown) passes locally with zero warnings"
    - "`cargo publish --dry-run` succeeds, exiting 0 with no missing-file or missing-metadata errors"
    - "D-08: A v2 sample crate exercising the top-3 breaking patterns only (ChromosomeT+LinearChromosome impl, Reporter removal, SelectionOperator trait impl) — no real optimization problem required — fails to compile against genetic_algorithms = \"=2.4.0\" → genetic_algorithms = \"3.0.0\" (no migration applied), and compiles cleanly after the MIGRATION.md recipes are applied"
    - "Every example registered in Cargo.toml runs to completion (or its CI smoke equivalent) on the working tree"
    - "Any divergence between the rustc errors captured during the v2 smoke-test and the `### Compiler error` blocks in MIGRATION.md is either reconciled (MIGRATION.md updated) or documented as 'rustc-version-dependent — wording differs but error code matches'"
    - "If the v3.0.0 git tag now exists, the bottom-of-file `[3.0.0]:` compare link in CHANGELOG.md has been rewritten from `...HEAD` to `...v3.0.0` (Plan 65-02 deferred this case)"
  artifacts:
    - path: ".planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md"
      provides: "Signed-off release gate checklist with command outputs and reconciliations"
      contains: "## Release gate — Phase 65 / Plan 65-03"
  key_links:
    - from: ".planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md"
      to: "MIGRATION.md"
      via: "Section 'Compiler error reconciliation' lists any deltas between captured rustc output and MIGRATION.md blocks"
      pattern: "MIGRATION\\.md"
---

<objective>
Run the four-part release gate defined in CONTEXT D-07 and produce a permanent, signed-off `65-03-RELEASE-GATE.md` artifact recording every command output. Reconcile any deltas between the `### Compiler error` blocks written in Plan 65-01 and the actual rustc output captured against a v2 sample crate.

Purpose: Before v3.0.0 is published to crates.io, the entire CI matrix must be green locally, `cargo publish --dry-run` must succeed, MIGRATION.md must be empirically correct (a real v2-to-v3 migration must compile), and every example must run. This is the last gate before tag-and-publish.

Output: `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md` containing the captured command outputs and reconciliations; any MIGRATION.md or Cargo.toml fixes the release gate surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-01-PLAN.md
@.planning/phases/65-v3-0-0-migration-guide-release-notes/65-02-PLAN.md
@MIGRATION.md
@CHANGELOG.md
@Cargo.toml
@README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full CI matrix locally + `cargo publish --dry-run` and record outputs (D-07 parts 1 & 2)</name>
  <files>
    .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md
    Cargo.toml
  </files>
  <read_first>
    - Cargo.toml (confirm `version = "3.0.0"`, `include` array, `readme`, `license`, `description`, `repository` fields are populated)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"Release Gate Sequence (Plan 65-03) — Four-Part" §"Part 1: Full CI Matrix" §"Part 2: `cargo publish --dry-run`"
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"Common Pitfalls — Pitfall 4: `build.rs` listed in Cargo.toml `include` but may not exist"
    - .github/workflows/feature-matrix.yml (if it exists — confirms which feature combinations CI already covers, so the local matrix mirrors CI rather than redoing it)
  </read_first>
  <behavior>
    - A new file `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md` is created with one top-level `## Part 1 — CI Matrix` section and one top-level `## Part 2 — cargo publish --dry-run` section.
    - Part 1 captures, for each of the five commands below, the exit code and a short head/tail of the output. The exit code MUST be 0 for every command; warning counts MUST be 0 where required.
      - `cargo test` (default features)
      - `cargo test --features serde`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo doc --no-deps --all-features` with explicit `2>&1 | grep -c '^warning:'` returning 0
      - `cargo check --target wasm32-unknown-unknown` (with `parallel` disabled per Phase 69 wasm rule — `cargo check --target wasm32-unknown-unknown --no-default-features --features logging` is the canonical form)
    - Part 2 captures `cargo publish --dry-run` output. If it fails because of a missing `include` entry (Pitfall 4), the fix step modifies Cargo.toml to remove the offending include path before re-running.
    - The `build.rs` file is verified to exist (`ls build.rs`) before Part 2 runs. The Cargo.toml `include` list already contains `/build.rs`; if `build.rs` is missing the include must be removed first.
  </behavior>
  <action>
    Create `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md` with the structure described in `<behavior>`.

    Pre-flight check (record in the file under a `## Pre-flight` section):

    1. `ls -la build.rs` — confirm the file exists. If `ls` exits non-zero, edit `Cargo.toml` to remove the `"/build.rs",` line from the `include` array, then continue. Record the decision in the gate file.
    2. `cargo --version` and `rustc --version` — capture in the gate file for reproducibility.
    3. `rustup target list --installed | grep -c wasm32-unknown-unknown` — must return ≥ 1. If 0, run `rustup target add wasm32-unknown-unknown` and record the install in the gate file.

    Part 1 — CI Matrix.

    Run the following five commands in sequence. After each, append a section to the gate file like:

    ```
    ### `cargo test`
    Exit code: 0
    Output (head):
    \```
    running 1661 tests
    ...
    \```
    Output (tail):
    \```
    test result: ok. 1661 passed; 0 failed
    \```
    ```

    Replace `\```` with real triple-backticks. Commands:

    1. `cargo test 2>&1 | tee /tmp/65-03-cargo-test.log; echo "exit=${PIPESTATUS[0]}"` — exit must be 0
    2. `cargo test --features serde 2>&1 | tee /tmp/65-03-cargo-test-serde.log; echo "exit=${PIPESTATUS[0]}"` — exit must be 0
    3. `cargo clippy --all-targets -- -D warnings 2>&1 | tee /tmp/65-03-clippy.log; echo "exit=${PIPESTATUS[0]}"` — exit must be 0
    4. `cargo doc --no-deps --all-features 2>&1 | tee /tmp/65-03-doc.log; echo "exit=${PIPESTATUS[0]}"` then `grep -c '^warning:' /tmp/65-03-doc.log` — exit must be 0 AND warning count must be 0
    5. `cargo check --target wasm32-unknown-unknown --no-default-features --features logging 2>&1 | tee /tmp/65-03-wasm.log; echo "exit=${PIPESTATUS[0]}"` — exit must be 0

    If any command fails: STOP. Append the full failure output to the gate file under a `### FAILURE: <command>` heading. Do NOT proceed to Part 2 until the failure is resolved (fix in-plan if it is a documentation issue Plan 65-01/65-02 missed; escalate via human checkpoint if it is a code regression).

    Part 2 — cargo publish --dry-run.

    Run: `cargo publish --dry-run 2>&1 | tee /tmp/65-03-publish-dryrun.log; echo "exit=${PIPESTATUS[0]}"`. Append exit code and full output to the gate file under `## Part 2 — cargo publish --dry-run`.

    Common Part-2 failure modes and the in-task fixes:
    - "file `build.rs` listed in `include` does not exist" → edit `Cargo.toml`, remove `"/build.rs",` from the `include` array, re-run the dry-run.
    - "missing `readme`/`license`/`description`" → these are confirmed present by the pre-flight; should not occur.
    - "could not publish package because the registry is unreachable" → record as `[ASSUMED env]` and skip; document in gate file.

    Save and commit the gate file. Do NOT proceed to Task 2 until all Part-1 and Part-2 commands report exit 0 (or the local network limitation is documented for Part 2).
  </action>
  <verify>
    <automated>test -f .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md && grep -cE '^### `(cargo test|cargo test --features serde|cargo clippy|cargo doc|cargo check --target wasm32)' .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The gate file exists at `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md`.
    - The gate file contains the literal heading `## Part 1 — CI Matrix`.
    - The gate file contains the literal heading `## Part 2 — cargo publish --dry-run`.
    - The gate file records exit code 0 for all five Part-1 commands.
    - The gate file records `warning: 0` (or equivalent) for the `cargo doc --no-deps --all-features` warning grep.
    - The gate file records exit code 0 for `cargo publish --dry-run` (or documents the network-unreachable case explicitly).
    - `build.rs` either exists OR the `Cargo.toml include` array no longer contains `"/build.rs"` — verify: `(ls build.rs && echo exists) || ! grep -F '"/build.rs"' Cargo.toml`.
    - Re-running `cargo test` from the repo root exits 0 (sanity post-edit). Command: `cargo test 2>&1 >/dev/null; echo $?` returns 0.
  </acceptance_criteria>
  <done>
    Part 1 (five CI commands) and Part 2 (`cargo publish --dry-run`) have been executed; their outputs and exit codes are recorded in `65-03-RELEASE-GATE.md`. All exits are 0 (or the network-unreachable case for Part 2 is explicitly documented and acknowledged).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the v2 sample crate smoke-test and reconcile MIGRATION.md compiler error blocks (D-07 part 3, D-08)</name>
  <files>
    .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md
    MIGRATION.md
  </files>
  <read_first>
    - MIGRATION.md (current state — post Plan 65-01 — focus on the `### Compiler error` blocks; their wording is approximate and may need reconciliation against real rustc output)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"v2 Sample Crate Smoke-Test — Design (Plan 65-03)" (test sequence steps 1–7)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-CONTEXT.md (D-08 — top 3 breaking patterns only: ChromosomeT+LinearChromosome impl, Reporter removal, SelectionOperator trait impl)
    - src/traits/operators.rs lines 38–54 (current v3 SelectionOperator::select signature — used as the "After" target for the smoke-test crate's migration)
  </read_first>
  <behavior>
    - A throwaway crate `ga_v2_smoke` is created in a temporary directory (`/tmp/ga_v2_smoke` is the default; on systems where `/tmp` is unsuitable, the task may use `${TMPDIR:-/tmp}/ga_v2_smoke`).
    - The crate's `src/lib.rs` exercises the top 3 breaking patterns from D-08 only — it does not have to solve a real optimization problem.
    - Build sequence:
      1. Build with `genetic_algorithms = "=2.4.0"` (or `version = "=2.4.0"` if v3.0.0 is already published) — expected to succeed (this is the "v2 baseline" step).
      2. Update to `genetic_algorithms = { path = "<absolute path to this repo>" }` so the smoke-test uses the in-progress v3 (avoids requiring a real v3 publish). Build — expected to FAIL with the compiler errors documented in MIGRATION.md.
      3. Apply the MIGRATION.md recipes to `src/lib.rs`. Build — expected to SUCCEED.
    - Each of the three FAIL outputs (one per breaking pattern) is captured verbatim. The captured `error[E...]` lines are compared against the `### Compiler error` blocks in MIGRATION.md.
    - For each delta:
      - If the error CODE matches but the message text differs: update MIGRATION.md to use the captured text verbatim.
      - If the error CODE differs: re-run rustc to confirm, then update MIGRATION.md with the correct code and message.
      - If MIGRATION.md is correct: record "matches verbatim" in the gate file.
    - The throwaway crate is deleted after the test completes (cleanup step).
    - If running offline / without crates.io access, step 1 is skipped and the gate file records "v2 baseline skipped — offline mode; step 2 still validates that the in-progress v3 fails to compile against unmigrated v2 patterns".
  </behavior>
  <action>
    Create a top-level `## Part 3 — v2 sample crate smoke-test` section in `65-03-RELEASE-GATE.md`. Document each step and its outcome inline.

    Sub-step A — Create the crate.

    ```bash
    SMOKE_DIR="${TMPDIR:-/tmp}/ga_v2_smoke"
    rm -rf "$SMOKE_DIR"
    cargo new --lib "$SMOKE_DIR"
    cd "$SMOKE_DIR"
    ```

    Sub-step B — Write the v2 `src/lib.rs` exercising D-08's top 3 patterns. The file must contain:

    1. A custom chromosome `SmokeChrom` that implements (v2-style) `ChromosomeT` with the `dna()` / `dna_mut()` / `set_dna()` / `set_fitness_fn()` / `new_gene()` methods inline on `ChromosomeT` (v2 contract).
    2. A reference to `SimpleReporter` or `Reporter` import — e.g. `use genetic_algorithms::reporter::SimpleReporter;` plus a `Ga::new().with_reporter(Box::new(SimpleReporter::new(10)))` call. (No need to actually run the GA; build-only check.)
    3. A custom `SmokeSelection` struct that impls `SelectionOperator` with the v2 signature: `fn select<U: ChromosomeT + Sync + Send + 'static + Clone>(&self, c: &[U], couples: usize, threads: usize) -> Vec<(usize, usize)>`.

    Edit `Cargo.toml` of the smoke crate to add `genetic_algorithms = "=2.4.0"`.

    Sub-step C — Baseline build against v2.4.0.

    `cargo build 2>&1 | tee build-v2.log; echo "exit=$?"`

    Expected: exit 0 (v2 code compiles against v2 lib). If offline or v2.4.0 unfetchable, record "skipped — offline" and move on.

    Sub-step D — Switch dependency to the in-progress v3.

    Edit the smoke crate's `Cargo.toml` to replace the registry line with a path dependency:

    ```toml
    genetic_algorithms = { path = "<absolute path of this repo>", default-features = false, features = ["logging", "parallel"] }
    ```

    `cargo build 2>&1 | tee build-v3-unmigrated.log; echo "exit=$?"`

    Expected: NON-ZERO exit. Capture the full error output.

    Extract every `error[EXXXX]: ...` line from `build-v3-unmigrated.log`. For each, append to the gate file under `### Captured v3 error: <error code>`:

    ```
    <full rustc error block, including --> pointer, code snippet, ^^^ caret, and = help line>
    ```

    Sub-step E — Compare captured errors against MIGRATION.md.

    For each of the three breaking patterns (ChromosomeT split, Reporter removal, SelectionOperator signature), find the corresponding `### Compiler error` block in MIGRATION.md and diff against the captured output. Record one of three outcomes per pattern:

    - `MATCHES VERBATIM` — MIGRATION.md text is byte-identical to captured output (modulo line-number / file-path placeholders).
    - `MATCHES (code, message paraphrased)` — error code is correct; message wording differs but the meaning is the same. Acceptable; record the actual rustc wording in the gate file as a reference. No MIGRATION.md edit required UNLESS the user-facing meaning has shifted.
    - `MISMATCH` — error code is wrong or the message implies a different fix. Update MIGRATION.md to use the captured `error[E...]` line and message verbatim; record the diff in the gate file.

    Sub-step F — Apply MIGRATION.md fixes and re-build.

    Update the smoke crate's `src/lib.rs` per MIGRATION.md:
    - Split `ChromosomeT` impl into `ChromosomeT` (core) + `LinearChromosome` (flat-slice) per the "Trait split" section After block.
    - Replace `use ... SimpleReporter` and `.with_reporter(...)` with `use genetic_algorithms::observer::LogObserver` and `.with_observer(Arc::new(LogObserver))` per the "Reporter removed" After block.
    - Update the `SelectionOperator` impl to the v3 signature (`num_parents: usize` parameter; return `Vec<Vec<usize>>`) per the "SelectionOperator::select" After block.

    `cargo build 2>&1 | tee build-v3-migrated.log; echo "exit=$?"`

    Expected: exit 0. Capture the result in the gate file. If still failing, the MIGRATION.md recipe is wrong — fix MIGRATION.md AND the smoke crate until both compile cleanly together; document each fix in the gate file under `### MIGRATION.md fix: <section>`.

    Sub-step G — Cleanup.

    `cd <repo root>; rm -rf "$SMOKE_DIR"` and record cleanup in the gate file.

    Final: a `### Reconciliation summary` block in the gate file tabulates: pattern | captured error code | MIGRATION.md outcome | MIGRATION.md change required (Y/N).
  </action>
  <verify>
    <automated>grep -c '^## Part 3 — v2 sample crate smoke-test$' .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The gate file contains the literal heading `## Part 3 — v2 sample crate smoke-test`.
    - The gate file contains a `### Reconciliation summary` block with at least 3 rows (one per breaking pattern from D-08).
    - For each of the three patterns the reconciliation row records one of: `MATCHES VERBATIM`, `MATCHES (paraphrased)`, or `MISMATCH (fixed)`.
    - The final `cargo build` against the migrated smoke crate exits 0 — recorded in the gate file as `exit=0`.
    - MIGRATION.md still passes the structural gate from Plan 65-01: `[ "$(grep -c '^### Compiler error$' MIGRATION.md)" = "11" ]` exits 0 (no compiler-error blocks were accidentally deleted during reconciliation).
    - The smoke crate directory `${TMPDIR:-/tmp}/ga_v2_smoke` has been cleaned up — `test ! -d "${TMPDIR:-/tmp}/ga_v2_smoke"` exits 0.
    - `cargo doc --no-deps --all-features 2>&1 | grep -c '^warning:'` still returns 0 after any MIGRATION.md edits.
  </acceptance_criteria>
  <done>
    A v2 smoke crate was built against the unmigrated `genetic_algorithms = "=2.4.0"`, switched to the path-based v3 dependency (failed to compile, errors captured), migrated per MIGRATION.md (compiled successfully), and deleted. Every captured `error[E...]` was reconciled against the corresponding MIGRATION.md `### Compiler error` block; any mismatches were fixed in MIGRATION.md.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run all examples + CHANGELOG compare-link tag check + final sign-off (D-07 part 4, Pitfall 3)</name>
  <files>
    .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md
    CHANGELOG.md
  </files>
  <read_first>
    - Cargo.toml `[[example]]` blocks (~21 examples; required-features per example)
    - .planning/phases/65-v3-0-0-migration-guide-release-notes/65-RESEARCH.md §"Part 4: `cargo run --example` Smoke Run" (lists the canonical short-generation commands per example)
    - .github/workflows/examples-smoke.yml (if it exists — confirms which examples CI already smoke-tests; prefer triggering that workflow over local re-runs for examples without `--generations` flag support)
    - CHANGELOG.md bottom-of-file compare-link block (post Plan 65-02 — verify the `[3.0.0]:` line is in the expected form)
  </read_first>
  <behavior>
    - Each registered example in Cargo.toml is run with its required features. Examples that accept a `--generations N` flag use `--generations 5` to keep the smoke fast; examples that do not are run with whatever default termination they have (typically max-generations capped low in their own source).
    - Each run records exit code and a one-line tail of stdout (e.g. "Best fitness: 0.0001") in the gate file under `## Part 4 — examples smoke-run`.
    - For any example that fails (exit != 0): the gate file records the failure and the task halts. This is a release-blocker.
    - After Part 4: the bottom-of-file CHANGELOG compare link is re-examined. If `git tag -l v3.0.0` is now non-empty AND the link still says `...HEAD`, the link is rewritten to `...v3.0.0` (Pitfall 3 from 65-RESEARCH).
    - A final `## Release sign-off` section is added to the gate file recording the date, the commit hash of the working tree, and a single line `Phase 65 release gate PASSED` (or `BLOCKED on: <reason>`).
    - A blocking human checkpoint is added at the end: the user must explicitly approve the release before tag-and-publish proceeds. This is the LAST gate; subsequent actions (cargo publish, git tag push) happen outside this plan.
  </behavior>
  <action>
    Append `## Part 4 — examples smoke-run` to the gate file.

    Enumerate every example in Cargo.toml. For each, determine its required features and whether its source accepts `--generations`. The known mapping (from 65-RESEARCH §Part 4) is:

    - `cma_es_rastrigin` → `--features logging`
    - `pso_rastrigin` → `--features logging`
    - `eda_trap` → `--features logging`
    - `ipop_rastrigin` → `--features logging`
    - `knapsack_binary` → `--features logging`
    - `job_scheduling` → `--features logging`
    - `onemax_extension` → `--features logging`
    - `island_model` → `--features logging`
    - `nsga2_zdt1` → `--features logging`
    - `feature_selection` → `--features logging`
    - `rastrigin` → `--features logging`
    - `nsga3_dtlz2` → `--features logging`
    - `nqueens_range` → `--features logging`
    - `niching` → `--features logging`
    - `onemax_binary` → `--features logging`
    - `spea2_zdt1` → `--features logging`
    - `moead_dtlz2` → `--features logging`
    - `sms_emoa_zdt1` → `--features "benchmarks logging"`
    - `ibea_zdt1` → `--features "benchmarks logging"`
    - `surrogate_rastrigin` → no required features
    - `memetic_rastrigin` → no required features
    - Additional examples present but not in 65-RESEARCH (verify against `ls examples/`): `aos_demo`, `constrained_g1`, `hall_of_fame_demo` — run with no required features unless Cargo.toml says otherwise.

    For each example, run:

    `cargo run --release --example <name> [<feature flags>] 2>&1 | tail -5; echo "exit=$?"`

    Record exit and last 5 lines in the gate file like:

    ```
    ### `cma_es_rastrigin`
    Exit code: 0
    Output (tail):
    \```
    Best fitness: 0.0008
    \```
    ```

    Use `--release` to avoid spending 20+ minutes on debug-mode example runs. If any example does NOT terminate within 60 seconds of wall-clock, kill it and record `KILLED (timeout 60s)` — do NOT consider this a release block if the example is known to be long-running by design (record reasoning).

    If any example exits non-zero (other than a documented long-running timeout): STOP. Append `### RELEASE-BLOCKING FAILURE: <example>` to the gate file with the full output. Hand back to the user as a checkpoint failure.

    After Part 4, re-check the CHANGELOG compare link state:

    `tag_exists=$(git tag -l v3.0.0); echo "v3.0.0 tag: ${tag_exists:-not present}"`

    If `tag_exists` is non-empty AND `grep -c 'compare/2\.4\.0\.\.\.HEAD$' CHANGELOG.md` returns 1: edit CHANGELOG.md to rewrite `2.4.0...HEAD` → `2.4.0...v3.0.0` in the `[3.0.0]:` line ONLY. Record the change in the gate file under `## Compare-link reconciliation`.

    Append `## Release sign-off` to the gate file with:
    - Date: `date -Iseconds`
    - Commit hash of working tree: `git rev-parse HEAD`
    - One-line conclusion: `Phase 65 release gate PASSED` if every Part-1/2/3/4 acceptance criterion is satisfied; `Phase 65 release gate BLOCKED: <reason>` otherwise.

    Commit the gate file and any MIGRATION.md / Cargo.toml / CHANGELOG.md fixes.
  </action>
  <verify>
    <automated>grep -c '^## Release sign-off$' .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md && grep -c '^Phase 65 release gate PASSED$' .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md</automated>
    <human-check>
      The user reviews `65-03-RELEASE-GATE.md` and confirms that every Part-1/2/3/4 acceptance criterion is met. After approval, the user (or a follow-up command outside this plan) cuts the `v3.0.0` git tag, runs `cargo publish`, and pushes the tag.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The gate file contains the literal heading `## Part 4 — examples smoke-run`.
    - The gate file contains the literal heading `## Release sign-off`.
    - The gate file contains exactly one of `Phase 65 release gate PASSED` or `Phase 65 release gate BLOCKED: <reason>`.
    - Every example registered in Cargo.toml has either an `Exit code: 0` line OR a documented `KILLED (timeout 60s)` line in the gate file. Command: `grep -cE '^### `[a-z_]+`$' .planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md` returns at least the number of `[[example]]` blocks in Cargo.toml (≥ 21).
    - The CHANGELOG `[3.0.0]:` compare link is in the form expected for the current tag state: if `git tag -l v3.0.0` is non-empty, the link contains `2.4.0...v3.0.0`; otherwise it contains `2.4.0...HEAD`. Command (case A): `[ -z "$(git tag -l v3.0.0)" ] || grep -c '2\.4\.0\.\.\.v3\.0\.0$' CHANGELOG.md | xargs -I{} test {} = 1`.
    - The gate file contains the working-tree commit hash (`git rev-parse HEAD` output) and an ISO-8601 date.
  </acceptance_criteria>
  <done>
    Every registered example has been run (or its timeout documented), the CHANGELOG compare link is reconciled with the current tag state, and the gate file contains a final `## Release sign-off` block. The user has approved the release via the human-check. The phase is ready to hand off to tag-cut + cargo-publish (those actions are NOT part of this plan).
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
The following artifact is newly produced by this plan and must be excluded from cross-phase drift verification as "newly created in 65-03":

- New file: `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md` containing the `## Pre-flight`, `## Part 1 — CI Matrix`, `## Part 2 — cargo publish --dry-run`, `## Part 3 — v2 sample crate smoke-test`, `## Part 4 — examples smoke-run`, optional `## Compare-link reconciliation`, and `## Release sign-off` sections.

The following files may be modified by this plan (the modifications are documented in the gate file):
- `Cargo.toml` — only if `build.rs` does not exist; in that case the `"/build.rs",` entry is removed from the `include` array.
- `MIGRATION.md` — only if Task 2's reconciliation step found a `MISMATCH` between captured rustc output and a `### Compiler error` block; in that case the block is updated to use the captured `error[E...]` line verbatim. The 11 `### Compiler error` blocks total established by Plan 65-01 must remain.
- `CHANGELOG.md` — only if the `v3.0.0` git tag was cut after Plan 65-02 ran; in that case the `[3.0.0]:` compare link is rewritten from `...HEAD` to `...v3.0.0`.

No new source files, types, traits, or public API symbols are produced. This is a verification-only plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev box → crates.io | `cargo publish --dry-run` simulates the registry handshake; a passing dry-run does not guarantee a successful real publish (network/auth differences) |
| In-progress v3 working tree → v2 smoke crate | Smoke crate consumes the working tree via `path = "..."` dependency; uncommitted local changes are exercised |
| Example execution → host system | `cargo run --example` executes arbitrary code from the examples; trusted because examples are in-repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03-01 | Tampering | MIGRATION.md `### Compiler error` blocks (reconciliation step) | mitigate | Captured rustc output is the source of truth; any MIGRATION.md edit must be diffed against the captured log and recorded in the gate file |
| T-65-03-02 | Denial of service | Examples smoke-run could hang | mitigate | 60-second wall-clock timeout per example; non-terminating examples are killed and explicitly documented |
| T-65-03-03 | Spoofing | `cargo publish --dry-run` is a simulation, not a real publish | accept | Documented as a dry-run only; the real publish happens after the human checkpoint at the end of this plan |
| T-65-03-04 | Tampering | `Cargo.toml include` array could drift if `build.rs` is removed | mitigate | If `build.rs` is missing, the `Cargo.toml` edit is captured in the gate file AND committed; future regressions caught by the pre-flight `ls build.rs` step |
| T-65-03-SC | Supply chain | npm/pip/cargo installs | n/a | This plan does not install new crates; it consumes the working tree via path dependency for the smoke crate. No package-legitimacy audit required. |
</threat_model>

<verification>
After all three tasks complete:

```bash
GATE=.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md

# Structural sanity
test -f "$GATE" && echo OK || echo FAIL
grep -c '^## Pre-flight$' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL
grep -c '^## Part 1 — CI Matrix$' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL
grep -c '^## Part 2 — cargo publish --dry-run$' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL
grep -c '^## Part 3 — v2 sample crate smoke-test$' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL
grep -c '^## Part 4 — examples smoke-run$' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL
grep -c '^## Release sign-off$' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL

# Plan 65-01 invariants must still hold
[ "$(grep -c '^### Compiler error$' MIGRATION.md)" = "11" ] && echo OK || echo FAIL
[ "$(grep -c '^## ' MIGRATION.md)" = "13" ] && echo OK || echo FAIL

# Plan 65-02 invariants must still hold
[ "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" = "0" ] && echo OK || echo FAIL
grep -c '^## \[3\.0\.0\] - 2026-06-17$' CHANGELOG.md | xargs -I{} test {} = 1 && echo OK || echo FAIL

# Final release-gate status line is present
grep -cE '^Phase 65 release gate (PASSED|BLOCKED)' "$GATE" | xargs -I{} test {} = 1 && echo OK || echo FAIL
```

If any verification fails, the release is BLOCKED. Resolve before approving the human checkpoint.
</verification>

<success_criteria>
Plan 65-03 is complete when:
- `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-RELEASE-GATE.md` exists with sections Pre-flight, Part 1, Part 2, Part 3, Part 4, and Release sign-off
- Part 1 (full CI matrix) records exit 0 for all five commands AND `cargo doc` warning count = 0
- Part 2 (`cargo publish --dry-run`) records exit 0 (or documents the offline-network case)
- Part 3 records the v2 smoke-test result: pre-migration build fails as expected; post-migration build succeeds with exit 0; the smoke crate directory has been cleaned up
- Part 3 reconciliation block reconciles every captured rustc error against MIGRATION.md (any MISMATCH triggered a MIGRATION.md fix)
- Part 4 records exit 0 (or documented timeout) for every example registered in Cargo.toml
- CHANGELOG `[3.0.0]:` compare link is in the correct form for the current tag state
- Release sign-off block contains a clear PASSED or BLOCKED line, the working-tree commit hash, and an ISO-8601 date
- The human-check at the end of Task 3 has been completed (the user approved the release)
- All edits committed as signed commits on the phase feat branch
</success_criteria>

<output>
Create `.planning/phases/65-v3-0-0-migration-guide-release-notes/65-03-SUMMARY.md` when done. Include:
- Final release-gate status (PASSED or BLOCKED with reason)
- Number of MIGRATION.md reconciliations performed in Task 2 (0 ideally, but any captured deltas are documented)
- Any Cargo.toml edits performed in Task 1 (e.g. `build.rs` include removal)
- CHANGELOG compare-link case taken in Task 3 (A: rewritten to `...v3.0.0`, B: left as `...HEAD`)
- A pointer to `65-03-RELEASE-GATE.md` as the authoritative artifact
- A statement that this plan does NOT itself cut the v3.0.0 git tag or run `cargo publish` — those are follow-up actions outside the GSD plan scope
</output>
