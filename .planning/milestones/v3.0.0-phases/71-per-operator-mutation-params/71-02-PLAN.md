---
phase: 71-per-operator-mutation-params
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/engines/ga/generation.rs
  - src/engines/island/mod.rs
  - src/engines/moead/mod.rs
  - src/engines/nsga2/mod.rs
  - src/engines/nsga3/mod.rs
  - src/engines/cellular/configuration.rs
  - src/engines/alps/configuration.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 4 factory_with_chromosome_length call sites pass 3 args (no trailing None, None)"
    - "The Differential match arm in generation.rs destructures DifferentialParams"
    - "moead/nsga2/nsga3 Differential guards use tuple wildcard (..) syntax"
    - "cellular and alps default Gaussian construction uses GaussianParams"
  artifacts:
    - path: "src/engines/ga/generation.rs"
      provides: "Differential(DifferentialParams { f }) match arm + 2 simplified factory_with_chromosome_length calls"
    - path: "src/engines/island/mod.rs"
      provides: "2 simplified factory_with_chromosome_length calls"
  key_links:
    - from: "src/engines/ga/generation.rs"
      to: "src/operations.rs"
      via: "destructures DifferentialParams from Mutation::Differential tuple variant"
      pattern: "Mutation::Differential\\(DifferentialParams"
    - from: "src/engines/cellular/configuration.rs"
      to: "src/operations.rs"
      via: "constructs Mutation::Gaussian(GaussianParams { sigma: Some(0.1) }) as default"
      pattern: "Mutation::Gaussian\\(GaussianParams"
---

<objective>
Update all engine-layer consumers of the reshaped `Mutation` enum: the 4 `factory_with_chromosome_length` call sites (drop trailing `None, None` per D-06), the `Differential` match arm in `generation.rs`, the `Differential { .. }` rejection guards in the three multi-objective engines (tuple wildcard per Pitfall 1 / Pattern 8), and the default `Gaussian` construction in the cellular and ALPS configuration defaults.

Purpose: Propagate the Phase 71 enum reshape (Plan 01) into the engine layer so the standard GA, island model, and multi-objective engines compile and behave identically.
Output: 7 engine files updated to the tuple-variant shape; zero behavioral change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-per-operator-mutation-params/71-CONTEXT.md
@.planning/phases/71-per-operator-mutation-params/71-RESEARCH.md
@.planning/phases/71-per-operator-mutation-params/71-PATTERNS.md
@.planning/phases/71-per-operator-mutation-params/71-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update standard GA + island call sites and Differential match arm</name>
  <files>src/engines/ga/generation.rs, src/engines/island/mod.rs</files>
  <read_first>
    - src/engines/ga/generation.rs (the `Mutation::Differential { f }` match arm ~line 267; the two `mutation::factory_with_chromosome_length(..., Some(cl), None, None)` calls ~lines 277 and ~305)
    - src/engines/island/mod.rs (the two `mutation::factory_with_chromosome_length(..., None, None, None)` calls ~lines 592 and ~691)
    - src/operations.rs (DifferentialParams struct + reshaped Differential variant from Plan 01)
    - src/operations/mutation.rs (the new 3-arg factory_with_chromosome_length signature from Plan 01)
    - .planning/phases/71-per-operator-mutation-params/71-CONTEXT.md (D-06: 4 call sites drop trailing None, None)
    - .planning/phases/71-per-operator-mutation-params/71-PATTERNS.md ("src/engines/ga/generation.rs — Call-site updates" and "src/engines/island/mod.rs — Call-site updates")
  </read_first>
  <action>
    In src/engines/ga/generation.rs:
    - Change the `Mutation::Differential { f }` match arm to `Mutation::Differential(DifferentialParams { f })`. The arm body (`let f_val = f.unwrap_or(0.5);` and the `differential_mutation(...)` call) stays byte-identical. Ensure `DifferentialParams` is in scope (it lives in `crate::operations` alongside `Mutation`; add it to the existing `Mutation` import or reference it as `crate::operations::DifferentialParams`).
    - At both `factory_with_chromosome_length` call sites (the first child mutation block ~line 277 and the second child mutation block ~line 305), remove the trailing two `None, None` arguments so each call passes exactly `(mutation_method, &mut child, Some(chromosome_length))`.

    In src/engines/island/mod.rs:
    - At both `factory_with_chromosome_length` call sites (~line 592 and ~line 691), remove the trailing two `None, None` arguments so each call passes exactly `(mutation_config.method.clone(), child, None)`.

    No default values change. The island engine has no `Differential` match arm to update (per Pattern Map — island's call pattern is simpler).
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Mutation::Differential { f }' src/engines/ga/generation.rs` returns 0
    - `grep -c 'Mutation::Differential(DifferentialParams { f })' src/engines/ga/generation.rs` returns 1
    - `grep -c 'None,$' src/engines/ga/generation.rs` does NOT include the two removed factory args — verified by: `grep -A4 'factory_with_chromosome_length' src/engines/ga/generation.rs` shows no `None, None` trailing the chromosome_length arg
    - `grep -A4 'factory_with_chromosome_length' src/engines/island/mod.rs` shows each call ending after the third argument (no `None, None,` trailing)
    - `cargo build` succeeds with src/engines/ga and src/engines/island compiling
  </acceptance_criteria>
  <done>generation.rs Differential arm destructures DifferentialParams; all 4 factory_with_chromosome_length calls (2 in generation.rs, 2 in island/mod.rs) pass 3 args; default f=0.5 unchanged; `cargo build` green.</done>
</task>

<task type="auto">
  <name>Task 2: Update multi-objective Differential guards and cellular/ALPS default Gaussian construction</name>
  <files>src/engines/moead/mod.rs, src/engines/nsga2/mod.rs, src/engines/nsga3/mod.rs, src/engines/cellular/configuration.rs, src/engines/alps/configuration.rs</files>
  <read_first>
    - src/engines/moead/mod.rs (the `matches!(..., crate::operations::Mutation::Differential { .. })` guard ~line 627; check for a second occurrence in a doc comment)
    - src/engines/nsga2/mod.rs (the same guard ~line 552)
    - src/engines/nsga3/mod.rs (the same guard ~line 572)
    - src/engines/cellular/configuration.rs (the `mutation: Mutation::Gaussian { sigma: Some(0.1) }` default ~line 112)
    - src/engines/alps/configuration.rs (the `mutation: Mutation::Gaussian { sigma: Some(0.1) }` default ~line 95)
    - src/operations.rs (GaussianParams struct + reshaped variants from Plan 01)
    - .planning/phases/71-per-operator-mutation-params/71-RESEARCH.md (Pattern 8 wildcard guards; Pitfall 1 — `{ .. }` invalid on tuple variants)
    - .planning/phases/71-per-operator-mutation-params/71-PATTERNS.md ("Wildcard guard fix" and "Construction site" sections)
  </read_first>
  <action>
    In src/engines/moead/mod.rs, src/engines/nsga2/mod.rs, and src/engines/nsga3/mod.rs:
    - Change every `crate::operations::Mutation::Differential { .. }` to `crate::operations::Mutation::Differential(..)`. The `{ .. }` struct-wildcard form is invalid Rust on a tuple variant (Pitfall 1 — E0769); the tuple form is `(..)`. Search each file for `Differential { .. }` including any occurrence in a doc-comment code block and update consistently.

    In src/engines/cellular/configuration.rs and src/engines/alps/configuration.rs:
    - Change the default `mutation: Mutation::Gaussian { sigma: Some(0.1) }` to `mutation: Mutation::Gaussian(GaussianParams { sigma: Some(0.1) })`. Ensure `GaussianParams` is in scope — extend the existing `Mutation` import in each file to also import `GaussianParams` (both live in the same `crate::operations` / `operations` path). The default sigma value 0.1 is unchanged.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'Mutation::Differential { \.\. }' src/engines/moead/mod.rs src/engines/nsga2/mod.rs src/engines/nsga3/mod.rs` returns 0 for each file
    - `grep -rc 'Mutation::Differential(\.\.)' src/engines/moead/mod.rs src/engines/nsga2/mod.rs src/engines/nsga3/mod.rs` returns at least 1 for each file
    - `grep -c 'Mutation::Gaussian { sigma: Some(0.1) }' src/engines/cellular/configuration.rs src/engines/alps/configuration.rs` returns 0
    - `grep -c 'Mutation::Gaussian(GaussianParams { sigma: Some(0.1) })' src/engines/cellular/configuration.rs` returns 1 and same for alps/configuration.rs
    - `cargo build` succeeds (no E0769 tuple-variant-used-like-struct errors)
  </acceptance_criteria>
  <done>All three multi-objective engine Differential guards use `(..)` tuple wildcard; cellular and ALPS default Gaussian construction uses GaussianParams with sigma=0.1 unchanged; `cargo build` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Internal Rust library refactor. No new trust boundary introduced — engine call sites are crate-internal dispatch. No network I/O, no user-supplied untrusted input crossing a process boundary, no auth, no new dependency.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02 | Tampering | engine match-arm dispatch | accept | Match arms are exhaustive over a closed enum; the compiler enforces totality. A missed arm is a compile error, not a runtime fault. No new attack surface. |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added. No package legitimacy audit required. |

No high-severity threats apply (ASVS L1: V2–V6 not applicable — no auth, no network, no external input validation surface).
</threat_model>

<verification>
- `cargo build` succeeds with all 7 engine files compiling
- No default value changed: Differential f=0.5, cellular/ALPS Gaussian sigma=0.1
- `grep -rn 'Mutation::Differential { \.\. }' src/engines/` returns zero matches
- `grep -rn 'Mutation::Gaussian { ' src/engines/cellular/configuration.rs src/engines/alps/configuration.rs` returns zero matches
</verification>

<success_criteria>
- 4 factory_with_chromosome_length call sites pass 3 args
- generation.rs Differential arm destructures DifferentialParams
- moead/nsga2/nsga3 Differential guards use tuple wildcard `(..)`
- cellular + ALPS default Gaussian uses GaussianParams
- `cargo build` green; zero behavioral change
</success_criteria>

<artifacts_this_phase_produces>
No new symbols — this plan only updates existing call sites and match arms to the tuple-variant shape introduced in Plan 01. Changed call signatures consumed: `factory_with_chromosome_length` now called with 3 args at 4 sites. Imports extended: `DifferentialParams` (generation.rs), `GaussianParams` (cellular/configuration.rs, alps/configuration.rs).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/71-per-operator-mutation-params/71-02-SUMMARY.md` when done
</output>
