---
phase: 71-per-operator-mutation-params
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/traits/configuration.rs
  - src/configuration.rs
  - src/engines/cellular/engine.rs
  - src/engines/alps/engine.rs
  - src/lib.rs
  - tests/operations/test_mutation_creep_gaussian.rs
  - tests/operations/test_mutation_cauchy_levy_uniform.rs
  - tests/operations/test_mutation_self_adaptive.rs
  - tests/operations/test_mutation.rs
  - tests/observe/test_serde.rs
  - tests/test_multi_parent_integration.rs
  - tests/test_surrogate.rs
  - tests/test_variable_length.rs
  - tests/types/chromosomes/test_multi_range.rs
  - tests/types/chromosomes/test_unique.rs
  - tests/types/chromosomes/test_multi_unique.rs
  - tests/engines/moead/test_moead.rs
  - examples/surrogate_rastrigin.rs
  - examples/constrained_g1.rs
  - examples/island_model.rs
  - examples/rastrigin.rs
  - examples/niching.rs
  - examples/memetic_rastrigin.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every test constructs parameterized Mutation variants via tuple + param struct syntax"
    - "Every example constructs parameterized Mutation variants via tuple + param struct syntax"
    - "The one factory_with_params test call is migrated to factory"
    - "All doc-comment code examples use tuple-variant syntax"
    - "Full CI gate passes: cargo test, cargo test --features serde, cargo clippy, cargo doc, wasm32 check"
  artifacts:
    - path: "tests/observe/test_serde.rs"
      provides: "serde round-trip coverage for all reshaped Mutation variants"
    - path: "tests/test_variable_length.rs"
      provides: "factory call (migrated from removed factory_with_params)"
  key_links:
    - from: "tests/operations/test_mutation_creep_gaussian.rs"
      to: "src/operations.rs"
      via: "constructs Mutation::Gaussian(GaussianParams { sigma }) via tuple syntax"
      pattern: "Mutation::Gaussian\\(GaussianParams"
---

<objective>
Propagate the Phase 71 `Mutation` enum reshape (Plan 01) into all remaining consumers: doc-comment code examples in 5 src files, ~12 test files, and 6 example binaries that construct parameterized `Mutation` variants — migrating struct-field syntax (`Mutation::Gaussian { sigma: None }`) to tuple syntax (`Mutation::Gaussian(GaussianParams { sigma: None })`). Migrate the single `factory_with_params` test call to `factory` (D-05). Run the full phase verification gate.

Purpose: Complete the mechanical migration so the entire crate — including the test suite, examples, and rustdoc — compiles and passes under the new tuple-variant shape. This is the phase verification plan.
Output: All consumers updated; full CI matrix green; `71-VERIFICATION.md` written.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-per-operator-mutation-params/71-CONTEXT.md
@.planning/phases/71-per-operator-mutation-params/71-RESEARCH.md
@.planning/phases/71-per-operator-mutation-params/71-PATTERNS.md
@.planning/phases/71-per-operator-mutation-params/71-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate src doc-comment examples + tests + examples to tuple-variant syntax</name>
  <files>src/traits/configuration.rs, src/configuration.rs, src/engines/cellular/engine.rs, src/engines/alps/engine.rs, src/lib.rs, tests/operations/test_mutation_creep_gaussian.rs, tests/operations/test_mutation_cauchy_levy_uniform.rs, tests/operations/test_mutation_self_adaptive.rs, tests/operations/test_mutation.rs, tests/observe/test_serde.rs, tests/test_multi_parent_integration.rs, tests/test_surrogate.rs, tests/test_variable_length.rs, tests/types/chromosomes/test_multi_range.rs, tests/types/chromosomes/test_unique.rs, tests/types/chromosomes/test_multi_unique.rs, tests/engines/moead/test_moead.rs, examples/surrogate_rastrigin.rs, examples/constrained_g1.rs, examples/island_model.rs, examples/rastrigin.rs, examples/niching.rs, examples/memetic_rastrigin.rs</files>
  <read_first>
    - .planning/phases/71-per-operator-mutation-params/71-RESEARCH.md ("Complete Change Inventory" — exact file→change table for trait/doc files, tests, examples; Pitfall 5 factory_with_params; Pitfall 6 valid_mutations lists are unit-only)
    - .planning/phases/71-per-operator-mutation-params/71-PATTERNS.md ("Test / Example Construction Site Pattern" — before/after construction syntax; "test_variable_length.rs line 58" factory migration)
    - src/operations.rs (the 8 param struct names + reshaped variants from Plan 01 — needed to know which struct wraps which variant)
    - tests/test_variable_length.rs (the `factory_with_params(Mutation::PermutationInsert, ...)` call ~line 58 to migrate)
    - tests/observe/test_serde.rs (full Mutation variant list exercised in round-trip tests)
  </read_first>
  <action>
    Apply a uniform, compiler-driven migration across all listed files. For every construction of a parameterized `Mutation` variant, wrap the inline fields in the matching param struct:
    - `Mutation::Creep { step: X }` → `Mutation::Creep(CreepParams { step: X })`
    - `Mutation::Gaussian { sigma: X }` → `Mutation::Gaussian(GaussianParams { sigma: X })`
    - `Mutation::Polynomial { eta: X }` → `Mutation::Polynomial(PolynomialParams { eta: X })`
    - `Mutation::NonUniform { b: X }` → `Mutation::NonUniform(NonUniformParams { b: X })`
    - `Mutation::Differential { f: X }` → `Mutation::Differential(DifferentialParams { f: X })`
    - `Mutation::Cauchy { scale: X }` → `Mutation::Cauchy(CauchyParams { scale: X })`
    - `Mutation::LevyFlight { alpha: X }` → `Mutation::LevyFlight(LevyFlightParams { alpha: X })`
    - `Mutation::SelfAdaptiveGaussian { tau, tau_prime, sigma_min, sigma_max }` → `Mutation::SelfAdaptiveGaussian(SelfAdaptiveGaussianParams { tau, tau_prime, sigma_min, sigma_max })`

    For any match arm in tests that uses the struct wildcard on a parameterized variant (e.g. `Mutation::Creep { .. }`, `Mutation::Gaussian { .. }` in `test_mutation.rs` factory_non_value rejection tests), convert to tuple wildcard `Mutation::Creep(..)`, `Mutation::Gaussian(..)`.

    Add the needed param-struct names to each file's `use genetic_algorithms::operations::{...}` import list alongside `Mutation` (e.g. add `GaussianParams`, `CreepParams`, etc. as used by that file). The structs are public via `pub mod operations`.

    In tests/test_variable_length.rs (~line 58): replace `factory_with_params(Mutation::PermutationInsert, &mut individual, None, None)` with `factory(Mutation::PermutationInsert, &mut individual)` and adjust the import from `factory_with_params` to `factory` (Pitfall 5). `Mutation::PermutationInsert` is a unit variant — no param struct needed.

    For the 5 src doc-comment files (traits/configuration.rs, configuration.rs, cellular/engine.rs, alps/engine.rs, lib.rs): update the `Mutation::Gaussian { sigma: ... }`-style snippets inside `///` / `//!` rustdoc code blocks to the tuple form, and add `GaussianParams` (or the relevant struct) to the snippet's `use` line so the doctest compiles.

    Do NOT alter `valid_mutations()` lists (Pitfall 6 — they contain only unit variants: Insertion, Swap, Inversion, etc.). Do NOT change any default values.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'Mutation::Gaussian {' tests/ examples/ src/` returns zero matches (no struct-field Gaussian construction or wildcard remains)
    - `grep -rn 'Mutation::\(Creep\|Polynomial\|NonUniform\|Differential\|Cauchy\|LevyFlight\|SelfAdaptiveGaussian\) {' tests/ examples/ src/` returns zero matches
    - `grep -rn 'factory_with_params' tests/ examples/ src/` returns zero matches
    - `grep -c 'factory(Mutation::PermutationInsert' tests/test_variable_length.rs` returns 1
    - `cargo test 2>&1 | tail -3` shows all tests passing (no compile errors, no failures)
  </acceptance_criteria>
  <done>All test, example, and src doc-comment construction sites use tuple + param struct syntax; the factory_with_params test call is migrated to factory; `cargo test` passes (default features).</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Phase verification gate — full CI matrix + 71-VERIFICATION.md</name>
  <files>.planning/phases/71-per-operator-mutation-params/71-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/71-per-operator-mutation-params/71-RESEARCH.md ("Validation Architecture" — full suite command, phase gate definition; "Phase Requirements → Test Map")
    - .planning/phases/71-per-operator-mutation-params/71-CONTEXT.md (success criteria: zero behavioral change, all existing tests pass)
    - CLAUDE.md (Testing section: required CI gates — cargo test, cargo test --features serde, cargo clippy, cargo doc --no-deps; WASM mandatory: cargo check --target wasm32-unknown-unknown)
  </read_first>
  <action>
    Run the full phase verification gate and capture the results. Execute, in order:
    1. `cargo test` — all tests pass (default features)
    2. `cargo test --features serde` — serde round-trip tests pass for the reshaped param structs (Pitfall 2 — this is the gate that catches a missing serde derive)
    3. `cargo clippy --all-targets -- -D warnings` — zero clippy warnings
    4. `cargo doc --no-deps` — zero rustdoc warnings (doctest snippets updated in Task 1 must compile)
    5. `cargo check --target wasm32-unknown-unknown` — WASM compile passes (no new par_iter / Instant introduced; this is purely a type-shape change so it should pass, but it is mandatory per CLAUDE.md)

    If any gate fails, fix the offending construction/match site (almost certainly a missed tuple migration or a missing param-struct import) and re-run from step 1. The compiler error names the exact file and line.

    Write `.planning/phases/71-per-operator-mutation-params/71-VERIFICATION.md` recording: each gate command, its pass/fail status, the test count from `cargo test`, and a one-line confirmation that no mutation default value changed (Creep 0.01, Gaussian 0.1, Polynomial 20.0, Cauchy 1.0, LevyFlight 1.5, SelfAdaptive ES-standard formulas). Confirm the phase success criteria from ROADMAP Phase 71 are met: (1) each parameterized operator has its own param struct; (2) the Mutation factory dispatches to the correct param type with no unused step/sigma; (3) cargo test / clippy / doc pass with zero warnings.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5 && cargo test --features serde 2>&1 | tail -5 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && cargo doc --no-deps 2>&1 | tail -5 && cargo check --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0 with all tests passing
    - `cargo test --features serde` exits 0 (serde round-trip green — confirms param structs carry serde derives)
    - `cargo clippy --all-targets -- -D warnings` exits 0 (zero warnings)
    - `cargo doc --no-deps` exits 0 with zero rustdoc warnings
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - `.planning/phases/71-per-operator-mutation-params/71-VERIFICATION.md` exists and records all 5 gate results plus the no-default-changed confirmation
  </acceptance_criteria>
  <done>All 5 CI gates pass; 71-VERIFICATION.md written documenting each gate result and confirming zero behavioral change and ROADMAP Phase 71 success criteria 1–3 are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Internal Rust library refactor — this plan touches tests, examples, and doc comments. No network I/O, no untrusted input crossing a process boundary, no auth, no new dependency. The only data-ingress path exercised is the existing serde checkpoint round-trip in `tests/observe/test_serde.rs`, whose effective behavior is unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Tampering | serde round-trip of reshaped param structs | mitigate | `cargo test --features serde` (gate step 2) asserts every reshaped variant serializes and deserializes; a missing `serde` derive on a param struct fails this gate — caught before merge |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added. No package legitimacy audit required. |

No high-severity threats apply (ASVS L1: V2–V6 not applicable — no auth, no network, no external input validation surface beyond existing serde, which step 2 verifies).
</threat_model>

<verification>
- `cargo test` green (default)
- `cargo test --features serde` green (serde derives present on all param structs)
- `cargo clippy --all-targets -- -D warnings` green
- `cargo doc --no-deps` green (doctest snippets compile under new syntax)
- `cargo check --target wasm32-unknown-unknown` green
- `grep -rn 'Mutation::Gaussian {' src/ tests/ examples/` returns zero matches crate-wide
- `grep -rn 'factory_with_params' src/ tests/ examples/` returns zero matches crate-wide
</verification>

<success_criteria>
- All test/example/doc consumers migrated to tuple-variant syntax
- factory_with_params call migrated to factory
- Full 5-gate CI matrix passes with zero warnings
- 71-VERIFICATION.md written
- ROADMAP Phase 71 success criteria 1–3 confirmed met
</success_criteria>

<artifacts_this_phase_produces>
New file: `.planning/phases/71-per-operator-mutation-params/71-VERIFICATION.md` (phase gate record).
No new code symbols — this plan migrates consumers to the tuple-variant shape from Plan 01 and runs the verification gate. Imports extended across test/example/doc files to bring the relevant `*Params` structs into scope. One test call migrated: `factory_with_params` → `factory` in tests/test_variable_length.rs.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/71-per-operator-mutation-params/71-03-SUMMARY.md` when done
</output>
