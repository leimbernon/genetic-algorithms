---
phase: 62
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - src/engines/ga.rs
  - tests/test_surrogate.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can call .with_surrogate(model, 0.5) on Ga<U> and the engine stores the (model, fraction) pair"
    - "build() rejects prescreening_fraction <= 0.0 or > 1.0 with a clear GaError::ConfigurationError"
    - "Each generation, after parent_crossover() returns offspring, the surrogate prescreens them in-place down to max(1, floor(n * fraction)) survivors before any FitnessCache or BatchFitnessEvaluator call"
    - "GenerationStats.true_fitness_calls reports Some(post_prescreening_offspring_count) when a surrogate is configured and None otherwise"
    - "Surrogate composes with BatchFitnessEvaluator (D-09) — both can be set simultaneously and the surrogate runs first"
    - "Rejected offspring (bottom 1 - fraction by predicted score) do not enter repair, constraint, or fitness paths"
  artifacts:
    - path: "src/engines/ga.rs"
      provides: "surrogate field, with_surrogate builder, build() validation, prescreening insertion, gen_stats wiring"
      contains: "with_surrogate"
    - path: "tests/test_surrogate.rs"
      provides: "Appended engine-runtime tests (SC-1b, SC-1c, SC-1e, SC-1f, SC-2a, SC-2b, SC-3)"
      contains: "#[test]"
  key_links:
    - from: "src/engines/ga.rs (parent_crossover output site, ~line 1711)"
      to: "src/fitness/surrogate.rs (SurrogateModel::predict)"
      via: "surrogate.predict(&offspring[i]) sort + truncate"
      pattern: "self.surrogate"
    - from: "src/engines/ga.rs (gen_stats site, ~line 2085)"
      to: "src/stats.rs (true_fitness_calls field)"
      via: "gen_stats.true_fitness_calls = true_fitness_calls;"
      pattern: "gen_stats.true_fitness_calls"
---

<objective>
Wire the `SurrogateModel<U>` trait from Plan 01 into the `Ga<U>` engine: storage
field, builder method, build()-time validation, prescreening step in the
generation loop, and per-generation `true_fitness_calls` stats assignment.
This plan APPENDS engine-runtime tests to `tests/test_surrogate.rs`; Plan 01 left
zero `#[ignore]` markers, so this plan does NOT un-ignore anything — it adds new tests.

Purpose: Deliver D-03 through D-11 in the engine hot path. The surrogate is a
filter that runs immediately after offspring generation (post-`parent_crossover()`,
pre-`batch_evaluate` / `cache check` / `repair` / `constraints` per D-08 and
Pitfall 1 in RESEARCH.md). Mirrors the Phase 60 `batch_evaluator` pattern exactly.

Output: One field, one builder method, one validation branch, one prescreening
block, one stats assignment, and seven new integration tests appended to
`tests/test_surrogate.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-surrogate-assisted-evaluation/62-CONTEXT.md
@.planning/phases/62-surrogate-assisted-evaluation/62-RESEARCH.md
@.planning/phases/62-surrogate-assisted-evaluation/62-VALIDATION.md
@.planning/phases/62-surrogate-assisted-evaluation/62-01-SUMMARY.md
@src/engines/ga.rs
@src/fitness/surrogate.rs
@src/stats.rs

<interfaces>
<!-- batch_evaluator pattern in src/engines/ga.rs — copy the structure verbatim, replacing
     batch_evaluator with surrogate where indicated. Line numbers reflect HEAD state and
     will shift after Task 1 edits — use grep to relocate, do not assume offsets. -->

```rust
// Field declaration (around line 289):
batch_evaluator: Option<Arc<dyn crate::fitness::BatchFitnessEvaluator<U> + Send + Sync>>,

// Default impl entry (around line 375):
batch_evaluator: None,

// build() validation block (around lines 778-784):
if self.fitness_fn.is_some() && self.batch_evaluator.is_some() {
    return Err(GaError::ConfigurationError(
        "Cannot use both fitness_fn and with_batch_evaluator() — they are mutually exclusive"
            .to_string(),
    ));
}

// Builder method (around lines 950-956):
pub fn with_batch_evaluator(
    mut self,
    evaluator: Arc<dyn crate::fitness::BatchFitnessEvaluator<U> + Send + Sync>,
) -> Self {
    self.batch_evaluator = Some(evaluator);
    self
}

// Insertion point — offspring is in scope right after parent_crossover() (line 1710):
let mut offspring = /* result of parent_crossover(...) */;
// Then at line 1712 batch_evaluate happens.

// gen_stats cache delta pattern (lines 2080-2085):
if let Some(ref ch) = self.fitness_cache {
    let c = ch.lock().expect("fitness cache lock poisoned");
    gen_stats.cache_hits = Some(c.hits().saturating_sub(prev_cache_hits));
    gen_stats.cache_misses = Some(c.misses().saturating_sub(prev_cache_misses));
}
```

<!-- New trait (created in Plan 01): -->

```rust
// src/fitness/surrogate.rs
pub trait SurrogateModel<U: ChromosomeT>: Send + Sync {
    fn predict(&self, chromosome: &U) -> f64;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add surrogate field, with_surrogate() builder, and build() validation</name>
  <files>src/engines/ga.rs, tests/test_surrogate.rs</files>
  <read_first>
    - src/engines/ga.rs (entire batch_evaluator integration: field decl ~line 289, Default impl ~line 375, build() validation ~lines 778-784, builder method ~lines 940-956) — locate via `grep -n batch_evaluator src/engines/ga.rs`
    - src/fitness/surrogate.rs (the trait being plugged in)
    - .planning/phases/62-surrogate-assisted-evaluation/62-RESEARCH.md (Pattern 2 and Pattern 7)
  </read_first>
  <behavior>
    - Test 1 (SC-1e) `invalid_fraction_zero_rejected`: `.with_surrogate(model, 0.0)` followed by `.build()` returns `GaError::ConfigurationError` whose message contains "prescreening_fraction".
    - Test 2 (SC-1f) `invalid_fraction_over_one_rejected`: `.with_surrogate(model, 1.5)` followed by `.build()` returns `GaError::ConfigurationError`.
    - Test 3 `boundary_fraction_one_accepted`: `.with_surrogate(model, 1.0)` followed by `.build()` succeeds (upper boundary is inclusive per D-03).
  </behavior>
  <action>
    Inside `impl<U> Ga<U> where U: LinearChromosome { ... }`, add a new field next to `batch_evaluator`:
    `surrogate: Option<(Arc<dyn crate::fitness::SurrogateModel<U> + Send + Sync>, f64)>` (research Pattern 2; the f64 is `prescreening_fraction` stored inline in the tuple per Claude's discretion in CONTEXT.md). Place it immediately after the `batch_evaluator` field declaration. Add a rustdoc `///` block explaining D-04 (rejected offspring dropped), D-06 (offspring only), D-08 (pipeline order: surrogate → cache → batch).
    Update the `Default` impl to include `surrogate: None` (Pitfall 5 in RESEARCH.md) — add it immediately after the existing `batch_evaluator: None,` line. Verify no other place constructs `Ga { ... }` with explicit fields; if found, add `surrogate: None` there too (search with `grep -n "batch_evaluator: None" src/engines/ga.rs`).
    Add `pub fn with_surrogate(mut self, model: Arc<dyn crate::fitness::SurrogateModel<U> + Send + Sync>, prescreening_fraction: f64) -> Self { self.surrogate = Some((model, prescreening_fraction)); self }`. Place it immediately after `with_batch_evaluator()`. Include a rustdoc block stating the (0.0, 1.0] valid range and D-09 (composes with `BatchFitnessEvaluator`).
    In `build()`, immediately after the existing batch/fitness mutual-exclusivity block (~lines 778-784), add a validation block matching research Pattern 7:
    `if let Some((_, fraction)) = &self.surrogate { if *fraction <= 0.0 || *fraction > 1.0 { return Err(GaError::ConfigurationError("prescreening_fraction must be in (0.0, 1.0]".to_string())); } }`.
    Do NOT yet add the prescreening step to the generation loop — that is Task 2.
    APPEND three new tests to `tests/test_surrogate.rs` (Plan 01 created the file with zero `#[ignore]`; this task adds tests, it does NOT un-ignore anything): `invalid_fraction_zero_rejected`, `invalid_fraction_over_one_rejected`, `boundary_fraction_one_accepted`. These tests only require build-time validation, NOT the prescreening step.
  </action>
  <verify>
    <automated>cargo build --lib &amp;&amp; cargo test --test test_surrogate invalid_fraction_zero_rejected invalid_fraction_over_one_rejected boundary_fraction_one_accepted 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "surrogate: Option&lt;(Arc&lt;dyn crate::fitness::SurrogateModel&lt;U&gt;" src/engines/ga.rs` returns 1
    - Source assertion: `grep -c "surrogate: None," src/engines/ga.rs` returns ≥1 (Default impl + any other explicit constructor)
    - Source assertion: `grep -c "pub fn with_surrogate(" src/engines/ga.rs` returns 1
    - Source assertion: `grep -c "prescreening_fraction must be in" src/engines/ga.rs` returns 1
    - Command: `cargo test --test test_surrogate invalid_fraction_zero_rejected` exits 0 with 1 passed
    - Command: `cargo test --test test_surrogate invalid_fraction_over_one_rejected` exits 0 with 1 passed
    - Command: `cargo test --test test_surrogate boundary_fraction_one_accepted` exits 0 with 1 passed
    - Command: `cargo clippy --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Surrogate storage and configuration surface complete; build() rejects out-of-range fractions; three build-time tests appended and green; no behavior change yet in the generation loop.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert prescreening hot-path block in ga.rs and verify build-clean</name>
  <files>src/engines/ga.rs</files>
  <read_first>
    - src/engines/ga.rs lines 1688-1740 (offspring collection from parent_crossover, batch_evaluate site at 1712, repair at 1728, constraints at 1736) — verified insertion point per RESEARCH.md Pitfall 1
    - src/engines/ga.rs lines 2070-2090 (gen_stats cache delta assignment pattern at 2083-2084 — true_fitness_calls assignment mirrors this)
    - src/fitness/surrogate.rs (trait — uses `surrogate.predict(&offspring[i])`)
    - .planning/phases/62-surrogate-assisted-evaluation/62-RESEARCH.md (Pattern 3, Pattern 5, Pitfall 1, Pitfall 2)
  </read_first>
  <behavior>
    - No new tests in this task. This is the highest-risk step (hot-path insertion); the gate is build-clean + clippy-clean + zero regression in existing GA tests. Test activation comes in Task 3, isolated from the hot-path edit.
  </behavior>
  <action>
    In `src/engines/ga.rs`, locate the offspring generation site by grepping for `if let Some(eval) = self.batch_evaluator.as_ref().map(Arc::clone) {` (currently ~line 1712). Immediately BEFORE that `if let Some(eval) = ...` block — and immediately after the `let mut offspring = parent_crossover(...)?;` assignment (~line 1710) — insert the prescreening block per RESEARCH.md Pattern 3:
    Bind `let true_fitness_calls: Option&lt;u64&gt; = if let Some((ref surrogate, fraction)) = self.surrogate { ... } else { None };` where the body: (a) early-returns `Some(0)` if `offspring.is_empty()`, (b) builds `let mut scores: Vec&lt;(usize, f64)&gt; = offspring.iter().enumerate().map(|(i, c)| { let s = surrogate.predict(c); (i, if s.is_nan() { f64::NEG_INFINITY } else { s }) }).collect();`, (c) sorts descending by score with `scores.sort_unstable_by(|a, b| b.1.partial_cmp(&a.1).unwrap());`, (d) computes `let keep = ((offspring.len() as f64 * fraction).floor() as usize).max(1);` then `scores.truncate(keep);`, (e) restores original order with `scores.sort_unstable_by_key(|&(idx, _)| idx);`, (f) rebuilds `offspring` as `scores.into_iter().map(|(idx, _)| offspring[idx].clone()).collect()`, (g) yields `Some(offspring.len() as u64)`.
    Use sequential sort only — do NOT add a `par_iter` or cfg gate (RESEARCH.md Anti-Pattern, Pattern 3 note). The block is unconditionally WASM-safe.
    After the existing cache-delta block (~line 2080-2085 — locate via grep `gen_stats.cache_hits = Some`), append: `gen_stats.true_fitness_calls = true_fitness_calls;` per RESEARCH.md Pattern 5. The local binding `true_fitness_calls` from earlier in the same iteration must be in scope — if it is not (lexical scope mismatch in the actual generation loop layout), promote it to a mutable `Option<u64>` declared at the top of the loop body and assigned in the surrogate branch.
    Do NOT touch `tests/test_surrogate.rs` in this task — that is Task 3.
  </action>
  <verify>
    <automated>cargo build --lib &amp;&amp; cargo clippy --lib -- -D warnings &amp;&amp; cargo test --lib 2>&amp;1 | tail -5 &amp;&amp; cargo check --target wasm32-unknown-unknown 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "if let Some((ref surrogate, fraction)) = self.surrogate" src/engines/ga.rs` returns 1
    - Source assertion: `grep -c "gen_stats.true_fitness_calls = true_fitness_calls" src/engines/ga.rs` returns 1
    - Source assertion: `grep -c "f64::NEG_INFINITY" src/engines/ga.rs` returns ≥1 (the NaN substitution)
    - Source assertion: `grep -c '\.max(1)' src/engines/ga.rs` returns ≥1 (the prescreening floor formula)
    - Source assertion: zero `par_iter` calls added to the prescreening block (`grep -B 2 -A 30 'if let Some((ref surrogate' src/engines/ga.rs | grep -c par_iter` returns 0)
    - Command: `cargo build --lib` exits 0
    - Command: `cargo clippy --lib -- -D warnings` exits 0
    - Command: `cargo test --lib` exits 0 (no regression in existing GA tests — gate before any new tests are added)
    - Command: `cargo check --target wasm32-unknown-unknown` exits 0 (hot-path edit must remain WASM-safe)
  </acceptance_criteria>
  <done>
    Prescreening block is inserted, builds clean, clippy-clean, no existing-test regression, WASM-compatible. No new tests added yet — Task 3 owns that work, isolated from the hot-path edit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append engine-runtime tests for SC-1b, SC-1c, SC-2a, SC-2b, SC-3</name>
  <files>tests/test_surrogate.rs</files>
  <read_first>
    - tests/test_surrogate.rs (current contents from Plan 01 + Task 1 of this plan)
    - src/engines/ga.rs (the now-inserted prescreening block from Task 2; locate the `with_surrogate` builder and the prescreening boundary via grep)
    - src/fitness/batch.rs (BatchFitnessEvaluator trait — needed to define a counting batch evaluator for SC-3)
    - examples or tests using `Ga::new()...build()...run()` to mirror the configuration boilerplate
  </read_first>
  <behavior>
    - Test 1 (SC-1b) `ga_with_surrogate_runs`: A `Ga` configured with `.with_surrogate(model, 0.5)` and a fitness fn completes a multi-generation run without panic.
    - Test 2 (SC-1c) `prescreening_fraction_reduces_evaluations`: with fraction=0.5 and offspring count n, the post-prescreening `gen_stats.true_fitness_calls` is `Some(max(1, n/2))` for at least one generation in a 5-generation run.
    - Test 3 (SC-2a) `true_fitness_calls_populated_in_stats`: with a surrogate configured, every emitted `GenerationStats` has `true_fitness_calls.is_some()`.
    - Test 4 (SC-2b) `true_fitness_calls_none_without_surrogate`: with no surrogate, every emitted `GenerationStats` has `true_fitness_calls.is_none()`.
    - Test 5 (SC-3) `surrogate_with_batch_evaluator_composes`: a `Ga` configured with both `.with_surrogate(model, 0.5)` and `.with_batch_evaluator(eval)` runs to completion and the batch evaluator receives at most `max(1, floor(offspring_count * 0.5))` chromosomes per generation (verify via an `AtomicUsize` counter inside the batch evaluator).
    - All five tests run green at end of Plan 02. No `#[ignore]` attributes in the test file.
  </behavior>
  <action>
    APPEND five new tests to `tests/test_surrogate.rs`. For SC-3, define a batch evaluator struct with an internal `Arc<AtomicUsize>` capturing the per-call chromosome slice length; after the run, assert the recorded max ≤ `max(1, floor(offspring_count * 0.5))`. For SC-1c, instrument the test to inspect `ga.stats()` (or whichever accessor exposes `Vec&lt;GenerationStats&gt;`) and check at least one generation matches the formula. Keep test runtime short: `population_size = 20`, `generations = 5`.
    Final `tests/test_surrogate.rs` contents at end of Plan 02 (file maintains zero `#[ignore]` markers): four tests from Plan 01 (SC-1a, SC-1d, SC-1g, SC-2c) + three from Task 1 (SC-1e, SC-1f, boundary 1.0) + five from this task (SC-1b, SC-1c, SC-2a, SC-2b, SC-3) = 12 tests, 12 passing.
  </action>
  <verify>
    <automated>cargo test --test test_surrogate 2>&amp;1 | tail -10; cargo test --test test_surrogate --features serde 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "fn ga_with_surrogate_runs" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn prescreening_fraction_reduces_evaluations" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn true_fitness_calls_populated_in_stats" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn true_fitness_calls_none_without_surrogate" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn surrogate_with_batch_evaluator_composes" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "#\[ignore" tests/test_surrogate.rs` returns 0 (zero ignored tests across all of Plan 01 + Plan 02)
    - Command: `cargo test --test test_surrogate` exits 0 with ≥11 named tests passing (12 with `--features serde`)
    - Command: `cargo test --lib` exits 0 (no regression)
    - Behavior: `prescreening_fraction_reduces_evaluations` test confirms `gen_stats.true_fitness_calls.unwrap() &lt;= max(1, offspring_count / 2)` for at least one observed generation
    - Behavior: `surrogate_with_batch_evaluator_composes` test confirms batch evaluator receives a slice of length ≤ `max(1, floor(offspring_count * 0.5))` per call
  </acceptance_criteria>
  <done>
    All engine-runtime tests appended and passing. Surrogate prescreens offspring before any cache/batch/repair/constraint path; rejected offspring are dropped; `true_fitness_calls` is populated; all SC-1*, SC-2*, SC-3 tests pass; existing Ga test suite is unchanged.
  </done>
</task>

</tasks>

<verification>
- `cargo build --lib` succeeds
- `cargo test --test test_surrogate` runs ≥11 tests, all passing (12 with `--features serde`)
- `cargo test --lib` shows no regression in the existing GA suite
- `cargo clippy --lib -- -D warnings` passes
- `cargo check --target wasm32-unknown-unknown` passes (no rayon added in prescreening block)
- Manual inspection: prescreening block sits BETWEEN `parent_crossover()` and the batch_evaluate `if let Some(eval) = ...` line; repair (~1728) and constraints (~1736) run AFTER prescreening on the reduced offspring slice (Pitfall 1)
</verification>

<success_criteria>
- `Ga::with_surrogate(model, fraction)` is part of the public builder API
- `build()` rejects `prescreening_fraction <= 0.0` and `> 1.0` with a clear error
- Surrogate prescreens offspring immediately after `parent_crossover()` and before any downstream evaluation step
- `GenerationStats.true_fitness_calls` is `Some(n)` when surrogate is configured, `None` otherwise
- Surrogate composes cleanly with `BatchFitnessEvaluator` per D-09
- All Plan 01 + Plan 02 tests in `tests/test_surrogate.rs` are active and green; zero `#[ignore]` markers
- Zero regression: pre-existing GA tests pass unchanged
</success_criteria>

<output>
Create `.planning/phases/62-surrogate-assisted-evaluation/62-02-SUMMARY.md` when done.
</output>
