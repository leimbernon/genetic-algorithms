---
phase: 73-move-inline-test-modules
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_benchmarks.rs
  - tests/benchmarks/dtlz.rs
  - tests/benchmarks/single_objective.rs
  - tests/benchmarks/zdt.rs
  - src/benchmarks/dtlz.rs
  - src/benchmarks/single_objective.rs
  - src/benchmarks/zdt.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "grep for #[cfg(test)] returns zero matches in the three src/benchmarks files"
    - "cargo test --features benchmarks --tests runs the 40 migrated benchmark tests (17 dtlz + 13 zdt + 10 single_objective)"
    - "Serde-gated benchmark tests run only under --features benchmarks,serde and pass"
    - "D-04: mirrored-subdirectory pattern followed — tests/benchmarks/ mirrors src/benchmarks/"
    - "D-05: benchmark files placed at tests/benchmarks/dtlz.rs, tests/benchmarks/zdt.rs, tests/benchmarks/single_objective.rs"
  artifacts:
    - path: "tests/test_benchmarks.rs"
      provides: "New harness binary declaring mod benchmarks { mod dtlz; mod single_objective; mod zdt; }"
      contains: "mod benchmarks"
    - path: "tests/benchmarks/dtlz.rs"
      provides: "17 feature-gated DTLZ tests (non-serde + serde) migrated from src"
      contains: "test_dtlz1_form"
    - path: "tests/benchmarks/zdt.rs"
      provides: "13 feature-gated ZDT tests migrated from src"
    - path: "tests/benchmarks/single_objective.rs"
      provides: "10 feature-gated single-objective tests migrated from src"
    - path: "src/benchmarks/dtlz.rs"
      provides: "dtlz source with inline test block removed; pub impls unchanged"
    - path: "src/benchmarks/zdt.rs"
      provides: "zdt source with inline test block removed"
    - path: "src/benchmarks/single_objective.rs"
      provides: "single_objective source with inline test block removed"
  key_links:
    - from: "tests/test_benchmarks.rs"
      to: "tests/benchmarks/dtlz.rs"
      via: "mod benchmarks { mod dtlz; }"
      pattern: "mod dtlz"
    - from: "tests/benchmarks/dtlz.rs"
      to: "src/benchmarks/dtlz.rs"
      via: "use genetic_algorithms::benchmarks::dtlz::{DTLZ1, ...} and genetic_algorithms::benchmarks::BenchmarkFn"
      pattern: "genetic_algorithms::benchmarks"
---

<objective>
Migrate the three feature-gated benchmark inline test blocks (`dtlz` 17 tests, `zdt` 13 tests, `single_objective` 10 tests, each with a nested `#[cfg(feature = "serde")] mod serde_tests`) into new files under a new `tests/benchmarks/` directory, create the new `tests/test_benchmarks.rs` harness to make Cargo discover them, then delete the inline blocks from the three source files.

Purpose: Removes the final 3 of the 10 inline test blocks (issue #266). This is the Wave 0 heavy lift — both the harness file and the `tests/benchmarks/` directory are net-new (nothing exists yet), and the benchmark module is behind `#[cfg(feature = "benchmarks")]` so every migrated test function must carry that gate, with serde tests gated `#[cfg(all(feature = "benchmarks", feature = "serde"))]` per D-10.
Output: New harness + three new benchmark test files; three benchmark source files with inline blocks removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-move-inline-test-modules/73-CONTEXT.md
@.planning/phases/73-move-inline-test-modules/73-RESEARCH.md
@.planning/phases/73-move-inline-test-modules/73-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_benchmarks.rs harness + the three feature-gated benchmark test files</name>
  <files>tests/test_benchmarks.rs, tests/benchmarks/dtlz.rs, tests/benchmarks/single_objective.rs, tests/benchmarks/zdt.rs</files>
  <read_first>
    - src/benchmarks/dtlz.rs lines 561-end (the inline block: 17 tests starting at the `#[cfg(test)] mod tests` near line 561, with a nested `#[cfg(feature = "serde")] mod serde_tests` near line 791; tests use `use super::*;` to reach `DTLZ1..DTLZ7` and the `BenchmarkFn::evaluate` method)
    - src/benchmarks/zdt.rs lines 391-end (13 tests near line 391, nested serde block near line 546)
    - src/benchmarks/single_objective.rs lines 222-end (10 tests near line 222, nested serde block near line 306)
    - src/benchmarks/mod.rs lines 10-16 (public re-exports: `pub trait BenchmarkFn` at line 22 of mod.rs is reachable as `genetic_algorithms::benchmarks::BenchmarkFn`; structs reachable as `genetic_algorithms::benchmarks::dtlz::DTLZ1` or the flat re-export `genetic_algorithms::benchmarks::DTLZ1`)
    - src/lib.rs line 321-322 (`#[cfg(feature = "benchmarks")] pub mod benchmarks;` — confirms the whole module is feature-gated)
    - tests/engines/multi_objective/indicators/test_generational_distance.rs (analog for the bare-`#[test]`, no-`mod tests`-wrapper integration file structure)
    - .planning/phases/73-move-inline-test-modules/73-PATTERNS.md (sections "tests/test_benchmarks.rs (new file)", "tests/benchmarks/dtlz.rs (new file)", and the "Feature-gated test pattern" shared pattern)
    - .planning/phases/73-move-inline-test-modules/73-RESEARCH.md (Pattern 4 feature-gated tests, Pitfall 2 forgetting the harness, Pitfall 6 nested serde_tests flattening)
  </read_first>
  <action>
    Create tests/test_benchmarks.rs (new top-level harness binary) declaring the three subdirectory test files: a `mod benchmarks { ... }` block containing `mod dtlz;`, `mod single_objective;`, `mod zdt;`. No imports and no `#[cfg]` gate on the harness itself — the feature gating lives inside each test file.

    Create the new `tests/benchmarks/` directory with three files. For each, copy the test function bodies verbatim from the corresponding source inline block, converting to integration-test form:
    - Replace `use super::*;` with fully-qualified imports: bring `BenchmarkFn` from `genetic_algorithms::benchmarks` and the relevant structs from `genetic_algorithms::benchmarks::dtlz` / `::zdt` / `::single_objective` (e.g. `DTLZ1..DTLZ7`; `ZDT1..ZDT6`; `Sphere, Rastrigin, Ackley`). Gate the import lines themselves with `#[cfg(feature = "benchmarks")]` so the file compiles with the feature off.
    - No `mod tests` wrapper. Each test is a top-level `#[test]` function gated with `#[cfg(feature = "benchmarks")]`.
    - Carry over any `const EPSILON` or other helper constants/functions used by the tests, also gated `#[cfg(feature = "benchmarks")]`.
    - Flatten the nested `#[cfg(feature = "serde")] mod serde_tests { use super::*; ... }` block (D-10 / Pitfall 6): each serde test becomes a top-level `#[test]` function gated `#[cfg(all(feature = "benchmarks", feature = "serde"))]` — do NOT recreate a `mod serde_tests` wrapper, and resolve its `use super::*;` to the same fully-qualified `genetic_algorithms::benchmarks::...` paths.

    Result: tests/benchmarks/dtlz.rs holds all 17 dtlz tests (non-serde gated `benchmarks`, serde gated `benchmarks,serde`); tests/benchmarks/zdt.rs holds 13; tests/benchmarks/single_objective.rs holds 10.
  </action>
  <verify>
    <automated>test -f tests/test_benchmarks.rs && cargo test --features benchmarks --test test_benchmarks 2>&1 | grep -q 'test_dtlz1_form' && cargo test --features benchmarks --test test_benchmarks</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `tests/test_benchmarks.rs`, `tests/benchmarks/dtlz.rs`, `tests/benchmarks/single_objective.rs`, `tests/benchmarks/zdt.rs`
    - `grep -c 'mod dtlz' tests/test_benchmarks.rs` returns 1
    - `grep -c 'mod serde_tests' tests/benchmarks/dtlz.rs tests/benchmarks/zdt.rs tests/benchmarks/single_objective.rs` returns 0 across all three (serde tests flattened, not nested)
    - `grep -c 'use super::\*' tests/benchmarks/dtlz.rs tests/benchmarks/zdt.rs tests/benchmarks/single_objective.rs` returns 0 across all three
    - `cargo test --features benchmarks --test test_benchmarks` exits 0 and lists 40 test functions (17+13+10) in `-- --list`
    - `cargo test --features benchmarks,serde --test test_benchmarks` exits 0 and includes the serde roundtrip tests in `-- --list` (count strictly greater than the benchmarks-only count)
    - `cargo test --test test_benchmarks` (feature OFF) compiles and exits 0 reporting 0 run tests (every function is gated off, no compile error)
  </acceptance_criteria>
  <done>The new harness and three benchmark test files exist; all 40 benchmark tests run and pass under `--features benchmarks`; serde tests run additionally under `--features benchmarks,serde`; the file compiles cleanly with the feature off.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the inline #[cfg(test)] blocks from the three benchmark source files</name>
  <files>src/benchmarks/dtlz.rs, src/benchmarks/single_objective.rs, src/benchmarks/zdt.rs</files>
  <read_first>
    - src/benchmarks/dtlz.rs (the inline `#[cfg(test)] mod tests` block begins near line 561 and runs to end of file, including the nested `#[cfg(feature = "serde")] mod serde_tests`)
    - src/benchmarks/zdt.rs (block near line 391 to end of file)
    - src/benchmarks/single_objective.rs (block near line 222 to end of file)
  </read_first>
  <action>
    In each of the three benchmark source files, delete the entire trailing `#[cfg(test)] mod tests { ... }` block (including its nested `#[cfg(feature = "serde")] mod serde_tests` sub-block) from the `#[cfg(test)]` attribute line through the matching closing brace at end of file. Do NOT touch any code above the `#[cfg(test)]` line — the `pub struct` definitions, `impl BenchmarkFn for ...` blocks, and any private helper methods stay exactly as-is. Leave no orphan imports.
  </action>
  <verify>
    <automated>grep -rn '#\[cfg(test)\]' src/benchmarks/ | wc -l | grep -qx 0 && cargo test --features benchmarks --tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '#\[cfg(test)\]' src/benchmarks/` returns zero matches
    - `cargo build --features benchmarks` exits 0
    - `cargo clippy --all-targets --features benchmarks -- -D warnings` exits 0 (no dead-code/unused-import warnings from the deletions)
    - `cargo test --features benchmarks --tests` exits 0 with the benchmark tests running from the new tests/benchmarks/ files
  </acceptance_criteria>
  <done>All three benchmark source files contain zero `#[cfg(test)]` blocks; the crate compiles with and without the benchmarks feature; benchmark coverage is preserved in the new integration test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan only relocates and deletes test code. No new attack surface, no input handling, no security control is added, removed, or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Tampering | Test coverage integrity (feature-gated) | mitigate | The new files are created and verified present in `--features benchmarks --test test_benchmarks -- --list` (40 tests) BEFORE the inline blocks are deleted; serde subset verified under `--features benchmarks,serde`; feature-off compile verified — prevents silent coverage loss across all three feature combinations |

No package installs occur in this plan; the supply-chain checkpoint threat (T-73-SC) is not applicable.
</threat_model>

<verification>
- `cargo test --features benchmarks --test test_benchmarks` exits 0 with 40 tests
- `cargo test --features benchmarks,serde --test test_benchmarks` exits 0 with the serde roundtrip tests additionally present
- `cargo test --test test_benchmarks` (feature off) compiles and runs 0 tests with no error
- `grep -rn '#\[cfg(test)\]' src/benchmarks/` returns zero matches
- `cargo clippy --all-targets --features benchmarks -- -D warnings` exits 0
</verification>

<success_criteria>
- Zero `#[cfg(test)]` blocks remain in the three src/benchmarks files
- All 40 benchmark tests run from tests/benchmarks/ under the benchmarks feature; serde tests run under benchmarks+serde
- No coverage regression; the file compiles cleanly with the feature disabled
</success_criteria>

<artifacts>
## Artifacts this plan produces

**New files:**
- `tests/test_benchmarks.rs` — new top-level harness binary (`mod benchmarks { mod dtlz; mod single_objective; mod zdt; }`)
- `tests/benchmarks/dtlz.rs` — new file, 17 feature-gated DTLZ tests (with flattened serde tests)
- `tests/benchmarks/zdt.rs` — new file, 13 feature-gated ZDT tests
- `tests/benchmarks/single_objective.rs` — new file, 10 feature-gated single-objective tests

**New directory:**
- `tests/benchmarks/` — net-new subdirectory mirroring `src/benchmarks/`

**Modified files:**
- `src/benchmarks/dtlz.rs` — inline `#[cfg(test)]` block removed
- `src/benchmarks/zdt.rs` — inline `#[cfg(test)]` block removed
- `src/benchmarks/single_objective.rs` — inline `#[cfg(test)]` block removed
</artifacts>

<output>
Create `.planning/phases/73-move-inline-test-modules/73-04-SUMMARY.md` when done
</output>
