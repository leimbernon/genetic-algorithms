---
phase: 73-move-inline-test-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_engines.rs
  - src/engines/multi_objective/indicators/generational_distance.rs
  - src/engines/multi_objective/indicators/hypervolume.rs
  - src/engines/multi_objective/indicators/inverted_generational_distance.rs
  - src/engines/multi_objective/indicators/spread.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo test runs the 4 indicator integration test files (they are no longer dead code)"
    - "grep for #[cfg(test)] returns zero matches in the 4 indicator source files"
    - "Indicator tests pass under cargo test with no count regression"
    - "D-04: mirrored-subdirectory pattern followed — tests/engines/multi_objective/indicators/ mirrors src/engines/multi_objective/indicators/"
    - "D-06: indicator files placed at tests/engines/multi_objective/indicators/test_generational_distance.rs, test_hypervolume.rs, test_inverted_generational_distance.rs, test_spread.rs"
  artifacts:
    - path: "tests/test_engines.rs"
      provides: "mod multi_objective { mod indicators { ... } } wiring inside mod engines"
      contains: "mod indicators"
    - path: "tests/engines/multi_objective/indicators/test_generational_distance.rs"
      provides: "Now-wired GD integration tests (already on disk, unchanged)"
    - path: "tests/engines/multi_objective/indicators/test_hypervolume.rs"
      provides: "Now-wired hypervolume integration tests (already on disk, unchanged)"
    - path: "tests/engines/multi_objective/indicators/test_inverted_generational_distance.rs"
      provides: "Now-wired IGD integration tests (already on disk, unchanged)"
    - path: "tests/engines/multi_objective/indicators/test_spread.rs"
      provides: "Now-wired spread integration tests (already on disk, unchanged)"
  key_links:
    - from: "tests/test_engines.rs"
      to: "tests/engines/multi_objective/indicators/test_generational_distance.rs"
      via: "mod engines { mod multi_objective { mod indicators { mod test_generational_distance; } } }"
      pattern: "mod test_generational_distance"
---

<objective>
Wire the four already-existing-but-dead-code indicator integration test files into the `tests/test_engines.rs` harness, then delete the now-redundant inline `#[cfg(test)] mod tests` blocks from the four indicator source files.

Purpose: Removes 4 of the 10 inline test blocks (issue #266) and activates 30 integration tests (8+8+8+6) that currently never run because they are not declared in any harness file.
Output: Updated `tests/test_engines.rs` with indicator wiring; four indicator source files with their inline test blocks removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-move-inline-test-modules/73-CONTEXT.md
@.planning/phases/73-move-inline-test-modules/73-RESEARCH.md
@.planning/phases/73-move-inline-test-modules/73-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire indicators into test_engines.rs harness</name>
  <files>tests/test_engines.rs</files>
  <read_first>
    - tests/test_engines.rs (current harness — see the `mod engines { ... }` block; the `mod aos { mod test_aos; }` block sits at lines 63-65)
    - tests/engines/multi_objective/indicators/test_generational_distance.rs (confirm filename and that it imports via `use genetic_algorithms::multi_objective::indicators::generational_distance;`)
    - .planning/phases/73-move-inline-test-modules/73-PATTERNS.md (section "tests/test_engines.rs (harness edit — add multi_objective wiring)")
  </read_first>
  <action>
    Inside the existing `mod engines { ... }` block in tests/test_engines.rs, add a new `mod multi_objective` block containing a nested `mod indicators` block that declares the four existing test files by their filenames-without-extension: test_generational_distance, test_hypervolume, test_inverted_generational_distance, test_spread. Place the new `mod multi_objective` block immediately after the closing brace of the `mod aos { mod test_aos; }` block (currently lines 63-65) and before the `mod pso { mod test_pso; }` block, keeping it inside `mod engines`. The four target files already exist on disk under tests/engines/multi_objective/indicators/ — do NOT modify their contents. Do not add any `#[cfg(...)]` gate to these declarations (indicator tests are not feature-gated). The mod path chain must mirror the filesystem path relative to tests/test_engines.rs: engines -> multi_objective -> indicators -> test_* (Pitfall 1 in RESEARCH.md).
  </action>
  <verify>
    <automated>cargo test --test test_engines 2>&1 | grep -E 'test_gd_identical_fronts|test_spread' && cargo test --test test_engines</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mod indicators' tests/test_engines.rs` returns 1
    - `grep -c 'mod test_generational_distance' tests/test_engines.rs` returns 1
    - `cargo test --test test_engines` exits 0
    - The test names from the indicator files (e.g. `test_gd_identical_fronts`) appear in `cargo test --test test_engines -- --list` output (they did not before this task)
  </acceptance_criteria>
  <done>The four indicator test files are wired into test_engines.rs and their tests now run under cargo test; the test_engines binary test count increases by the indicator test totals (30 tests: 8 GD + 8 HV + 8 IGD + 6 spread).</done>
</task>

<task type="auto">
  <name>Task 2: Delete inline #[cfg(test)] blocks from the four indicator source files</name>
  <files>src/engines/multi_objective/indicators/generational_distance.rs, src/engines/multi_objective/indicators/hypervolume.rs, src/engines/multi_objective/indicators/inverted_generational_distance.rs, src/engines/multi_objective/indicators/spread.rs</files>
  <read_first>
    - src/engines/multi_objective/indicators/generational_distance.rs (inline block begins at the `#[cfg(test)]` line near line 61 and runs to end of file)
    - src/engines/multi_objective/indicators/hypervolume.rs (`#[cfg(test)]` near line 80)
    - src/engines/multi_objective/indicators/inverted_generational_distance.rs (`#[cfg(test)]` near line 64)
    - src/engines/multi_objective/indicators/spread.rs (`#[cfg(test)]` near line 98)
  </read_first>
  <action>
    In each of the four indicator source files, delete the entire trailing `#[cfg(test)] mod tests { ... }` block: from the `#[cfg(test)]` attribute line through the matching closing brace at end of file. These inline blocks (6+6+6+5 tests) test `pub(crate)` helpers (`nearest_distance`, `validate_non_empty`, `squared_euclidean_distance`, etc.) via `use super::*;` — their behavior is already covered by the now-wired external files which exercise the same invariants through the public indicator functions (`generational_distance`, `hypervolume`, `inverted_generational_distance`, `spread`) per D-01/D-02. Do NOT promote any `pub(crate)` helper to `pub`. Do NOT modify any non-test code (the public functions and helpers above the `#[cfg(test)]` line stay exactly as-is). Leave no orphan `use` import that existed only inside the deleted block.
  </action>
  <verify>
    <automated>grep -rn '#\[cfg(test)\]' src/engines/multi_objective/indicators/ | wc -l | grep -qx 0 && cargo test --test test_engines</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '#\[cfg(test)\]' src/engines/multi_objective/indicators/` returns zero matches
    - `cargo build` exits 0 (no orphaned imports, no compile errors in the four source files)
    - `cargo clippy --all-targets -- -D warnings` exits 0 (no dead-code or unused-import warnings introduced by the deletions)
    - `cargo test --test test_engines` still exits 0 with the indicator tests present
  </acceptance_criteria>
  <done>All four indicator source files contain zero `#[cfg(test)]` blocks; the crate compiles and clippy is clean; indicator test coverage is preserved via the wired external files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan only relocates and deletes test code. No new attack surface, no input handling, no security control is added, removed, or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | Test coverage integrity | mitigate | `cargo test` runs before/after; indicator tests must appear in `--list` output and pass, preventing silent coverage loss when inline blocks are deleted |

No package installs occur in this plan; the supply-chain checkpoint threat (T-73-SC) is not applicable.
</threat_model>

<verification>
- `cargo test --test test_engines` exits 0 with indicator tests present
- `grep -rn '#\[cfg(test)\]' src/engines/multi_objective/indicators/` returns zero matches
- `cargo clippy --all-targets -- -D warnings` exits 0
</verification>

<success_criteria>
- The four indicator test files are wired and their tests run under cargo test
- Zero `#[cfg(test)]` blocks remain in the four indicator source files
- No test-count regression for indicator coverage (external files contain >= the inline counts)
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Modified files (no new files created):**
- `tests/test_engines.rs` — gains a `mod multi_objective { mod indicators { ... } }` block inside `mod engines`
- `src/engines/multi_objective/indicators/generational_distance.rs` — inline `#[cfg(test)]` block removed
- `src/engines/multi_objective/indicators/hypervolume.rs` — inline `#[cfg(test)]` block removed
- `src/engines/multi_objective/indicators/inverted_generational_distance.rs` — inline `#[cfg(test)]` block removed
- `src/engines/multi_objective/indicators/spread.rs` — inline `#[cfg(test)]` block removed

**No new test files** — the four indicator test files at `tests/engines/multi_objective/indicators/test_*.rs` already exist on disk; this plan only wires them in.
</artifacts>

<output>
Create `.planning/phases/73-move-inline-test-modules/73-01-SUMMARY.md` when done
</output>
