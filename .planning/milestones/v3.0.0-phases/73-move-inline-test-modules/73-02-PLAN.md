---
phase: 73-move-inline-test-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/aos.rs
  - src/operations/local_search.rs
  - tests/engines/local_search.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "grep for #[cfg(test)] returns zero matches in src/aos.rs and src/operations/local_search.rs"
    - "The 7 local_search HillClimbingConfig/factory/config-default tests run from tests/engines/local_search.rs"
    - "All local_search and AOS coverage passes under cargo test with no regression"
    - "D-08: local_search inline tests merged into existing tests/engines/local_search.rs (not a separate file)"
  artifacts:
    - path: "tests/engines/local_search.rs"
      provides: "Existing GA-integration tests PLUS 7 appended migrated unit tests (improve, factory, factory_with_config, strategy/mode defaults)"
      contains: "test_hill_climbing_returns_improvements_count"
    - path: "src/aos.rs"
      provides: "AOS source with inline test block removed (superseded by tests/engines/aos/test_aos.rs)"
    - path: "src/operations/local_search.rs"
      provides: "local_search source with inline test block removed"
  key_links:
    - from: "tests/engines/local_search.rs"
      to: "src/operations/local_search.rs"
      via: "use genetic_algorithms::operations::{factory, factory_with_config, HillClimbingConfig, LocalSearch, LocalSearchApplicationStrategy, LocalSearchMode}"
      pattern: "factory_with_config"
---

<objective>
Delete two inline `#[cfg(test)]` blocks: the AOS block in `src/aos.rs` (fully superseded by the already-wired `tests/engines/aos/test_aos.rs`, so delete-only), and the local_search block in `src/operations/local_search.rs` (its 7 tests use public-API items, so merge them into the already-wired `tests/engines/local_search.rs` first, then delete the inline block).

Purpose: Removes 2 of the 10 inline test blocks (issue #266) while preserving all coverage. AOS is delete-only because its inline tests reach private state (`state.arms`) that the external file already covers via the public `num_arms()` accessor.
Output: `tests/engines/local_search.rs` with 7 appended tests; `src/aos.rs` and `src/operations/local_search.rs` with inline blocks removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-move-inline-test-modules/73-CONTEXT.md
@.planning/phases/73-move-inline-test-modules/73-RESEARCH.md
@.planning/phases/73-move-inline-test-modules/73-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the inline #[cfg(test)] block from src/aos.rs (delete-only)</name>
  <files>src/aos.rs</files>
  <read_first>
    - src/aos.rs (the inline `#[cfg(test)] mod tests` block begins at the `#[cfg(test)]` line near line 425 and runs to end of file; note the test `test_new_creates_correct_number_of_arms` reads private field `state.arms` which cannot move to integration tests)
    - tests/engines/aos/test_aos.rs (already wired in test_engines.rs at `mod aos { mod test_aos; }`; contains 25 tests that cover the same cases through the public API, e.g. `state.num_arms()` instead of `state.arms.len()` — confirm it exists and is non-empty)
    - .planning/phases/73-move-inline-test-modules/73-RESEARCH.md (Pitfall 4 — AOS private field access; the inline block is deleted, not ported)
  </read_first>
  <action>
    Delete the entire trailing `#[cfg(test)] mod tests { ... }` block from src/aos.rs: from the `#[cfg(test)]` attribute line (near line 425) through the matching closing brace at end of file. Do NOT create a new AOS test file and do NOT modify tests/engines/aos/test_aos.rs — the external file (25 tests, public API) already supersedes the 24 inline tests per the AOS discretion note (D-07 leaves the filename to discretion; the recommendation is to simply delete since coverage already exists). Do NOT promote `AosState.arms` or any private field to `pub`. Do NOT touch any non-test code above the `#[cfg(test)]` line.
  </action>
  <verify>
    <automated>grep -c '#\[cfg(test)\]' src/aos.rs | grep -qx 0 && cargo test --test test_engines 2>&1 | grep -q 'test_aos' && cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#\[cfg(test)\]' src/aos.rs` returns 0
    - `cargo build` exits 0
    - AOS tests still run: `cargo test --test test_engines -- aos` reports the AOS test functions from test_aos.rs and exits 0
    - tests/engines/aos/test_aos.rs is unchanged (no new AOS test file created)
  </acceptance_criteria>
  <done>src/aos.rs has zero `#[cfg(test)]` blocks; AOS coverage is preserved entirely by the pre-existing wired external file.</done>
</task>

<task type="auto">
  <name>Task 2: Append 7 local_search tests to tests/engines/local_search.rs, then delete the inline block</name>
  <files>tests/engines/local_search.rs, src/operations/local_search.rs</files>
  <read_first>
    - src/operations/local_search.rs lines 271-365 (the inline block — 7 tests: test_hill_climbing_returns_improvements_count, test_hill_climbing_unsupported_type, test_hill_climbing_empty_dna, test_local_search_application_strategy_default, test_local_search_mode_default, test_factory_returns_enum, test_factory_with_config; the block uses `use super::*;` plus `use crate::chromosomes::Range as RangeChromosome;`, `use crate::genotypes::Range as RangeGene;`, `use crate::traits::{ChromosomeT, LinearChromosome};`)
    - src/operations.rs lines 29-37 (the public re-exports: `pub use local_search::{factory, factory_with_config, HillClimbingConfig, LocalSearch, LocalSearchApplicationStrategy, LocalSearchMode};` — all 6 items used by the inline tests are public)
    - tests/engines/local_search.rs (current 6 GA-integration tests; confirm the 7 incoming function names do NOT collide with existing `test_local_search_all_offspring` / `_best_n` / `_probabilistic` / `_every_n_generations` / `_baldwinian` / `_not_configured`)
    - .planning/phases/73-move-inline-test-modules/73-PATTERNS.md (section "tests/engines/local_search.rs (existing file — append 7 tests)")
  </read_first>
  <action>
    First, append the 7 inline tests from src/operations/local_search.rs to the end of tests/engines/local_search.rs, preceded by a separator comment line marking them as migrated from src/operations/local_search.rs. Copy the test function bodies verbatim, but resolve every `use super::*;` reference to a fully-qualified `genetic_algorithms::` path: import `factory`, `factory_with_config`, `HillClimbingConfig`, `LocalSearch`, `LocalSearchApplicationStrategy`, `LocalSearchMode` from `genetic_algorithms::operations`; import `GaError` from `genetic_algorithms::GaError`; import `ChromosomeT` and `LinearChromosome` from `genetic_algorithms::traits` (LinearChromosome supplies the `dna`/`set_dna`/`set_gene` flat-slice API and `Cow` usage); import `Range as RangeChromosome` from `genetic_algorithms::chromosomes` and `Range as RangeGene` from `genetic_algorithms::genotypes`; import `std::borrow::Cow`. The local `fn quadratic(dna: &[RangeGene<f64>]) -> f64` helper must be copied too (rename it if it would collide with an existing helper in the file — the existing file uses `sphere_fitness`, so `quadratic` is collision-free and may keep its name). Add any of these imports that are not already present at the top of tests/engines/local_search.rs; do not duplicate imports that already exist there. Then, delete the entire `#[cfg(test)] mod tests { ... }` block (lines 271-365) from src/operations/local_search.rs through end of file. No `mod tests` wrapper in the integration file — all 7 functions are top-level `#[test]` items.
  </action>
  <verify>
    <automated>grep -c '#\[cfg(test)\]' src/operations/local_search.rs | grep -qx 0 && cargo test --test test_engines 2>&1 | grep -q 'test_factory_with_config' && cargo test --test test_engines</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test_hill_climbing_returns_improvements_count' tests/engines/local_search.rs` returns 1
    - `grep -c 'test_factory_with_config' tests/engines/local_search.rs` returns 1
    - `grep -c '#\[cfg(test)\]' src/operations/local_search.rs` returns 0
    - `grep -c 'use super::\*' tests/engines/local_search.rs` returns 0 (no `super` reference in an integration test)
    - `cargo test --test test_engines` exits 0 and the 7 migrated test names appear in `cargo test --test test_engines -- --list`
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>tests/engines/local_search.rs contains the original 6 integration tests plus the 7 migrated unit tests (13 total), all passing; src/operations/local_search.rs has zero `#[cfg(test)]` blocks; no coverage lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan only relocates and deletes test code. No new attack surface, no input handling, no security control is added, removed, or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-02 | Tampering | Test coverage integrity | mitigate | The 7 local_search tests are appended (verified present by grep) BEFORE the inline block is deleted; AOS coverage verified present in test_aos.rs before deleting the inline block — prevents silent coverage loss |

No package installs occur in this plan; the supply-chain checkpoint threat (T-73-SC) is not applicable.
</threat_model>

<verification>
- `cargo test --test test_engines` exits 0 with the 7 migrated local_search tests and AOS tests present
- `grep -c '#\[cfg(test)\]' src/aos.rs` and `src/operations/local_search.rs` both return 0
- `cargo clippy --all-targets -- -D warnings` exits 0
</verification>

<success_criteria>
- Zero `#[cfg(test)]` blocks remain in src/aos.rs and src/operations/local_search.rs
- The 7 local_search tests run from the integration file using public-API paths
- No coverage regression for AOS or local_search
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Modified files (no new files created):**
- `tests/engines/local_search.rs` — gains 7 appended `#[test]` functions migrated from src/operations/local_search.rs
- `src/aos.rs` — inline `#[cfg(test)]` block removed (delete-only)
- `src/operations/local_search.rs` — inline `#[cfg(test)]` block removed (after merge)

**No new test files** — AOS coverage stays in the existing wired `tests/engines/aos/test_aos.rs`; local_search tests are merged into the existing wired `tests/engines/local_search.rs` (Claude's discretion per D-08 chose merge over a separate file to keep all local_search tests in one place).
</artifacts>

<output>
Create `.planning/phases/73-move-inline-test-modules/73-02-SUMMARY.md` when done
</output>
