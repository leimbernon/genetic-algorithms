---
phase: 72-audit-ignored-doctests
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - src/engines/cma/engine.rs
  - src/engines/cma/restart.rs
  - src/engines/de/engine.rs
  - src/engines/ga/mod.rs
  - src/engines/gp/engine.rs
  - src/engines/gp/node.rs
  - src/engines/gp/primitives.rs
  - src/engines/ibea/mod.rs
  - src/engines/island/mod.rs
  - src/engines/island/nsga2.rs
  - src/engines/moead/mod.rs
  - src/engines/nsga2/mod.rs
  - src/engines/nsga3/mod.rs
  - src/engines/sms_emoa/mod.rs
  - src/engines/spea2/mod.rs
  - src/observe/observer/metrics_observer.rs
  - src/observe/observer/tracing_observer.rs
  - src/observe/visualization/mod.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "cargo test --doc passes with zero failures and zero ignored tests"
    - "Every ignored doctest in src/engines/ is either fully restored or converted to no_run"
    - "No # ignore annotations remain in any src/ file"
    - "cargo test --doc --all-features also passes with zero ignored"
  artifacts:
    - path: "src/engines/ga/mod.rs"
      provides: "4 doctests converted from ignore"
      contains: "no_run"
    - path: "src/engines/de/engine.rs"
      provides: "DeEngine doctest converted from ignore"
      contains: "no_run"
    - path: "src/engines/cma/engine.rs"
      provides: "CmaEngine doctest converted from ignore"
      contains: "no_run"
  key_links:
    - from: "src/engines/ga/mod.rs"
      to: "Ga struct doc"
      via: "doctest annotation"
      pattern: "no_run|```$"
---

<objective>
Audit and fix all 18 ignored doctests in src/engines/ — the bulk of the 29 ignored doctests — then run the full verification gate to confirm zero ignored doctests remain across the entire src/ tree.

Purpose: Complete the doctest audit by processing all engine module doctests, which represent the largest concentration of `ignore` annotations. Most are full algorithm-run examples that should be `no_run` (compile-only) due to runtime cost.
Output: Zero `ignore` annotations remain in src/engines/; `cargo test --doc` and `cargo test --doc --all-features` both pass with zero failures and zero ignored.
</objective>

<execution_context>
@/Users/luis/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/luis/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-audit-ignored-doctests/72-CONTEXT.md
@.planning/phases/72-audit-ignored-doctests/72-RESEARCH.md
@.planning/phases/72-audit-ignored-doctests/72-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all engine module doctests (18 ignored across 13 files)</name>
  <files>src/engines/cma/engine.rs, src/engines/cma/restart.rs, src/engines/de/engine.rs, src/engines/ga/mod.rs, src/engines/gp/engine.rs, src/engines/gp/node.rs, src/engines/gp/primitives.rs, src/engines/ibea/mod.rs, src/engines/island/mod.rs, src/engines/island/nsga2.rs, src/engines/moead/mod.rs, src/engines/nsga2/mod.rs, src/engines/nsga3/mod.rs, src/engines/sms_emoa/mod.rs, src/engines/spea2/mod.rs</files>
  <read_first>
    - src/engines/ga/mod.rs (lines 60-90, 795-830, 1205-1230, 2430-2460 — 4 ignored doctests)
    - src/engines/de/engine.rs (lines 30-50 — DeEngine doctest at line 34)
    - src/engines/cma/engine.rs (lines 310-340 — CmaEngine doctest at line 318)
    - src/engines/cma/restart.rs (lines 18-35 — RestartStrategy doctest at line 22)
    - src/engines/gp/engine.rs (lines 78-100 — GpGa doctest at line 83)
    - src/engines/gp/node.rs (lines 26-50 — GpNode doctest at line 31)
    - src/engines/gp/primitives.rs (lines 255-280 — eval_with_vars doctest at line 259)
    - src/engines/ibea/mod.rs (lines 62-85 — module-level doctest at line 67)
    - src/engines/island/mod.rs (lines 60-85 — module-level doctest at line 66)
    - src/engines/island/nsga2.rs (lines 10-30 — module-level doctest at line 14)
    - src/engines/moead/mod.rs (lines 64-90 — module-level doctest at line 69)
    - src/engines/nsga2/mod.rs (lines 55-80 — module-level doctest at line 60)
    - src/engines/nsga3/mod.rs (lines 73-100 — module-level doctest at line 78)
    - src/engines/sms_emoa/mod.rs (lines 58-80 — module-level doctest at line 63)
    - src/engines/spea2/mod.rs (lines 59-80 — module-level doctest at line 64)
  </read_first>
  <action>
Process all 18 ignored doctests across 13 engine files. Per D-05, engine module-level examples demonstrating full algorithm runs are `no_run` candidates due to runtime.

**For each file, read the doctest code and apply the appropriate fix:**

**Pattern A — Module-level `//!` examples showing engine configuration (most common):**
These show `DeConfiguration::default()`, `Ga::new()`, etc. with `todo!()` or incomplete initialization. Convert to `rust,no_run` with reason comment:
```
// no_run: [engine] engine example — illustrative API usage, not a runnable benchmark
```

**Pattern B — Struct-level examples showing API shape:**
These show `CmaEngine::new(config, ...)` or `GpGa::new(config, ...)`. Convert to `rust,no_run` with reason.

**Specific files and expected conversions:**

1. **src/engines/ga/mod.rs** — 4 doctests:
   - Line 69: Module-level GA example → `no_run` (full algorithm run)
   - Line 803: `Ga::build()` example → evaluate; if just API scaffolding, `no_run`
   - Line 1214: `with_local_search` example → `no_run` (full algorithm run)
   - Line 2437: `select_parents_lexicase` example → evaluate; if just API, `no_run`

2. **src/engines/de/engine.rs line 34** — DE engine example with `todo!()` → `no_run`

3. **src/engines/cma/engine.rs line 318** — CMA-ES engine example → `no_run`

4. **src/engines/cma/restart.rs line 22** — RestartStrategy example → `no_run`

5. **src/engines/gp/engine.rs line 83** — GP engine example → `no_run`

6. **src/engines/gp/node.rs line 31** — GpNode trait example → evaluate; if simple, restore fully

7. **src/engines/gp/primitives.rs line 259** — `eval_with_vars` example → evaluate; might restore fully if it's a pure function call

8. **src/engines/ibea/mod.rs line 67** — Module-level IBEA example → `no_run`

9. **src/engines/island/mod.rs line 66** — Module-level Island example → `no_run`

10. **src/engines/island/nsga2.rs line 14** — Module-level Island NSGA2 example → `no_run`

11. **src/engines/moead/mod.rs line 69** — Module-level MOEA/D example → `no_run`

12. **src/engines/nsga2/mod.rs line 60** — Module-level NSGA2 example → `no_run`

13. **src/engines/nsga3/mod.rs line 78** — Module-level NSGA3 example → `no_run`

14. **src/engines/sms_emoa/mod.rs line 63** — Module-level SMS-EMOA example → `no_run`

15. **src/engines/spea2/mod.rs line 64** — Module-level SPEA2 example → `no_run`

**Conversion format:**
- Change ````ignore` to ````rust,no_run`
- Change ````rust,ignore` to ````rust,no_run`
- Add `// no_run: [reason]` as the first line inside the code block
- If a doctest is a pure function call with no side effects and compiles cleanly, consider full restoration (remove annotation entirely)

**IMPORTANT:** After converting each file, run `cargo test --doc 2>&1 | grep "<filename>"` to verify the doctest compiles (not FAILED). If it fails to compile, fix the compilation error before moving to the next file.
  </action>
  <verify>
    <automated>cargo test --doc 2>&1 | grep -c "ignored" && cargo test --doc 2>&1 | grep -c "FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '```ignore\|```rust,ignore' src/engines/` returns zero matches
    - `cargo test --doc 2>&1 | grep "test result"` shows `0 ignored` and `0 failed`
    - Every engine doctest either runs fully or is `no_run` with a reason comment
    - No compilation errors from any doctest conversion
  </acceptance_criteria>
  <done>All 18 engine doctests are converted. Zero `ignore` annotations remain in src/engines/. cargo test --doc passes with zero failures and zero ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification gate — full CI matrix for doctests</name>
  <files></files>
  <read_first>
    - src/observe/observer/metrics_observer.rs (lines 20-35 — feature-gated doctest at line 25)
    - src/observe/observer/tracing_observer.rs (lines 20-35 — feature-gated doctest at line 25)
    - src/observe/visualization/mod.rs (lines 15-30 — feature-gated doctest at line 20)
  </read_first>
  <action>
**Step 1: Run default-features doctest verification.**
```bash
cargo test --doc 2>&1
```
Confirm: zero failures, zero ignored. Record the test result line.

**Step 2: Run all-features doctest verification.**
```bash
cargo test --doc --all-features 2>&1
```
This catches feature-gated ignored doctests in:
- src/observe/observer/metrics_observer.rs (line 25) — observer-metrics feature
- src/observe/observer/tracing_observer.rs (line 25) — observer-tracing feature
- src/observe/visualization/mod.rs (line 20) — visualization feature

If any of these show as ignored, fix them per D-01/D-02:
- Read the doctest, evaluate if it can run
- Convert to `no_run` with reason comment if it has external deps or long runtime
- Fully restore if it's simple API usage

**Step 3: Grep verification.**
```bash
grep -rn '```ignore\|```rust,ignore' src/
```
This MUST return zero matches across all of src/.

**Step 4: Run full test suite to ensure no regressions.**
```bash
cargo test
cargo clippy --all-targets -- -D warnings
cargo doc --no-deps
```
All must pass with zero warnings.

**Step 5: WASM check.**
```bash
cargo check --target wasm32-unknown-unknown
```
Must pass (doctest changes don't affect compilation, but verify).
  </action>
  <verify>
    <automated>bash -c 'cargo test --doc 2>&1 | tail -1 | grep -q "0 ignored" && cargo test --doc 2>&1 | tail -1 | grep -q "0 failed" && grep -rn "```ignore\|```rust,ignore" src/ | wc -l | grep -q "^0$"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --doc` exits 0 with "0 ignored" and "0 failed" in test result line
    - `cargo test --doc --all-features` exits 0 with "0 ignored" and "0 failed" in test result line
    - `grep -rn '```ignore\|```rust,ignore' src/` returns zero matches (empty output)
    - `cargo test` passes all tests
    - `cargo clippy --all-targets -- -D warnings` passes with zero warnings
    - `cargo doc --no-deps` passes with zero warnings
    - `cargo check --target wasm32-unknown-unknown` passes
  </acceptance_criteria>
  <done>The entire src/ tree has zero ignored doctests. All doctests compile and pass. Full CI matrix green. Phase 72 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase modifies only doc comments — no code logic changes, no trust boundary crossings |

## STRIDE Threat Register

No threats — documentation-only phase. `security_enforcement: false`.
</threat_model>

<verification>
Phase-level verification:
1. `cargo test --doc` — zero failures, zero ignored
2. `cargo test --doc --all-features` — zero failures, zero ignored
3. `grep -rn '```ignore\|```rust,ignore' src/` — zero matches
4. `cargo test` — all tests pass
5. `cargo clippy --all-targets -- -D warnings` — zero warnings
6. `cargo doc --no-deps` — zero warnings
7. `cargo check --target wasm32-unknown-unknown` — passes
</verification>

<success_criteria>
- Zero `#[ignore]` or `# ignore` annotations remain anywhere in src/
- `cargo test --doc` passes with zero failures and zero ignored
- `cargo test --doc --all-features` passes with zero failures and zero ignored
- All existing tests continue to pass
- clippy, rustdoc, and WASM checks all green
</success_criteria>

<artifacts_this_phase_produces>
This phase produces no new symbols — it modifies only doc-comment annotations in existing files. Changed items:
- Doc-comment annotation changes in src/engines/cma/engine.rs
- Doc-comment annotation changes in src/engines/cma/restart.rs
- Doc-comment annotation changes in src/engines/de/engine.rs
- Doc-comment annotation changes in src/engines/ga/mod.rs (4 doctests)
- Doc-comment annotation changes in src/engines/gp/engine.rs
- Doc-comment annotation changes in src/engines/gp/node.rs
- Doc-comment annotation changes in src/engines/gp/primitives.rs
- Doc-comment annotation changes in src/engines/ibea/mod.rs
- Doc-comment annotation changes in src/engines/island/mod.rs
- Doc-comment annotation changes in src/engines/island/nsga2.rs
- Doc-comment annotation changes in src/engines/moead/mod.rs
- Doc-comment annotation changes in src/engines/nsga2/mod.rs
- Doc-comment annotation changes in src/engines/nsga3/mod.rs
- Doc-comment annotation changes in src/engines/sms_emoa/mod.rs
- Doc-comment annotation changes in src/engines/spea2/mod.rs
- Possible doc-comment changes in src/observe/observer/metrics_observer.rs (feature-gated)
- Possible doc-comment changes in src/observe/observer/tracing_observer.rs (feature-gated)
- Possible doc-comment changes in src/observe/visualization/mod.rs (feature-gated)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/72-audit-ignored-doctests/72-02-SUMMARY.md` when done
</output>
