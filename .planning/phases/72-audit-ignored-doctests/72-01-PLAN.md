---
phase: 72-audit-ignored-doctests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/operations.rs
  - src/lib.rs
  - src/rng.rs
  - src/traits/configuration.rs
  - src/traits/operator_compat.rs
  - src/initializers/unique_initializer.rs
  - src/fitness/batch.rs
  - src/fitness/cache.rs
  - src/fitness/surrogate.rs
  - src/observe/observer/log.rs
  - src/observe/observer/composite.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "cargo test --doc passes with zero failures for all non-engine modules"
    - "Every ignored doctest in non-engine src/ files is either fully restored or converted to no_run"
    - "The CreepParams failing doctest compiles and passes"
    - "No # ignore annotations remain in non-engine src/ files"
  artifacts:
    - path: "src/operations.rs"
      provides: "Fixed CreepParams doctest with GaussianParams import"
      contains: "GaussianParams"
    - path: "src/lib.rs"
      provides: "Module-level doctest converted from ignore to no_run or fully restored"
    - path: "src/fitness/batch.rs"
      provides: "BatchFitnessEvaluator doctest converted from ignore"
    - path: "src/observe/observer/log.rs"
      provides: "LogObserver doctests converted from ignore"
  key_links:
    - from: "src/operations.rs"
      to: "CreepParams struct"
      via: "doctest import"
      pattern: "use genetic_algorithms::operations.*GaussianParams"
---

<objective>
Fix the 1 failing doctest (CreepParams missing import) and audit all ignored doctests in non-engine source files (src/lib.rs, src/rng.rs, src/traits/, src/initializers/, src/fitness/, src/observe/).

Purpose: Eliminate all ignored doctests outside src/engines/ — each is either restored to full execution or converted to `no_run` with a reason comment per D-01/D-02.
Output: All non-engine src/ files have zero `# ignore` annotations; `cargo test --doc` shows zero failures for these files.
</objective>

<execution_context>
@/Users/luis/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/luis/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-audit-ignored-doctests/72-CONTEXT.md
@.planning/phases/72-audit-ignored-doctests/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CreepParams failing doctest + audit core module doctests</name>
  <files>src/operations.rs, src/lib.rs, src/rng.rs, src/traits/configuration.rs, src/traits/operator_compat.rs, src/initializers/unique_initializer.rs</files>
  <read_first>
    - src/operations.rs (lines 235-265 — the failing CreepParams doctest at line 244)
    - src/lib.rs (lines 1-60 — module-level doctest at line 15)
    - src/rng.rs (lines 15-35 — module-level doctest at line 18)
    - src/traits/configuration.rs (lines 100-120 — MutationConfig doctest at line 103)
    - src/traits/operator_compat.rs (lines 15-40 — module-level doctest at line 20)
    - src/initializers/unique_initializer.rs (lines 15-45 — doctest at line 20)
  </read_first>
  <action>
**Task 1a: Fix the 1 failing doctest (CreepParams) per D-03.**

In `src/operations.rs` at line 244, the doctest uses `GaussianParams` but does not import it. Fix by adding the missing import:

Change line 247 from:
```
use genetic_algorithms::operations::Mutation;
```
to:
```
use genetic_algorithms::operations::{Mutation, GaussianParams};
```

This is the only compilation fix needed — all other 29 ignored doctests compile successfully when the `ignore` annotation is removed.

**Task 1b: Audit and fix doctests in core modules (per D-01, D-02, D-04).**

For each file below, read the doctest, evaluate whether it can run fully or needs `no_run`, then apply the fix:

1. **src/lib.rs line 15** — Module-level Quick Start example using `Ga::new()` with full configuration. Evaluate: does it compile and run in reasonable time? If it runs a full GA loop, convert to `no_run` with reason comment. If it's just API scaffolding, restore fully.

2. **src/rng.rs line 18** — Module-level doctest. Read the code, evaluate runtime. Apply fix per D-01/D-02.

3. **src/traits/configuration.rs line 103** — `MutationConfig::with_mutation_method` doctest. Read the code, evaluate. Apply fix.

4. **src/traits/operator_compat.rs line 20** — Module-level doctest. Read the code, evaluate. Apply fix.

5. **src/initializers/unique_initializer.rs line 20** — `unique_random_initialization` doctest. Read the code, evaluate. Apply fix.

**Conversion rules per D-01/D-02:**
- If doctest uses `todo!()`, has external deps (GPU, network), or runs a full GA loop with large population/generations → convert to `rust,no_run` with `// no_run: [reason]` comment
- If doctest is simple API usage that compiles and runs fast → remove `ignore` entirely (fully restore)
- For each conversion: change ````ignore` or ````rust,ignore` to ````rust,no_run` (or just ```` ``` ```` for full restore), and add a comment line after the opening fence explaining why if using `no_run`
  </action>
  <verify>
    <automated>cargo test --doc 2>&1 | grep -E "(operations|lib\.rs|rng\.rs|configuration|operator_compat|unique_initializer)" | grep -v "passed"</automated>
  </verify>
  <acceptance_criteria>
    - src/operations.rs line 244 doctest PASSES (not FAILED, not ignored) — `cargo test --doc 2>&1 | grep "CreepParams"` shows `ok`
    - `grep -n '```ignore\|```rust,ignore' src/operations.rs src/lib.rs src/rng.rs src/traits/configuration.rs src/traits/operator_compat.rs src/initializers/unique_initializer.rs` returns zero matches
    - `cargo test --doc 2>&1 | grep -c "ignored"` returns a number less than 29 (reduced by the count fixed in this task)
    - Each modified file either has no `ignore` annotation or has `no_run` with a reason comment
  </acceptance_criteria>
  <done>All 6 non-engine files have zero `ignore` annotations. CreepParams doctest passes. Each doctest is either fully runnable or marked `no_run` with reason.</done>
</task>

<task type="auto">
  <name>Task 2: Audit fitness and observe module doctests</name>
  <files>src/fitness/batch.rs, src/fitness/cache.rs, src/fitness/surrogate.rs, src/observe/observer/log.rs, src/observe/observer/composite.rs</files>
  <read_first>
    - src/fitness/batch.rs (lines 34-50 — BatchFitnessEvaluator doctest at line 38)
    - src/fitness/cache.rs (lines 145-170 — wrap_with_cache doctest at line 149)
    - src/fitness/surrogate.rs (lines 59-80 — SurrogateModel doctest at line 63)
    - src/observe/observer/log.rs (lines 1-55 — module-level doctest at line 9, struct-level at line 40)
    - src/observe/observer/composite.rs (lines 8-25 — module-level doctest at line 11)
  </read_first>
  <action>
Audit and fix each ignored doctest per D-01/D-02/D-04:

1. **src/fitness/batch.rs line 38** — `BatchFitnessEvaluator` trait doctest showing a stub implementation. The doctest defines a struct implementing the trait. Evaluate: does it compile? Does it need external resources? Likely `no_run` candidate (stub implementation).

2. **src/fitness/cache.rs line 149** — `wrap_with_cache` doctest. Read the full doctest code. Evaluate: does it run a full GA loop? Likely `no_run` candidate.

3. **src/fitness/surrogate.rs line 63** — `SurrogateModel` trait doctest. Read the full doctest code. Evaluate: does it run a full GA loop? Likely `no_run` candidate.

4. **src/observe/observer/log.rs line 9** — Module-level doctest showing `LogObserver` usage with `Ga::new()`. This doctest uses `Ga::new()` without full configuration — it won't compile as-is. Convert to `no_run` with reason comment explaining it's an API illustration.

5. **src/observe/observer/log.rs line 40** — Struct-level doctest showing `ga.with_observer(Arc::new(LogObserver))`. This references an undefined `ga` variable. Convert to `no_run` with reason comment.

6. **src/observe/observer/composite.rs line 11** — Module-level doctest. Read the code, evaluate. Apply fix per D-01/D-02.

**For each file:**
- Remove the `ignore` or `rust,ignore` annotation
- If the doctest compiles and runs in reasonable time → fully restore (no annotation)
- If the doctest needs external resources, has long runtime, or uses stubs → convert to `rust,no_run` with `// no_run: [reason]` comment
- If the doctest has compilation issues (undefined variables, missing imports) that are intentional API illustrations → keep as `no_run` with reason explaining it's illustrative
  </action>
  <verify>
    <automated>cargo test --doc 2>&1 | grep -E "(batch|cache|surrogate|log\.rs|composite)" | grep -v "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '```ignore\|```rust,ignore' src/fitness/batch.rs src/fitness/cache.rs src/fitness/surrogate.rs src/observe/observer/log.rs src/observe/observer/composite.rs` returns zero matches
    - `cargo test --doc 2>&1 | grep -E "(batch|cache|surrogate|log|composite)" | grep -c "ignored"` returns 0
    - Each modified file has either no doctest annotation (fully restored) or `no_run` with a reason comment
  </acceptance_criteria>
  <done>All 5 fitness/observe files have zero `ignore` annotations. Each doctest is either fully runnable or marked `no_run` with reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase modifies only doc comments — no code logic changes, no trust boundary crossings |

## STRIDE Threat Register

No threats — documentation-only phase. `security_enforcement: false`.
</threat_model>

<verification>
Run `cargo test --doc` and confirm:
1. Zero failures (the CreepParams fix must pass)
2. Reduced ignored count (all non-engine `ignore` annotations removed)
3. `grep -rn '```ignore\|```rust,ignore' src/` shows no matches in non-engine files
</verification>

<success_criteria>
- CreepParams doctest at src/operations.rs:244 passes (not FAILED)
- Zero `ignore` annotations remain in: src/operations.rs, src/lib.rs, src/rng.rs, src/traits/, src/initializers/, src/fitness/, src/observe/
- `cargo test --doc` shows zero failures
- All converted doctests either run fully or are `no_run` with reason comments
</success_criteria>

<artifacts_this_phase_produces>
This phase produces no new symbols — it modifies only doc-comment annotations in existing files. Changed items:
- Doc-comment annotation changes in src/operations.rs (CreepParams example)
- Doc-comment annotation changes in src/lib.rs (module-level example)
- Doc-comment annotation changes in src/rng.rs (module-level example)
- Doc-comment annotation changes in src/traits/configuration.rs (MutationConfig example)
- Doc-comment annotation changes in src/traits/operator_compat.rs (module-level example)
- Doc-comment annotation changes in src/initializers/unique_initializer.rs (initializer example)
- Doc-comment annotation changes in src/fitness/batch.rs (BatchFitnessEvaluator example)
- Doc-comment annotation changes in src/fitness/cache.rs (wrap_with_cache example)
- Doc-comment annotation changes in src/fitness/surrogate.rs (SurrogateModel example)
- Doc-comment annotation changes in src/observe/observer/log.rs (module-level + struct-level examples)
- Doc-comment annotation changes in src/observe/observer/composite.rs (module-level example)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/72-audit-ignored-doctests/72-01-SUMMARY.md` when done
</output>
