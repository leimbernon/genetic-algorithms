---
phase: 64-test-doc-quality
plan: 03
type: execute
wave: 2
depends_on: [64-01, 64-02]
files_modified:
  - tests/operations/
  - tests/engines/
  - tests/test_operations.rs
  - tests/test_engines.rs
autonomous: true
requirements: [D-05, D-14]
tags: [coverage, tests, integration]

must_haves:
  truths:
    - "After this plan, `cargo llvm-cov --all-features --ignore-filename-regex '<regex from Plan 1>' --fail-under-lines 80` exits 0 (≥80% line coverage for src/engines/ and src/operations/)"
    - "All new test files live under `tests/` per D-14 — zero `#[cfg(test)] mod tests` added to `src/`"
    - "Tests reuse `tests/structures.rs` helpers and use `.with_seed(42)` for determinism"
    - "Any transitional `#[allow(dead_code)]` suppressions left by Plan 2 on engine impl blocks are CLOSED here — their underlying methods are now exercised by integration tests"
    - "Tests follow the `tests/test_*.rs` naming pattern and are registered via mod declarations in `tests/test_operations.rs` or `tests/test_engines.rs` as applicable"
  artifacts:
    - path: "tests/operations/test_mutation_gaussian.rs (if in lowest-5)"
      provides: "Gaussian mutation unit tests"
    - path: "tests/operations/test_mutation_levy_flight.rs (if in lowest-5)"
      provides: "Levy flight mutation unit tests"
    - path: "tests/operations/test_selection_fitness_proportionate.rs (if in lowest-5)"
      provides: "FPS selection unit tests"
  key_links:
    - from: "new test files"
      to: "tests/structures.rs"
      via: "use crate::structures::*"
      pattern: "use.*structures"
    - from: "new test files"
      to: "deterministic RNG"
      via: ".with_seed(42)"
      pattern: "with_seed"
---

<objective>
Close the coverage gap to ≥80% lines on `src/engines/` and `src/operations/` by writing integration tests in `tests/` for the modules identified as lowest-coverage in Plan 1's baseline report.

Purpose: This is the test-writing wave. Per D-05, work is driven by the baseline — NOT by structural guessing. Plan 1's `64-COVERAGE-BASELINE.md` already names the 5 lowest-coverage modules; this plan writes tests for them and re-measures until the gate passes.

Output: New test files in `tests/operations/` and/or `tests/engines/`; `cargo llvm-cov` reports ≥80% on both target subtrees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-test-doc-quality/64-CONTEXT.md
@.planning/phases/64-test-doc-quality/64-RESEARCH.md

# CRITICAL — read first: the baseline drives this plan
@.planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md
@.planning/phases/64-test-doc-quality/64-01-SUMMARY.md
@.planning/phases/64-test-doc-quality/64-02-SUMMARY.md

# Test-writing patterns to follow
@tests/structures.rs
@tests/test_operations.rs
@tests/test_engines.rs
</context>

<interfaces>
<!-- Read the baseline file before starting; the lowest-5 list there governs which test files to create. -->
<!-- RESEARCH.md candidates (subject to baseline confirmation): -->
<!--   - src/operations/mutation/gaussian.rs -->
<!--   - src/operations/mutation/levy_flight.rs -->
<!--   - src/operations/mutation/value.rs -->
<!--   - src/operations/selection/fitness_proportionate.rs -->
<!--   - src/engines/{sms_emoa,ibea,moead,spea2}/mod.rs methods marked dead by Plan 2 transitional list -->

<!-- Pattern from tests/structures.rs (use crate::structures::* in new test files): -->
<!--   - BinaryChromosome — basic test chromosome -->
<!--   - RangeChromosome<i32> — range-based test chromosome -->
<!--   - All standard helpers, plus seed_rng helpers -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Read Plan 1 baseline, identify exact target modules, and write coverage tests for operations gaps</name>
  <files>
    tests/operations/,
    tests/test_operations.rs
  </files>
  <read_first>
    - .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md (THE INPUT — lowest 5 modules list)
    - .planning/phases/64-test-doc-quality/64-01-SUMMARY.md (lowest-5 verbatim + current %)
    - tests/structures.rs (shared helpers)
    - tests/test_operations.rs (mod declaration pattern)
    - For each target module in the lowest-5 list, read its src file fully (e.g., `src/operations/mutation/gaussian.rs`)
  </read_first>
  <action>
    Step 1: Identify which of the lowest-5 modules from `64-COVERAGE-BASELINE.md` live under `src/operations/`. These are this task's targets.

    Step 2: For each target module, create a `tests/operations/test_<module>.rs` file (e.g., `tests/operations/test_mutation_gaussian.rs`). Use `tests/structures.rs` helpers (chromosome types, seeded RNG via `.with_seed(42)`). Test cases must cover:
    - Construction with default parameters
    - The primary public function's happy path with at least 2 distinct inputs and verified outputs (use exact f64 equality with seed 42, OR `assert!((a - b).abs() < 1e-9)` for floating-point math)
    - At least one edge case (empty pop, single-element pop, boundary value, etc.)
    - For mutation operators: verify the chromosome shape is preserved (length, gene type bounds)
    - For selection operators: verify returned indices are valid (`< pop.len()`)

    Step 3: Register each new test file in `tests/test_operations.rs` via `mod test_<module>;` declarations (follow the existing pattern in that file).

    Step 4: Re-run `cargo llvm-cov --all-features --ignore-filename-regex '<regex from Plan 1>' --summary-only` and confirm the `src/operations/` line % is ≥80. If not, identify the next-lowest module from the report and add tests for it. Repeat until the gate passes for `src/operations/`.

    Per D-14, all new tests live under `tests/`. Zero `#[cfg(test)] mod tests` in `src/`.

    Use `--with-seed(42)` (or whatever pattern `tests/structures.rs` uses) for deterministic test results.
  </action>
  <verify>
    <automated>cargo test --all-features --tests 2>&1 | tail -5 | grep -q 'test result: ok' && cargo llvm-cov --all-features --ignore-filename-regex '(tests/|src/traits/|src/types/|src/configuration|src/population|src/error|src/rng|src/stats|src/validators|src/fitness|src/observe|src/niching|src/extension|src/engines/)' --summary-only 2>&1 | tail -10 | grep -E 'TOTAL|src/operations' | head -3</automated>
  </verify>
  <acceptance_criteria>
    - For each `src/operations/` module in Plan 1's lowest-5 list, a corresponding `tests/operations/test_<module>.rs` file exists
    - Each new test file uses `tests/structures.rs` helpers (`use` statement present)
    - Each new test file uses deterministic seeding (`.with_seed(42)` or equivalent)
    - `cargo test --all-features --tests` passes with all new tests green
    - `cargo llvm-cov --all-features --ignore-filename-regex '<Plan 1 regex with engines/ added>' --summary-only` shows `src/operations/` line % ≥ 80
    - Zero `#[cfg(test)] mod tests` blocks added to any file under `src/`
  </acceptance_criteria>
  <done>Operations subtree coverage at ≥80%; new test files committed; mod declarations registered.</done>
</task>

<task type="auto">
  <name>Task 2: Write coverage tests for engines gaps and any transitional dead_code suppressions from Plan 2</name>
  <files>
    tests/engines/,
    tests/test_engines.rs,
    src/engines/sms_emoa/mod.rs,
    src/engines/ibea/mod.rs,
    src/engines/moead/mod.rs,
    src/engines/spea2/mod.rs,
    src/engines/cma/engine.rs,
    src/engines/ga.rs
  </files>
  <read_first>
    - .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md (lowest-5 engine modules)
    - .planning/phases/64-test-doc-quality/64-02-SUMMARY.md (transitional dead_code suppressions list from Plan 2)
    - tests/structures.rs
    - tests/test_engines.rs (mod declaration pattern)
    - For each target engine module, read its src file (e.g., `src/engines/sms_emoa/mod.rs`)
    - Existing engine test patterns: `tests/engines/sms_emoa/`, `tests/engines/ibea/`, etc.
  </read_first>
  <action>
    Step 1: From the lowest-5 list in `64-COVERAGE-BASELINE.md`, identify modules under `src/engines/`. Cross-reference with the transitional dead_code list in `64-02-SUMMARY.md` — the impl blocks left with `#[allow(dead_code)]` by Plan 2 are mandatory test targets here.

    Step 2: For each target:
    - If the engine module has an existing `tests/engines/<engine>/` directory, add a new `tests/engines/<engine>/test_coverage.rs` file (or extend the existing test file) with tests that exercise the previously-uncovered methods.
    - If no test directory exists, create one and register it in `tests/test_engines.rs`.
    - For dead_code-suppressed `impl` blocks, write at least one integration test per impl method that calls the engine's public `run()` (or equivalent) with a configuration that triggers the relevant code path. The coverage of the method should rise to >0% after the test runs.
    - Use `tests/structures.rs` helpers; seed RNG with `.with_seed(42)`; keep `max_generations` low (~10) for fast test execution.

    Step 3: After adding tests, REMOVE the corresponding transitional `#[allow(dead_code)]` lines from the engine files listed in `64-02-SUMMARY.md`. Confirm `cargo clippy --all-features --all-targets -- -D warnings` exits 0 (no dead_code warning surfaces). For `src/engines/ga.rs::batch_evaluate_pop` (Plan 2 left this for Plan 3 per D-06): add a test that wires a `BatchFitnessEvaluator` into `Ga` and confirms `batch_evaluate_pop` is called in `Ga::run`. Then remove its `#[allow(dead_code)]`.

    Step 4: Re-run `cargo llvm-cov --all-features --ignore-filename-regex '<regex from Plan 1>' --summary-only` and confirm `src/engines/` line % ≥ 80. Iterate on additional modules from the baseline if needed.

    Step 5: Run the full CI matrix:
    ```
    cargo test --all-features \
    && cargo test --doc --all-features \
    && cargo clippy --all-features -- -D warnings \
    && cargo doc --no-deps --all-features
    ```
    All four must exit 0.
  </action>
  <verify>
    <automated>cargo test --all-features 2>&1 | tail -5 | grep -q 'test result: ok' && cargo clippy --all-features --all-targets -- -D warnings 2>&1 | tail -3 | grep -qv warning && cargo llvm-cov --all-features --ignore-filename-regex '(tests/|src/traits/|src/types/|src/configuration|src/population|src/error|src/rng|src/stats|src/validators|src/fitness|src/observe|src/niching|src/extension)' --fail-under-lines 80 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - For each `src/engines/` module in Plan 1's lowest-5 list, new tests exist under `tests/engines/<engine>/`
    - Every transitional `#[allow(dead_code)]` from `64-02-SUMMARY.md` is REMOVED in this task
    - `grep -rn '#\[allow(' src/ --include='*.rs' | grep -v 'pso/engine.rs'` returns 0 lines
    - `cargo clippy --all-features --all-targets -- -D warnings` exits 0
    - `cargo llvm-cov --all-features --ignore-filename-regex '<Plan 1 regex>' --fail-under-lines 80` exits 0 (≥80% across engines + operations combined)
    - All four full-suite CI commands exit 0
    - Zero `#[cfg(test)] mod tests` blocks added under `src/`
  </acceptance_criteria>
  <done>Engines subtree at ≥80% coverage; all transitional dead_code allows closed; the global gate from Plan 1's `coverage.yml` workflow passes; full CI matrix green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → engine internals | New tests may exercise previously-untested code paths and surface latent bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-06 | Tampering | New tests use `.with_seed(42)` | mitigate | Determinism via fixed seed; if tests still flake, mark `#[ignore]` and file an issue, do NOT remove the test |
| T-64-07 | Denial of Service | New tests slow `cargo test` | mitigate | Keep `max_generations` low (~10), population sizes small (~20) |
</threat_model>

<verification>
1. `cargo test --all-features` exits 0 with all tests passing
2. `cargo test --features serde` exits 0
3. `cargo test --doc --all-features` exits 0 (this also validates Plan 4's doc examples — Plan 3 and Plan 4 share this gate)
4. `cargo clippy --all-features --all-targets -- -D warnings` exits 0
5. `cargo doc --no-deps --all-features` exits 0 with zero warnings
6. `cargo llvm-cov --all-features --ignore-filename-regex '<Plan 1 regex>' --fail-under-lines 80` exits 0
7. `grep -rn '#\[allow(' src/ --include='*.rs' | grep -v 'pso/engine.rs'` returns 0 lines
</verification>

<success_criteria>
- `src/engines/` ≥ 80% line coverage
- `src/operations/` ≥ 80% line coverage
- Combined gate passes via `coverage.yml` workflow
- Zero non-PSO `#[allow(...)]` suppressions remain
- All new tests in `tests/`, none in `src/`
- Full CI matrix green
</success_criteria>

<output>
Create `.planning/phases/64-test-doc-quality/64-03-SUMMARY.md` with:
- Final per-subtree coverage percentages (engines, operations)
- List of new test files added
- Confirmation that all transitional dead_code allows from `64-02-SUMMARY.md` are closed
- Output of `grep -rn '#\[allow(' src/ --include='*.rs'` (should be 1 line: PSO)
</output>
