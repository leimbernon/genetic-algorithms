---
phase: 64-test-doc-quality
plan: 02
type: execute
wave: 1
depends_on: [64-01]
files_modified:
  - src/engines/de/mutation.rs
  - src/engines/ga.rs
  - src/engines/alps/configuration.rs
  - src/engines/alps/engine.rs
  - src/engines/cellular/configuration.rs
  - src/engines/cellular/engine.rs
  - src/observe/observer/composite.rs
  - src/operations/mutation.rs
  - src/engines/sms_emoa/mod.rs
  - src/engines/ibea/mod.rs
  - src/engines/moead/mod.rs
  - src/engines/spea2/mod.rs
  - src/engines/cma/engine.rs
  - examples/
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10]
tags: [clippy, suppressions, dead_code, deprecated, type_complexity]

must_haves:
  truths:
    - "Running `cargo clippy --all-features --all-targets -- -D warnings` exits 0 across the entire workspace"
    - "Running `grep -rn '#\\[allow(' src/ --include='*.rs'` returns AT MOST 1 hit (the justified PSO needless_range_loop at src/engines/pso/engine.rs:~337)"
    - "All callers of removed deprecated builder methods (`with_mutation_step`, `with_mutation_sigma`) in `src/`, `tests/`, and `examples/` use `Mutation::Gaussian { sigma }` instead"
    - "DeMutationParams struct exists and is used by `src/engines/de/mutation.rs::mutate()` (and `current_to_best` if it also exceeds the Clippy arg-count limit)"
    - "Type aliases `ConstraintFn`, `RepairFn`, `RewardAccumulator` are defined in `src/engines/ga.rs` and replace the original complex closure types"
    - "`CompositeObserver::add` is renamed to `CompositeObserver::register`; all internal call sites updated"
    - "`#[allow(deprecated)]` no longer appears at lines ~246 and ~1501 in `src/engines/ga.rs`"
  artifacts:
    - path: "src/engines/de/mutation.rs"
      provides: "DeMutationParams struct and de-suppressed mutate function"
      contains: "DeMutationParams"
    - path: "src/engines/ga.rs"
      provides: "Type aliases ConstraintFn/RepairFn/RewardAccumulator + dead_code removal + ParentCrossoverParams"
      contains: "type ConstraintFn"
    - path: "src/observe/observer/composite.rs"
      provides: "Renamed `register` method"
      contains: "fn register"
  key_links:
    - from: "src/engines/de/mutation.rs"
      to: "DeMutationParams"
      via: "mutate() signature accepts &DeMutationParams or DeMutationParams"
      pattern: "DeMutationParams"
    - from: "src/engines/ga.rs"
      to: "ConstraintFn / RepairFn type aliases"
      via: "Ga<U> struct field types use the aliases"
      pattern: "ConstraintFn|RepairFn"
---

<objective>
Remove all 22 removable `#[allow(...)]` suppressions in `src/` by fixing their root causes per D-06 through D-10. Keep only the 1 justified PSO `needless_range_loop` suppression with a strengthened comment.

Purpose: Zero suppressions is a Phase 64 success criterion. The fixes also improve API quality: dead `impl` blocks become tested code, deprecated v2.x fields are deleted in this v3.0.0 milestone, complex closure types get readable aliases, and a confusingly-named observer method is renamed.

Output: All targeted files updated; `cargo clippy --all-features --all-targets -- -D warnings` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-test-doc-quality/64-CONTEXT.md
@.planning/phases/64-test-doc-quality/64-RESEARCH.md

# Files containing the suppressions (read each before editing)
@src/engines/de/mutation.rs
@src/engines/ga.rs
@src/engines/alps/configuration.rs
@src/engines/cellular/configuration.rs
@src/observe/observer/composite.rs
@src/operations/mutation.rs

# Engine mod.rs files with dead_code on impl blocks
@src/engines/sms_emoa/mod.rs
@src/engines/ibea/mod.rs
@src/engines/moead/mod.rs
@src/engines/spea2/mod.rs
@src/engines/cma/engine.rs
</context>

<interfaces>
<!-- Per RESEARCH.md §`#[allow(...)]` Suppression Inventory, exact target shapes: -->

DeMutationParams (new — `src/engines/de/mutation.rs`):
  - `pub struct DeMutationParams { strategy: DeMutationStrategy, target_idx: usize, best_idx: usize, f: f64 }`
  - Replaces 4 of the 7 args on `mutate()`; `pop`, `rng`, `archive` remain positional
  - Exact field set may be tuned during implementation; the goal is that `mutate()` falls below Clippy's `too_many_arguments` limit (default 7)

Type aliases (new — `src/engines/ga.rs`):
  - `type ConstraintFn<G> = Arc<dyn Fn(&[G]) -> f64 + Send + Sync>`
  - `type RepairFn<U> = Arc<dyn Fn(&mut U) -> Result<(), GaError> + Send + Sync>`
  - `type RewardAccumulator = Option<Arc<Mutex<Vec<(usize, f64)>>>>`

ParentCrossoverParams (new — `src/engines/ga.rs`):
  - Groups the AOS and fitness parameters from `parent_crossover()` (currently 15 args)
  - Exact field set determined during implementation; goal: signature falls below `too_many_arguments` limit

CompositeObserver rename (in `src/observe/observer/composite.rs`):
  - `pub fn add(self, obs) -> Self` → `pub fn register(self, obs) -> Self`
  - All internal callers in `src/` and `tests/` must be updated
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove deprecated mutation_step/mutation_sigma fields from Alps/Cellular configs and update ALL callers</name>
  <files>
    src/engines/alps/configuration.rs,
    src/engines/alps/engine.rs,
    src/engines/cellular/configuration.rs,
    src/engines/cellular/engine.rs,
    examples/
  </files>
  <read_first>
    - src/engines/alps/configuration.rs (full file — confirm field locations and `#[deprecated]` attrs)
    - src/engines/cellular/configuration.rs (full file)
    - src/engines/alps/engine.rs (doc comment at ~line 66 references with_mutation_sigma)
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§Common Pitfalls Pitfall 3 — caller audit before removal)
    - Output of `grep -rn "with_mutation_step\\|with_mutation_sigma\\|mutation_step\\|mutation_sigma" src/ tests/ examples/`
  </read_first>
  <behavior>
    - After removal, `cargo build --all-features` succeeds with no deprecation warnings emitted from alps/cellular
    - After removal, `cargo test --all-features` continues to pass (callers updated to `Mutation::Gaussian { sigma }`)
    - Test in tests/engines/alps/ verifies that constructing AlpsConfiguration without `with_mutation_step`/`with_mutation_sigma` and instead configuring `Mutation::Gaussian { sigma: ... }` still produces working ALPS runs
    - Test in tests/engines/cellular/ does the same for Cellular
  </behavior>
  <action>
    Per D-08:

    Step 1 — Caller audit: `grep -rn 'with_mutation_step\|with_mutation_sigma\|\.mutation_step\|\.mutation_sigma' src/ tests/ examples/` and list every hit. The migration target is `Mutation::Gaussian { sigma: <value> }` set via the existing mutation configuration mechanism.

    Step 2 — In `src/engines/alps/configuration.rs`:
    - Delete the deprecated fields `mutation_step` and `mutation_sigma` from `AlpsConfiguration`
    - Delete the builder methods `with_mutation_step` and `with_mutation_sigma`
    - Delete the corresponding `#[allow(deprecated)]` lines (at ~64, 127, 134 per RESEARCH.md)
    - Update the `Default` impl to no longer reference these fields

    Step 3 — Same removals in `src/engines/cellular/configuration.rs` (~75, 134, 141).

    Step 4 — Update `src/engines/alps/engine.rs` doc comment at ~line 66 — remove the `.with_mutation_sigma(0.1)` example; replace with the `Mutation::Gaussian { sigma }` form per the standard pattern used elsewhere in the engine docs.

    Step 5 — Update every caller found in Step 1 (including any in `examples/`) to use `Mutation::Gaussian { sigma: ... }` instead.

    Step 6 — Add a regression test in tests/engines/alps/ and tests/engines/cellular/ that constructs the engine with `Mutation::Gaussian` and runs a short generation count to confirm the migration path works.

    This IS a breaking change. It is acceptable because we are on `milestone/v3.0.0` (CONTEXT.md D-08).
  </action>
  <verify>
    <automated>cargo build --all-features 2>&1 | grep -v '^#' | grep -c 'with_mutation_step\|with_mutation_sigma' | grep -q '^0$' && cargo test --all-features --tests 2>&1 | tail -3 | grep -q 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'with_mutation_step\|with_mutation_sigma' src/ examples/` returns 0 hits (only test files documenting the migration in comments are allowed if needed)
    - `grep -n '#\[allow(deprecated)\]' src/engines/alps/configuration.rs src/engines/cellular/configuration.rs` returns 0 hits
    - `cargo build --all-features` produces zero deprecation warnings from alps/cellular configuration modules
    - `cargo test --all-features` passes
  </acceptance_criteria>
  <done>The deprecated fields are gone, all callers migrated, alps/cellular tests still green, and `#[allow(deprecated)]` lines in these two config files are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor src/engines/ga.rs — type aliases (D-09), DeMutationParams analog ParentCrossoverParams (D-07), remove stale allow(deprecated) (D-08), fix unused_mut (D-10)</name>
  <files>src/engines/ga.rs</files>
  <read_first>
    - src/engines/ga.rs (offsets 240-260, 1180-1210, 1490-1530, 2800-2880 — the suppression locations per RESEARCH.md)
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§`#[allow(...)]` Suppression Inventory rows for `ga.rs`, §Common Pitfalls Pitfall 4)
    - tests/engines/test_ga.rs (any test that calls `parent_crossover` or `batch_evaluate_pop` directly, if exposed)
  </read_first>
  <behavior>
    - `cargo clippy --all-features --all-targets -- -D warnings` exits 0 on `src/engines/ga.rs`
    - `cargo clippy --no-default-features -- -D warnings` exits 0 on `src/engines/ga.rs` (the unused_mut fix per Pitfall 4 must work for the non-serde build too)
    - `cargo test --all-features --test test_ga` passes with identical behavior to pre-refactor
    - Public API of `Ga<U>` is unchanged: callers in `tests/engines/test_ga.rs` and `examples/` continue to compile without modification
  </behavior>
  <action>
    Per D-09: Add three type aliases at the top of `src/engines/ga.rs` (after the `use` block):
    - `type ConstraintFn<G> = Arc<dyn Fn(&[G]) -> f64 + Send + Sync>`
    - `type RepairFn<U> = Arc<dyn Fn(&mut U) -> Result<(), GaError> + Send + Sync>`
    - `type RewardAccumulator = Option<Arc<Mutex<Vec<(usize, f64)>>>>`
    Use them in the `Ga<U>` struct fields (`constraint_fns`, `repair_operator`) and in the two local variables in `parent_crossover`.

    Per D-08: At line ~246, change `#[allow(deprecated, clippy::type_complexity)]` to remove BOTH attributes (type_complexity is no longer needed because of the aliases; deprecated is stale per A1). At line ~1501 on `run_with_callback`, remove `#[allow(deprecated)]` entirely (it is stale per A1).

    Per D-07: Introduce `struct ParentCrossoverParams<U>` (or similar; pick a name that compiles) bundling the AOS-related and fitness-related arguments of `parent_crossover` (currently 15 args). Update the call site to construct and pass the struct. The signature must drop below Clippy's `too_many_arguments` limit (default 7). Then remove the `#[allow(clippy::too_many_arguments)]` line at ~2810.

    Per D-06: For `fn batch_evaluate_pop` at ~line 1195, do NOT delete it — Plan 3 will add a test that exercises it. For this task, replace `#[allow(dead_code)]` with `#[cfg_attr(test, allow(dead_code))]` ONLY if a test cannot be added in this plan; otherwise leave the `#[allow(dead_code)]` for now and let Plan 3 remove it after adding the test. Document the choice in the SUMMARY.

    Per D-10 + Pitfall 4: At ~line 1518, the `let mut checkpoint_generation` binding triggers `unused_mut` without the `serde` feature. Restructure so the binding is declared inside the `if self.checkpoint_path.is_some()` block (or whatever block houses the `#[cfg(feature = "serde")]` mutation), so that the `mut` is only present where the mutation lives. Remove `#[allow(unused_mut)]`. Verify with `cargo clippy --no-default-features -- -D warnings`.

    Do NOT change observable runtime behavior of `Ga::run` or any public method.
  </action>
  <verify>
    <automated>grep -c '#\[allow(' src/engines/ga.rs | grep -qE '^(0|1)$' && cargo clippy --all-features --all-targets -- -D warnings 2>&1 | tail -3 | grep -qv 'warning' && cargo test --all-features --test test_ga 2>&1 | tail -3 | grep -q 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '#\[allow(' src/engines/ga.rs` returns at most 1 line (only the dead_code on `batch_evaluate_pop` if Plan 3 is removing it; otherwise 0)
    - `grep -c 'type ConstraintFn' src/engines/ga.rs` returns ≥1
    - `grep -c 'type RepairFn' src/engines/ga.rs` returns ≥1
    - `grep -c 'type RewardAccumulator' src/engines/ga.rs` returns ≥1
    - `grep -c 'ParentCrossoverParams' src/engines/ga.rs` returns ≥2 (struct definition + call site)
    - `cargo clippy --all-features --all-targets -- -D warnings` exits 0
    - `cargo clippy --no-default-features -- -D warnings` exits 0
    - `cargo test --all-features --test test_ga` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All five ga.rs suppressions are addressed at the root cause; type aliases readable; ParentCrossoverParams reduces argument count; non-serde clippy clean; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix remaining suppressions — DE mutation (D-07), CompositeObserver rename (D-10), mutation.rs unused_variables (D-10), engine dead_code impl blocks (D-06)</name>
  <files>
    src/engines/de/mutation.rs,
    src/observe/observer/composite.rs,
    src/operations/mutation.rs,
    src/engines/sms_emoa/mod.rs,
    src/engines/ibea/mod.rs,
    src/engines/moead/mod.rs,
    src/engines/spea2/mod.rs,
    src/engines/cma/engine.rs,
    src/engines/pso/engine.rs
  </files>
  <read_first>
    - src/engines/de/mutation.rs (lines 51, 131 per RESEARCH.md)
    - src/observe/observer/composite.rs (line 62)
    - src/operations/mutation.rs (line 377)
    - src/engines/pso/engine.rs (line 337 — the JUSTIFIED suppression to keep)
    - Each engine mod.rs file at the `#[allow(dead_code)]` line per RESEARCH.md inventory table
    - Output of `grep -rn '\.add(' src/observe/ src/ tests/` to find CompositeObserver callers
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§`#[allow(...)]` Suppression Inventory)
  </read_first>
  <behavior>
    - `DeMutationParams` struct exists in `src/engines/de/mutation.rs` and groups the configuration args of `mutate()`
    - `CompositeObserver::register` replaces `add`; chaining still works in the `tests/observe/` integration tests
    - `factory_with_params` uses `_step`, `_sigma` for ignored params (no warnings, no `#[allow(unused_variables)]`)
    - Each `#[allow(dead_code)]` on engine `impl` blocks is REMOVED; if removal surfaces real dead_code warnings, they are addressed by Plan 3 adding tests (mark in SUMMARY for Plan 3 to pick up). For this task, attempt removal — if the warning is genuine, replace with `#[cfg_attr(not(test), allow(dead_code))]` so the warning is gated to non-test builds (a transitional state Plan 3 can finalize) OR leave the allow with a comment `// TODO(64-03): remove once integration tests cover this impl`.
    - PSO `needless_range_loop` allow is KEPT with a strengthened comment per A2.
  </behavior>
  <action>
    Per D-07 — In `src/engines/de/mutation.rs`:
    - Define `pub(crate) struct DeMutationParams { pub strategy: DeMutationStrategy, pub target_idx: usize, pub best_idx: usize, pub f: f64 }` (exact field set chosen to bring `mutate()` below Clippy's `too_many_arguments` limit).
    - Refactor `pub fn mutate()` (line 51) to take `params: &DeMutationParams` plus the remaining positional args (`pop`, `rng`, `archive`). Update internal call sites.
    - For `fn current_to_best` (line 131): if it also exceeds the limit, reuse `DeMutationParams`; otherwise leave it as-is. Remove `#[allow(clippy::too_many_arguments)]` lines after the refactor.

    Per D-10 — In `src/observe/observer/composite.rs` line 62:
    - Rename `pub fn add(self, obs: ...) -> Self` to `pub fn register(self, obs: ...) -> Self`
    - Update doc comment to describe registration semantics
    - Remove `#[allow(clippy::should_implement_trait)]`
    - Update ALL call sites found via `grep -rn '\\.add(' src/ tests/` that target a `CompositeObserver`. If external user-facing callers exist (per A5/Open Question 3), additionally add a `#[deprecated]` alias `pub fn add` that delegates to `register` to preserve backward compatibility WITHIN v3.0.0 — but only if grep finds external callers. If no external callers exist, the rename is clean.

    Per D-10 — In `src/operations/mutation.rs` line 377:
    - Rename ignored params `step` → `_step` and `sigma` → `_sigma` in `factory_with_params(...)`
    - Remove `#[allow(unused_variables)]`

    Per D-06 — For each `#[allow(dead_code)]` on engine `impl` blocks (`sms_emoa/mod.rs:150`, `ibea/mod.rs:154`, `moead/mod.rs:163`, `spea2/mod.rs:150`, `cma/engine.rs:178` on `CmaState`):
    - Remove the `#[allow(dead_code)]` attribute.
    - Run `cargo build --all-features` and observe whether dead_code warnings actually surface. If they do, this confirms the impl/struct really is not reached by any test. In that case, leave a transitional `#[cfg_attr(not(test), allow(dead_code))]` (or keep the original `#[allow(dead_code)]`) with a comment `// TODO(plan 64-03): integration tests will exercise these methods`. Record the list of still-suppressed locations in the task SUMMARY so Plan 3 can close them.
    - If no warning surfaces (e.g., the test suite already exercises the methods through `run()`), the removal is complete and final.

    PSO needless_range_loop (`src/engines/pso/engine.rs:337`) — KEEP this `#[allow(clippy::needless_range_loop)]`. Replace the existing comment with: `// `i` is required to cross-index state.velocities[i], state.pbest_positions[i], state.pbest_fitness[i] — three independent vectors. Refactoring to enumerate() over a single vec would break this contract.` This single justified suppression is the ONLY one allowed to remain in `src/`.
  </action>
  <verify>
    <automated>SUPP=$(grep -rn '#\[allow(' src/ --include='*.rs' | grep -v 'src/engines/pso/engine.rs' | wc -l | tr -d ' '); echo "non-PSO suppressions: $SUPP"; test "$SUPP" -le 5 && cargo clippy --all-features --all-targets -- -D warnings 2>&1 | tail -3 | grep -qv warning && cargo test --all-features 2>&1 | tail -3 | grep -q 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '#\[allow(' src/ --include='*.rs' | grep -v 'src/engines/pso/engine.rs'` returns AT MOST 5 lines (transitional dead_code on engine impl blocks that Plan 3 will close — documented in SUMMARY). Ideally 0 if Plan 3 closes the dead_code now.
    - PSO suppression at `src/engines/pso/engine.rs` line ~337 STILL exists and has the strengthened comment
    - `grep -c 'DeMutationParams' src/engines/de/mutation.rs` returns ≥2 (struct + usage)
    - `grep -c 'pub fn register' src/observe/observer/composite.rs` returns ≥1
    - `grep -c 'pub fn add' src/observe/observer/composite.rs` returns 0 OR 1 (only the `#[deprecated]` alias if external callers exist)
    - `grep -c 'fn factory_with_params' src/operations/mutation.rs` returns ≥1, with `_step` and `_sigma` parameter names
    - `cargo clippy --all-features --all-targets -- -D warnings` exits 0
    - `cargo test --all-features` exits 0 with all tests passing
    - `cargo test --features serde` exits 0
    - List of any transitional suppressions documented in `64-02-SUMMARY.md` for Plan 3 to close
  </acceptance_criteria>
  <done>All 22 removable suppressions addressed (or transitional and documented for Plan 3); PSO justified allow kept with stronger comment; full CI gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library API → downstream users | `CompositeObserver::add` rename is a breaking change in observer registration |
| Library API → downstream users | Alps/Cellular deprecated field removal is a breaking change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03 | Tampering | CompositeObserver::add rename | mitigate | If `grep -rn '\\.add(' src/ tests/` finds external-facing call sites in `examples/`, add a `#[deprecated]` alias forwarding to `register` |
| T-64-04 | Repudiation | Silent removal of deprecated AlpsConfiguration::mutation_step | accept | v3.0.0 milestone explicitly authorizes breaking changes (CONTEXT.md D-08, project ROADMAP) |
| T-64-05 | Information Disclosure | Type aliases in `ga.rs` could leak internal generics | accept | All aliases are private (not `pub`) — only used internally |
</threat_model>

<verification>
1. `cargo clippy --all-features --all-targets -- -D warnings` exits 0
2. `cargo clippy --no-default-features -- -D warnings` exits 0 (per Pitfall 4)
3. `cargo test --all-features` exits 0
4. `cargo test --features serde` exits 0
5. `cargo doc --no-deps --all-features` exits 0 with zero warnings
6. `grep -rn '#\[allow(' src/ --include='*.rs' | grep -v 'pso/engine.rs' | wc -l` returns ≤ 5 (ideally 0)
</verification>

<success_criteria>
- All 22 removable suppressions addressed at root cause
- PSO justified suppression kept with strengthened comment (the only allow in `src/`)
- All call sites of `with_mutation_step`/`with_mutation_sigma` migrated
- Type aliases readable in `ga.rs`
- DeMutationParams in DE mutation
- CompositeObserver renamed to `register`
- CI passes on all four gate commands
</success_criteria>

<output>
Create `.planning/phases/64-test-doc-quality/64-02-SUMMARY.md` listing:
- Final suppression count in `src/` (target: 1, the PSO justified one)
- Any transitional suppressions that Plan 3 must close (with file:line)
- Whether external `.add(` callers were found and how the rename handled them
</output>
