---
phase: 64-test-doc-quality
plan: 04
type: execute
wave: 2
depends_on: [64-02]
files_modified:
  - src/types/
  - src/operations/
  - src/engines/
autonomous: false
requirements: [D-11, D-12, D-13]
tags: [rustdoc, examples, documentation]

must_haves:
  truths:
    - "Every user-facing public entry point (`pub fn`, `pub struct`, `pub trait`, `pub enum` at module root in `src/`) has a `# Examples` rustdoc section per D-11"
    - "Complex items (engines, configurations, operators requiring full GA setup) use ` ```rust,no_run ` annotation per D-12"
    - "Simple leaf items (gene types, error variants, small utilities) use runnable ` ```rust ` examples per D-13"
    - "Excluded from scope per D-11: trait impl items, re-exported type aliases, enum variants, `pub(crate)` items"
    - "`cargo test --doc --all-features` exits 0 — every doc example compiles"
    - "`cargo doc --no-deps --all-features` exits 0 with zero warnings"
  artifacts:
    - path: "src/types/, src/operations/ (multiple files)"
      provides: "Runnable rustdoc # Examples blocks on simple leaf items (D-13)"
    - path: "src/engines/ (multiple files)"
      provides: "no_run rustdoc # Examples blocks on complex items (D-12)"
  key_links:
    - from: "doc examples"
      to: "public re-export paths from `src/lib.rs`"
      via: "use genetic_algorithms::..."
      pattern: "use genetic_algorithms::"
---

<objective>
Add `# Examples` rustdoc blocks to every in-scope user-facing public item in `src/` per D-11. Use `no_run` for complex items per D-12 and runnable examples for simple leaf items per D-13.

Purpose: Phase 64 success criterion: every `pub` item in `src/` has a rustdoc `# Examples` block that compiles via `cargo test --doc`. This is the third independent quality workstream in Phase 64.

Split rationale: Task 1 was split into 1a (types + operations, simpler runnable examples, ~50 items) and 1b (engines, complex no_run examples, ~50-100 items) to stay within the ~50-file-per-task quality envelope. Each subtree commits independently for review.

Output: Doc comments updated across `src/`; `cargo test --doc --all-features` and `cargo doc --no-deps --all-features` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-test-doc-quality/64-CONTEXT.md
@.planning/phases/64-test-doc-quality/64-RESEARCH.md
@.planning/phases/64-test-doc-quality/64-02-SUMMARY.md

# Public API surface to scan
@src/lib.rs

# Reference patterns: phases 46-02, 46-03, 46-06 added rustdoc to many engines.
# Read existing rustdoc on src/engines/ga.rs and src/engines/nsga2/mod.rs for the established `# Examples` style.
@src/engines/ga.rs
@src/engines/nsga2/mod.rs
</context>

<interfaces>
<!-- Per RESEARCH.md §Pattern 3 (no_run examples) and §Pitfall 5 (use statements required): -->

<!-- no_run example template (D-12) — for complex items requiring full GA setup: -->
<!--   /// # Examples -->
<!--   /// -->
<!--   /// ```rust,no_run -->
<!--   /// use genetic_algorithms::engines::ga::Ga; -->
<!--   /// use genetic_algorithms::configuration::ProblemSolving; -->
<!--   /// // ... realistic usage, no need to actually run -->
<!--   /// ``` -->

<!-- Runnable example template (D-13) — for simple leaf items: -->
<!--   /// # Examples -->
<!--   /// -->
<!--   /// ```rust -->
<!--   /// use genetic_algorithms::types::genotypes::Binary; -->
<!--   /// let g = Binary::default(); -->
<!--   /// assert_eq!(g.id(), 0); -->
<!--   /// ``` -->

<!-- Scope inclusion (D-11): pub fn, pub struct, pub trait, pub enum AT MODULE ROOT in src/ that users call directly -->
<!-- Scope exclusion: trait impl items, internal re-exports, enum variants, type aliases, pub(crate) items -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1a: Inventory user-facing pub items, then add runnable # Examples to src/types/ and src/operations/ (D-13, ~50 items)</name>
  <files>
    src/types/,
    src/operations/,
    .planning/phases/64-test-doc-quality/64-DOC-INVENTORY.md
  </files>
  <read_first>
    - src/lib.rs (full file — defines the public API surface via re-exports)
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§Pattern 3, §Pitfall 5)
    - .planning/phases/64-test-doc-quality/64-CONTEXT.md (D-11, D-12, D-13)
    - Output of `grep -rn '^pub fn\|^pub struct\|^pub trait\|^pub enum' src/types/ src/operations/ --include='*.rs' | grep -v 'pub(crate)' | wc -l` to size the subtree inventory
  </read_first>
  <action>
    Step 1 — Build the FULL inventory (covers Task 1a AND Task 1b):
    Run `grep -rn '^pub fn\|^pub struct\|^pub trait\|^pub enum' src/ --include='*.rs'` and filter out:
    - Lines containing `pub(crate)` (excluded per D-11)
    - Items inside `impl` blocks (these are trait impl items per D-11 exclusion — only MODULE-ROOT `pub fn` count)
    - Type aliases (`pub type ...`) — excluded per D-11
    - Enum variants — excluded per D-11 (only the `pub enum` itself counts, not its variants)

    Cross-reference each remaining hit against `src/lib.rs` to confirm it is actually re-exported (user-facing). Items NOT re-exported are still "user-facing" if their containing module is `pub mod` and reachable through a re-exported parent.

    Save the inventory to `.planning/phases/64-test-doc-quality/64-DOC-INVENTORY.md` with columns: `file:line | item | subtree (types/operations/engines) | classification (complex|simple) | needs_examples (yes|already_has) | task (1a|1b)`. Items in `src/types/` and `src/operations/` are assigned to Task 1a; items in `src/engines/` are assigned to Task 1b.

    Step 2 — Classify and document items in `src/types/` and `src/operations/` (Task 1a scope):
    - Per D-13, these are predominantly **simple leaf items** (gene types like `Binary`, `Range<T>`; small utility structs/enums; operator factory functions with self-contained inputs). Default to runnable ` ```rust ` examples.
    - Exception: operator enums (`Selection`, `Crossover`, `Mutation`, `Survivor`, `Extension`) re-exported from `src/operations/` are **complex** (require engine setup) — these stay in 1a but use `no_run` per D-12. Flag them in the inventory.

    Step 3 — Add `# Examples` blocks for every Task 1a inventory item marked `needs_examples: yes`:
    - Place AFTER existing `# Errors`/`# Panics`/argument descriptions
    - Use complete `use` statements via crate re-export paths (`use genetic_algorithms::types::genotypes::Binary;`) per Pitfall 5
    - Runnable examples must include at least one `assert!` / `assert_eq!` to be self-validating
    - Keep runnable examples ≤ 10 lines
    - For items that ALREADY have a `# Examples` block (leftover from phase 46), VERIFY it still compiles under `cargo test --doc --all-features`

    Step 4 — Iteratively run `cargo test --doc --all-features` while editing; fix compilation errors immediately (most common per Pitfall 5: missing `use` statements, incorrect public paths).

    Step 5 — Confirm `cargo doc --no-deps --all-features 2>&1 | grep -i warning` returns 0 lines for the touched subtrees.

    Commit per-subtree (`src/types/` then `src/operations/`) for review clarity.
  </action>
  <verify>
    <automated>test -f .planning/phases/64-test-doc-quality/64-DOC-INVENTORY.md && cargo test --doc --all-features 2>&1 | tail -5 | grep -q 'test result: ok' && cargo doc --no-deps --all-features 2>&1 | grep -i warning | wc -l | grep -q '^0$' && cargo check --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/64-test-doc-quality/64-DOC-INVENTORY.md` exists with FULL per-item classification table (covers types/operations/engines)
    - For every Task-1a-assigned item marked `needs_examples: yes`, the corresponding rustdoc block in `src/types/` or `src/operations/` now contains a `# Examples` section
    - Simple items use ` ```rust ` per D-13 and include at least one `assert!` or `assert_eq!`
    - Complex operator-enum items in `src/operations/` use ` ```rust,no_run ` per D-12
    - Every doc example uses `use genetic_algorithms::...` paths (crate-relative), not internal `crate::` paths
    - `cargo test --doc --all-features` exits 0 with all doc tests passing
    - `cargo doc --no-deps --all-features 2>&1 | grep -i warning` returns 0 lines
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - No `# Examples` block was added to a trait impl item, enum variant, type alias, or `pub(crate)` item
  </acceptance_criteria>
  <done>Inventory file exists and is complete; every Task-1a-scoped item in `src/types/` and `src/operations/` has a `# Examples` block; doc tests and rustdoc pass cleanly; wasm target clean.</done>
</task>

<task type="auto">
  <name>Task 1b: Add no_run # Examples to src/engines/ items (D-12, ~50-100 complex items)</name>
  <files>src/engines/</files>
  <read_first>
    - .planning/phases/64-test-doc-quality/64-DOC-INVENTORY.md (built in Task 1a — Task 1b scope is the engines subset)
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§Pattern 3, §Pitfall 5)
    - .planning/phases/64-test-doc-quality/64-CONTEXT.md (D-12)
    - src/engines/ga.rs (reference for established # Examples style from phases 46-02 / 46-06)
    - src/engines/nsga2/mod.rs (same — reference style)
  </read_first>
  <action>
    Step 1 — Load Task-1b-assigned items from `64-DOC-INVENTORY.md` (all items under `src/engines/`). These are by definition **complex** per D-12: they require full GA setup (engine construction, configuration builders, problem definition).

    Step 2 — For each item marked `needs_examples: yes`, add a `# Examples` rustdoc section using ` ```rust,no_run ` per D-12:
    - Place AFTER existing `# Errors`/`# Panics`/argument descriptions
    - Use complete `use` statements via the crate's re-export paths (`use genetic_algorithms::engines::ga::Ga;` etc.) per Pitfall 5
    - Show realistic usage that compiles — `no_run` means it does not need to execute, but it MUST compile under `cargo test --doc`
    - Reference the style established by `src/engines/ga.rs` and `src/engines/nsga2/mod.rs` (touched by phase 46)
    - For items that ALREADY have a `# Examples` block (leftover from phase 46), VERIFY it still compiles; update if not. Confirm `CompositeObserver` examples reflect the `register` rename from Plan 2 (not `add`).

    Step 3 — Work subtree-by-subtree within `src/engines/` (commit per engine for review clarity): `ga.rs`, `island/`, `nsga2/`, `nsga3/`, `moead/`, `spea2/`, `sms_emoa/`, `ibea/`, `de/`, `scatter_search/`, `cellular/`, `alps/`, `gp/`, `pso/`, `cma/`. Iteratively run `cargo test --doc --all-features` after each subtree; fix compilation errors immediately.

    Step 4 — Confirm `cargo doc --no-deps --all-features 2>&1 | grep -i warning` returns 0 lines for `src/engines/`.
  </action>
  <verify>
    <automated>cargo test --doc --all-features 2>&1 | tail -5 | grep -q 'test result: ok' && cargo doc --no-deps --all-features 2>&1 | grep -i warning | wc -l | grep -q '^0$' && cargo check --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - For every Task-1b-assigned item marked `needs_examples: yes`, the corresponding rustdoc block in `src/engines/` now contains a `# Examples` section
    - All examples in `src/engines/` use ` ```rust,no_run ` per D-12
    - All examples use `use genetic_algorithms::...` paths (crate-relative), not internal `crate::` paths
    - `CompositeObserver` example (if any) uses the new `register` method name from Plan 2
    - `cargo test --doc --all-features` exits 0 with all doc tests passing
    - `cargo doc --no-deps --all-features 2>&1 | grep -i warning` returns 0 lines
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - No `# Examples` block was added to a trait impl item, enum variant, type alias, or `pub(crate)` item
  </acceptance_criteria>
  <done>Every Task-1b-scoped item in `src/engines/` has a `# Examples` block with `no_run` annotation; doc tests pass; rustdoc has zero warnings; wasm target clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of doc example quality (spot-check sample)</name>
  <what-built>~100-150 `# Examples` blocks added across `src/` via Task 1a (types + operations, runnable per D-13) and Task 1b (engines, no_run per D-12). All compile under `cargo test --doc --all-features`. `cargo doc --no-deps --all-features` produces zero warnings. Inventory at `64-DOC-INVENTORY.md`.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/64-test-doc-quality/64-DOC-INVENTORY.md` and confirm the table covers the four item categories from D-11 and assigns each to Task 1a or Task 1b.
    2. Spot-check 5 random items from the inventory:
       - `src/engines/ga.rs::Ga` (1b — complex — should use `no_run`)
       - `src/types/genotypes/` Binary or Range type (1a — simple — should be runnable with `assert!`)
       - One operator enum (e.g., `Mutation` in `src/operations/` — should use `no_run`)
       - `src/observe/observer/composite.rs::CompositeObserver` (verify it uses the new `register` name from Plan 2, not `add`)
       - One trait (e.g., `ChromosomeT` or `LinearChromosome` — should use `no_run` showing impl)
    3. For each spot-check, read the `# Examples` block and confirm:
       - It uses the crate's re-export path (`use genetic_algorithms::...`)
       - The code is realistic (a user could plausibly copy/paste it)
       - For runnable examples: there is at least one `assert!` line
    4. Run locally: `cargo test --doc --all-features 2>&1 | tail` — confirm all pass
    5. Run locally: `cargo doc --no-deps --all-features --open` — visually inspect a few rendered pages
  </how-to-verify>
  <resume-signal>Type "approved" if spot-checks pass, or describe items that need revision (file:line + issue).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc examples → users copying them | Users may paste runnable examples into their code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-08 | Tampering | `no_run` examples that compile but produce wrong behavior | mitigate | Use `no_run` ONLY for examples that require runtime setup; otherwise prefer runnable so the test harness validates behavior |
| T-64-09 | Information Disclosure | Examples reference internal-only paths | mitigate | Pitfall 5 enforcement: every example must use `use genetic_algorithms::...` re-export paths |
</threat_model>

<verification>
1. `cargo test --doc --all-features` exits 0
2. `cargo doc --no-deps --all-features` exits 0 with zero warnings
3. `cargo test --all-features` still exits 0 (doc changes do not break unit tests)
4. `cargo clippy --all-features --all-targets -- -D warnings` exits 0
5. `cargo check --target wasm32-unknown-unknown` exits 0
6. `64-DOC-INVENTORY.md` exists and the table is complete (covers Task 1a + Task 1b assignments)
7. Spot-check approval from human checkpoint
</verification>

<success_criteria>
- Every in-scope user-facing public item in `src/` has a `# Examples` block per D-11
- Complex items use `no_run` per D-12
- Simple items use runnable examples per D-13
- All doc examples compile (`cargo test --doc --all-features`)
- Zero rustdoc warnings (`cargo doc --no-deps --all-features`)
- WASM target compiles cleanly
- Human spot-check approves quality
</success_criteria>

<output>
Create `.planning/phases/64-test-doc-quality/64-04-SUMMARY.md` including:
- Final count of items with `# Examples` blocks (broken down by complex vs simple, Task 1a vs Task 1b)
- Output of `cargo test --doc --all-features 2>&1 | tail -5`
- Output of `cargo doc --no-deps --all-features 2>&1 | grep -ic warning`
- Confirmation from human checkpoint
</output>
</content>
</invoke>