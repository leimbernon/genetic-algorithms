---
phase: 64-test-doc-quality
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .github/workflows/coverage.yml
  - .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
tags: [coverage, ci, llvm-cov, baseline]

must_haves:
  truths:
    - "Developer can run `cargo llvm-cov --all-features --summary-only` locally after `cargo install cargo-llvm-cov`"
    - "A baseline coverage report file exists in the phase directory listing per-module line percentages for src/engines/ and src/operations/"
    - "A new GitHub Actions workflow `coverage.yml` runs `cargo llvm-cov` with `--fail-under-lines 80` scoped to src/engines/ and src/operations/ via `--ignore-filename-regex`"
    - "The 3-5 lowest-coverage modules are documented in the baseline file so Plan 3 (coverage tests) is data-driven, not guesswork"
  artifacts:
    - path: ".github/workflows/coverage.yml"
      provides: "Coverage CI gate"
      contains: "cargo-llvm-cov"
    - path: ".planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md"
      provides: "Baseline per-module coverage percentages and lowest-coverage modules list"
  key_links:
    - from: ".github/workflows/coverage.yml"
      to: "cargo-llvm-cov"
      via: "cargo install cargo-llvm-cov --locked"
      pattern: "cargo install cargo-llvm-cov"
    - from: ".github/workflows/coverage.yml"
      to: "src/engines/ and src/operations/"
      via: "--ignore-filename-regex excluding everything else"
      pattern: "--ignore-filename-regex"
---

<objective>
Establish the coverage measurement infrastructure and capture a baseline report before any test-writing work begins.

Purpose: Per D-05, all subsequent test additions in Plan 3 MUST be driven by actual per-module coverage percentages, not by structural guesswork. This plan installs the tooling, captures the baseline, and gates CI on the ≥80% threshold for the two target subtrees.

Output:
- `64-COVERAGE-BASELINE.md` recording per-module percentages and the lowest-coverage modules
- `.github/workflows/coverage.yml` CI workflow with `--fail-under-lines 80`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/64-test-doc-quality/64-CONTEXT.md
@.planning/phases/64-test-doc-quality/64-RESEARCH.md

# Existing CI workflows to mirror style and triggers
@.github/workflows/rust-unit-tests.yml
@.github/workflows/wasm-check.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cargo-llvm-cov locally and capture baseline coverage report</name>
  <files>.planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md</files>
  <read_first>
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§Architecture Patterns Pattern 1 and Pattern 2, §Code Examples - baseline report commands)
    - .planning/phases/64-test-doc-quality/64-CONTEXT.md (D-01, D-03, D-04, D-05)
    - Cargo.toml (to confirm feature list for --all-features)
  </read_first>
  <action>
    Per D-01, install the coverage tool with `cargo install cargo-llvm-cov --locked` and `rustup component add llvm-tools-preview`. Verify with `cargo llvm-cov --version`.

    Per D-03 and D-05, run two baseline commands:
    1. `cargo llvm-cov --all-features --ignore-filename-regex 'tests/' --summary-only > /tmp/cov-summary.txt 2>&1`
    2. `cargo llvm-cov --all-features --ignore-filename-regex 'tests/' --json --output-path /tmp/cov-baseline.json`

    Create `.planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md` with these sections:
    - `## Tool Versions` — output of `cargo llvm-cov --version` and `rustc --version`
    - `## Per-File Coverage Summary` — paste the `--summary-only` text output verbatim inside a fenced block
    - `## src/engines/ Per-Module Breakdown` — table with columns `Module | Lines | Covered | %` for each file under `src/engines/`
    - `## src/operations/ Per-Module Breakdown` — same table format
    - `## Lowest 5 Modules (combined engines + operations)` — ordered list of the 5 files with the lowest line coverage % among the two target subtrees, with their exact file paths and percentages. This is the input list for Plan 3.
    - `## Exact File Paths Observed` — list the `filename` field values from the JSON baseline for `src/engines/` and `src/operations/` files (used to validate the `--ignore-filename-regex` pattern in Task 2)
    - `## --ignore-filename-regex Validation` — show the regex from Task 2 alongside the file paths it includes/excludes, confirming only `src/engines/**` and `src/operations/**` are included in the 80% gate computation

    Do NOT write any new tests in this task. Plan 3 will use the lowest-5 list.

    Per D-04 / Pitfall 1: do NOT use `#[coverage(off)]` — research confirms E0658 on rustc 1.94.1. WASM-gated branches are excluded by virtue of running tests on a non-WASM host (the cfg-gated code is not compiled), not via any attribute.
  </action>
  <verify>
    <automated>test -f .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md && grep -q "Lowest 5 Modules" .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md && grep -q "src/engines/" .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md && grep -q "src/operations/" .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md` exists
    - File contains a `## Lowest 5 Modules (combined engines + operations)` section with 5 entries
    - File contains at least one row referencing a file path matching `src/engines/`
    - File contains at least one row referencing a file path matching `src/operations/`
    - `cargo llvm-cov --version` exits 0 in the local environment
  </acceptance_criteria>
  <done>The baseline report is captured, the 5 lowest-coverage modules are explicitly named, and the exact file-path format observed in the JSON output is recorded for use in Task 2's regex.</done>
</task>

<task type="auto">
  <name>Task 2: Create coverage.yml GitHub Actions workflow with --fail-under-lines 80 gate</name>
  <files>.github/workflows/coverage.yml</files>
  <read_first>
    - .planning/phases/64-test-doc-quality/64-COVERAGE-BASELINE.md (just created — contains exact file paths observed)
    - .planning/phases/64-test-doc-quality/64-RESEARCH.md (§CI Integration - New Coverage Workflow)
    - .github/workflows/rust-unit-tests.yml (style/trigger conventions)
    - .github/workflows/wasm-check.yml (style/trigger conventions)
  </read_first>
  <action>
    Per D-02, create a new file `.github/workflows/coverage.yml` (do NOT extend `rust-unit-tests.yml` — coverage instrumentation is 2-4x slower and must not slow standard tests).

    Workflow contents:
    - `name: Coverage Gate`
    - Trigger: `on: pull_request: branches: [main, "milestone/**"]` (matches the wasm-check.yml convention)
    - Single job `coverage` on `ubuntu-latest` with `permissions: contents: read`
    - Steps in order:
      1. `actions/checkout@v4`
      2. `dtolnay/rust-toolchain@stable` with `components: llvm-tools-preview`
      3. `Swatinem/rust-cache@v2` with `key: coverage`
      4. `cargo install cargo-llvm-cov --locked`
      5. The coverage gate command (see below)

    The coverage gate command MUST use `--all-features` (D-03) and `--fail-under-lines 80` and an `--ignore-filename-regex` that — based on the exact file paths recorded in 64-COVERAGE-BASELINE.md — excludes every source directory EXCEPT `src/engines/**` and `src/operations/**`, plus excludes `tests/`. Build the regex from the actual paths observed in Task 1's baseline; do NOT copy the example regex from RESEARCH.md verbatim without checking it against the observed paths.

    After the workflow file exists, document the final regex (and the rationale for each excluded directory) at the end of `64-COVERAGE-BASELINE.md` under a new section `## Final CI Regex`.

    Per Pitfall 2: the regex MUST scope to the two target subtrees; otherwise the 80% gate applies to traits/types/configuration/etc.
  </action>
  <verify>
    <automated>test -f .github/workflows/coverage.yml && grep -q "cargo-llvm-cov" .github/workflows/coverage.yml && grep -q "fail-under-lines 80" .github/workflows/coverage.yml && grep -q "all-features" .github/workflows/coverage.yml && grep -q "ignore-filename-regex" .github/workflows/coverage.yml && grep -q "llvm-tools-preview" .github/workflows/coverage.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/coverage.yml` exists and parses as valid YAML
    - Workflow uses `cargo-llvm-cov` with both `--all-features` and `--fail-under-lines 80`
    - Workflow uses `--ignore-filename-regex` to scope the gate to `src/engines/` and `src/operations/`
    - Workflow installs `llvm-tools-preview` via the rust-toolchain action
    - Workflow uses `Swatinem/rust-cache@v2` for cache hits
    - `64-COVERAGE-BASELINE.md` contains a `## Final CI Regex` section documenting the regex used
    - Workflow triggers on `pull_request` to `main` and `milestone/**`
  </acceptance_criteria>
  <done>The CI workflow exists, enforces the 80% gate scoped to engines/operations, and the regex is documented against actual observed file paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → cargo install | Tool installation via cargo registry on each PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | `cargo install cargo-llvm-cov` in CI | mitigate | Use `--locked` flag to honor `Cargo.lock`; pin the installer's checksum behavior to the published crate state at install time |
| T-64-02 | Denial of Service | Coverage workflow slows PR feedback | accept | Workflow is intentionally isolated from `rust-unit-tests.yml` so standard test feedback is not slowed |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | `cargo-llvm-cov` is a cargo dev tool (not a library dependency); not added to `Cargo.toml`; no Package Legitimacy Audit required per RESEARCH.md |
</threat_model>

<verification>
1. `cargo llvm-cov --version` runs locally without error
2. Baseline file `64-COVERAGE-BASELINE.md` lists lowest-5 modules
3. `coverage.yml` workflow exists and parses
4. Running the workflow command body locally exits non-zero if engines/operations coverage is currently <80% (expected — Plan 3 closes this), OR exits zero if already ≥80% (Plan 3 then becomes a no-op confirmation)
</verification>

<success_criteria>
- Baseline report committed
- CI workflow file committed
- Lowest-5 modules list is concrete and ready to feed Plan 3
- Final `--ignore-filename-regex` validated against actual file paths observed in JSON output
</success_criteria>

<output>
Create `.planning/phases/64-test-doc-quality/64-01-SUMMARY.md` when done, including:
- The lowest-5 modules list (verbatim from the baseline file)
- The exact `--ignore-filename-regex` used in `coverage.yml`
- Current overall % for `src/engines/` and `src/operations/` subtrees
</output>
