---
phase: 57-pso-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/traits/real_gene.rs
  - tests/engines/pso/test_pso.rs
autonomous: true
requirements:
  - PSO-BOUNDS (D-02, D-07, D-08 enabler — `RealGene::bounds()` non-breaking extension)
  - PSO-TESTS-WAVE0 (Nyquist test scaffold for engine-dependent behaviors)
must_haves:
  truths:
    - "User can call `gene.bounds()` on any `RealGene` and receive `Option<(f64, f64)>`"
    - "`Range<f64>` returns `Some((lo, hi))` from `bounds()` matching the gene's first range"
    - "`MultiRangeGenotype<f64>` returns `Some((lo, hi))` from `bounds()` matching gene's first range"
    - "`tests/engines/pso/test_pso.rs` exists with #[ignore]-gated stubs for all PSO-engine-dependent behaviors enumerated in 57-RESEARCH.md `Phase Requirements to Test Map`"
    - "`cargo test` and `cargo test --features serde` pass — no regressions from the non-breaking trait change"
  artifacts:
    - path: src/traits/real_gene.rs
      provides: "`bounds()` default method on `RealGene` trait + impls for `Range<f64>` and `MultiRangeGenotype<f64>`"
      contains: "fn bounds(&self) -> Option<(f64, f64)>"
    - path: tests/engines/pso/test_pso.rs
      provides: "Nyquist test stubs for the PSO engine (gated with `#[ignore]` markers, un-ignored in Plan 03)"
      contains: "#[ignore"
  key_links:
    - from: src/traits/real_gene.rs
      to: src/types/genotypes/range.rs
      via: "`Range<f64>::bounds()` reads `self.ranges.first().copied()`"
      pattern: "self\\.ranges\\.first\\(\\)\\.copied\\(\\)"
    - from: tests/engines/pso/test_pso.rs
      to: "(future) src/engines/pso/"
      via: "Stub tests reference `genetic_algorithms::pso::*` symbols that Plans 02–03 will provide"
      pattern: "genetic_algorithms::pso"
---

<objective>
Add the non-breaking `bounds()` method to the `RealGene` trait (with default `None`) and implement it for the two existing real-valued gene types (`Range<f64>`, `MultiRangeGenotype<f64>`). Then scaffold the PSO Nyquist test file `tests/engines/pso/test_pso.rs` with `#[ignore]`-gated stubs for every PSO behavior listed in 57-RESEARCH.md `Phase Requirements to Test Map`. This unblocks Plans 02–03 (engine implementation) and Plan 04 (verification gate) by providing the bounds-access API PSO requires (D-02, D-07, D-08) and the test scaffold that downstream plans un-ignore as implementations land.

Purpose: PSO needs per-gene `(lo, hi)` for velocity initialization (D-02), `v_max` derivation (D-08), and absorbing-boundary enforcement (D-07). `RealGene` currently exposes only `real_value()` and `with_real_value()`. A non-breaking trait extension (default-impl `None`) plus per-type overrides is the cleanest solution chosen from RESEARCH Option A.

Output: `RealGene::bounds()` available throughout the crate; PSO test scaffold ready to be progressively un-ignored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-pso-engine/57-CONTEXT.md
@.planning/phases/57-pso-engine/57-RESEARCH.md
@.planning/phases/57-pso-engine/57-PATTERNS.md
@src/traits/real_gene.rs
@src/types/genotypes/range.rs
@src/types/genotypes/multi_range.rs
@tests/engines/cma/test_cma.rs

<interfaces>
Current `RealGene` surface (src/traits/real_gene.rs lines 23-29):

```
pub trait RealGene: GeneT {
    fn real_value(&self) -> f64;
    fn with_real_value(&self, value: f64) -> Self;
}
```

`Range<T>` struct (src/types/genotypes/range.rs lines 44-48):

```
pub struct Range<T> {
    pub id: i32,
    pub ranges: Arc<[(T, T)]>,
    pub value: T,
}
```

`MultiRangeGenotype<T>` stores per-gene ranges in `ranges: Arc<[(T, T)]>` (same layout as `Range<T>` — verify by reading `src/types/genotypes/multi_range.rs`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RealGene with bounds() default method + Range/MultiRange impls</name>
  <files>src/traits/real_gene.rs</files>
  <read_first>
    - src/traits/real_gene.rs (current trait surface)
    - src/types/genotypes/range.rs (Range<T>.ranges field layout)
    - src/types/genotypes/multi_range.rs (MultiRangeGenotype<T>.ranges field layout)
    - .planning/phases/57-pso-engine/57-PATTERNS.md (section `src/traits/real_gene.rs (trait extension)`)
  </read_first>
  <behavior>
    - `RealGene::bounds()` exists as a trait method with default body returning `None`.
    - `Range<f64>::bounds()` returns `Some(self.ranges.first().copied().unwrap())` — i.e. the first `(lo, hi)` pair, or `None` if `ranges` is empty.
    - `MultiRangeGenotype<f64>::bounds()` returns `self.ranges.first().copied()` (same shape).
    - Gene types that do NOT override `bounds()` (none currently other than the two above for `f64`) inherit `None` — verifies non-breaking nature.
    - `cargo test` continues to pass for all existing tests (no behavioral regression).
    - `cargo clippy --all-targets -- -D warnings` passes (no new warnings introduced).
  </behavior>
  <action>
    Add a third method to the `RealGene` trait in src/traits/real_gene.rs:

      `fn bounds(&self) -> Option<(f64, f64)> { None }`

    Include a rustdoc `///` comment explaining: "Returns the `(lo, hi)` bounds for this gene if available. Used by the PSO engine for velocity initialization and boundary enforcement. The default implementation returns `None`; gene types backed by an explicit range table should override this method to expose those bounds."

    Then override `bounds()` in the `impl RealGene for Range<f64>` block (currently spans lines 32–44 of the analog) to return `self.ranges.first().copied()`. Mark the override `#[inline]` to match the style of the surrounding methods.

    Add the same `bounds()` override to the `impl RealGene for MultiRangeGenotype<f64>` block (currently spans lines 47–58 of the analog) — body identical to the `Range<f64>` override (`self.ranges.first().copied()`), `#[inline]` attribute included.

    Do NOT change the existing `real_value` / `with_real_value` method bodies. Do NOT change any other trait or struct. This is a strictly additive non-breaking change.
  </action>
  <acceptance_criteria>
    - src/traits/real_gene.rs contains the literal token sequence `fn bounds(&self) -> Option<(f64, f64)>` exactly three times (once in trait definition, once per impl override).
    - The trait method has a default body returning `None` (grep: `bounds.*None`).
    - Both `Range<f64>` and `MultiRangeGenotype<f64>` impls call `self.ranges.first().copied()` inside their `bounds()` override.
    - `cargo build` succeeds.
    - `cargo test` exits 0 (no existing test regressions).
    - `cargo clippy --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo build && cargo test 2>&1 | tail -5 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && grep -c 'fn bounds(&self) -> Option<(f64, f64)>' src/traits/real_gene.rs</automated>
  </verify>
  <done>`RealGene::bounds()` available with default `None`; `Range<f64>` and `MultiRangeGenotype<f64>` return their first-range pair; existing test suite green; clippy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/engines/pso/test_pso.rs with #[ignore]-gated Nyquist stubs</name>
  <files>tests/engines/pso/test_pso.rs</files>
  <read_first>
    - tests/engines/cma/test_cma.rs (full file — pattern to mirror for SpyObserver, random_pop helper, observer-lifecycle test layout)
    - .planning/phases/57-pso-engine/57-RESEARCH.md (section `Phase Requirements to Test Map` — enumerates the 10 PSO behaviors to stub)
    - .planning/phases/57-pso-engine/57-PATTERNS.md (section `tests/engines/pso/test_pso.rs (test)` — pattern assignments)
    - .planning/phases/57-pso-engine/57-CONTEXT.md (locked decisions D-01 through D-10)
  </read_first>
  <action>
    Create the file `tests/engines/pso/test_pso.rs`. Mirror the import block from tests/engines/cma/test_cma.rs lines 7–20 but substitute `cma::` paths for `pso::` paths. Specifically the file MUST import:
      - `genetic_algorithms::chromosomes::Range as RangeChromosome`
      - `genetic_algorithms::genotypes::Range as RangeGene`
      - `genetic_algorithms::pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoTopology}`
      - `genetic_algorithms::configuration::ProblemSolving`
      - `genetic_algorithms::observer::GaObserver`
      - `genetic_algorithms::ga::TerminationCause`
      - `genetic_algorithms::stats::GenerationStats`
      - `genetic_algorithms::traits::{ChromosomeT, GeneT, LinearChromosome, RealGene}`
      - `genetic_algorithms::rng`
      - `std::sync::Arc`, `std::sync::atomic::{AtomicUsize, Ordering}`, `std::borrow::Cow`, `rand::Rng`

    Copy `SpyObserver` verbatim from tests/engines/cma/test_cma.rs lines 52–80 (only renaming the surrounding test-module comments to reference PSO).

    Copy the `random_pop(n, dim, lo, hi, seed)` helper verbatim from tests/engines/cma/test_cma.rs lines 30–46.

    Add ONE `#[test] #[ignore = "Plan 02/03 will implement engine; un-ignore once available"]` stub function per behavior listed in 57-RESEARCH.md `Phase Requirements to Test Map`. Each stub body MUST be `unimplemented!("Plan 0X: <behavior>");` where the X plan number matches the wave that lights the test (Plan 02 for config-only stubs; Plan 03 for engine-runtime stubs; Plan 04 for the WASM placeholder). Stub names MUST match the table exactly:

      - `test_pso_run_returns_result`           (un-ignore in Plan 03)
      - `test_pso_pbest_update`                 (un-ignore in Plan 03)
      - `test_pso_observer_run_start`           (un-ignore in Plan 03)
      - `test_pso_observer_generation_count`    (un-ignore in Plan 03)
      - `test_pso_observer_new_best`            (un-ignore in Plan 03)
      - `test_pso_observer_run_end`             (un-ignore in Plan 03)
      - `test_pso_ring_wrap`                    (un-ignore in Plan 03)
      - `test_pso_absorbing_boundary`           (un-ignore in Plan 03)
      - `test_pso_linear_decay`                 (un-ignore in Plan 02 — pure inertia function, no engine needed)
      - `test_pso_sphere_converges`             (un-ignore in Plan 03)
      - `test_pso_wasm_compiles`                (un-ignore in Plan 04, body remains `unimplemented!` placeholder per 57-PATTERNS.md WASM placeholder pattern)

    Each `#[ignore = "..."]` message MUST name the plan that will un-ignore the test. The file MUST compile under `cargo build --tests` immediately (no symbol-resolution failures), which means the `pso::` import line will fail until Plan 02 lands. To allow this Wave-1 file to compile cleanly while Plan 02 is unbuilt, wrap the entire body of the test file (after the file-level doc comment) in `#[cfg(not_yet)]` IS NOT ALLOWED — instead, do NOT add the `pso::` imports yet. Add a TODO comment at the top of the imports block: `// TODO(plan-02): re-add `use genetic_algorithms::pso::{...};` once src/engines/pso lands.` and use a placeholder `let _ = (PsoConfiguration::default(),);`-style code only INSIDE the stub bodies that need it (which they don't — every body is `unimplemented!()`).

    Net result: file compiles in Wave 1 with only stdlib + crate-level imports (no `pso::` import yet); 11 stubs are present, each `#[ignore]`-gated; SpyObserver and random_pop helpers are defined for Plan 03 reuse.
  </action>
  <acceptance_criteria>
    - File tests/engines/pso/test_pso.rs exists.
    - `cargo build --tests` exits 0 (file compiles standalone in Wave 1 — no `pso::` symbols referenced yet).
    - `cargo test --test test_pso 2>&1 | grep -E '11 passed|11 ignored|ignored' | wc -l` is at least 1 (all 11 stubs are recognized as test functions and ignored).
    - File contains exactly 11 `#[test]` attributes.
    - File contains exactly 11 `#[ignore =` attributes.
    - File contains `struct SpyObserver` and the helper function `fn random_pop`.
    - Each stub function name listed in `<action>` appears as a function definition (`fn <name>(`).
  </acceptance_criteria>
  <verify>
    <automated>cargo build --tests 2>&1 | tail -5 && cargo test --test test_pso 2>&1 | tail -10 && grep -c '#\[test\]' tests/engines/pso/test_pso.rs && grep -c '#\[ignore' tests/engines/pso/test_pso.rs</automated>
  </verify>
  <done>tests/engines/pso/test_pso.rs compiles, runs (all 11 stubs ignored), and contains the SpyObserver + random_pop helpers ready for Plan 03 reuse.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds (trait extension compiles).
- `cargo test` passes (no regressions from `RealGene` extension; 11 PSO stubs marked ignored).
- `cargo clippy --all-targets -- -D warnings` is clean.
- `RealGene::bounds()` available at `crate::traits::RealGene` with default `None` and per-type overrides for `Range<f64>` / `MultiRangeGenotype<f64>`.
- `tests/engines/pso/test_pso.rs` exists with 11 `#[ignore]`-gated stub tests, SpyObserver, and random_pop helper.
</verification>

<success_criteria>
- `RealGene` exposes `bounds()` (non-breaking) and the two existing f64 gene types override it correctly.
- PSO Nyquist test scaffold ready: 11 stubs, SpyObserver, helpers — all gated `#[ignore]`.
- Zero existing test regressions; zero clippy warnings.
- Plans 02–03 unblocked.
</success_criteria>

<output>
Create `.planning/phases/57-pso-engine/57-01-SUMMARY.md` when done.
</output>
