---
phase: 57-pso-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 57-03
files_modified:
  - examples/pso_rastrigin.rs
  - Cargo.toml
  - tests/engines/pso/test_pso.rs
autonomous: false
requirements:
  - PSO-EXAMPLE (D-10 — pso_rastrigin example, 10D consistent with cma_es_rastrigin)
  - PSO-PHASE-GATE (cargo test + cargo test --features serde + cargo clippy + cargo doc + cargo check --target wasm32-unknown-unknown + cargo run --example pso_rastrigin)
must_haves:
  truths:
    - "User can run `cargo run --release --example pso_rastrigin` and observe convergence on 10D Rastrigin to a fitness near 0 (target 1e-3 reached or final fitness < 1.0) in under 30 seconds wall-clock on the developer's machine"
    - "Example uses LogObserver wired via `.with_observer(Arc::new(LogObserver))`, demonstrating the D-09 observer-from-day-1 contract"
    - "`cargo test` passes (all PSO tests + all pre-existing tests)"
    - "`cargo test --features serde` passes"
    - "`cargo clippy --all-targets -- -D warnings` passes with zero warnings"
    - "`cargo doc --no-deps` passes with zero rustdoc warnings"
    - "`cargo check --target wasm32-unknown-unknown` passes (no Instant, no rayon in PSO code paths)"
    - "Phase 57 verification gate documented in 57-VALIDATION.md is satisfied"
  artifacts:
    - path: examples/pso_rastrigin.rs
      provides: "Working 10D Rastrigin minimization example with LogObserver wiring and detailed prints (gens, best fitness, best DNA)"
      contains: "fn rastrigin"
    - path: Cargo.toml
      provides: "`[[example]] name = \"pso_rastrigin\"` entry"
      contains: "pso_rastrigin"
    - path: tests/engines/pso/test_pso.rs
      provides: "`test_pso_wasm_compiles` un-ignored — its body documents that WASM verification is performed by the `cargo check --target wasm32-unknown-unknown` CI gate (test body remains a no-op placeholder; the real verification is performed in the checkpoint task below)"
      contains: "test_pso_wasm_compiles"
  key_links:
    - from: examples/pso_rastrigin.rs
      to: src/engines/pso/engine.rs
      via: "use genetic_algorithms::pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoTopology}"
      pattern: "genetic_algorithms::pso"
    - from: examples/pso_rastrigin.rs
      to: src/observer/log_observer.rs
      via: "use genetic_algorithms::LogObserver"
      pattern: "LogObserver"
---

<objective>
Write the `pso_rastrigin` example (10D, mirrors `cma_es_rastrigin` structure per D-10) and register it in `Cargo.toml`. Light up the WASM placeholder test. Run the full phase 57 verification gate: `cargo test`, `cargo test --features serde`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`, `cargo check --target wasm32-unknown-unknown`, and `cargo run --release --example pso_rastrigin`. A blocking human-verify checkpoint reviews the example output before phase completion.

Purpose: Close out Phase 57 with a runnable demonstration of the engine and a verified-clean CI matrix. Provides the user-facing artifact (`cargo run --example pso_rastrigin`) that the milestone roadmap success criteria reference.

Output: A runnable, observable example; a fully-green CI matrix on the milestone branch; phase 57 ready to merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-pso-engine/57-CONTEXT.md
@.planning/phases/57-pso-engine/57-RESEARCH.md
@.planning/phases/57-pso-engine/57-PATTERNS.md
@.planning/phases/57-pso-engine/57-VALIDATION.md
@.planning/phases/57-pso-engine/57-01-SUMMARY.md
@.planning/phases/57-pso-engine/57-02-SUMMARY.md
@.planning/phases/57-pso-engine/57-03-SUMMARY.md
@examples/cma_es_rastrigin.rs
@src/engines/pso/engine.rs
@src/engines/pso/configuration.rs
@src/lib.rs
@Cargo.toml
@tests/engines/pso/test_pso.rs

<interfaces>
Example mirrors examples/cma_es_rastrigin.rs structure (verbatim per 57-PATTERNS.md):
  - `const DIMENSIONS: usize = 10;` (10D vs CMA's 5D)
  - `const SEARCH_LO: f64 = -5.12;`, `const SEARCH_HI: f64 = 5.12;`
  - `fn rastrigin(dna: &[RangeGene<f64>]) -> f64` — standard Rastrigin formula
  - `fn init_population(n: usize) -> Vec<RangeChromosome<f64>>` — uniform random init with seed 42
  - `fn main()` — config build, engine new + with_observer, run, print result summary

`Cargo.toml` `[[example]]` entry pattern: locate the existing `[[example]] name = "cma_es_rastrigin"` block and add a parallel `[[example]] name = "pso_rastrigin"` block (no `required-features` — PSO has no feature gates).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write examples/pso_rastrigin.rs + register in Cargo.toml + un-ignore WASM test</name>
  <files>examples/pso_rastrigin.rs, Cargo.toml, tests/engines/pso/test_pso.rs</files>
  <read_first>
    - examples/cma_es_rastrigin.rs (full file — template to mirror)
    - Cargo.toml (locate the `[[example]] name = "cma_es_rastrigin"` block — usually near the end of the file)
    - src/engines/pso/engine.rs (verify the PsoEngine API the example will call)
    - src/engines/pso/configuration.rs (verify PsoConfiguration fields)
    - src/lib.rs (verify `LogObserver` is re-exported at crate root — adjust import if it sits at a different path like `genetic_algorithms::observer::LogObserver`)
    - tests/engines/pso/test_pso.rs (currently 10 active + 1 ignored)
    - .planning/phases/57-pso-engine/57-PATTERNS.md (section `examples/pso_rastrigin.rs (example)` — full file template)
  </read_first>
  <action>
    Create examples/pso_rastrigin.rs:

    - Module-level `//!` doc comment: "PSO on 10D Rastrigin. Demonstrates Particle Swarm Optimization with LogObserver, LinearDecay inertia, gbest topology, and fitness-target early-stop. Run with `cargo run --release --example pso_rastrigin`."

    - Imports (verify exact paths against src/lib.rs — adjust if `LogObserver` lives at `genetic_algorithms::observer::LogObserver`):
      ```
      use std::f64::consts::PI;
      use std::sync::Arc;
      use std::borrow::Cow;
      use genetic_algorithms::chromosomes::Range as RangeChromosome;
      use genetic_algorithms::pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoTopology};
      use genetic_algorithms::configuration::ProblemSolving;
      use genetic_algorithms::genotypes::Range as RangeGene;
      use genetic_algorithms::rng;
      use genetic_algorithms::traits::{ChromosomeT, GeneT, LinearChromosome, RealGene};
      use genetic_algorithms::LogObserver;   // or genetic_algorithms::observer::LogObserver — match cma_es_rastrigin.rs
      use rand::Rng;
      ```

    - Constants: `const DIMENSIONS: usize = 10; const SEARCH_LO: f64 = -5.12; const SEARCH_HI: f64 = 5.12;`

    - `fn rastrigin(dna: &[RangeGene<f64>]) -> f64`: standard Rastrigin `10.0 * n + Σ (x² − 10 cos(2πx))`. Mirror the body shown in 57-PATTERNS.md `examples/pso_rastrigin.rs (example)`.

    - `fn init_population(n: usize) -> Vec<RangeChromosome<f64>>`: mirror the init pattern from examples/cma_es_rastrigin.rs lines 49–65 — seed RNG to 42, uniform random sampling on `[SEARCH_LO, SEARCH_HI]` per gene.

    - `fn main()`:
      * Build config using struct-literal syntax (matches cma_es_rastrigin.rs style):
        ```
        let config = PsoConfiguration {
            population_size: 30,
            max_generations: 1000,
            problem_solving: ProblemSolving::Minimization,
            fitness_target: Some(1e-3),
            inertia: PsoInertia::LinearDecay { w_start: 0.9, w_end: 0.4 },
            c1: 2.0,
            c2: 2.0,
            topology: PsoTopology::Global,
        };
        ```
      * Build engine: `let mut engine = PsoEngine::new(config, init_population, rastrigin).with_observer(Arc::new(LogObserver));`
      * Header print: `println!("== PSO: {DIMENSIONS}D Rastrigin Minimization ==");`
      * Run: `let result = engine.run();`
      * Footer prints (mirror cma_es_rastrigin.rs lines 85–91): generations, best fitness, best DNA values (formatted to 4 decimal places).

    Edit Cargo.toml: locate the `[[example]] name = "cma_es_rastrigin"` block (use grep on the file to find the right offset; do NOT scan the entire file blindly). Add a parallel block immediately after it:
      ```
      [[example]]
      name = "pso_rastrigin"
      path = "examples/pso_rastrigin.rs"
      ```
      (Use `path =` only if the cma_es_rastrigin block uses `path =`; otherwise omit it — match the surrounding style exactly.)

    Edit tests/engines/pso/test_pso.rs: REMOVE the `#[ignore = "..."]` attribute from `test_pso_wasm_compiles`. REPLACE its `unimplemented!(...)` body with a small no-op assertion that documents the actual WASM gate is the CI step in the verification checkpoint:
      ```
      // Real WASM verification is performed by `cargo check --target wasm32-unknown-unknown`
      // as part of the phase verification gate (see Plan 04 verification checkpoint).
      // This test exists to compile the PSO public API surface in normal `cargo test` runs.
      let _config = PsoConfiguration::default();
      let _topo = PsoTopology::Global;
      let _inertia = PsoInertia::Constant(0.7);
      ```
  </action>
  <acceptance_criteria>
    - examples/pso_rastrigin.rs exists and contains `fn rastrigin`, `fn init_population`, `fn main`, and an import of `genetic_algorithms::pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoTopology}`.
    - Cargo.toml contains a literal `name = "pso_rastrigin"` example entry.
    - `cargo build --example pso_rastrigin` exits 0.
    - `cargo build --examples` exits 0 (no regression to other examples).
    - `cargo test --test test_pso 2>&1 | grep "test result" | grep -E '11 passed.*0 failed.*0 ignored'` — all 11 PSO tests now active and passing.
    - `cargo clippy --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f examples/pso_rastrigin.rs && grep -F 'pso_rastrigin' Cargo.toml && cargo build --example pso_rastrigin 2>&1 | tail -5 && cargo build --examples 2>&1 | tail -5 && cargo test --test test_pso 2>&1 | tail -5 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Example builds, all 11 PSO tests pass, clippy clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 57 verification gate — full CI matrix + example run</name>
  <what-built>
    The full Phase 57 PSO engine: `RealGene::bounds()` extension (Plan 01), `PsoConfiguration`/`PsoInertia`/`PsoTopology` (Plan 02), `PsoEngine::run()` core loop with topology dispatch and absorbing boundary (Plan 03), and the `pso_rastrigin` example (this plan). Eleven integration tests cover engine behavior; WASM compatibility is verified by `cargo check --target wasm32-unknown-unknown`.
  </what-built>
  <how-to-verify>
    From the repo root, run each command below and confirm clean exit:

      1. `cargo test 2>&1 | tail -20` — entire crate test suite passes, including 11 PSO tests.
      2. `cargo test --features serde 2>&1 | tail -20` — serde-feature test suite passes.
      3. `cargo clippy --all-targets -- -D warnings 2>&1 | tail -20` — zero warnings, zero errors.
      4. `cargo doc --no-deps 2>&1 | tail -20` — zero rustdoc warnings.
      5. `cargo check --target wasm32-unknown-unknown 2>&1 | tail -20` — PSO compiles for WASM.
      6. `cargo run --release --example pso_rastrigin` — example runs and prints final best fitness. Expected: fitness < 1.0 (typically << 1e-2 since target is 1e-3), generations < 1000 (target early-stops).

    For each command, paste the last 20 lines of output (or confirm green) when responding "approved" / "issues".

    If any gate fails, describe which one and the failure mode. Do NOT mark the phase complete unless all six gates pass.
  </how-to-verify>
  <resume-signal>Type "approved" once all six gates pass and the example output is satisfactory; otherwise describe failures.</resume-signal>
</task>

</tasks>

<verification>
- All six CI gates green: `cargo test`, `cargo test --features serde`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`, `cargo check --target wasm32-unknown-unknown`, `cargo run --release --example pso_rastrigin`.
- 11 PSO tests passing (was 10 + 1 ignored).
- `examples/pso_rastrigin.rs` registered in `Cargo.toml` and produces a converging run with LogObserver output.
- No regressions in any other example, test, or engine.
- 57-VALIDATION.md gate requirements satisfied.
</verification>

<success_criteria>
- Phase 57 complete: full PSO engine shipped, observable, tested, documented, WASM-compatible, with a runnable example.
- ROADMAP entry for Phase 57 can be marked complete and Plans count updated.
- Branch ready for PR to milestone/v3.0.0.
</success_criteria>

<output>
Create `.planning/phases/57-pso-engine/57-04-SUMMARY.md` when done.
</output>
