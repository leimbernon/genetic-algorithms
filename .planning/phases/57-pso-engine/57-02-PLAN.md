---
phase: 57-pso-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - src/engines/pso/configuration.rs
  - src/engines/pso/engine.rs
  - src/engines/pso/mod.rs
  - src/engines/mod.rs
  - src/lib.rs
  - tests/engines/pso/test_pso.rs
autonomous: true
requirements:
  - PSO-CONFIG (D-03, D-04, D-05 — PsoConfiguration / PsoInertia / PsoTopology)
  - PSO-SKELETON (D-09 — PsoEngine struct + with_observer; run() stub returns empty PsoResult)
  - PSO-LIB-REEXPORT (lib.rs `#[path]` alias + pub use re-exports per 57-PATTERNS.md)
must_haves:
  truths:
    - "User can write `PsoConfiguration::default()` and receive: population_size=30, max_generations=1000, problem_solving=Minimization, fitness_target=None, inertia=LinearDecay { w_start: 0.9, w_end: 0.4 }, c1=2.0, c2=2.0, topology=PsoTopology::Global"
    - "User can chain `PsoConfiguration::default().with_population_size(50).with_max_generations(500).with_c1(1.5).with_c2(1.5).with_inertia(PsoInertia::Constant(0.7)).with_topology(PsoTopology::Ring { neighborhood_size: 3 }).with_fitness_target(1e-6).with_problem_solving(ProblemSolving::Minimization)` and receive each field set"
    - "User can construct `PsoEngine::new(config, init_fn, fitness_fn)` and `.with_observer(Arc::new(...))` — engine compiles and returns the expected types"
    - "`genetic_algorithms::pso::{PsoEngine, PsoConfiguration, PsoResult, PsoInertia, PsoTopology}` are re-exported at the crate root"
    - "`test_pso_linear_decay` (un-ignored in this plan) verifies that `LinearDecay { w_start: 0.9, w_end: 0.4 }` produces w=0.9 at gen=0 and w=0.4 at gen=max_generations-1 (and guards max_generations<=1 → returns w_end)"
    - "`cargo build`, `cargo test --test test_pso` (10 ignored, 1 passing), `cargo clippy --all-targets -- -D warnings` all clean"
  artifacts:
    - path: src/engines/pso/configuration.rs
      provides: "PsoConfiguration struct + Default + builder methods + PsoInertia enum + PsoTopology enum + inertia_weight helper"
      contains: "pub struct PsoConfiguration"
    - path: src/engines/pso/engine.rs
      provides: "PsoEngine<U> struct + new() + with_observer() + notify() helper + PsoResult<U> + stub run() that returns an empty PsoResult"
      contains: "pub struct PsoEngine"
    - path: src/engines/pso/mod.rs
      provides: "Module wiring + pub use re-exports"
      contains: "pub mod configuration"
    - path: src/lib.rs
      provides: "`#[path = \"engines/pso/mod.rs\"] pub mod pso;` + `pub use pso::{...}` re-export"
      contains: "engines/pso/mod.rs"
  key_links:
    - from: src/engines/pso/configuration.rs
      to: src/configuration.rs
      via: "import ProblemSolving"
      pattern: "use crate::configuration::ProblemSolving"
    - from: src/engines/pso/engine.rs
      to: src/observer/mod.rs
      via: "import GaObserver"
      pattern: "use crate::observer::GaObserver"
    - from: src/lib.rs
      to: src/engines/pso/mod.rs
      via: "`#[path]` module alias + re-export block"
      pattern: "engines/pso/mod\\.rs"
---

<objective>
Stand up the entire PSO module skeleton: configuration (struct + enums + Default + builder methods + linear-decay helper), engine skeleton (`PsoEngine`, `PsoResult`, `new`, `with_observer`, `notify`, and a stub `run()` that returns an empty `PsoResult`), module wiring, and the lib.rs re-export block. Un-ignore the one PSO test that is engine-independent (`test_pso_linear_decay`) and update its body. Plan 03 will fill `run()` with the full PSO loop.

Purpose: Lock the public API surface (D-03 topology, D-05 inertia, D-09 observer wiring) before the heavy run-loop implementation. Provides compile-checked types for users / docs / Plan 03 / Plan 04 example.

Output: `genetic_algorithms::pso::*` re-exported and usable; `LinearDecay` helper verified by a real (un-ignored) test; engine `run()` is a typed stub returning a minimal `PsoResult` from the initial population.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-pso-engine/57-CONTEXT.md
@.planning/phases/57-pso-engine/57-RESEARCH.md
@.planning/phases/57-pso-engine/57-PATTERNS.md
@.planning/phases/57-pso-engine/57-01-SUMMARY.md
@src/engines/cma/configuration.rs
@src/engines/cma/engine.rs
@src/engines/cma/mod.rs
@src/engines/mod.rs
@src/lib.rs
@src/observer/mod.rs
@src/traits/real_gene.rs
@src/traits/linear_chromosome.rs
@src/configuration.rs
@src/ga.rs
@tests/engines/pso/test_pso.rs

<interfaces>
Existing `CmaConfiguration` shape (src/engines/cma/configuration.rs) is the template — mirror its layout but with PSO fields.

`PsoConfiguration` fields (from 57-CONTEXT.md and 57-RESEARCH.md `PsoConfiguration Structure`):
  - `pub population_size: usize`              (0 = auto → 30)
  - `pub max_generations: usize`              (default 1000)
  - `pub problem_solving: ProblemSolving`     (default Minimization)
  - `pub fitness_target: Option<f64>`         (default None)
  - `pub inertia: PsoInertia`                 (default LinearDecay { w_start: 0.9, w_end: 0.4 })
  - `pub c1: f64`                             (default 2.0)
  - `pub c2: f64`                             (default 2.0)
  - `pub topology: PsoTopology`               (default PsoTopology::Global)

`PsoInertia` enum:
  - `Constant(f64)`
  - `LinearDecay { w_start: f64, w_end: f64 }`

`PsoTopology` enum:
  - `Global`
  - `Ring { neighborhood_size: usize }`

`PsoResult<U: LinearChromosome>` fields:
  - `pub population: Vec<U>`
  - `pub best: U`
  - `pub best_fitness: f64`
  - `pub generations: usize`

`PsoEngine<U>` struct bounds: `U: LinearChromosome`, `U::Gene: RealGene` (verbatim from CmaEngine).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/engines/pso/configuration.rs with PsoConfiguration, PsoInertia, PsoTopology, and inertia_weight() helper</name>
  <files>src/engines/pso/configuration.rs</files>
  <read_first>
    - src/engines/cma/configuration.rs (full file — template to mirror)
    - src/configuration.rs (ProblemSolving enum definition)
    - .planning/phases/57-pso-engine/57-CONTEXT.md (decisions D-03, D-05)
    - .planning/phases/57-pso-engine/57-RESEARCH.md (sections `PsoConfiguration Structure`, `Common Pitfalls #1` ring-wrap, `Common Pitfalls #5` LinearDecay div-by-zero)
    - .planning/phases/57-pso-engine/57-PATTERNS.md (section `src/engines/pso/configuration.rs (config)`)
  </read_first>
  <action>
    Create the file containing:

    1. A module-level `//!` doc comment: "Configuration types for the PSO engine. See `PsoConfiguration` for the entry point."

    2. `use crate::configuration::ProblemSolving;` import.

    3. `#[derive(Debug, Clone)] pub enum PsoInertia` with two variants: `Constant(f64)` and `LinearDecay { w_start: f64, w_end: f64 }`. Add `///` docs explaining each variant and recommended values.

    4. `#[derive(Debug, Clone)] pub enum PsoTopology` with `Global` and `Ring { neighborhood_size: usize }`. Add `///` docs explaining each variant.

    5. `#[derive(Debug, Clone)] pub struct PsoConfiguration` with the 8 `pub` fields listed in `<interfaces>` and a `///` doc on each field.

    6. `impl Default for PsoConfiguration` returning population_size=30, max_generations=1000, problem_solving=ProblemSolving::Minimization, fitness_target=None, inertia=PsoInertia::LinearDecay { w_start: 0.9, w_end: 0.4 }, c1=2.0, c2=2.0, topology=PsoTopology::Global.

    7. `impl PsoConfiguration` block with builder methods, one per field, named `with_population_size`, `with_max_generations`, `with_problem_solving`, `with_fitness_target` (takes `f64`, wraps in Some), `with_inertia`, `with_c1`, `with_c2`, `with_topology`. Each builder takes `mut self` and returns `Self` (verbatim CmaConfiguration pattern).

    8. A `pub fn inertia_weight(inertia: &PsoInertia, gen: usize, max_generations: usize) -> f64` free function (not a method) at module scope that:
       - For `PsoInertia::Constant(w)` returns `*w`.
       - For `PsoInertia::LinearDecay { w_start, w_end }` returns `*w_end` when `max_generations <= 1` (guards Pitfall #5 div-by-zero); otherwise returns `w_start + (w_end - w_start) * (gen as f64) / ((max_generations - 1) as f64)`.

    Match the doc-comment style and field-ordering used in src/engines/cma/configuration.rs.
  </action>
  <acceptance_criteria>
    - src/engines/pso/configuration.rs exists.
    - `pub struct PsoConfiguration` and `impl Default for PsoConfiguration` present (grep both).
    - All 8 `with_*` builder methods present: grep returns `with_population_size`, `with_max_generations`, `with_problem_solving`, `with_fitness_target`, `with_inertia`, `with_c1`, `with_c2`, `with_topology`.
    - `pub enum PsoInertia` contains both `Constant` and `LinearDecay` variants.
    - `pub enum PsoTopology` contains both `Global` and `Ring` variants.
    - `pub fn inertia_weight` present at module scope.
    - Module compiles after Task 2 wires it in (verified by Task 2 build).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/engines/pso/configuration.rs && grep -c 'pub struct PsoConfiguration\|pub enum PsoInertia\|pub enum PsoTopology\|pub fn inertia_weight\|impl Default for PsoConfiguration' src/engines/pso/configuration.rs && grep -cE 'with_(population_size|max_generations|problem_solving|fitness_target|inertia|c1|c2|topology)' src/engines/pso/configuration.rs</automated>
  </verify>
  <done>Configuration module file created with all types, Default impl, 8 builders, and the inertia_weight helper that guards against max_generations <= 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PsoEngine skeleton (engine.rs + mod.rs) + wire into src/engines/mod.rs + src/lib.rs re-export</name>
  <files>src/engines/pso/engine.rs, src/engines/pso/mod.rs, src/engines/mod.rs, src/lib.rs</files>
  <read_first>
    - src/engines/cma/engine.rs (struct layout lines 332-377; PsoResult template lines 297-306; notify helper)
    - src/engines/cma/mod.rs (full file — module wiring template)
    - src/engines/mod.rs (where `pub mod cma;` is declared — pattern to add `pub mod pso;`)
    - src/lib.rs (find `#[path = "engines/cma/mod.rs"] pub mod cma;` and the trait re-export block — sections at lines ~330 and ~362 per 57-PATTERNS.md)
    - src/observer/mod.rs (GaObserver trait + FitnessFn type — verify import paths)
    - src/traits/linear_chromosome.rs and src/traits/real_gene.rs (bounds for engine type parameter)
    - src/ga.rs (TerminationCause enum — needed for stub `on_run_end` notification, even though run() is a stub)
    - src/engines/pso/configuration.rs (just created in Task 1 — import PsoConfiguration)
  </read_first>
  <action>
    Create `src/engines/pso/mod.rs` containing:
      `//! PSO engine. Particle Swarm Optimization for real-valued continuous optimization.`
      `pub mod configuration;`
      `pub mod engine;`
      `pub use configuration::{PsoConfiguration, PsoInertia, PsoTopology};`
      `pub use engine::{PsoEngine, PsoResult};`

    Create `src/engines/pso/engine.rs` containing:

    1. Module-level `//!` doc comment describing the PSO engine: mirror the wording style of src/engines/cma/engine.rs `//!` header (one-paragraph summary + reference to Kennedy & Eberhart 1995 / Shi & Eberhart 1998).

    2. Imports (mirror src/engines/cma/engine.rs lines 13–24):
       ```
       use std::sync::Arc;

       use crate::configuration::ProblemSolving;
       use crate::ga::TerminationCause;
       use crate::observer::GaObserver;
       use crate::stats::GenerationStats;
       use crate::traits::{FitnessFn, LinearChromosome, RealGene};

       use super::configuration::PsoConfiguration;
       ```
       (Plan 03 will add `std::borrow::Cow`, `rand::Rng`, `crate::rng::make_rng` as the run loop needs them.)

    3. `pub struct PsoResult<U: LinearChromosome>` with fields `pub population: Vec<U>, pub best: U, pub best_fitness: f64, pub generations: usize`. Derive nothing (matches CmaResult).

    4. `pub struct PsoEngine<U: LinearChromosome> where U::Gene: RealGene` with fields: `config: PsoConfiguration`, `init_fn: Arc<dyn Fn(usize) -> Vec<U> + Send + Sync>`, `fitness_fn: Arc<FitnessFn<U::Gene>>`, `observer: Option<Arc<dyn GaObserver<U> + Send + Sync>>`.

    5. `impl<U: LinearChromosome + Clone> PsoEngine<U> where U::Gene: RealGene` with:
       - `pub fn new(config: PsoConfiguration, init_fn: impl Fn(usize) -> Vec<U> + Send + Sync + 'static, fitness_fn: impl Fn(&[U::Gene]) -> f64 + Send + Sync + 'static) -> Self` — wraps both fns in Arc, observer = None.
       - `pub fn with_observer(mut self, obs: Arc<dyn GaObserver<U> + Send + Sync>) -> Self { self.observer = Some(obs); self }`
       - `#[inline] fn notify<F: FnOnce(&dyn GaObserver<U>)>(&self, f: F) { if let Some(ref obs) = self.observer { f(obs.as_ref()); } }`
       - `pub fn run(&mut self) -> PsoResult<U>` — STUB body for Plan 02 only: builds initial population via `(self.init_fn)(self.config.population_size.max(1))`, evaluates fitness on every individual via `(self.fitness_fn)(ind.dna())`, computes a `best` by linear scan honouring `self.config.problem_solving` (Minimization → smallest fitness; Maximization → largest; FixedFitness → closest to target if set else smallest), fires `on_run_start` then immediately `on_run_end(TerminationCause::GenerationLimitReached, &[])`, returns `PsoResult { population: pop, best, best_fitness, generations: 0 }`. If init_fn returns empty, follow the CmaEngine pattern (lines 432–447): `log::warn!(target: "pso_events", ...)`, fire `on_run_end`, and `panic!` with message `"PsoEngine: init_fn returned an empty population"`. Add an inline TODO comment: `// TODO(plan-03): replace stub with full PSO velocity-update loop`.

    6. Population-size auto-default: if `self.config.population_size == 0`, treat it as 30 in the stub (call `(self.init_fn)(self.config.population_size.max(1).max(30))`). Document this in the rustdoc on `run()`: "When `population_size` is 0, a default of 30 particles is used (PSO literature standard)."

    Edit `src/engines/mod.rs`: locate the existing `pub mod cma;` declaration and add `pub mod pso;` directly after it (alphabetical position).

    Edit `src/lib.rs`:
      - After the line `#[path = "engines/cma/mod.rs"] pub mod cma;` add `#[path = "engines/pso/mod.rs"] pub mod pso;` on the next non-blank line (preserve the existing comment/grouping style).
      - In the `pub use` block (~line 362 per 57-PATTERNS.md), add `pub use pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoResult, PsoTopology};` adjacent to the existing `pub use cma::{...};` re-export (alphabetical insertion).

    Update `tests/engines/pso/test_pso.rs`: now that `genetic_algorithms::pso::*` exists, REPLACE the TODO comment from Plan 01 with the actual imports `use genetic_algorithms::pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoTopology};`. Un-ignore `test_pso_linear_decay` (remove the `#[ignore = "..."]` line) and replace its `unimplemented!()` body with a real test: build `let w0 = inertia_weight(&PsoInertia::LinearDecay { w_start: 0.9, w_end: 0.4 }, 0, 100); assert!((w0 - 0.9).abs() < 1e-12);` and similarly assert that at `gen = 99, max_generations = 100` the result is within 1e-12 of 0.4, AND that with `max_generations = 1` the result equals `w_end = 0.4` (Pitfall #5 guard). Add the `use genetic_algorithms::engines::pso::configuration::inertia_weight;` (or whatever the verified public path is — confirm against the actual file just written) import. If `inertia_weight` is not re-exported, expose it via `pub use configuration::inertia_weight;` in `src/engines/pso/mod.rs` and re-export through `src/lib.rs` adjacent to the PSO block.
  </action>
  <acceptance_criteria>
    - Files `src/engines/pso/engine.rs`, `src/engines/pso/mod.rs` exist.
    - `src/engines/mod.rs` contains `pub mod pso;`.
    - `src/lib.rs` contains `#[path = "engines/pso/mod.rs"]` and `pub use pso::{PsoConfiguration, PsoEngine, PsoInertia, PsoResult, PsoTopology}` (grep both literal substrings).
    - `cargo build` exits 0.
    - `cargo doc --no-deps` exits 0 with zero warnings.
    - `cargo test --test test_pso test_pso_linear_decay -- --nocapture` exits 0 (one passing test).
    - `cargo test --test test_pso 2>&1 | grep -E 'test result' | grep -E '1 passed.*10 ignored'` matches (1 active test + 10 still-ignored stubs).
    - `cargo clippy --all-targets -- -D warnings` exits 0.
    - The stub `run()` method does not panic on a non-empty population and returns a `PsoResult` with `generations == 0` and a `best` whose `best_fitness` equals the best-of-initial-pop value.
  </acceptance_criteria>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && grep -F '#[path = "engines/pso/mod.rs"]' src/lib.rs && grep -F 'pub use pso::' src/lib.rs && cargo test --test test_pso test_pso_linear_decay 2>&1 | tail -10 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && cargo doc --no-deps 2>&1 | tail -5</automated>
  </verify>
  <done>PSO module compiles, is re-exported from crate root, `LinearDecay` math verified by a real passing test, engine `run()` is a typed stub honoring Minimization/Maximization/FixedFitness for the initial-population best.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds; PSO public API surface is locked.
- `cargo test --test test_pso` → 1 passing (`test_pso_linear_decay`) + 10 still-ignored.
- `cargo clippy --all-targets -- -D warnings` clean.
- `cargo doc --no-deps` clean (no broken doc-links from PSO).
- `genetic_algorithms::pso::{PsoEngine, PsoConfiguration, PsoResult, PsoInertia, PsoTopology}` all resolve at crate root.
- Stub `run()` returns a `PsoResult` from the initial-population scan; Plan 03 will replace the stub body with the full PSO loop.
</verification>

<success_criteria>
- PSO module compiles end-to-end and is publicly re-exported per 57-PATTERNS.md.
- `LinearDecay` inertia math correct at gen=0, gen=max-1, and guarded against `max_generations <= 1` (Pitfall #5).
- `PsoEngine::new(...).with_observer(...)` typechecks and observer is held as `Option<Arc<...>>`.
- Plan 03 unblocked: can fill `run()` with the full PSO loop using the established struct, helpers, and notify pattern.
</success_criteria>

<output>
Create `.planning/phases/57-pso-engine/57-02-SUMMARY.md` when done.
</output>
