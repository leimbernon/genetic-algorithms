---
phase: 59-restart-strategies-ipop-bipop
plan: "03"
type: execute
wave: 3
depends_on: ["59-02"]
files_modified:
  - examples/ipop_rastrigin.rs
  - Cargo.toml
autonomous: false
requirements: [SC-4]

must_haves:
  truths:
    - "cargo run --example ipop_rastrigin terminates without panic and prints Total restarts: N and Best fitness: F"
    - "cargo check --target wasm32-unknown-unknown passes"
    - "cargo test && cargo test --features serde && cargo clippy --all-targets -- -D warnings all pass"
    - "cargo doc --no-deps produces zero warnings"
  artifacts:
    - path: "examples/ipop_rastrigin.rs"
      provides: "IPOP-CMA-ES Rastrigin minimization demonstration"
      contains: "RestartStrategy::Ipop"
    - path: "Cargo.toml"
      provides: "ipop_rastrigin example registration"
      contains: "ipop_rastrigin"
  key_links:
    - from: "examples/ipop_rastrigin.rs"
      to: "CmaEngine::run()"
      via: "with_restart_strategy(RestartStrategy::Ipop { ... })"
      pattern: "result.total_restarts"
---

<objective>
Create the ipop_rastrigin example demonstrating IPOP-CMA-ES restart behavior, then run the full phase verification gate.

Purpose: Give users a runnable demonstration of IPOP restarts; validate the complete phase is production-ready across all CI gates (cargo test, serde, clippy, rustdoc, WASM check).

Output: examples/ipop_rastrigin.rs; Cargo.toml example entry; human checkpoint after example runs visually; all CI gates green.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-restart-strategies-ipop-bipop/59-02-SUMMARY.md
@.planning/phases/59-restart-strategies-ipop-bipop/59-CONTEXT.md
@.planning/phases/59-restart-strategies-ipop-bipop/59-PATTERNS.md

<interfaces>
<!-- Key patterns the executor must follow. -->

From examples/cma_es_rastrigin.rs (structural analog — exact template):
```rust
/*!
# <example name>
...
*/
use std::f64::consts::PI;
use std::sync::Arc;
use genetic_algorithms::chromosomes::Range as RangeChromosome;
use genetic_algorithms::cma::{CmaConfiguration, CmaEngine};
use genetic_algorithms::configuration::ProblemSolving;
use genetic_algorithms::genotypes::Range as RangeGene;
use genetic_algorithms::rng;
use genetic_algorithms::traits::{LinearChromosome, RealGene};
use genetic_algorithms::LogObserver;
use rand::Rng;
use std::borrow::Cow;

const DIMENSIONS: usize = 5;  // use 10 for IPOP (shows restart benefit better)

fn init_population(n: usize) -> Vec<RangeChromosome<f64>> { ... }
fn rastrigin(dna: &[RangeGene<f64>]) -> f64 { ... }
fn main() { ... }
```

From Cargo.toml (existing example registration pattern — find it with grep):
Each example registered as:
[[example]]
name = "pso_rastrigin"
path = "examples/pso_rastrigin.rs"

New types available from Plan 01/02:
```rust
use genetic_algorithms::cma::RestartStrategy;
// RestartStrategy::Ipop { population_scale: 2.0, stagnation_threshold: 50, max_restarts: 3 }
// result.total_restarts: usize
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples/ipop_rastrigin.rs and register in Cargo.toml</name>
  <files>examples/ipop_rastrigin.rs, Cargo.toml</files>
  <read_first>
    - examples/cma_es_rastrigin.rs (full file — exact structural analog per PATTERNS.md; copy init_population, rastrigin, import block verbatim)
    - Cargo.toml (find the [[example]] section; find the pso_rastrigin or eda_trap entry as the registration pattern)
    - .planning/phases/59-restart-strategies-ipop-bipop/59-PATTERNS.md (examples/ipop_rastrigin.rs section with main() body)
  </read_first>
  <action>
    1. Create examples/ipop_rastrigin.rs:
       - Copy the module-level doc comment pattern from cma_es_rastrigin.rs but update the title to "IPOP-CMA-ES: Rastrigin Minimization with Restart Strategies".
       - Copy all imports from cma_es_rastrigin.rs. Add: "use genetic_algorithms::cma::RestartStrategy;".
       - Set DIMENSIONS = 10 (higher dimension shows restart benefit more clearly on Rastrigin).
       - Copy init_population helper verbatim from cma_es_rastrigin.rs (update bounds to [-5.12, 5.12] for standard Rastrigin).
       - Copy rastrigin fitness function verbatim from cma_es_rastrigin.rs.
       - Write main() following PATTERNS.md template:
         - CmaConfiguration::default_for_dim(DIMENSIONS).with_sigma0(0.5).with_max_generations(200).with_problem_solving(Minimization).with_restart_strategy(RestartStrategy::Ipop { population_scale: 2.0, stagnation_threshold: 50, max_restarts: 3 })
         - CmaEngine::new(config, init_population, rastrigin).with_observer(Arc::new(LogObserver))
         - let result = engine.run();
         - println!("Total restarts: {}", result.total_restarts);
         - println!("Generations:    {}", result.generations);
         - println!("Best fitness:   {:.6}", result.best_fitness);
         - assert!(result.best_fitness.is_finite(), "Expected finite best fitness");

    2. Register in Cargo.toml: find the [[example]] block (grep for "[[example]]") and add a new entry:
       [[example]]
       name = "ipop_rastrigin"
       path = "examples/ipop_rastrigin.rs"

       Place it near the other CMA/PSO/EDA examples (after eda_trap or cma_es_rastrigin entry).
  </action>
  <verify>
    <automated>cargo build --example ipop_rastrigin 2>&1 | tail -5</automated>
  </verify>
  <done>cargo build --example ipop_rastrigin succeeds with zero errors; Cargo.toml contains ipop_rastrigin entry; examples/ipop_rastrigin.rs uses RestartStrategy::Ipop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 created examples/ipop_rastrigin.rs demonstrating IPOP-CMA-ES restarts on 10D Rastrigin. The example uses max_generations=200 per restart, stagnation_threshold=50, max_restarts=3.
  </what-built>
  <how-to-verify>
    Run: cargo run --release --example ipop_rastrigin

    Expected output:
    - Log lines showing generation progress (from LogObserver)
    - One or more "restart" log events if stagnation threshold is hit
    - "Total restarts: N" line at the end (N >= 0)
    - "Generations:    G" line (G may be up to 200 * (N+1))
    - "Best fitness:   F.FFFFFF" line with a finite number
    - No panic; process exits 0

    Note: Restarts are NOT guaranteed on every run (the engine may converge before stagnation threshold). "Total restarts: 0" is also valid if the engine converges cleanly. The important check is that the run completes, output is finite, and the LogObserver output is readable.
  </how-to-verify>
  <resume-signal>Type "approved" if the example runs and produces finite output. Describe any panic or unexpected behavior to investigate.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Phase 59 verification gate — full CI suite</name>
  <files></files>
  <read_first>
    - .planning/phases/59-restart-strategies-ipop-bipop/59-VALIDATION.md (full suite command and sign-off checklist)
  </read_first>
  <action>
    Run the full phase verification gate in sequence. For each command, if it fails, diagnose and fix before proceeding to the next:

    1. cargo test
    2. cargo test --features serde
    3. cargo clippy --all-targets -- -D warnings
    4. cargo doc --no-deps
    5. cargo check --target wasm32-unknown-unknown

    Common issues to watch for:
    - clippy: unused variable warnings in the restart loop (use _ prefix or address the warning)
    - rustdoc: missing doc on RestartStrategy, RestartEvent, RestartKind public items (all fields must have /// comments per project rules)
    - WASM: if any new code accidentally introduces Instant or par_iter, gate with #[cfg(not(target_arch = "wasm32"))]
    - serde: CmaResult now has total_restarts field; if CmaResult derives Serialize/Deserialize (check — it may not), add the field to any test fixtures. Check tests/engines/cma/test_cma.rs for serde-gated tests.

    After all commands pass: update VALIDATION.md frontmatter to nyquist_compliant: true and wave_0_complete: true and status: complete.
  </action>
  <verify>
    <automated>cargo test && cargo test --features serde && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps && cargo check --target wasm32-unknown-unknown && echo "ALL_GATES_PASSED"</automated>
  </verify>
  <done>All five CI gates pass; VALIDATION.md updated to nyquist_compliant: true; "ALL_GATES_PASSED" printed by the verify command chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example output → user | Best fitness and restart count are printed; both are engine-internal values with no user-controlled interpretation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06 | Denial of Service | ipop_rastrigin running indefinitely | mitigate | assert!(result.best_fitness.is_finite()) as a compile-verified termination check; max_restarts=3 bounds total work |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies added in this phase |
</threat_model>

<verification>
All CI gates must be green before this plan is considered complete:

```
cargo test && cargo test --features serde
cargo clippy --all-targets -- -D warnings
cargo doc --no-deps
cargo check --target wasm32-unknown-unknown
cargo run --release --example ipop_rastrigin   (human-verified)
```
</verification>

<success_criteria>
1. examples/ipop_rastrigin.rs runs without panic and prints Total restarts / Generations / Best fitness
2. result.best_fitness is finite (assert in example)
3. cargo check --target wasm32-unknown-unknown passes (SC-4 from ROADMAP)
4. cargo test (including all CMA-01 through CMA-17) passes
5. cargo test --features serde passes
6. cargo clippy --all-targets -- -D warnings passes with zero warnings
7. cargo doc --no-deps produces zero warnings
8. VALIDATION.md updated to nyquist_compliant: true
</success_criteria>

<output>
Create `.planning/phases/59-restart-strategies-ipop-bipop/59-03-SUMMARY.md` when done
</output>
