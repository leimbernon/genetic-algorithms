---
phase: 63-visualization-pareto-front-plotting-example-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/observe/visualization/mod.rs
  - tests/observe/visualization/test_visualization.rs
  - .github/workflows/wasm-check.yml
autonomous: true
requirements: []
tags: [visualization, plotters, pareto, wasm]

must_haves:
  truths:
    - "plotters dep in Cargo.toml includes the point_series feature"
    - "plot_pareto_front_2d renders a PNG/SVG scatter chart from &[(f64,f64)]"
    - "plot_pareto_front_3d renders a 1200x400 PNG/SVG with three side-by-side scatter panels (f1xf2, f1xf3, f2xf3) from &[(f64,f64,f64)]"
    - "plot_true_fitness_calls renders a PNG/SVG line chart from &[GenerationStats] using GenerationStats.true_fitness_calls"
    - "All four plot_* functions (existing plot_fitness, plot_diversity, plot_histogram plus the three new ones) gate their BitMapBackend PNG branch with cfg(not(target_arch = wasm32)) and return VisualizationError::UnsupportedFormat for PNG on wasm32"
    - "cargo check --target wasm32-unknown-unknown --lib --features visualization passes in CI"
    - "Empty / <2-point inputs return VisualizationError::InsufficientData"
  artifacts:
    - path: "Cargo.toml"
      provides: "plotters features list with point_series"
      contains: "point_series"
    - path: "src/observe/visualization/mod.rs"
      provides: "plot_pareto_front_2d, plot_pareto_front_3d, plot_true_fitness_calls public fns; compute_pareto_range helper; WASM-gated PNG branches in all plot_* fns"
      contains: "pub fn plot_pareto_front_2d"
    - path: "tests/observe/visualization/test_visualization.rs"
      provides: "Unit tests for the three new functions + InsufficientData error cases"
      contains: "test_plot_pareto_front_2d_png"
    - path: ".github/workflows/wasm-check.yml"
      provides: "cargo check step with --features visualization on wasm32 target"
      contains: "visualization"
  key_links:
    - from: "src/observe/visualization/mod.rs::plot_pareto_front_2d"
      to: "BitMapBackend / SVGBackend"
      via: "extension dispatch + cfg gate"
      pattern: "BitMapBackend::new"
    - from: "src/observe/visualization/mod.rs::plot_true_fitness_calls"
      to: "GenerationStats.true_fitness_calls"
      via: "filter_map on Option<u64>"
      pattern: "true_fitness_calls"
    - from: "tests/observe/visualization/test_visualization.rs"
      to: "genetic_algorithms::visualization::plot_pareto_front_2d"
      via: "use import + fn call"
      pattern: "plot_pareto_front_2d"
---

<objective>
Extend the `visualization` module with three new public functions (`plot_pareto_front_2d`, `plot_pareto_front_3d`, `plot_true_fitness_calls`), enable the missing `point_series` plotters feature, add WASM cfg gates around every `BitMapBackend` use (existing + new), expand the integration test suite, and add a `--features visualization` step to the wasm32 CI workflow.

Purpose: Deliver the library-side of Phase 63 in a single wave so the example/README work in Plan 02 and Plan 03 runs against a stable, WASM-clean API.

Output: Updated `Cargo.toml`, extended `src/observe/visualization/mod.rs`, expanded `tests/observe/visualization/test_visualization.rs`, updated `.github/workflows/wasm-check.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-CONTEXT.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-RESEARCH.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-VALIDATION.md

@src/observe/visualization/mod.rs
@src/stats.rs
@tests/observe/visualization/test_visualization.rs
@Cargo.toml
@.github/workflows/wasm-check.yml

<interfaces>
From src/observe/visualization/mod.rs (existing):
- pub enum VisualizationError { DrawingError(String), IoError(String), UnsupportedFormat, InsufficientData }
- pub fn plot_fitness(stats: &[GenerationStats], path: &str) -> Result<(), VisualizationError>
- pub fn plot_diversity(stats: &[GenerationStats], path: &str) -> Result<(), VisualizationError>
- pub fn plot_histogram(fitness_values: &[f64], path: &str) -> Result<(), VisualizationError>
- fn compute_y_range(stats: &[GenerationStats]) -> (f64, f64)
- fn draw_fitness_chart<DB>(root: &DrawingArea<DB, Shift>, stats: &[GenerationStats]) -> Result<(), DrawingAreaErrorKind<DB::ErrorType>>

From src/stats.rs:
- pub struct GenerationStats with field true_fitness_calls: Option<u64>; derives Clone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo.toml point_series feature, WASM cfg gates on existing PNG branches, test scaffolding, and wasm-check CI step</name>
  <files>Cargo.toml, src/observe/visualization/mod.rs, tests/observe/visualization/test_visualization.rs, .github/workflows/wasm-check.yml</files>
  <read_first>
    - Cargo.toml (line 50 plotters dependency line)
    - src/observe/visualization/mod.rs (full file)
    - tests/observe/visualization/test_visualization.rs (full file)
    - src/stats.rs (confirm GenerationStats fields, especially true_fitness_calls: Option<u64> and derive(Clone))
    - .github/workflows/wasm-check.yml (existing serde step pattern to copy)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md (Shared Patterns section)
  </read_first>
  <behavior>
    - Cargo.toml plotters features list contains "point_series" inserted after "line_series".
    - For every public plot_* function in mod.rs (plot_fitness, plot_diversity, plot_histogram), the Some("png") arm wraps BitMapBackend usage inside cfg(not(target_arch = "wasm32")) and adds a sibling cfg(target_arch = "wasm32") block returning VisualizationError::UnsupportedFormat.
    - wasm-check.yml has a new "cargo check (visualization feature)" step that runs cargo check --target wasm32-unknown-unknown --lib --features visualization.
    - test_visualization.rs imports plot_pareto_front_2d, plot_pareto_front_3d, plot_true_fitness_calls and contains seven #[ignore]-gated test stubs.
    - make_stats helper includes true_fitness_calls: None if the GenerationStats struct literal needs it; new make_stats_with_surrogate helper sets Some(50 + i as u64).
    - cargo test --features visualization passes with stubs ignored.
    - cargo check --target wasm32-unknown-unknown --lib --features visualization passes.
  </behavior>
  <action>
    Cargo.toml: on the existing plotters line (line ~50), insert the literal string "point_series" into the features list between "line_series" and "histogram". Do not change any other field of that dependency line.

    src/observe/visualization/mod.rs: locate the three existing public functions plot_fitness, plot_diversity, plot_histogram. In each, find the Some("png") arm of the extension match. Wrap the entire current body of that arm in #[cfg(not(target_arch = "wasm32"))] { ... } and add a sibling #[cfg(target_arch = "wasm32")] { return Err(VisualizationError::UnsupportedFormat); } block. Do NOT modify Some("svg") arms. Do NOT alter signatures, the InsufficientData guard, or behavior on non-WASM targets. Do NOT add the new public functions yet — Task 2 owns them.

    .github/workflows/wasm-check.yml: after the existing "cargo check (serde feature)" step, append a new step with name "cargo check (visualization feature)" and run command "cargo check --target wasm32-unknown-unknown --lib --features visualization". Match indentation of surrounding steps exactly.

    tests/observe/visualization/test_visualization.rs:
    1. Extend the existing use genetic_algorithms::visualization::{ ... } import list to also import plot_pareto_front_2d, plot_pareto_front_3d, plot_true_fitness_calls. (These symbols don't exist until Task 2 — the test stubs are #[ignore]-gated so this compiles after Task 2 lands, but imports of yet-to-exist items will fail to compile NOW. Therefore: also leave the new symbol imports out for Task 1; add them in Task 2. In Task 1, only add the make_stats audit, the new make_stats_with_surrogate helper, and the seven test stubs as plain placeholder #[test] #[ignore] functions with unimplemented!() bodies that DO NOT reference the new functions yet — keep their bodies as `let _ = "stub";` to compile cleanly.)
    2. Audit the existing make_stats helper: if the GenerationStats struct literal omits true_fitness_calls, add true_fitness_calls: None to every constructed entry. If the helper already uses ..Default::default() and compiles, leave it.
    3. Add a new helper make_stats_with_surrogate(n: usize) -> Vec<GenerationStats> that returns n entries where each has true_fitness_calls: Some(50 + i as u64) and other fields match make_stats conventions.
    4. Add seven #[test] #[ignore] stubs named: test_plot_pareto_front_2d_png, test_plot_pareto_front_3d_png, test_plot_true_fitness_calls_png, test_pareto_2d_insufficient_empty, test_pareto_2d_insufficient_single, test_pareto_3d_insufficient_empty, test_true_fitness_calls_insufficient_all_none. Bodies in Task 1 are placeholder `let _ = "stub";` — they are activated in Task 2.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && cargo build --features visualization && cargo test --features visualization --test test_visualization && cargo check --target wasm32-unknown-unknown --lib --features visualization</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'point_series' Cargo.toml returns the plotters line containing "point_series".
    - In src/observe/visualization/mod.rs filtered with `grep -v '^[[:space:]]*//'`: count of `cfg(not(target_arch = "wasm32"))` is >= 3 (one per existing PNG branch) AND count of `cfg(target_arch = "wasm32")` is >= 3.
    - grep -n 'visualization feature' .github/workflows/wasm-check.yml returns the new step name.
    - cargo build --features visualization exits 0.
    - cargo check --target wasm32-unknown-unknown --lib --features visualization exits 0.
    - tests/observe/visualization/test_visualization.rs contains all seven listed #[ignore] stub function names (verifiable via `grep -c '#\[ignore\]' tests/observe/visualization/test_visualization.rs` >= 7) and a make_stats_with_surrogate function definition.
    - cargo test --features visualization exits 0.
  </acceptance_criteria>
  <done>Cargo.toml has point_series; all three existing PNG branches in mod.rs are WASM-gated; wasm-check.yml has a --features visualization step that passes locally; test file has make_stats_with_surrogate plus seven #[ignore] stubs; full test + wasm32 lib+visualization checks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement plot_pareto_front_2d, plot_pareto_front_3d, plot_true_fitness_calls and activate test stubs</name>
  <files>src/observe/visualization/mod.rs, src/lib.rs, tests/observe/visualization/test_visualization.rs</files>
  <read_first>
    - src/observe/visualization/mod.rs (current state after Task 1 — confirm WASM gates exist on existing fns; copy the helper/dispatcher pattern from draw_fitness_chart + plot_fitness verbatim)
    - src/lib.rs (existing visualization re-export pattern, if any)
    - tests/observe/visualization/test_visualization.rs (Task 1 stubs to activate)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md (Layer 1/2a/2b pattern blocks)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-RESEARCH.md (Patterns 1, 2, 3, 5; Pitfalls 2, 5)
  </read_first>
  <behavior>
    - plot_pareto_front_2d(&[(0.0,1.0),(0.5,0.5),(1.0,0.0)], "<tempdir>/test.png") returns Ok(()) and produces a non-empty PNG with axis descriptions f1 and f2.
    - plot_pareto_front_2d(&[], path) returns Err(VisualizationError::InsufficientData); single-element input also returns InsufficientData (matching plot_fitness < 2 guard).
    - plot_pareto_front_2d with a ".txt" extension returns Err(VisualizationError::UnsupportedFormat).
    - plot_pareto_front_3d(&[(0,0,1),(1,1,0)] as f64 tuples, path.png) returns Ok(()) and produces a 1200x400 PNG with three side-by-side scatter panels labeled f1/f2, f1/f3, f2/f3.
    - plot_pareto_front_3d(&[], path) returns Err(VisualizationError::InsufficientData).
    - plot_true_fitness_calls(make_stats_with_surrogate(5).as_slice(), path.png) returns Ok(()) and writes a non-empty PNG line chart of (generation, value).
    - plot_true_fitness_calls on stats where every true_fitness_calls is None returns Err(VisualizationError::InsufficientData).
    - Degenerate clustered fronts (x_min ≈ x_max) do not panic — axis range is expanded by 1.0.
    - All three new PNG branches wrap BitMapBackend in cfg(not(target_arch = "wasm32")) with sibling cfg(target_arch = "wasm32") returning UnsupportedFormat.
    - cargo test --features visualization runs all seven previously-ignored tests and they pass.
    - cargo check --target wasm32-unknown-unknown --lib --features visualization passes.
    - cargo clippy --features visualization --all-targets -- -D warnings passes.
  </behavior>
  <action>
    In src/observe/visualization/mod.rs append three new public functions, each with its private draw_*_chart<DB> helper, following the existing compute_y_range + draw_fitness_chart<DB> + plot_fitness three-layer structure.

    1. Helper fn compute_pareto_range(iter: impl Iterator<Item = f64>) -> (f64, f64). Iterate once tracking min/max with f64::INFINITY / f64::NEG_INFINITY sentinels. If (max - min).abs() < f64::EPSILON, set max = min + 1.0. Used by both pareto fns for x and y axes and by each panel of the 3d chart.

    2. fn draw_pareto_2d_chart<DB>(root: &DrawingArea<DB, Shift>, points: &[(f64, f64)]) -> Result<(), DrawingAreaErrorKind<DB::ErrorType>>, generic bounds identical to draw_fitness_chart. Body: compute x_range from points.iter().map(|p| p.0) and y_range from .map(|p| p.1) via compute_pareto_range; ChartBuilder::on(root).margin(10).x_label_area_size(30).y_label_area_size(30).build_cartesian_2d(x_range.0..x_range.1, y_range.0..y_range.1)?; chart.configure_mesh().disable_mesh().x_desc("f1").y_desc("f2").draw()?; chart.draw_series(points.iter().map(|&(x,y)| Circle::new((x,y), 3, BLUE.filled())))?; Ok(()).

    3. pub fn plot_pareto_front_2d(points: &[(f64, f64)], path: &str) -> Result<(), VisualizationError>. Returns InsufficientData if points.len() < 2. Dispatches on Path::new(path).extension() to Some("png") / Some("svg") / _ => UnsupportedFormat. PNG arm: wrapped in #[cfg(not(target_arch = "wasm32"))] block using BitMapBackend::new(path, (800, 600)).into_drawing_area(); three-step fill(&WHITE) -> draw_pareto_2d_chart(&root, points) -> root.present(), each .map_err mapped to VisualizationError::DrawingError(format!("{:?}", e)); sibling #[cfg(target_arch = "wasm32")] block returns Err(VisualizationError::UnsupportedFormat). SVG arm: same three-step with SVGBackend::new(path, (800, 600)), no cfg gate.

    4. fn draw_pareto_3d_chart<DB>(root: &DrawingArea<DB, Shift>, points: &[(f64, f64, f64)]) -> Result<(), DrawingAreaErrorKind<DB::ErrorType>>. Body: let panels = root.split_evenly((1, 3)); iterate by index for i in 0..panels.len() (no destructuring move). For i=0 use axis indices (0,1) with labels ("f1","f2"); i=1 -> (0,2) ("f1","f3"); i=2 -> (1,2) ("f2","f3"). Per panel: compute x and y ranges via compute_pareto_range on the appropriate coordinate; build a ChartBuilder on &panels[i] with margin(10), x_label_area_size(30), y_label_area_size(30), x_desc and y_desc set to the panel's labels; draw_series of Circle::new((px, py), 3, BLUE.filled()).

    5. pub fn plot_pareto_front_3d(points: &[(f64, f64, f64)], path: &str) -> Result<(), VisualizationError>. Same dispatcher shape as plot_pareto_front_2d, image dimensions (1200, 400) for both PNG and SVG arms.

    6. fn draw_true_fitness_calls_chart<DB>(root: &DrawingArea<DB, Shift>, data: &[(usize, u64)]) -> Result<(), DrawingAreaErrorKind<DB::ErrorType>>. Body: compute x_range from data.iter().map(|&(g,_)| g) (use 0usize..max_gen + 1), y_range from data.iter().map(|&(_,v)| v as f64) via compute_pareto_range; ChartBuilder with x_label_area_size(30) y_label_area_size(40); configure_mesh().disable_mesh().x_desc("Generation").y_desc("True fitness calls").draw()?; chart.draw_series(LineSeries::new(data.iter().map(|&(g,v)| (g, v as f64)), &MAGENTA))?.

    7. pub fn plot_true_fitness_calls(stats: &[GenerationStats], path: &str) -> Result<(), VisualizationError>. Body: let data: Vec<(usize, u64)> = stats.iter().filter_map(|s| s.true_fitness_calls.map(|v| (s.generation, v))).collect(); if data.len() < 2 return Err(VisualizationError::InsufficientData); dispatch PNG/SVG with WASM-gated PNG branch and image dimensions (800, 600).

    src/lib.rs: if existing visualization re-exports list individual function names (e.g. pub use crate::observe::visualization::{plot_fitness, plot_diversity, plot_histogram, VisualizationError}), add plot_pareto_front_2d, plot_pareto_front_3d, plot_true_fitness_calls to that list. If the module is only re-exported as a path (pub use crate::observe::visualization), no change needed beyond Task 1 state.

    tests/observe/visualization/test_visualization.rs:
    - Extend the use genetic_algorithms::visualization::{ ... } import to include plot_pareto_front_2d, plot_pareto_front_3d, plot_true_fitness_calls (now valid since they exist).
    - Remove the #[ignore] attribute from each of the seven Task-1 stubs.
    - test_plot_pareto_front_2d_png: let points: Vec<(f64,f64)> = (0..10).map(|i| (i as f64 * 0.1, 1.0 - i as f64 * 0.1)).collect(); path = std::env::temp_dir().join("test_viz_pareto2d.png"); remove_file ignore; call plot_pareto_front_2d, assert Ok, assert path exists and metadata len > 0; cleanup.
    - test_plot_pareto_front_3d_png: same shape with Vec<(f64,f64,f64)> fixture; path "test_viz_pareto3d.png"; call plot_pareto_front_3d, assert Ok, file non-empty.
    - test_plot_true_fitness_calls_png: stats = make_stats_with_surrogate(5); path "test_viz_tfc.png"; assert Ok, file non-empty.
    - test_pareto_2d_insufficient_empty: let p: Vec<(f64,f64)> = vec![]; assert matches!(plot_pareto_front_2d(&p, path_str), Err(VisualizationError::InsufficientData)).
    - test_pareto_2d_insufficient_single: vec![(0.0, 0.0)]; same Err(InsufficientData) assertion.
    - test_pareto_3d_insufficient_empty: vec![] (Vec<(f64,f64,f64)>); same.
    - test_true_fitness_calls_insufficient_all_none: stats vector where every entry has true_fitness_calls: None; assert Err(InsufficientData).
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && cargo test --features visualization --test test_visualization && cargo clippy --features visualization --all-targets -- -D warnings && cargo check --target wasm32-unknown-unknown --lib --features visualization</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'pub fn plot_pareto_front_2d' src/observe/visualization/mod.rs returns one definition line.
    - grep -n 'pub fn plot_pareto_front_3d' returns one definition line.
    - grep -n 'pub fn plot_true_fitness_calls' returns one definition line.
    - grep -c '#\[ignore\]' tests/observe/visualization/test_visualization.rs returns 0 (all seven stubs activated).
    - cargo test --features visualization --test test_visualization runs exactly the seven new tests plus all pre-existing tests, all green.
    - cargo clippy --features visualization --all-targets -- -D warnings exits 0.
    - cargo check --target wasm32-unknown-unknown --lib --features visualization exits 0.
    - After running tests, the temp PNG files exist with non-zero size during the test and are cleaned up afterwards (verified in test body).
  </acceptance_criteria>
  <done>Three new public functions implemented with WASM-gated PNG branches, all seven test stubs activated and passing, clippy clean, wasm32 lib+visualization check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> visualization fn | Caller passes pre-computed f64 tuples and a filesystem path. No network input, no untrusted data. |
| visualization fn -> filesystem | Library writes to the path argument via plotters backends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Denial of Service | plot_pareto_front_2d/3d with degenerate (clustered) inputs | mitigate | compute_pareto_range expands (max - min) < f64::EPSILON cases by +1.0 to prevent plotters panic on zero-width axis |
| T-63-02 | Information Disclosure | path argument controls filesystem write target | accept | Library users control the path; no untrusted-caller scenario in a library used as an in-process dependency |
| T-63-03 | Tampering | plotters dependency (existing) feature list mutation | accept | No new packages added; only a feature flag added to the existing plotters 0.3.7 dep already pre-audited and in use |
| T-63-04 | Denial of Service | WASM target panic on BitMapBackend usage | mitigate | cfg(not(target_arch = "wasm32")) gates on every PNG branch return UnsupportedFormat instead of attempting bitmap render |
</threat_model>

<verification>
- cargo build --features visualization (after Task 1)
- cargo test --features visualization (after each task)
- cargo clippy --features visualization --all-targets -- -D warnings (after Task 2)
- cargo check --target wasm32-unknown-unknown --lib --features visualization (after both tasks)
- cargo doc --no-deps --features visualization (rustdoc clean — Phase 63 requirement under CLAUDE.md)
</verification>

<success_criteria>
- Plan 02 and Plan 03 can call plot_pareto_front_2d, plot_pareto_front_3d, plot_fitness, plot_true_fitness_calls without further library changes.
- All seven new unit tests pass.
- WASM CI step green for the visualization feature.
- Zero clippy warnings, zero rustdoc warnings.
</success_criteria>

<output>
Create `.planning/phases/63-visualization-pareto-front-plotting-example-images/63-01-SUMMARY.md` when done.
</output>
