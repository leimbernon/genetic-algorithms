---
phase: 63-visualization-pareto-front-plotting-example-images
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - examples/nsga2_zdt1.rs
  - examples/spea2_zdt1.rs
  - examples/sms_emoa_zdt1.rs
  - examples/ibea_zdt1.rs
  - examples/nsga3_dtlz2.rs
  - examples/rastrigin.rs
autonomous: true
requirements: []
tags: [visualization, examples, pareto]

must_haves:
  truths:
    - "Running `cargo run --example nsga2_zdt1 --features visualization -- --plot` writes docs/images/nsga2_zdt1.png"
    - "Running `cargo run --example spea2_zdt1 --features visualization -- --plot` writes docs/images/spea2_zdt1.png"
    - "Running `cargo run --example sms_emoa_zdt1 --features visualization,benchmarks -- --plot` writes docs/images/sms_emoa_zdt1.png"
    - "Running `cargo run --example ibea_zdt1 --features visualization,benchmarks -- --plot` writes docs/images/ibea_zdt1.png"
    - "Running `cargo run --example nsga3_dtlz2 --features visualization -- --plot` writes docs/images/nsga3_dtlz2.png"
    - "Running `cargo run --example rastrigin --features visualization -- --plot` writes docs/images/rastrigin.png"
    - "Without --plot, every example behaves exactly as it did before this plan (output text unchanged; no PNG written)"
    - "Each example without the visualization feature still compiles and runs (--plot block silently skipped via cfg)"
  artifacts:
    - path: "examples/nsga2_zdt1.rs"
      provides: "--plot block emitting docs/images/nsga2_zdt1.png"
      contains: "plot_pareto_front_2d"
    - path: "examples/spea2_zdt1.rs"
      provides: "--plot block emitting docs/images/spea2_zdt1.png"
      contains: "plot_pareto_front_2d"
    - path: "examples/sms_emoa_zdt1.rs"
      provides: "--plot block emitting docs/images/sms_emoa_zdt1.png"
      contains: "plot_pareto_front_2d"
    - path: "examples/ibea_zdt1.rs"
      provides: "--plot block emitting docs/images/ibea_zdt1.png"
      contains: "plot_pareto_front_2d"
    - path: "examples/nsga3_dtlz2.rs"
      provides: "--plot block emitting docs/images/nsga3_dtlz2.png using plot_pareto_front_3d"
      contains: "plot_pareto_front_3d"
    - path: "examples/rastrigin.rs"
      provides: "--plot block accumulating GenerationStats and emitting docs/images/rastrigin.png via plot_fitness"
      contains: "plot_fitness"
  key_links:
    - from: "examples/nsga2_zdt1.rs --plot block"
      to: "genetic_algorithms::visualization::plot_pareto_front_2d"
      via: "fully-qualified call"
      pattern: "plot_pareto_front_2d"
    - from: "examples/nsga3_dtlz2.rs --plot block"
      to: "genetic_algorithms::visualization::plot_pareto_front_3d"
      via: "fully-qualified call"
      pattern: "plot_pareto_front_3d"
    - from: "examples/rastrigin.rs callback"
      to: "Arc<Mutex<Vec<GenerationStats>>> accumulator -> plot_fitness"
      via: "stats.clone() push inside callback"
      pattern: "Arc::new(Mutex::new"
---

<objective>
Add a `--plot` block (gated with `#[cfg(feature = "visualization")]`) to each of six examples so users can generate a Pareto front PNG or fitness-progress PNG by re-running the example with `--features visualization` and `-- --plot`. All multi-objective examples reuse `plot_pareto_front_2d` / `plot_pareto_front_3d` from Plan 01; the single-objective `rastrigin.rs` reuses the existing `plot_fitness`.

Purpose: Make the library's multi-objective and single-objective examples self-documenting via PNG output. These examples are the source of the images Plan 03 commits and links.

Output: Six modified example files, each with an additive `--plot` block. No behavioral change to existing example output when `--plot` is absent or the visualization feature is off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-CONTEXT.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md
@.planning/phases/63-visualization-pareto-front-plotting-example-images/63-01-PLAN.md

@examples/nsga2_zdt1.rs
@examples/spea2_zdt1.rs
@examples/sms_emoa_zdt1.rs
@examples/ibea_zdt1.rs
@examples/nsga3_dtlz2.rs
@examples/rastrigin.rs

<interfaces>
After Plan 01:
- genetic_algorithms::visualization::plot_pareto_front_2d(points: &[(f64,f64)], path: &str) -> Result<(), VisualizationError>
- genetic_algorithms::visualization::plot_pareto_front_3d(points: &[(f64,f64,f64)], path: &str) -> Result<(), VisualizationError>
- genetic_algorithms::visualization::plot_fitness(stats: &[GenerationStats], path: &str) -> Result<(), VisualizationError>

ParetoFront access (verified in PATTERNS.md):
- front.individuals: Vec<Individual>
- ind.objectives: Vec<f64>   // index 0,1 for 2-obj; 0,1,2 for nsga3_dtlz2

rastrigin callback signature (verified):
- run_with_callback(Some(|gen, pop, stats: &GenerationStats, cause| -> ControlFlow<()> { ... }), interval)
- stats is passed by reference per generation; caller must accumulate manually.
- GenerationStats derives Clone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --plot blocks to the four 2-objective NSGA-II-family examples</name>
  <files>examples/nsga2_zdt1.rs, examples/spea2_zdt1.rs, examples/sms_emoa_zdt1.rs, examples/ibea_zdt1.rs</files>
  <read_first>
    - examples/nsga2_zdt1.rs (full file — identify the Ok(mut front) arm and the final print loop; the --plot block goes after the print loop, before the arm's closing brace)
    - examples/spea2_zdt1.rs (locate same Ok(mut front) arm)
    - examples/sms_emoa_zdt1.rs (locate same Ok(mut front) arm; note required-features = ["benchmarks"] in Cargo.toml)
    - examples/ibea_zdt1.rs (same as sms_emoa)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md (the example-specific --plot block snippets)
    - Cargo.toml (verify [[example]] entries for sms_emoa_zdt1 and ibea_zdt1 carry required-features = ["benchmarks"]; do not modify Cargo.toml)
  </read_first>
  <action>
    In each of the four files, locate the match arm where the engine's run() returns Ok(mut front) (or equivalent — the existing code prints front individuals/objectives). Immediately after the existing per-individual print loop and before that arm's closing brace, append a #[cfg(feature = "visualization")] gated if block.

    Block shape (concrete output path is the only per-file difference):

    #[cfg(feature = "visualization")]
    if std::env::args().any(|a| a == "--plot") {
        // requires --features visualization (sms_emoa/ibea: --features "visualization,benchmarks")
        let points: Vec<(f64, f64)> = front.individuals.iter()
            .map(|ind| (ind.objectives[0], ind.objectives[1]))
            .collect();
        std::fs::create_dir_all("docs/images").expect("failed to create docs/images");
        genetic_algorithms::visualization::plot_pareto_front_2d(
            &points,
            "docs/images/<EXAMPLE>.png",
        )
        .expect("plot failed");
        println!("Pareto front plot saved to docs/images/<EXAMPLE>.png");
    }

    Concrete <EXAMPLE> values per file (literal output paths — do NOT substitute):
    - examples/nsga2_zdt1.rs -> "docs/images/nsga2_zdt1.png"
    - examples/spea2_zdt1.rs -> "docs/images/spea2_zdt1.png"
    - examples/sms_emoa_zdt1.rs -> "docs/images/sms_emoa_zdt1.png" (comment must mention `--features "visualization,benchmarks"`)
    - examples/ibea_zdt1.rs -> "docs/images/ibea_zdt1.png" (comment must mention `--features "visualization,benchmarks"`)

    Do NOT modify any other code in these examples. Do NOT change Cargo.toml. Do NOT add any new use imports — use the fully-qualified `genetic_algorithms::visualization::plot_pareto_front_2d` path. If `front` is referenced by `mut front` in the existing arm but the new block only reads it, that is fine — no mut requirement is added.

    Compile-check by running each example without --plot (so it executes existing behavior unchanged) and once with --plot for nsga2_zdt1.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && cargo build --example nsga2_zdt1 --features visualization && cargo build --example spea2_zdt1 --features visualization && cargo build --example sms_emoa_zdt1 --features "visualization,benchmarks" && cargo build --example ibea_zdt1 --features "visualization,benchmarks" && cargo build --example nsga2_zdt1 && cargo build --example spea2_zdt1</automated>
  </verify>
  <acceptance_criteria>
    - grep -l 'docs/images/nsga2_zdt1.png' examples/nsga2_zdt1.rs returns the filename.
    - grep -l 'docs/images/spea2_zdt1.png' examples/spea2_zdt1.rs returns the filename.
    - grep -l 'docs/images/sms_emoa_zdt1.png' examples/sms_emoa_zdt1.rs returns the filename.
    - grep -l 'docs/images/ibea_zdt1.png' examples/ibea_zdt1.rs returns the filename.
    - grep -c 'plot_pareto_front_2d' on each of the four files returns 1.
    - grep -c 'cfg(feature = "visualization")' on each of the four files returns at least 1.
    - cargo build --example <name> succeeds for each of the four examples both with and without --features visualization (no compile errors when feature is off — cfg-gated block must compile away cleanly).
    - cargo build --example sms_emoa_zdt1 --features "visualization,benchmarks" and same for ibea_zdt1 succeed.
  </acceptance_criteria>
  <done>Four examples have additive --plot blocks. Each writes to the correct docs/images/<example>.png path. Examples build with and without the visualization feature.</done>
</task>

<task type="auto">
  <name>Task 2: Add --plot block to nsga3_dtlz2 (3-objective) and rastrigin (single-objective with stats accumulator)</name>
  <files>examples/nsga3_dtlz2.rs, examples/rastrigin.rs</files>
  <read_first>
    - examples/nsga3_dtlz2.rs (full file — locate Ok(mut front) arm; objectives access uses .objectives[0..3])
    - examples/rastrigin.rs (full file — locate run_with_callback call and Ok(population) arm; the callback closure captures must allow stats accumulation)
    - .planning/phases/63-visualization-pareto-front-plotting-example-images/63-PATTERNS.md (nsga3_dtlz2 and rastrigin example-specific snippets)
    - src/stats.rs (confirm GenerationStats derives Clone)
  </read_first>
  <action>
    examples/nsga3_dtlz2.rs:
    In the Ok(mut front) arm, after the existing print loop and before the arm's closing brace, append:

    #[cfg(feature = "visualization")]
    if std::env::args().any(|a| a == "--plot") {
        // requires --features visualization
        let points: Vec<(f64, f64, f64)> = front.individuals.iter()
            .map(|ind| (ind.objectives[0], ind.objectives[1], ind.objectives[2]))
            .collect();
        std::fs::create_dir_all("docs/images").expect("failed to create docs/images");
        genetic_algorithms::visualization::plot_pareto_front_3d(
            &points,
            "docs/images/nsga3_dtlz2.png",
        )
        .expect("plot failed");
        println!("Pareto front plot saved to docs/images/nsga3_dtlz2.png");
    }

    examples/rastrigin.rs:
    BEFORE the existing `let result = ga.run_with_callback(...)` call, add a stats accumulator gated by cfg(feature = "visualization"):

    #[cfg(feature = "visualization")]
    let plot_stats: std::sync::Arc<std::sync::Mutex<Vec<genetic_algorithms::stats::GenerationStats>>> =
        std::sync::Arc::new(std::sync::Mutex::new(Vec::new()));
    #[cfg(feature = "visualization")]
    let plot_stats_clone = plot_stats.clone();

    Modify the existing callback closure passed into run_with_callback: at the END of the closure body, just before returning ControlFlow::Continue(()), append a cfg-gated push:

    #[cfg(feature = "visualization")]
    plot_stats_clone.lock().unwrap().push(_stats.clone());

    If the existing closure binding is `|_stats: &GenerationStats|`, leave the underscore-prefix in place (the push reads through the reference, no warning).
    If the closure captures plot_stats_clone, use `move` on the closure — if `move` is not already on the closure signature, add it (any captured references in the existing closure body that need to live after move must be cloned beforehand — verify by reading the current closure body; if it captures any non-Copy items by reference, hoist a `.clone()` outside the move closure). Document in a single comment line that move was added for accumulator capture.

    In the Ok(population) arm, after the existing post-run print, append:

    #[cfg(feature = "visualization")]
    if std::env::args().any(|a| a == "--plot") {
        // requires --features visualization
        let stats = plot_stats.lock().unwrap();
        std::fs::create_dir_all("docs/images").expect("failed to create docs/images");
        genetic_algorithms::visualization::plot_fitness(
            &stats,
            "docs/images/rastrigin.png",
        )
        .expect("plot failed");
        println!("Fitness plot saved to docs/images/rastrigin.png");
    }

    Constraint: when the visualization feature is OFF, the file must still compile unchanged — Arc/Mutex bindings and the callback push are all cfg-gated. The closure's `move` keyword (if newly added) is unconditional; verify nothing captured by reference is needed past the move.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && cargo build --example nsga3_dtlz2 --features visualization && cargo build --example rastrigin --features visualization && cargo build --example nsga3_dtlz2 && cargo build --example rastrigin && cargo clippy --example nsga3_dtlz2 --features visualization -- -D warnings && cargo clippy --example rastrigin --features visualization -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'plot_pareto_front_3d' examples/nsga3_dtlz2.rs returns 1.
    - grep -c 'docs/images/nsga3_dtlz2.png' examples/nsga3_dtlz2.rs returns at least 1.
    - grep -c 'plot_fitness' examples/rastrigin.rs returns at least 1.
    - grep -c 'docs/images/rastrigin.png' examples/rastrigin.rs returns at least 1.
    - grep -c 'Arc::new(Mutex::new' examples/rastrigin.rs returns at least 1 OR the file uses Arc<Mutex<...>> equivalently; verify Mutex import resolves.
    - cargo build --example nsga3_dtlz2 succeeds with and without --features visualization.
    - cargo build --example rastrigin succeeds with and without --features visualization.
    - cargo clippy on both examples with --features visualization passes with -D warnings.
  </acceptance_criteria>
  <done>nsga3_dtlz2 emits a 3-panel PNG; rastrigin accumulates GenerationStats inside its callback and emits a fitness PNG. Both examples compile cleanly with and without the visualization feature; clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example CLI -> filesystem | Examples write to a relative path `docs/images/<name>.png` from the working directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05 | Denial of Service | --plot panics if docs/images cannot be created or written | accept | Examples are user-run demos; `.expect("failed to create docs/images")` produces an actionable panic message |
| T-63-06 | Tampering | example writes to a relative directory | accept | Examples document `cargo run --example` from repo root; no untrusted-path scenario |
</threat_model>

<verification>
- cargo build for each modified example with and without --features visualization
- cargo clippy on each modified example
- Smoke run of nsga2_zdt1 with --plot to confirm PNG is produced (Plan 03 owns the final image-generation runs)
</verification>

<success_criteria>
- Six examples have additive, cfg-gated --plot blocks.
- Without --plot, examples produce the same stdout as before.
- With --plot and the right features, the expected docs/images/<name>.png file is produced.
- All examples build with `cargo build --example <name>` both with and without the visualization feature.
- Plan 03 can now run each example with --plot to populate docs/images/.
</success_criteria>

<output>
Create `.planning/phases/63-visualization-pareto-front-plotting-example-images/63-02-SUMMARY.md` when done.
</output>
