---
phase: 51-multi-parent-crossover-self-adaptive-mutation
plan: 03
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - src/operations/mutation/self_adaptive_gaussian.rs
  - src/operations/mutation.rs
autonomous: true
requirements:
  - MUT-05
  - TRAITS-02
must_haves:
  truths:
    - "User can call factory_with_params on a RangeChromosome<T> with Mutation::SelfAdaptiveGaussian and receive a successful mutation including sigma update"
    - "Calling Mutation::SelfAdaptiveGaussian on a BinaryChromosome returns GaError::MutationError directing users to SelfAdaptive"
    - "All sigmas are clamped to MutationConfiguration.sigma_min (default 1e-5) after every mutate call"
    - "When MutationConfiguration.self_adaptive_tau is None, tau is computed as 1/sqrt(2*n) inside the dispatcher; same for tau_prime as 1/sqrt(2*sqrt(n))"
    - "Wave 0 tests test_mutation_self_adaptive (3 tests) are GREEN"
    - "Existing Cauchy/LevyFlight/Uniform arms in mutation.rs are unchanged"
  artifacts:
    - path: "src/operations/mutation/self_adaptive_gaussian.rs"
      provides: "self_adaptive_gaussian_mutation(individual, tau, tau_prime, sigma_min) for RangeChromosome<T>: SelfAdaptive"
      contains: "pub fn self_adaptive_gaussian_mutation"
    - path: "src/operations/mutation.rs"
      provides: "Mutation::SelfAdaptiveGaussian match arm + try_self_adaptive Any downcast dispatcher + pub mod self_adaptive_gaussian"
      contains: "Mutation::SelfAdaptiveGaussian"
  key_links:
    - from: "src/operations/mutation.rs Mutation::SelfAdaptiveGaussian arm"
      to: "src/operations/mutation/self_adaptive_gaussian.rs"
      via: "try_self_adaptive Any downcast"
      pattern: "downcast_mut::<RangeChromosome<"
    - from: "src/operations/mutation/self_adaptive_gaussian.rs"
      to: "src/traits/self_adaptive.rs"
      via: "individual.adapt_strategy_params(tau, tau_prime, sigma_min)"
      pattern: "adapt_strategy_params"
---

<objective>
Implement the `SelfAdaptiveGaussian` mutation operator and wire it into the `Mutation` enum dispatch in `mutation.rs` via the `Any` downcast pattern used by `try_cauchy` / `try_polynomial`.

Purpose: Completes the production code path for MUT-05 (per-chromosome sigma co-evolution) and TRAITS-02 (SelfAdaptive integration). Turns Wave 0 test file `test_mutation_self_adaptive.rs` from RED to GREEN.

Output: One new operator file implementing the two-step ES mutation (log-normal sigma update via `adapt_strategy_params`, then single-gene Gaussian mutation using the updated sigma) and one modified dispatch site in `mutation.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-multi-parent-crossover-self-adaptive-mutation/51-CONTEXT.md
@.planning/phases/51-multi-parent-crossover-self-adaptive-mutation/51-RESEARCH.md
@.planning/phases/51-multi-parent-crossover-self-adaptive-mutation/51-PATTERNS.md
@.planning/phases/51-multi-parent-crossover-self-adaptive-mutation/51-01-SUMMARY.md

@src/operations/mutation/gaussian.rs
@src/operations/mutation/cauchy.rs
@src/operations/mutation.rs
@src/operations.rs
@src/configuration.rs
@src/traits/self_adaptive.rs
@src/types/chromosomes/range.rs
@tests/operations/test_mutation_self_adaptive.rs

<interfaces>
<!-- Contracts established by Plan 01 (already shipped). -->

From `src/traits/self_adaptive.rs`:
- `pub trait SelfAdaptive: ChromosomeT` — methods `strategy_params(&self) -> &[f64]`, `set_strategy_params(&mut self, Vec<f64>)`, `adapt_strategy_params(&mut self, tau: f64, tau_prime: f64, sigma_min: f64)` (the last is a default impl).

From `src/types/chromosomes/range.rs`:
- `impl<T: ...> SelfAdaptive for Range<T>` — `RangeChromosome<T>` is the canonical implementor for `f64`, `f32`, `i32`, `i64`.
- `Range<T>::set_dna` lazy-initializes `strategy_params` to `vec![1.0; n]`.

From `src/operations.rs`:
- `Mutation::SelfAdaptiveGaussian` (unit variant).

From `src/configuration.rs`:
- `MutationConfiguration { self_adaptive_tau: Option<f64>, self_adaptive_tau_prime: Option<f64>, sigma_min: Option<f64>, ... }`.

From `src/operations/mutation.rs` (existing, untouched here):
- `pub fn factory_with_params(individual: &mut U, configuration: &GaConfiguration, step: ..., sigma: f64) -> Result<(), GaError>` (or whatever the exact existing signature is — confirm during Read step).
- Existing `try_cauchy<U: LinearChromosome + 'static>(individual, scale) -> Option<Result<(), GaError>>` macro-with-try_type! pattern.
- Existing `Mutation::Cauchy { ... } => { ... }` match arm style.

From `src/operations/mutation/gaussian.rs` lines 49-60:
- Box-Muller `N(0, sigma)` idiom: `let u1 = rng.random_range(f64::EPSILON..1.0); let u2 = rng.random_range(0.0..std::f64::consts::TAU); let noise = (-2.0 * u1.ln()).sqrt() * u2.cos() * sigma;`.
- Gene mutation: `gene.value = T::from_f64((current + noise).clamp(lo, hi))` after pulling `(lo, hi)` from `gene.ranges[range_idx]`.

New public function to add:
- `pub fn self_adaptive_gaussian_mutation<T>(individual: &mut RangeChromosome<T>, tau: f64, tau_prime: f64, sigma_min: f64) -> Result<(), GaError> where T: Sync + Send + Clone + Default + Debug + PartialOrd + Copy + 'static + GaussianConvertible, RangeChromosome<T>: SelfAdaptive`.

New private dispatcher (added inside `mutation.rs`):
- `fn try_self_adaptive<U: LinearChromosome + 'static>(individual: &mut U, tau: f64, tau_prime: f64, sigma_min: f64) -> Option<Result<(), GaError>>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement self_adaptive_gaussian_mutation operator function</name>
  <files>src/operations/mutation/self_adaptive_gaussian.rs</files>
  <read_first>
    - src/operations/mutation/gaussian.rs (full file — copy Box-Muller and gene mutation idiom, plus `GaussianConvertible` trait import path)
    - src/operations/mutation/cauchy.rs (analog operator structure: imports, generic bounds, gene/range handling)
    - src/traits/self_adaptive.rs (just-created trait — confirm exact method signatures)
    - src/types/chromosomes/range.rs (confirm `set_gene` exists and signature, gene `.ranges` shape)
    - tests/operations/test_mutation_self_adaptive.rs (RED expectations to satisfy)
    - .planning/phases/51-multi-parent-crossover-self-adaptive-mutation/51-PATTERNS.md (validated implementation excerpt for this file)
    - src/rng.rs (confirm `make_rng()` signature)
  </read_first>
  <behavior>
    - `self_adaptive_gaussian_mutation(&mut c, 0.0, 0.0, 1e-5)` with c.dna() of length 4 and `c.strategy_params() == &[1e-8; 4]`: returns `Ok(())`; after the call, every value in `c.strategy_params()` is `>= 1e-5` (sigma_min floor enforced inside `adapt_strategy_params`)
    - `self_adaptive_gaussian_mutation(&mut c, 0.5, 0.5, 1e-5)` on a 4-gene chromosome: returns `Ok(())`; at least one sigma differs from the pre-call value (log-normal update applied); exactly one gene value may have changed
    - Empty DNA case: `self_adaptive_gaussian_mutation(&mut c, 0.5, 0.5, 1e-5)` on a chromosome where `c.dna().is_empty()` returns `Ok(())` without panic and without sampling RNG
    - Gene with empty `ranges` is skipped (returns `Ok(())` without modifying the gene)
    - The function does NOT mutate the parent chromosome's age / fitness fields directly (delegates entirely through `adapt_strategy_params` and `set_gene`)
  </behavior>
  <action>
    Create `src/operations/mutation/self_adaptive_gaussian.rs` with module docstring "Self-adaptive Gaussian mutation for chromosomes implementing `SelfAdaptive`."

    Imports: `use crate::chromosomes::Range as RangeChromosome;`, `use crate::error::GaError;`, `use crate::operations::mutation::gaussian::GaussianConvertible;`, `use crate::traits::{LinearChromosome, SelfAdaptive};`, `use log::debug;`, `use rand::Rng;`, `use std::fmt::Debug;`.

    Define:

    ```text
    pub fn self_adaptive_gaussian_mutation<T>(
        individual: &mut RangeChromosome<T>,
        tau: f64,
        tau_prime: f64,
        sigma_min: f64,
    ) -> Result<(), GaError>
    where
        T: Sync + Send + Clone + Default + Debug + PartialOrd + Copy + 'static + GaussianConvertible,
        RangeChromosome<T>: SelfAdaptive,
    ```

    Function body:
    1. `let len = individual.dna().len(); if len == 0 { return Ok(()); }`
    2. Step 1 — sigma update: `individual.adapt_strategy_params(tau, tau_prime, sigma_min);` (default trait method handles log-normal + sigma_min clamp)
    3. Step 2 — pick one random gene index:
       ```text
       let mut rng = crate::rng::make_rng();
       let idx = rng.random_range(0..len);
       ```
    4. Read sigma for that gene: `let sigma = individual.strategy_params().get(idx).copied().unwrap_or(1.0);` (defensive fallback in case `strategy_params().len() < dna.len()` after a manual `set_strategy_params` with a shorter vector)
    5. Clone the gene at `idx`: `let mut gene = individual.dna()[idx].clone();` (use `Clone` — `Range<T>::Gene` is `Clone`).
    6. If `gene.ranges.is_empty()` return `Ok(())` (no range, no mutation).
    7. Pick a range: `let range_idx = rng.random_range(0..gene.ranges.len()); let (lo, hi) = gene.ranges[range_idx];` (mirrors `gaussian.rs`).
    8. Convert `current = T::to_f64(gene.value)`, `lo_f64 = T::to_f64(lo)`, `hi_f64 = T::to_f64(hi)`.
    9. Box-Muller N(0, sigma): `let u1: f64 = rng.random_range(f64::EPSILON..1.0); let u2: f64 = rng.random_range(0.0..std::f64::consts::TAU); let noise: f64 = (-2.0 * u1.ln()).sqrt() * u2.cos() * sigma;`
    10. `let new_val = (current + noise).clamp(lo_f64, hi_f64); gene.value = T::from_f64(new_val);`
    11. Write back: `individual.set_gene(idx, gene);` — confirm `set_gene(usize, Gene)` exists on `Range<T>`; if instead the API is `set_gene_value`, follow that exact name.
    12. Emit `debug!(target: "mutation_events", "SelfAdaptiveGaussian mutation applied at idx={} sigma={}", idx, sigma);` and return `Ok(())`.

    WASM: no `std::time` or `rayon` imports. RNG via `crate::rng::make_rng()`. Confirm by inspecting the imports at the top of the file before saving.

    The function must compile when `RangeChromosome<T>: SelfAdaptive` is satisfied by the impl added in Plan 01. No additional trait bounds beyond what Plan 01 established.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo clippy -- -D warnings &amp;&amp; cargo test --test test_mutation_self_adaptive self_adaptive_sigma_min_enforced &amp;&amp; cargo check --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - File `src/operations/mutation/self_adaptive_gaussian.rs` exists
    - `grep -F "pub fn self_adaptive_gaussian_mutation" src/operations/mutation/self_adaptive_gaussian.rs` returns at least 1 match
    - `grep -F "individual.adapt_strategy_params(tau, tau_prime, sigma_min)" src/operations/mutation/self_adaptive_gaussian.rs` returns at least 1 match
    - `grep -F "if len == 0" src/operations/mutation/self_adaptive_gaussian.rs` returns at least 1 match (empty DNA early return)
    - `grep -E "use (rayon|std::time)" src/operations/mutation/self_adaptive_gaussian.rs` returns empty (WASM safe)
    - `cargo build` exits 0
    - `cargo test --test test_mutation_self_adaptive self_adaptive_sigma_min_enforced` exits 0 (the sigma_min test passes)
    - `cargo clippy -- -D warnings` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>Operator file exists with the two-step ES mutation; the sigma_min Wave 0 test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SelfAdaptiveGaussian into mutation.rs via try_self_adaptive dispatcher and the Mutation match arm</name>
  <files>src/operations/mutation.rs</files>
  <read_first>
    - src/operations/mutation.rs (full file — find `pub mod cauchy;` declarations, `try_cauchy` function, and the `Mutation::Cauchy { ... }` match arm inside `impl MutationOperator for Mutation` or `factory_with_params`)
    - src/operations.rs (Mutation enum variant `SelfAdaptiveGaussian` from Plan 01)
    - src/configuration.rs (MutationConfiguration with self_adaptive_tau, self_adaptive_tau_prime, sigma_min fields — confirm exact field names from Plan 01)
    - src/operations/mutation/self_adaptive_gaussian.rs (the function this dispatches to)
    - src/types/chromosomes/range.rs (RangeChromosome import)
    - src/error.rs (GaError::MutationError signature)
    - tests/operations/test_mutation_self_adaptive.rs (Wave 0 expectations including the BinaryChromosome error test)
  </read_first>
  <behavior>
    - `pub mod self_adaptive_gaussian;` is declared
    - `try_self_adaptive<U: LinearChromosome + 'static>(individual, tau, tau_prime, sigma_min)` returns `Some(Ok(()))` when `U` concretely is `RangeChromosome<f64|f32|i32|i64>`
    - `try_self_adaptive` returns `None` when `U` is any other type (e.g., `BinaryChromosome`)
    - The new `Mutation::SelfAdaptiveGaussian` match arm reads `mutation_config.self_adaptive_tau`, `mutation_config.self_adaptive_tau_prime`, `mutation_config.sigma_min`; when any is `None`, computes the default:
      - tau default: when `None`, `tau = 1.0 / (2.0 * n as f64).sqrt()` where `n = individual.strategy_params().len().max(1)` (but for non-SelfAdaptive types, fall back to 1.0 so `try_self_adaptive` still triggers the appropriate error path)
      - tau_prime default: when `None`, `tau_prime = 1.0 / (2.0 * (n as f64).sqrt()).sqrt()`
      - sigma_min default: when `None`, `1e-5`
    - When `try_self_adaptive` returns `None`, the arm returns `Err(GaError::MutationError("SelfAdaptiveGaussian requires a chromosome implementing SelfAdaptive (RangeChromosome<T>).".to_string()))`
    - Existing `Mutation::Cauchy`, `Mutation::LevyFlight`, `Mutation::Uniform`, `Mutation::Gaussian`, etc. arms are bit-for-bit unchanged
  </behavior>
  <action>
    Open `src/operations/mutation.rs`. Find the existing module declarations near the top (e.g., `pub mod cauchy;`, `pub mod levy_flight;`, `pub mod uniform;`, `pub mod gaussian;`). Add `pub mod self_adaptive_gaussian;` alongside them (maintain alphabetical / existing order). Confirm `use std::any::Any;` and `use crate::chromosomes::Range as RangeChromosome;` are already imported (they should be, since `try_cauchy` uses them); add if missing.

    Add `use crate::traits::SelfAdaptive;` to the imports (used only as a marker that downstream types must satisfy; the operator function takes care of the bound).

    Add a new private dispatcher function, modeled exactly on `try_cauchy`:

    ```text
    fn try_self_adaptive<U: LinearChromosome + 'static>(
        individual: &mut U,
        tau: f64,
        tau_prime: f64,
        sigma_min: f64,
    ) -> Option<Result<(), GaError>> {
        macro_rules! try_type {
            ($t:ty) => {
                if let Some(ind) = (individual as &mut dyn Any).downcast_mut::<RangeChromosome<$t>>() {
                    return Some(self_adaptive_gaussian::self_adaptive_gaussian_mutation(
                        ind, tau, tau_prime, sigma_min,
                    ));
                }
            };
        }
        try_type!(f64);
        try_type!(f32);
        try_type!(i32);
        try_type!(i64);
        None
    }
    ```

    Find the existing `Mutation::Cauchy { scale: _ }` match arm (or `Mutation::Cauchy => { ... }` — confirm exact shape during Read step). Confirm where that arm sits inside the function body (likely `impl MutationOperator for Mutation::mutate` or a `factory_with_params` helper). Place the new `Mutation::SelfAdaptiveGaussian => { ... }` arm immediately AFTER the `Mutation::Uniform` arm to preserve grouping with the other real-valued scalar mutations.

    Inside the new arm:
    1. Read the three config fields from `configuration.mutation_configuration` (or whatever name the surrounding scope uses for `MutationConfiguration` — match the Cauchy arm exactly).
    2. Compute `n` defensively. The dispatcher does not have direct access to `strategy_params().len()` because the generic `U` may not implement `SelfAdaptive`. Compute `n_hint = individual.dna().len().max(1)` (every `LinearChromosome` exposes `dna()`); this is sufficient because for `RangeChromosome<T>` the strategy_params length matches dna length after `set_dna` lazy-init.
    3. Resolve defaults:
       ```text
       let tau = mutation_config.self_adaptive_tau.unwrap_or_else(|| 1.0 / (2.0 * n_hint as f64).sqrt());
       let tau_prime = mutation_config.self_adaptive_tau_prime.unwrap_or_else(|| 1.0 / (2.0 * (n_hint as f64).sqrt()).sqrt());
       let sigma_min_val = mutation_config.sigma_min.unwrap_or(1e-5);
       ```
    4. Dispatch:
       ```text
       return try_self_adaptive(individual, tau, tau_prime, sigma_min_val).unwrap_or_else(|| {
           Err(GaError::MutationError(
               "SelfAdaptiveGaussian requires a chromosome implementing SelfAdaptive (RangeChromosome<T>).".to_string(),
           ))
       });
       ```

    Use `return` (matching the Cauchy arm style) so the surrounding control flow is preserved exactly.

    Do NOT modify any other match arm. Do NOT modify `factory_with_params` signature. Do NOT modify the `MutationOperator` trait.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo build --features serde &amp;&amp; cargo clippy -- -D warnings &amp;&amp; cargo test --test test_mutation_self_adaptive &amp;&amp; cargo check --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "pub mod self_adaptive_gaussian;" src/operations/mutation.rs` returns at least 1 match
    - `grep -F "fn try_self_adaptive" src/operations/mutation.rs` returns at least 1 match
    - `grep -F "Mutation::SelfAdaptiveGaussian" src/operations/mutation.rs` returns at least 1 match (the new match arm)
    - `grep -F "1.0 / (2.0 * n_hint as f64).sqrt()" src/operations/mutation.rs` returns at least 1 match (tau default formula)
    - `grep -F "1.0 / (2.0 * (n_hint as f64).sqrt()).sqrt()" src/operations/mutation.rs` returns at least 1 match (tau_prime default formula)
    - `grep -F "SelfAdaptiveGaussian requires a chromosome implementing SelfAdaptive" src/operations/mutation.rs` returns at least 1 match (error message)
    - `try_self_adaptive` covers f64, f32, i32, i64 (verifiable via `grep -cE "try_type!\(([fi](32|64))\)" src/operations/mutation.rs` returns ≥ 4 NEW occurrences relative to baseline)
    - Existing `Mutation::Cauchy`, `Mutation::LevyFlight`, `Mutation::Uniform`, `Mutation::Gaussian` match arms are unchanged (verifiable via `git diff src/operations/mutation.rs | grep -E "^- *Mutation::(Cauchy|LevyFlight|Uniform|Gaussian)"` empty)
    - `cargo test --test test_mutation_self_adaptive` exits 0 (all three Wave 0 tests pass: sigma_min, sigma_spread_evolves, returns_error_for_non_self_adaptive)
    - `cargo build && cargo build --features serde` both exit 0
    - `cargo clippy -- -D warnings` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>Mutation dispatch is wired; the BinaryChromosome error path and the sigma evolution path are both verified by Wave 0 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User config → dispatcher | Users supply `self_adaptive_tau`, `self_adaptive_tau_prime`, `sigma_min` via `MutationConfiguration` |
| Library → chromosome | The dispatcher downcasts `&mut U` to `&mut RangeChromosome<T>` using `Any`; failure returns an error rather than panicking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-01 | Tampering | `self_adaptive_gaussian_mutation` — empty DNA | mitigate | Early return `Ok(())` when `individual.dna().len() == 0`; avoids `rng.random_range(0..0)` panic. |
| T-51-03-02 | Tampering | Gene with empty `ranges` | mitigate | Early return `Ok(())` before attempting to read `gene.ranges[range_idx]`; avoids panic. |
| T-51-03-03 | Tampering | `try_self_adaptive` on non-`SelfAdaptive` type (e.g., `BinaryChromosome`) | mitigate | All `try_type!` calls return `None`; outer dispatcher returns `Err(GaError::MutationError(...))` with a clear remediation message. |
| T-51-03-04 | Tampering | Default tau / tau_prime when n=0 | mitigate | `n_hint = individual.dna().len().max(1)` floors `n` at 1 so `(2.0 * 1.0).sqrt() = sqrt(2.0)` is finite; the operator still early-returns on empty DNA before sampling. |
| T-51-03-05 | Tampering | Box-Muller numerical safety | mitigate | `u1` lower-bounded by `f64::EPSILON` in `random_range(f64::EPSILON..1.0)` — prevents `ln(0)` producing `-inf`. Matches `gaussian.rs` convention. |
| T-51-03-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this phase. |
</threat_model>

<verification>
- `cargo build` and `cargo build --features serde` — both green
- `cargo clippy -- -D warnings` — no lints
- `cargo test --test test_mutation_self_adaptive` — all three Wave 0 tests GREEN
- `cargo test --test test_self_adaptive` — still GREEN (no regression on trait tests from Plan 01)
- `cargo check --target wasm32-unknown-unknown` — WASM compiles
- `git diff src/operations/mutation.rs | grep -E "^- *Mutation::(Cauchy|LevyFlight|Uniform|Gaussian)"` — empty (no regressions on other arms)
</verification>

<success_criteria>
- `src/operations/mutation/self_adaptive_gaussian.rs` implements the two-step ES mutation
- `src/operations/mutation.rs` has the new `pub mod self_adaptive_gaussian;`, the `try_self_adaptive` dispatcher across 4 numeric types, and the `Mutation::SelfAdaptiveGaussian` match arm with default tau / tau_prime / sigma_min resolution
- Existing mutation arms are bit-for-bit unchanged
- Wave 0 test file `test_mutation_self_adaptive.rs` is fully GREEN (3 tests pass)
- All gates pass: `cargo build`, `cargo build --features serde`, `cargo clippy -- -D warnings`, `cargo check --target wasm32-unknown-unknown`
</success_criteria>

<output>
Create `.planning/phases/51-multi-parent-crossover-self-adaptive-mutation/51-03-SUMMARY.md` when done. Record: new operator function signature, dispatcher coverage (4 numeric types), default formulas used for tau / tau_prime, and the test names now passing.
</output>
