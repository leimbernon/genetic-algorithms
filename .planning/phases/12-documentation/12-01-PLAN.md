---
phase: 12-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "README contains an ## Examples section listing all 10 runnable examples"
    - "Every example entry includes the exact cargo run --example <name> command"
    - "A first-time user can identify which example matches their problem domain from the table"
    - "There is exactly one authoritative place for examples (no duplicate ### Run Examples)"
  artifacts:
    - path: "README.md"
      provides: "Examples section with table of all 10 examples"
      contains: "## Examples"
  key_links:
    - from: "README.md ToC"
      to: "## Examples section"
      via: "anchor link"
      pattern: "\\[Examples\\]\\(#examples\\)"
---

<objective>
Add a top-level `## Examples` section to README.md documenting all 10 runnable examples in a table, update the Table of Contents, and remove the now-redundant `### Run Examples` subsection.

Purpose: Users can discover and run all examples without reading source code (DOC-01).
Output: Updated README.md with Examples section.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-documentation/12-CONTEXT.md
@.planning/phases/12-documentation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Examples section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (current full content — understand ToC structure, section ordering, exact line positions)
    - .planning/phases/12-documentation/12-CONTEXT.md (locked decisions on format, placement, content)
    - .planning/phases/12-documentation/12-RESEARCH.md (verified table content and line numbers)
  </read_first>
  <action>
Make three coordinated edits to README.md:

**Edit 1 — ToC insertion (after line 33):**
Insert the following line after the `- [Full Example (Range)](#full-example-range)` entry (current line 33) and before the `- [Usage](#usage)` entry (current line 34):

```
- [Examples](#examples)
```

**Edit 2 — Section insertion (between line 228 and line 230):**
Insert the following block after the closing ` ``` ` of the `## Full Example (Range)` code block (line 228, which is the line `println!("Best fitness: {}", population.unwrap().best_chromosome.get_fitness());`) — specifically after line 229 (the closing triple-backtick) and before `## Usage` (current line 230). Insert a blank line, then:

```markdown
## Examples

Run any example directly with `cargo run`:

| Example | Domain | Command |
|---------|--------|---------|
| `rastrigin` | Continuous optimization | `cargo run --example rastrigin` |
| `nsga2_zdt1` | Multi-objective (NSGA-II) | `cargo run --example nsga2_zdt1` |
| `island_model` | Parallel / island model | `cargo run --example island_model` |
| `job_scheduling` | Permutation / scheduling | `cargo run --example job_scheduling` |
| `feature_selection` | Binary / adaptive GA | `cargo run --example feature_selection` |
| `niching` | Multimodal / niching | `cargo run --example niching` |
| `knapsack_binary` | Binary / combinatorial | `cargo run --example knapsack_binary` |
| `nqueens_range` | Constraint satisfaction | `cargo run --example nqueens_range` |
| `onemax_binary` | Binary / baseline | `cargo run --example onemax_binary` |
| `onemax_extension` | Binary / diversity control | `cargo run --example onemax_extension` |
```

Follow with a blank line before `## Usage`.

**Edit 3 — Remove `### Run Examples` subsection:**
Delete the entire `### Run Examples` subsection under `## Development`. This includes:
- The `### Run Examples` heading (current line 268)
- The opening triple-backtick with `bash` (line 269)
- The three `cargo run --example ...` lines (lines 270-272)
- The closing triple-backtick (line 273)

After removal, there should be a single blank line between the end of `### Run Benchmarks` and `### Code Quality`. Do NOT leave orphaned blank lines.

**Important constraints:**
- Do NOT change any other content in the file
- The table must have exactly 10 rows (6 new + 4 existing examples)
- Domain labels for the 6 new examples are LOCKED per CONTEXT.md: continuous, multi-objective, parallel, permutation, binary, multimodal
- Domain labels for the 4 existing examples: "Binary / combinatorial" (knapsack_binary), "Constraint satisfaction" (nqueens_range), "Binary / baseline" (onemax_binary), "Binary / diversity control" (onemax_extension)
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms && grep -c "## Examples" README.md | grep -q "1" && grep -c "\\[Examples\\](#examples)" README.md | grep -q "1" && grep -c "cargo run --example" README.md | grep -q "10" && grep -c "### Run Examples" README.md | grep -q "0" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains exactly one `## Examples` heading (grep -c "## Examples" README.md returns 1)
    - README.md ToC contains `[Examples](#examples)` (grep -c "\[Examples\](#examples)" README.md returns 1)
    - README.md contains exactly 10 `cargo run --example` lines in the Examples table (grep -c "cargo run --example" README.md returns 10)
    - README.md does NOT contain `### Run Examples` heading (grep -c "### Run Examples" README.md returns 0)
    - README.md contains `| \`rastrigin\`` (grep confirms row exists)
    - README.md contains `| \`nsga2_zdt1\`` (grep confirms row exists)
    - README.md contains `| \`island_model\`` (grep confirms row exists)
    - README.md contains `| \`job_scheduling\`` (grep confirms row exists)
    - README.md contains `| \`feature_selection\`` (grep confirms row exists)
    - README.md contains `| \`niching\`` (grep confirms row exists)
    - README.md contains `| \`knapsack_binary\`` (grep confirms row exists)
    - README.md contains `| \`nqueens_range\`` (grep confirms row exists)
    - README.md contains `| \`onemax_binary\`` (grep confirms row exists)
    - README.md contains `| \`onemax_extension\`` (grep confirms row exists)
    - `## Examples` section appears AFTER `## Full Example (Range)` and BEFORE `## Usage` in file order
    - `cargo build --examples` exits 0 (no accidental source edits)
  </acceptance_criteria>
  <done>README.md has a single authoritative Examples section with all 10 examples in a table, ToC updated, redundant Run Examples subsection removed</done>
</task>

</tasks>

<verification>
- `grep -c "## Examples" README.md` returns 1
- `grep -c "\[Examples\](#examples)" README.md` returns 1
- `grep -c "cargo run --example" README.md` returns 10
- `grep -c "### Run Examples" README.md` returns 0
- `cargo build --examples` exits 0
</verification>

<success_criteria>
1. README contains an `## Examples` section listing all 10 examples with domain labels and `cargo run --example <name>` commands
2. Table of Contents includes `[Examples](#examples)` link
3. No duplicate `### Run Examples` subsection exists under Development
4. A first-time user reading only the README can identify which example matches their problem domain
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation/12-01-SUMMARY.md`
</output>
