---
phase: 47-architecture-audit-chromosomet-split
plan: 03
type: execute
wave: 3
depends_on:
  - 47-02
files_modified:
  - src/engines/ga.rs
  - src/engines/de.rs
  - src/engines/scatter.rs
  - src/engines/cellular.rs
  - src/engines/alps.rs
  - src/engines/nsga2/mod.rs
  - src/engines/nsga3/mod.rs
  - src/engines/moead/mod.rs
  - src/engines/spea2/mod.rs
  - src/engines/sms_emoa/mod.rs
  - src/engines/ibea/mod.rs
  - src/engines/island/mod.rs
autonomous: true
requirements:
  - ARCH-02
pr_group: 1
tags:
  - rust
  - engines
  - bound-change
  - phase-gate

must_haves:
  truths:
    - "All engine orchestrators (Ga, DE, Scatter, Cellular, ALPS, Nsga2Ga, Nsga3Ga, MoeaDGa, Spea2Ga, SmsEmoaGa, IbeaGa, Island) constrain their U with LinearChromosome"
    - "`cargo test` (all features) and `cargo check --target wasm32-unknown-unknown` pass on the milestone branch"
    - "PR 1 is mergeable: ARCH-01 + ARCH-02 fully satisfied"
  artifacts:
    - path: "src/engines/ga.rs"
      provides: "Ga<U: LinearChromosome> orchestrator"
      contains: "U: LinearChromosome"
    - path: "src/engines/island/mod.rs"
      provides: "Island<U: LinearChromosome> orchestrator"
      contains: "U: LinearChromosome"
  key_links:
    - from: "src/engines/ga.rs"
      to: "src/traits/linear_chromosome.rs"
      via: "U: LinearChromosome generic bound"
      pattern: "U: LinearChromosome"
    - from: "src/engines/nsga2/mod.rs"
      to: "src/traits/linear_chromosome.rs"
      via: "U: LinearChromosome generic bound"
      pattern: "U: LinearChromosome"
---

<objective>
Upgrade every engine orchestrator (single-population, alternative metaheuristics, multi-objective, island) to `U: LinearChromosome` because all orchestrators call `dna()` and/or pass chromosomes into bound-changed operators. This completes ARCH-02 across the entire crate and unlocks the PR 1 merge per D-13.

Purpose: Final piece of PR 1. Engines are the integration point where the trait split must flow through to keep the build green. Tree chromosomes (Phase 53) get their own `GpGa<U: TreeChromosome>` engine, so this constraint does not lock them out.

Output: 12 engine files updated, full `cargo test` + WASM check pass, PR 1 ready to open against `milestone/v3.0.0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md
@.planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Per 47-RESEARCH.md and 47-PATTERNS.md: -->
<!-- src/engines/ga.rs — orchestrator, calls dna() throughout — change ALL U: ChromosomeT → U: LinearChromosome -->
<!-- src/engines/{de,scatter,cellular,alps}.rs (or their mod.rs equivalents) — orchestrators of alt metaheuristics — change bounds -->
<!-- src/engines/{nsga2,nsga3,moead,spea2,sms_emoa,ibea}/mod.rs — multi-obj orchestrators — change bounds -->
<!-- src/engines/island/mod.rs — island engine — change bounds -->

<!-- Watch for: -->
<!-- 1. Imports — each file needs `use crate::traits::LinearChromosome;` instead of/alongside ChromosomeT -->
<!-- 2. Trait impls that take a callback/closure parameterized over `U: ChromosomeT` — these usually become LinearChromosome too -->
<!-- 3. Helper free functions that operate only on fitness/age — these can STAY at ChromosomeT (rare in orchestrators, but possible) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update single-population and alt-metaheuristic engine orchestrators to U: LinearChromosome</name>
  <files>src/engines/ga.rs, src/engines/de.rs, src/engines/scatter.rs, src/engines/cellular.rs, src/engines/alps.rs</files>
  <read_first>
    - src/engines/ga.rs (full file — primary orchestrator; ~30+ bound sites)
    - src/engines/de.rs OR src/engines/de/mod.rs (Differential Evolution engine)
    - src/engines/scatter.rs OR src/engines/scatter/mod.rs (Scatter Search engine)
    - src/engines/cellular.rs OR src/engines/cellular/mod.rs (Cellular GA engine)
    - src/engines/alps.rs OR src/engines/alps/mod.rs (ALPS engine)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ src/engines/ga.rs bound change pattern)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-CONTEXT.md (D-05)
    - run `ls src/engines/` to confirm exact module layout (some engines use `engine.rs`, others `engine/mod.rs`)
  </read_first>
  <action>
    For each of the 5 engine files:
    1. Confirm the file path with `ls src/engines/`. Use the canonical filename (e.g., `src/engines/de/mod.rs` if it is a directory module, otherwise `src/engines/de.rs`).
    2. Apply sed: `sed -i '' 's/U: ChromosomeT/U: LinearChromosome/g' <engine_file>`.
    3. Update the import block: add `use crate::traits::LinearChromosome;` if not present; keep `ChromosomeT` import only if a helper still uses it.
    4. Compile: `cargo check --lib`. Resolve any new errors. Most likely surface error: a closure or callback type alias that referenced `U: ChromosomeT` and now needs `LinearChromosome`.

    Special handling for `src/engines/ga.rs` — this file has ~30+ bound sites including the main `Ga<U>` struct, multiple `impl<U> Ga<U>` blocks, helper functions, and trait impl blocks. After sed, perform an explicit grep audit:
    ```
    grep -n 'ChromosomeT\|LinearChromosome' src/engines/ga.rs
    ```
    For every `ChromosomeT` reference remaining, verify it is intentional (e.g., a helper that only reads fitness). If any are LinearChromosome contexts missed by sed (e.g., because formatting put `U:` on a separate line), fix manually.

    Do NOT yet touch reporter wiring, configuration encapsulation, or StoppingCriteria — those belong to PR 2 and PR 3. This task is BOUND-CHANGE ONLY.

    After all 5 files are updated, run `cargo check --lib` and `cargo check --target wasm32-unknown-unknown --lib`. Both must be GREEN.

    Commit message: `refactor(47-03): upgrade single-population and alt-metaheuristic engine bounds to LinearChromosome`.
  </action>
  <verify>
    <automated>cargo check --lib 2>&amp;1 | tail -10 &amp;&amp; cargo check --target wasm32-unknown-unknown --lib 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --lib` exits 0
    - `cargo check --target wasm32-unknown-unknown --lib` exits 0
    - src/engines/ga.rs contains `U: LinearChromosome` on the main Ga struct definition (grep: `grep -E 'pub struct Ga<U: LinearChromosome' src/engines/ga.rs` or equivalent impl-block bound returns ≥1 match)
    - src/engines/ga.rs primary type bounds no longer reference `U: ChromosomeT` for orchestration generics (any remaining `ChromosomeT` usage is in helper context or a `: ChromosomeT` supertrait reference; grep audit logged in task output)
    - The four alt-metaheuristic engines (de, scatter, cellular, alps) each contain at least one `U: LinearChromosome` bound (grep: `grep -c 'U: LinearChromosome' src/engines/de* src/engines/scatter* src/engines/cellular* src/engines/alps*` returns >= 4)
    - Each touched file imports `LinearChromosome` (grep: `grep -l 'use .*LinearChromosome' src/engines/ga.rs src/engines/de* src/engines/scatter* src/engines/cellular* src/engines/alps*` returns 5 paths)
  </acceptance_criteria>
  <done>Single-population and 4 alt-metaheuristic engines compile against LinearChromosome bound on native and wasm32.</done>
</task>

<task type="auto">
  <name>Task 2: Update multi-objective + island engine orchestrators and run PR 1 phase verification gate</name>
  <files>src/engines/nsga2/mod.rs, src/engines/nsga3/mod.rs, src/engines/moead/mod.rs, src/engines/spea2/mod.rs, src/engines/sms_emoa/mod.rs, src/engines/ibea/mod.rs, src/engines/island/mod.rs</files>
  <read_first>
    - src/engines/nsga2/mod.rs (full file — primary multi-obj orchestrator pattern)
    - src/engines/nsga3/mod.rs, src/engines/moead/mod.rs, src/engines/spea2/mod.rs, src/engines/sms_emoa/mod.rs, src/engines/ibea/mod.rs (each follows the same pattern as nsga2)
    - src/engines/island/mod.rs (island engine; may wrap an inner Ga/Nsga2 engine — check both bounds)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-RESEARCH.md (§ Common Pitfalls 2: alleles_can_be_repeated is read by 8 multi-obj engine files — these reads STAY in PR 1 but are flagged for PR 2; do NOT remove them in this task)
    - .planning/phases/47-architecture-audit-chromosomet-split/47-PATTERNS.md (§ Shared Patterns › Trait Bound Change)
  </read_first>
  <action>
    For each of the 7 multi-objective + island engine files:
    1. Apply sed: `sed -i '' 's/U: ChromosomeT/U: LinearChromosome/g' <engine_file>`.
    2. Update imports: add `use crate::traits::LinearChromosome;` if not present.
    3. Compile: `cargo check --lib`.

    For src/engines/island/mod.rs specifically — verify both the outer `Island<U>` orchestrator and any inner-engine wrapper structs (e.g., `IslandNsga2` if it exists) get the bound updated. The Island engine wraps engines per island; both layers need the bound.

    For each multi-obj engine, leave the existing `self.ga_config.limit_configuration.alleles_can_be_repeated` reads UNCHANGED — those are PR 2 territory (ARCH-04). Mention this in the commit message body.

    After all 7 files compile, run the **PR 1 phase verification gate**:
    1. `cargo test` (default features)
    2. `cargo test --features serde`
    3. `cargo clippy --all-features -- -D warnings`
    4. `cargo check --target wasm32-unknown-unknown`
    5. `cargo doc --no-deps --all-features 2>&1 | grep -E 'warning|error'` — must be empty for rustdoc-clean policy per CLAUDE.md

    All five checks must exit 0 (or empty output for the doc warning grep). If any fail, debug and fix before considering this task done. ARCH-01 and ARCH-02 are now satisfied — PR 1 is mergeable.

    Commit message: `refactor(47-03): upgrade multi-objective and island engine bounds to LinearChromosome; PR 1 gate green`.
  </action>
  <verify>
    <automated>cargo test 2>&amp;1 | tail -5 &amp;&amp; cargo test --features serde 2>&amp;1 | tail -5 &amp;&amp; cargo clippy --all-features -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo check --target wasm32-unknown-unknown 2>&amp;1 | tail -5 &amp;&amp; cargo doc --no-deps --all-features 2>&amp;1 | grep -cE 'warning|error' || true</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` (default features) exits 0
    - `cargo test --features serde` exits 0
    - `cargo clippy --all-features -- -D warnings` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - `cargo doc --no-deps --all-features` produces zero warnings (CLAUDE.md mandatory rule)
    - Each of nsga2/mod.rs, nsga3/mod.rs, moead/mod.rs, spea2/mod.rs, sms_emoa/mod.rs, ibea/mod.rs, island/mod.rs contains at least one `U: LinearChromosome` bound (grep: `grep -l 'U: LinearChromosome' src/engines/{nsga2,nsga3,moead,spea2,sms_emoa,ibea,island}/mod.rs` returns 7 paths)
    - Each touched file imports LinearChromosome
    - `self.ga_config.limit_configuration.alleles_can_be_repeated` and `.needs_unique_ids` reads in the multi-obj files are intentionally LEFT IN PLACE (those are removed in plan 47-04; preserve grep evidence of the reads still existing)
  </acceptance_criteria>
  <done>All 12 engine files (5 from Task 1 + 7 here) updated to LinearChromosome bound. PR 1 phase verification gate is GREEN. ARCH-01 and ARCH-02 fully satisfied. PR 1 is mergeable to milestone/v3.0.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine orchestrator → user chromosome types | Engines now require `U: LinearChromosome`. Users of tree/variable-length types (Phases 52, 53) must use the corresponding dedicated engine (GpGa<U: TreeChromosome>); this is the explicit type-level firewall that v3.0.0 establishes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-07 | Tampering (silent semantic drift) | ga.rs ~30 bound sites | mitigate | Explicit grep audit after sed; CLAUDE.md rustdoc-warnings-zero gate catches missed re-export issues; full `cargo test` + clippy + WASM gate in Task 2 |
| T-47-08 | Denial of Service (WASM regression) | all engine orchestrators | mitigate | `cargo check --target wasm32-unknown-unknown` is the Task 2 phase gate; CI also runs wasm-check.yml on this branch |
| T-47-09 | Information Disclosure (API contract break without migration path) | ARCH-01/02 user-visible breakage | mitigate | Plan 47-07 publishes MIGRATION.md with before/after examples; D-11 mandates it ship in the published crate via Cargo.toml `include` |
| T-47-SC | Tampering (npm/pip/cargo install) | crate dependency graph | n/a | This phase installs no new packages; no Package Legitimacy Gate applicable (verified per 47-RESEARCH.md) |
</threat_model>

<verification>
## PR 1 Phase Verification Gate (this plan)
- `cargo test` exits 0
- `cargo test --features serde` exits 0
- `cargo clippy --all-features -- -D warnings` exits 0
- `cargo check --target wasm32-unknown-unknown` exits 0
- `cargo doc --no-deps --all-features` zero warnings

These are the five gates required by CLAUDE.md before opening any PR.
</verification>

<success_criteria>
- 12 engine files compile against `U: LinearChromosome`
- Full test suite, clippy, doc-zero-warning, and WASM compile all GREEN
- ARCH-01 and ARCH-02 satisfied across the entire crate
- PR 1 (per D-13) is ready: branch `feat/<issue>-chromosomet-split` off `milestone/v3.0.0`, target `milestone/v3.0.0`
- LimitConfiguration field reads in multi-obj engines deliberately untouched — PR 2 territory
</success_criteria>

<output>
Create `.planning/phases/47-architecture-audit-chromosomet-split/47-03-SUMMARY.md` when done. Summary must list PR 1 boundary, verification gate output, and the precise list of follow-up cleanup items deferred to PR 2.
</output>
