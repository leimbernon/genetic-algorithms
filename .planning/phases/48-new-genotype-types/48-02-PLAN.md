---
phase: 48-new-genotype-types
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - src/types/genotypes/unique.rs
  - src/types/genotypes/mod.rs
  - src/types/chromosomes/unique.rs
  - src/types/chromosomes/mod.rs
  - src/initializers/unique_initializer.rs
  - src/initializers.rs
  - src/lib.rs
  - tests/types/chromosomes/test_unique.rs
  - tests/types/genotypes/test_unique.rs
  - tests/initializers/test_initializers.rs
  - examples/job_scheduling.rs
autonomous: true
requirements:
  - GEN-01
  - GEN-02
tags:
  - rust
  - chromosomes
  - genotypes
  - initializers
  - examples

must_haves:
  truths:
    - "D-02, D-03: User can construct `UniqueChromosome<T>` whose `dna` is a Fisher-Yates permutation of the provided alphabet — no duplicate genes, all alphabet elements present"
    - "D-06: User can use `UniqueChromosome<T>` with PMX, Order, and EdgeRecombination crossovers and Insertion/Swap/Inversion mutations without modification of those operators"
    - "User who configures `Crossover::SinglePoint` or `Crossover::Arithmetic` with `UniqueChromosome<T>` sees `GaError::ConfigurationError` from `Ga::build()` before any generation runs"
    - "User can run `cargo run --example job_scheduling` and the example uses `UniqueChromosome<i32>` (not `RangeChromosome<i32>` with manual shuffle); valid permutations are produced each generation"
    - "D-01, D-02, D-03: User can `use genetic_algorithms::{UniqueChromosome, UniqueGenotype}` and `use genetic_algorithms::initializers::unique_random_initialization`"
  artifacts:
    - path: "src/types/genotypes/unique.rs"
      provides: "D-01: UniqueGenotype<T> { id: i32, value: T } + GeneT impl (no per-gene alphabet)"
      contains: "pub struct UniqueGenotype"
    - path: "src/types/chromosomes/unique.rs"
      provides: "D-02: UniqueChromosome<T> { dna, alphabet: Arc<[T]>, fitness, age, fitness_fn } + ChromosomeT + LinearChromosome + OperatorCompat impls"
      contains: "pub struct UniqueChromosome"
    - path: "src/initializers/unique_initializer.rs"
      provides: "D-03: unique_random_initialization<T>(alphabet: &[T]) -> Vec<UniqueGenotype<T>> using Fisher-Yates (full permutation)"
      contains: "pub fn unique_random_initialization"
    - path: "examples/job_scheduling.rs"
      provides: "Migrated to UniqueChromosome<i32>"
      contains: "UniqueChromosome<i32>"
  key_links:
    - from: "src/types/chromosomes/unique.rs"
      to: "src/types/genotypes/unique.rs"
      via: "use crate::genotypes::UniqueGenotype"
      pattern: "UniqueGenotype"
    - from: "src/types/chromosomes/unique.rs"
      to: "src/traits/operator_compat.rs"
      via: "impl OperatorCompat with Some(&[...]) for valid_crossovers/valid_mutations"
      pattern: "impl.*OperatorCompat.*for UniqueChromosome"
    - from: "examples/job_scheduling.rs"
      to: "src/initializers/unique_initializer.rs"
      via: "with_initialization_fn closure calling unique_random_initialization(&alphabet)"
      pattern: "unique_random_initialization"
---

<objective>
Deliver `UniqueGenotype<T>`, `UniqueChromosome<T>`, the Fisher-Yates `unique_random_initialization`, the `OperatorCompat` restriction set, and the `job_scheduling` example migration. Replaces the existing `RangeChromosome<i32>` + manual shuffle hack with a semantically correct permutation type.

Purpose: Implements GEN-01 (UniqueChromosome with no-duplicate guarantee + invalid-operator runtime rejection) and GEN-02 (job_scheduling example migration). Reuses the OperatorCompat foundation from 48-01.

Output: New gene struct, new chromosome struct, new initializer function, three module re-export edits, migrated example, populated Wave 0 test files for the gene + chromosome + initializer behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-new-genotype-types/48-CONTEXT.md
@.planning/phases/48-new-genotype-types/48-RESEARCH.md
@.planning/phases/48-new-genotype-types/48-PATTERNS.md
@.planning/phases/48-new-genotype-types/48-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Source: src/types/genotypes/range.rs lines 35-87 (gene struct + GeneT impl model) -->
<!-- UniqueGenotype: NO ranges field — alphabet lives on chromosome (D-01) -->
pub struct UniqueGenotype<T> { pub id: i32, pub value: T }
impl<T: Clone + Default + Sync + Send> GeneT for UniqueGenotype<T> { ... }

<!-- Source: src/types/chromosomes/list.rs lines 34-169 (chromosome model with Clone gene; NOT Copy) -->
<!-- UniqueChromosome: adds alphabet: Arc<[T]> field beyond standard layout (D-02) -->
pub struct UniqueChromosome<T: Sync + Send + Clone + Default + Debug> {
    pub dna: Vec<UniqueGenotype<T>>,
    pub alphabet: Arc<[T]>,
    pub fitness: f64,
    pub age: usize,
    #[cfg_attr(feature = "serde", serde(skip, default))]
    pub fitness_fn: FitnessFnWrapper<UniqueGenotype<T>>,
}

<!-- Default alphabet: Arc::from([]) — pattern verified from src/types/genotypes/range.rs Range<T>::default -->

<!-- OperatorCompat impl (D-06, RESEARCH §Code Examples) -->
impl<T: ...> OperatorCompat for UniqueChromosome<T> {
    fn valid_crossovers() -> Option<&'static [Crossover]> {
        Some(&[Crossover::Pmx, Crossover::Order, Crossover::EdgeRecombination,
               Crossover::Clone, Crossover::Rejuvenate])
    }
    fn valid_mutations() -> Option<&'static [Mutation]> {
        Some(&[Mutation::Insertion, Mutation::Swap, Mutation::Inversion])
    }
}

<!-- Note: MultiGroupPmx / MultiGroupOx are NOT in UniqueChromosome's valid set per A5 recommendation — those belong to MultiUniqueChromosome only -->

<!-- Source: src/initializers/list_initializer.rs lines 98-135 — Fisher-Yates pattern -->
pub fn unique_random_initialization<T>(alphabet: &[T]) -> Vec<UniqueGenotype<T>>
where T: Clone + Sync + Send + Default + Debug;

<!-- Example migration target: examples/job_scheduling.rs line ~90 (fitness closure) and ~107-125 (alleles + manual shuffle) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UniqueGenotype gene struct + module re-export + unique_random_initialization + tests</name>
  <files>src/types/genotypes/unique.rs, src/types/genotypes/mod.rs, src/initializers/unique_initializer.rs, src/initializers.rs, src/lib.rs, tests/types/genotypes/test_unique.rs, tests/initializers/test_initializers.rs</files>
  <read_first>
    - src/types/genotypes/range.rs (lines 35-107 — full canonical gene struct pattern: derives, serde attrs, Default, Display, GeneT impl, constructor `new`)
    - src/types/genotypes/list.rs (alternate model — non-Copy gene type)
    - src/types/genotypes/mod.rs (current `pub mod` + `pub use` structure)
    - src/initializers/list_initializer.rs (lines 98-135 — `list_random_initialization_without_repetitions` Fisher-Yates implementation; uses `crate::rng::make_rng()`, `rng.random_range(0..=i)`, `indices.swap(i, j)`)
    - src/initializers/range_initializer.rs (per-gene sampling pattern)
    - src/initializers.rs (re-export pattern: `pub mod ...; pub use ...::*;`)
    - src/traits/gene.rs (GeneT trait signature)
    - src/lib.rs (where existing chromosome/gene types are re-exported at the crate root)
    - tests/types/genotypes/test_unique.rs (Wave 0 scaffold from 48-01 — to be populated)
    - tests/initializers/test_initializers.rs (existing tests; add cases for unique_random_initialization)
    - .planning/phases/48-new-genotype-types/48-PATTERNS.md (§`src/types/genotypes/unique.rs` — full struct + impl pattern; §`src/initializers/unique_initializer.rs`)
    - .planning/phases/48-new-genotype-types/48-RESEARCH.md (§Pattern 1, §Pattern 3, §Code Examples)
  </read_first>
  <action>
    Create `src/types/genotypes/unique.rs` containing `pub struct UniqueGenotype<T> { pub id: i32, pub value: T }` with `#[derive(Debug, PartialEq, Clone)]` and the `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize), serde(bound(serialize = "T: serde::Serialize", deserialize = "T: serde::de::DeserializeOwned")))]` block. Do NOT add an `Arc<[T]>` or any per-gene alphabet field — the alphabet lives on the chromosome (D-01). Add `impl<T: Default> Default`, `impl<T: fmt::Display> fmt::Display`, `impl<T: Clone + Default + Sync + Send> GeneT` (only `id()` and `set_id()` per the model). Add `impl<T: Clone + Default> UniqueGenotype<T>` block with `pub fn new(id: i32, value: T) -> Self`, `pub fn value(&self) -> T` (clones), `pub fn set_value(&mut self, value: T) -> &mut Self`. Module-doc `//!` header matching the style of `range.rs`.

    In `src/types/genotypes/mod.rs`, add `pub mod unique;` and `pub use unique::UniqueGenotype;` alongside existing entries.

    Create `src/initializers/unique_initializer.rs` containing `pub fn unique_random_initialization<T>(alphabet: &[T]) -> Vec<UniqueGenotype<T>> where T: Clone + Sync + Send + Default + Debug` with the Fisher-Yates body from 48-PATTERNS §`src/initializers/unique_initializer.rs`: collect `(0..alphabet.len())` into a Vec, run the standard reverse-iteration Fisher-Yates swap loop, then map indices into `UniqueGenotype { id: idx as i32, value: alphabet[idx].clone() }`. Use `crate::rng::make_rng()`. The function takes only `alphabet: &[T]` — no `genes_per_chromosome` parameter (D-03; full permutation). Add a module `//!` header.

    In `src/initializers.rs`, add `pub mod unique_initializer;` and `pub use unique_initializer::*;` alongside existing entries.

    In `src/lib.rs`, add re-exports at crate root for `UniqueGenotype` and `unique_random_initialization` matching the existing pattern (whatever style is used for `Range`/`RangeGenotype`/`range_random_initialization`).

    Populate `tests/types/genotypes/test_unique.rs` (replace the Wave 0 placeholder): tests must verify (1) `UniqueGenotype::new(7, 42i32).id() == 7`, (2) `GeneT::set_id` mutates in place and returns `&mut Self`, (3) `Default::default()` returns `id: 0, value: T::default()`, (4) `Display` formats as `"{id}:{value}"` for a `Display` value type. Use a concrete `i32` value where generics are needed.

    Add a `unique_initialization` test module/section to `tests/initializers/test_initializers.rs` (use the same pattern existing tests in the file use) with at least three assertions: alphabet length 0 returns empty vec; alphabet length n returns dna of length n; the multiset of `value` fields in the result equals the multiset of the alphabet (i.e., permutation property — every alphabet element present exactly once). Use `alphabet = vec![10, 20, 30, 40, 50]` for the populated case.
  </action>
  <verify>
    <automated>cargo test --all-features test_unique test_initializers 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub struct UniqueGenotype' src/types/genotypes/unique.rs` returns 1
    - `grep -c 'impl.*GeneT for UniqueGenotype' src/types/genotypes/unique.rs` returns 1
    - File `src/types/genotypes/unique.rs` does NOT contain `Arc<[T]>` (alphabet must not live per-gene)
    - `grep -c 'pub use unique::UniqueGenotype' src/types/genotypes/mod.rs` returns 1
    - `grep -c 'pub fn unique_random_initialization' src/initializers/unique_initializer.rs` returns 1
    - `grep -c 'pub use unique_initializer' src/initializers.rs` returns 1
    - `grep -c 'pub use .*UniqueGenotype\|pub use .*unique_random_initialization' src/lib.rs` returns >= 2
    - `cargo test --all-features test_unique 2>&1 | grep 'test result: ok'` shows passing
    - The unique_random_initialization permutation-property test passes (multiset equality)
  </acceptance_criteria>
  <done>UniqueGenotype is a working GeneT with no alphabet field; initializer produces full permutations; gene and initializer tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: UniqueChromosome struct + ChromosomeT + LinearChromosome + OperatorCompat impls + chromosome tests</name>
  <files>src/types/chromosomes/unique.rs, src/types/chromosomes/mod.rs, src/lib.rs, tests/types/chromosomes/test_unique.rs</files>
  <read_first>
    - src/types/chromosomes/list.rs (lines 1-200 — closest analog: ChromosomeT + LinearChromosome dual impl with Clone gene, FitnessFnWrapper, Default impl using `FitnessFnWrapper::default()`)
    - src/types/chromosomes/range.rs (lines 35-170 — alternate analog; specifically the Default impl which uses `Arc::from([])` for the empty ranges slice — same syntax for alphabet)
    - src/types/genotypes/range.rs (line ~73 — `Arc::from([])` default value pattern for `Arc<[T]>`)
    - src/types/chromosomes/mod.rs (current `pub mod` + `pub use` structure; note `mod range` is currently private; new modules should be `pub mod`)
    - src/fitness/fitness_fn_wrapper.rs (FitnessFnWrapper API: `new<F>(F)`, `call(&[Gene]) -> f64`, `default()`)
    - src/traits/chromosome.rs (current ChromosomeT signature)
    - src/traits/linear_chromosome.rs (current LinearChromosome signature)
    - src/traits/operator_compat.rs (created in 48-01 — trait to implement)
    - src/operations.rs (Crossover and Mutation enums — confirm variants Pmx, Order, EdgeRecombination, Clone, Rejuvenate for crossovers and Insertion, Swap, Inversion for mutations all exist)
    - .planning/phases/48-new-genotype-types/48-PATTERNS.md (§`src/types/chromosomes/unique.rs` — full struct, Default, ChromosomeT, LinearChromosome, OperatorCompat impls)
    - .planning/phases/48-new-genotype-types/48-RESEARCH.md (§Pattern 2, §Code Examples §UniqueChromosome<T>, §Pitfall 2 Arc default syntax)
    - tests/test_chromosomet_core.rs (chromosome test style — instantiate, call fitness/age methods, assert state)
    - tests/test_linear_chromosome.rs (LinearChromosome test style)
  </read_first>
  <action>
    Create `src/types/chromosomes/unique.rs` with the imports: `crate::fitness::FitnessFnWrapper`, `crate::genotypes::UniqueGenotype`, `crate::traits::{ChromosomeT, LinearChromosome, OperatorCompat}`, `crate::operations::{Crossover, Mutation}`, `std::borrow::Cow`, `std::fmt`, `std::fmt::Debug`, `std::sync::Arc`.

    Define `pub struct UniqueChromosome<T: Sync + Send + Clone + Default + Debug>` with the five fields per D-02: `dna: Vec<UniqueGenotype<T>>`, `alphabet: Arc<[T]>`, `fitness: f64`, `age: usize`, `fitness_fn: FitnessFnWrapper<UniqueGenotype<T>>` (gated `#[serde(skip, default)]`). Top-of-struct derives: `#[derive(Clone, Debug, PartialEq)]`. Apply the full `#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize), serde(bound(...)))]` block matching `list.rs`. Note: `alphabet: Arc<[T]>` is serialized normally — do NOT mark it `serde(skip)` (Assumption A4 — needed for checkpoint restoration).

    Implement `Default`: `dna: Vec::new()`, `alphabet: Arc::from([])` (Pitfall 2 — same pattern as `Range<T>` gene default), `fitness: f64::NAN`, `age: 0`, `fitness_fn: FitnessFnWrapper::default()`.

    Implement `Display` (optional — match `list.rs` if it has one; otherwise omit).

    Implement `ChromosomeT for UniqueChromosome<T>` with `type Gene = UniqueGenotype<T>` and the five required methods (`calculate_fitness`, `fitness`, `set_fitness`, `set_age`, `age`) using the `list.rs` body shape. Bounds on `T`: `Sync + Send + Clone + Default + Debug + 'static`.

    Implement `LinearChromosome for UniqueChromosome<T>` with `dna()`, `dna_mut()`, `set_dna()` (Cow match — see Shared Patterns), `set_fitness_fn<F>` wrapping in `FitnessFnWrapper::new(fitness_fn)`. Bounds match ChromosomeT.

    Implement `OperatorCompat for UniqueChromosome<T>` with bound `T: Sync + Send + Clone + Default + Debug + 'static`, returning `Some(&[Crossover::Pmx, Crossover::Order, Crossover::EdgeRecombination, Crossover::Clone, Crossover::Rejuvenate])` for `valid_crossovers()` (per D-06, plus Clone/Rejuvenate per RESEARCH Q1 recommendation; do NOT include `MultiGroupPmx`/`MultiGroupOx` — those belong to `MultiUniqueChromosome` per A5) and `Some(&[Mutation::Insertion, Mutation::Swap, Mutation::Inversion])` for `valid_mutations()`. 48-01 already established that no blanket impl exists (existing chromosomes have explicit empty impls), so this concrete restriction impl compiles without orphan-rule conflict.

    In `src/types/chromosomes/mod.rs`, add `pub mod unique;` and `pub use unique::UniqueChromosome;` alongside existing entries.

    In `src/lib.rs`, re-export `UniqueChromosome` at crate root matching the existing pattern.

    Populate `tests/types/chromosomes/test_unique.rs` (replacing the Wave 0 scaffold): verify (1) `UniqueChromosome::<i32>::default()` returns a chromosome with empty dna, empty alphabet, NaN fitness, age 0; (2) building a Ga with `UniqueChromosome<i32>` and `Crossover::SinglePoint` returns `Err(GaError::ConfigurationError(_))` from `build()`; (3) building a Ga with `UniqueChromosome<i32>` and `Crossover::Pmx` succeeds; (4) building a Ga with `UniqueChromosome<i32>` and `Mutation::ValueScalar` (or any non-permutation mutation) returns `Err`; (5) `ChromosomeT::calculate_fitness` invokes the wrapped fitness function (set via `LinearChromosome::set_fitness_fn`); (6) `LinearChromosome::set_dna` with `Cow::Owned` replaces the dna and returns `&mut Self`. Use existing test scaffolding patterns from `tests/test_chromosomet_core.rs`.

  </action>
  <verify>
    <automated>cargo test --all-features test_unique 2>&1 | tail -20 && cargo test --all-features 2>&1 | grep -E '^test result' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub struct UniqueChromosome' src/types/chromosomes/unique.rs` returns 1
    - `grep -c 'impl.*ChromosomeT for UniqueChromosome' src/types/chromosomes/unique.rs` returns 1
    - `grep -c 'impl.*LinearChromosome for UniqueChromosome' src/types/chromosomes/unique.rs` returns 1
    - `grep -c 'impl.*OperatorCompat for UniqueChromosome' src/types/chromosomes/unique.rs` returns 1
    - `grep -c 'Crossover::Pmx\|Crossover::Order\|Crossover::EdgeRecombination' src/types/chromosomes/unique.rs` returns >= 3
    - `grep -c 'Mutation::Insertion\|Mutation::Swap\|Mutation::Inversion' src/types/chromosomes/unique.rs` returns >= 3
    - `grep -c 'pub use unique::UniqueChromosome' src/types/chromosomes/mod.rs` returns 1
    - `grep -c 'alphabet: Arc::from' src/types/chromosomes/unique.rs` returns 1 (Default impl uses `Arc::from([])`)
    - `tests/types/chromosomes/test_unique.rs` contains a test that asserts `Ga::build()` with `Crossover::SinglePoint` and `UniqueChromosome<i32>` returns `Err(GaError::ConfigurationError(_))`
    - `cargo build --all-features` exits 0
    - `cargo test --all-features` exits 0 (no regressions; new tests pass)
  </acceptance_criteria>
  <done>UniqueChromosome implements all three traits; OperatorCompat restriction is enforced at build time; chromosome tests pass; existing test suite unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate examples/job_scheduling.rs from RangeChromosome to UniqueChromosome + smoke-test</name>
  <files>examples/job_scheduling.rs, tests/test_examples.rs</files>
  <read_first>
    - examples/job_scheduling.rs (all 188 lines — current `RangeChromosome<i32>` + manual `with_initialization_fn` shuffle workaround; specifically lines 47-59 imports, line ~90 fitness closure signature, lines 107-125 alleles + initializer closure, line ~156 run_with_callback type annotation, lines 15-16 module doc)
    - examples/feature_selection.rs (smaller analog example for reference if needed)
    - src/types/chromosomes/unique.rs (created in Task 2 — confirm public API for instantiation)
    - src/initializers/unique_initializer.rs (created in Task 1 — confirm signature `(&[T]) -> Vec<UniqueGenotype<T>>`)
    - tests/test_examples.rs (existing smoke-test entry — confirm whether `job_scheduling` is already covered or needs to be added)
    - .planning/phases/48-new-genotype-types/48-PATTERNS.md (§`examples/job_scheduling.rs (migration)` — 6 specific changes listed)
    - .planning/phases/48-new-genotype-types/48-RESEARCH.md (§Pitfall 5 — fitness closure gene type rename)
  </read_first>
  <action>
    Edit `examples/job_scheduling.rs` per PATTERNS §`examples/job_scheduling.rs (migration)`:

    1. Update imports: replace `use genetic_algorithms::types::chromosomes::Range as RangeChromosome` (or equivalent) and `use genetic_algorithms::types::genotypes::Range as RangeGenotype` with `use genetic_algorithms::UniqueChromosome; use genetic_algorithms::UniqueGenotype; use genetic_algorithms::initializers::unique_random_initialization;`. Remove the `rand::seq::SliceRandom` import if it was used solely for the manual shuffle. (Inspect current imports first to confirm exact names.)

    2. Update the fitness closure signature: change `|dna: &[RangeGenotype<i32>]|` to `|dna: &[UniqueGenotype<i32>]|` (Pitfall 5). The `gene.value` accesses still work because `UniqueGenotype<T>` also exposes `pub value: T` (same field name).

    3. Replace the alleles + manual shuffle initializer block (lines ~107-125): remove the `let alleles = vec![RangeGenotype::new(...)]` line, the clone, and the manual-shuffle closure. Replace with `let alphabet: Vec<i32> = (0..N_JOBS as i32).collect();` and `.with_initialization_fn({ let alphabet = alphabet.clone(); move |_n, _alleles, _| unique_random_initialization(&alphabet) })`. Inspect the existing `with_initialization_fn` closure arity in `ga.rs` to confirm the exact closure signature — adjust accordingly (RESEARCH Anti-Pattern §Partial initializer override calls out the signature unknown).

    4. Update the type annotation in `run_with_callback` (or wherever `Ga::<RangeChromosome<i32>>` appears): change to `Ga::<UniqueChromosome<i32>>`.

    5. Remove the migration TODO comment at line ~116 (the one stating "Phase 48 will migrate to UniqueChromosome").

    6. Update the module doc comment at lines 15-16 to mention `UniqueChromosome<i32>` instead of `RangeChromosome<i32>` with unique-id hack — frame it as a permutation example.

    7. If `tests/test_examples.rs` does not already smoke-test `job_scheduling`, add a `#[test] fn job_scheduling_smoke() { /* invoke main() body or spawn `cargo run --example job_scheduling --quiet` via assert_cmd if available; otherwise call a `run()` helper extracted from the example */ }`. If the existing pattern is to NOT include example smoke tests in `tests/test_examples.rs`, leave that file alone and rely on the ARCH-07 CI workflow (Phase 47-08).

    Use the configured crossover `Crossover::Pmx` or `Crossover::Order` (already permutation-safe) in the migrated example — confirm the existing example uses one of these or migrate to one if it currently uses something incompatible with `UniqueChromosome::valid_crossovers()`.
  </action>
  <verify>
    <automated>cargo build --example job_scheduling --all-features 2>&1 | tail -10 && cargo run --example job_scheduling --quiet 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UniqueChromosome<i32>\|UniqueChromosome::<i32>' examples/job_scheduling.rs` returns >= 1
    - `grep -c 'RangeChromosome\|RangeGenotype' examples/job_scheduling.rs` returns 0
    - `grep -c 'unique_random_initialization' examples/job_scheduling.rs` returns >= 1
    - `grep -ci 'Phase 48 will migrate\|TODO.*migrate' examples/job_scheduling.rs` returns 0 (migration TODO removed)
    - `cargo build --example job_scheduling --all-features` exits 0
    - `cargo run --example job_scheduling --quiet` exits 0 and produces output (no panic, no compile error)
    - `cargo test --all-features` exits 0 (no regressions)
  </acceptance_criteria>
  <done>job_scheduling example builds and runs using UniqueChromosome; migration TODO removed; no compile or runtime errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User fitness fn closure → UniqueChromosome::calculate_fitness | User-supplied closure invoked on each chromosome's dna; arbitrary user code |
| User alphabet → unique_random_initialization | User-provided alphabet slice; no validation beyond length |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | unique_random_initialization with empty alphabet | accept | Empty alphabet returns empty Vec; downstream Ga::build will catch zero-length dna via existing same_dna_length / chromosome_length validators (verified in src/validators/generic_validator.rs) |
| T-48-05 | Denial of Service | UniqueChromosome clone with large Arc<[T]> alphabet | mitigate | `alphabet: Arc<[T]>` is shared — clone is O(1) atomic refcount increment (verified existing pattern in Range<T>); Vec<UniqueGenotype<T>> dna is O(n) per chromosome — no change vs RangeChromosome cost |
| T-48-06 | Information Disclosure | Serde Deserialize on alphabet | accept | Untrusted input would be a checkpoint file; existing trust model assumes checkpoint files are local-only artifacts; no new exposure |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (RESEARCH §Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `cargo build --all-features` exits 0
- `cargo test --all-features` exits 0; new gene + chromosome + initializer tests pass; no existing tests regress
- `cargo build --example job_scheduling --all-features` exits 0
- `cargo run --example job_scheduling --quiet` exits 0 (smoke check)
- `cargo clippy --all-features 2>&1 | grep -E 'warning|error'` shows no new warnings introduced by Phase 48 code
- `cargo check --target wasm32-unknown-unknown` exits 0
</verification>

<success_criteria>
1. `UniqueGenotype<T>` is a publicly accessible `GeneT` with no per-gene alphabet field
2. `UniqueChromosome<T>` implements `ChromosomeT`, `LinearChromosome`, and `OperatorCompat` with a restricted operator set
3. `unique_random_initialization` produces full permutations (no duplicates, all alphabet elements present)
4. Selecting `Crossover::SinglePoint` with `UniqueChromosome<i32>` returns `Err(GaError::ConfigurationError(_))` from `Ga::build()` BEFORE running
5. `examples/job_scheduling.rs` compiles and runs using `UniqueChromosome<i32>`; the migration TODO is removed
6. Full test suite green; clippy clean; WASM check clean
</success_criteria>

<output>
Create `.planning/phases/48-new-genotype-types/48-02-SUMMARY.md` when done, documenting: gene struct location and field set; chromosome struct location and trait impls; initializer signature; example before/after snippets; list of test cases added.
</output>
