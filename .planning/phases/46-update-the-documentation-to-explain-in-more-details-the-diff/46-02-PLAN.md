---
phase: 46-update-the-documentation-to-explain-in-more-details-the-diff
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/engines/ga.rs
  - src/engines/de/engine.rs
  - src/engines/scatter/engine.rs
  - src/engines/cellular/engine.rs
  - src/engines/alps/engine.rs
  - src/engines/island/
autonomous: true
requirements:
  - D-04
must_haves:
  truths:
    - "User can read each single-objective engine //! doc and understand: algorithm description, when to use, parameters, and see a complete compilable example"
    - "Each engine //! doc follows the D-04 ficha tecnica template consistently"
  artifacts:
    - path: "src/engines/ga.rs"
      provides: "Ga engine //! doc with complete ficha tecnica"
      min_lines: 60
    - path: "src/engines/de/engine.rs"
      provides: "DeEngine //! doc with complete ficha tecnica"
      min_lines: 60
    - path: "src/engines/scatter/engine.rs"
      provides: "ScatterEngine //! doc with complete ficha tecnica"
      min_lines: 50
    - path: "src/engines/cellular/engine.rs"
      provides: "CellularEngine //! doc with complete ficha tecnica"
      min_lines: 50
    - path: "src/engines/alps/engine.rs"
      provides: "AlpsEngine //! doc with complete ficha tecnica"
      min_lines: 50
    - path: "src/engines/island/"
      provides: "IslandEngine //! doc with complete ficha tecnica"
      min_lines: 40
  key_links:
    - from: "Each engine //! doc"
      to: "Similar engines via intra-doc links"
      via: "When to Choose This vs [Other Engine] section"
---

<objective>
Expand all single-objective engine //! module-level documentation to the full D-04 "ficha tecnica completa" standard: algorithm description, mathematical context, when-to-use guidance, complete parameter tables, compilable example, cross-references, and configuration tips.

Purpose: Every single-objective engine module doc serves as the SSOT for that algorithm on docs.rs and in IDE hover tooltips.
Output: 6 engine //! doc blocks expanded to the D-04 ficha tecnica template (60+ lines each), 3 island model files with expanded //! docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-update-the-documentation-to-explain-in-more-details-the-diff/46-CONTEXT.md
@.planning/phases/46-update-the-documentation-to-explain-in-more-details-the-diff/46-RESEARCH.md

<interfaces>
The D-04 ficha tecnica template (from RESEARCH.md §Pattern 1):

```markdown
//! # Engine Name
//!
//! ## Description
//! [Algorithm description and mathematical context]
//!
//! ## When to Use
//! - **Problem type:** [single-objective / multi-objective / combinatorial]
//! - **Number of objectives:** [1 / 2+ / 3+]
//! - **Variable type:** [binary / real-valued / permutation / List<T> / mixed]
//! - **Key strength:** [What is this engine best at?]
//! - **Key weakness:** [When should you NOT use this?]
//!
//! ## Quick Reference
//!
//! ### Mandatory Parameters
//! | Parameter | Type | Required | Default | Description |
//! |-----------|------|----------|---------|-------------|
//!
//! ### Optional Parameters
//! | Parameter | Type | Required | Default | Description |
//! |-----------|------|----------|---------|-------------|
//!
//! ## Complete Example
//! ```rust,ignore
//! // Full compilable example
//! ```
//!
//! ## Configuration Tips
//! - [Tip 1]
//! - [Tip 2]
//!
//! ## When to Choose This vs [Similar Engine]
//! | Factor | This Engine | [Alternative Engine] |
//! |--------|------------|---------------------|
//!
//! ## References
//! - [Paper/author, year]
```

Current engine //! line counts (from RESEARCH.md and direct verification):
- ga.rs: 26 lines — expand to 60+
- de/engine.rs: 1 line — expand to 60+
- scatter/engine.rs: 6 lines — expand to 50+
- cellular/engine.rs: 18 lines — expand to 50+
- alps/engine.rs: 20 lines — expand to 50+
- island/: needs //! doc — expand to 40+
</interfaces>

<context_extra>
Read each engine file before editing — understand the configuration struct, the builder methods, and the run() signature to write accurate docs.

For each engine, determine the key parameters by reading the Configuration struct:
- Ga: GaConfiguration — population_size, max_generations, genes_per_chromosome
- De: DeConfiguration — population_size, max_generations, strategy, crossover_mode
- Scatter: ScatterConfiguration — reference_set_size, max_iterations
- Cellular: CellularConfiguration — grid_rows, grid_cols, neighborhood, update_mode
- Alps: AlpsConfiguration — n_layers, age_gap, age_scheme
- Island: IslandConfiguration — n_islands, migration_interval, migration_rate
</context_extra>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Ga and DeEngine //! docs to D-04 ficha tecnica standard</name>
  <files>src/engines/ga.rs, src/engines/de/engine.rs</files>
  <read_first>
    - src/engines/ga.rs — read the first 100 lines for current //! block and Configuration struct
    - src/engines/de/engine.rs — read the first 100 lines for current //! block and DeConfiguration
    - src/configuration.rs — GaConfiguration struct fields (population_size, max_generations, etc.)
    - src/engines/de/configuration.rs — DeConfiguration struct fields
    - .planning/phases/46-update-the-documentation-to-explain-in-more-details-the-diff/46-RESEARCH.md — D-04 template, AI-Ready pattern
  </read_first>
  <action>
Expand the //! module-level documentation for Ga (src/engines/ga.rs) and DeEngine (src/engines/de/engine.rs) to the full D-04 ficha tecnica standard.

**For Ga (src/engines/ga.rs):**
Replace the current 26-line //! block with a 60+ line //! block following the D-04 template:

```rust
//! # Standard Genetic Algorithm (Ga)
//!
//! ## Description
//!
//! The [`Ga`] struct is the primary single-population genetic algorithm orchestrator.
//! It implements the classic evolutionary cycle: initialization → selection → crossover
//! + mutation (parallelized via rayon) → fitness evaluation → survivor selection →
//! elitism → statistics collection. Each generation, parents are selected via a
//! configurable [`Selection`](crate::operations::Selection) operator, offspring are
//! produced through [`Crossover`](crate::operations::Crossover) and
//! [`Mutation`](crate::operations::Mutation), and the population is updated via a
//! [`Survivor`](crate::operations::Survivor) strategy.
//!
//! ## When to Use
//!
//! - **Problem type:** Single-objective — continuous, combinatorial, binary, or permutation
//! - **Number of objectives:** 1
//! - **Variable type:** Any (binary via [`chromosomes::Binary`](crate::chromosomes::Binary),
//!   real-valued via [`chromosomes::Range`](crate::chromosomes::Range), symbolic via
//!   [`chromosomes::ListChromosome`](crate::chromosomes::ListChromosome))
//! - **Key strength:** General-purpose; broadest operator library; best for learning
//! - **Key weakness:** Single-population convergence can be premature on multimodal landscapes
//!
//! ## Quick Reference
//!
//! ### Mandatory Parameters
//!
//! | Parameter | Type | Required | Default | Description |
//! |-----------|------|----------|---------|-------------|
//! | `population_size` | `usize` | Yes (via builder) | — | Number of chromosomes in the population |
//! | `max_generations` | `usize` | Yes (via builder) | — | Maximum number of generations |
//! | `genes_per_chromosome` | `usize` | Yes (via builder) | — | Length of each chromosome's DNA |
//! | `fitness_fn` | `FitnessFn<U>` | Yes (via builder) | — | Function evaluating a chromosome's fitness |
//!
//! ### Optional Parameters
//!
//! | Parameter | Type | Required | Default | Description |
//! |-----------|------|----------|---------|-------------|
//! | `selection` | `Selection` | No | `Tournament(3)` | Parent selection strategy |
//! | `crossover` | `Crossover` | No | `Uniform` | Offspring crossover strategy |
//! | `mutation` | `Mutation` | No | `Swap(0.05)` | Gene mutation strategy |
//! | `survivor` | `Survivor` | No | `Truncation` | Population replacement strategy |
//! | `elitism` | `Option<usize>` | No | `None` | Number of elite chromosomes preserved |
//! | `initialization_fn` | `InitializationFn<U>` | No | random init | Custom population initialization |
//! | `observer` | `Option<Arc<dyn GaObserver<U>>>` | No | `None` | Lifecycle observer |
//! | `max_duration` | `Option<Duration>` | No | `None` | Wall-clock time limit |
//! | `fitness_target` | `Option<f64>` | No | `None` | Stop when best fitness reaches this |
//! | `constraint_handling` | `Option<ConstraintHandling>` | No | `None` | Constraint handling strategy |
//! | `hall_of_fame_size` | `Option<usize>` | No | `None` | Archive size for best solutions |
//! | `aos_config` | `Option<AosConfig>` | No | `None` | Adaptive operator selection |
//! | `local_search` | `Option<Arc<dyn LocalSearchOperator<U>>>` | No | `None` | Memetic local search operator |
//!
//! ## Complete Example
//!
//! ```rust,ignore
//! use genetic_algorithms::prelude::*;
//! use genetic_algorithms::chromosomes::Range as RangeChromosome;
//! use genetic_algorithms::genotypes::Range as RangeGene;
//! use genetic_algorithms::ga::Ga;
//! use genetic_algorithms::operations::{Selection, Crossover, Mutation, Survivor};
//!
//! fn rastrigin(dna: &[RangeGene<f64>]) -> f64 {
//!     let a = 10.0;
//!     let n = dna.len() as f64;
//!     let sum: f64 = dna.iter().map(|g| {
//!         let x = g.value();
//!         x.powi(2) - a * (2.0 * std::f64::consts::PI * x).cos()
//!     }).sum();
//!     -(a * n + sum)
//! }
//!
//! let mut ga = Ga::new()
//!     .with_population_size(200)
//!     .with_max_generations(500)
//!     .with_genes_per_chromosome(10)
//!     .with_fitness_fn(rastrigin)
//!     .with_selection(Selection::Tournament(5))
//!     .with_crossover(Crossover::Blend(0.5))
//!     .with_mutation(Mutation::Gaussian { sigma: 0.1, probability: 0.1 })
//!     .with_survivor(Survivor::Truncation)
//!     .build()?;
//!
//! let population = ga.run()?;
//! println!("Best fitness: {:?}", population.best_fitness());
//! ```
//!
//! ## Configuration Tips
//!
//! - **Tournament size 3-5** is a good starting point; larger tournament = more selection pressure
//! - **Blend crossover (BLX-α)** works well for continuous problems; use **Uniform** for binary
//! - **Gaussian mutation** with σ=0.1 and p=0.1 is a solid default for real-valued genes
//! - Enable **elitism (1-2)** to prevent loss of the best solution found
//! - Attach a [`LogObserver`](crate::observer::LogObserver) during development for per-generation stats
//!
//! ## When to Choose This vs Differential Evolution
//!
//! | Factor | Ga | DeEngine |
//! |--------|-----|----------|
//! | Problem type | Any (binary, real, symbolic) | Continuous only |
//! | Convergence speed | Moderate | Fast on unimodal |
//! | Exploration | Good | Excellent |
//! | Operator flexibility | Very high (10+ each) | Limited |
//! | Ease of tuning | Moderate | Easy (F, CR) |
//!
//! ## References
//!
//! - Goldberg, D. E. (1989). *Genetic Algorithms in Search, Optimization, and Machine Learning.*
//! - Holland, J. H. (1975). *Adaptation in Natural and Artificial Systems.*
```

**For DeEngine (src/engines/de/engine.rs):**
Replace the current 1-line //! doc with a 60+ line //! block following the same D-04 template. Key content:

- Description: Differential Evolution — population-based stochastic optimization for continuous spaces. Mutation via vector differences (v = x_r1 + F*(x_r2 - x_r3)), crossover via binomial or exponential, selection via one-to-one replacement.
- When to Use: Single-objective, continuous variables only, fast convergence on real-valued problems
- Mandatory params: population_size, max_generations, dimensions, fitness_fn (f64 vector)
- Optional params: strategy (DeMutationStrategy, default Rand1Bin), F scale factor, CR crossover rate, adaptive variant (JADE, L-SHADE)
- Complete example with Rastrigin using DeEngine (f64 slice fitness)
- Cross-reference: When to choose DE vs Ga
- References: Storn & Price (1997), Zhang & Sanderson (2009) for JADE

Ensure all intra-doc links use correct crate paths. Verify against actual struct fields in the Configuration files.
</action>
  <acceptance_criteria>
    - grep -c "^//!" src/engines/ga.rs returns 40+ (expanded from 26)
    - grep "## Description" src/engines/ga.rs matches in //! block
    - grep "## When to Use" src/engines/ga.rs matches in //! block
    - grep "## Quick Reference" src/engines/ga.rs matches in //! block
    - grep "Mandatory Parameters" src/engines/ga.rs matches in //! block
    - grep "## Complete Example" src/engines/ga.rs matches in //! block
    - grep "## Configuration Tips" src/engines/ga.rs matches in //! block
    - grep "When to Choose This vs" src/engines/ga.rs matches in //! block
    - grep -c "^//!" src/engines/de/engine.rs returns 30+ (expanded from 1)
    - grep "## Description" src/engines/de/engine.rs matches in //! block
    - grep "When to Choose This vs" src/engines/de/engine.rs matches in //! block
    - cargo doc --no-deps 2>&1 produces zero warnings from ga.rs or de/engine.rs
  </acceptance_criteria>
  <verify>
    <automated>cargo doc --no-deps 2>&1 | grep -E "warning.*ga\.rs|warning.*de/engine\.rs"; test $? -ne 0 || (echo "FAIL: doc warnings in ga or de"; exit 1)</automated>
  </verify>
  <done>
ga.rs //! block is 60+ lines following D-04 template. de/engine.rs //! block is 60+ lines following D-04 template. Both have Description, When to Use, Quick Reference (parameter tables), Complete Example, Configuration Tips, and cross-references. cargo doc --no-deps is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand ScatterEngine and CellularEngine //! docs to D-04 ficha tecnica standard</name>
  <files>src/engines/scatter/engine.rs, src/engines/cellular/engine.rs</files>
  <read_first>
    - src/engines/scatter/engine.rs — read the first 100 lines for current //! block, ScatterConfiguration fields
    - src/engines/cellular/engine.rs — read the first 100 lines for current //! block, CellularConfiguration fields
    - src/engines/scatter/configuration.rs (if exists) or the Configuration struct in engine.rs
    - src/engines/cellular/configuration.rs (if exists) or the Configuration struct in engine.rs
    - .planning/phases/46-update-the-documentation-to-explain-in-more-details-the-diff/46-RESEARCH.md — D-04 template
  </read_first>
  <action>
Expand the //! module-level documentation for ScatterEngine (src/engines/scatter/engine.rs) and CellularEngine (src/engines/cellular/engine.rs) to the D-04 ficha tecnica standard.

**For ScatterEngine:**
Replace current 6-line //! block with 50+ lines following the D-04 template:
- Description: Reference-set metaheuristic. Five-method cycle: Diversification Generation → Improvement → Reference Set Update → Subset Generation → Solution Combination. Reference set maintains a balance of high-quality and diverse solutions.
- When to Use: Single-objective, continuous or combinatorial, good for problems with expensive fitness evaluation (small population), excels at intensification around promising regions
- Mandatory params: reference_set_size, max_iterations, dimensions
- Optional params: combination_method, local_search, improvement_iterations
- Complete example: ScatterEngine on Rastrigin
- Cross-reference: When to choose ScatterSearch vs Ga
- References: Glover (1977), Laguna & Marti (2003)

**For CellularEngine:**
Replace current 18-line //! block with 50+ lines following the D-04 template:
- Description: 2D toroidal grid where each cell holds one chromosome. Individuals compete and mate only with neighbors, producing slow information diffusion that preserves diversity. Synchronous (all cells update simultaneously) or asynchronous (sequential) update modes.
- When to Use: Single-objective, spatial structure problems, multimodal landscapes, preservation of population diversity
- Mandatory params: grid_rows, grid_cols, generations
- Optional params: neighborhood (Linear5, Compact9, Moore, VonNeumann), update_mode (Synchronous, Asynchronous), selection, crossover, mutation
- Complete example: CellularEngine on Rastrigin
- Cross-reference: When to choose Cellular vs Island model
- References: Whitley (1993), Alba & Dorronsoro (2008)

Ensure all intra-doc links use correct crate paths. Verify against actual struct fields.
</action>
  <acceptance_criteria>
    - grep -c "^//!" src/engines/scatter/engine.rs returns 30+ (expanded from 6)
    - grep "## Description" src/engines/scatter/engine.rs matches
    - grep "## When to Use" src/engines/scatter/engine.rs matches
    - grep "## Quick Reference" src/engines/scatter/engine.rs matches
    - grep "Mandatory Parameters" src/engines/scatter/engine.rs matches
    - grep "## Complete Example" src/engines/scatter/engine.rs matches
    - grep "When to Choose This vs" src/engines/scatter/engine.rs matches
    - grep -c "^//!" src/engines/cellular/engine.rs returns 30+ (expanded from 18)
    - grep "## Description" src/engines/cellular/engine.rs matches
    - grep "## When to Use" src/engines/cellular/engine.rs matches
    - grep "## Quick Reference" src/engines/cellular/engine.rs matches
    - grep "Mandatory Parameters" src/engines/cellular/engine.rs matches
    - grep "## Complete Example" src/engines/cellular/engine.rs matches
    - cargo doc --no-deps 2>&1 produces zero warnings from scatter/ or cellular/
  </acceptance_criteria>
  <verify>
    <automated>cargo doc --no-deps 2>&1 | grep -E "warning.*scatter|warning.*cellular"; test $? -ne 0 || (echo "FAIL: doc warnings in scatter or cellular"; exit 1)</automated>
  </verify>
  <done>
scatter/engine.rs //! block is 50+ lines following D-04 template. cellular/engine.rs //! block is 50+ lines following D-04 template. Both have complete parameter tables, examples, and cross-references. cargo doc --no-deps is clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand AlpsEngine and IslandEngine //! docs to D-04 ficha tecnica standard</name>
  <files>src/engines/alps/engine.rs, src/engines/island/</files>
  <read_first>
    - src/engines/alps/engine.rs — read the first 100 lines for current //! block, AlpsConfiguration fields
    - src/engines/island/mod.rs or src/engines/island/engine.rs — find the main module file with Configuration struct
    - src/engines/alps/configuration.rs (if separate file)
    - .planning/phases/46-update-the-documentation-to-explain-in-more-details-the-diff/46-RESEARCH.md — D-04 template
  </read_first>
  <action>
Expand the //! module-level documentation for AlpsEngine and IslandEngine to the D-04 ficha tecnica standard.

**For AlpsEngine (src/engines/alps/engine.rs):**
Replace current 20-line //! block with 50+ lines following the D-04 template:
- Description: Age-Layered Population Structure — maintains multiple age layers to prevent premature convergence. Each layer has its own age limit. Younger individuals are periodically introduced into the bottom layer. Breeding occurs within and across layers, ensuring young genetic material proliferates while older layers maintain convergence pressure.
- When to Use: Single-objective, problems prone to premature convergence, open-ended optimization, anytime algorithms
- Mandatory params: n_layers, age_gap, population_size
- Optional params: age_scheme (Linear, Fibonacci, Exponential), layer_size_strategy, injection_interval
- Complete example: AlpsEngine on Rastrigin
- Cross-reference: When to choose ALPS vs standard GA
- References: Hornby (2006)

**For IslandEngine (src/engines/island/):**
If no //! doc exists, create a new 40+ line //! block at the top of the main module file. If one exists, expand it:
- Description: Island model — multiple sub-populations (islands) evolve independently with periodic migration of individuals between islands. Maintains diversity through geographic isolation while allowing beneficial traits to spread via migration. Each island can use different operator configurations.
- When to Use: Single-objective, parallel/distributed computation, multimodal landscapes, coarse-grained parallelism
- Mandatory params: n_islands, migration_interval, migration_rate, island_population_size
- Optional params: migration_topology (Ring, FullyConnected, Random), migrant_selection, replacement_strategy
- Complete example: IslandGa on Rastrigin
- Cross-reference: When to choose Island model vs Cellular GA
- References: Cantu-Paz (2000), Whitley et al. (1998)

Because the island module may have its //! doc in a different file (mod.rs, engine.rs, or the main island file), find the correct file by checking which file contains the main Configuration struct and the run() method. Place the //! doc there.

Ensure all intra-doc links use correct crate paths. Verify against actual struct fields.
</action>
  <acceptance_criteria>
    - grep -c "^//!" src/engines/alps/engine.rs returns 30+ (expanded from 20)
    - grep "## Description" src/engines/alps/engine.rs matches
    - grep "## When to Use" src/engines/alps/engine.rs matches
    - grep "## Quick Reference" src/engines/alps/engine.rs matches
    - grep "Mandatory Parameters" src/engines/alps/engine.rs matches
    - grep "## Complete Example" src/engines/alps/engine.rs matches
    - grep "When to Choose This vs" src/engines/alps/engine.rs matches
    - find src/engines/island/ -name "*.rs" | xargs grep -l "## Description" (at least one island file has expanded docs)
    - find src/engines/island/ -name "*.rs" | xargs grep -l "Mandatory Parameters" (at least one file has parameter table)
    - cargo doc --no-deps 2>&1 produces zero warnings from alps/ or island/
  </acceptance_criteria>
  <verify>
    <automated>cargo doc --no-deps 2>&1 | grep -E "warning.*alps|warning.*island"; test $? -ne 0 || (echo "FAIL: doc warnings in alps or island"; exit 1)</automated>
  </verify>
  <done>
alps/engine.rs //! block is 50+ lines following D-04 template. Island module has a 40+ line //! doc following D-04 template. Both have complete parameter tables, examples, and cross-references. cargo doc --no-deps is clean.
  </done>
</task>

</tasks>

<verification>
Overall plan verification after all 3 tasks:
1. `grep -c "^//!" src/engines/ga.rs` returns 40+
2. `grep -c "^//!" src/engines/de/engine.rs` returns 30+
3. `grep -c "^//!" src/engines/scatter/engine.rs` returns 30+
4. `grep -c "^//!" src/engines/cellular/engine.rs` returns 30+
5. `grep -c "^//!" src/engines/alps/engine.rs` returns 30+
6. Island module has //! docs following D-04 template
7. All 6 engines have: Description, When to Use, Quick Reference (tables), Complete Example, Configuration Tips, cross-references
8. `cargo doc --no-deps` zero warnings from all 6 engine files
</verification>

<success_criteria>
- All 6 single-objective + island engine //! doc blocks follow the D-04 ficha tecnica template
- Each has: Description, When to Use, Quick Reference (parameter tables), Complete Example, Configuration Tips, and When to Choose This vs [alternative] section
- Parameter tables reference actual Configuration struct fields (not made-up names)
- Complete examples are compilable against the current API
- `cargo doc --no-deps` produces zero warnings from these engine files
</success_criteria>

<output>
After completion, create `.planning/phases/46-update-the-documentation-to-explain-in-more-details-the-diff/46-02-SUMMARY.md`
</output>
