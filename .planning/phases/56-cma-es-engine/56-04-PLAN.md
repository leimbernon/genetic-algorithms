---
phase: 56-cma-es-engine
plan: 04
type: execute
wave: 4
depends_on: [56-03]
files_modified:
  - examples/cma_es_rastrigin.rs
  - Cargo.toml
autonomous: true
requirements: []
tags: [cma-es, example, wasm-gate, phase-verification]

must_haves:
  truths:
    - "User can run `cargo run --example cma_es_rastrigin` and see CMA-ES converge on the multimodal Rastrigin function"
    - "`cargo check --target wasm32-unknown-unknown` exits 0 — the CMA-ES engine compiles for WebAssembly"
    - "Full test suite (`cargo test && cargo test --features serde`) is green"
    - "`cargo clippy --all-targets -- -D warnings` is clean"
    - "`cargo doc --no-deps` produces zero rustdoc warnings"
  artifacts:
    - path: "examples/cma_es_rastrigin.rs"
      provides: "Runnable example demonstrating CMA-ES on the 5D Rastrigin function"
      contains: "use genetic_algorithms::cma::"
    - path: "Cargo.toml"
      provides: "Optional [[example]] entry for cma_es_rastrigin (if other examples follow this convention)"
      contains: "cma_es_rastrigin"
  key_links:
    - from: "examples/cma_es_rastrigin.rs"
      to: "src/engines/cma/engine.rs"
      via: "use genetic_algorithms::cma::{CmaConfiguration, CmaEngine}"
      pattern: "CmaEngine"
---

<objective>
Land the runnable `cma_es_rastrigin` example demonstrating CMA-ES on a multimodal benchmark (D-07 — planner choice). Run the full Phase 56 verification gate: complete test suite, WASM target compile, clippy, rustdoc. This is the phase-closing plan; on completion Phase 56 ships.

Purpose: Provide a discoverable, runnable demonstration of the new engine (per the established pattern of every other engine having a named example), then enforce the v3.0.0 quality bar (WASM + tests + clippy + docs) before declaring the phase done.

Output: One new example file, optional Cargo.toml `[[example]]` entry, and a passing four-way verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-cma-es-engine/56-CONTEXT.md
@.planning/phases/56-cma-es-engine/56-RESEARCH.md
@.planning/phases/56-cma-es-engine/56-PATTERNS.md
@.planning/phases/56-cma-es-engine/56-03-SUMMARY.md

@examples/
@Cargo.toml
@src/engines/cma/engine.rs
@src/engines/cma/configuration.rs

<interfaces>
Public CMA API surface (after Plan 03):

use genetic_algorithms::cma::{CmaConfiguration, CmaEngine, CmaResult};
use genetic_algorithms::configuration::ProblemSolving;
use genetic_algorithms::chromosomes::Range as RangeChromosome;
use genetic_algorithms::genotypes::Range as RangeGene;
use genetic_algorithms::traits::{LinearChromosome, RealGene};
use genetic_algorithms::rng;

let config = CmaConfiguration::default_for_dim(5)
    .with_sigma0(0.5)
    .with_max_generations(300)
    .with_fitness_target(1e-3)
    .with_problem_solving(ProblemSolving::Minimization);
let mut engine = CmaEngine::new(config, init_fn, rastrigin);
let result = engine.run();
println!("Best fitness: {}", result.best_fitness);

Rastrigin function (standard formulation):
f(x) = 10*n + sum_i (x_i^2 - 10*cos(2*pi*x_i))   for x_i in [-5.12, 5.12]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `examples/cma_es_rastrigin.rs` demonstrating CMA-ES convergence on the 5D Rastrigin function with a LogObserver attached</name>
  <files>examples/cma_es_rastrigin.rs, Cargo.toml</files>
  <read_first>
    examples/
    Cargo.toml
    src/engines/cma/engine.rs
    src/engines/cma/configuration.rs
    .planning/phases/56-cma-es-engine/56-CONTEXT.md
    tests/engines/cma/test_cma.rs
  </read_first>
  <action>
    First list the `examples/` directory and read one existing example file (e.g., the closest analog `examples/de_*` or `examples/scatter_*` if they exist; otherwise read any single-objective example like `examples/rastrigin.rs` if present) to copy the doc-comment style, `fn main()` layout, and observer-attachment pattern.

    Create `examples/cma_es_rastrigin.rs`:

    1. Top-of-file doc comment summarizing the example: "CMA-ES on the 5-dimensional Rastrigin function — a classic multimodal benchmark with many local optima. CMA-ES typically escapes the local minima where plain GA struggles." Include the `cargo run --example cma_es_rastrigin` invocation.

    2. Imports per the `<interfaces>` block above. Also import `genetic_algorithms::observe::LogObserver` (or the actual path used by the existing examples — read one example to confirm the canonical import path) so the example demonstrates the observer integration from D-06.

    3. Define `fn rastrigin(dna: &[RangeGene<f64>]) -> f64` returning the standard Rastrigin formula: `10.0 * n + sum_i (x_i^2 - 10.0 * (2.0 * PI * x_i).cos())` where `x_i = dna[i].real_value()` and `n = dna.len()`. Import `std::f64::consts::PI`.

    4. Define `fn init_population(n: usize) -> Vec<RangeChromosome<f64>>` that constructs `n` chromosomes of dimension 5, each gene sampled uniformly from `[-5.12, 5.12]`. Mirror the test helper pattern in `tests/engines/cma/test_cma.rs`. Use a fixed seed (`rng::set_seed(Some(42))`) for reproducibility.

    5. `fn main()`:
       - Build `let config = CmaConfiguration::default_for_dim(5).with_sigma0(0.5).with_max_generations(300).with_fitness_target(1e-3).with_problem_solving(ProblemSolving::Minimization);`
       - Construct the engine via `CmaEngine::new(config, init_population, rastrigin)`.
       - Attach `LogObserver` via `.with_observer(Arc::new(LogObserver::default()))` (confirm exact construction by reading one existing example that uses LogObserver).
       - Call `.run()`, then `println!("Best fitness: {:.6}", result.best_fitness);` and print the best chromosome's DNA values.

    6. Cargo.toml: check whether other examples have explicit `[[example]] name = "..." path = "..."` entries. If they do, add one for `cma_es_rastrigin`. If examples are auto-discovered (most common in this crate — examples are picked up by Cargo from `examples/` directory without explicit registration), do NOT add anything to Cargo.toml. Verify by running `cargo run --example cma_es_rastrigin` at the end of this task.

    Keep the example short (≤ 80 lines including doc comment) — its purpose is to demonstrate the API, not to benchmark.
  </action>
  <verify>
    <automated>cargo build --example cma_es_rastrigin 2>&amp;1 | tail -5 &amp;&amp; cargo run --example cma_es_rastrigin --quiet 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/cma_es_rastrigin.rs` exists
    - File contains `use genetic_algorithms::cma::`
    - File contains `fn rastrigin(`
    - File contains `fn main()`
    - File contains `.with_observer(` — proves D-06 integration is demonstrated
    - `cargo build --example cma_es_rastrigin` exits 0
    - `cargo run --example cma_es_rastrigin` exits 0 and prints a "Best fitness: ..." line
    - The printed best fitness is finite (not NaN/Inf)
  </acceptance_criteria>
  <done>
    The `cma_es_rastrigin` example is runnable and demonstrates CMA-ES + observer integration.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Phase 56 verification gate — full test suite (default + serde), WASM target compile, clippy, rustdoc</name>
  <files>(none — verification only)</files>
  <read_first>
    .planning/phases/56-cma-es-engine/56-CONTEXT.md
    .planning/phases/56-cma-es-engine/56-RESEARCH.md
  </read_first>
  <action>
    Run the four verification commands required by CLAUDE.md ("All PRs must pass: `cargo test`, `cargo test --features serde`, `cargo clippy`, zero rustdoc warnings") plus the WASM gate mandatory for v3.0.0 (CLAUDE.md "WASM Compatibility (mandatory)"):

    1. `cargo test` — full default test suite.
    2. `cargo test --features serde` — covers serde test paths (CMA-ES does not add serde derives in this phase, but the gate is required by the project quality bar).
    3. `cargo clippy --all-targets -- -D warnings` — zero warnings (treat warnings as errors).
    4. `cargo doc --no-deps` — zero rustdoc warnings. If `cargo doc --no-deps` emits warnings, fail the gate; do not auto-patch documentation here — surface the warnings and stop.
    5. `cargo check --target wasm32-unknown-unknown` — if the target is not installed, run `rustup target add wasm32-unknown-unknown` first.

    Each command must exit 0. If any fails, do NOT attempt to patch — print the failure tail (last 20 lines) and stop. The plan reverts to the appropriate predecessor for fixes.

    Document the gate result in `.planning/phases/56-cma-es-engine/56-04-SUMMARY.md` (written in the final output step):
    - cargo test: PASS / FAIL (N passed, M failed)
    - cargo test --features serde: PASS / FAIL
    - cargo clippy: PASS / FAIL (warning count)
    - cargo doc: PASS / FAIL (rustdoc warning count)
    - cargo check wasm32: PASS / FAIL
  </action>
  <verify>
    <automated>cargo test --quiet 2>&amp;1 | tail -10 &amp;&amp; cargo test --features serde --quiet 2>&amp;1 | tail -5 &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -10 &amp;&amp; cargo doc --no-deps 2>&amp;1 | grep -E "warning|error" | head -20; cargo check --target wasm32-unknown-unknown 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0 with `0 failed`
    - `cargo test --features serde` exits 0 with `0 failed`
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo doc --no-deps 2>&amp;1 | grep -c warning` returns 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - All five commands document their outcomes in `56-04-SUMMARY.md`
  </acceptance_criteria>
  <done>
    Phase 56 ships green: tests + serde + clippy + rustdoc + WASM all pass. The phase is ready for the milestone branch merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example user code | Demonstrates public API; does not interact with untrusted external data |
| CI verification gate | Validates the entire library, not just Phase 56 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-01 | Denial of Service | Example takes too long to converge under typical user invocation | accept | 300 generations × λ ≈ 8 evaluations on 5D Rastrigin completes in well under 1 second on commodity hardware |
| T-56-04-02 | Denial of Service | WASM target regresses due to a stray `Instant::now()` in the CMA engine | mitigate | Task 2 runs `cargo check --target wasm32-unknown-unknown` explicitly; the engine in Plan 03 omits `Instant` usage entirely |
| T-56-04-03 | Tampering | Rustdoc warnings indicate broken intra-doc links exposing wrong public APIs | mitigate | `cargo doc --no-deps` warning count == 0 is a hard acceptance criterion |
| T-56-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
- `cargo run --example cma_es_rastrigin` produces a finite "Best fitness" line
- `cargo test` exits 0
- `cargo test --features serde` exits 0
- `cargo clippy --all-targets -- -D warnings` exits 0
- `cargo doc --no-deps` produces zero warnings
- `cargo check --target wasm32-unknown-unknown` exits 0
</verification>

<success_criteria>
Phase 56 closes with: a runnable `cma_es_rastrigin` example demonstrating the new engine + observer integration; a green full verification gate (tests, serde tests, clippy, rustdoc, WASM target); a phase summary documenting the gate outcomes.
</success_criteria>

<output>
Create `.planning/phases/56-cma-es-engine/56-04-SUMMARY.md` documenting the gate outcomes. Phase 56 is then ready for ROADMAP completion-marking and milestone merge.
</output>
