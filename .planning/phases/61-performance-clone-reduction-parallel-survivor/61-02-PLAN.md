---
phase: 61-performance-clone-reduction-parallel-survivor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/operations/survivor/fitness.rs
  - src/operations/survivor/mu_plus_lambda.rs
  - src/operations/survivor/age.rs
  - src/operations/survivor/mu_comma_lambda.rs
autonomous: true
requirements: []
tags: [performance, rayon, parallel, survivor, wasm]

must_haves:
  truths:
    - "User can run all four sort-based survivor operators (fitness_based, mu_plus_lambda, age_based, mu_comma_lambda) and receive deterministic same-shape output as before (modulo unstable sort tie-breaking)"
    - "On native targets, the four sort operations execute via `rayon::slice::ParallelSliceMut::par_sort_unstable_by`"
    - "On wasm32-unknown-unknown, the four sort operations execute via sequential `sort_unstable_by`"
    - "DeterministicCrowding remains unchanged (order-dependent — explicitly excluded per D-08)"
    - "`cargo test`, `cargo test --features serde`, `cargo clippy --all-targets -- -D warnings`, and `cargo check --target wasm32-unknown-unknown` all pass with zero warnings"
  artifacts:
    - path: "src/operations/survivor/fitness.rs"
      provides: "Parallel + WASM-fallback fitness-based survivor selection"
      contains: "par_sort_unstable_by"
    - path: "src/operations/survivor/mu_plus_lambda.rs"
      provides: "Parallel + WASM-fallback mu+lambda survivor selection"
      contains: "par_sort_unstable_by"
    - path: "src/operations/survivor/age.rs"
      provides: "Parallel + WASM-fallback age-based survivor selection"
      contains: "par_sort_unstable_by"
    - path: "src/operations/survivor/mu_comma_lambda.rs"
      provides: "Parallel + WASM-fallback (mu,lambda) survivor selection"
      contains: "par_sort_unstable_by"
  key_links:
    - from: "src/operations/survivor/fitness.rs"
      to: "rayon::prelude"
      via: "wasm-gated use statement"
      pattern: "cfg\\(not\\(target_arch = \"wasm32\"\\)\\)\\]\\s*use rayon"
    - from: "src/operations/survivor/deterministic_crowding.rs"
      to: "(unchanged)"
      via: "explicit exclusion per D-08"
      pattern: "."
---

<objective>
Apply `par_sort_unstable_by` (with sequential `sort_unstable_by` WASM fallback) to the four sort-based survivor operators per D-06/D-07/D-08/D-09. Eliminate `sort_by` in favor of unstable sort in both branches (D-06 allows unstable; faster on the sequential path too). `DeterministicCrowding` is NOT touched — it is order-dependent and explicitly excluded.

Purpose: Contribute the survivor-sort portion of the ≥10% wall-time reduction success criterion. All four operators are sort-based, run once per generation on the combined (parents + offspring) population, and are pure transformations — ideal for in-place parallel sort.

Output: Four modified survivor files; behavior preserved; native parallel + wasm sequential paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-CONTEXT.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-RESEARCH.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Parallelize fitness.rs and mu_plus_lambda.rs (two-branch sort variants)</name>
  <files>src/operations/survivor/fitness.rs, src/operations/survivor/mu_plus_lambda.rs</files>
  <read_first>
    - src/operations/survivor/fitness.rs (full file — current `sort_by` at lines ~32-47; lines 1-12 imports)
    - src/operations/survivor/mu_plus_lambda.rs (full file — current `sort_by` at lines ~31-44)
    - src/engines/ga.rs lines 155-165 (existing `#[cfg(not(target_arch = "wasm32"))] use rayon::prelude::*;` import pattern — source of truth)
    - src/engines/ga.rs lines 3020-3035 (existing dual-cfg parallel/sequential pattern)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-PATTERNS.md sections on fitness.rs and mu_plus_lambda.rs
  </read_first>
  <action>
For BOTH `src/operations/survivor/fitness.rs` AND `src/operations/survivor/mu_plus_lambda.rs`:

1. Add at the top of the file (after existing `use` block):
   `#[cfg(not(target_arch = "wasm32"))] use rayon::prelude::*;`

2. Replace each `chromosomes.sort_by(|a, b| { ... })` call with a dual-cfg block. The existing comparator closures stay byte-for-byte identical — only the method name and the cfg gates change:
   - Non-wasm branch: `chromosomes.par_sort_unstable_by(|a, b| { /* same closure */ });`
   - Wasm branch: `chromosomes.sort_unstable_by(|a, b| { /* same closure */ });`

Each file has TWO `sort_by` sites: one in the `ProblemSolving != FixedFitness` branch (compares `fitness()`) and one in the `else` branch (compares `fitness_distance(&target)`). BOTH get the dual-cfg replacement. Keep the `target` binding (`let target = limit_configuration.fitness_target.unwrap_or(0.0);`) outside the cfg blocks so it is captured by both branches' closures — `target: f64` is `Copy` so this works in both parallel and sequential closures.

Do NOT modify:
- Truncate/drain logic after the sort
- `debug!` / `trace!` log statements
- Function signatures
- Any other survivor file in this task

Per D-06, unstable sort is acceptable — the previous `sort_by` was already non-deterministic on fitness ties in practice.
  </action>
  <verify>
    <automated>cargo test --test test_survivor 2>&1 | tail -20 && cargo check --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "par_sort_unstable_by" src/operations/survivor/fitness.rs` == 2
    - `grep -c "par_sort_unstable_by" src/operations/survivor/mu_plus_lambda.rs` == 2
    - `grep -c "sort_unstable_by" src/operations/survivor/fitness.rs` >= 4 (2 par + 2 seq)
    - `grep -c "sort_unstable_by" src/operations/survivor/mu_plus_lambda.rs` >= 4
    - `grep -c "sort_by(" src/operations/survivor/fitness.rs` == 0 (all replaced)
    - `grep -c "sort_by(" src/operations/survivor/mu_plus_lambda.rs` == 0
    - `grep -c 'cfg(not(target_arch = "wasm32"))\] use rayon' src/operations/survivor/fitness.rs` == 1
    - `grep -c 'cfg(not(target_arch = "wasm32"))\] use rayon' src/operations/survivor/mu_plus_lambda.rs` == 1
    - `cargo test` exits 0 (existing survivor tests still pass — behavior preserved)
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>fitness.rs and mu_plus_lambda.rs use par_sort_unstable_by on native + sort_unstable_by on wasm; all four sort sites converted; tests green; wasm gate green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Parallelize age.rs and mu_comma_lambda.rs (single-sort + sub-vec sort variants)</name>
  <files>src/operations/survivor/age.rs, src/operations/survivor/mu_comma_lambda.rs</files>
  <read_first>
    - src/operations/survivor/age.rs (full file — current `sort_by_key(Reverse)` at line ~20)
    - src/operations/survivor/mu_comma_lambda.rs (full file — current `sort_by` block at lines ~41-54, after the `retain(|c| c.age() == 0)` filter at line ~32)
    - src/operations/survivor/fitness.rs (the pattern just applied in Task 1 — reference for dual-cfg structure)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-PATTERNS.md sections on age.rs and mu_comma_lambda.rs
  </read_first>
  <action>
**`src/operations/survivor/age.rs`:**

1. Add at the top of the file (after existing `use` block):
   `#[cfg(not(target_arch = "wasm32"))] use rayon::prelude::*;`

2. Replace the single `chromosomes.sort_by_key(|a| std::cmp::Reverse(a.age()));` call with a dual-cfg block. Convert the key-based `Reverse` form to an explicit comparator (per 61-PATTERNS.md note — avoids needing `par_sort_unstable_by_key`):
   - Non-wasm: `chromosomes.par_sort_unstable_by(|a, b| b.age().cmp(&a.age()));`
   - Wasm: `chromosomes.sort_unstable_by(|a, b| b.age().cmp(&a.age()));`

`age() -> usize` implements `Ord` — `cmp` returns descending order via `b.age().cmp(&a.age())` (equivalent to `Reverse`). The `std::cmp::Reverse` import may become unused — remove it if so. Keep `debug!`/`trace!` log calls unchanged.

**`src/operations/survivor/mu_comma_lambda.rs`:**

1. Add at the top of the file (after existing `use` block):
   `#[cfg(not(target_arch = "wasm32"))] use rayon::prelude::*;`

2. Per D-07, the sort applies to the offspring sub-vec only — the `retain(|c| c.age() == 0)` at line ~32 has already filtered to offspring. The sort block at lines ~41-54 contains the same two-branch structure as `fitness.rs` (one branch for `ProblemSolving != FixedFitness` using `fitness()`, one `else` branch using `fitness_distance(&target)`). Apply the SAME dual-cfg transformation as Task 1 to both `sort_by` sites — keep the `target` binding outside the cfg blocks so both closures capture it.

Do NOT modify:
- The `retain` filter call
- Truncate/drain logic
- Function signatures
- `DeterministicCrowding` (entirely out of scope per D-08)
  </action>
  <verify>
    <automated>cargo test --test test_survivor 2>&1 | tail -20 && cargo check --target wasm32-unknown-unknown 2>&1 | tail -5 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "par_sort_unstable_by" src/operations/survivor/age.rs` == 1
    - `grep -c "par_sort_unstable_by" src/operations/survivor/mu_comma_lambda.rs` == 2
    - `grep -c "sort_by_key" src/operations/survivor/age.rs` == 0 (Reverse-key form gone)
    - `grep -c "sort_by(" src/operations/survivor/mu_comma_lambda.rs` == 0
    - `grep -c 'cfg(not(target_arch = "wasm32"))\] use rayon' src/operations/survivor/age.rs` == 1
    - `grep -c 'cfg(not(target_arch = "wasm32"))\] use rayon' src/operations/survivor/mu_comma_lambda.rs` == 1
    - `grep -c "par_sort_unstable_by\|par_sort_unstable_by_key" src/operations/survivor/deterministic_crowding.rs` == 0 (untouched per D-08)
    - `cargo test` exits 0
    - `cargo test --features serde` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>age.rs and mu_comma_lambda.rs converted to par_sort_unstable_by (native) + sort_unstable_by (wasm); DeterministicCrowding untouched; full CI matrix green.</done>
</task>

</tasks>

<verification>
- `cargo test` exits 0
- `cargo test --features serde` exits 0
- `cargo clippy --all-targets -- -D warnings` exits 0
- `cargo check --target wasm32-unknown-unknown` exits 0
- Survivor unit/integration tests pass with no behavioral changes
- `DeterministicCrowding` remains unchanged (grep confirms zero `par_sort_unstable_by` in `deterministic_crowding.rs`)
</verification>

<success_criteria>
- Four survivor operators (`fitness`, `mu_plus_lambda`, `age`, `mu_comma_lambda`) execute sort via `par_sort_unstable_by` on native, `sort_unstable_by` on wasm32
- `DeterministicCrowding` untouched per D-08
- Zero new warnings; full CI matrix passes
- All sort sites use unstable sort (D-06 explicitly accepts unstable tie-breaking)
</success_criteria>

<output>
Create `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-02-SUMMARY.md` when done.
</output>
