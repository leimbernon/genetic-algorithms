---
phase: 61-performance-clone-reduction-parallel-survivor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/rastrigin.rs
  - Cargo.toml
autonomous: true
requirements: []
tags: [performance, benchmark, criterion, rastrigin]

must_haves:
  truths:
    - "User can run `cargo bench --bench rastrigin` and see three Criterion benchmarks (dim=10, dim=20, dim=50) execute"
    - "Each benchmark constructs a Ga<RangeChromosome<f64>> at population size 500 with bounds [-5.12, 5.12]"
    - "Each benchmark runs `Ga::run()` for max_generations=50 against the inline Rastrigin fitness function"
    - "`cargo bench --bench rastrigin -- --test` exits 0 (smoke compile + 1 iteration)"
    - "`cargo check --target wasm32-unknown-unknown` continues to pass (benches do not affect wasm gate)"
  artifacts:
    - path: "benches/rastrigin.rs"
      provides: "Criterion benchmark harness for Rastrigin GA throughput measurement"
      contains: "criterion_main"
    - path: "Cargo.toml"
      provides: "[[bench]] name = \"rastrigin\" registration"
      contains: "name = \"rastrigin\""
  key_links:
    - from: "benches/rastrigin.rs"
      to: "genetic_algorithms::ga::Ga"
      via: "Ga::new() builder chain"
      pattern: "Ga::new\\(\\)"
    - from: "Cargo.toml"
      to: "benches/rastrigin.rs"
      via: "[[bench]] entry"
      pattern: "name = \"rastrigin\""
---

<objective>
Create the Rastrigin benchmark harness (`benches/rastrigin.rs`) that measures `Ga::run()` wall-time at population=500 across three dimensionalities (10, 20, 50). This benchmark is the measurement instrument for the ROADMAP Phase 61 success criterion: ≥10% wall-time reduction after the Plan 02 + Plan 03 optimizations land.

Purpose: Establish the before/after measurement baseline before any optimization work touches `ga.rs` or the survivor operators. The benchmark must compile and run a single smoke iteration before merge.

Output: `benches/rastrigin.rs` (new file) + `Cargo.toml` `[[bench]]` registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-CONTEXT.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-RESEARCH.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create benches/rastrigin.rs Criterion harness</name>
  <files>benches/rastrigin.rs</files>
  <read_first>
    - benches/ga_run.rs (the canonical pattern for Ga construction + iter_batched + BenchmarkId — copy structure)
    - src/types/genotypes/range.rs (confirm RangeGenotype<f64> value accessor — per 61-PATTERNS.md line ~362 it is `value()` at range.rs line 110)
    - src/types/chromosomes/range.rs (confirm RangeChromosome<f64> construction signature)
    - src/engines/ga.rs lines 1-200 (confirm public Ga<U> API + builder methods available)
    - src/configuration.rs (confirm ProblemSolving::Minimization variant + LimitConfiguration field names)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-PATTERNS.md (sections "benches/rastrigin.rs (benchmark, batch) — NEW FILE" and "Shared Patterns")
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-CONTEXT.md (decisions D-10, D-11, D-12, D-13)
  </read_first>
  <action>
Create `benches/rastrigin.rs` following the exact criterion+iter_batched pattern in `benches/ga_run.rs`. Per D-10/D-11/D-12:

- Inline `rastrigin(genes: &[RangeGenotype<f64>]) -> f64` implementing `A*n + sum(x_i^2 - A*cos(2*pi*x_i))` with `A=10.0`. Use `RangeGenotype::value()` (verified at src/types/genotypes/range.rs line 110) for gene access. Use `std::f64::consts::PI`.
- Helper `build_rastrigin_ga(population_size: usize, dims: usize, max_generations: usize) -> Ga<RangeChromosome<f64>>` that constructs a `Population<RangeChromosome<f64>>` of `population_size` chromosomes, each containing `dims` `RangeGenotype<f64>` genes with bounds `(-5.12, 5.12)`, fills each gene with a uniformly-sampled initial value in that range, then builds the `Ga` via builder methods: `.with_problem_solving(ProblemSolving::Minimization)`, `.with_selection_method(Selection::Tournament)`, `.with_crossover_method(Crossover::Uniform)`, `.with_mutation_method(Mutation::Swap)`, `.with_survivor_method(Survivor::Fitness)`, `.with_max_generations(max_generations)`, attach the `rastrigin` fitness fn, and seed the population. Mirror the construction approach used by `benches/ga_run.rs::build_ga` (read it first to copy exact builder method names, since builder API may differ from the example sketch in 61-PATTERNS.md).
- Benchmark function `benchmark_rastrigin(c: &mut Criterion)` that creates a `c.benchmark_group("rastrigin")`, iterates `dims = vec![10usize, 20, 50]`, and for each dim emits `group.bench_with_input(BenchmarkId::new("Ga::run", format!("pop_500_dim_{}", dim)), &dim, |b, &d| b.iter_batched(|| build_rastrigin_ga(500, d, 50), |mut ga| { let _ = ga.run(); }, BatchSize::SmallInput))`.
- `criterion_group!` named `rastrigin_benchmarks` and `criterion_main!(rastrigin_benchmarks)`.
- Use `max_generations = 50` per the Claude's-discretion note in 61-RESEARCH.md.
- Per D-12, the fitness fn is inline ONLY — do not add any public function to the library.

If construction patterns differ from `ga_run.rs` (e.g. RangeChromosome requires a different constructor, or `with_max_generations` is actually `.with_limit_configuration(LimitConfiguration { max_generations: Some(50), ... })`), follow `ga_run.rs` verbatim — it is the source of truth for the working build pattern. Do not invent builder method names.
  </action>
  <verify>
    <automated>cargo bench --bench rastrigin -- --test</automated>
  </verify>
  <acceptance_criteria>
    - `benches/rastrigin.rs` exists and compiles
    - `grep -c "criterion_main" benches/rastrigin.rs` == 1
    - `grep -c "fn rastrigin" benches/rastrigin.rs` == 1
    - `grep -c "fn build_rastrigin_ga" benches/rastrigin.rs` == 1
    - `grep -c "pop_500_dim_" benches/rastrigin.rs` >= 1
    - `grep -E "10\.0_f64|10\.0f64|let a = 10" benches/rastrigin.rs` returns at least one match (A=10 constant present)
    - `grep -c "5.12" benches/rastrigin.rs` >= 1 (Rastrigin bounds present)
    - `grep -c "iter_batched" benches/rastrigin.rs` == 1
    - `cargo bench --bench rastrigin -- --test` exits 0
    - `cargo check --target wasm32-unknown-unknown` exits 0 (benches not built for wasm; library still compiles)
  </acceptance_criteria>
  <done>Rastrigin benchmark file compiles, smoke-runs one iteration via `cargo bench -- --test`, three benchmarks declared for dims 10/20/50 at pop=500.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register rastrigin bench in Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (read full file — locate existing `[[bench]]` entries to determine placement and confirm `harness = false` is the convention)
  </read_first>
  <action>
Add a new `[[bench]]` block to `Cargo.toml` immediately after the last existing `[[bench]]` entry:

```
[[bench]]
name = "rastrigin"
harness = false
```

Do not modify any other section of `Cargo.toml`. Do not change the `version` field. Do not touch `[dependencies]` (rayon 1.10 and criterion 0.8.2 are already present per 61-RESEARCH.md "Standard Stack" verification).
  </action>
  <verify>
    <automated>cargo bench --bench rastrigin -- --test 2>&1 | grep -v "no benchmark target named"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 'name = "rastrigin"' Cargo.toml | grep -c "harness = false"` == 1
    - `cargo bench --bench rastrigin -- --test` exits 0 (no "no benchmark target named rastrigin" error)
    - `cargo test --no-run` exits 0 (Cargo.toml still parses)
  </acceptance_criteria>
  <done>Cargo.toml registers the rastrigin bench; `cargo bench --bench rastrigin -- --test` finds and runs it.</done>
</task>

</tasks>

<verification>
- `cargo bench --bench rastrigin -- --test` exits 0
- `cargo test --no-run` exits 0
- `cargo check --target wasm32-unknown-unknown` exits 0
- `cargo clippy --benches -- -D warnings` exits 0
</verification>

<success_criteria>
- Rastrigin benchmark harness compiles and smoke-runs (1 iteration per bench)
- Three Criterion benchmarks exposed: pop_500_dim_10, pop_500_dim_20, pop_500_dim_50
- Inline `rastrigin` fitness fn uses A=10, [-5.12, 5.12] bounds, 2*PI cos term
- No new library dependencies added
- WASM compile gate still green (benches excluded from wasm target)
</success_criteria>

<output>
Create `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-01-SUMMARY.md` when done.
</output>
