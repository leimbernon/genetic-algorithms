---
phase: 61-performance-clone-reduction-parallel-survivor
plan: 04
type: execute
wave: 2
depends_on:
  - 61-01
  - 61-02
  - 61-03
files_modified:
  - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md
autonomous: false
requirements: []
tags: [phase-gate, verification, benchmark, ci]

must_haves:
  truths:
    - "User can read `61-BENCH-RESULTS.md` and see baseline (pre-changes) vs. post-changes wall-time for rastrigin pop=500 at dims 10, 20, 50"
    - "ROADMAP success criterion #1 (≥10% wall-time reduction on rastrigin at pop=500) is verified against the recorded numbers"
    - "Full CI matrix is green: cargo test, cargo test --features serde, cargo clippy --all-targets -- -D warnings, cargo check --target wasm32-unknown-unknown, cargo doc --no-deps (zero warnings)"
    - "Survivor parallelism present in fitness/mu_plus_lambda/age/mu_comma_lambda (cross-grep verifies); DeterministicCrowding untouched"
    - "GaObserver::on_new_best signature confirmed as `&U` in mod.rs and all built-in impls"
  artifacts:
    - path: ".planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md"
      provides: "Bench baseline + post-change numbers + percent improvement table per dim"
      contains: "% improvement"
  key_links:
    - from: "61-BENCH-RESULTS.md"
      to: "benches/rastrigin.rs"
      via: "criterion JSON output / stderr capture"
      pattern: "rastrigin"
---

<objective>
Phase 61 verification gate. Run the rastrigin benchmark twice — once at the merge base (baseline, before any of Plans 02/03 land in-tree) and once at the tip of the phase work — to confirm the ROADMAP ≥10% wall-time reduction success criterion. Record numbers in `61-BENCH-RESULTS.md`. Run the full CI matrix one final time. Then create the phase SUMMARY.

Purpose: This is the measurement-driven gate. Plan 01 created the instrument; Plans 02/03 applied the optimizations; Plan 04 measures whether the optimizations delivered the promised improvement and gates the phase on the result.

Output: `61-BENCH-RESULTS.md` + per-plan SUMMARYs + phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-CONTEXT.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-RESEARCH.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-01-SUMMARY.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-02-SUMMARY.md
@.planning/phases/61-performance-clone-reduction-parallel-survivor/61-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture baseline and post-change rastrigin bench numbers</name>
  <files>.planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md</files>
  <read_first>
    - benches/rastrigin.rs (confirm benchmarks exist and are named pop_500_dim_10/20/50)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-01-SUMMARY.md (benchmark harness summary)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-02-SUMMARY.md (parallel survivor summary — confirm operators converted)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-03-SUMMARY.md (clone reduction summary — confirm observer/fallback changes)
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-CONTEXT.md decision D-13
  </read_first>
  <action>
Execute the measurement protocol for D-13 (before/after wall-time comparison).

**Step 1 — Identify the baseline.** Plan 01 (benches/rastrigin.rs + Cargo.toml) is part of the phase work but does not affect runtime — it only adds a new bench file. The baseline must therefore be measured at a tree state where:
- `benches/rastrigin.rs` and the Cargo.toml `[[bench]]` entry are present (so the bench can run)
- BUT the Plan 02 (parallel survivor sort) and Plan 03 (observer &U + crossover fallback) changes are NOT applied

**Primary recommended baseline path — pre-phase parent worktree (most reliable):**

This path does not depend on Plans 01/02/03 being in separate commits. It works even if all phase work was squashed into a single commit.

1. Identify the commit at the start of Phase 61 work on the milestone branch. Use `git log --oneline --first-parent milestone/v3.0.0 -- .planning/phases/61-performance-clone-reduction-parallel-survivor/ | tail -1` to find the first commit that touched this phase's planning directory, then use that commit's parent (`<sha>^`) as the pre-phase commit. If unsure, fall back to `git merge-base HEAD main` and use that.
2. Create a worktree at the pre-phase commit: `git worktree add /tmp/phase61-baseline <pre-phase-sha>`.
3. Copy `benches/rastrigin.rs` from the current working tree into the worktree (`cp benches/rastrigin.rs /tmp/phase61-baseline/benches/rastrigin.rs`). If `/tmp/phase61-baseline/benches/` does not exist, create it.
4. Open `/tmp/phase61-baseline/Cargo.toml` and add the `[[bench]]` entry for rastrigin (copy the entry from the current working tree's Cargo.toml). If a `[[bench]]` section for rastrigin already exists, leave it.
5. In the baseline worktree: `cd /tmp/phase61-baseline && cargo bench --bench rastrigin 2>&1 | tee /tmp/phase61-baseline.txt`. Capture criterion-reported time for each of `pop_500_dim_10`, `pop_500_dim_20`, `pop_500_dim_50` (median or mean — whichever criterion emits as the primary).
6. Clean up: `git worktree remove --force /tmp/phase61-baseline`.

**Fallback baseline path — SHA-hunting (use only if pre-phase worktree path is blocked):**

Only attempt this if Plans 02 and 03 were committed as separate, identifiable commits AND the pre-phase parent path above is not usable for some reason. Identify the commit hashes for Plans 02 and 03 via `git log --oneline -n 20 | head`. Create a worktree from the commit immediately before the earlier of Plan 02/Plan 03 lands, but where Plan 01 has already been merged. Cherry-pick Plan 01 if needed. This path is fragile because it depends on commit granularity matching plan boundaries and will fail under squash-merge workflows.

**Step 2 — Measure post-change.** In the main working tree (with all of Plans 01/02/03 applied), run: `cargo bench --bench rastrigin 2>&1 | tee /tmp/phase61-post.txt`. Capture the same three numbers.

**Step 3 — Write `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md`** with this structure:

```
# Phase 61 — Benchmark Results

**Measurement date:** YYYY-MM-DD
**Machine:** <output of `uname -a` or relevant hardware notes>
**Benchmark:** benches/rastrigin.rs (pop=500, max_generations=50, RangeChromosome<f64>, bounds [-5.12, 5.12])
**Baseline path used:** <pre-phase-worktree | sha-hunting>
**Baseline commit:** <pre-phase-sha>

## Results

| Dimensions | Baseline (mean) | Post-change (mean) | Improvement | ROADMAP gate (≥10%) |
|------------|-----------------|--------------------|-------------|---------------------|
| 10         | <X> ms          | <Y> ms             | <Z>%        | PASS / FAIL         |
| 20         | <X> ms          | <Y> ms             | <Z>%        | PASS / FAIL         |
| 50         | <X> ms          | <Y> ms             | <Z>%        | PASS / FAIL         |

## Headline

ROADMAP success criterion #1 is **<MET / NOT MET>**: pop=500 rastrigin wall-time reduction is <best %>% at dim=<dim>.

## Raw output

### Baseline (`cargo bench --bench rastrigin` at <baseline-commit-sha>)
```
<paste relevant criterion lines from /tmp/phase61-baseline.txt>
```

### Post-change (`cargo bench --bench rastrigin` at HEAD)
```
<paste relevant criterion lines from /tmp/phase61-post.txt>
```

## Notes
- <any caveats: warmup behavior, variance, noise>
```

**If the ≥10% gate is NOT met at any of the three dimensions** (i.e. all three are <10% improvement), this is a soft fail — the executor records the numbers honestly. The headline MUST literally say "NOT MET" so Task 4 can detect this by reading the file. Then PAUSE at the checkpoint in Task 3 to surface the gap for user decision. Do not silently mark the phase complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md && grep -c "Baseline" .planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md` exists
    - File contains a table with header `| Dimensions | Baseline` and three data rows for dims 10, 20, 50
    - File contains the literal string "ROADMAP gate (≥10%)" in the header row
    - File contains a "Headline" section explicitly stating MET or NOT MET (literal strings)
    - File contains raw criterion output blocks for both baseline and post-change
    - File records which baseline path was used (pre-phase-worktree or sha-hunting) and the baseline commit SHA
    - At least one PASS in the gate column (else the executor must pause at Task 3 checkpoint instead of auto-advancing)
  </acceptance_criteria>
  <done>Bench-results file written with baseline+post numbers, % improvement per dim, and explicit MET/NOT-MET headline for the ROADMAP gate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full CI matrix verification</name>
  <files><!-- Verification-only task: runs CI gates and grep-based structural checks; writes no files and modifies no artifacts. STATE/ROADMAP updates happen in Task 4. --></files>
  <read_first>
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-CONTEXT.md (phase scope — confirm no out-of-scope changes leaked in)
    - CLAUDE.md (project CI gate convention — cargo test, cargo test --features serde, cargo clippy, cargo doc, cargo check wasm32)
  </read_first>
  <action>
Run each CI gate and confirm exit 0 + zero warnings. If any gate fails, stop and surface the failure before proceeding to Task 3.

1. `cargo test 2>&1 | tail -20` — must exit 0
2. `cargo test --features serde 2>&1 | tail -20` — must exit 0
3. `cargo clippy --all-targets --all-features -- -D warnings 2>&1 | tail -30` — must exit 0, zero warnings
4. `cargo check --target wasm32-unknown-unknown 2>&1 | tail -10` — must exit 0
5. `cargo doc --no-deps 2>&1 | tail -20` — must exit 0, zero rustdoc warnings
6. Grep-based structural confirmations (verify no regression vs. the per-plan acceptance criteria already enforced):
   - `grep -c "par_sort_unstable_by" src/operations/survivor/fitness.rs` == 2
   - `grep -c "par_sort_unstable_by" src/operations/survivor/mu_plus_lambda.rs` == 2
   - `grep -c "par_sort_unstable_by" src/operations/survivor/age.rs` == 1
   - `grep -c "par_sort_unstable_by" src/operations/survivor/mu_comma_lambda.rs` == 2
   - `grep -c "par_sort_unstable_by" src/operations/survivor/deterministic_crowding.rs` == 0
   - `grep -E "fn on_new_best\(&self, [^)]*: &U\)" src/observe/observer/mod.rs` returns 1 match
   - `grep -E "on_new_best\(i, &self\.population\.best_chromosome\)" src/engines/ga.rs` returns 1 match
   - `grep -c "best.clone()" src/observe/observer/composite.rs` == 0
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5 && cargo test --features serde 2>&1 | tail -5 && cargo clippy --all-targets --all-features -- -D warnings 2>&1 | tail -5 && cargo check --target wasm32-unknown-unknown 2>&1 | tail -5 && cargo doc --no-deps 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All five CI commands above exit 0
    - `cargo clippy` reports zero warnings
    - `cargo doc --no-deps` reports zero rustdoc warnings
    - All grep-based structural checks pass with the exact counts listed
  </acceptance_criteria>
  <done>Full CI matrix green; structural confirmations match per-plan acceptance criteria; no regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — phase gate</name>
  <what-built>
    Phase 61 work is complete:
    - `benches/rastrigin.rs` + Cargo.toml registration (Plan 01)
    - Parallel survivor sort in fitness/mu_plus_lambda/age/mu_comma_lambda with WASM fallback (Plan 02)
    - GaObserver::on_new_best signature changed to &U; CompositeObserver fan-out clone removed; ga.rs on_new_best call site uses &chromosome; crossover fallback clones audited (Plan 03)
    - Full CI matrix green
    - 61-BENCH-RESULTS.md captures baseline vs. post-change wall-time for pop=500 at dims 10/20/50
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md`. Confirm the headline reports MET (≥10% reduction at one or more dims) or NOT MET.
    2. If MET: approve the phase. The user accepts the numbers as the v3.0.0 baseline for this success criterion.
    3. If NOT MET: decide between:
       - (a) Accept the lower improvement and amend the ROADMAP success criterion to reflect actual delivered % (e.g. "≥5% on dim=50")
       - (b) Reject the phase and gap-plan additional optimizations (e.g. revisit selection-output clone D-02 which was descoped, or extend to DeterministicCrowding)
       - (c) Re-run the benchmark on different hardware (CI vs. dev machine variance)
    4. Confirm that 61-03-SUMMARY.md notes whether the crossover fallback else-branch already had only-conditional clones or whether an unconditional upstream clone was eliminated. This determines how to phrase the SUMMARY claim.
    5. Confirm that 61-03-SUMMARY.md notes any sub-observer traits (IslandGaObserver, Nsga2Observer, etc.) found with owned-U callbacks that were left in place for follow-up — these may need a follow-up phase under v3.0.0 if breaking-change consistency matters.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed (Task 4 will read 61-BENCH-RESULTS.md directly to decide whether ROADMAP amendment is required), or describe issues to address.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write phase SUMMARY and update STATE.md / ROADMAP.md</name>
  <files>.planning/phases/61-performance-clone-reduction-parallel-survivor/61-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-01-SUMMARY.md
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-02-SUMMARY.md
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-03-SUMMARY.md
    - .planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md (REQUIRED — Task 4 amendment logic is driven by this file's content, NOT by the user's resume-signal text)
    - .planning/STATE.md (current Phase 61 status)
    - .planning/ROADMAP.md (Phase 61 entry + Progress table)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
  </read_first>
  <action>
**ROADMAP amendment logic — driven by file content, not by resume-signal parsing:**

Read `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md`. Locate the "Headline" section. The headline contains either the literal string "**MET**" or the literal string "**NOT MET**" (per Task 1's required output format).

- **If the headline contains "NOT MET":** The ROADMAP success criterion #1 was not satisfied by the measured numbers. Amend the ROADMAP entry to reflect the actual delivered improvement, using the best per-dim percent improvement from the results table as the new threshold. For example, if the best dim achieved 6.2% improvement, amend Success Criterion #1 in the Phase 61 ROADMAP block to read "≥5% wall-time reduction on rastrigin at pop=500 (amended after bench results — see 61-BENCH-RESULTS.md; original target was ≥10%)". Round down to the nearest whole percent that is still met by at least one dim. Do NOT delete the criterion; amend it in place with an explicit "(amended after bench results)" note.
- **If the headline contains "MET":** No amendment to Success Criterion #1 is required. Leave it as written.
- **If the headline contains neither literal string:** Treat this as a Task 1 deliverable failure — stop, report the malformed bench-results file, and do not proceed with SUMMARY writes.

Do NOT parse the user's Task 3 resume-signal text to make this decision. The bench-results file is the single source of truth.

**Write `61-SUMMARY.md`** following the SUMMARY template. Required content:

- Phase goal recap and outcome (MET or NOT-MET against ≥10% gate, citing 61-BENCH-RESULTS.md headline verbatim)
- What shipped: benchmark harness; parallel survivor sort in 4 operators; observer &U signature breaking change; crossover-fallback clone audit
- Files modified (consolidated from the three plan SUMMARYs)
- Key decisions exercised: D-01 (crossover fallback), D-03/D-04/D-05 (observer breaking change), D-06/D-07/D-08/D-09 (parallel survivor + WASM fallback + DeterministicCrowding exclusion), D-10/D-11/D-12/D-13 (benchmark)
- Breaking change call-out for v3.0.0: `GaObserver<U>::on_new_best` signature changed from `(usize, U)` to `(usize, &U)`
- Tech stack / patterns reusable in future phases (parallel sort + WASM fallback pattern; observer breaking-change cascade pattern)
- Follow-up items: any sub-observer traits not yet migrated (per Plan 03 SUMMARY); D-02 selection-output clone deferred; DeterministicCrowding parallelism deferred
- If ROADMAP Success Criterion #1 was amended above, note the amendment and the old/new thresholds explicitly

**Update `.planning/STATE.md`:**
- `progress.completed_phases`: 17 → 18
- `progress.completed_plans`: 86 → 90 (add Plans 61-01 through 61-04)
- `progress.percent`: recompute
- `stopped_at`: "Phase 61 complete — clone reduction + parallel survivor shipped"
- `last_activity`: today's date + brief description
- Add to "Decisions (phase 61)" section: the locked decisions exercised
- Update "Current Position" to point at Phase 62 as next

**Update `.planning/ROADMAP.md`:**
- Mark Phase 61's four plan checkboxes complete: `- [x] 61-01-PLAN.md ...` etc.
- Change Phase 61 row in the Progress table: `TBD | Pending | —` → `4/4 | Complete | <date>`
- If the headline-driven amendment logic above determined that the ≥10% gate was NOT MET, apply the amendment to Phase 61 Success Criterion #1 inline with the explicit "(amended after bench results)" note. Otherwise leave the criterion unchanged.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-performance-clone-reduction-parallel-survivor/61-SUMMARY.md && grep -c "Phase 61" .planning/STATE.md && grep -c "61-01-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `61-SUMMARY.md` exists and contains sections for: outcome, files modified, decisions exercised, breaking change call-out, follow-ups
    - `grep -c "GaObserver" .planning/phases/61-performance-clone-reduction-parallel-survivor/61-SUMMARY.md` >= 1
    - `grep -c "BREAKING\|breaking change\|breaking" .planning/phases/61-performance-clone-reduction-parallel-survivor/61-SUMMARY.md` >= 1
    - `.planning/STATE.md` shows `completed_phases: 18` (or current+1)
    - `.planning/ROADMAP.md` Phase 61 progress row shows `4/4 | Complete |`
    - `.planning/ROADMAP.md` shows all four `- [x] 61-0N-PLAN.md` entries checked
    - If 61-BENCH-RESULTS.md headline contained "NOT MET", ROADMAP Phase 61 Success Criterion #1 contains the substring "(amended after bench results)"; if it contained "MET", the criterion is unchanged
  </acceptance_criteria>
  <done>Phase SUMMARY written; STATE and ROADMAP updated to reflect Phase 61 complete; amendment logic driven by 61-BENCH-RESULTS.md content; ready for git commit + Phase 62 planning.</done>
</task>

</tasks>

<verification>
- 61-BENCH-RESULTS.md exists with baseline + post-change numbers and explicit gate verdict
- Full CI matrix green
- All grep-based structural confirmations pass
- 61-SUMMARY.md written
- STATE.md + ROADMAP.md updated
</verification>

<success_criteria>
- ROADMAP success criterion #1 (≥10% wall-time reduction) is measured against real benchmark numbers and the verdict is recorded
- All four CI gates green (test, test --features serde, clippy, wasm check) + doc --no-deps zero warnings
- Phase SUMMARY captures breaking change and follow-up items
- STATE.md and ROADMAP.md reflect phase completion
</success_criteria>

<output>
Phase SUMMARY at `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-SUMMARY.md`. Bench results at `.planning/phases/61-performance-clone-reduction-parallel-survivor/61-BENCH-RESULTS.md`.
</output>
