---
phase: 62
plan: 03
type: execute
wave: 3
depends_on:
  - 62-01
  - 62-02
files_modified:
  - examples/surrogate_rastrigin.rs
  - Cargo.toml
  - .planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A runnable example demonstrates surrogate prescreening reducing true fitness call count on a multimodal problem"
    - "`cargo test`, `cargo test --features serde`, `cargo clippy --all-targets -- -D warnings`, and `cargo doc --no-deps` all pass with zero warnings"
    - "`cargo check --target wasm32-unknown-unknown` passes (no rayon or Instant introduced in the new code paths)"
    - "Phase 62 SUMMARY records SurrogateModel as a publicly re-exported trait, GenerationStats.true_fitness_calls field, and the with_surrogate builder method"
  artifacts:
    - path: "examples/surrogate_rastrigin.rs"
      provides: "End-to-end demonstration of .with_surrogate() on a Rastrigin run, asserting true_fitness_calls < offspring_count for at least one generation"
      contains: "with_surrogate"
    - path: ".planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md"
      provides: "Phase 62 final summary for downstream phases"
      contains: "SurrogateModel"
  key_links:
    - from: "examples/surrogate_rastrigin.rs"
      to: "src/fitness/surrogate.rs + src/engines/ga.rs"
      via: "Arc::new(MySurrogate).with_surrogate(_, 0.4)"
      pattern: "with_surrogate"
    - from: "Cargo.toml"
      to: "examples/surrogate_rastrigin.rs"
      via: "[[example]] entry"
      pattern: "name = \"surrogate_rastrigin\""
---

<objective>
Close Phase 62 with a runnable example, the full CI matrix, and the phase summary.
This is the verification-gate plan: no new library code, only demonstration and validation.

Purpose: Prove end-to-end that `SurrogateModel` reduces true fitness calls in a
realistic run (Rastrigin / multimodal), and clear all CI gates (test + serde + clippy +
rustdoc + WASM) before declaring the phase complete.

Output: One example file, one Cargo.toml registration, one phase SUMMARY, and a
green run of the entire CI matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-surrogate-assisted-evaluation/62-CONTEXT.md
@.planning/phases/62-surrogate-assisted-evaluation/62-RESEARCH.md
@.planning/phases/62-surrogate-assisted-evaluation/62-VALIDATION.md
@.planning/phases/62-surrogate-assisted-evaluation/62-01-SUMMARY.md
@.planning/phases/62-surrogate-assisted-evaluation/62-02-SUMMARY.md
@Cargo.toml
@src/fitness/surrogate.rs
@src/engines/ga.rs

<interfaces>
<!-- Cargo.toml example registration pattern (use an existing example as model). -->
<!-- Read Cargo.toml [[example]] entries to confirm the conventional layout. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build examples/surrogate_rastrigin.rs and register it in Cargo.toml</name>
  <files>examples/surrogate_rastrigin.rs, Cargo.toml</files>
  <read_first>
    - Cargo.toml (locate the `[[example]]` block layout — copy structure from an existing example registration; suggested model: the cma_es_rastrigin or pso_rastrigin example registration if present, else any example)
    - examples/ directory listing (find an existing rastrigin-style example to mirror: shared problem definition, GA configuration, output format) — `ls examples/ | grep -i rastrigin`
    - src/fitness/surrogate.rs (the trait signature to implement)
    - src/engines/ga.rs (the `with_surrogate` builder method signature)
  </read_first>
  <action>
    Create `examples/surrogate_rastrigin.rs`. The example must:
    (1) Define a 10-dimensional Rastrigin fitness function as the "expensive" true evaluator (use the Rastrigin formula `A*n + sum_i(x_i^2 - A*cos(2*pi*x_i))` with `A = 10.0`) wrapped to count invocations via `Arc<AtomicUsize>`.
    (2) Define a `LinearSurrogate { coeffs: Vec<f64> }` struct implementing `SurrogateModel<RangeChromosome<f64>>` — `predict` returns `dna.iter().zip(coeffs).map(|(g, c)| g.value() * c).sum()`. Use the existing chromosome import path (locate via `grep -n "use genetic_algorithms" examples/cma_es_rastrigin.rs` or another real-valued example).
    (3) Configure a `Ga` with `population_size = 100`, `generations = 30`, the wrapped fitness fn, and `.with_surrogate(Arc::new(LinearSurrogate { coeffs: vec![1.0; 10] }), 0.4)`.
    (4) After `.run()`, iterate `ga.stats()` (or whichever accessor exposes `Vec<GenerationStats>`) and print `generation`, `best_fitness`, `true_fitness_calls`. Assert at least one generation has `true_fitness_calls.unwrap() < offspring_size` (use `assert!` with a descriptive message; this turns the example into an executable smoke test for surrogate effectiveness).
    (5) Use `LogObserver` (or simply observer-less) to keep the example readable; do NOT introduce rayon, Instant::now, or any other WASM-incompatible code.
    Register the example in `Cargo.toml` under a new `[[example]]` block with `name = "surrogate_rastrigin"` and `path = "examples/surrogate_rastrigin.rs"` (only include `path` if existing examples include `path`; otherwise just `name = ...`). Follow the exact format of the nearest existing example entry.
    Do NOT edit `README.md` in this task.
  </action>
  <verify>
    <automated>cargo build --example surrogate_rastrigin &amp;&amp; cargo run --example surrogate_rastrigin --release 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/surrogate_rastrigin.rs` exists
    - Source assertion: `grep -c "with_surrogate" examples/surrogate_rastrigin.rs` returns ≥1
    - Source assertion: `grep -c "impl SurrogateModel" examples/surrogate_rastrigin.rs` returns 1
    - Source assertion: `grep -c "true_fitness_calls" examples/surrogate_rastrigin.rs` returns ≥1
    - Source assertion: `grep -c 'name = "surrogate_rastrigin"' Cargo.toml` returns 1
    - Command: `cargo build --example surrogate_rastrigin` exits 0
    - Command: `cargo run --example surrogate_rastrigin --release` exits 0 (the embedded `assert!` for surrogate-reduced calls must hold)
    - Command: `cargo clippy --example surrogate_rastrigin -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    The example runs, prints per-generation `true_fitness_calls`, and asserts surrogate-driven reduction. Cargo recognises the registration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase verification gate — full CI matrix + Phase 62 SUMMARY</name>
  <files>.planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md</files>
  <read_first>
    - .planning/phases/62-surrogate-assisted-evaluation/62-01-SUMMARY.md
    - .planning/phases/62-surrogate-assisted-evaluation/62-02-SUMMARY.md
    - .planning/phases/62-surrogate-assisted-evaluation/62-CONTEXT.md (for decision IDs D-01 through D-11 traceability)
    - $HOME/.claude/get-shit-done/templates/summary.md (template structure)
    - .planning/phases/61-*/61-*-SUMMARY.md or any recent phase SUMMARY to mirror format
  </read_first>
  <action>
    Run the full CI matrix and capture results. Commands to execute in order:
    (1) `cargo test` — entire library test suite must pass
    (2) `cargo test --features serde` — serde-gated tests must pass
    (3) `cargo clippy --all-targets -- -D warnings` — zero warnings
    (4) `cargo doc --no-deps` — zero rustdoc warnings
    (5) `cargo check --target wasm32-unknown-unknown` — WASM compile-check
    (6) `cargo run --example surrogate_rastrigin --release` — example smoke test
    If any step fails, STOP and fix the underlying issue before continuing (do NOT proceed to SUMMARY with red gates).
    Create `.planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` following the GSD summary template. Sections required:
    - Phase goal and outcome (one paragraph each)
    - Decisions implemented: enumerate D-01 through D-11 with the file/line that satisfies each (e.g. "D-01: trait in src/fitness/surrogate.rs", "D-10: src/stats.rs true_fitness_calls field", "D-08: src/engines/ga.rs prescreening block at line N")
    - Public API additions: `SurrogateModel<U>` trait, `Ga::with_surrogate`, `GenerationStats.true_fitness_calls`
    - Tests added: name each test file and the count per file
    - CI gate results: copy the pass/fail line for each of the six commands above
    - Out-of-scope confirmations: CmaEngine and IslandGa surrogate support remain deferred (D-07)
    - Files modified summary: list of all files touched across Plans 01, 02, 03
    - Hand-off notes for downstream phases (Phase 63 visualization may consume `true_fitness_calls` in plotting; Phase 64 may need doctests on the new trait)
  </action>
  <verify>
    <automated>cargo test &amp;&amp; cargo test --features serde &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo doc --no-deps &amp;&amp; cargo check --target wasm32-unknown-unknown &amp;&amp; cargo run --example surrogate_rastrigin --release</automated>
  </verify>
  <acceptance_criteria>
    - Command: `cargo test` exits 0
    - Command: `cargo test --features serde` exits 0
    - Command: `cargo clippy --all-targets -- -D warnings` exits 0
    - Command: `cargo doc --no-deps 2>&amp;1 | grep -c '^warning:'` returns 0
    - Command: `cargo check --target wasm32-unknown-unknown` exits 0
    - Command: `cargo run --example surrogate_rastrigin --release` exits 0
    - File `.planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` exists
    - Source assertion: `grep -cE "^D-[0-9]" .planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` returns ≥11 (one line per locked decision D-01 through D-11; pattern captures both D-0X and D-10/D-11)
    - Source assertion: `grep -c "SurrogateModel" .planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` returns ≥1
    - Source assertion: `grep -c "true_fitness_calls" .planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` returns ≥1
    - Source assertion: `grep -c "wasm32" .planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` returns ≥1
  </acceptance_criteria>
  <done>
    All six CI gate commands are green, the example runs, and the phase SUMMARY documents D-01 through D-11 implementation evidence.
  </done>
</task>

</tasks>

<verification>
- Full CI matrix green: cargo test, cargo test --features serde, cargo clippy --all-targets, cargo doc --no-deps, cargo check --target wasm32-unknown-unknown
- Example runs and embedded surrogate-reduction assertion holds
- Phase SUMMARY enumerates D-01..D-11 with file-level evidence
</verification>

<success_criteria>
- Phase 62 ships an end-to-end runnable demonstration of surrogate prescreening
- Every CI gate Pass/Fail is recorded in the SUMMARY
- Zero deferred or skipped CI gates
- Downstream phases (63, 64) receive a clean hand-off
</success_criteria>

<output>
The phase SUMMARY at `.planning/phases/62-surrogate-assisted-evaluation/62-SUMMARY.md` is the deliverable; no per-plan SUMMARY needed beyond it.
</output>
