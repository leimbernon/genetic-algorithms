---
phase: 62
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fitness/surrogate.rs
  - src/fitness.rs
  - src/lib.rs
  - src/stats.rs
  - tests/test_surrogate.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can declare a struct implementing SurrogateModel<U> with a single predict(&self, &U) -> f64 method"
    - "User can import the trait at crate root as genetic_algorithms::SurrogateModel"
    - "GenerationStats carries an Option<u64> field named true_fitness_calls, initialised to None in from_fitness_values"
    - "tests/test_surrogate.rs compiles and contains stubs for SC-1a, SC-1d, SC-1g, SC-2c"
  artifacts:
    - path: "src/fitness/surrogate.rs"
      provides: "SurrogateModel<U> trait definition (D-01, D-02)"
      contains: "pub trait SurrogateModel"
    - path: "src/stats.rs"
      provides: "true_fitness_calls field on GenerationStats (D-10)"
      contains: "true_fitness_calls"
    - path: "tests/test_surrogate.rs"
      provides: "Wave 0 test stubs covering trait-level invariants"
      contains: "#[test]"
  key_links:
    - from: "src/lib.rs"
      to: "src/fitness/surrogate.rs"
      via: "pub use fitness::SurrogateModel"
      pattern: "pub use fitness::SurrogateModel"
    - from: "src/fitness.rs"
      to: "src/fitness/surrogate.rs"
      via: "pub mod surrogate"
      pattern: "pub mod surrogate"
---

<objective>
Establish the Phase 62 API surface and Wave 0 test scaffold before any engine wiring:
the `SurrogateModel<U>` trait, the `GenerationStats.true_fitness_calls` field, the
crate-root re-export, and the Nyquist-compliant test stubs that Plan 02 will activate.

Purpose: Lock the public contract (D-01, D-02, D-10) and the test set so Plan 02
implements against fixed surfaces. Mirrors the Phase 60 `BatchFitnessEvaluator`
pattern exactly — same module layout, same field-level serde gating, same re-export.

Output: One new trait module, one new `pub use`, one new stats field initialised
in two constructor sites, one new integration-test file with `#[ignore]`-gated stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-surrogate-assisted-evaluation/62-CONTEXT.md
@.planning/phases/62-surrogate-assisted-evaluation/62-RESEARCH.md
@.planning/phases/62-surrogate-assisted-evaluation/62-VALIDATION.md
@src/fitness/batch.rs
@src/fitness.rs
@src/stats.rs

<interfaces>
<!-- Extracted from src/fitness/batch.rs — this is the direct model to copy. -->

```rust
// src/fitness/batch.rs
use crate::traits::ChromosomeT;

pub trait BatchFitnessEvaluator<U: ChromosomeT>: Send + Sync {
    fn evaluate_batch(&self, chromosomes: &[U]) -> Vec<f64>;
}
```

<!-- Extracted from src/fitness.rs (the fitness facade — there is NO src/fitness/mod.rs). -->

```rust
// src/fitness.rs  (lines 20-28 today)
pub mod batch;
pub mod cache;
pub mod count_true;
pub mod fitness_fn_wrapper;

pub use batch::BatchFitnessEvaluator;
pub use cache::FitnessCache;
pub use count_true::count_true;
pub use fitness_fn_wrapper::FitnessFnWrapper;
```

<!-- Extracted from src/stats.rs (cache_hits / cache_misses pattern at lines 54-65, 89-90). -->

```rust
// In GenerationStats struct:
#[cfg_attr(feature = "serde", serde(default))]
pub cache_hits: Option<u64>,
#[cfg_attr(feature = "serde", serde(default))]
pub cache_misses: Option<u64>,

// In from_fitness_values (n == 0 branch AND main constructor):
cache_hits: None,
cache_misses: None,
```

<!-- Extracted from src/lib.rs line 342. -->

```rust
pub use fitness::BatchFitnessEvaluator;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SurrogateModel trait and wire module</name>
  <files>src/fitness/surrogate.rs, src/fitness.rs, src/lib.rs</files>
  <read_first>
    - src/fitness/batch.rs (model file — copy structure verbatim)
    - src/fitness.rs (add `pub mod surrogate;` and `pub use surrogate::SurrogateModel;`)
    - src/lib.rs lines 270-345 (locate the `pub use fitness::BatchFitnessEvaluator;` line at 342 and append the new re-export immediately after)
  </read_first>
  <behavior>
    - Test 1: `SurrogateModel` is implementable on a user struct with one `predict(&self, &U) -> f64` method.
    - Test 2: `genetic_algorithms::SurrogateModel` resolves at crate root (re-export works).
    - Test 3: Trait bounds enforce `Send + Sync` (storable in `Arc<dyn SurrogateModel<U> + Send + Sync>`).
  </behavior>
  <action>
    Create `src/fitness/surrogate.rs` containing one public trait `SurrogateModel<U: ChromosomeT>: Send + Sync` with a single required method `fn predict(&self, chromosome: &U) -> f64;` (D-01).
    Use `use crate::traits::ChromosomeT;` exactly as `batch.rs` does. Include a module-level `//!` docblock describing the prescreening contract (D-04: rejected offspring are dropped; D-08: surrogate runs before FitnessCache + BatchFitnessEvaluator) and a rustdoc `# Example` block showing a minimal user impl on `RangeChromosome<f64>` (mark the codeblock `rust,ignore` to avoid doctest dependency on a non-existent imported type).
    In `src/fitness.rs` add `pub mod surrogate;` alphabetically (after `pub mod fitness_fn_wrapper;`) and `pub use surrogate::SurrogateModel;` after the existing `pub use fitness_fn_wrapper::FitnessFnWrapper;` line.
    In `src/lib.rs` add `pub use fitness::SurrogateModel;` immediately after the existing `pub use fitness::BatchFitnessEvaluator;` line (line 342).
    Do NOT add the trait to `validators/`, do NOT touch `Ga<U>` — that is Plan 02 scope.
  </action>
  <verify>
    <automated>cargo build --lib 2>&amp;1 | grep -E "(error|warning)" | grep -v "unused" || echo OK; cargo doc --no-deps --lib 2>&amp;1 | grep -c "warning: " | grep -q "^0$" || echo "rustdoc warnings present"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/fitness/surrogate.rs` exists and contains `pub trait SurrogateModel<U: ChromosomeT>: Send + Sync`
    - Source assertion: `grep -c "fn predict(&self, chromosome: &U) -> f64" src/fitness/surrogate.rs` returns ≥1
    - Source assertion: `grep -c "^pub mod surrogate;" src/fitness.rs` returns 1
    - Source assertion: `grep -c "^pub use surrogate::SurrogateModel;" src/fitness.rs` returns 1
    - Source assertion: `grep -c "^pub use fitness::SurrogateModel;" src/lib.rs` returns 1
    - Command: `cargo build --lib` exits 0
    - Command: `cargo doc --no-deps --lib 2>&amp;1 | grep -c '^warning: '` returns 0
  </acceptance_criteria>
  <done>
    SurrogateModel trait is defined, re-exported at crate root, builds clean, generates clean rustdoc, and is ready for engine integration in Plan 02.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add true_fitness_calls field to GenerationStats</name>
  <files>src/stats.rs</files>
  <read_first>
    - src/stats.rs (lines 26-66 for struct definition; lines 72-92 for `from_fitness_values` empty branch — both must be updated to initialise the new field)
  </read_first>
  <behavior>
    - Test 1: A default `GenerationStats::from_fitness_values(0, &[], false)` has `true_fitness_calls == None`.
    - Test 2: An existing checkpoint without the field deserialises with serde default (`None`).
    - Test 3: Field can be assigned `Some(42)` and serialised/deserialised round-trip under `--features serde`.
  </behavior>
  <action>
    In `src/stats.rs`, add a new field `pub true_fitness_calls: Option&lt;u64&gt;` to `GenerationStats` immediately after `cache_misses` (preserving the established Option&lt;u64&gt; cluster at lines 54-65). Apply `#[cfg_attr(feature = "serde", serde(default))]` exactly as `cache_hits`/`cache_misses` do (D-10, Pitfall 3 in RESEARCH.md). Add a rustdoc `///` block stating: `None` when no surrogate configured; `Some(n)` is the post-prescreening offspring count that reached true fitness evaluation in this generation.
    In `from_fitness_values` initialise `true_fitness_calls: None` in BOTH places: the early-return empty branch (currently lines 79-91) and the main constructor block where `cache_hits: None, cache_misses: None` already appear. Do NOT add a setter — direct field assignment from `ga.rs` is the pattern (consistent with `cache_hits` write at line 2083). Do NOT add `true_fitness_calls` to any other constructor or to a `Default` impl elsewhere — `GenerationStats` has no `Default` impl, only `from_fitness_values`.
  </action>
  <verify>
    <automated>cargo build --lib &amp;&amp; cargo build --features serde --lib</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "pub true_fitness_calls: Option&lt;u64&gt;" src/stats.rs` returns 1
    - Source assertion: `grep -c "true_fitness_calls: None" src/stats.rs` returns ≥2 (both empty-branch and main constructor)
    - Source assertion: the `#[cfg_attr(feature = \"serde\", serde(default))]` attribute appears on the line directly preceding `pub true_fitness_calls`
    - Command: `cargo build --lib` exits 0
    - Command: `cargo build --lib --features serde` exits 0
    - Command: `cargo clippy --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `GenerationStats.true_fitness_calls: Option<u64>` is declared, serde-gated with default, initialised to None in both constructor paths, and ready for Plan 02 to assign Some(n) at the gen_stats site in ga.rs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_surrogate.rs Wave 0 test stubs</name>
  <files>tests/test_surrogate.rs</files>
  <read_first>
    - .planning/phases/62-surrogate-assisted-evaluation/62-VALIDATION.md (Per-Task Verification Map and Wave 0 Requirements)
    - .planning/phases/62-surrogate-assisted-evaluation/62-RESEARCH.md (lines 455-480: Phase Requirements → Test Map)
    - tests/test_stats.rs (existing test patterns for stats fields, if any — otherwise base on tests/test_chromosomet_core.rs structure)
    - src/fitness/surrogate.rs (created in Task 1; ensures the trait import works)
  </read_first>
  <behavior>
    - Stub 1 (SC-1a) `test_predict_called`: a minimal `SurrogateModel` impl on a stub chromosome is callable; counts predict invocations via interior mutability.
    - Stub 2 (SC-1d) `test_prescreening_floor`: assert `max(1, floor(n * f)) == 1` when `n=10, f=0.0001` and `== 5` when `n=10, f=0.5` (pure-math helper, no engine).
    - Stub 3 (SC-1g) `test_nan_prediction_treated_as_worst`: build a small offspring vector, score with a surrogate that returns NaN for one entry, and assert the NaN entry sorts last after the documented `f64::NEG_INFINITY` substitution.
    - Stub 4 (SC-2c, gated `#[cfg(feature = "serde")]`) `stats_serde_default`: deserialise a JSON `GenerationStats` payload missing `true_fitness_calls` and assert the field is `None`.
    - All four stubs are `#[ignore = "Wave 1 — activated by Plan 02"]` EXCEPT `test_prescreening_floor` and `stats_serde_default`, which require no engine wiring and MUST run green now.
  </behavior>
  <action>
    Create `tests/test_surrogate.rs` (NOT under a subdirectory — Cargo discovers tests at `tests/*.rs` per existing project layout; verified by `ls tests/`). Add `use genetic_algorithms::SurrogateModel;` at the top.
    Implement the four stubs above. For `test_prescreening_floor`, the assertion calls a local `fn floor_keep(n: usize, f: f64) -> usize { ((n as f64 * f).floor() as usize).max(1) }` defined inline in the test module — this is the same formula Plan 02 will inline into `ga.rs`, so the stub locks the contract early.
    For `test_nan_prediction_treated_as_worst`, define a local `struct NanSurrogate` that returns `f64::NAN` for chromosomes with fitness > 0 and a finite score otherwise; use the documented substitution `if s.is_nan() { f64::NEG_INFINITY } else { s }` directly in the test to verify sort order; use `Binary` chromosome from `genetic_algorithms::chromosomes::Binary` if it exists, otherwise fall back to `RangeChromosome<f64>` (read tests/test_chromosomet_core.rs to confirm the canonical chromosome import path).
    For `stats_serde_default`, gate the entire test under `#[cfg(feature = "serde")]` and use `serde_json::from_str` on a literal JSON string omitting the new field, asserting `parsed.true_fitness_calls.is_none()`. Use the JSON shape of an existing minimal `GenerationStats` payload (read `tests/test_stats.rs` if present; otherwise hand-construct from the struct).
    Mark the engine-dependent stub `test_predict_called` with `#[ignore = "Plan 02: requires Ga::with_surrogate wiring"]` only if it cannot be made trait-only; the trait method alone IS callable without the engine, so prefer to keep it un-ignored and verify the call count via `AtomicUsize`.
  </action>
  <verify>
    <automated>cargo test --test test_surrogate -- --include-ignored 2>&amp;1 | tail -20; cargo test --test test_surrogate --features serde 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_surrogate.rs` exists
    - Source assertion: `grep -c "fn test_prescreening_floor" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn test_nan_prediction_treated_as_worst" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn test_predict_called" tests/test_surrogate.rs` returns 1
    - Source assertion: `grep -c "fn stats_serde_default" tests/test_surrogate.rs` returns 1
    - Command: `cargo test --test test_surrogate test_prescreening_floor` exits 0 with 1 passed
    - Command: `cargo test --test test_surrogate test_nan_prediction_treated_as_worst` exits 0 with 1 passed
    - Command: `cargo test --test test_surrogate --features serde stats_serde_default` exits 0 with 1 passed
    - Command: `cargo test --test test_surrogate test_predict_called` exits 0 with 1 passed (trait-only test; no engine wiring needed)
  </acceptance_criteria>
  <done>
    All four Wave 0 stubs exist and the trait-only / pure-math / serde-only stubs already pass; no stub is left in a state where Plan 02 must reach into the test file to flip an `#[ignore]` flag.
  </done>
</task>

</tasks>

<verification>
- `cargo build --lib` and `cargo build --lib --features serde` both succeed
- `cargo clippy --lib -- -D warnings` passes
- `cargo doc --no-deps --lib` emits zero warnings
- `cargo test --test test_surrogate` runs at least four named tests, all passing
- `cargo check --target wasm32-unknown-unknown` still passes (no behavior change, but verify the trait file has no rayon / Instant references)
</verification>

<success_criteria>
- `genetic_algorithms::SurrogateModel` is importable at crate root
- `GenerationStats.true_fitness_calls: Option<u64>` exists with `serde(default)`
- `tests/test_surrogate.rs` contains the four Wave 0 stubs and all four pass
- Zero engine code in `src/engines/ga.rs` modified — Plan 02 owns that work exclusively
</success_criteria>

<output>
Create `.planning/phases/62-surrogate-assisted-evaluation/62-01-SUMMARY.md` when done.
</output>
