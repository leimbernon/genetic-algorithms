---
phase: 60-batch-fitness-fitness-cache-extension
plan: 03
type: execute
wave: 3
depends_on: [60-02]
files_modified:
  - src/engines/cma/engine.rs
  - src/engines/cma/configuration.rs
  - tests/engines/cma/test_cma.rs
autonomous: true
requirements: []
tags: [rust, cma-es, fitness-cache, batch-evaluator, phase-gate]

must_haves:
  truths:
    - "User can configure `.with_batch_evaluator(...)` on `CmaEngine`"
    - "User can configure `.with_fitness_cache(size)` on `CmaEngine`"
    - "CMA initial population AND per-generation offspring are batch-evaluated when batch evaluator is configured"
    - "CMA `GenerationStats.cache_hits` / `cache_misses` carry delta values when cache is configured"
    - "All Phase 60 CI gates pass: cargo test, cargo test --features serde, cargo clippy -D warnings, cargo doc, cargo check wasm32"
  artifacts:
    - path: "src/engines/cma/engine.rs"
      provides: "batch_evaluator + fitness_cache fields, batch path at sites 1 and 2, cache delta wiring"
      contains: "batch_evaluator"
    - path: "src/engines/cma/configuration.rs"
      provides: "with_fitness_cache builder + fitness_cache_size field"
      contains: "with_fitness_cache"
    - path: "tests/engines/cma/test_cma.rs"
      provides: "Active batch + cache tests under batch_and_cache_tests module"
      contains: "batch_and_cache_tests"
  key_links:
    - from: "src/engines/cma/engine.rs (run() initial pop site ~line 563)"
      to: "BatchFitnessEvaluator::evaluate_batch"
      via: "evaluate_batch(&pop)"
      pattern: "evaluate_batch"
    - from: "src/engines/cma/engine.rs (offspring loop ~lines 609-633)"
      to: "BatchFitnessEvaluator::evaluate_batch"
      via: "collect offspring → evaluate_batch(&offspring)"
      pattern: "evaluate_batch"
---

<objective>
Apply the Phase 60 contracts to `CmaEngine`: add `with_batch_evaluator` and `with_fitness_cache(size)` builders, replace both inline fitness-eval sites with a batch+cache helper mirroring `Ga::batch_evaluate_pop`, populate per-generation `cache_hits`/`cache_misses`, and run the Phase 60 verification gate (full CI matrix).

Purpose: Phase 60 SC #1 and SC #2 (CMA side) and SC #4 (all CI gates green) delivered here.
Output: CMA engine reaches feature parity with Ga for batch + cache; Phase 60 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-CONTEXT.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-02-SUMMARY.md
@src/engines/cma/engine.rs
@src/engines/cma/configuration.rs
@src/engines/ga.rs
@src/fitness/cache.rs
@src/fitness/batch.rs

<interfaces>
From Wave 2 (60-02) — the `Ga` implementation is the reference:
- Helper signature on Ga: `fn batch_evaluate_pop(&self, pop: &mut [U]) -> Result<(), GaError>` — CMA mirrors this shape
- Mutual-exclusivity error string: "Cannot use both fitness_fn and with_batch_evaluator() — they are mutually exclusive"
- Cache delta pattern: snapshot `(prev_hits, prev_misses)` at generation iteration start; `saturating_sub` at gen_stats write site

CMA-specific facts (from 60-PATTERNS.md and 60-RESEARCH.md):
- `CmaConfiguration` is NOT generic over `U` — `batch_evaluator` field MUST live on `CmaEngine` itself; only `fitness_cache_size: Option<usize>` lives on `CmaConfiguration`
- Initial population eval site: `src/engines/cma/engine.rs` lines ~562-566 (`for ind in &mut pop { ... (self.fitness_fn)(ind.dna()) ... }`)
- Per-generation offspring eval site: lines ~609-633 (`(self.fitness_fn)(child.dna())` called inside offspring build loop)
- CMA has no separate `build()` — wiring of `wrap_with_cache` happens at the top of `run()`
- CMA's `fitness_fn` is non-Optional (`Arc<FitnessFn<U::Gene>>`); when `batch_evaluator` is configured, mutual exclusivity is enforced at `run()` start or constructor

Test harness wiring (from `tests/test_engines.rs`):
- All engine tests run via `cargo test --test test_engines`
- CMA tests live in `tests/engines/cma/test_cma.rs` and are reachable as `cargo test --test test_engines engines::cma::test_cma::batch_and_cache_tests`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CmaConfiguration fitness_cache_size + builder; CmaEngine fields, builder, helper, both eval sites, delta stats; activate all CMA tests</name>
  <files>src/engines/cma/engine.rs, src/engines/cma/configuration.rs, tests/engines/cma/test_cma.rs</files>
  <read_first>
    - src/engines/cma/engine.rs (full file — at minimum the struct declaration, run() top, lines 555-640 around both fitness eval sites)
    - src/engines/cma/configuration.rs (full file — builder block lines 116-197)
    - src/engines/ga.rs (`batch_evaluate_pop` from plan 60-02 — replicate algorithm structurally)
    - src/fitness/batch.rs
    - src/fitness/cache.rs (wrap_with_cache signature; FitnessCache::new; hash_dna)
    - tests/engines/cma/test_cma.rs (Wave 0 stubs from plan 60-01 — find `mod batch_and_cache_tests`)
    - tests/test_engines.rs (test harness wiring — DO NOT modify; just verify path)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md (CmaEngine section)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md (Pitfall 5 — both sites must be patched)
  </read_first>
  <behavior>
    - `CmaConfiguration::with_fitness_cache(size: usize)` stores `Some(size)` in a new `fitness_cache_size: Option<usize>` field
    - `CmaEngine::with_batch_evaluator(Arc<dyn BatchFitnessEvaluator<U> + Send + Sync>) -> Self` stores the evaluator
    - At top of `CmaEngine::run()`, if `batch_evaluator` AND `fitness_fn` are both effectively configured, return `Err(GaError::ConfigurationError(...))`
    - At top of `run()`, cache handle is created via `wrap_with_cache` when `batch_evaluator.is_none()`, or directly via `FitnessCache::new` when `batch_evaluator.is_some()`
    - Initial population eval site uses helper when batch mode active
    - Per-generation offspring loop builds without fitness, then helper assigns fitness in one call
    - `gen_stats.cache_hits` / `cache_misses` populated via delta snapshot when cache is active
    - All 5 ignored CMA test stubs from 60-01 are activated and pass
  </behavior>
  <action>
    Add field `pub(crate) fitness_cache_size: Option<usize>` to `CmaConfiguration` (default `None` in the existing `Default` impl). Add builder method `pub fn with_fitness_cache(mut self, size: usize) -> Self { self.fitness_cache_size = Some(size); self }` next to `with_sigma0` in `src/engines/cma/configuration.rs`.

    Add two fields to `CmaEngine<U>` struct in `src/engines/cma/engine.rs`:
      `batch_evaluator: Option<Arc<dyn BatchFitnessEvaluator<U> + Send + Sync>>`
      `fitness_cache: Option<Arc<Mutex<crate::fitness::cache::FitnessCache>>>`
    Initialize both to `None` in every `CmaEngine` constructor / new fn.

    Add builder method on `CmaEngine`: `pub fn with_batch_evaluator(mut self, evaluator: Arc<dyn BatchFitnessEvaluator<U> + Send + Sync>) -> Self { self.batch_evaluator = Some(evaluator); self }`.

    Mutual-exclusivity check: since CMA currently requires `fitness_fn` at construction (non-Optional), document the semantics in the SUMMARY: when `with_batch_evaluator` is called, the prior `fitness_fn` becomes ignored (overwritten in spirit). Enforce via a `run()`-top check: if `self.batch_evaluator.is_some()`, set a runtime flag `batch_mode = true` and skip all `(self.fitness_fn)(...)` calls. There is no double-config error to surface (because batch evaluator opt-in implicitly disables the scalar path). Document this in the plan's SUMMARY as the discretion choice for D-03 mutual-exclusivity mechanism on CMA. (Ga's mutual-exclusivity check is stricter because Ga's `fitness_fn` is `Option`.)

    At the top of `CmaEngine::run()` add cache-handle bootstrap:
      If `self.config.fitness_cache_size.is_some()` AND `self.fitness_cache.is_none()`:
        When `self.batch_evaluator.is_none()`: call `wrap_with_cache(Arc::clone(&self.fitness_fn), size)` and assign the returned tuple to `self.fitness_fn` and `self.fitness_cache` respectively. Mirrors `Ga::build()`.
        When `self.batch_evaluator.is_some()`: `self.fitness_cache = Some(Arc::new(Mutex::new(FitnessCache::new(size))));`

    Implement private method `fn batch_evaluate_pop(&self, pop: &mut [U]) -> Result<(), GaError>` on `CmaEngine<U>` with the SAME algorithm as `Ga::batch_evaluate_pop` from plan 60-02 (Case A no-op / Case B batch-only / Case C batch+cache partition with lock-release-before-batch). Replicate structurally; do not extract to a shared utility in this phase. Use `debug_assert_eq!` on returned vector length (T-60-01).

    Replace fitness eval site 1 (initial population, lines ~562-566). Before the existing inline loop, branch:
      If `self.batch_evaluator.is_some()`: call `self.batch_evaluate_pop(&mut pop)?;` and skip the inline loop.
      Else: keep the existing inline loop unchanged.

    Replace fitness eval site 2 (offspring loop, lines ~609-633). Restructure the loop so when `self.batch_evaluator.is_some()`, the per-child `set_fitness(f)` call is skipped (children are pushed to `offspring` with default 0.0 fitness). After the offspring build loop completes:
      If `self.batch_evaluator.is_some()`: call `self.batch_evaluate_pop(&mut offspring)?;`
      Else: do nothing (the existing inline loop already set fitness).
    Both branches must happen BEFORE any sort or selection that orders by fitness.

    Cache delta stats — wrap CMA's `gen_stats` construction with the same snapshot/saturating_sub pattern from 60-02. At the start of each generation iteration:
      `let (prev_cache_hits, prev_cache_misses) = match &self.fitness_cache { Some(ch) => { let c = ch.lock().expect("fitness cache lock poisoned"); (c.hits(), c.misses()) } None => (0, 0) };`
    Before pushing `gen_stats` into CMA's stats vector:
      `if let Some(ref ch) = self.fitness_cache { let c = ch.lock().expect("fitness cache lock poisoned"); gen_stats.cache_hits = Some(c.hits().saturating_sub(prev_cache_hits)); gen_stats.cache_misses = Some(c.misses().saturating_sub(prev_cache_misses)); }`

    Activate all 5 tests in `tests/engines/cma/test_cma.rs::batch_and_cache_tests` (remove `#[ignore]` and provide bodies):
      `cma_with_fitness_cache_accepted` — build `CmaEngine` with `.with_fitness_cache(64)`, run 3 generations on a small Rastrigin-style problem, assert `result.generations >= 3` and run succeeds.
      `cma_with_batch_evaluator_accepted` — build with `.with_batch_evaluator(Arc::new(CountingEvaluator::default()))`, run 3 generations, assert success.
      `cma_batch_evaluator_initial_population` — using `CountingEvaluator` returning `vec![7.0; n]`, after running 1 generation assert the initial population's chromosomes all have `fitness() == 7.0` (proves Pitfall 5 site 1 was patched).
      `cma_batch_evaluator_offspring_loop` — with `CountingEvaluator`, run 3 generations, assert call count >= 4 (1 init + 3 offspring batches) and every offspring chromosome's fitness is non-default.
      `cma_cache_stats_in_generation_stats` — build with a scalar `fitness_fn` + `with_fitness_cache(64)`, run 3 generations, assert the last `gen_stats.cache_hits.is_some()`, `cache_misses.is_some()`, and their sum > 0.

    Define `CountingEvaluator` locally inside the test module (or share with the Ga test module by `pub use` if convenient). Implement `BatchFitnessEvaluator<U>` for the concrete CMA chromosome type used in the test.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms &amp;&amp; cargo build &amp;&amp; cargo test --test test_engines engines::cma::test_cma::batch_and_cache_tests &amp;&amp; cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "with_fitness_cache" src/engines/cma/configuration.rs` matches the builder declaration
    - `grep -n "fitness_cache_size: Option<usize>" src/engines/cma/configuration.rs` matches the field declaration
    - `grep -n "batch_evaluator: Option<Arc<dyn BatchFitnessEvaluator" src/engines/cma/engine.rs` matches the field declaration
    - `grep -n "fitness_cache: Option<Arc<Mutex<" src/engines/cma/engine.rs` matches the field declaration
    - `grep -n "fn with_batch_evaluator" src/engines/cma/engine.rs` matches the builder declaration
    - `grep -n "fn batch_evaluate_pop" src/engines/cma/engine.rs` matches the helper declaration
    - `grep -c "evaluate_batch" src/engines/cma/engine.rs` is at least 2 (helper Case B and Case C call sites)
    - `grep -n "saturating_sub" src/engines/cma/engine.rs` matches cache delta computation
    - `grep -n "gen_stats.cache_hits = Some" src/engines/cma/engine.rs` matches one line
    - All 5 tests in `tests/engines/cma/test_cma.rs::batch_and_cache_tests` are active (no `#[ignore]`) and pass
    - `cargo test --test test_engines engines::cma::test_cma::batch_and_cache_tests` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `CmaEngine` mirrors `Ga` for batch + cache. Both fitness eval sites in CMA are batch-aware. Cache delta stats flow into `GenerationStats`. All 5 activated CMA tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 60 verification gate — full CI matrix + SUMMARY</name>
  <files>.planning/phases/60-batch-fitness-fitness-cache-extension/60-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-CONTEXT.md
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-01-SUMMARY.md
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-02-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <behavior>
    - Full CI matrix passes locally with zero warnings
    - WASM target compiles
    - serde feature passes including old-checkpoint backwards-compat test
    - Phase 60 SUMMARY committed documenting: D-01..D-08 status, discretion choices (BatchFitnessEvaluator module placement, CMA mutual-exclusivity mechanism), threat-model dispositions, deferred items (PSO/EDA/ALPS/etc. cache+batch), assumptions verified (A1, A2, A3)
  </behavior>
  <action>
    Run the full phase verification gate, in order. If any command fails, fix the underlying issue (do not skip):
      cargo build
      cargo build --features serde
      cargo test
      cargo test --features serde
      cargo clippy --all-targets -- -D warnings
      cargo doc --no-deps
      cargo check --target wasm32-unknown-unknown

    Confirm that ALL Phase 60 tests are active (no remaining `#[ignore = "Wave 0 stub"]` from plans 60-01..60-03). Run `cargo test -- --ignored --list` and verify the output contains zero entries tagged with the Phase 60 Wave 0 stub message.

    Write `.planning/phases/60-batch-fitness-fitness-cache-extension/60-03-SUMMARY.md` covering:
      - Decisions implemented: D-01 (trait signature), D-02 (full replacement), D-03 (Ga = ConfigurationError; CMA = batch overrides scalar — document this discretion choice), D-04 (CMA offspring collect-then-batch), D-05 (CMA with_fitness_cache), D-06 (batch+cache partition algorithm), D-07 (GenerationStats fields), D-08 (wrap_with_cache tuple return)
      - Discretion choices made: BatchFitnessEvaluator placed in `src/fitness/batch.rs` and re-exported via `src/lib.rs`; CMA replicates `batch_evaluate_pop` structurally (no shared utility extraction) to keep Phase 60 footprint bounded
      - Assumption verification: A1 (fitness_fn=None to parent_crossover suppresses per-child eval — verified via `ga_batch_evaluator_replaces_calculate_fitness` test), A2 (CmaEngine fields accessible), A3 (miss-chromosome clone is the correct cost given D-01 locked signature)
      - Threat dispositions delivered: T-60-01 mitigated by debug_assert, T-60-02 accepted, T-60-04 mitigated (Ga ConfigurationError; CMA last-writer-wins), T-60-05 mitigated (lock release before batch call)
      - Deferred items reaffirmed: batch+cache for PSO/EDA/ALPS/ScatterSearch/CellularGA/DE remain out of scope; async evaluator deferred; per-observer cache hooks deferred
      - Test counts: number of Phase 60 active tests across `tests/engines/test_ga.rs::batch_evaluator_tests` and `tests/engines/cma/test_cma.rs::batch_and_cache_tests`, plus the foundation tests in `tests/test_stats.rs` and `tests/fitness/test_cache.rs`
      - CI gate output summary: all 7 commands passed with zero warnings
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms &amp;&amp; cargo build &amp;&amp; cargo build --features serde &amp;&amp; cargo test &amp;&amp; cargo test --features serde &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo doc --no-deps &amp;&amp; cargo check --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - All 7 verification commands exit 0
    - `cargo doc --no-deps 2>&1 | grep -i "warning"` is empty
    - `cargo clippy --all-targets -- -D warnings 2>&1 | grep -i "warning"` is empty
    - `cargo test -- --ignored --list 2>&1 | grep "Wave 0 stub" | wc -l` returns 0 (no remaining Phase 60 ignored stubs)
    - `.planning/phases/60-batch-fitness-fitness-cache-extension/60-03-SUMMARY.md` exists and contains sections covering all 8 decisions, discretion choices, assumption verification, threat dispositions, deferred items, test counts, and CI gate results
  </acceptance_criteria>
  <done>
    Phase 60 closes with green CI across all gates, zero remaining Wave 0 stubs, and a SUMMARY documenting decisions, discretion choices, threat dispositions, and the deferred scope for future engine integration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User trait impl | `Arc<dyn BatchFitnessEvaluator<U>>` runs in-process; same trust posture as `Arc<dyn GaObserver<U>>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `evaluate_batch` returns wrong-length `Vec<f64>` for CMA | mitigate | `debug_assert_eq!` in CMA's `batch_evaluate_pop` (mirrors Ga) |
| T-60-02 | Denial of Service | Cache mutex poisoning | accept | `.expect("fitness cache lock poisoned")` matches existing pattern |
| T-60-04 | Tampering | CMA caller sets fitness_fn after with_batch_evaluator (or vice versa) | mitigate | When `batch_evaluator.is_some()`, scalar path is skipped at `run()`; last writer effectively wins. Document in SUMMARY as discretion choice for D-03 on CMA. |
| T-60-05 | Denial of Service | Cache lock held across expensive `evaluate_batch` | mitigate | Helper drops lock before batch call; re-acquires for puts |
| T-60-SC | Tampering | npm/pip/cargo installs | n/a | No new external dependencies |
</threat_model>

<verification>
- `cargo build` + `cargo build --features serde` pass
- `cargo test` + `cargo test --features serde` pass
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo doc --no-deps` passes (zero warnings)
- `cargo check --target wasm32-unknown-unknown` passes
- All Phase 60 ignored Wave 0 stubs have been activated (`cargo test -- --ignored --list` shows zero Phase 60 Wave 0 entries)
</verification>

<success_criteria>
- Phase 60 SC #1 fully satisfied (Ga + CMA): batch evaluator replaces `calculate_fitness` end-to-end
- Phase 60 SC #2 fully satisfied (Ga + CMA): cache delta stats appear in `GenerationStats` when cache active
- Phase 60 SC #3 fully satisfied: WASM compiles
- Phase 60 SC #4 fully satisfied: all CI gates green with zero warnings
- Phase 60 SUMMARY committed
</success_criteria>

<output>
Create `.planning/phases/60-batch-fitness-fitness-cache-extension/60-03-SUMMARY.md` when done (Task 2 produces it).
</output>
