---
phase: 60-batch-fitness-fitness-cache-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fitness/cache.rs
  - src/fitness/batch.rs
  - src/fitness/mod.rs
  - src/stats.rs
  - src/lib.rs
  - tests/test_stats.rs
  - tests/fitness/test_cache.rs
  - tests/engines/test_ga.rs
  - tests/engines/cma/test_cma.rs
autonomous: true
requirements: []
tags: [rust, ga, fitness-cache, batch-evaluator, traits]

must_haves:
  truths:
    - "Users can import `BatchFitnessEvaluator` from the crate root"
    - "`wrap_with_cache` returns both the wrapped fn and the shared cache handle"
    - "`GenerationStats` carries optional `cache_hits` / `cache_misses` fields"
    - "Old serde checkpoints (without the new fields) still deserialize"
    - "Wave 0 test stubs exist (ignored) for every Phase 60 SC item"
  artifacts:
    - path: "src/fitness/batch.rs"
      provides: "BatchFitnessEvaluator<U> trait"
      contains: "pub trait BatchFitnessEvaluator"
    - path: "src/fitness/cache.rs"
      provides: "Refactored wrap_with_cache returning tuple"
      contains: "Arc<Mutex<FitnessCache>>"
    - path: "src/stats.rs"
      provides: "cache_hits / cache_misses fields on GenerationStats"
      contains: "pub cache_hits: Option<u64>"
    - path: "src/lib.rs"
      provides: "BatchFitnessEvaluator re-export"
      contains: "pub use fitness::BatchFitnessEvaluator"
  key_links:
    - from: "src/lib.rs"
      to: "src/fitness/batch.rs"
      via: "pub use fitness::BatchFitnessEvaluator"
      pattern: "pub use fitness::BatchFitnessEvaluator"
    - from: "src/fitness/mod.rs"
      to: "src/fitness/batch.rs"
      via: "pub mod + pub use"
      pattern: "pub mod batch"
---

<objective>
Establish the Phase 60 foundation: introduce the public `BatchFitnessEvaluator<U>` trait (D-01), refactor `wrap_with_cache` to expose the shared cache handle (D-08), extend `GenerationStats` with delta `cache_hits` / `cache_misses` fields (D-07), and scaffold all Wave 0 test stubs (Nyquist gate) so downstream waves have failing tests to turn green.

Purpose: Downstream `Ga` and `CmaEngine` integration depends on these contracts; landing them in a single foundation wave keeps the API surface stable before wiring.
Output: New trait module, refactored cache helper, extended stats struct, ignored test stubs covering every Phase 60 success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-CONTEXT.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md
@.planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md
@src/fitness/cache.rs
@src/fitness/mod.rs
@src/stats.rs
@src/lib.rs
@src/traits/chromosome.rs
@src/observe/observer/mod.rs

<interfaces>
<!-- Contracts that downstream waves rely on -->

Current `wrap_with_cache` (src/fitness/cache.rs lines 119-147):
- Signature: `pub fn wrap_with_cache<G>(fitness_fn: Arc<FitnessFn<G>>, cache_size: usize) -> Arc<FitnessFn<G>>`
- After this plan: returns `(Arc<FitnessFn<G>>, Arc<Mutex<FitnessCache>>)`

`FitnessCache` accessors (src/fitness/cache.rs lines 77-84):
- `pub fn hits(&self) -> u64`
- `pub fn misses(&self) -> u64`

`GaObserver` declaration shape (src/observe/observer/mod.rs line 66) — model for `BatchFitnessEvaluator`:
- `pub trait GaObserver<U: ChromosomeT>: Send + Sync { ... }`

`GenerationStats` serde pattern to copy (src/stats.rs lines 41-53):
- `#[cfg_attr(feature = "serde", serde(default))]` on every new optional field

`ChromosomeT` import path (src/traits/chromosome.rs): `use crate::traits::ChromosomeT;`

`FitnessFn<G>` type alias (src/traits/common.rs): `pub type FitnessFn<G> = dyn Fn(&[G]) -> f64 + Send + Sync;`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BatchFitnessEvaluator trait, refactor wrap_with_cache, extend GenerationStats</name>
  <files>src/fitness/batch.rs, src/fitness/cache.rs, src/fitness/mod.rs, src/stats.rs, src/lib.rs</files>
  <read_first>
    - src/fitness/cache.rs (full file — signature + closure body must be preserved with new return shape)
    - src/fitness/mod.rs (module declaration block — add `pub mod batch;` and re-export)
    - src/stats.rs (full file — confirm both construction sites at lines 66-78 and 107-118 receive new fields)
    - src/lib.rs (find the fitness re-export block; add `pub use fitness::BatchFitnessEvaluator;`)
    - src/observe/observer/mod.rs (lines around 66 — model the trait declaration shape: `pub trait X<U: ChromosomeT>: Send + Sync`)
    - src/traits/chromosome.rs (ChromosomeT trait — required as the trait bound)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-PATTERNS.md (full file)
  </read_first>
  <behavior>
    - `BatchFitnessEvaluator<U: ChromosomeT>: Send + Sync` exposes `fn evaluate_batch(&self, chromosomes: &[U]) -> Vec<f64>` (D-01)
    - `wrap_with_cache(fn, size)` returns `(Arc<FitnessFn<G>>, Arc<Mutex<FitnessCache>>)`; the wrapped closure still performs LRU lookup + store unchanged (D-08)
    - `GenerationStats.cache_hits: Option<u64>` and `cache_misses: Option<u64>` default to `None` at every construction site (D-07)
    - Old `GenerationStats` JSON (without the new fields) deserializes successfully under `--features serde` due to `#[serde(default)]`
    - `use genetic_algorithms::BatchFitnessEvaluator;` compiles from a downstream crate (re-exported from `lib.rs`)
  </behavior>
  <action>
    Create `src/fitness/batch.rs` defining `pub trait BatchFitnessEvaluator<U: ChromosomeT>: Send + Sync` with the single required method `fn evaluate_batch(&self, chromosomes: &[U]) -> Vec<f64>;`. Document the contract (return Vec length MUST equal input slice length; assignment is positional).

    In `src/fitness/mod.rs` add `pub mod batch;` and `pub use batch::BatchFitnessEvaluator;` alongside the existing module declarations.

    In `src/fitness/cache.rs` change `wrap_with_cache` signature to `pub fn wrap_with_cache<G>(fitness_fn: Arc<FitnessFn<G>>, cache_size: usize) -> (Arc<FitnessFn<G>>, Arc<Mutex<FitnessCache>>)`. Preserve the existing closure body verbatim by cloning `Arc::clone(&cache)` into `cache_for_fn` used inside the closure, and return the tuple `(wrapped, cache)`. Keep `hash_dna`, `FitnessCache::new`, `hits()`, `misses()` unchanged.

    In `src/stats.rs` add two fields to `GenerationStats`:
    - `pub cache_hits: Option<u64>` annotated with `#[cfg_attr(feature = "serde", serde(default))]`
    - `pub cache_misses: Option<u64>` annotated with `#[cfg_attr(feature = "serde", serde(default))]`
    Update BOTH construction sites: the empty-population early-return (lines 66-78) and the main `from_fitness_values` builder (lines 107-118). Default both to `None`.

    In `src/lib.rs` add `pub use fitness::BatchFitnessEvaluator;` to the existing fitness re-export block (next to other `pub use fitness::*;` lines).

    Do NOT modify `src/engines/ga.rs` or `src/engines/cma/engine.rs` in this task — Wave 2/3 will consume these contracts. Do NOT change `parent_crossover`. Do NOT touch the single existing call site of `wrap_with_cache` in `Ga::build()` yet — Task 2 below covers the transient build break with a compile-time stub.
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms &amp;&amp; cargo build &amp;&amp; cargo build --features serde</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub trait BatchFitnessEvaluator" src/fitness/batch.rs` matches exactly one line
    - `grep -n "evaluate_batch" src/fitness/batch.rs` matches the method declaration with signature `fn evaluate_batch(&self, chromosomes: &[U]) -> Vec<f64>`
    - `grep -n "pub use batch::BatchFitnessEvaluator" src/fitness/mod.rs` matches one line
    - `grep -n "pub use fitness::BatchFitnessEvaluator" src/lib.rs` matches one line
    - `grep -n "Arc<Mutex<FitnessCache>>" src/fitness/cache.rs` appears in the `wrap_with_cache` return type
    - `grep -n "pub cache_hits: Option<u64>" src/stats.rs` matches one line; same for `cache_misses`
    - `grep -c '#\[cfg_attr(feature = "serde", serde(default))\]' src/stats.rs` count increases by exactly 2 vs pre-task
    - `cargo build` and `cargo build --features serde` succeed (the existing call site in `Ga::build()` is fixed in Task 2)
  </acceptance_criteria>
  <done>
    Trait module created, cache helper returns tuple, stats struct extended with two optional fields, re-export wired in `lib.rs`. Crate compiles in both default and serde feature modes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix existing wrap_with_cache call site in Ga::build() and scaffold Wave 0 test stubs</name>
  <files>src/engines/ga.rs, tests/test_stats.rs, tests/fitness/test_cache.rs, tests/engines/test_ga.rs, tests/engines/cma/test_cma.rs</files>
  <read_first>
    - src/engines/ga.rs (lines 250-330 for struct fields, lines 760-790 for the existing cache-wiring call site)
    - tests/test_stats.rs (full file — extend, do not replace)
    - tests/fitness/test_cache.rs (full file — extend)
    - tests/engines/test_ga.rs (read lines 1-80 + grep for existing module-organisation patterns)
    - tests/engines/cma/test_cma.rs (read lines 1-80)
    - .planning/phases/60-batch-fitness-fitness-cache-extension/60-RESEARCH.md (Phase Requirements to Test Map + Wave 0 Gaps)
  </read_first>
  <action>
    Step A — fix the single existing `wrap_with_cache` call site in `src/engines/ga.rs` (around lines 761-768). Update the destructuring to receive the tuple. Discard the cache handle for now using `let (wrapped, _cache_handle) = ...`. Wave 2 will replace `_cache_handle` with assignment to a new field on `Ga`.

    Step B — add a private `fitness_cache: Option<Arc<Mutex<crate::fitness::cache::FitnessCache>>>` field to the `Ga<U>` struct (alongside `fitness_cache_size`) so its type is reserved. Initialize to `None` in every constructor / `Default` impl / builder reset path that constructs a `Ga`. Do NOT use it yet (Wave 2 wires it). This avoids a second struct churn in Wave 2.

    Step C — add `use std::sync::{Arc, Mutex};` and `use crate::fitness::cache::FitnessCache;` imports to `src/engines/ga.rs` if not already present.

    Step D — scaffold ignored test stubs (Nyquist gate). All tests use `#[ignore = "Wave 0 stub — implemented in Phase 60 Wave 2/3"]` and have empty or `unimplemented!()` bodies so they appear in `cargo test -- --ignored` output:

    In `tests/test_stats.rs` add tests:
    - `cache_stats_default_none` (verifies `GenerationStats::from_fitness_values(...)` yields `cache_hits = None`, `cache_misses = None` — this can be ACTIVATED now, not ignored, since the foundation is already in place)
    - `#[ignore] cache_stats_serde_compat_old_checkpoint` (verifies a JSON without `cache_hits`/`cache_misses` deserializes successfully — feature-gated `#[cfg(feature = "serde")]`)

    In `tests/fitness/test_cache.rs` add tests:
    - `wrap_with_cache_returns_handle` (ACTIVE — calls `wrap_with_cache`, asserts the handle's `lock().unwrap().hits() == 0` initially, then invokes the wrapped fn twice with identical DNA and asserts `hits() == 1`, `misses() == 1`)

    In `tests/engines/test_ga.rs` add ignored stubs under a new `mod batch_evaluator_tests { ... }` block:
    - `#[ignore] ga_with_batch_evaluator_runs_to_completion`
    - `#[ignore] ga_batch_evaluator_called_once_per_generation`
    - `#[ignore] ga_batch_evaluator_replaces_calculate_fitness`
    - `#[ignore] ga_batch_evaluator_initial_population_evaluated`
    - `#[ignore] ga_batch_and_fitness_fn_mutually_exclusive_returns_configuration_error`
    - `#[ignore] ga_cache_stats_populated_in_generation_stats`
    - `#[ignore] ga_cache_stats_none_when_no_cache`
    - `#[ignore] ga_batch_plus_cache_only_misses_evaluated`

    In `tests/engines/cma/test_cma.rs` add ignored stubs under a new `mod batch_and_cache_tests { ... }` block:
    - `#[ignore] cma_with_fitness_cache_accepted`
    - `#[ignore] cma_with_batch_evaluator_accepted`
    - `#[ignore] cma_batch_evaluator_initial_population`
    - `#[ignore] cma_batch_evaluator_offspring_loop`
    - `#[ignore] cma_cache_stats_in_generation_stats`

    Each ignored stub body: `unimplemented!("Wave 2/3 — Phase 60");`

    Make sure new test modules are wired into the parent test file (e.g. `mod batch_evaluator_tests;` inline or proper `#[cfg(test)] mod`).
  </action>
  <verify>
    <automated>cd /Users/luis/RustroverProjects/genetic-algorithms &amp;&amp; cargo build &amp;&amp; cargo test &amp;&amp; cargo test --features serde &amp;&amp; cargo test -- --ignored --list 2>&amp;1 | grep -E "(batch_evaluator|cache_stats|wrap_with_cache|batch_and_cache)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds after the `wrap_with_cache` call-site fix
    - `cargo test` passes (active tests green; ignored stubs do not run)
    - `cargo test --features serde` passes
    - `cargo test -- --ignored --list` lists at least 13 new ignored tests across `test_ga.rs`, `test_cma.rs`, `test_stats.rs`
    - `grep -n "fitness_cache:" src/engines/ga.rs` shows the new `Option<Arc<Mutex<FitnessCache>>>` field declaration
    - `grep -c "wrap_with_cache" src/engines/ga.rs` is 1 (one call site, now destructuring the tuple)
    - Active test `wrap_with_cache_returns_handle` in `tests/fitness/test_cache.rs` passes
    - Active test `cache_stats_default_none` in `tests/test_stats.rs` passes
  </acceptance_criteria>
  <done>
    `Ga::build()` compiles with the new tuple return; `Ga` struct has the unused `fitness_cache` field reserved; all Phase 60 SC items have an ignored test stub awaiting Wave 2/3 activation; the two foundation-level active tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied trait object | `Arc<dyn BatchFitnessEvaluator<U>>` runs inside the engine; same trust model as existing `Arc<dyn GaObserver<U>>` — user-controlled code is in-process and trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `BatchFitnessEvaluator::evaluate_batch` returns wrong-length `Vec<f64>` | mitigate | `debug_assert_eq!(values.len(), chromosomes.len())` in the engine-side caller (added in Wave 2/3); trait doc explicitly states the contract |
| T-60-02 | Denial of Service | `FitnessCache` lock poisoning if user code panics inside a `.lock()` critical section | accept | `.expect("fitness cache lock poisoned")` matches the existing pattern in `wrap_with_cache`; panic surfaces immediately rather than corrupting state |
| T-60-03 | Information disclosure | Cache delta stats expose hit/miss counts to observers | accept | Counts are non-sensitive numeric metrics; no PII; intentional feature |
| T-60-SC | Tampering | npm/pip/cargo installs | n/a | No new external dependencies in Phase 60 — RESEARCH.md confirms zero packages added |
</threat_model>

<verification>
- `cargo build` succeeds
- `cargo build --features serde` succeeds
- `cargo test` passes
- `cargo test --features serde` passes
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo check --target wasm32-unknown-unknown` passes (no `Instant` introduced; `Arc<Mutex<...>>` is WASM-safe)
</verification>

<success_criteria>
- `BatchFitnessEvaluator<U>` trait is publicly importable from the crate root
- `wrap_with_cache` returns `(Arc<FitnessFn<G>>, Arc<Mutex<FitnessCache>>)`
- `GenerationStats` has `cache_hits: Option<u64>` and `cache_misses: Option<u64>`, defaulting to `None`
- All Phase 60 success-criteria items have a corresponding ignored test stub (Nyquist gate)
- Two active foundation tests (`wrap_with_cache_returns_handle`, `cache_stats_default_none`) pass
- All existing tests continue to pass; serde checkpoint backwards-compat preserved
</success_criteria>

<output>
Create `.planning/phases/60-batch-fitness-fitness-cache-extension/60-01-SUMMARY.md` when done.
</output>
