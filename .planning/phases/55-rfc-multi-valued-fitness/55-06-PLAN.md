---
phase: 55-rfc-multi-valued-fitness
plan: 06
type: execute
wave: 4
depends_on:
  - 55-01
  - 55-02
  - 55-03
  - 55-04
  - 55-05
files_modified:
  - tests/structures.rs
  - tests/operations/test_selection_lexicase.rs
  - tests/operations/test_selection_lexicase_diversity.rs
  - tests/engines/test_ga.rs
  - tests/engines/nsga2/test_nsga2.rs
  - tests/engines/nsga3/test_nsga3.rs
  - tests/engines/moead/test_moead.rs
  - tests/engines/spea2/test_spea2.rs
  - tests/engines/sms_emoa/test_sms_emoa.rs
  - tests/engines/ibea/test_ibea.rs
  - tests/engines/island/test_island_nsga2.rs
  - examples/nsga2_zdt1.rs
  - examples/moead_dtlz2.rs
  - examples/spea2_zdt1.rs
  - examples/sms_emoa_zdt1.rs
  - examples/ibea_zdt1.rs
autonomous: false
requirements:
  - TRAITS-01
  - SEL-02
  - SEL-03
tags:
  - rust
  - tests
  - examples
  - migration
  - phase-gate

must_haves:
  truths:
    - "`cargo test` passes for all renamed and migrated test files"
    - "`cargo test --features serde` passes (serde checkpoint round-trip preserves `fitness_values`)"
    - "`cargo clippy` passes with zero warnings"
    - "`cargo check --target wasm32-unknown-unknown` passes"
    - "Every MO example uses chromosomes implementing `VectorFitness` instead of `.with_objective_fns(...)`"
  artifacts:
    - path: "tests/structures.rs"
      provides: "Renamed test chromosome (MultiCaseChromosome → VectorFitnessChromosome OR retained name with renamed methods)"
      contains: "VectorFitness"
    - path: "tests/operations/test_selection_lexicase.rs"
      provides: "Lexicase tests with renamed methods"
      contains: "fitness_values"
    - path: "examples/nsga2_zdt1.rs"
      provides: "NSGA-II example migrated to VectorFitness pattern"
      contains: "VectorFitness"
  key_links:
    - from: "Every MO example chromosome's calculate_fitness()"
      to: "set_fitness_values(...)"
      via: "objective vector populated inside the chromosome"
      pattern: "set_fitness_values"
    - from: "test_<engine>_validate_mismatched_objective_fns (or equivalent)"
      to: "runtime objective-count mismatch check"
      via: "run() returns GaError::Invalid<Engine>Configuration"
      pattern: "Expected .* objectives from fitness_values"
---

<objective>
Update every test file and every example that references `MultiCaseFitness`, `case_fitness`, `set_case_fitness`, `with_objective_fns`, or `ObjectiveFn`. Replace `with_objective_fns(...)` patterns with `VectorFitness` impls on the test/example chromosomes. Replace build-time `objective_fns`-count validation tests with runtime mismatch tests. Run the phase verification gate (cargo test, cargo test --features serde, cargo clippy, wasm32 check). Per D-07, D-09, D-10, D-11 and RESEARCH.md Pitfalls 5 + 6.

Purpose: Bring tests and examples in line with the v3.0.0 contract. After this plan, the full test suite is GREEN and the phase is ready for `/gsd:verify-work`.
Output: All affected test/example files migrated; full CI matrix passing; phase verification checkpoint signed off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rfc-multi-valued-fitness/55-CONTEXT.md
@.planning/phases/55-rfc-multi-valued-fitness/55-RESEARCH.md
@.planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md
@.planning/phases/55-rfc-multi-valued-fitness/55-04-PLAN.md
@.planning/phases/55-rfc-multi-valued-fitness/55-05-PLAN.md

<interfaces>
Wave 1-3 produced:
- `VectorFitness` trait with `fitness_values()` / `set_fitness_values()` methods.
- All built-in chromosomes implement `VectorFitness`.
- All MO engines require `U: VectorFitness` and have NO `with_objective_fns` builder.
- Lexicase callers updated.

Test file rename map:
- `case_fitness` → `fitness_values` (method calls)
- `set_case_fitness` → `set_fitness_values` (method calls)
- `MultiCaseFitness` → `VectorFitness` (bounds and imports)
- `with_objective_fns(vec![Box::new(|dna| ...), ...])` → REMOVE the call entirely; ensure the test/example chromosome implements `VectorFitness` and populates `fitness_values` in `calculate_fitness()`.

Validation test rename map:
- `test_<engine>_validate_mismatched_objective_fns` build-time test → `test_<engine>_run_rejects_mismatched_objective_count` runtime test that calls `.run()` and asserts `GaError::Invalid<Engine>Configuration` is returned.

PATTERNS.md identifies the specific line ranges per file:
- `tests/engines/test_ga.rs`: lines 1175, 1217, 1266, 1325, 1392
- `tests/engines/island/test_island_nsga2.rs`: lines 17, 39, 51, 67, 83, 99
- `tests/operations/test_selection_lexicase.rs`: lines 10, 27, 40, 42, 43, 249
- `tests/operations/test_selection_lexicase_diversity.rs`: lines 21, 44, 59, 93
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate test files — structures.rs + lexicase tests + every MO engine test directory</name>
  <files>tests/structures.rs, tests/operations/test_selection_lexicase.rs, tests/operations/test_selection_lexicase_diversity.rs, tests/engines/test_ga.rs, tests/engines/nsga2/test_nsga2.rs, tests/engines/nsga3/test_nsga3.rs, tests/engines/moead/test_moead.rs, tests/engines/spea2/test_spea2.rs, tests/engines/sms_emoa/test_sms_emoa.rs, tests/engines/ibea/test_ibea.rs, tests/engines/island/test_island_nsga2.rs</files>
  <read_first>
    - tests/structures.rs (full file — find `MultiCaseChromosome` struct ~lines 95-178; identify whether the test struct is renamed or kept; rename or retain at your discretion but rename methods to `fitness_values`/`set_fitness_values`)
    - tests/operations/test_selection_lexicase.rs (full file)
    - tests/operations/test_selection_lexicase_diversity.rs (full file)
    - tests/engines/test_ga.rs (focus on lines 1175, 1217, 1266, 1325, 1392 per PATTERNS.md)
    - tests/engines/nsga2/test_nsga2.rs (full file — focus on `with_objective_fns` call sites and `test_nsga2_validate_mismatched_objective_fns`)
    - tests/engines/nsga3/test_nsga3.rs, tests/engines/moead/test_moead.rs, tests/engines/spea2/test_spea2.rs, tests/engines/sms_emoa/test_sms_emoa.rs, tests/engines/ibea/test_ibea.rs (all MO engine test files)
    - tests/engines/island/test_island_nsga2.rs (lines 17, 39, 51, 67, 83, 99 per PATTERNS.md)
    - src/traits/vector_fitness.rs (Wave 1)
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (test-files section)
  </read_first>
  <behavior>
    - All `case_fitness()` / `set_case_fitness()` test method calls become `fitness_values()` / `set_fitness_values()`.
    - All `MultiCaseFitness` test trait bounds become `VectorFitness`.
    - All `.with_objective_fns(vec![...])` calls are removed; the test chromosome implements `VectorFitness` and populates `fitness_values` inside `calculate_fitness()`.
    - The build-time `test_<engine>_validate_mismatched_objective_fns` test is renamed (e.g., to `test_<engine>_run_rejects_mismatched_objective_count`) and rewritten to: (1) construct an engine with `num_objectives = 3`, (2) supply a chromosome whose `calculate_fitness()` populates `fitness_values` with length 2, (3) call `.run(...)`, (4) assert `Err(GaError::Invalid<Engine>Configuration(_))` is returned.
    - `cargo test` passes for every modified test file.
    - Lexicase tests still verify specialist-preservation behavior.
  </behavior>
  <action>
    Per RESEARCH.md Pitfalls 5 + 6 and PATTERNS.md:

    1. **`tests/structures.rs`** — Rename `case_scores` field to `fitness_values` (keeping `Vec<f64>` type). Rename the `MultiCaseFitness` impl to `VectorFitness` impl with methods `fitness_values` / `set_fitness_values`. Optionally rename the struct itself (`MultiCaseChromosome` → `VectorFitnessChromosome`) — this is at the implementer's discretion (D-09 is silent on test-only naming); whichever choice you make, propagate it consistently to every test file that imports it.

    2. **`tests/operations/test_selection_lexicase.rs`** — Update imports (`MultiCaseFitness` → `VectorFitness`). Rename test function `test_multi_case_fitness_trait_roundtrip` to `test_vector_fitness_trait_roundtrip` (or remove if duplicated by Plan 01's new test file; keep one canonical location). Rename method calls. Update test struct usage to match Step 1.

    3. **`tests/operations/test_selection_lexicase_diversity.rs`** — Update imports + method renames per Step 1.

    4. **`tests/engines/test_ga.rs`** — Update method renames at lines 1175, 1217, 1266, 1325, 1392 (per PATTERNS.md). If any test there uses `.with_objective_fns`, remove the call and ensure the test chromosome implements `VectorFitness`.

    5. **Each MO engine test file** (`tests/engines/{nsga2,nsga3,moead,spea2,sms_emoa,ibea}/test_<engine>.rs`):
       - Remove every `.with_objective_fns(vec![Box::new(|dna| ...), ...])` call.
       - Wherever a test uses `Binary` (or another built-in) with an MO engine, the test chromosome must populate `fitness_values` in `calculate_fitness()`. If the test uses `Binary` directly without overriding `calculate_fitness`, switch to a custom test chromosome (define one inline or in `tests/structures.rs`) whose `calculate_fitness()` writes a `Vec<f64>` of length equal to `num_objectives`.
       - Rename the build-time validation test `test_<engine>_validate_mismatched_objective_fns` to `test_<engine>_run_rejects_mismatched_objective_count`. Rewrite the body per the behavior block: construct a chromosome whose `fitness_values().len()` does not match `num_objectives`, call `engine.run(...)`, assert `Err(GaError::Invalid<Engine>Configuration(_))`.
       - Tests that exercised only validate-time errors with no run loop must be updated to call `.run(...)` because the check is now runtime. If a test was strictly a build-time validation regression (no run), it can be removed if replaced by an equivalent runtime test.

    6. **`tests/engines/island/test_island_nsga2.rs`** — Same recipe applied to the 6 occurrence sites at lines 17, 39, 51, 67, 83, 99 per PATTERNS.md.

    7. Run `cargo test` iteratively after each test file; use compiler errors as the to-do list. Stop when `cargo test` is green.
  </action>
  <verify>
    <automated>cargo test 2>&amp;1 | tail -30 ; grep -rcE "MultiCaseFitness|with_objective_fns|set_case_fitness|[^_]case_fitness" tests/ | grep -v ':0$' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "MultiCaseFitness" tests/` returns no matches.
    - `grep -rn "with_objective_fns" tests/` returns no matches.
    - `grep -rn "set_case_fitness" tests/` returns no matches.
    - `grep -rnE "[^_]case_fitness\b" tests/` returns no matches in live code (comments may be acceptable but should be cleaned up too).
    - `cargo test 2>&1 | tail -5` shows `test result: ok.` with zero failures.
    - For each MO engine `<engine>` in {nsga2, nsga3, moead, spea2, sms_emoa, ibea}: `grep -c "test_${engine}_run_rejects_mismatched_objective_count\|run_rejects_mismatched" tests/engines/${engine}/test_${engine}.rs` is at least `1` (runtime mismatch test installed).
  </acceptance_criteria>
  <done>All test files migrated to `VectorFitness`. Build-time validation tests for `objective_fns` count are replaced by runtime mismatch tests. `cargo test` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate MO examples — nsga2_zdt1, moead_dtlz2, spea2_zdt1, sms_emoa_zdt1, ibea_zdt1</name>
  <files>examples/nsga2_zdt1.rs, examples/moead_dtlz2.rs, examples/spea2_zdt1.rs, examples/sms_emoa_zdt1.rs, examples/ibea_zdt1.rs</files>
  <read_first>
    - examples/nsga2_zdt1.rs (full file — find `with_objective_fns(...)` call and the closures it passes)
    - examples/moead_dtlz2.rs (full file)
    - examples/spea2_zdt1.rs (full file)
    - examples/sms_emoa_zdt1.rs (full file)
    - examples/ibea_zdt1.rs (full file)
    - .planning/phases/55-rfc-multi-valued-fitness/55-RESEARCH.md (Example 4 — the user-migration pattern)
  </read_first>
  <behavior>
    - Each example defines its own chromosome type (or reuses a built-in) whose `calculate_fitness()` populates BOTH `fitness` (scalar) AND `fitness_values` (Vec<f64>) — the latter via `set_fitness_values(...)`.
    - Each example implements `VectorFitness` on its chromosome.
    - No example calls `.with_objective_fns(...)`.
    - Each example still produces the same final Pareto-front output as before (verified by running it).
  </behavior>
  <action>
    Apply the migration pattern from RESEARCH.md Example 4 to each example:

    1. If the example uses a built-in `Binary`, `Range<T>`, or similar, the chromosome already has `fitness_values` (from Plan 02). But the example's existing `fitness_fn`-based scalar evaluation is incompatible — these examples typically pass closures via `with_objective_fns`, expecting the engine to evaluate. After this phase, the chromosome must evaluate itself. Choose one of:
       - **Option A (recommended):** Define a custom example chromosome type wrapping the gene vector, implement `ChromosomeT` with a real `calculate_fitness()` that computes ZDT1/DTLZ2/etc. and calls both `self.set_fitness(scalar)` and `self.set_fitness_values(vec![f1, f2, ...])`. Implement `VectorFitness` referencing the stored `fitness_values: Vec<f64>` field.
       - **Option B:** Reuse the built-in `Range<f64>` but override `calculate_fitness` via the existing `FitnessFnWrapper` mechanism — this only works if `FitnessFnWrapper` lets the user populate `fitness_values` too. If not, fall back to Option A.

    2. Remove every `.with_objective_fns(vec![Box::new(|dna| ...), Box::new(|dna| ...)])` call chain from the engine builder.

    3. The objective-direction configuration (minimize/maximize per objective) STAYS on the engine config — only the closure storage moves into the chromosome.

    4. Verify each example by running it:
       ```
       cargo run --example nsga2_zdt1 --release
       cargo run --example moead_dtlz2 --release
       cargo run --example spea2_zdt1 --release
       cargo run --example sms_emoa_zdt1 --release
       cargo run --example ibea_zdt1 --release
       ```
       Each must produce a non-empty Pareto front (or equivalent expected output) and exit successfully.

    5. Update the inline comments / module-level docs in each example to explain the new pattern (chromosome owns objective evaluation; no `with_objective_fns` builder).
  </action>
  <verify>
    <automated>for e in nsga2_zdt1 moead_dtlz2 spea2_zdt1 sms_emoa_zdt1 ibea_zdt1; do echo "== $e =="; grep -c "with_objective_fns" examples/${e}.rs; grep -c "VectorFitness\|set_fitness_values" examples/${e}.rs; cargo run --example $e --release 2>&amp;1 | tail -3; done</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 example files:
      - `grep -c "with_objective_fns" examples/{file}` returns `0`.
      - `grep -c "VectorFitness\|set_fitness_values" examples/{file}` is at least `1`.
      - `cargo run --example {name} --release` exits with status `0`.
    - `grep -rn "with_objective_fns\|MultiCaseFitness\|set_case_fitness" examples/` returns no matches.
  </acceptance_criteria>
  <done>All 5 MO examples migrated to chromosome-owned objective evaluation. Every example runs successfully and produces its expected output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 55 verification gate — full CI matrix + manual review</name>
  <what-built>
    Phase 55 is complete:
    - Plan 01: `VectorFitness` trait renamed + re-exported + baseline test added.
    - Plan 02: All 7 built-in chromosomes carry `fitness_values: Vec<f64>` + `VectorFitness` impl.
    - Plan 03: Lexicase callers (`selection.rs`, `selection/lexicase.rs`, `ga.rs`) migrated.
    - Plan 04: NSGA-II, NSGA-III, MOEA/D migrated — `objective_fns` removed; `VectorFitness` bound added; runtime objective-count guard installed.
    - Plan 05: SPEA2, SMS-EMOA, IBEA, Island NSGA-II migrated identically.
    - Plan 06 (this plan): All tests and examples migrated; build-time mismatch tests replaced with runtime mismatch tests.
  </what-built>
  <how-to-verify>
    Run each command from the repo root and confirm each terminates with success:

    1. **Full test suite (default features):**
       ```
       cargo test 2>&1 | tail -20
       ```
       Expected: every `test result: ok.` line; zero failures across all test modules.

    2. **Full test suite with serde feature:**
       ```
       cargo test --features serde 2>&1 | tail -20
       ```
       Expected: every `test result: ok.` line; serde round-trip tests for built-in chromosomes pass with the new `fitness_values` field (`#[serde(default)]` works for old payloads).

    3. **Clippy clean:**
       ```
       cargo clippy --all-targets 2>&1 | tail -20
       ```
       Expected: zero warnings; finishes with `Finished` line and no errors.

    4. **WASM target compile:**
       ```
       cargo check --target wasm32-unknown-unknown 2>&1 | tail -10
       ```
       Expected: `Finished` line; no compilation errors. (No new threading or `Instant` usage was introduced in this phase, so WASM must continue to work.)

    5. **Examples smoke test:**
       ```
       for e in nsga2_zdt1 moead_dtlz2 spea2_zdt1 sms_emoa_zdt1 ibea_zdt1; do
         echo "== $e ==" ; cargo run --example $e --release 2>&1 | tail -3
       done
       ```
       Expected: each example exits with status 0; no panic; produces its expected Pareto-front output.

    6. **Symbol-level audit:**
       ```
       grep -rn "MultiCaseFitness\|case_fitness\|set_case_fitness\|with_objective_fns" src/ tests/ examples/ | grep -v '^\s*//'
       ```
       Expected: NO matches in live code.

    7. **Manual review of CONTEXT.md decision coverage:**
       - D-01 `VectorFitness` is supertrait of `ChromosomeT` and opt-in (no blanket impl) → confirmed by reading `src/traits/vector_fitness.rs`.
       - D-02 default impl decision → confirmed: NO default impl (Option A); each implementor provides the body. Document the rationale in trait rustdoc.
       - D-03 method renames → confirmed by symbol audit.
       - D-05 re-export from lib.rs at `genetic_algorithms::VectorFitness` → confirmed.
       - D-06 all MO engines have `U: VectorFitness` bound → spot-check each engine's `impl` block.
       - D-07 `objective_fns` removed from all 7 engine locations → confirmed by symbol audit.
       - D-08 `ParetoIndividual.objectives` populated from `fitness_values()` → spot-check `initialize_population` in each engine.
       - D-09 no alias bridge → confirmed by symbol audit.
       - D-10 `factory_lexicase` bound updated → confirmed.
       - D-11 `Ga::select_parents_lexicase` bound updated → confirmed.
  </how-to-verify>
  <resume-signal>Type `approved` once all six verification commands pass and the decision-coverage spot-check is satisfied. If any command fails, report which one and the error output before resuming.</resume-signal>
</task>

</tasks>

<verification>
- `cargo test` green.
- `cargo test --features serde` green.
- `cargo clippy --all-targets` zero warnings.
- `cargo check --target wasm32-unknown-unknown` succeeds.
- All 5 MO examples run successfully.
- No remaining symbol of `MultiCaseFitness`, `case_fitness`, `set_case_fitness`, or `with_objective_fns` anywhere in `src/`, `tests/`, or `examples/`.
- All 11 CONTEXT.md decisions (D-01 through D-11) verified.
</verification>

<success_criteria>
- Phase 55 ships a v3.0.0 breaking change: `MultiCaseFitness` is gone, replaced by `VectorFitness` with renamed methods.
- All 7 MO engine locations no longer carry external objective closures.
- All built-in chromosomes implement `VectorFitness` with `#[serde(default)]` for the new field.
- Lexicase selection works against `VectorFitness`.
- All tests pass on default and serde feature configurations.
- WASM compatibility is preserved.
- All examples run.
- Manual verification gate signed off.
</success_criteria>

<output>
Create `.planning/phases/55-rfc-multi-valued-fitness/55-06-SUMMARY.md` when done.
</output>
