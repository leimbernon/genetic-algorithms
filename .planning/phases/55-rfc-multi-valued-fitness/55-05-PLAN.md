---
phase: 55-rfc-multi-valued-fitness
plan: 05
type: execute
wave: 3
depends_on:
  - 55-01
  - 55-02
files_modified:
  - src/engines/spea2/mod.rs
  - src/engines/sms_emoa/mod.rs
  - src/engines/ibea/mod.rs
  - src/engines/island/nsga2.rs
autonomous: true
requirements:
  - TRAITS-01
tags:
  - rust
  - multi-objective
  - spea2
  - sms-emoa
  - ibea
  - island

must_haves:
  truths:
    - "User cannot call `.with_objective_fns(...)` on SPEA2, SMS-EMOA, IBEA, or Island NSGA-II — method removed (D-07)"
    - "All four engines require `U: VectorFitness` and populate `ParetoIndividual.objectives` from `chrom.fitness_values()` (D-06, D-08)"
    - "Runtime objective-count check rejects mismatched configurations during `run()`"
  artifacts:
    - path: "src/engines/spea2/mod.rs"
      provides: "Spea2Ga migrated"
      contains: "VectorFitness"
    - path: "src/engines/sms_emoa/mod.rs"
      provides: "SmsEmoaGa migrated"
      contains: "VectorFitness"
    - path: "src/engines/ibea/mod.rs"
      provides: "IbeaGa migrated"
      contains: "VectorFitness"
    - path: "src/engines/island/nsga2.rs"
      provides: "Island NSGA-II migrated (RESEARCH.md Pitfall 4)"
      contains: "VectorFitness"
  key_links:
    - from: "Each engine's population init"
      to: "VectorFitness::fitness_values"
      via: "chrom.fitness_values().to_vec()"
      pattern: "fitness_values\\(\\)\\.to_vec\\(\\)"
    - from: "SMS-EMOA steady-state offspring eval"
      to: "VectorFitness::fitness_values"
      via: "single-offspring evaluation path"
      pattern: "fitness_values\\(\\)"
    - from: "Island NSGA-II per-island engine"
      to: "VectorFitness bound"
      via: "engine struct field removed + bound added"
      pattern: "VectorFitness"
---

<objective>
Apply the same `objective_fns` removal + `VectorFitness` bound migration to the remaining four MO engine locations: SPEA2, SMS-EMOA, IBEA, and the Island NSGA-II engine. Per D-06, D-07, D-08 and RESEARCH.md Pitfall 4. Pattern is identical to Plan 04.

Purpose: Complete the v3.0.0 breaking change for all 7 MO engine locations (3 in Plan 04 + 4 here). Pitfall 4 explicitly calls out Island NSGA-II as the most-likely-to-be-missed file.
Output: Four engine files migrated with the same recipe used in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rfc-multi-valued-fitness/55-CONTEXT.md
@.planning/phases/55-rfc-multi-valued-fitness/55-RESEARCH.md
@.planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md
@.planning/phases/55-rfc-multi-valued-fitness/55-04-PLAN.md

<interfaces>
Same as Plan 04. The migration recipe is identical — applied to four additional engine files.

Engine-specific differences:
- SPEA2: archive-based fitness assignment + truncation. `objective_fns` removal does NOT change the archive logic. Error variant: `GaError::InvalidSpea2Configuration`. Config field: `self.spea2_config.num_objectives`.
- SMS-EMOA: steady-state (mu+1) loop. Offspring evaluation happens once per generation on a single new individual. The `.objective_fns` direct-access in an inner lambda at ~line 426 of `sms_emoa/mod.rs` must also be cleaned up (RESEARCH.md anti-pattern). Error variant: `GaError::InvalidSmsEmoaConfiguration`. Config field: `self.sms_emoa_config.num_objectives`.
- IBEA: indicator-based fitness (epsilon+ pairwise). Error variant: `GaError::InvalidIbeaConfiguration`. Config field: `self.ibea_config.num_objectives`.
- Island NSGA-II (`src/engines/island/nsga2.rs`): separate engine struct from `Nsga2Ga`. Per RESEARCH.md A3, this file has `pub objective_fns` at line ~87 with 11 references. Error variant likely shares with Nsga2: `GaError::InvalidNsga2Configuration` or an island-specific one — confirm by reading the file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate src/engines/spea2/mod.rs and src/engines/ibea/mod.rs</name>
  <files>src/engines/spea2/mod.rs, src/engines/ibea/mod.rs</files>
  <read_first>
    - src/engines/spea2/mod.rs (full file — locate `objective_fns`, `with_objective_fns`, `validate()`, `run()`, `initialize_population()`, archive eval, offspring eval)
    - src/engines/ibea/mod.rs (full file — same scan; indicator-based fitness is separate from objective extraction)
    - src/engines/nsga2/mod.rs AFTER Plan 04 (canonical post-migration template)
    - src/traits/vector_fitness.rs
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (engine sections)
  </read_first>
  <behavior>
    - `Spea2Ga` and `IbeaGa` carry no `objective_fns` field.
    - Both require `U: LinearChromosome + VectorFitness + ...` on impl blocks calling `fitness_values()`.
    - Both populate `ParetoIndividual.objectives` from `chrom.fitness_values().to_vec()` after `calculate_fitness()`.
    - Both perform a runtime objective-count check at the start of `run()` using their respective config field and error variant.
    - SPEA2 archive truncation logic unchanged. IBEA epsilon+ indicator computation unchanged.
    - WASM split preserved in both.
  </behavior>
  <action>
    Apply the Plan 04 Task 1 ten-step recipe to each file:
    - SPEA2: error variant `GaError::InvalidSpea2Configuration`, config `self.spea2_config.num_objectives`. Be careful: SPEA2 has both initial population init AND an archive maintenance loop that re-evaluates fitness — both call sites that read `objective_fns` must be migrated. Preserve the strength + density fitness assignment and Euclidean truncation entirely (those are not objective-eval logic).
    - IBEA: error variant `GaError::InvalidIbeaConfiguration`, config `self.ibea_config.num_objectives`. The pairwise indicator fitness assignment reads `objectives` from `ParetoIndividual`, not from `objective_fns` directly — confirm by reading. Migrate only the population-init and offspring-eval paths.
    - In both files: drop `objective_fns` field + `with_objective_fns` builder + `validate()` count check + closure use; add `VectorFitness` bound + import; add runtime guard at start of `run()`; preserve WASM cfg gates.
    - Preserve `constraint_fns` (if present in either engine) and the `ObjectiveFn` re-export (if `constraint_fns` still references it).
  </action>
  <verify>
    <automated>for f in src/engines/spea2/mod.rs src/engines/ibea/mod.rs; do echo "== $f =="; grep -c "self\.objective_fns" "$f"; grep -c "with_objective_fns" "$f"; grep -c "VectorFitness" "$f"; grep -c "fitness_values()\.to_vec()" "$f"; done ; cargo check 2>&amp;1 | grep -E "(spea2|ibea)/mod\.rs" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - For each of `src/engines/spea2/mod.rs` and `src/engines/ibea/mod.rs`:
      - `grep -c "self\.objective_fns" {file}` returns `0`.
      - `grep -c "with_objective_fns" {file}` returns `0`.
      - `grep -cE "pub\s+objective_fns" {file}` returns `0`.
      - `grep -c "VectorFitness" {file}` is at least `3`.
      - `grep -c "fitness_values()\.to_vec()" {file}` is at least `1`.
      - `grep -c "Expected .* objectives from fitness_values" {file}` is at least `1`.
    - `cargo check` produces no errors originating in either file (assuming Plans 01, 02, 04 are also applied).
  </acceptance_criteria>
  <done>SPEA2 and IBEA fully migrated; archive / indicator logic untouched; runtime guard installed; WASM-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate src/engines/sms_emoa/mod.rs and src/engines/island/nsga2.rs</name>
  <files>src/engines/sms_emoa/mod.rs, src/engines/island/nsga2.rs</files>
  <read_first>
    - src/engines/sms_emoa/mod.rs (full file — find `objective_fns` field, `with_objective_fns`, `validate()`, `run()`, `initialize_population()`, the inner-lambda direct access at ~line 426 noted in RESEARCH.md anti-patterns)
    - src/engines/island/nsga2.rs (full file — RESEARCH.md A3 says this has `pub objective_fns` at ~line 87 with 11 references; this is the file most-likely-to-be-missed per Pitfall 4)
    - src/engines/nsga2/mod.rs AFTER Plan 04 (canonical template)
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (engine sections)
  </read_first>
  <behavior>
    - SMS-EMOA: `SmsEmoaGa<U>` requires `U: VectorFitness`; no `objective_fns` field; offspring evaluation in the steady-state loop reads `chrom.fitness_values().to_vec()`. The inner-lambda direct access at ~line 426 is also migrated.
    - Island NSGA-II: the island engine struct in `src/engines/island/nsga2.rs` has no `objective_fns` field; per-island `Nsga2Ga` instances are constructed without `with_objective_fns(...)`; the island engine requires `U: VectorFitness`. Inter-island migration (Arc<[migrant]>) is unaffected.
    - Both files preserve WASM cfg gates.
    - Both files install the runtime objective-count guard at the start of their respective `run()` methods.
  </behavior>
  <action>
    Apply the Plan 04 Task 1 ten-step recipe to each file. Engine-specific notes:

    **`src/engines/sms_emoa/mod.rs`:**
    - Error variant: `GaError::InvalidSmsEmoaConfiguration`. Config field: `self.sms_emoa_config.num_objectives`.
    - Standard steps: remove field, remove builder, remove `validate()` count check, add bound + import, add runtime guard, migrate `initialize_population()`.
    - Special: the inner-lambda direct access to `.objective_fns` at ~line 426 (RESEARCH.md anti-pattern) must also be cleaned up. Find it and replace with `chrom.fitness_values().to_vec()` after `calculate_fitness()`. After the edit, `grep -c "self\.objective_fns\|\.objective_fns" src/engines/sms_emoa/mod.rs` should be `0`.
    - Preserve hypervolume contribution removal logic in the steady-state (mu+1) loop.

    **`src/engines/island/nsga2.rs`:**
    - RESEARCH.md A3 confirms this file has `pub objective_fns` at ~line 87 with 11 references. Read the file in full, then locate every reference and migrate per the recipe.
    - Error variant: confirm by inspection — likely `GaError::InvalidNsga2Configuration` (shared with standalone Nsga2). Config field: confirm name by reading the configuration struct used.
    - If the island engine constructs per-island `Nsga2Ga` instances via `Nsga2Ga::new(...).with_objective_fns(...)`, the `with_objective_fns` call MUST be removed — the user is now expected to supply chromosomes that already implement `VectorFitness` and populate `fitness_values` in `calculate_fitness()`.
    - Add `VectorFitness` to the engine's `U` bound in every relevant `impl` block.
    - Preserve the migration/Arc<[migrant]> logic (this was a v2.2.1 optimization).
    - Add the runtime objective-count guard at the start of `run()` after the first population is evaluated, before the first generation loop.
  </action>
  <verify>
    <automated>for f in src/engines/sms_emoa/mod.rs src/engines/island/nsga2.rs; do echo "== $f =="; grep -c "self\.objective_fns\|\.objective_fns" "$f"; grep -c "with_objective_fns" "$f"; grep -c "VectorFitness" "$f"; grep -c "fitness_values()\.to_vec()" "$f"; done ; cargo check 2>&amp;1 | grep -E "(sms_emoa/mod\.rs|island/nsga2\.rs)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - For each of `src/engines/sms_emoa/mod.rs` and `src/engines/island/nsga2.rs`:
      - `grep -c "self\.objective_fns" {file}` returns `0`.
      - `grep -cE "\.objective_fns\b" {file}` returns `0` (catches inner-lambda direct access).
      - `grep -c "with_objective_fns" {file}` returns `0`.
      - `grep -cE "pub\s+objective_fns" {file}` returns `0`.
      - `grep -c "VectorFitness" {file}` is at least `2`.
      - `grep -c "fitness_values()\.to_vec()" {file}` is at least `1`.
      - `grep -c "Expected .* objectives from fitness_values" {file}` is at least `1`.
    - `cargo check` (assuming Plans 01, 02, 04 and Task 1 of this plan are merged) produces no errors originating in either file.
  </acceptance_criteria>
  <done>SMS-EMOA's steady-state loop and inner-lambda `.objective_fns` access cleaned up. Island NSGA-II migrated — Pitfall 4 closed. All 7 MO engine locations now consume objectives via `VectorFitness`.</done>
</task>

</tasks>

<verification>
- `grep -rn "with_objective_fns" src/engines/` returns no matches across the full engines tree.
- `grep -rn "pub objective_fns" src/engines/` returns no matches.
- `grep -rn "\.objective_fns" src/engines/` returns no matches (catches direct-access leftovers per RESEARCH.md anti-pattern).
- After Plans 01-05, `cargo check` should produce errors ONLY in `tests/` and `examples/` — these are addressed in Plan 06.
</verification>

<success_criteria>
- All four engine files have removed `objective_fns` state and `with_objective_fns` builder.
- All four require `U: VectorFitness`.
- All four populate `ParetoIndividual.objectives` (where present) from `fitness_values()`.
- All four install the runtime objective-count guard.
- WASM split preserved everywhere.
- The seven MO engine locations across Plan 04 + Plan 05 are now consistent.
</success_criteria>

<output>
Create `.planning/phases/55-rfc-multi-valued-fitness/55-05-SUMMARY.md` when done.
</output>
