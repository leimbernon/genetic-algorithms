---
phase: 55-rfc-multi-valued-fitness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/traits/multi_case_fitness.rs
  - src/traits/vector_fitness.rs
  - src/traits.rs
  - src/lib.rs
  - tests/traits/test_vector_fitness.rs
autonomous: true
requirements:
  - TRAITS-01
tags:
  - rust
  - traits
  - rename

must_haves:
  truths:
    - "User can `use genetic_algorithms::VectorFitness;` and the import resolves"
    - "User cannot `use genetic_algorithms::MultiCaseFitness;` — symbol no longer exists"
    - "User can call `chromosome.fitness_values()` and `chromosome.set_fitness_values(vec)` on any `VectorFitness` implementor"
  artifacts:
    - path: "src/traits/vector_fitness.rs"
      provides: "VectorFitness trait definition"
      contains: "pub trait VectorFitness: ChromosomeT"
    - path: "tests/traits/test_vector_fitness.rs"
      provides: "Trait roundtrip + re-export tests"
      contains: "test_vector_fitness_trait_roundtrip"
  key_links:
    - from: "src/traits.rs"
      to: "src/traits/vector_fitness.rs"
      via: "pub mod vector_fitness; pub use vector_fitness::VectorFitness;"
      pattern: "pub use vector_fitness::VectorFitness"
    - from: "src/lib.rs"
      to: "src/traits"
      via: "pub use traits::{..., VectorFitness, ...}"
      pattern: "VectorFitness"
---

<objective>
Rename the `MultiCaseFitness` trait to `VectorFitness` with renamed methods (`case_fitness` → `fitness_values`, `set_case_fitness` → `set_fitness_values`) — the foundation Wave 0 step. Per D-01, D-02, D-03, D-05, D-09 (hard rename, no alias bridge).

Purpose: Unify the trait name for both lexicase selection and multi-objective engines, per RFC decision.
Output: A new `src/traits/vector_fitness.rs`, module wiring, `lib.rs` re-export swap, and a baseline test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rfc-multi-valued-fitness/55-CONTEXT.md
@.planning/phases/55-rfc-multi-valued-fitness/55-RESEARCH.md
@.planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md

<interfaces>
Current trait shape (from `src/traits/multi_case_fitness.rs`, to be renamed):
- `pub trait MultiCaseFitness: ChromosomeT`
- `fn case_fitness(&self) -> &[f64];`
- `fn set_case_fitness(&mut self, scores: Vec<f64>);`

Target trait shape:
- `pub trait VectorFitness: ChromosomeT`
- `fn fitness_values(&self) -> &[f64];`
- `fn set_fitness_values(&mut self, values: Vec<f64>);`

Decision D-02 resolution (per RESEARCH.md Pitfall 1 + PATTERNS.md): NO default impl on the trait. Each implementor (Wave 2) must store a `fitness_values: Vec<f64>` field and provide both methods explicitly. Document this in the trait rustdoc under a `# No default impl` section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename trait file + module declaration + lib.rs re-export</name>
  <files>src/traits/multi_case_fitness.rs (deleted), src/traits/vector_fitness.rs (new), src/traits.rs, src/lib.rs</files>
  <read_first>
    - src/traits/multi_case_fitness.rs (current content — 15 lines, copy/transform)
    - src/traits.rs (lines 1-80 — find `pub mod multi_case_fitness;` and `pub use multi_case_fitness::MultiCaseFitness;`)
    - src/lib.rs (around line 359 — `pub use traits::{LinearChromosome, MultiCaseFitness, OperatorCompat, Strategy};`)
    - .planning/phases/55-rfc-multi-valued-fitness/55-PATTERNS.md (sections "src/traits/vector_fitness.rs" and "src/traits.rs" and "src/lib.rs")
  </read_first>
  <behavior>
    - After this task: `cargo check` produces errors of the form "cannot find ... `MultiCaseFitness`" at every old call site — proving the symbol no longer exists.
    - The new `VectorFitness` trait compiles in isolation.
    - The trait has NO default impl for `fitness_values()` (Option A per RESEARCH.md Pitfall 1).
  </behavior>
  <action>
    Perform a hard rename per D-09:
    1. Use `git mv src/traits/multi_case_fitness.rs src/traits/vector_fitness.rs` to preserve git history.
    2. Inside the new file: rename the trait `MultiCaseFitness` → `VectorFitness`; rename methods `case_fitness` → `fitness_values` and `set_case_fitness` → `set_fitness_values`; rename parameter `scores` → `values`. Update the module rustdoc to mention both lexicase and multi-objective engines (NSGA-II, NSGA-III, MOEA/D, SPEA2, SMS-EMOA, IBEA). Add a `# No default impl` rustdoc section explaining why (lifetime constraint — `fitness()` returns `f64` by value, no `&f64` to borrow).
    3. In `src/traits.rs`: replace `pub mod multi_case_fitness;` with `pub mod vector_fitness;`. Replace `pub use multi_case_fitness::MultiCaseFitness;` with `pub use vector_fitness::VectorFitness;`. Keep alphabetical ordering.
    4. In `src/lib.rs` (~line 359): replace `MultiCaseFitness` with `VectorFitness` in the `pub use traits::{...}` import list. No alias.
    5. Do NOT add any `pub use MultiCaseFitness = VectorFitness;` alias — D-09 forbids it.
  </action>
  <verify>
    <automated>cargo check 2>&amp;1 | grep -E "MultiCaseFitness" | grep -v "^\s*//" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/traits/vector_fitness.rs` exists and `src/traits/multi_case_fitness.rs` does NOT exist (`test ! -e src/traits/multi_case_fitness.rs &amp;&amp; test -e src/traits/vector_fitness.rs`).
    - `grep -c "pub trait VectorFitness: ChromosomeT" src/traits/vector_fitness.rs` returns `1`.
    - `grep -c "fn fitness_values(&self) -> &\[f64\]" src/traits/vector_fitness.rs` returns `1`.
    - `grep -c "fn set_fitness_values(&mut self, values: Vec<f64>)" src/traits/vector_fitness.rs` returns `1`.
    - `grep -c "default" src/traits/vector_fitness.rs | grep -v rustdoc` — no default impl body present (only doc text mentioning "No default impl").
    - `grep -c "pub mod vector_fitness" src/traits.rs` returns `1`; `grep -c "pub use vector_fitness::VectorFitness" src/traits.rs` returns `1`; `grep -c "multi_case_fitness" src/traits.rs` returns `0`.
    - `grep -c "VectorFitness" src/lib.rs` ≥ `1`; `grep -c "MultiCaseFitness" src/lib.rs` returns `0`.
  </acceptance_criteria>
  <done>Trait file renamed, methods renamed, module wiring + lib.rs re-export updated. `cargo check` fails with "cannot find MultiCaseFitness" errors at downstream call sites — these are addressed in Waves 2-4. No `MultiCaseFitness` symbol remains in `src/traits/` or `src/lib.rs`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trait roundtrip + re-export baseline test</name>
  <files>tests/traits/test_vector_fitness.rs (new), tests/traits/mod.rs (or tests/traits.rs — confirm wiring)</files>
  <read_first>
    - tests/structures.rs (find `MultiCaseChromosome` struct, ~lines 95-178 — replicate the storage pattern with renamed methods)
    - tests/operations/test_selection_lexicase.rs (find existing `test_multi_case_fitness_trait_roundtrip` test for the pattern reference)
    - tests/traits.rs OR tests/traits/mod.rs (determine which module-declaration style this crate uses)
    - src/traits/vector_fitness.rs (the trait just created in Task 1)
  </read_first>
  <behavior>
    - Test `test_vector_fitness_trait_roundtrip` constructs a chromosome implementing `VectorFitness`, calls `set_fitness_values(vec![1.0, 2.0, 3.0])`, then asserts `fitness_values() == &[1.0, 2.0, 3.0]`.
    - Test `test_vector_fitness_reexport` defines `fn accepts<U: genetic_algorithms::VectorFitness>(_: &U) {}` and calls it — proves D-05 re-export accessibility.
    - Both tests pass under `cargo test --test traits test_vector_fitness` (or equivalent invocation matching this crate's tests/ layout).
  </behavior>
  <action>
    Per D-05 and TRAITS-01 verification:
    1. Create `tests/traits/test_vector_fitness.rs` (new file). Import `genetic_algorithms::traits::{ChromosomeT, VectorFitness}` and `genetic_algorithms::VectorFitness` (the re-export).
    2. Define a minimal local test struct `VfTestChromosome` with fields `dna: Vec<i32>`, `fitness: f64`, `age: usize`, `fitness_values: Vec<f64>`. Implement `ChromosomeT` minimally (use `unimplemented!()` only for methods not exercised — `fitness()`, `set_fitness()`, `age()`, `set_age()`, `calculate_fitness()` must be real). Implement `VectorFitness` returning `&self.fitness_values` and storing into `self.fitness_values`.
    3. Write `#[test] fn test_vector_fitness_trait_roundtrip()` per the behavior block: construct, set, assert.
    4. Write `#[test] fn test_vector_fitness_reexport()` calling `fn accepts<U: genetic_algorithms::VectorFitness>(_: &U) {}` with the test struct.
    5. Wire the new file into the `tests/traits/` module per this crate's convention (add `pub mod test_vector_fitness;` to `tests/traits.rs` or `tests/traits/mod.rs`, whichever exists).
  </action>
  <verify>
    <automated>cargo test --test traits test_vector_fitness -- --exact 2>&amp;1 | tail -20 || cargo test test_vector_fitness 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/traits/test_vector_fitness.rs` exists.
    - `grep -c "fn test_vector_fitness_trait_roundtrip" tests/traits/test_vector_fitness.rs` returns `1`.
    - `grep -c "fn test_vector_fitness_reexport" tests/traits/test_vector_fitness.rs` returns `1`.
    - `grep -c "genetic_algorithms::VectorFitness" tests/traits/test_vector_fitness.rs` ≥ `1`.
    - Both tests appear in `cargo test test_vector_fitness 2>&amp;1` output as `test ... ok` (or are blocked only by Wave 2+ compile errors elsewhere — confirm by running with `--no-run` if downstream errors exist: `cargo test --no-run --test traits test_vector_fitness` must succeed once Wave 2 lands; in this Wave 1 plan, compile errors from old call sites are expected and acceptable; the file content itself must be syntactically valid).
  </acceptance_criteria>
  <done>New test file created with both tests; test file compiles in isolation (verified by `cargo check --tests` once Wave 2-3 land); module wiring done. Tests will be GREEN at phase verification (Plan 06) after all waves merge.</done>
</task>

</tasks>

<verification>
- `git status` shows `src/traits/multi_case_fitness.rs` deleted and `src/traits/vector_fitness.rs` added.
- `grep -rn "MultiCaseFitness" src/traits/ src/lib.rs` returns no matches.
- `grep -rn "MultiCaseFitness" src/` shows only the expected downstream call sites (operations/selection, engines/ga, engines/nsga*, engines/moead, engines/spea2, engines/sms_emoa, engines/ibea, engines/island/nsga2) — those are fixed in Waves 2-4.
- `cargo check 2>&1 | grep -c "cannot find type \`MultiCaseFitness\`"` returns a positive number (proving the rename forces downstream fixes).
</verification>

<success_criteria>
- `src/traits/vector_fitness.rs` exists with the renamed trait and no default impl.
- `src/traits/multi_case_fitness.rs` does not exist.
- `src/traits.rs` re-exports `VectorFitness`; no `MultiCaseFitness` reference remains in the file.
- `src/lib.rs` re-exports `VectorFitness` at the crate root.
- `tests/traits/test_vector_fitness.rs` exists with two tests.
- Build is broken at downstream call sites (expected) — fixed in subsequent waves.
</success_criteria>

<output>
Create `.planning/phases/55-rfc-multi-valued-fitness/55-01-SUMMARY.md` when done.
</output>
